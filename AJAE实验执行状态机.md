# AJAE 实验执行状态机

## 1. 目的

本文件只规定 schema 33 的实际执行顺序。状态推进依据能够改变研究判断的真实证据，不以文件数量、测试数量或程序可运行为完成标准。

当前唯一问题是：五帧对齐到当前帧形成的稠密伪扫描，能否被冻结 STU 直接利用。B0、B1、B2、B3 训练矩阵及其正式门控已经停止，不得从 schema 32 恢复到当前主线。

## 2. 状态顺序

```text
F0  当前帧坐标和成对输入实现
 ↓
F1  五帧几何稠密性
 ↓
F2  冻结 STU 正常稳定性
 ↓
F3  少量合成异常信号
 ↓
 ├─ 直接稠密 STU 得到支持 → V1
 └─ 直接输入不成立       → F4 最小训练分支
                                  ↓
                                  V1
                                  ↓
                                  T1
```

每个状态只回答一个会改变后续路线的问题。

## 3. F0：实现语义

### 输入

- schema 33 协议；
- STU 官方冻结权重；
- 连续五帧的原始点、强度和位姿。

### 必须成立的行为

1. 窗口为 \(X_{t-4:t}\)，当前时刻为最后一帧 \(t\)。
2. 程序使用 \(T_{t\leftarrow i}=T_{W\leftarrow t}^{-1}T_{W\leftarrow i}\) 对齐前四帧。
3. 第 (t) 帧经过单位变换后逐点保持不变。
4. 稠密伪扫描包含五帧全部可见回波，但只提取第 (t) 帧原有点的 STU 输出。
5. 单帧和稠密条件使用同一个冻结 STU 与同一个 MaxLogit 定义。
6. 每个当前帧只产生一次输出，不执行重叠窗口概率平均。
7. 活动代码中不存在可训练 AJAE 模型、B2 扫描隔离分支或 B3 联合网络入口。

### 完成条件

相关单元测试和机械资格检查全部通过，只说明实现符合以上定义。它不说明真实点云更稠密，也不说明 STU 输出有效。

## 4. F1：几何稠密性

### 数据

只读取 `train/201` 帧 4--553。全部 546 个合法窗口都进入描述统计。

### 执行

```bash
python src/evaluate.py F1 --data-root <STU数据根目录> --output-dir runs/ajae/schema33
```

程序逐窗口统计五帧与当前单帧的可见回波数、唯一体素数和新增体素数，并为当前帧 8、198、387、553 导出 PLY。

### 判断

F1 必须先确认坐标和 PLY 没有系统性配准错误。点数增加是必然结果；真正有信息的是唯一空间体素覆盖是否增加，以及新增体素是否来自合理表面。动态目标拖影需要记录，但暂不据此设计专用模型。

若末帧自身发生变化、位姿方向错误或 PLY 出现普遍重影，停止后续实验并修正坐标构造。否则进入 F2。

## 5. F2：正常稳定性

### 数据

仍只读取正常开发区间。当前帧预先固定为：

```text
8, 32, 55, 79, 103, 126, 150, 174,
198, 221, 245, 269, 292, 316, 340, 363,
387, 411, 435, 458, 482, 506, 529, 553
```

### 执行

```bash
python src/evaluate.py F2 --data-root <STU数据根目录> --output-dir runs/ajae/schema33 --device cuda
```

每个当前帧执行一次单帧 STU 和一次稠密 STU。两者在相同当前点上比较 MaxLogit、正常语义准确率和平均交并比。

### 判断

以下三项必须同时成立：

- MaxLogit 中位数增加不超过 0.02；
- MaxLogit 第 95 百分位增加不超过 0.05；
- 正常语义准确率下降不超过 0.02。

若 F2 失败，直接稠密输入不能作为无训练主方法，但仍执行 F3，以确定失败来自普遍分布失稳，还是同时存在可利用的异常信号。F2 结果会决定 F4 需要修复的具体问题。

## 6. F3：少量合成异常信号

### 数据和生成

F3 使用 `train/201` 开发区间内 8 个互不重叠片段，起点为：

```text
4, 79, 153, 228, 302, 377, 451, 526
```

每段包含 28 帧。程序为每段采样一个只含异常代理的固定世界，连续渲染全部 28 帧，再形成 24 个候选当前时刻输出。同一物体、同一帧和同一射线在相邻窗口中不能改变。只有在 2.5--50.0 米范围内至少含 5 个异常点的当前帧才进入平均精确率计算；结果文件分别记录候选帧数和实际纳入帧数。

### 执行

```bash
python src/evaluate.py F3 --data-root <STU数据根目录> --output-dir runs/ajae/schema33 --device cuda
```

### 判断

程序在每个世界内计算单帧和稠密 STU 的点级平均精确率差，再以 8 个世界为配对单位执行 10,000 次自助重采样。

- F2 通过且平均精确率差的 95% 区间下界大于 0：采用直接稠密 STU，进入方法固定和 V1。
- F2 通过但区间下界不大于 0：五帧直接输入没有得到正向支持，进入 F4。
- F2 失败：无论 F3 是否正向，都不能直接部署稠密 STU；进入 F4，并保留 F3 作为异常信号是否存在的诊断证据。

F3 是合成异常可行性证据，不能写成真实异常性能。

## 7. F4：最小训练分支

F4 只有在前三步说明直接稠密 STU 不足时才允许设计。第一候选固定为：每帧单独通过冻结 STU，特征和几何再对齐到当前帧，只训练一个小型异常头。

进入 F4 后才根据 F1--F3 的真实失败模式确定输入特征、损失、训练预算和开发选择规则。训练数据由 `train/206` 生成多个完整连续异常世界，再切成滑动窗口；每轮可以从冻结窗口池抽样，不能逐窗口重新生成世界。

在小型异常头未被真实结果否定以前，不授权新的大型 Point Transformer。移动正常目标仍不是训练目标或停止条件；它只在主异常指标出现正向结果后作为安全子组检查。

## 8. V1 与 T1

V1 在方法、阈值和可能的训练配方全部确定后，一次性执行两部分：

1. 打开 `train/201` 帧 554--681 的连续正常确认区间，检查正常输出是否失稳；
2. 在 19 条公开真实异常序列上检验合成异常判断能否迁移。

只有 V1 同时支持正常安全和真实异常迁移，才进入 51 条隐藏测试序列的 T1。V1 失败时，结论必须限定在开发区间或合成异常分布，不能调整阈值后重复打开同一确认数据。

## 9. 当前状态

F0 已通过机械核验，当前状态为待执行的 F1。尚未执行真实 F1、F2、F3，没有训练参数，没有 schema 33 性能结果，也没有打开正常确认、真实异常或隐藏测试。

下一项能够改变研究判断的动作是：在真实 `train/201` 上执行 F1，并检查几何统计和四个 PLY 是否支持正确稠密化。
