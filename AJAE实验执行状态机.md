# AJAE 实验执行状态机

## 1. 目的

本文件只规定 schema 33 的实际执行顺序。状态推进依据能够改变研究判断的真实证据，不以文件数量、测试数量或程序可运行为完成标准。

当前唯一问题是：五帧对齐到当前帧形成的稠密伪扫描，能否被冻结 STU 直接利用。B0、B1、B2、B3 训练矩阵及其正式门控已经停止，不得从 schema 32 恢复到当前主线。

## 2. 状态顺序

```text
F0  当前帧坐标和成对输入实现
 ↓
F1  五帧几何稠密性
 ↓
F2  冻结 STU 正常稳定性
 ↓
F3  固定世界的合成异常信号
 ↓
 ├─ 直接稠密 STU 得到支持 → C1 正常确认 → V1 真实异常
 └─ 直接输入不成立       → F4 最小训练分支
                                  ↓
                                  C1
                                  ↓
                                  V1
                                  ↓
                                  T1 隐藏测试
```

每个状态只回答一个会改变后续路线的问题。

## 3. F0：实现语义

### 输入

- schema 33 协议；
- STU 官方冻结权重；
- 连续五帧的原始点、强度和位姿。

### 必须成立的行为

1. 窗口为 \(X_{t-4:t}\)，当前时刻为最后一帧 \(t\)。
2. 程序使用 \(T_{t\leftarrow i}=T_{W\leftarrow t}^{-1}T_{W\leftarrow i}\) 对齐前四帧。
3. 第 (t) 帧经过单位变换后逐点保持不变。
4. 稠密伪扫描包含五帧全部可见回波，STU 为全部输入点产生分数和类别；程序同时保留全点输出、当前点行号和当前点视图。
5. 单帧和稠密条件使用同一个冻结 STU 与同一个 MaxLogit 定义。
6. 每个当前帧只产生一次输出，不执行重叠窗口概率平均。
7. 活动代码中不存在可训练 AJAE 模型、B2 扫描隔离分支或 B3 联合网络入口。
8. 过去四帧没有真实回波时，稠密输入、分数和类别精确退化为单帧结果。
9. 历史点与当前点共享 5 厘米体素时，STU 官方稀疏量化逆映射仍能恢复正确的当前点输出。
10. `normal_class` 逐点遵循 STU 官方查询类别与掩码置信度分配语义。
11. 在预定的 1--3 个真实单帧上，官方 `sweep=1` 数据读取、体素整理和模型路径与 AJAE 路径得到完全相同的逆映射和类别，以及浮点容差内相同的 MaxLogit。

### 完成条件

相关单元测试和机械资格检查全部通过，只说明实现符合以上定义。它不说明真实点云更稠密，也不说明 STU 输出有效。

## 4. F1：几何稠密性

### 数据

只读取 `train/201` 帧 4--553。全部 546 个合法窗口都进入描述统计。

### 执行

```bash
python src/evaluate.py F1 --data-root <STU数据根目录> --output-dir runs/ajae/schema33
```

程序逐窗口统计五帧与当前单帧的可见回波数、唯一体素数和新增体素数，并为当前帧 8、198、387、553 导出 PLY。每个体素尺度的汇总必须包含唯一体素比例的中位数、第 5 百分位、第 95 百分位以及新增体素数中位数。

### 判断

F1 必须先确认坐标和 PLY 没有系统性配准错误。点数增加是必然结果；真正有信息的是唯一空间体素覆盖是否增加，以及新增体素是否来自合理表面。动态目标拖影需要记录，但暂不据此设计专用模型。

若末帧自身发生变化、位姿方向错误或 PLY 出现普遍重影，停止后续实验并修正坐标构造。否则进入 F2。

## 5. F2：正常稳定性

### 数据

仍只读取正常开发区间。当前帧预先固定为：

```text
8, 32, 55, 79, 103, 126, 150, 174,
198, 221, 245, 269, 292, 316, 340, 363,
387, 411, 435, 458, 482, 506, 529, 553
```

### 执行

```bash
python src/evaluate.py F2 --data-root <STU数据根目录> --output-dir runs/ajae/schema33 --device cpu
```

在正式统计前，程序先在当前帧 8、198、387、553 上确认 `normal_class` 与 STU 官方语义预测逐点一致。每个当前帧执行一次单帧 STU 和一次稠密 STU。两者始终只比较相同当前点，但使用两个不同的科学掩码：MaxLogit 稳定性使用原始语义不等于 0、也不等于异常类 2，并位于 2.5--50.0 米闭区间的点；正常语义准确率和平均交并比使用语义目标不等于 255 且位于同一距离范围的点。

### 判断

以下三项必须同时成立：

- MaxLogit 中位数增加不超过 0.02；
- MaxLogit 第 95 百分位增加不超过 0.05；
- 正常语义准确率下降不超过 0.02。

若 F2 失败，直接稠密输入不能作为无训练主方法，但仍执行 F3，以确定失败来自普遍分布失稳，还是同时存在可利用的异常信号。F2 结果会决定 F4 需要修复的具体问题。

## 6. F3：固定世界的合成异常信号

### 数据和生成

F3 使用 `train/201` 开发区间内 16 个全局互不重叠片段，起点为：

```text
4, 38, 72, 106, 140, 174, 208, 242,
276, 310, 344, 378, 412, 446, 480, 514
```

每段包含 28 帧，相邻片段间隔 6 帧。交替片段构成 8 个 F3a 筛选世界和 8 个 F3b 扩展世界，根种子为 33000--33015。支撑面估计锚点只能取片段首尾各内缩 2 帧后的范围，保证所需的前后各 2 帧上下文完整位于本片段。程序为每段构造一个只含异常代理的固定世界，连续渲染全部 28 帧，再形成 24 个候选当前时刻输出。同一物体、同一帧和同一射线在相邻窗口中不能改变。

每个根种子最多允许 48 次世界放置尝试，第 (k) 次尝试种子固定为 `root_seed + 1000003 * k`。每个实体的合法支撑位置按世界与实体种子、锚点帧号和文件槽身份的散列排序；同一种子重复执行得到相同顺序，不同种子改变候选前缀。只有渲染开始前的物理放置失败可以尝试下一个种子；不得替换根种子，不得在渲染后重试，也不得因某个窗口不可见而重采样。只有在 2.5--50.0 米范围内至少含 5 个异常点的当前帧才进入点级指标计算。若固定世界没有任何合格当前帧，结果文件必须保留它及不可评价原因，并将其排除出指标自助重采样。

### 执行

```bash
python src/evaluate.py F3 --data-root <STU数据根目录> --output-dir runs/ajae/schema33 --device cpu
```

### 判断

程序按绑定字节哈希的 STU 官方点级评价定义，在每个世界内计算单帧和稠密 STU 的平均精确率、AUROC 和 FPR95，再以世界为配对单位执行 10,000 次自助重采样。官方库对并列分数的处理也是冻结评价语义的一部分。

- 8 个筛选世界全部可评价且平均精确率差的均值和中位数都不大于 0：F3a 否定异常信号，进入 F4。
- 其余情况，包括筛选结果为正或存在不可评价世界：自动执行预先固定的 8 个 F3b 世界，最终报告全部 16 个计划世界。
- 至少 12 个世界可评价、F2 通过且平均精确率差 95% 区间下界大于 0：采用直接稠密 STU，进入 C1。
- 最终可评价世界少于 12 个：合成可行性证据不足，进入 F4，不能宣称直接方法成功。
- F2 通过但最终区间下界不大于 0：五帧直接输入没有得到正向支持，进入 F4。
- F2 失败：无论 F3 是否正向，都不能直接部署稠密 STU；进入 F4，并保留 F3 作为异常信号是否存在的诊断证据。

F3 是合成异常可行性证据，不能写成真实异常性能。

## 7. F4：最小训练分支

F4 只有在前三步说明直接稠密 STU 不足时才允许设计。第一候选固定为：每帧单独通过冻结 STU，特征和几何再对齐到当前帧，只训练一个小型异常头。

进入 F4 后才根据 F1--F3 的真实失败模式确定输入特征、损失、训练预算和开发选择规则。训练数据由 `train/206` 生成多个完整连续异常世界，再切成滑动窗口；每轮可以从冻结窗口池抽样，不能逐窗口重新生成世界。

在小型异常头未被真实结果否定以前，不授权新的大型 Point Transformer。移动正常目标仍不是训练目标或停止条件；它只在主异常指标出现正向结果后作为安全子组检查。

## 8. C1、V1 与 T1

协议用三个字段封住方法分支：F1--F3 期间 `selected_method=null`；直接方法同时得到 F2 与 F3 支持后记录 `selected_method=direct_dense_stu`、`f4_required=false`；否则记录 `f4_required=true` 并只能进入 F4。F4 完成后记录 `selected_method=f4_small_head`、`f4_completed=true`。不满足其中一条完整分支时，协议拒绝进入 C1。

C1 在方法、阈值和可能的训练配方全部确定后，只打开 `train/201` 帧 554--681 的连续正常确认区间。合法窗口的源起点为 554--677，在线输出帧为 558--681，共 124 个输出。C1 只检查正常输出是否失稳，不读取真实异常。

V1 只在 C1 通过后打开 19 条公开真实异常序列，检验合成异常判断能否迁移。C1 与 V1 都不能用于回调方法、阈值或训练配方。

只有 C1 支持正常安全且 V1 支持真实异常迁移，才进入 51 条隐藏测试序列的 T1。C1 或 V1 失败时，结论必须限定在相应的更早证据范围，不能调整阈值后重复打开同一确认数据。

## 9. 当前状态

F0 已通过机械核验，协议状态顺序固定为 F1、F2、F3、F4、C1、V1、T1。命令行只允许运行与当前协议状态同名的 F1、F2 或 F3，不能跳过阶段。当前状态为待执行的 F1；尚未执行真实 F1、F2、F3，没有训练参数，没有 schema 33 性能结果，也没有打开正常确认、真实异常或隐藏测试。

下一项能够改变研究判断的动作是：在真实 `train/201` 上执行 F1，并检查几何统计和四个 PLY 是否支持正确稠密化。
