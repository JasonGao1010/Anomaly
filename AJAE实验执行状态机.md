# AJAE 实验执行状态机

本文由 `AJAE新主线方案.md` 与 `protocol.json` 派生，只记录 schema 31 当前主线、可用证据和失效边界，不自行引入科学规则。schema 30 的协议、代码和叙事由 Git 标签 `schema30-history-baseline` 保存；未纳入 Git 的旧实验产物保留原位并标识为 schema 30，不在当前权威文件中复制，也不能被 schema 31 正式流程消费。

# 1. 当前状态

```text
协议版本：schema 31
当前节点：R02
实现状态：schema 31 生成、审查、模型、训练与评价实现已就绪
正式生成：未执行
R02 数值阈值：占位，尚未结果盲冻结
正式训练：禁止
真实异常验证：密封
隐藏测试：密封
schema 31 性能证据：无
```

R00 已将 schema 30 基线 `f9b348e98de798366f3ca9291ed90ef752ed348a` 固定为 Git 标签 `schema30-history-baseline`，统一了本文、`AJAE新主线方案.md` 与 `protocol.json`，并使正式入口拒绝旧协议。R01 已用独立小型输入核验五姿态对称参考系、全点恢复、扫描置换、统一刚体变换、重复构造和旋转正交性。当前进入 R02；这两个已完成节点只提供合同与机械证据。

R02 的窗口世界生成、九帧开发片段、联合密度描述量、匹配、捷径审查和终裁代码已经就位。R02 开发总体只包含 24 个同生成机制片段，每个片段含 9 个连续扫描和 5 个重叠窗口；检查点选择只取这 24 个片段的逐片段融合 AP 宏平均。检查点记录必须绑定实际开发清单，并由所保存的逐片段标签与融合分数数组重新计算 AP。`dev.json` 当前为 `not_generated_R02`，片段数和窗口数均为 0，因此候选世界肉眼检查、匹配平衡、捷径安全和代理可用性尚未得到真实证据。R02 数值阈值仍为占位，必须在观察正式候选总体前结果盲冻结，R02 目前不能标记为通过。

另有 6 个环面有符号距离场片段的计划已经结果盲冻结，但它们属于 S01 留出合成移位检查，不属于 R02 开发总体。到达 S01 前，不得渲染、查看或资格审查这 6 个片段，也不得用其调节超参数、选择检查点或修改方法。当前既没有生成 24 个 R02 开发片段或 schema 31 训练 bank，也没有生成 6 个 S01 留出片段。

R03 所需的十项 `Window Densification Audit` 已在小型确定性夹具上全部通过，其中扫描置换最大 logit 误差为 $4.47\times10^{-8}$，出现概率融合与独立复算误差为 0。该结果只验证实现的机械语义；由于 R02 尚未通过，当前节点仍是 R02，不能越级把 R03 记为状态机完成。

当前不得运行任何 schema 31 训练、打开 19 条真实异常验证序列或读取隐藏测试。后续模型与训练代码的实现检查即使通过，也不能绕过 R02；R04 还需要真实执行 8 个固定窗口的微型过拟合，R05 才能在结果盲状态冻结训练配方。

# 2. 全局科学不变量

后续每个节点都必须保持以下五条合同。

1. 五个连续真实扫描组成基本观测单位，以 `window_start` 标识。
2. 窗口没有中心帧或主帧，五帧全部输出并接受同等监督。
3. 主模型不接收帧位置、相对时间、绝对时间或时间嵌入。
4. B3 在联合坐标中共同体素化、共同建立空间邻域并共同解码；B2 只增加扫描分组边界。
5. 每个可见回波按原始 `frame-ray` 身份恢复，跨合法窗口对概率作等权平均。

任何节点若破坏这些不变量，应停止并修复科学定义或实现；不得用后处理、包装器或第二套模型绕过。

# 3. 权威执行图

```text
R00  Schema-31 科学合同重写
 ↓
R01  对称五帧窗口语义
 ↓
R02  窗口级异常代理构造
 ↓
R03  联合稠密模型资格
 ↓
R04  训练体系资格
 ↓
R05  训练配方冻结
 ↓
G2   B1 与 B0，三个正式种子
 ↓
G3   B3 与 B1、B3 与 B2，三个正式种子
 ↓
S01  留出代理机制与合成分布移位
 ↓
M01  点级方法冻结
 ↓
V01  19 条真实异常序列一次性确认
 ↓
T01  51 条隐藏测试序列最终测试
```

节点只能在其前置节点的真实输出通过后执行。代码存在、文件存在、单元测试通过或 `passed=true` 不能单独解锁科学 Gate。

# 4. 节点合同

## R00：schema 31 科学合同

### 要消除的不确定性

文本、机器可读协议和正式入口是否仍把中心帧、时间分支或 B4/B5 当作当前科学对象。

### 必须完成

- 就地重写 `AJAE新主线方案.md`、本文与 `protocol.json`。
- `protocol.json` 只保存当前规则，不嵌入长篇历史结果。
- 为 schema 30 基线提交建立历史标签。
- 正式训练入口在任何数据读取、产物复用或优化开始前拒绝 schema 30。
- 检查权威文件中不存在中心目标、`q=0`、时间嵌入、B4/B5 主线和单成员输出规则。

### 解锁 R01 的条件

三份合同对五帧单位、无特权帧、无时间输入、联合空间运算和全点概率融合的定义完全一致。该检查只证明定义一致，不证明代码已经实现。

## R01：对称五帧窗口语义

### 要消除的不确定性

位姿对齐、点身份和窗口构造是否真正不依赖某个固定成员。

### 输入与实现

- 合法窗口起点与五个连续 `frame_ids`。
- 五个位姿共同决定的平移均值与 chordal 旋转均值。
- 每个点保留 `source_frame`、`source_slot`、`source_ray` 和仅用于 B2 分组的 `scan_group`。
- 返回完整联合点集，不提供时间位置作为模型特征。

### 通过证据

- 扫描顺序置换后坐标集合与点身份一致。
- 施加同一全局刚体变换后窗口相对坐标不变。
- 重复构造逐元素确定。
- 旋转正交且行列式为 $+1$。
- 五帧全部可见点无损恢复。

## R02：窗口级异常代理

### 要消除的不确定性

异常代理是否在完整五帧反事实世界中形成可观察几何，以及正常对照与代理能否在窗口层面排除明显捷径。

### 输入与实现

- 每个样本独立固定一个五帧 `WindowWorld` 和完整 `WorldSpec`。
- 五帧渲染前冻结实体、位置、朝向、尺度、材质和随机流。
- 复用 schema 7 几何、支撑、放置、射线和首次回波渲染器。
- 计算联合回波数、联合体素数、稠密增益、重复率、距离、遮挡、支撑、可见扫描数、最低可见高度、强度与 beam 分布。
- 在窗口层面重新匹配正常对照和异常代理并执行标签捷径审计。
- 正式开发总体固定为 24 个同生成机制片段；每个片段包含 9 个连续扫描和 5 个重叠窗口。

### 禁止事项

禁止中心帧资格、指定局部位置必须含异常点、补点、补底部回波、重复扫描或复制一帧。S01 前还禁止渲染、查看、资格审查或使用 6 个环面有符号距离场留出片段。

### 通过证据

24 个开发片段具备足够联合异常回波与空间覆盖；相比任意单帧存在可量化稠密增益；匹配后低层描述量不能以预注册标准恢复正常对照/代理标签。具体阈值须在观察正式审计结果前冻结，当前协议中的数值仍是占位，不能作为通过标准。6 个 S01 留出片段不参与本节点裁决。

## R03：联合稠密模型资格

### 要消除的不确定性

B3 是否真的使用跨扫描联合空间观测，以及 B2/B3 的差异是否只来自扫描分组边界。

### 必须验证

1. B3 中不同扫描、同一空间体素的点确实合并。
2. B2 中相同点因 `scan_group` 保持隔离。
3. 增加真实回波后 `log1p(population)` 发生对应变化。
4. 模型参数和输入张量不含帧顺序或时间特征。
5. 五帧置换后，恢复点身份的输出逐点等价。
6. B3 的空间邻域和三近邻上采样实际可以跨扫描取点，B2 不可以。
7. 所有输入点的监督梯度能够回到联合层级。
8. B2/B3 共用同一 `JointWindowPointTransformer` 与同一初始化。
9. 五帧每个可见输入回波都有输出，按原始身份进行的 occurrence 概率平均与独立逐点复算完全一致，并覆盖出现次数 1 至 5 的边界。

旧 E64–E71 时间分支资格和“时间图可观察性”不属于本节点，也不能作为通过证据。

## R04：训练体系资格

### 要消除的不确定性

B1、B2、B3是否看到完全相同的监督，损失是否按有效批次平衡，以及模型是否具备基本可学习性。

### 必须验证

- 固定 `window_train_bank` 的条目和顺序在三个条件间一致。
- 训练 bank 与 R02 的 24 个开发片段分别绑定其内容生成规则；R05 的配方状态推进不能改写或伪造这两类已冻结输入，正式模型与评价另绑定完整协议科学身份。S01 的 6 个留出片段保持独立计划身份，不能提前并入开发总体。
- B1 的五次独立单帧前向汇总完整五帧损失。
- B2/B3 对完整五帧全部点计算损失。
- 类别平衡在有效批次全部点上计算；单类批次安全。
- 一个 epoch 完整遍历一次 bank，梯度累计不在世界边界强制部分更新。
- 8 个固定窗口的微型过拟合在 500 次更新内达到训练 AP 不低于 99% 或损失低于 0.02。
- 微型过拟合固定使用种子 2001、学习率 $10^{-4}$、梯度累计 1、恒定学习率、权重衰减 0 和一次完整时期，不能参与配方选择。
- `tiny_overfit`、`pilot` 和 `formal` 模式及其随机种子互斥。

微型过拟合只证明实现可学习，不证明代理有效或具备泛化能力。

## R05：训练配方冻结

### 要消除的不确定性

正式比较前，学习率、有效批次、调度、权重衰减、检查点选择、统计单位和停止阈值是否已经结果盲固定。

### 先导范围

- 128 个固定窗口。
- 先导种子 1001、1002，与正式种子隔离。
- 学习率候选 $3\times10^{-5}$、$10^{-4}$、$3\times10^{-4}$。
- 梯度累计候选 1、2。
- 50 次更新先排除发散、饱和和梯度异常；200 次更新保留学习最快且安全的两组；600 次更新使用第二个先导种子确定最终配置。
- 50 次阶段要求六组都产生有限、可审计记录，排除完全概率饱和后至少保留两组；200 次阶段保留开发 AP 严格领先的两组；600 次阶段固定种子 1002。所有数值平局均按协议网格顺序裁决。
- 最佳学习率再比较恒定学习率与 5% 预热后余弦退火。
- 权重衰减候选 0、$10^{-5}$、$10^{-4}$。

### 输出

唯一正式训练配方、三个正式种子 0/1/2、结果盲固定的最终条件 B3 与部署种子 0、检查点选择规则、G2/G3 数值阈值、置信区间与安全裁决，以及 V01 的迁移条件。三个种子都进入 Gate 统计，最终条件和部署种子不得依据正式结果选择；V01 的 B1 参照同样固定使用种子 0。上述内容必须在任何正式训练前共同冻结，使正式检查点、方法冻结和真实评价绑定同一科学身份；冻结后不得依据正式结果更改。

## G2：异常代理监督有效性

使用三个正式种子比较 B1 与冻结 B0。该 Gate 回答单帧模型能否从新窗口代理监督中学到有效异常信号。必须同时报告代理性能与正常安全；通过条件由 R05 预注册。

每个正式评价记录必须引用内容寻址的逐片段标签/分数数组和正常安全原始数组，并在加载时重新计算汇总数字。只含调用者填写 AP、AUROC、FPR95 或 `passed=true` 的 JSON 不能成为 G2 证据。

G2 失败否定当前代理监督路线，不应通过扩大联合模型掩盖监督问题。

## G3：联合稠密化增益

使用三个正式种子同时比较 B3 与 B1、B3 与 B2。B2/B3必须保持相同五帧点、参数量、初始化、训练 bank、损失、优化器和窗口融合。

只有两项比较均满足 R05 的预注册条件，且正常安全不恶化到预注册边界之外，才支持“联合稠密化具有额外价值”。B3 优于 B1但不优于 B2时，现有证据不能把差异归因于跨扫描联合空间建模。

G3 训练授权必须读取已通过且已写入协议状态的真实 G2 裁决，并核对其中三个 B1 正式结果身份；不能只依赖协议中的一个字符串。G3 裁决继续从各条件、各种子的原始指标证据独立复算。

## S01：机制与合成移位

到达本节点后，才首次允许按已冻结计划渲染并查看 6 个环面有符号距离场留出片段。唯一正式生成入口必须先验证已通过且与协议绑定的 G3 裁决，再按计划位置 24--29 重建源窗口、`WorldSpec`、种子与递增重试规则，调用留出渲染器并保存完整片段清单和逐窗口描述量。在不改变主 Gate 的前提下，用它们检查留出代理家族与合成分布移位，并同时检查联合密度分层、异常边界和正常运动安全。这 6 个片段只解释适用边界，不得用于资格审查、超参数调节、检查点选择或方法修改，也不能用探索性结果重新选择 G2/G3 配方。

## M01：点级方法冻结

冻结模型、权重、点阈值、预处理、窗口构造、融合、评价器、依赖和输入身份，并绑定 R05 已经结果盲固定的 V01 迁移条件。B0、B1、B3 的点阈值必须从被选中正式结果的原始正常安全证据派生，不能由调用者填入。冻结清单只用于保护随后的一次性真实数据确认，不能替代模型有效性证据。

## V01：19 条真实异常确认

仅在 M01 完成后打开一次 19 条公开真实异常验证序列。每条记录必须保存完整覆盖域中的点身份、坐标、原始语义和分数数组，并由评价器重新计算逐序列和汇总 AP、AUROC、FPR95及正常安全。只有通过的 V01 裁决与 M01 同时绑定后才能解封 T01。若未达到预注册迁移条件，停止当前研究周期；不得在已打开序列上继续调参。

## T01：51 条隐藏测试

仅在 V01 支持迁移后运行一次冻结方法。测试结果只描述冻结方法在隐藏序列上的表现，不回流到方法设计。

# 5. 证据继承与失效边界

## 5.1 继续有效

- schema 7 程序化几何、连续尺寸和覆盖证据。
- 支撑面、放置、碰撞和世界冻结机制。
- OS1-128 ray mapping、首次回波、遮挡、强度和空射线机制。
- 正常对照与异常代理共用渲染路径。
- 冻结 STU 官方权重、128 维特征、19 维证据及点级恢复。
- E61 原始正常安全集合的 frame/ray 身份；schema 31 覆盖域上的安全分数必须重算。
- 稳定点身份、官方评价器数值等价和密封数据访问。

这些证据只支持各自的机械或语义范围，不自动证明 schema 31 的窗口代理、联合模型或性能。

## 5.2 可复用但必须重新资格

- E26 的世界构造原语；旧长序列遍历方式无效。
- E37 的点身份一致性原语；中心窗口资格无效。
- E55 的 150 维特征内容；中心坐标和时间输入无效。
- E70 的空类别安全损失原语；逐窗口平衡与世界尾部分步无效。
- E71 的按点身份概率平均原语；B4 专属地位无效。
- E72 的 B0 定义；旧开发总体上的 B0 数值必须重算。

## 5.3 仅作旧分布历史证据

- E45B-v2：旧中心窗口的正常对照/异常代理匹配。
- E48：旧中心窗口的低层难度诊断。
- E76-C1：旧开发分布的可见离地高度捷径审计。

R02 必须在 schema 31 窗口总体上重新执行对应审计。

## 5.4 不进入 schema 31 正式统计

- E56 的中心坐标资格。
- E57–E60 的中心开发世界。
- E63 的中心 $q=0$ 共同域和训练冻结。
- E64–E69 的时间分支模型资格。
- E73–E76、AJAE-F0-X 与 AJAE-F1-X。
- schema 30 的 B1、B2、B3、B4、B5及 `fast-v5`。
- 所有基于中心帧的 Gate 2/3训练和结果。

# 6. R00 一致性检查

R00 的检查对象仅为当前权威文件与正式训练入口，检查内容如下。

| 检查项 | `AJAE新主线方案.md` | `protocol.json` | 入口护栏 |
|---|---|---|---|
| 五帧基本单位 | 必须明确 | `window.frames=5`、连续起点 | schema 31 才可继续 |
| 无特权帧 | 必须明确 | `privileged_frame=null` | schema 30 被拒绝 |
| 无时间输入 | 必须明确 | 禁止特征清单 | 无旧配置复用 |
| B3 联合空间运算 | 必须明确 | `grouping_mode=joint` | R03 机械实现已核验，节点未越级推进 |
| 全点概率融合 | 必须明确 | 全成员输出与 `frame-ray` 均值 | R03/R04 机械实现已核验，尚无真实实验 |

禁止词检查应区分“当前规则”与“失效历史说明”。历史边界章节可以提及中心帧、$q=0$、B4/B5 和时间分支，但必须明确它们不属于当前路线。

# 7. 推进规则

每次只执行当前已解锁节点中能够消除关键科学不确定性的最小工作。节点完成时分别报告实现准备、真实执行、结果核验和科学结论，不能相互替代。若权威文档、代码和真实产物冲突，以查明冲突为先，不把旧成功与新定义拼接。
