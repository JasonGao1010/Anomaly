{
  "schema_version": 30,
  "authority": {
    "document": "AJAE新主线方案.md",
    "route": "normal controls and anomaly proxies rendered by one OS1-128 first-return approximation, frozen STU, four-level time-stratified point model",
    "supersedes": "schema 29 counterfactual-memory route"
  },
  "task": {
    "name": "AJAE",
    "window_output": "one anomaly logit for every visible return in every input frame",
    "sequence_output": "one equal mean of window probabilities for every canonical frame-ray return",
    "does_not_predict": ["object_identity", "instance_mask", "trajectory", "object_class"]
  },
  "data": {
    "normal_training": {
      "partition": "train", "sequence_id": 206,
      "role": "AJAE_parameter_updates_and_renderer_calibration",
      "labels_available": true, "frame_range": [0, 448], "excluded_source_frames": []
    },
    "development": {
      "partition": "train", "sequence_id": 201,
      "role": "development_only_no_gradients",
      "labels_available": true, "frame_range": [0, 681],
      "excluded_source_frames": [0, 1, 2, 3],
      "exclusion_reason": "verified exact internal duplication in scans and labels"
    },
    "public_anomaly_validation": {
      "partition": "val", "role": "one_time_real_anomaly_confirmation_after_method_freeze",
      "labels_available": true, "method_freeze_required": true,
      "sequence_ids": [125, 137, 138, 139, 140, 141, 142, 143, 144, 145, 146, 147, 148, 149, 150, 151, 152, 153, 169],
      "sequence_frame_counts": {
        "125": 255, "137": 560, "138": 516, "139": 542, "140": 481,
        "141": 697, "142": 537, "143": 469, "144": 323, "145": 337,
        "146": 374, "147": 401, "148": 602, "149": 378, "150": 505,
        "151": 277, "152": 534, "153": 271, "169": 600
      }
    },
    "hidden_test": {
      "partition": "test", "role": "final_hidden_test_after_supported_public_confirmation",
      "labels_available": false, "method_freeze_required": true,
      "sequence_ids": [100, 101, 102, 103, 104, 105, 106, 107, 108, 109, 110, 111, 112, 113, 114, 115, 116, 117, 118, 119, 120, 121, 122, 123, 124, 126, 127, 128, 129, 130, 131, 132, 133, 134, 135, 136, 154, 155, 156, 157, 158, 159, 160, 161, 162, 163, 164, 165, 166, 167, 168]
    }
  },
  "labels": {
    "packed_label": {"semantic_bits": [0, 15], "instance_bits": [16, 31]},
    "binary_anomaly": {
      "raw_semantic_0": "ignore unless replaced by a valid inserted return",
      "raw_semantic_2": "anomaly", "other_nonzero_semantics": "normal",
      "normal_control_return": "normal", "anomaly_proxy_return": "anomaly"
    },
    "zero_coordinate_slot": "not a visible return unless rendering creates a valid inserted return",
    "normal_control_classes": {"10": "car", "11": "bicycle", "15": "motorcycle", "18": "truck", "20": "other-vehicle", "30": "person", "31": "bicyclist", "32": "motorcyclist"},
    "moving_normal_semantic_ids": [252, 253, 254, 255, 256, 257, 258, 259],
    "normal_semantic_class_map": {
      "0": 255, "1": 255, "2": 255, "10": 0, "11": 1, "13": 4,
      "15": 2, "16": 4, "18": 3, "20": 4, "30": 5, "31": 6,
      "32": 7, "40": 8, "44": 9, "48": 10, "49": 11, "50": 12,
      "51": 13, "52": 255, "60": 8, "70": 14, "71": 15, "72": 16,
      "80": 17, "81": 18, "99": 255, "252": 0, "253": 6, "254": 5,
      "255": 7, "256": 4, "257": 4, "258": 3, "259": 4
    }
  },
  "stu": {
    "source": "STU official Mask4Former-3D", "checkpoint": "weights/59p6pq_ens1.ckpt",
    "repository": "../DynaCAN-deps/stu_dataset/Mask4Former3D", "voxel_size_m": 0.05,
    "input_channels": ["intensity", "official_STU_distance"],
    "point_feature_dim": 128, "normal_evidence_dim": 19,
    "assignment_reliability_dim": 1, "no_object_reliability_dim": 1,
    "query_count": 100, "point_feature_source": "point_features_head sparse output",
    "normal_evidence_rule": "select minimum-index argmax_q sigmoid(mask[v,q])*max_c<19 softmax(logits[q])[c], then use sigmoid(mask[v,q*])*softmax(logits[q*])[:19]",
    "assignment_reliability_rule": "sigmoid(mask[v,q*])*max_c<19 softmax(logits[q*])[c]",
    "no_object_reliability_rule": "softmax(logits[q*])[19]",
    "b0_score": "official_STU_MaxLogit", "frozen": true, "full_forward_is_eval": true
  },
  "render": {
    "name": "rigid-single-frame OS1-128 first-return counterfactual approximation",
    "source_sequence_id": 206,
    "calibration_file": "runs/ajae/calibration.pt",
    "ray_grid": {
      "beam_count": 128, "column_count": 1024,
      "canonical_identity": ["beam_id", "azimuth_column"],
      "file_slot_role": "input_output_mapping_only",
      "audit_required": ["slot_and_empty_pattern", "beam_elevation_rows", "beam_periodicity", "azimuth_continuity", "phase_aligned_cross_frame_direction_stability", "multi_return_or_duplicate_ray_reordering", "range_image_round_trip"],
      "azimuth_continuity_definition": {
        "version": "E10-v3",
        "unit": "within-beam-row adjacent candidate columns",
        "eligible_edge": "both (b,a) and (b,(a+1) mod 1024) contain real XYZ returns; never bridge empty columns",
        "circular_increment_deg": [0.1, 0.6],
        "minimum_eligible_edges_per_beam_frame": 512,
        "single_orientation_across_all_beams_and_frames": true,
        "wraparound_checked_separately": true,
        "all_observed_wraparound_edges_must_pass": true,
        "unobserved_wraparound_rule": "if column 1023 or 0 is always empty across all audit frames, classify as unidentifiable_from_observed_returns",
        "directly_identified_wraparound_beams": 120,
        "unidentifiable_wraparound_beams": 8,
        "interpolate_or_fabricate_missing_wraparound": false,
        "repeat_read_exact_match": true,
        "beam_offset_correction": false,
        "cross_frame_absolute_phase_checked": false,
        "frozen_conclusion": "All observable azimuth-adjacent edges are continuous; wraparound is directly evidenced for 120/128 rows and unidentifiable from observed returns for 8/128 rows in train/206."
      },
      "phase_aligned_cross_frame_direction_stability_definition": {
        "version": "E11-v2",
        "independent_from_azimuth_continuity": true,
        "historical_fixed_slot_result": "E11-v1 FAIL; a fixed file slot is not a stable physical ray and must not be reused as rho_f",
        "checks_frame_specific_canonical_ray_mapping": true,
        "beam_identity": "inherit the deterministic ordered 128-row identity from E09-v2",
        "mapping_family": "one integer cyclic column shift per frame and beam; no pointwise nearest-neighbour assignment, non-cyclic permutation, duplicate, or fabricated return",
        "mapping_estimator": "deterministic alternating maximization of observed unit-direction agreement with one fixed canonical direction template; equal-weight template update and exhaustive 1024-shift row update",
        "mapping_gauge": "for every beam, anchor the frame-0 cyclic shift to zero by an equivalent canonical-template roll",
        "initial_template": "equal-weight normalized mean by raw row/column slot; raw slots never observed in train/206 use deterministic same-beam periodic interpolation only for the direction template, never as a return; the template is re-estimated after every mapping update",
        "maximum_mapping_iterations": 100,
        "mapping_convergence": "all 449x128 integer shifts unchanged",
        "empty_slot_rule": "all 128x1024 canonical rays and raw slots remain in rho_f; an empty mapped ray has no return and normal range +infinity",
        "round_trip": "raw slot -> (b,a) -> rho_f(b,a) -> raw slot must be exact for every slot in every frame; valid-return count and raw XYZI/XYZ/range must be exactly recoverable",
        "minimum_observed_frames_per_qualified_slot": 2,
        "q99_direction_error_deg_exclusive_maximum": 0.17578125,
        "maximum_direction_error_deg_exclusive_maximum": 0.3515625,
        "per_beam_q99_deg_exclusive_maximum": 0.17578125,
        "per_column_q99_deg_exclusive_maximum": 0.17578125,
        "per_frame_q99_deg_exclusive_maximum": 0.17578125,
        "repeat_read_and_reconstruction_exact_match": true,
        "criteria_frozen_before_formal_run": true
      },
      "coordinate_source_diagnostic": {
        "version": "E11-D1",
        "question": "Determine whether released STU .bin XYZ is raw lidar/sensor-frame Cartesian output, a whole-frame rigid transform, per-column or per-point motion-compensated output, or unresolved from released evidence.",
        "required_evidence": ["official STU paper and supplementary material", "official STU repository including full history", "released calib.txt and poses.txt semantics", "released file inventory for timestamps, packets, bags, and Ouster metadata", "official Ouster Cartesian projection and motion-compensation definitions"],
        "ouster_geometry_check": "distinguish physical beam direction from normalized Cartesian XYZ because Ouster XYZLut uses range-scaled direction plus a beam-origin-dependent offset",
        "allowed_outcomes": ["raw_lidar_or_sensor_cartesian", "whole_frame_rigid_transformed", "per_column_or_per_point_motion_compensated", "insufficient_released_evidence"],
        "whole_frame_test_unlock_rule": "E11-D2 is eligible only if a concrete whole-frame transform applied to released XYZ is identified and invertible from released calibration or poses.",
        "deskew_test_unlock_rule": "E11-D3 is eligible only if per-point/per-column timestamps plus the applied deskew trajectory/model, or original Ouster packet metadata sufficient to reconstruct them, are available.",
        "forbidden_followups": ["unconstrained permutation", "higher-capacity column relabelling", "free ray mapping fitted to E11 residuals"],
        "E12_locked": true
      },
      "row_layout_phase_diagnostic": {
        "version": "E11-D4a",
        "question": "Determine whether train/206 has a deterministic fixed per-beam column-phase structure consistent with common-azimuth columns or with an Ouster-like row shift, without naming the shift as factory pixel_shift_by_row.",
        "nominal_column_angle": "theta_a = -2*pi*a/1024, inheriting the E10-v3 scan orientation",
        "frame_beam_phase": "equal-weight circular mean of atan2(y,x)-theta_a over real returns only",
        "minimum_real_returns_per_frame_beam": 512,
        "frame_common_phase": "equal-weight circular mean of all 128 finite frame-beam phases",
        "relative_phase": "wrap(frame_beam_phase-frame_common_phase)",
        "fixed_beam_phase": "equal-weight circular mean of the 449 relative phases for each beam",
        "integer_shift": "nearest integer of fixed_beam_phase/(360/1024), represented relative to the circular row consensus; ties choose the smaller integer",
        "stability_q99_deg_exclusive_maximum": 0.17578125,
        "stability_maximum_deg_exclusive_maximum": 0.3515625,
        "common_azimuth_column_rule": "stability passes and max_b(abs(wrap(fixed_beam_phase_b))) < 0.17578125 deg after the per-frame common-phase gauge",
        "stable_nonconstant_row_phase_rule": "stability passes, the common-azimuth-column rule fails, and integer_shift is nonconstant across beams",
        "allowed_outcomes": ["common_azimuth_column_consistent", "stable_nonconstant_row_phase_structure", "unstable_or_unidentifiable"],
        "interpretation_limit": "A stable nonconstant row phase is jointly compatible with pixel_shift_by_row, beam azimuth offsets, beam-origin range effects, and deskew; D4a alone cannot identify factory metadata or change rho_f.",
        "repeat_read_exact_match": true,
        "cross_sequence_validation_reserved_for": "E11-D4c",
        "criteria_frozen_before_formal_run": true
      },
      "ouster_projection_self_calibration": {
        "version": "E11-D4b",
        "question": "Can the released Cartesian returns identify one Ouster-form projection model whose intrinsics learned from one disjoint frame split predict physical ray lines in the other split?",
        "official_model": {
          "encoder_angle": "eta_ba = gamma - 2*pi*a/1024 + s_b*(2*pi/1024), with D4a integer shifts s_b fixed and never re-optimized",
          "beam_origin": "o_ba = (o_x*cos(eta_ba), o_x*sin(eta_ba), o_z)",
          "ray_direction": "u_ba = (cos(eta_ba+beta_b)*cos(alpha_b), sin(eta_ba+beta_b)*cos(alpha_b), sin(alpha_b))",
          "nuisance_range_elimination": "t = dot(u_ba, X-o_ba); X_hat=o_ba+t*u_ba; official range d=t+sqrt(o_x^2+o_z^2)"
        },
        "identifiable_parameters": ["one global column phase gamma", "o_x and o_z beam-origin translations", "128 beam altitude angles alpha_b", "128 beam azimuth offsets beta_b"],
        "forbidden_parameters": ["per-frame phase", "per-frame beam parameters", "per-column free offsets", "per-point transform", "free permutation"],
        "fit_split": "even-indexed frames of train/206",
        "heldout_split": "odd-indexed frames of train/206",
        "reverse_split_stability_check": true,
        "global_fit_sample": "all real returns at columns a mod 16 = 0 in the fitting split; final beam angles are re-estimated from all real fitting returns",
        "robust_loss": "Huber loss on orthogonal Cartesian line residual with 0.05 m transition",
        "optimizer": "scipy L-BFGS-B, numerical gradient, maxiter=500, ftol=1e-12, gtol=1e-9",
        "fixed_multistarts": "D4a common-phase consensus paired with (o_x,o_z) in {(0,0),(0.05,0),(0.05,0.05),(0.05,-0.05)} metres; choose the lowest objective, then lexicographic parameters on an exact tie",
        "global_bounds": {
          "gamma": "within one nominal column of the D4a circular common-phase consensus",
          "o_x_m": [0.0, 0.2],
          "o_z_m": [-0.2, 0.2]
        },
        "heldout_angular_q99_deg_exclusive_maximum": 0.17578125,
        "heldout_angular_maximum_deg_exclusive_maximum": 0.3515625,
        "every_beam_heldout_q99_deg_exclusive_maximum": 0.17578125,
        "every_column_heldout_q99_deg_exclusive_maximum": 0.17578125,
        "split_origin_l2_difference_m_exclusive_maximum": 0.01,
        "split_ray_grid_q99_deg_exclusive_maximum": 0.17578125,
        "split_ray_grid_maximum_deg_exclusive_maximum": 0.3515625,
        "all_recovered_ranges_positive": true,
        "repeat_read_and_fit_exact_match": true,
        "interpretation_limit": "D4b is within-sequence identification. It cannot establish factory metadata identity or cross-sequence transfer; those are reserved for D4c.",
        "criteria_frozen_before_formal_run": true
      },
      "ouster_intrinsics_cross_sequence_validation": {
        "version": "E11-D4c-v1",
        "question": "Does the unchanged train/206 D4b main model predict the physical ray lines of the independent normal development sequence train/201?",
        "source_model": "the even-frame main model in runs/ajae/e11_d4b_calibration.npz",
        "source_model_sha256": "42791278e2a6b36d975bbe9dc957c6f303b8b424c5b97cf54e42380ad191f253",
        "validation_sequence": "train/201 frames 4 through 681 inclusive",
        "excluded_frames": [0, 1, 2, 3],
        "exclusion_basis": "frozen protocol exclusion for verified exact internal scan and label duplication; all 678 included files are exactly 128x1024x4 float32",
        "forbidden_adaptation": ["sequence-global phase fitting", "row-shift fitting", "beam-angle fitting", "beam-origin fitting", "per-frame fitting", "per-point transform"],
        "minimum_real_returns_per_frame_beam": 512,
        "overall_q99_deg_exclusive_maximum": 0.17578125,
        "overall_maximum_deg_exclusive_maximum": 0.3515625,
        "every_beam_q99_deg_exclusive_maximum": 0.17578125,
        "every_column_q99_deg_exclusive_maximum": 0.17578125,
        "every_frame_q99_deg_exclusive_maximum": 0.17578125,
        "all_recovered_ranges_positive": true,
        "repeat_raw_read_exact_match": true,
        "labels_read": false,
        "criteria_frozen_before_formal_run": true
      },
      "ouster_intrinsics_cross_sequence_validation_v2": {
        "version": "E11-D4c-v2",
        "question": "Does the unchanged train/206 D4b main model predict every actually observed return in independent normal sequence train/201 without confusing visibility support with ray validity?",
        "source_model": "the even-frame main model in runs/ajae/e11_d4b_calibration.npz",
        "source_model_sha256": "42791278e2a6b36d975bbe9dc957c6f303b8b424c5b97cf54e42380ad191f253",
        "validation_sequence": "train/201 frames 4 through 681 inclusive",
        "excluded_frames": [0, 1, 2, 3],
        "observed_return_rule": "evaluate every finite nonzero XYZ return; do not interpolate, fabricate, or borrow a neighbouring column",
        "per_frame_beam_minimum": null,
        "support_reporting": ["frame-beam minimum, median, q01, q05, q25, q75, q95, q99, maximum", "number of frame-beam cells below the historical 512 threshold", "number of affected frames and beams", "canonical-ray observation-count distribution and zero-count rays"],
        "key_coverage_strata": ["each of 128 beams across all validation frames", "each of 1024 columns across all validation frames", "each of 678 validation frames"],
        "unobservable_rule": "A key coverage stratum with zero real returns is a systematic coverage failure. An individual canonical ray (b,a) with zero returns is marked unobservable, receives no residual, and is deferred to E11-v3 qualification rather than fabricated or treated as a zero residual.",
        "forbidden_adaptation": ["sequence-global phase fitting", "row-shift fitting", "beam-angle fitting", "beam-origin fitting", "per-frame fitting", "per-point transform"],
        "overall_q99_deg_exclusive_maximum": 0.17578125,
        "overall_maximum_deg_exclusive_maximum": 0.3515625,
        "every_observable_beam_q99_deg_exclusive_maximum": 0.17578125,
        "every_observable_column_q99_deg_exclusive_maximum": 0.17578125,
        "every_observable_frame_q99_deg_exclusive_maximum": 0.17578125,
        "all_observed_residuals_finite": true,
        "all_recovered_ranges_positive": true,
        "repeat_raw_read_exact_match": true,
        "labels_read": false,
        "criteria_frozen_before_formal_run": true
      },
      "calibrated_canonical_ray_identity_qualification": {
        "version": "E11-v3",
        "question": "After replacing normalized XYZ with the D4a-D4c validated Ouster-like geometry, does one deterministic canonical ray identity r=(b,c) give a bijective raw-slot mapping and satisfy the frozen physical-direction limits on train/206 and independent train/201?",
        "source_model": "the even-frame main model in runs/ajae/e11_d4b_calibration.npz",
        "source_model_sha256": "42791278e2a6b36d975bbe9dc957c6f303b8b424c5b97cf54e42380ad191f253",
        "source_cross_sequence_evidence": "runs/ajae/e11_d4c_v2_validation.npz",
        "artifact_decoder_coordinate_convention": "the stored encoder gauge gamma is converted to Cartesian bearing by adding the fixed pi-radian Ouster encoder offset; this is a coordinate gauge only and adds no fitted parameter",
        "source_cross_sequence_evidence_sha256": "bdc85457de6add74f6e061ab7db43c8cb69919c0ac22f14fe812897bc3bf4fe8",
        "canonical_mapping": {
          "raw_to_ray": "raw slot (b,a) maps to r=(b,c), c=(a-s_b) mod 1024",
          "ray_to_raw": "rho_f(b,c)=raw slot (b,(c+s_b) mod 1024), identical for every frame",
          "encoder_angle": "eta_c=gamma-2*pi*c/1024",
          "empty_return": "ray identity remains defined and normal range is +infinity"
        },
        "qualification_data": ["train/206 frames 0 through 448", "train/201 frames 4 through 681"],
        "forbidden_adaptation": ["any refit", "per-sequence phase", "per-frame mapping", "per-point transform", "free permutation", "fabricated return"],
        "round_trip": "all 128x1024 raw slots per frame must be recovered bijectively; XYZ and intensity are bitwise identical after raw-to-ray-to-raw",
        "per_sequence_overall_q99_deg_exclusive_maximum": 0.17578125,
        "per_sequence_overall_maximum_deg_exclusive_maximum": 0.3515625,
        "every_observable_beam_column_frame_q99_deg_exclusive_maximum": 0.17578125,
        "all_observed_residuals_finite": true,
        "all_recovered_ranges_positive": true,
        "unobservable_individual_ray_rule": "report as model-defined but not directly qualified; assign no residual and do not fabricate a return",
        "key_coverage_rule": "every beam, canonical column, and included frame must have at least one real return",
        "repeat_raw_read_exact_match": true,
        "labels_read": false,
        "criteria_frozen_before_formal_run": true
      },
      "published_return_cardinality_audit": {
        "version": "E12-v1",
        "question": "Does the released STU interface expose exactly one fixed record slot per calibrated canonical ray, containing either no return or one published return, without a multi-return dimension or dynamic ray reassignment?",
        "data": ["train/206 frames 0 through 448", "train/201 frames 4 through 681"],
        "canonical_mapping": "inherit E11-v3 c=(a-s_b) mod 1024 and its fixed bijective inverse",
        "record_layout": "one float32 (x,y,z,intensity) record for each of 128x1024 raw slots",
        "empty_record": "x=y=z=0; nonzero intensity on an empty XYZ record is invalid",
        "published_return": "finite XYZ not all zero; exactly one record occupies its canonical ray slot",
        "required_checks": ["every included file has exactly 128x1024x4 float32 values", "no non-finite published XYZ or intensity", "no empty-XYZ record has nonzero intensity", "raw-to-canonical mapping is fixed and bijective in every frame", "raw-to-ray-to-raw XYZI is bitwise exact", "released files and official loader expose no return index, return count, first/second/strongest flag, or parallel return array", "occupancy changes across frames remain attached to the same canonical ray rather than changing the mapping"],
        "pass_definition": "Every canonical ray has exactly one stable published record slot per frame and therefore 0 or 1 published point; no observable multi-return field or dynamic reorder exists in the released interface.",
        "interpretation_limit": "PASS does not identify the upstream Ouster return-selection policy and does not prove the physical sensor emitted only one return. Renderer wording remains a single-published-return Ouster-like approximation; first-return is a modelling convention unless independently documented.",
        "repeat_raw_read_exact_match": true,
        "labels_read": false,
        "criteria_frozen_before_formal_run": true
      },
      "published_return_cardinality_audit_v2": {
        "version": "E12-v2",
        "question": "Is the released STU interface a stable single-published-return/empty-slot canonical-ray interface when occupancy inherits the E08 XYZ-only definition?",
        "data": ["train/206 frames 0 through 448", "train/201 frames 4 through 681"],
        "canonical_mapping": "inherit E11-v3 c=(a-s_b) mod 1024 and its fixed bijective inverse",
        "occupancy": "return exists iff XYZ is not exactly (0,0,0); intensity is payload and never participates in occupancy",
        "empty_intensity_rule": "ignore for occupancy and preserve bitwise through round trip; report its finite-value distribution but do not require zero",
        "published_return": "one finite (x,y,z,intensity) record with nonzero XYZ in its fixed canonical ray slot",
        "inherited_checks": ["every included file has exactly 128x1024x4 float32 values", "one fixed record slot per canonical ray per frame", "published cardinality is 0 or 1", "no non-finite nonempty XYZI", "fixed bijective canonical mapping", "raw-to-ray-to-raw XYZI bitwise exact", "no released return index, return count, first/second/strongest flag, or parallel return array", "occupancy transitions remain attached to the same canonical ray", "two independent raw reads exactly match"],
        "pass_definition": "Every canonical ray has one stable record slot containing either XYZ-zero empty payload or one finite published point; no observable multi-return container or dynamic reorder exists.",
        "interpretation_limit": "PASS does not identify upstream first/strongest/last selection and does not prove the physical sensor emitted only one return. First-return remains a renderer modelling convention.",
        "labels_read": false,
        "criteria_frozen_before_formal_run": true
      },
      "canonical_return_count_round_trip": {
        "version": "E13",
        "question": "Does the E11-v3 raw-slot/canonical-ray bijection preserve every XYZ-defined published return without loss, duplication, or identity conflict?",
        "data": ["train/206 frames 0 through 448", "train/201 frames 4 through 681"],
        "canonical_mapping": "inherit E11-v3 c=(a-s_b) modulo 1024 and fixed inverse without refitting",
        "occupancy": "inherit E12-v2: return exists iff XYZ is not exactly (0,0,0); intensity is payload only",
        "round_trip": "raw valid slot -> unique canonical ray -> inverse raw slot",
        "pass_conditions": ["raw, canonical, and recovered valid-return counts are equal in every frame", "the recovered occupancy mask is bitwise identical to the raw occupancy mask", "every valid raw slot maps to exactly one in-range canonical ray", "no valid canonical ray is duplicated and the inverse recovers every source slot identity", "two independent complete reads reproduce all per-frame counts, masks, identities, hashes, and summaries exactly"],
        "forbidden": ["interpolation", "fabricated return", "dropping an empty slot from the mapping domain", "geometry-dependent nearest-neighbour matching", "reading labels"],
        "interpretation_limit": "PASS establishes only return-count and slot/ray identity preservation. Range, direction, and Cartesian fidelity remain E14 questions.",
        "labels_read": false,
        "criteria_frozen_before_formal_run": true
      },
      "calibrated_geometry_round_trip": {
        "version": "E14",
        "question": "Does encoding each observed STU point as a scalar distance on its fixed E11-v3 Ouster-like beam and decoding it on the same beam preserve numerical range/direction while keeping the calibrated projection error within the already frozen ray-grid resolution?",
        "data": ["train/206 frames 0 through 448", "train/201 frames 4 through 681"],
        "ray_model": "unchanged E11-D4b even-frame model and E11-v3 raw-slot/canonical-ray bijection; the D4b artifact encoder gauge uses a fixed pi-radian encoder-to-Cartesian bearing offset",
        "encoding": "t=dot(u,X-o); official range d=t+sqrt(o_x^2+o_z^2); decoded point X_hat=o+t*u",
        "occupancy": "inherit E12-v2 XYZ-only occupancy; empty rays remain empty and are not assigned a fabricated range",
        "numerical_round_trip_limits": {"maximum_scalar_range_error_m": 1e-9, "maximum_decoded_direction_error_rad": 1e-6},
        "calibrated_projection_limits": {"angular_q99_deg_exclusive_maximum": 0.17578125, "angular_maximum_deg_exclusive_maximum": 0.3515625, "normalized_cartesian_q99_exclusive_maximum": "sin(0.17578125 deg)", "normalized_cartesian_maximum_exclusive_maximum": "sin(0.3515625 deg)"},
        "normalized_cartesian_error": "norm(X-X_hat)/norm(X-o); absolute Cartesian error in metres is fully reported but has no post-hoc fixed-metre threshold because the same angular ray error produces range-dependent displacement",
        "additional_conditions": ["all observed t and official d are positive and finite", "decoded occupancy count equals raw occupancy count in every frame", "two independent complete runs reproduce masks, per-frame statistics, hashes, and summaries exactly"],
        "forbidden": ["XYZ/norm(XYZ) as the physical ray", "per-frame or per-point fitting", "changing E11-v3 geometry", "fabricated return", "labels"],
        "interpretation_limit": "PASS qualifies the calibrated projection and its numerical encode/decode path; it does not claim published XYZ lies exactly on a factory-metadata ray or that the self-calibrated parameters are factory metadata.",
        "labels_read": false,
        "criteria_frozen_before_formal_run": true
      },
      "multi_sequence_ray_qualification": {
        "version": "E15-v1",
        "question": "Does one unchanged canonical-ray topology, mapping, published-return interface, and calibrated geometry satisfy the applicable E08-E14 claims separately on normal train/206 and independent normal train/201?",
        "data": ["train/206 frames 0 through 448", "train/201 frames 4 through 681"],
        "unchanged_model": "E11-D4b even-frame geometry plus E11-v3 fixed c=(a-s_b) modulo 1024 mapping",
        "per_sequence_topology_checks": ["every file is finite float32 128x1024x4", "all 128 raw beam rows exist and every frame/beam has at least one real return", "all actually observed immediate within-row column edges use one negative orientation and increments in the inherited [0.10,0.60] degree interval", "every observed wraparound edge satisfies the same interval; a never-observed wraparound is identified as unobservable only when at least one endpoint is structurally empty for the whole sequence", "observable-edge support is fully reported but is not confused with ray validity"],
        "per_sequence_identity_and_geometry_checks": ["fixed mapping is bijective", "XYZ-only occupancy and 0-or-1 published-return cardinality hold", "raw-to-canonical-to-raw count, mask, and identity round trip is exact", "E14 scalar range and decoded-direction numerical limits hold", "E14 angular and normalized Cartesian projection limits hold"],
        "evidence_binding": ["independently reread both raw sequences twice for topology, occupancy, mapping, and identity", "bind and re-evaluate the E14 per-sequence frozen summaries and artifact hash", "produce separate per-sequence verdicts before the combined verdict"],
        "pass_definition": "Both sequences independently pass every applicable check with the same fixed model and mapping, and both complete runs reproduce exactly.",
        "interpretation_limit": "PASS qualifies the canonical ray interface only for the allowed normal sequences 206 and 201. It does not access or qualify public anomaly validation or hidden test sequences and does not identify factory metadata.",
        "labels_read": false,
        "criteria_frozen_before_formal_run": true
      },
      "multi_sequence_ray_qualification_v2": {
        "version": "E15-v2",
        "amends": "E15-v1 FAIL remains permanent; v2 changes only the legal classification of a wraparound edge whose two endpoint returns never co-occur",
        "question": "Do the unchanged E08-E14 canonical-ray rules hold on normal train/206 and independent normal train/201 when observable continuity is separated from absence of co-observation?",
        "data": ["train/206 frames 0 through 448", "train/201 frames 4 through 681"],
        "unchanged_model": "E11-D4b even-frame geometry plus E11-v3 fixed c=(a-s_b) modulo 1024 mapping",
        "wraparound_evidence_states": {
          "observed_continuous": "the two endpoint returns co-occur in at least one frame and every actually observed circular increment uses the single negative orientation and lies in the inherited [0.10,0.60] degree magnitude interval",
          "observed_discontinuous": "the endpoints co-occur in at least one frame and any actually observed circular increment violates the inherited orientation or interval; this directly fails E15-v2",
          "unidentifiable_from_observed_returns": "the endpoint returns never co-occur in the audited sequence, including either a structurally empty endpoint or endpoints that are separately observed but never together"
        },
        "unidentifiable_reporting": ["beam ID", "column 1023 observed-frame count", "column 0 observed-frame count", "co-observed-frame count", "reason subtype: structurally_empty_endpoint or separately_observed_never_coobserved"],
        "unchanged_pass_conditions": ["both sequence layouts are finite float32 128x1024x4", "all 128 rows exist and every frame/beam has at least one real return", "the fixed mapping is bijective", "XYZ-only occupancy and 0-or-1 published-return cardinality hold", "E13 count, mask, and identity round trip remains exact", "E14 numerical and calibrated geometry limits remain satisfied", "all actually observed internal adjacent edges are continuous", "all actually observed wraparound edges are continuous", "no wraparound edge is observed_discontinuous", "every unidentifiable edge is reported without interpolation, fabrication, or cross-beam substitution", "two independent complete runs reproduce every array, classification, hash, and summary exactly"],
        "pass_conclusion": "Canonical ray identity, internal azimuth adjacency, and every observable wraparound edge are stable on train/206 and independent train/201; wraparound edges lacking endpoint co-observation remain model-defined and are not directly validated by returns.",
        "forbidden_conclusion": "All 128 wraparound edges were directly validated in both sequences.",
        "labels_read": false,
        "criteria_frozen_before_formal_run": true
      },
      "empty_ray_direction": "the fixed E11-v3 self-calibrated Ouster-like ray direction; no return is interpolated or fabricated"
    },
    "normal_controls": {
      "source_sequence_id": 206,
      "semantic_ids": [10, 11, 15, 18, 20, 30, 31, 32],
      "template_surface": "closed convex hull of one observed labeled instance scan",
      "scale_range": [0.9, 1.1],
      "vehicle_support_semantics": [40, 44],
      "person_rider_support_semantics": [40, 48, 60]
    },
    "anomaly_proxies": {
      "count_range": [0, 9], "primitive_count_range": [1, 5],
      "size_m_range": [0.2, 3.0],
      "training_mechanisms": ["superquadric", "CSG", "bend", "twist", "taper", "low_frequency_surface"],
      "held_out_mechanism": "torus_SDF",
      "primitive_finite_bounded_audit": {
        "version": "E16-v1",
        "question": "Does the deterministic training anomaly generator produce finite, closed, connected, bounded single-superquadric entities across a fixed broad seed audit?",
        "seeds": [0, 1023],
        "primitive_count": 1,
        "size_m_range": [0.2, 3.0],
        "geometry_report_resolution": 31,
        "sdf_audit_grid": "9x9x9 Cartesian grid spanning each shape's conservative bound",
        "pass_conditions": ["all 1024 deterministic seeds return a shape without rejection exhaustion", "every parameter and conservative bound radius is finite", "every sampled shape has exactly one primitive and its first operation is union", "every SDF audit value is finite", "every geometry report is bounded and closed with exactly one connected component", "reported occupied bounds are finite, ordered, and contained strictly inside the conservative bound", "the audited occupied maximum axis diameter remains in the generator's frozen [0.2,3.0] metre interval", "two independent complete runs reproduce every parameter digest, statistic, and summary exactly"],
        "failure_tolerance": 0,
        "interpretation_limit": "E16 qualifies numerical finiteness and bounded single-entity output only. Ray intersection is E17; multi-primitive CSG and deformation stability are E18.",
        "criteria_frozen_before_formal_run": true
      },
      "continuous_size_estimator_qualification": {
        "version": "E16-v2a",
        "amends": "E16-v1 FAIL remains permanent; replace its fixed-resolution padded voxel diameter with a continuous implicit-surface AABB estimator before judging generator sizes",
        "size_definition": "maximum axis-aligned span max(upper-lower) of the continuous single-primitive implicit surface after twist, bend, taper, and low-frequency displacement",
        "estimator": "for every undeformed spherical direction solve the outer continuous implicit-surface root, map it through the analytic forward deformation, and optimize each signed Cartesian coordinate with deterministic scipy differential_evolution; add a fixed 1e-6 metre outward numerical margin",
        "mesh_resolution_role": "none; geometry_report resolution 31, 41, or any other voxel resolution is not an estimator input",
        "analytic_fixtures": ["radius-0.5 m sphere", "axis-aligned ellipsoid with half-axes (0.3,0.7,1.1) m", "the same ellipsoid at yaw 0.4 rad with analytic rotated AABB"],
        "analytic_bound_coordinate_absolute_tolerance_m": 5e-6,
        "deformed_fixture_seeds": [0, 5, 276, 559, 639, 987],
        "standard_optimizer": {"maximum_iterations": 80, "population_size": 10},
        "strict_optimizer": {"maximum_iterations": 160, "population_size": 15},
        "standard_strict_bound_coordinate_max_difference_m": 0.0001,
        "independent_surface_directions_per_deformed_fixture": 16384,
        "surface_containment_tolerance_m": 2e-6,
        "pass_conditions": ["all analytic fixture bounds match their closed-form continuous bounds", "all standard and strict estimates are finite and ordered", "every standard/strict coordinate difference is below the frozen convergence tolerance", "all independently evaluated continuous surface points lie within the strict bounds", "calling voxel geometry reports at resolutions 31 and 41 does not alter the estimator output", "two independent complete runs are bitwise identical"],
        "interpretation_limit": "E16-v2a qualifies the size estimator only; it does not decide whether the 1024 formal generator samples satisfy [0.2,3.0] m. That decision is E16-v2b.",
        "criteria_frozen_before_formal_run": true
      },
      "primitive_continuous_size_audit": {
        "version": "E16-v2b",
        "question": "Do the original 1024 deterministic single-primitive generator samples satisfy finite, bounded, closed, connected output and the frozen [0.2,3.0] metre size range under the E16-v2a qualified continuous estimator?",
        "generator": "unchanged ShapeSpec.sample(seed, primitive_count=1, size_m_range=(0.2,3.0))",
        "seeds": [0, 1023],
        "size_estimator": "E16-v2a qualified continuous_bounds with maximum_iterations=80, population_size=10, and 1e-6 metre outward margin",
        "size_definition": "maximum axis-aligned span max(upper-lower) of the continuous deformed implicit surface",
        "inherited_non_size_checks": ["generation succeeds without rejection exhaustion", "all parameters, conservative radius, and fixed 9x9x9-bound-grid SDF values are finite", "resolution-31 geometry report is bounded and closed with one connected component", "continuous bounds are finite and ordered"],
        "pass_conditions": ["all 1024 samples satisfy every inherited non-size check", "every continuous size lies in the inclusive frozen [0.2,3.0] metre range", "two independent complete runs reproduce every generated parameter, bound, size, classification, hash, and summary bitwise exactly"],
        "failure_tolerance": 0,
        "forbidden": ["changing generator parameters", "rescaling a failing sample", "using resolution-31 or resolution-41 reported diameter as size", "changing the [0.2,3.0] metre interval after observing results"],
        "interpretation_limit": "FAIL identifies a generator implementation mismatch with its frozen continuous-size range; it does not invalidate the AJAE scientific route or authorize silent distribution changes.",
        "criteria_frozen_before_formal_run": true
      },
      "continuous_size_acceptance_generator": {
        "version": "E16-v3",
        "amends": "E16-v2b FAIL remains permanent; generator schema 2 replaces the implicit schema-1 resolution-41 size acceptance for the single-primitive path with the E16-v2a qualified continuous-size acceptance",
        "question": "Does generator schema 2 deterministically produce 1024 accepted single primitives that satisfy every inherited finite/bounded/closed/connected condition and the frozen continuous [0.2,3.0] metre size range?",
        "generator_schema": 2,
        "superseded_generator_schema": 1,
        "seeds": [0, 1023],
        "primitive_count": 1,
        "proposal_parameter_distribution": "unchanged from E16-v2b",
        "candidate_limit_per_seed": 64,
        "acceptance_size_estimator": "E16-v2a qualified continuous_bounds with maximum_iterations=80, population_size=10, and 1e-6 metre outward margin",
        "acceptance_rule": "accept if and only if the final candidate continuous AABB maximum axis span lies in the inclusive [0.2,3.0] metre interval after all inherited construction and geometry checks pass",
        "resolution_41_role": "diagnostic or downstream numerical support only; it cannot accept or reject a single-primitive candidate",
        "inherited_non_size_checks": ["generation succeeds within 64 deterministic proposals", "all parameters, conservative radius, and fixed 9x9x9-bound-grid SDF values are finite", "resolution-31 geometry report is bounded and closed with one connected component", "continuous bounds are finite and ordered"],
        "pass_conditions": ["all 1024 seeds return an accepted single primitive", "every accepted continuous size lies in the inclusive frozen [0.2,3.0] metre interval", "size violation count is zero", "every inherited non-size check passes", "two independent complete runs reproduce every accepted parameter, continuous bound, size, proposal statistic, classification, hash, and summary bitwise exactly"],
        "required_efficiency_diagnostics": ["proposal count per seed", "total and per-proposal rejection rate", "too-small rejection count", "too-large rejection count", "other construction or geometry rejection count", "maximum proposal count"],
        "efficiency_interpretation": "efficiency diagnostics do not change the E16-v3 correctness verdict; extreme rejection motivates a separately preregistered proposal-efficiency experiment rather than an unregistered proposal-distribution change",
        "scope_limit": "E16-v3 qualifies only primitive_count=1. Multi-primitive CSG continuous sizing and acceptance remain unqualified and locked for E18.",
        "generator_identity": "schema 2 must enter the experiment manifest and the renderer/generator cache identity together with the render.py source hash",
        "forbidden": ["seed-specific rejection", "changing the proposal parameter distribution", "shrinking the proposal size range after observing E16-v2b", "using resolution-31 or resolution-41 diameter for acceptance", "changing the continuous [0.2,3.0] metre interval"],
        "failure_tolerance": 0,
        "criteria_frozen_before_formal_run": true
      },
      "single_primitive_intersection_audit": {
        "version": "E17",
        "question": "Does the authoritative ShapeSpec.intersect default path recover the nearest positive analytic intersection and outward normal for basic single quadrics while rejecting analytic misses?",
        "fixtures": ["sphere radius 0.1 m", "sphere radius 1.5 m", "ellipsoid half-axes (0.1,0.3,0.5) m at yaw 0.4 rad", "ellipsoid half-axes (0.3,0.7,1.5) m at yaw -0.8 rad"],
        "fixture_constraints": "one centered union primitive, exponents (1,1), no twist/bend/taper/surface displacement",
        "rays_per_fixture": {"outside_to_inside": 2048, "inside_to_outside": 512, "outward_analytic_miss": 1024},
        "ray_construction": "deterministic Fibonacci directions; outside hit rays aim through points at normalized ellipsoid radius 0.95, inside rays start at normalized radius 0.4, and miss rays point radially outward; direction magnitudes vary deterministically and the analytic oracle normalizes them exactly as ShapeSpec.intersect does",
        "tangent_scope": "no exact tangent ray is used; every registered hit passes through a strictly interior point and therefore has an unambiguous positive crossing",
        "implementation_path": "ShapeSpec.intersect with its authoritative default steps=96",
        "analytic_oracle": "closed-form quadratic roots of the rotated ellipsoid matrix; choose the nearest positive root for outside origins and the positive exit root for inside origins",
        "pass_conditions": ["zero hit/miss classification mismatch", "every hit distance is finite and positive and every miss distance is +infinity", "maximum absolute distance error is below 1e-5 m", "maximum absolute implicit surface residual at returned hit points is below 1e-5 m", "every hit normal is finite, unit length, outward-facing, and within 0.01 degree of the analytic ellipsoid normal", "every miss normal is exactly zero", "two independent complete runs reproduce every distance, normal, validity mask, statistic, hash, and summary bitwise exactly"],
        "failure_tolerance": 0,
        "interpretation_limit": "E17 qualifies basic single-quadratic primitive intersection only. CSG, non-quadratic exponents, and twist/bend/taper/surface deformation remain E18.",
        "criteria_frozen_before_formal_run": true
      },
      "csg_continuous_size_certificate_qualification": {
        "version": "E18a-A",
        "question": "Can final multi-primitive CSG geometry receive a deterministic mesh-resolution-independent conservative size certificate whose lower and upper bounds enclose the true continuous maximum-axis span?",
        "certificate_definition": "L is the largest continuous axis-line boundary chord found from verified interior Sobol witnesses and U is the maximum axis span of an analytic outer AABB propagated through primitive level-set expansion, sequential CSG, twist, taper, and bend; certify L <= D_continuous <= U",
        "outer_bound": "expand each primitive level set by the maximum surface displacement, propagate AABBs by envelope for union, retain the left bound for difference, intersect bounds for intersection, then analytically propagate the global deformation and add a fixed 1e-6 metre outward margin",
        "lower_witness": "draw deterministic nested Sobol points in the analytic outer AABB, retain verified interior points in sequence order, and solve continuous signed-distance roots from each witness toward both AABB faces along every Cartesian axis; every chord endpoint must independently satisfy the continuous surface equation",
        "standard_search": {"sobol_probes": 4096, "maximum_interior_lines": 64},
        "strict_search": {"sobol_probes": 32768, "maximum_interior_lines": 256},
        "analytic_fixtures": ["union of radius-1 spheres centered at x=-0.5 and x=0.5", "intersection of radius-1 spheres centered at x=-0.5 and x=0.5", "radius-1 sphere minus an internal radius-0.35 sphere centered at x=0.2"],
        "deformed_generator_fixtures": [{"seed": 0, "primitive_count": 2}, {"seed": 1, "primitive_count": 3}, {"seed": 2, "primitive_count": 4}, {"seed": 3, "primitive_count": 5}],
        "independent_containment_probes_per_fixture": 131072,
        "pass_conditions": ["all bounds and witness points are finite and ordered", "every analytic exact AABB is contained by the certified outer AABB within 2e-6 m", "every analytic exact maximum-axis size lies in the reported [L,U] interval", "every analytic certificate width U-L is below 0.35 m", "every returned chord endpoint has absolute continuous signed-distance residual below 1e-8 m", "the strict lower certificate is not smaller than the nested standard lower certificate", "standard and strict outer bounds are exactly equal", "no independently evaluated inside point lies outside the analytic outer AABB", "calling voxel geometry reports at resolutions 31 and 41 does not alter either certificate", "two independent complete runs reproduce every bound, witness, statistic, hash, and summary bitwise exactly"],
        "mesh_resolution_role": "none in the certificate; voxel reports are invoked only for the frozen invariance check",
        "interpretation_limit": "E18a-A qualifies the continuous lower witness and original conservative outer certificate. Schema 4 retains its lower witness but uses the independently qualified E18a-D2-v2 tight conservative upper bound for multi-primitive acceptance.",
        "criteria_frozen_before_formal_run": true
      },
      "csg_continuous_acceptance_generator": {
        "version": "E18a-B-v2",
        "question": "Does generator schema 4 preserve the qualified single-primitive path and use the E18a-D2-v2 tight conservative upper bound only for multi-primitive acceptance while satisfying the original correctness and efficiency criteria?",
        "generator_schema": 4,
        "superseded_generator_schema": 3,
        "single_primitive_acceptance": "unchanged E16-v3 qualified continuous_bounds; its qualified continuous size must lie in [0.2,3.0] m",
        "multi_primitive_acceptance": "retain the E18a-A standard certificate boundary-chord lower_size_m, replace only its outer upper bound with the E18a-D2-v2 fixed-256-slab tight conservative AABB, and accept only if lower_size_m >= requested minimum and tight_upper_size_m <= requested maximum",
        "proposal_parameter_distribution": "unchanged from schema 3",
        "candidate_limit_per_seed": 64,
        "audit_groups": [{"name": "default-training-path", "primitive_count": "sampled 1 through 5 by the unchanged proposal stream", "seeds": [0,1023]}, {"name": "fixed-count-2", "primitive_count": 2, "seeds": [0,255]}, {"name": "fixed-count-3", "primitive_count": 3, "seeds": [0,255]}, {"name": "fixed-count-4", "primitive_count": 4, "seeds": [0,255]}, {"name": "fixed-count-5", "primitive_count": 5, "seeds": [0,255]}],
        "pass_conditions": ["all 2048 audited calls return an accepted geometry within 64 proposals", "every accepted single primitive satisfies its unchanged E16-v3 qualified continuous-size interval", "every accepted multi primitive has the E18a-A boundary-chord lower bound at or above the requested minimum and the E18a-D2-v2 tight conservative upper bound at or below the requested maximum", "every accepted shape has finite parameters, conservative radius, and fixed 9x9x9 SDF values and passes resolution-31/41 bounded/closed/single-component checks", "recomputing the qualified lower witness and tight upper bound exactly reproduces the generation report", "two independent complete runs reproduce every accepted parameter, certificate, proposal statistic, classification, hash, and summary bitwise exactly"],
        "efficiency_pass_conditions": ["overall rejection rate per proposal is below 0.50", "99th percentile proposal count is at most 8", "maximum proposal count is at most 64"],
        "required_efficiency_diagnostics": ["proposal count per audit item", "overall and per-group rejection rate", "lower-certificate rejection count", "upper-certificate rejection count", "other construction or geometry rejection count", "proposal-count quantiles and maximum"],
        "generator_identity": "schema 4 must enter the experiment manifest and renderer/generator cache identity together with the render.py source hash",
        "forbidden": ["seed-specific rejection", "changing the proposal parameter distribution", "shrinking the requested size range", "using resolution-31 or resolution-41 diameter for acceptance", "treating a conservative certificate rejection as proof that the actual geometry is invalid"],
        "failure_tolerance": 0,
        "criteria_frozen_before_formal_run": true
      }
    },
    "world_types": {"pure_normal": 0.2, "control_only": 0.2, "mixed": 0.4, "anomaly_only": 0.2},
    "common_entity_rules": {
      "static_world_pose": true, "ground_semantic_ids": [40, 44, 48, 49, 60],
      "support_plane": true, "observed_surface_collision_rejection": true,
      "inserted_entity_collision_rejection": true, "tie_tolerance_m": 0.000001
    },
    "sensor_model": {
      "conditioning": ["beam", "range", "incidence", "material"],
      "return_probability": true, "intensity_quantiles": true,
      "empty_surface_opportunity": "same-beam bracketing-return interpolation when adjacent ranges agree",
      "range_bin_edges_m": [0.0, 10.0, 20.0, 30.0, 40.0, 50.0, 120.0],
      "incidence_bin_edges_rad": [0.0, 0.5235987755982988, 1.0471975511965976, 1.5707963267948966],
      "low_count_backoff": "pooled across beam at the same range-incidence cell",
      "nearest_accepted_return": true, "allow_new_return_on_empty_ray": true,
      "bidirectional_occlusion": true
    },
    "world_rule": "fix every entity, source template, placement, material and seed before rendering any frame",
    "physical_scope_excludes": ["within-scan motion distortion", "beam divergence", "finite footprint", "multiple returns", "multipath", "all electronic noise", "unobserved hidden surfaces"]
  },
  "window": {
    "offline_main": {"frames": 5, "frame_offsets": [-2, -1, 0, 1, 2], "coordinate_reference_offset": 0, "output_offsets": [-2, -1, 0, 1, 2]},
    "causal_ablation": {"frames": 5, "frame_offsets": [-4, -3, -2, -1, 0], "coordinate_reference_offset": 0, "output_offsets": [0]},
    "padding": false, "frame_repetition": false,
    "point_identity": ["frame_id", "beam_id", "azimuth_column"],
    "file_slot_is_not_identity": true, "center_frame_weighting": "none"
  },
  "model": {
    "name": "AJAE four-level time-stratified spatiotemporal point transformer",
    "stu_feature_dim": 128, "normal_evidence_dim": 19,
    "assignment_reliability_dim": 1, "no_object_reliability_dim": 1,
    "input_intensity_dim": 1, "input_dim": 150, "hidden_dim": 128,
    "levels": 4, "level_names": ["L0_points", "L1_fine", "L2_part", "L3_local_scene"],
    "voxel_sizes_m": [0.1, 0.2, 0.4], "attention_deltas": [-2, -1, 0, 1, 2],
    "attention_radii_m": [[0.45, 0.35, 0.25, 0.35, 0.45], [0.9, 0.7, 0.5, 0.7, 0.9], [1.8, 1.4, 1.0, 1.4, 1.8], [3.6, 2.8, 2.0, 2.8, 3.6]],
    "neighbors": [[6, 8, 12, 8, 6], [8, 12, 16, 12, 8], [12, 16, 24, 16, 12], [16, 24, 32, 24, 16]],
    "heads": 4, "pooling": "per_time_mean_max", "same_time_residual": true,
    "cross_time_gate": "sigmoid_zero_when_neighborhood_empty",
    "upsample": "same_time_3NN_with_high_resolution_skip", "upsample_neighbors": 3,
    "output": "one anomaly logit for every input return"
  },
  "training": {
    "source_partition": "train", "source_sequence_id": 206, "seeds": [0, 1, 2],
    "learning_rate": 0.0001, "weight_decay": 0.0001, "maximum_worlds": null,
    "micro_batch": 1, "gradient_accumulation": 8, "worlds_per_evaluation": 5,
    "patience": 8, "chunk_centers": 16, "cache_frames": 7,
    "window_coverage": "every legal window exactly once per complete world",
    "loss": "range-filtered empty-class-safe class-balanced binary cross entropy only",
    "world_type_probabilities": {"pure_normal": 0.2, "control_only": 0.2, "mixed": 0.4, "anomaly_only": 0.2},
    "output_dir": "runs/ajae"
  },
  "development": {
    "worlds_file": "dev.json", "sequence_id": 201,
    "in_generator_worlds": 24, "generator_held_out_worlds": 6,
    "held_out_affects_selection": false, "pure_normal_is_separate": true,
    "fixed_world_evaluation": {
      "status": "unresolved_requires_owner_decision",
      "scope": null
    },
    "checkpoint_selection": {
      "status": "proposed_requires_owner_confirmation",
      "primary": "maximum macro mean of per-world AP over the 24 in-generator worlds",
      "tie_tolerance": 0.000001,
      "first_tie_break": "lower pure-normal score q99.9",
      "second_tie_break": "earlier completed world index",
      "held_out_input_forbidden": true
    },
    "difficulty_statistics": {
      "Nvis": "median accepted visible returns per entity over its visible frames",
      "O": "one minus visible inserted returns divided by accepted object returns before nearest-return occlusion",
      "d": "median range of accepted visible entity returns",
      "V": "number of frames in a five-frame window with at least one accepted entity return"
    },
    "boundary_leakage_radius_m": 0.5,
    "position_score_scale": ["standard_deviation", "q05", "q50", "q95"]
  },
  "experiments": {
    "B0": "frozen STU single-frame official MaxLogit",
    "B1": "single-frame AJAE point model with the common counterfactual supervision",
    "B2": "five-frame AJAE with every nonzero-delta edge disabled; evaluate q=0 only",
    "B3": "full center-symmetric five-frame AJAE; evaluate q=0 only",
    "B4": "the exact B3 checkpoint with equal probability fusion over every valid window occurrence",
    "B5": "causal five-frame AJAE [t-4,t]; evaluate current frame only"
  },
  "evaluation": {
    "minimum_range_m": 2.5, "maximum_range_m": 50.0,
    "minimum_range_inclusive": true, "maximum_range_inclusive": true,
    "minimum_anomaly_points": 5, "point_metrics": ["AP", "AUROC", "FPR95"],
    "comparison_frame_domain": {
      "status": "proposed_requires_owner_confirmation",
      "rule": "intersection_of_complete_centered_q0_and_complete_causal_current_frames",
      "required_source_offsets": [-4, -3, -2, -1, 0, 1, 2],
      "applies_to": ["B0", "B1", "B2", "B3", "B4", "B5"],
      "padding_or_zero_fill_forbidden": true,
      "coverage_manifest_required": true
    },
    "score_fusion": "equal_mean_of_probabilities_by_frame_and_canonical_ray",
    "zero_coordinate_slot_score": 0.0, "normal_point_alarm_rate": 0.001,
    "threshold_comparison": "score_strictly_greater_than_threshold",
    "instance_construction": "per_frame_3D_DBSCAN_without_temporal_linking",
    "dbscan_eps_m": 1.0, "dbscan_min_samples": 1,
    "object_metrics": ["RecallQ", "SQ", "RQ", "UQ", "PQ", "TP", "FP", "FN"],
    "uncertainty_units": ["training_seed", "development_world", "real_anomaly_sequence"],
    "cost_reports": ["latency", "peak_memory", "throughput", "STU_cache_status", "temporal_input_budget"]
  },
  "decision_gates": {
    "gate1": ["ray_slot_audit", "range_image_round_trip", "render_source_leakage", "beam_range_intensity"],
    "gate2": ["B1_vs_B0", "pure_normal_safety", "normal_control_safety"],
    "gate3": ["three_seed_B3_vs_B1", "three_seed_B3_vs_B2", "B4_vs_same_checkpoint_B3"],
    "gate4": ["frozen_public_19_vs_B0_B1", "moving_normal_safety"],
    "criteria": {
      "status": "unresolved_requires_owner_decision",
      "gate1": null,
      "gate2": null,
      "gate3": null,
      "gate4": null,
      "development_difficulty_coverage": null
    },
    "verdict_rule": "a gate verdict requires its real statistics and an explicit predeclared scientific judgment; artifact existence alone is never evidence"
  },
  "status": {"implementation_target": "schema30", "scientific_route": "conditional_continue", "performance_claims_available": false}
}
