# AJAE 数据协议复现说明

当前活动协议是 schema 34。科学定义见 `PROTOCOL.md`，机器可检查合同见 `protocol.json`。

当前正式数据池已生成并通过完整窗口检查，协议状态为 `frozen`：206 训练池包含 128 个单代理世界、3080 个窗口；201 合成验证池包含 92 个单代理世界、2360 个窗口；原始完整 201 另有 678 个在线窗口。完整检查记录见 `artifacts/data/qualification.json`。这说明数据实现通过本协议检查，不代表模型效果已经得到验证。

训练池和合成验证池的 5440 个真值着色 PLY 曾完整导出，共 49,311,794,379 字节（45.93 GiB）。用户完成可视化检查后，已按要求删除 `artifacts/ply/` 中全部 5455 个可视化文件，包括另行授权的 15 个真实异常窗口；冻结数据池和逐点预测未删除，下面保留重新导出方法。220 个正式世界在末帧均有可见异常回波；训练池仍有 20 个、合成验证池仍有 10 个全窗口无异常回波的样本，均按规则保留。末帧物理可见不等于进入官方 2.5–50 米评价范围，资格记录分别保存两种计数。

`vendor/stu/` 保存官方点级评价源码和许可证。为直接导入评价类，只将原始数据命令行工具的 `utils.common` 导入移至 `main` 内，点筛选和指标公式不变；模型训练和最终评价的完成情况应以真实运行结果为准。

最近完成的是19条公开真实验证序列的全量观测画像，见[统计执行说明与结果](STU19_统计执行说明.md)、[CSV目录说明](STU19_数据画像/说明.csv)和[31项要素总表](STU19_数据画像/要素总表.csv)。按用户最新要求，交付改为20个CSV文件，数值、分母和缺失说明保留，公式转换为已复算数值。本轮读取全部8,659帧，完整五帧因素使用8,583窗，76个启动窗口单列；没有新增模型前向、训练或合成生成。此前FullTrain-v1仍以第七轮候选结束，首次真实评价及强度置换结论不因这份数据统计而改写。

画像的31项要素均有统计或明确的不可观测／可靠性说明。实际回波共987,807,187点，其中异常90,739点、忽略243,679,266点；四态帧数及原合格点数与已有报告一致。完整五帧中整窗无异常为4,355/8,583，即50.7398%，不能用当前帧无异常比例替代。32种可见模式均有覆盖，00001为97窗、11111为2,856窗。4,729条实例×帧记录中只有1,611条满足可见形状支持规则；`val/149`第232帧的1个零实例标识异常点保留点级统计而不强行分组。

全量异常强度中位数按点、帧和序列等权分别为0.171714、0.117714和0.224857。原始异常距离按点合并的中位数为6.761米，而3,545个有异常帧的各帧距离中位数再取中位数为23.788米。这些差异要求后续覆盖比较保留不同分母。五帧联合异常占据体素96.4044%为纯异常，历史新增正常混合影响1,633/75,412个当前异常占据体素；这些只是输入分布，尚未识别预测失败的因果因素。

唯一入口为`python -m src.profile --data-root /absolute/path/to/STU --workers 12`，CSV默认保存于`STU19_数据画像/`，可用`--tables`指定目录。统计使用原`STUSequence`和`joint_voxelize`，完整保留正常、异常、忽略及范围外成员；`src/profile_report.py`完成分布汇总和CSV导出。原始强度精确合并取值计数；大量残差分位数附宽度至多0.1毫米的区间；静态表面残差明确为固定抽样代理。依赖、规则、覆盖、6项必要检查、原始帧独立复算及资源记录均在执行说明中。正式原始扫描耗时486.842秒；随后报告补充只复用已有统计。`runs/profile_v1/`保存逐帧、逐窗口、逐实例、连续阶段、统计长表和三页字体已核验的结果图，沿用本地忽略规则。

本轮在真实画像完成后结束。下一步才按相同定义统计206训练池与201合成验证池，确认覆盖差异，再决定生成或采样因素；本轮没有调整生成参数、训练配比或模型。

## 原始数据

STU 原始数据不随仓库分发。先按 `protocol.json` 的 `data.official_archive_sha256` 核对官方压缩包，再整理为：

```text
STU/
├── train/201/{velodyne,labels,poses.txt,calib.txt}
├── train/206/{velodyne,labels,poses.txt,calib.txt}
├── val/<sequence>/{velodyne,labels,poses.txt,calib.txt}
└── test/<sequence>/{velodyne,poses.txt,calib.txt}
```

数据池生成与资格检查只读取 `train/206` 和 `train/201`。真实 `val` 现已获授权作为开发验证集，首次只评价第七轮固定候选；具体规则见 `PROTOCOL.md`。此前 `val/125` 第 0–18 帧有限目视检查属于历史记录。隐藏 `test` 序列不参与本轮。

## 代码检查

数据链路使用 NumPy、SciPy 和 PyTorch（读取传感器标定文件），检查使用 pytest 和 Ruff。安装所需依赖后，在仓库根目录运行：

```bash
python src/protocol.py
python -m pytest -q
ruff check src tests
```

协议摘要应显示 schema 34、206 训练源、201 完整验证源、3080 个合成训练窗口和 2360 个合成验证窗口。正式池尚未完成时，状态必须保持 `qualification_pending`，`training_allowed` 必须为 `false`。

## 正式训练数据入口

仓库保留下文的历史学习诊断，并新增 `AJAE-FullTrain-v1` 全池正式训练。正式训练的实际进度与停止原因读取 `runs/fulltrain_v1/summary.json`；运行期间逐步记录在 `metrics.jsonl`，不能由入口存在推断训练完成。数据入口实际执行冻结清单核验，不依赖调用者先手动运行检查命令：

```python
from pathlib import Path
from src.data import FrozenWindowDataset, WindowPartition
from src.protocol import load_protocol

protocol = load_protocol()
data_root = Path("/absolute/path/to/STU")
train = FrozenWindowDataset(data_root, protocol, pool_name="train")
validation = FrozenWindowDataset(data_root, protocol, pool_name="validation")
normal_validation = WindowPartition(validation.source_sequence, 4, 681)
```

每个构造函数在返回数据前核验原资格文件、两份清单及全部 220 个片段文件，任一文件缺失、哈希不符、身份或边界不符都会拒绝加载。数据集按索引返回完整 `SceneWindow`，窗口数量分别为 3080 和 2360；正常 201 保留全部 682 帧，在线窗口仍为 678 个。只有 `train.gradient_updates_allowed` 为真，验证数据不允许用于参数更新。

以下只读命令调用同一个训练数据构造函数，并实际加载所选池的首末窗口；它不启动训练：

```bash
python src/data.py check --pool train --data-root /absolute/path/to/STU
python src/data.py check --pool validation --data-root /absolute/path/to/STU
```

预测写入使用 `PredictionBatch.from_window(window, scores).save(path, window=window)`；读取使用 `PredictionBatch.load(path, window=window, expected_sha256=file_sha256)`。`scores` 必须已恢复为原输入窗口的全部点顺序；保存、读取均核对实际窗口，已有文件不覆盖。

## 首次真实开发验证

`AJAE-FullTrain-v1` 已结束；第七轮按完整合成 201 选模规则成为当前候选。公开真实 `val` 用于开发反馈，隐藏 `test` 不参与模型选择。固定模型、启动例外和评价范围见 `PROTOCOL.md`。同一个评价入口先在 206／201 检查启动和完整五帧等价性，再按每条真实序列从第 0 帧滑动至结尾：

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -u -m src.evaluate --real \
  --data-root /absolute/path/to/STU --output runs/real_val_v1
```

`startup/checks.json` 记录实际启动核验，`inventory.json` 记录原始数据规模和空间估计，`samples.json` 固定本次候选和全部时间窗口。`results.jsonl` 逐帧保留官方资格、正常及异常分数、全窗口预测路径和精确指标记录。`summary.json` 汇总全部原始帧、完整五帧区间、每条序列和无异常回波阶段；必须实际完成后才能解释为完整真实结果。

首次真实验证已经完整执行并通过独立核验。固定第七轮候选的全帧 AP 为 **3.475451%**，AUROC 为 **88.248106%**，FPR95 为 **57.674281%**。这些结果显示，该候选在这 19 条真实开发验证序列上的异常区分和高召回表现较弱；合成 201 的 AP 97.161646% 不能支持真实异常表现可靠。两套数据的异常比例和场景不同，不能把两个 AP 的差直接解释为同分布配对效果或某个机制的损失。

本次共读取 8,659 个原始扫描、1,134,952,448 个文件槽位，其中 987,807,187 个为实际可见点。连续滑动推理保存了 4,917,038,560 个窗口内逐点分数。模型只在序列开始时进入启动阶段，没有按合成片段、异常出现或消失的位置重新启动。参数、归一化缓冲区与检查点身份在前后保持不变，优化器更新次数为零。

AP 表示综合异常排序精确率与召回率的平均精确率；AUROC 描述正常点与异常点的排序区分能力；FPR95 使用官方严格召回率超过 95% 时的正常点误报比例。下表单位均为百分数。指标按 2.5–50 米、忽略语义 0、范围内异常点至少 5 个的官方规则合并全部合格点计算，不平均逐帧或逐序列 AP。官方评价类与本轮读取的 [STU 官方评价源码](https://github.com/kumuji/stu_dataset/blob/main/compute_point_level_ood.py) 程序结构一致。公开验证集的开发用途与 [STU 官方说明](https://github.com/kumuji/stu_dataset) 一致；隐藏测试集不参与本轮。

| 范围 | 原始帧数 | 合格帧数 | AP | AUROC | FPR95 | 正常点数 | 异常点数 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 全帧主结果 | 8,659 | 1,960 | 3.475451 | 88.248106 | 57.674281 | 193,792,470 | 87,499 |
| 第 4 帧及以后补充结果 | 8,583 | 1,956 | 3.482498 | 88.270711 | 57.637700 | 193,383,258 | 87,398 |
| 第 0–3 帧启动区间 | 76 | 4 | 0.076625 | 74.590666 | 76.210131 | 409,212 | 101 |

76 个启动帧中，只有序列 125 的 4 帧符合官方异常帧资格。去掉启动区间后，合并 AP 仅增加 0.007047 个百分点，AUROC 增加 0.022606 个百分点，FPR95 降低 0.036581 个百分点。完整五帧区间仍然较弱，当前整体低分不能由前四帧解释。两组指标来自不同点集，这种差异只是启动区间影响的描述，不是启动策略的因果对照。

19 条序列的主结果如下。最后一列仅统计当前帧无任何可见异常回波时、官方距离和语义范围内正常点的固定 0.5 高分比例；它与使用另一工作阈值的 FPR95 是不同统计。

| 序列 | 全部帧 | 合格帧 | AP | AUROC | FPR95 | 无异常阶段误报率 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 125 | 255 | 210 | 0.445640 | 63.072510 | 83.463943 | 0.009630% |
| 137 | 560 | 46 | 0.348429 | 88.100049 | 54.098216 | 0.546117% |
| 138 | 516 | 83 | 2.454386 | 97.024581 | 18.251881 | 0.517146% |
| 139 | 542 | 99 | 0.742275 | 93.747139 | 29.846843 | 0.699788% |
| 140 | 481 | 75 | 3.161783 | 98.003382 | 12.501837 | 0.444619% |
| 141 | 697 | 307 | 29.345866 | 94.619240 | 34.975777 | 0.721578% |
| 142 | 537 | 87 | 0.106870 | 82.097436 | 62.550573 | 0.724108% |
| 143 | 469 | 91 | 1.425375 | 95.566191 | 21.709415 | 1.121201% |
| 144 | 323 | 79 | 1.099023 | 93.389576 | 25.519496 | 0.930837% |
| 145 | 337 | 92 | 15.789474 | 99.303919 | 1.183632 | 1.010430% |
| 146 | 374 | 57 | 4.299734 | 98.960949 | 4.005686 | 0.651256% |
| 147 | 401 | 111 | 2.734264 | 97.838907 | 12.702122 | 0.620619% |
| 148 | 602 | 115 | 3.152725 | 97.596799 | 16.671325 | 0.077202% |
| 149 | 378 | 102 | 3.751301 | 97.336044 | 17.364916 | 0.192339% |
| 150 | 505 | 156 | 4.602514 | 96.144303 | 25.656795 | 0.184627% |
| 151 | 277 | 51 | 17.531583 | 99.760524 | 0.515327 | 0.183290% |
| 152 | 534 | 61 | 1.258974 | 96.634292 | 20.716571 | 0.385363% |
| 153 | 271 | 75 | 9.557652 | 97.514320 | 16.060897 | 0.983501% |
| 169 | 600 | 63 | 0.113040 | 69.465505 | 80.350765 | 0.214770% |

其余 18 条序列的完整五帧区间 AP、AUROC、FPR95 与全帧结果逐值相同。序列 125 的完整五帧区间为 251 帧，其中 206 帧合格，正常点 21,041,670、异常点 25,770，AP 0.472982%、AUROC 63.248899%、FPR95 83.297904%。

逐序列 AP 的中位数为 2.734264%。19 条中有 16 条 AP 低于 10%，5 条低于 1%；较高的序列 141、151、145 分别为 29.345866%、17.531583%、15.789474%。这说明真实表现存在明显的序列差异，不能概括为所有异常都完全没有信号，也不能以少数较好序列代替合并主结果。

全部 5,114 个无异常回波帧中，正常评价点共有 366,706,288 个，2,021,812 个分数不低于 0.5，误报率为 **0.551344%**。正常分数中位数为 2.2250211e-08，第 95 百分位为 7.1773002e-06；低分位数与高分尾部同时存在，不能用中位数很低代替误报分析。

| 正常阶段范围 | 帧数 | 正常点数 | 中位数 | 第 95 百分位 | 不低于 0.5 的点数 | 高分比例 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 启动区间 | 72 | 2,493,914 | 1.0821348e-07 | 0.00035209616 | 76,999 | 3.087476% |
| 完整五帧区间 | 5042 | 364,212,374 | 2.1990104e-08 | 7.0506821e-06 | 1,944,813 | 0.533978% |

启动区间正常误报率为 3.087476%，完整五帧区间为 0.533978%。序列 153 第 0 帧有 13,792／84,773 个正常评价点高分，比例为 16.269331%；序列 143 第 0 帧为 10,876／78,746，即 13.811495%。完整五帧区间也存在误报：序列 137 第 79 帧为 991／8,879，即 11.161167%。因此，启动输入条件变化需要披露，但正常误报并不限于启动阶段。

| 序列 | 无异常帧数 | 正常点数 | 正常分数中位数 | 第 95 百分位 | 高分点数 | 高分比例 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 125 | 44 | 3,530,697 | 3.0864793e-08 | 6.5623794e-06 | 340 | 0.009630% |
| 137 | 410 | 24,117,546 | 1.6736402e-08 | 5.8867131e-06 | 131,710 | 0.546117% |
| 138 | 361 | 22,510,877 | 1.465589e-08 | 5.6301142e-06 | 116,414 | 0.517146% |
| 139 | 337 | 20,015,503 | 1.8244561e-08 | 7.3320512e-06 | 140,066 | 0.699788% |
| 140 | 305 | 24,735,110 | 2.2937293e-08 | 6.7544324e-06 | 109,977 | 0.444619% |
| 141 | 316 | 21,173,314 | 1.7834099e-08 | 6.9892835e-06 | 152,782 | 0.721578% |
| 142 | 377 | 28,586,740 | 2.5145601e-08 | 8.2515198e-06 | 206,999 | 0.724108% |
| 143 | 295 | 24,552,066 | 1.0265346e-08 | 6.3731313e-06 | 275,278 | 1.121201% |
| 144 | 167 | 12,660,427 | 1.5786039e-08 | 6.9710577e-06 | 117,848 | 0.930837% |
| 145 | 184 | 17,274,527 | 1.298633e-08 | 7.2107264e-06 | 174,547 | 1.010430% |
| 146 | 262 | 15,298,746 | 1.5460317e-08 | 6.490231e-06 | 99,634 | 0.651256% |
| 147 | 197 | 15,802,286 | 1.1571671e-08 | 5.3684089e-06 | 98,072 | 0.620619% |
| 148 | 368 | 16,011,341 | 1.4988686e-08 | 3.8306248e-06 | 12,361 | 0.077202% |
| 149 | 170 | 12,962,522 | 1.510754e-08 | 4.4838689e-06 | 24,932 | 0.192339% |
| 150 | 238 | 13,753,163 | 1.4378672e-08 | 4.7175477e-06 | 25,392 | 0.184627% |
| 151 | 152 | 13,825,074 | 1.274113e-08 | 4.8679021e-06 | 25,340 | 0.183290% |
| 152 | 336 | 23,137,386 | 1.6188375e-08 | 5.3531612e-06 | 89,163 | 0.385363% |
| 153 | 136 | 12,885,600 | 1.1192856e-08 | 6.6259577e-06 | 126,730 | 0.983501% |
| 169 | 459 | 43,873,363 | 1.5534015e-07 | 1.7202938e-05 | 94,227 | 0.214770% |

异常尚未出现时也有持续误报。例如，序列 143 首次物理可见异常位于第 164 帧；此前连续 164 帧中的 14,531,736 个正常评价点已有 256,525 个高分，比例为 1.765274%。序列 137 首次异常前 230 帧为 103,642／13,530,954，即 0.765962%。这些时间分组由标签事后确定，模型执行并不以标签为条件。

共有 5,579 帧在官方范围内没有异常点，1,120 帧只有 1–4 个异常点，均不进入官方合并异常指标。少点帧共有 2,595 个范围内异常点，其中 1,113 帧在固定 0.5 阈值下没有检出任何异常点；1,960 个官方合格帧中，也有 1,310 帧出现同样的阈值下完全漏检。这里的漏检是固定工作阈值下的描述，不能替代 AP 等排序指标。

下表按每条序列的时间顺序记录连续失败位置。区间为包含两端的原始帧编号；少点指每帧范围内有 1–4 个异常点；合格帧指每帧至少 5 个。两个漏检区间都要求区间内各帧在固定 0.5 阈值下零异常检出。

| 序列 | 首次物理可见 | 首次进入评价范围 | 首个合格帧 | 最长连续少点漏检 | 最长连续合格帧完全漏检 |
| --- | ---: | ---: | ---: | --- | --- |
| 125 | 0 | 0 | 0 | 无 | 0–81（82 帧） |
| 137 | 230 | 263 | 325 | 301–324（24 帧） | 385–400（16 帧） |
| 138 | 255 | 294 | 326 | 444–457（14 帧） | 335–354（20 帧） |
| 139 | 187 | 273 | 297 | 273–296（24 帧） | 311–344（34 帧） |
| 140 | 164 | 218 | 263 | 236–262（27 帧） | 343–353（11 帧） |
| 141 | 195 | 261 | 261 | 623–640（18 帧） | 485–618（134 帧） |
| 142 | 221 | 249 | 263 | 392–401（10 帧） | 319–365（47 帧） |
| 143 | 164 | 214 | 225 | 355–382（28 帧） | 319–346（28 帧） |
| 144 | 116 | 206 | 241 | 206–240（35 帧） | 252–286（35 帧） |
| 145 | 130 | 211 | 236 | 211–235（25 帧） | 253–273（21 帧） |
| 146 | 245 | 276 | 306 | 282–305（24 帧） | 326–335（10 帧） |
| 147 | 120 | 189 | 225 | 200–224（25 帧） | 343–360（18 帧） |
| 148 | 29 | 72 | 127 | 99–126（28 帧） | 288–310（23 帧） |
| 149 | 52 | 118 | 147 | 127–146（20 帧） | 248–267（20 帧） |
| 150 | 39 | 115 | 136 | 354–375（22 帧） | 267–309（43 帧） |
| 151 | 117 | 161 | 219 | 185–218（34 帧） | 236–238（3 帧） |
| 152 | 180 | 248 | 327 | 281–326（46 帧） | 398–404（7 帧） |
| 153 | 97 | 173 | 183 | 173–182（10 帧） | 205–228（24 帧） |
| 169 | 261 | 300 | 310 | 300–309（10 帧） | 327–351（25 帧） |

除序列 125 外，其余序列最初的物理可见异常均未进入官方距离范围。不能把此时范围内异常点为零说成漏检。事后统一从首次出现范围内异常的时刻起取连续五帧，19 条序列在这些阶段都没有范围内异常分数达到 0.5；统一从首个合格帧起取连续五帧，也观察到零阈值检出。这些是同一已保存预测上的固定规则描述，没有据此改变推理或阈值。

较长的连续失败包括：序列 152 第 281–326 帧连续 46 个少点帧、共 97 个范围内异常点均低于阈值；序列 141 第 485–618 帧连续 134 个合格帧、共 2,731 个异常点均低于阈值；序列 125 第 0–81 帧连续 82 个合格帧、共 2,332 个异常点均低于阈值。序列 141 的整序列 AP 较高，并不表示它没有连续失败区间。

在最后一次物理可见异常之后仍有误报。例如序列 140 第 409–413 帧为 6,906／530,285，即 1.302319%；序列 138 第 472–476 帧为 3,004／312,325，即 0.961819%。这些数值说明消失后仍出现高分正常点，不能据此确定误报由历史异常残留造成。所有逐帧正常与异常分数、首次可见阶段、消失后阶段和连续失败区间保存在 `results.jsonl` 与 `summary.json`。

本轮按实际顺序完成了以下实质工作：

1. 确认正式训练已经按第七轮完整训练与监控结束，核对指定检查点；重新检查 CPU、GPU、WSL 内存、Windows 内存和 E 盘，确认没有其他训练任务。检查官方读取与评价类，以及真实序列的实际帧数、点数和评价范围。
2. 开始更新公开真实 `val` 的开发角色及启动输入实现。用户随后授权清理旧探索运行，共删除约 11.98 GiB 大文件；立即复查宿主机空间时仅回收约 0.38 GiB。一次非交互空闲块回收尝试因需要管理员密码而未执行，没有关闭 WSL。
3. 完成原始序列启动、连续评价、完整分数保存和官方槽位恢复。索引使用可逆差分压缩，正常分位数使用精确分块选择。代码检查发现并修复一处数据角色分支引用错误；旧格式测试按解码后的真实身份核对。
4. 在 206／201 的第 0、1、2、3、4、5 帧和末帧实际运行候选，共 14 窗，保存并回读全部分数。原始文本槽位九位有效数字回读完全恢复 `float32`；当前帧始终在最后扫描槽，缺失槽没有点或命中。两条序列各三个完整五帧时刻的新旧输入逐元素相同，预测最大绝对差均为 0。随后核验两份冻结合成池的内容身份仍为 `2f9a993ddc6d02f572e9aa31286bd2c5bbd8258977c07d15267d22a6c288db28` 和 `639fe3d776ce0cd0d9591d15e5ae8b901b751063db1414ca85e608dd03281db1`，没有重新生成数据。修复角色判断后，用最终代码重新完成启动检查；真实结果未用于选择启动策略。
5. 依据 19 条真实序列的实际点数、身份压缩长度、精确记录和排序缓冲，估计新增峰值为 25,820,628,442 字节，即 24.05 GiB。正式推理前 E 盘已延迟回收到约 54.36 GiB，安全余量为 22.58 GiB。推理期间对首个已完成序列 125 的全部 255 帧进行了官方文本导出和回读，逐元素核对 33,423,360 个原始槽位，官方汇总与精确计算一致；核验用临时文本已释放，完整无损预测保留。
6. 一次连续运行完成全部 8,659 帧，没有中断或恢复。推理、保存和最终汇总墙钟时间为 2172.305 秒（36.205 分钟），其中网络前向累计 1103.028 秒。读取、体素准备、GPU 前向和写入有重叠，各阶段累计耗时不能相加当作总墙钟。初始规模与预算核对、启动检查和后续独立核验不计入这段时间。
7. 全量推理后核对 8,659 个帧身份、17,318 个精确记录块及 76 个文件；对每条序列的第 0、3、4 帧和末帧共 76 窗回读全窗口预测并恢复原始槽位。19 条序列独立调用官方评价类，最大绝对差为 1.42e-14。366,706,288 个正常点另用完整 NumPy 数组独立复算，中位数、百分位和高分计数全部相同。独立核验用时 84.446 秒。
8. 最终代码回归为 77 项通过、2 项可选实数据测试跳过，静态检查通过；真实执行证据来自上述启动、全量推理及实际预测回读，而不由测试数量替代。结果图 `results.pdf` 实际嵌入 SimSun 与 TimesNewRomanPSMT，渲染无缺字或遮挡。

采样资源记录位于 `resources.jsonl`。正式推理采样中观察到的最大常驻内存约 2.88 GiB、最大 PyTorch 保留显存约 9.74 GiB；Windows 可用内存最低约 11.92 GiB，所有采样中的交换占用与开始时相同。每分钟采样不能替代未采样瞬间的精确峰值，也不能把窗口之间测得的 GPU 利用率当作整段平均利用率。推理产物逻辑大小约 20.53 GiB；结束后的 E 盘实测约余 39.81 GiB，仍高于安全余量。

核心结果为 `runs/real_val_v1/summary.json`；逐帧记录为 `results.jsonl`；独立核验为 `verification.json` 与 `official_check.json`；候选、帧清单、数据规模和预算分别记录在 `samples.json`、`inventory.json`；结果图为 `results.pdf`。这些原始预测与运行产物遵守既有忽略规则，不上传到远端。

当前可以确认：第七轮候选已经完成固定条件下的首次真实开发验证，评价输入、点槽和指标计算的必要核验通过，真实合并表现仍弱，启动区间之外也存在连续失败。当前证据没有确定体素、强度、时间特征或合成生成因素的责任，也没有比较单帧输入、其他模型或多随机种子。下一项能改变机制判断的工作应是同一训练来源和预算、同一真实点级评价范围下的一项明确对照，例如对应的单帧模型；本轮在交付这些真实结果后结束，不自动追加对照或训练。

分数保持 `float32` 原值，点槽索引以可逆差分压缩，读取后恢复原始身份并检查内容；已有预测格式仍可读取。正常分数的精确分位数以分块字节选择计算，无需全量排序副本。模型每个实际可见点均输出并保存分数；在线评价只使用当前帧。官方文本按序列导出，每个原始槽位一行，零坐标槽固定填 0，九位有效数字经读取恢复为相同 `float32`：

```bash
.venv/bin/python -m src.evaluate --data-root /absolute/path/to/STU \
  --export-official runs/real_val_v1 --sequence 125 --output /path/to/official_predictions
```

本轮按用户授权删除旧探索运行 `runs/learn`、`runs/transfer`、`runs/coverage`、`runs/validation` 中 3,667 个检查点、逐点预测和指标二进制文件，合计 12,864,079,537 字节（约 11.98 GiB）。保留 `runs/learn/initial.pt`、历史汇总记录和整个 `runs/fulltrain_v1`。下文旧报告中的已删除预测和探索检查点不再能够直接回读；B 的历史比较数字保留原始汇总依据，不声称本轮重新运行了 B。删除后 Windows E 盘实测只由约 47.03 GiB 增至 47.41 GiB，不能将 WSL 内部释放量当作宿主机回收量。

## 固定候选的强度依赖检查

本轮在预先指定的 416 个窗口上完成了强度分布统计和三次强度置换。结果没有显示合成高分强烈依赖原有强度与位置的对应关系：合成统一占比 AP 从 **74.815918%** 升至 **76.983228%–77.136743%**；真实统一占比 AP 从 **3.747151%** 降至 **3.506528%–3.540985%**。真实正常点在固定 0.5 阈值下的高分比例也增加。各世界和序列的变化并不一致，不能由这些合并结果推出强度完全无用。

合成异常与真实观测的离散取值差异已经由数据测得，但本轮没有证实它是模型迁移失败的原因。置换使至少 99.98% 的所选合成异常当前点改变强度，并使约 99.3%–99.5% 的这些点落到原始观测的离散网格上，合成性能仍保持较高。因此，应降低“当前合成高分主要依赖这些点原有强度对应”的解释优先级，不把分布差异直接写成模型利用了合成漏洞。

### 比较范围与实际输入

模型保持为 `runs/fulltrain_v1/epoch_07.pt`，SHA-256 为 `eb84e5383e9134968a3421749dd7e2f5b04248e631772eed0befb5486e44a7bb`。本轮没有更新参数、归一化缓冲区、骨干、冻结生成器或数据池，没有选择其他检查点，也没有启动训练。既有真实全帧官方 AP **3.475451%** 原样保留。

验证窗口直接取 `runs/diagnostic_v1/frames.jsonl` 的 `stratum="1_1"`，即当前官方范围内异常点数 20–99、异常距离中位数 [10,20) 米。合成侧固定 113 窗、15 个世界，真实侧固定 303 窗、14 条序列。每个窗口输入完整五帧，指标只使用当前帧原有的官方合格点。没有按原始或置换分数筛选样本。正常点的指标范围仍为全部官方合格点，不额外截到 10–20 米。

| 特征统计来源 | 同条件窗口 | 世界／序列 | 当前正常点 | 当前异常点 |
| --- | ---: | ---: | ---: | ---: |
| 206 训练合成 | 170 | 24 | 20,409,266 | 9,041 |
| 201 验证合成 | 113 | 15 | 10,049,116 | 5,412 |
| 真实验证 | 303 | 14 | 31,233,470 | 10,546 |

训练侧先读取全部 3,080 个训练窗口的当前帧，按同一规则确定条件覆盖；只有选出的 170 窗进入本节强度分布统计，没有训练前向。共有 3,004 个官方合格当前帧，57 帧范围内没有异常点、19 帧只有 1–4 个异常点。合格帧的全部非空分组如下；其余八个距离中位数至少 20 米的分组为空。

| 当前异常点数 | 距离中位数（米） | 206 合格帧 | 当前异常点 |
| --- | --- | ---: | ---: |
| 5–19 | [2.5,10) | 61 | 737 |
| 5–19 | [10,20) | 60 | 754 |
| 20–99 | [2.5,10) | 338 | 18,221 |
| 20–99 | [10,20) | 170 | 9,041 |
| 100–499 | [2.5,10) | 776 | 209,581 |
| 100–499 | [10,20) | 282 | 61,910 |
| ≥500 | [2.5,10) | 1,245 | 1,678,919 |
| ≥500 | [10,20) | 72 | 49,002 |

这次直接确认训练覆盖了所选粗条件。合格当前帧共含 2,028,165 个异常点，其中 1,727,921 点来自至少 500 点的当前帧。这个约 85.2% 的比例是当前帧点数构成，不能当作训练损失或梯度贡献比例：实际训练在完整五帧上按窗计算类别平衡损失。

### 强度分布的实测结果

逐点强度指当前点自身输入的原始数值。体素平均强度由完整五帧中同属一个 5 厘米体素的全部可见点求均值，再把该值取回到每个当前评价点。下表和分布文件中的体素统计因此按当前点加权；同一体素可能被重复计入，正常／异常标签来自当前点，没有对体素做多数标签投票。

| 来源及类别 | 逐点最小值 | 第 25 百分位 | 中位数 | 第 75 百分位 | 逐点最大值 | 精确在 k/3500 网格上的比例 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 训练合成正常 | 0.001143 | 0.106286 | 0.198000 | 0.337429 | 1.634857 | 100% |
| 训练合成异常 | 0.024861 | 0.135818 | 0.271476 | 0.392912 | 1.622571 | 1.725473% |
| 验证合成正常 | 0.001143 | 0.076571 | 0.153429 | 0.314000 | 1.636857 | 100% |
| 验证合成异常 | 0.008203 | 0.101106 | 0.184350 | 0.318700 | 0.715556 | 0.036955% |
| 真实正常 | 0.000571 | 0.128571 | 0.249143 | 0.439714 | 2.428571 | 100% |
| 真实异常 | 0.002857 | 0.116571 | 0.284857 | 0.444286 | 1.085429 | 100% |

网格检验逐值判断 `float32(round(float64(I)*3500)/3500) == I`，没有容差吸附或量化输入。候选网格来自前期原始 206 数值检查，是实测的格式特征，不是对传感器官方量化规格的推断。所选三侧的正常点及真实异常点全部满足该网格；合成训练异常为 156/9,041，合成验证异常只有 2/5,412。

三侧两类当前点的零强度比例均为 0。正常点的不同取值数分别为 5,714、5,716、5,681；异常点分别为 8,924、5,411、2,759。异常点中，取值在相应池内至少重复一次的点占比分别约 1.571%、0.037%、93.941%。这些是重复取值比例，不是重复物体或重复点身份比例。每个实体的零值、精确边界值、重复值比例及前十个常见值也保存在 `distributions.json`。

生成器校准强度支持范围为 [0.0011428571,1.6368571520]。选中真实正常点中有 5 点落在此范围之外，其余上述类别均没有范围外值；合成异常没有集中堆积在两个精确边界上。本轮没有根据这些统计裁剪或缩放输入。原始强度可以大于 1，它不是概率值。

| 来源及类别 | 逐点第 95 百分位 | 逐点第 99 百分位 | 体素均值第 25 百分位 | 体素均值中位数 | 体素均值第 75 百分位 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 训练合成正常 | 0.617143 | 0.848286 | 0.112545 | 0.204690 | 0.340222 |
| 训练合成异常 | 0.566147 | 1.518286 | 0.140779 | 0.273324 | 0.391713 |
| 验证合成正常 | 0.629143 | 0.858857 | 0.079429 | 0.156286 | 0.316743 |
| 验证合成异常 | 0.488145 | 0.598221 | 0.100425 | 0.188567 | 0.308581 |
| 真实正常 | 0.768571 | 0.996000 | 0.134571 | 0.255184 | 0.442762 |
| 真实异常 | 0.736214 | 0.932286 | 0.119871 | 0.286357 | 0.439393 |

强度尺度差异在两条入口上都可见。正常点的体素平均强度落在上述网格上的比例只有约 47.924%、69.815%、39.530%；平均运算本身就会产生网格之外的值，不能把体素均值离开网格解释成异常格式。

按实体分别统计后，24 个训练世界、15 个验证世界及 14 条真实序列的正常点仍全部落在网格上；14 条真实序列的异常点也全部如此。训练世界的异常网格比例为 0%–37.124%，验证世界为 0%–0.702%，所以合成与真实的离散取值差异没有仅由一个点数较多的世界造成。实体强度中位数的中位数如下；这里每个实体只贡献一个中位数，不替代前面的点汇总。

| 来源 | 正常点实体中位数的中位数 | 异常点实体中位数的中位数 |
| --- | ---: | ---: |
| 训练合成 | 0.198286 | 0.294270 |
| 验证合成 | 0.159429 | 0.209124 |
| 真实验证 | 0.264143 | 0.337000 |

为了检查逐点距离构成的影响，另将所选窗口的当前合格点按 2.5 米分段，统计两条强度入口的正常／异常分布。下面列出 10–20 米范围的逐点中位数和异常点数；完整 [2.5,50] 米各段及逐实体结果均保存。这些特征子集没有替换模型指标的原有点集合。

| 逐点距离（米） | 训练正常／异常中位数 | 训练异常点 | 合成验证正常／异常中位数 | 合成异常点 | 真实正常／异常中位数 | 真实异常点 |
| --- | --- | ---: | --- | ---: | --- | ---: |
| [10,12.5) | 0.271143／0.262709 | 5,325 | 0.280571／0.160346 | 2,978 | 0.290571／0.360143 | 4,718 |
| [12.5,15) | 0.218000／0.288202 | 3,478 | 0.236571／0.175440 | 1,182 | 0.239143／0.298000 | 2,703 |
| [15,17.5) | 0.182571／0.084704 | 107 | 0.243143／0.380345 | 635 | 0.187714／0.197571 | 1,436 |
| [17.5,20) | 0.170286／无异常点 | 0 | 0.174857／0.443564 | 516 | 0.148286／0.077429 | 1,477 |

相近逐点距离下，异常强度的变化仍不一致，不能用一个共同尺度差解释全部条件。所选 170 个训练窗口的当前异常点在 15 米以后尤其稀少；这一发现只限于此条件子集，不能写成全部训练历史扫描在这些距离完全没有异常。

### 单因素置换规则与全部合并指标

在计算前固定种子 **0、1、2** 和距离段宽 **2.5 米**。每个实际源扫描使用自己的原始坐标计算距离，按 `[0,2.5)、[2.5,5)、…` 左闭右开分段，对所有非零坐标可见点的强度统一排列，含忽略语义以及官方距离范围之外的点。算法没有标签输入。随机流由序列身份、源帧号和种子确定，同一源扫描在不同重叠窗口中得到完全相同的排列，不跨扫描或序列交换。

排列后只替换逐点强度，并按原体素成员重算体素平均强度。坐标、体素成员、逆映射、扫描命中、相对扫描位置、点身份和真值保持原样。平均强度的快速更新与完整重新体素化逐元素相同。原始 416 窗分数直接复用；三组置换预测分别保存在 `seed_0`、`seed_1`、`seed_2`，没有与原候选混用。

每个合并池及每个实体分别使用既定参考异常占比 **0.0451737756%**，即 87,398/(87,398+193,383,258)，以正负点常数权重计算统一占比 AP。低误报召回仍是在全部实际可达阈值上，FPR 不超过 1% 的最高召回；同分点整体纳入，不拆分、不插值。正常高分比例使用相同当前合格正常点及固定 0.5 阈值。下表均为百分数。

| 数据 | 输入 | 统一占比 AP | AUROC | 低误报召回 | 正常高分比例 |
| --- | --- | ---: | ---: | ---: | ---: |
| 合成 | 原始 | 74.815918 | 98.344263 | 93.477458 | 0.084903 |
| 合成 | 种子 0 | 76.983228 | 98.295121 | 93.606800 | 0.081132 |
| 合成 | 种子 1 | 77.136743 | 98.302212 | 93.699187 | 0.081251 |
| 合成 | 种子 2 | 76.997986 | 98.289887 | 93.643755 | 0.081858 |
| 真实 | 原始 | 3.747151 | 90.337291 | 44.557178 | 0.153880 |
| 真实 | 种子 0 | 3.540985 | 90.193807 | 43.997724 | 0.172568 |
| 真实 | 种子 1 | 3.517959 | 90.174907 | 43.770150 | 0.172523 |
| 真实 | 种子 2 | 3.506528 | 90.154011 | 43.732221 | 0.172469 |

合成统一 AP 增加 2.167310–2.320825 个百分点，低误报召回增加 0.129342–0.221729 个百分点；AUROC 小幅下降 0.042051–0.054376 个百分点。真实统一 AP 下降 0.206166–0.240623 个百分点，AUROC 下降 0.143484–0.183280 个百分点，低误报召回下降 0.559454–0.824957 个百分点。三个种子的正常高分计数从合成原始 8,532 降至 8,153、8,165、8,226；真实从 48,062 增至 53,899、53,885、53,868，增加约 12.1%。这些计数的分母分别固定为 10,049,116 和 31,233,470。

独立检查了全部 416 个当前扫描的置换幅度。合成异常点发生数值变化的比例为 100%、100%、99.981523%，其离散网格占比由原始 0.036955% 变为 99.334812%、99.519586%、99.408721%；真实异常点改变比例为 99.943106%、99.933624%、99.924142%。三次置换的异常点平均绝对强度变化分别约为合成 0.209391、0.208678、0.205663，真实 0.226446、0.226483、0.226567。可见，本轮结果不能解释为“排列几乎没有改变输入”。正常点及忽略／范围外点的变化数、变化幅度和网格比例也保存在 `verification.json`。

### 全部世界与序列的结果

下表保留每个实体的统一占比 AP，单位为百分比。合成标识 `序列/片段` 对应 `synthetic/validation/序列/segment_片段`，并非新的真实道路序列。每个实体的 AUROC、低误报召回、实际阈值、固定阈值正常高分计数和比例均完整保存在 `summary.json`；三页结果图同时显示所有实体的 AP、低误报召回和正常高分比例变化。

| 合成世界 | 帧数 | 异常点 | 原始 AP | 种子 0 | 种子 1 | 种子 2 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 000/05 | 7 | 467 | 98.789961 | 99.004747 | 98.969537 | 99.031853 |
| 000/18 | 1 | 40 | 52.622201 | 70.711637 | 65.187203 | 72.486811 |
| 001/05 | 7 | 287 | 82.239146 | 88.096025 | 88.003956 | 87.732287 |
| 001/10 | 13 | 1,001 | 97.698450 | 98.963538 | 98.934352 | 98.952978 |
| 001/20 | 7 | 285 | 82.477037 | 85.502603 | 86.597035 | 86.113739 |
| 001/21 | 2 | 195 | 0.108508 | 0.094972 | 0.096950 | 0.096432 |
| 002/02 | 4 | 86 | 100.000000 | 100.000000 | 100.000000 | 100.000000 |
| 002/04 | 1 | 97 | 98.195543 | 97.249879 | 97.616451 | 97.856870 |
| 002/06 | 1 | 98 | 86.296602 | 87.101922 | 86.455343 | 86.773363 |
| 002/07 | 5 | 235 | 99.665426 | 99.572455 | 99.583682 | 99.551368 |
| 002/12 | 8 | 189 | 85.227693 | 84.829426 | 84.789649 | 84.901739 |
| 003/00 | 17 | 465 | 89.174760 | 88.155875 | 87.939666 | 88.454399 |
| 003/02 | 22 | 1,028 | 84.954679 | 84.949408 | 84.963805 | 84.853066 |
| 003/04 | 10 | 734 | 91.257739 | 87.551771 | 88.382080 | 86.998796 |
| 003/18 | 8 | 205 | 70.007788 | 78.896516 | 78.783326 | 77.849431 |

| 真实序列 | 帧数 | 异常点 | 原始 AP | 种子 0 | 种子 1 | 种子 2 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| val/125 | 87 | 3,034 | 0.188724 | 0.199140 | 0.197921 | 0.198290 |
| val/138 | 13 | 331 | 7.291134 | 6.872736 | 6.769875 | 6.901126 |
| val/139 | 19 | 521 | 2.695601 | 2.087822 | 2.029018 | 2.051976 |
| val/141 | 55 | 2,980 | 4.748108 | 4.000991 | 3.988339 | 3.957113 |
| val/143 | 9 | 214 | 0.369620 | 0.335735 | 0.334452 | 0.331559 |
| val/144 | 10 | 289 | 0.856547 | 0.749214 | 0.743994 | 0.757868 |
| val/145 | 13 | 361 | 18.269588 | 34.009580 | 34.212075 | 34.327599 |
| val/146 | 3 | 72 | 15.713379 | 18.629479 | 18.555169 | 18.494851 |
| val/147 | 11 | 254 | 19.081302 | 23.666671 | 23.118017 | 23.081825 |
| val/148 | 9 | 219 | 17.922068 | 18.816111 | 19.012585 | 19.007870 |
| val/149 | 15 | 388 | 9.244794 | 9.270737 | 9.034816 | 8.601205 |
| val/150 | 29 | 896 | 12.547374 | 11.878656 | 11.727936 | 11.841842 |
| val/153 | 16 | 510 | 5.152924 | 3.495055 | 3.488450 | 3.492192 |
| val/169 | 14 | 477 | 0.106797 | 0.106513 | 0.106033 | 0.106152 |

合成共有 7 个世界三个种子都提高、6 个都下降、1 个方向不一致、1 个始终为 100%。真实共有 5 条序列三个种子都提高、8 条都下降、1 条方向不一致。`val/145` 的 AP 从 18.270% 提高至 34.010%–34.328%，而 `val/153` 从 5.153% 降至 3.488%–3.495%，`val/141` 从 4.748% 降至 3.957%–4.001%。这些相反变化不能被合并数值省略，也不能挑其中改善的序列声称置换改善了真实任务。

作为实体等权的补充描述，真实逐序列统一 AP 的算术均值从 8.156% 升至 9.511%–9.580%，与合并 AP 的方向相反。算术均值和合并指标回答不同的加权问题，也使用不同的正常背景比较集合，不能相互替代；这个现象尚未隔离出跨场景分数尺度的独立作用。

### 实际执行、核验与边界

本轮按以下顺序执行，包含中断和恢复：

1. 读取当前协议、上一轮固定分组、生成器强度计算与模型两条输入路径，测量 CPU、GPU、内存、交换空间和 Windows E 盘；预先保存 416 窗身份、三个种子、2.5 米段宽、参考占比及空间估算。
2. 前期检查原始数值时额外读取过 `val/141` 第 400 帧，随后核对到它不属于本轮固定分组。这次范围偏差已明确说明，该帧没有进入正式当前帧强度统计或任何新增模型前向；416 窗清单未改变。网格候选也已从允许读取的原始 206 第 0 帧获得。不能把这一额外查看当作未发生。
3. 读取整个 206 训练池的当前帧并统计条件覆盖，再对选中的 170＋113＋303 窗计算两条强度入口的分布及逐实体结果。该阶段耗时 368.734 秒，没有模型前向。第一次运行出现过只读 NumPy 掩码传入 PyTorch 的警告，没有修改底层数组；现实现改用等价的 NumPy 索引，并检查当前正常／异常点数与原记录一致。
4. 复用原始预测，执行三组置换。初次运行在第一个真实窗口的原始预测逐位检查处停止：GPU 重算与历史分数存在约 10⁻⁶ 量级差异。停止时已登记 338 份有效置换预测，另有一份写完但未登记的本轮预测。原始模型和历史预测没有被覆盖。
5. 对固定集合中的 `val/125` 第 4 帧、`val/141` 第 349 帧、`val/169` 第 376 帧分别重复原始推理，共 6 次控制前向。新旧准备输入逐元素相同，同一进程两次分数也逐元素相同；相对历史预测的最大分数绝对差为 2.563×10⁻⁶，均满足仓库原有 `atol=1e-6, rtol=1e-5` 模型回归容差。统一 AP 最大差为 4.342×10⁻⁸ 个百分点，AUROC 最大差为 4.420×10⁻⁵ 个百分点，低误报召回与固定阈值正常高分计数未变。具体 GPU 内核差异来源没有定位，不能直接宣称由某个后端造成。原始保存分数继续作为全部比较基准。
6. 按上述既有容差恢复计算，保留全部登记结果，只回退本轮未登记的一份预测及其记录尾部，并重新计算该窗。最终保存 **1,248 份有效置换窗口预测、732,392,814 个完整窗口点分数**。每个种子均为 416 窗、244,130,938 个点；全部历史输入点也保留。有效置换前向为 1,248 次，恢复产生额外 1 次重复置换前向；初始检查、独立数值核查与恢复检查合计另有 10 次原始控制前向。`summary.json` 的 `intervention_forward_calls=1248` 对应最终有效窗口，额外恢复计算在这里单独说明。恢复运行耗时 312.954 秒，该时间不含之前的中断运行与独立数值核查，不能当作整轮总耗时。
7. 对全部三个种子的窗口身份、预测文件、全窗口点数和当前两类点数检查完整性；另读取两侧首末窗口的全部三个种子，共 12 份预测、7,056,762 点，核对来源身份、有限数值和当前指标记录逐元素一致。对全部 416 个当前扫描计算置换幅度，确认不是弱干预。
8. 用 Scikit-learn 独立复算种子 0 的全部合成评价点 10,054,528 点，以及种子 2 的全部真实评价点 31,244,016 点。统一 AP 最大绝对差为 1.199×10⁻¹² 个百分点，AUROC、低误报召回、实际阈值和正常高分计数一致。完整回归为 **81 项通过、2 项可选实数据测试跳过**；唯一警告来自第三方库弃用接口，静态检查通过。三页 PDF 实际嵌入 SimSun 与 TimesNewRomanPSMT，逐页检查了渲染。

本轮使用 24 个可用 CPU 核、约 23.47 GiB WSL 内存和 RTX 5080 Laptop 16 GiB 显存。模型保持原推理后端，数值库使用单线程，读取、GPU 推理与有界保存重叠执行；没有把整批预测留在主存。恢复运行最大常驻内存 **3.298 GiB**，最大 GPU 实际分配 **1.640 GiB**、缓存保留 **8.178 GiB**。独立核验耗时 55.668 秒，最大常驻内存约 3.063 GiB。交换空间用量未随正式推理增加。

执行前 Windows E 盘剩余约 **40.09 GiB**，安全余量 **22.58 GiB**；按真实固定窗口点数计算的预测及排序峰值预算为 **9.666 GiB**，特征临时文件预算为 **4.762 GiB**，两阶段依次执行。临时文件用后释放，本轮产物合计约 **3.56 GiB**；推理与汇总结束时 E 盘实测剩余 **36.799 GiB**。没有删除正式模型、原始预测或旧诊断证据，没有导出 PLY 或复制既有大文件。

`src/intensity.py` 是这项新诊断的唯一入口，复用原窗口、体素化、推理、无损保存和精确指标实现。`tests/test_model.py` 检查标签不参与排列、组内强度多重集合、重叠源帧一致性、几何不变及两入口与完整重算一致；`tests/test_evaluate.py` 检查强度分位数和离散取值统计。原模型、生成器及训练程序没有修改。

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
.venv/bin/python -m src.intensity --data-root /absolute/path/to/STU
```

默认结果目录为 `runs/intensity_v1`。同一规则下再次调用可恢复已登记的置换前缀；已完成时复用统计并重绘结果图，不追加推理。如要从头完整复算，应显式提供另一个 `--output` 目录并重新检查空间。`--statistics-only` 只运行数据统计。

`spec.json` 保存规则及固定样本，`training.json` 保存训练覆盖和所选训练窗，`distributions.json` 保存全部三侧、两类、两入口、逐实体及逐点距离分布，`summary.json` 保存原始与所有置换指标，`numerics.json` 保存数值中断的独立核查，`verification.json` 保存实际置换幅度与独立复算，`results.pdf` 给出三页结果图。各 `seed_*/results.jsonl` 保留逐窗结果，`seed_*/predictions` 保留完整五帧的逐点分数。模型与预测仍遵循既有忽略规则，不上传远端。

输入置换衡量固定模型对强度与具体位置关系的依赖，不衡量从头训练时该通道的全部价值。它保留每个源扫描每个距离段的强度多重集合，也仍保留全局及分段强度统计；它改变物理关联，相关的几何特征也可能补偿。因此，本轮不能排除所有强度作用，不能把真实局部改善当作去强度训练一定有效，也不能把置换作为部署后处理。其解释边界与[Scikit-learn 对置换重要性的说明](https://scikit-learn.org/stable/modules/permutation_importance.html)一致。

当前证据降低了直接投入一轮去强度长训练的优先级。若继续诊断，下一项更有信息量的候选是在相同固定窗口上统计异常点与正常背景在 5 厘米体素内的混合程度，检验两侧的局部几何输入是否仍存在可观测差异；该机制仍是待检验假设，本轮没有实施。强度分布差异、少点远距覆盖不足和真实失败均继续保留为事实，具体迁移原因仍未确定。

## 异常占比统一与条件分层诊断

本次诊断复用第七轮的完整合成 201 预测和首次真实 `val` 预测，没有加载网络、重新前向、训练、重新生成异常或改变分数。合成侧包含 2,360 个窗口、92 个世界，真实侧包含第 4 帧及以后的 8,583 帧、19 条序列。进入点级指标的合格帧分别为 2,278 和 1,956。真实全帧官方 AP **3.475451%** 原样保留，本节的统一占比与分层指标只用于开发诊断。

实际结果支持两个同时存在的判断：少点、远距离条件在两侧都较难，合成汇总又高度偏向真实验证中没有出现的多点条件；在若干有充分共同覆盖的近距离条件下，仍有很大的真实迁移差距。因此，仅调整异常点比例或追求更高合成 AP，不能解决已经观察到的真实失败。本次没有识别出强度、形状、背景或具体网络部件中的哪一个因素造成了差距。

### 固定统计方法

参考异常占比在计算前写入 `runs/diagnostic_v1/spec.json`：

$$
\pi_\mathrm{ref}=\frac{87\,398}{87\,398+193\,383\,258}
=0.00045173775603469293.
$$

即 **0.0451737756%**。对于每个汇总池或分组，设异常点数为 $P$、正常点数为 $N$，每个异常点权重为 $\pi_\mathrm{ref}/P$，每个正常点权重为 $(1-\pi_\mathrm{ref})/N$。在原分数阈值 $t$ 下，加权精确率为

$$
\operatorname{Precision}_{\mathrm{ref}}(t)=
\frac{\pi_\mathrm{ref}\operatorname{TPR}(t)}
{\pi_\mathrm{ref}\operatorname{TPR}(t)+(1-\pi_\mathrm{ref})\operatorname{FPR}(t)}.
$$

统一占比 AP 按相邻阈值的召回增量加权求和，不在精确率—召回率曲线上做梯形插值。这与 Scikit-learn 的逐点权重及 AP 定义一致（[官方 AP 文档](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.average_precision_score.html)）。所有原始 `float32` 分数均保留，没有抽样、量化或删点。类别内常数权重不会改变 AUROC。

点数分组固定为 5–19、20–99、100–499、至少 500；距离分组固定为 [2.5,10)、[10,20)、[20,35)、[35,50] 米。每帧用当前 LiDAR 坐标中的异常点计算距离中位数，异常点必须满足原官方距离和语义规则。一个帧分入某组后，其全部官方合格正常点和异常点共同进入该组指标；没有再把正常点截成同一距离区间。两侧均保持原先至少 5 个范围内异常点的帧资格规则。

表中的低误报召回指 **FPR 不超过 1% 时，在实际分数阈值上能够达到的最高召回率**。预测规则为分数大于或等于阈值，同分点必须整体纳入，不插值拆分；相同最高召回时取更高的阈值。全部判正常的正无穷阈值也保留，结果文件用 `null` 表示。每组实际阈值、真阳性数、假阳性数和实际 FPR 均保存在汇总文件中。这些阈值仅用于描述评价曲线，不用于部署，也没有改变原固定 0.5 阈值报告（[官方 ROC 文档](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.roc_curve.html)）。

### 全体合格点的结果

所有指标以百分比表示。真实完整五帧区间本身就是参考比例，因此它的统一占比 AP 等于原 AP。

| 数据 | 正常点 | 异常点 | 原 AP | 统一占比 AP | AUROC | 低误报召回 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 完整合成 201 | 162,980,036 | 1,207,724 | 97.161646 | 90.327629 | 99.349523 | 97.917902 |
| 真实完整五帧区间 | 193,383,258 | 87,398 | 3.482498 | 3.482498 | 88.270711 | 50.338681 |

统一比例后，两侧 AP 仍相差 **86.845131 个百分点**。这项修正仅消除了类别比例对统计权重的直接差异，没有使点数、距离、物体、场景或正常背景的分布相同，不能把前后差额解释成某个因素的因果贡献。

### 全部分组的覆盖

合成列依次表示“合成序列数／世界数／帧数／异常点数”，真实列依次表示“真实序列数／帧数／异常点数”。四条合成序列共享原始 201 道路来源，不是四条独立采集的道路序列。同一世界或序列可以跨越多个分组，覆盖数不能跨行相加当作独立样本数。

| 异常点数 | 距离中位数（米） | 合成覆盖 | 真实覆盖 | 共同覆盖情况 |
| --- | --- | ---: | ---: | --- |
| 5–19 | [2.5,10) | 4/13/119/1,538 | 11/56/749 | 有 |
| 5–19 | [10,20) | 4/10/107/1,034 | 18/455/5,055 | 有 |
| 5–19 | [20,35) | 2/2/15/218 | 16/410/3,544 | 有 |
| 5–19 | [35,50] | 3/3/63/792 | 4/54/357 | 有 |
| 20–99 | [2.5,10) | 4/28/262/14,340 | 19/371/19,394 | 有 |
| 20–99 | [10,20) | 4/15/113/5,412 | 14/303/10,546 | 有 |
| 20–99 | [20,35) | 4/4/68/2,769 | 1/55/1,261 | 有 |
| 20–99 | [35,50] | 3/3/66/3,108 | 0/0/0 | 仅合成 |
| 100–499 | [2.5,10) | 4/50/487/133,284 | 14/247/45,876 | 有 |
| 100–499 | [10,20) | 4/26/246/66,407 | 1/5/616 | 有 |
| 100–499 | [20,35) | 3/3/35/5,599 | 0/0/0 | 仅合成 |
| 100–499 | [35,50] | 1/1/1/105 | 0/0/0 | 仅合成 |
| ≥500 | [2.5,10) | 4/45/657/944,636 | 0/0/0 | 仅合成 |
| ≥500 | [10,20) | 4/9/39/28,482 | 0/0/0 | 仅合成 |
| ≥500 | [20,35) | 0/0/0/0 | 0/0/0 | 两侧均空 |
| ≥500 | [35,50] | 0/0/0/0 | 0/0/0 | 两侧均空 |

16 组中，9 组有共同覆盖、5 组仅合成侧有数据、2 组两侧均空。按这些粗边界，没有仅真实侧有数据的分组，但存在覆盖量很不对称的条件。完整序列、世界身份、正常点数和组内原始异常占比也保存在 `summary.json`，逐帧归属保存在 `frames.jsonl`。

| 异常点数 | 合成帧数（占合格帧） | 真实帧数（占合格帧） | 合成异常点占全部异常点 | 真实异常点占全部异常点 |
| --- | ---: | ---: | ---: | ---: |
| 5–19 | 304（13.345%） | 975（49.847%） | 0.297% | 11.104% |
| 20–99 | 509（22.344%） | 729（37.270%） | 2.122% | 35.700% |
| 100–499 | 769（33.758%） | 252（12.883%） | 17.007% | 53.196% |
| ≥500 | 696（30.553%） | 0（0%） | 80.575% | 0% |

合成汇总中超过八成的异常点来自真实验证没有出现的至少 500 点条件，统一正负比例后，这些点仍占合成 AP 的大部分异常权重。本轮没有统计 206 训练池的同类分布，因此这些覆盖结论严格限于合成验证与真实验证的比较。

### 全部分组的指标

下表中的 AP 均为统一异常占比后的诊断 AP，所有指标单位为百分比。“—”表示缺少数据；不能将仅合成组用于声称真实迁移改善或失败。

| 异常点数 | 距离中位数（米） | 合成 AUROC | 合成 AP | 合成低误报召回 | 真实 AUROC | 真实 AP | 真实低误报召回 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 5–19 | [2.5,10) | 96.947 | 4.322 | 74.382 | 91.661 | 3.882 | 49.533 |
| 5–19 | [10,20) | 95.407 | 5.729 | 66.151 | 92.412 | 1.158 | 41.365 |
| 5–19 | [20,35) | 64.840 | 0.066 | 0.917 | 79.248 | 0.134 | 1.213 |
| 5–19 | [35,50] | 52.088 | 0.054 | 2.273 | 65.224 | 0.063 | 0.280 |
| 20–99 | [2.5,10) | 98.361 | 56.218 | 90.481 | 96.806 | 12.292 | 79.427 |
| 20–99 | [10,20) | 98.344 | 74.816 | 93.477 | 90.337 | 3.747 | 44.557 |
| 20–99 | [20,35) | 65.811 | 0.076 | 1.770 | 78.812 | 0.113 | 0.000 |
| 20–99 | [35,50] | 52.563 | 0.091 | 2.960 | — | — | — |
| 100–499 | [2.5,10) | 99.932 | 84.116 | 99.661 | 83.571 | 18.528 | 47.833 |
| 100–499 | [10,20) | 97.561 | 77.354 | 90.496 | 97.569 | 71.499 | 82.468 |
| 100–499 | [20,35) | 60.732 | 0.363 | 4.733 | — | — | — |
| 100–499 | [35,50] | 47.491 | 0.040 | 0.000 | — | — | — |
| ≥500 | [2.5,10) | 99.942 | 95.683 | 99.748 | — | — | — |
| ≥500 | [10,20) | 99.478 | 71.293 | 96.429 | — | — | — |
| ≥500 | [20,35) | — | — | — | — | — | — |
| ≥500 | [35,50] | — | — | — | — | — | — |

这些结果显示，异常距离中位数至少 20 米的共同分组中，两侧低误报召回均很低，不能把远距离合成数据概括为“已经学得很好”。真实共有 519 个这类合格帧，占 26.534%，涉及 5,162 个异常点，占 5.906%；这些点在原 0.5 阈值下全部漏检。合成相应远距离条件为 248 帧、12,591 个异常点，其中仅 2 点达到 0.5。统一占比后的真实 AP 在三个远距离共同组还略高于合成，但两侧绝对表现都弱。

在近距离中等点数条件下，仍有较大的残余差距。20–99 点、10–20 米组包含 15 个合成世界的 113 帧和 14 条真实序列的 303 帧；统一 AP 为 74.816% 对 3.747%，低误报召回为 93.477% 对 44.557%。100–499 点、2.5–10 米组包含 50 个世界和 14 条真实序列；统一 AP 为 84.116% 对 18.528%，低误报召回为 99.661% 对 47.833%。因此，“真实仅仅更少点、更远”不足以描述全部结果。

100–499 点、10–20 米组的真实统一 AP 达到 71.499%，但仅来自 `val/141` 第 474–478 帧的 616 个异常点，不能推广为这个条件下普遍迁移良好。20–99 点、20–35 米的真实侧也仅覆盖 `val/141` 一条序列。各组内点数和距离仍有分布差异，正常背景更没有完成匹配；例如 5–19 点、35–50 米组的合成正常点只有 14,383 个，真实正常点有 5,277,963 个。类别权重消除了数量比例对 AP 的直接作用，但不会把不同正常背景变成相同背景。

本次分析没有估计跨场景分数尺度的独立贡献，没有将重叠帧或共享道路的合成世界当作独立重复，也没有做因果识别或显著性检验。不能据此确定模型依赖合成痕迹、强度不适用、五帧有害或 LitePT 不适合。

### 1–4 点帧与执行证据

1–4 个官方范围内异常点的帧单独保留，不进入上面的 AP、AUROC 或低误报召回计算。固定 0.5 阈值下，合成侧 25 帧、7 个世界、75 个异常点中，检出 11 点，21 帧完全未检出；真实侧 1,120 帧、19 条序列、2,595 个异常点中，检出 15 点，1,113 帧完全未检出。逐点召回分别为 14.667% 和 0.578%。这仍是原阈值的漏检描述，没有据此调阈值。

本轮按实际顺序完成：确认两份已完成预测均来自指定第七轮模型，核对资源与完整样本范围；实现并检查统一占比、分层边界及完整同分组的统计；在计算前保存固定规则；补读当前帧观测，对全部有异常帧核对原官方点数，对全部 4,234 个合格帧核对当前记录中的标签顺序和有限分数；读取合成侧原先未保存合格指标记录的 25 个少点窗口预测，真实少点帧直接使用已有逐帧统计；合并全体与每组点级分数；独立复算并生成结果图。

旧合成汇总和真实完整五帧汇总的 AP、AUROC、FPR95 均由已有分数重新得到，与原记录一致。另对合成 5–19 点、2.5–10 米组的 9,668,253 点、真实同组的 5,524,452 点、真实 100–499 点、10–20 米组的 559,823 点，分别调用 Scikit-learn 的带权 AP、AUROC 和保留全部阈值的 ROC 曲线独立复算。统一 AP 最大绝对差为 **5.80e-13 个百分点**，AUROC 最大绝对差为 **1.42e-14 个百分点**，低误报召回、实际阈值和 FPR 完全一致。加权、同分数边界和分组边界的检查通过；完整代码检查为 **79 项通过、2 项可选实数据测试跳过**，静态检查通过。代码检查出现一条第三方库弃用警告，没有指标计算错误；本轮科学结果的核验依据是实际预测与上述独立复算。

统计运行耗时 **171.652 秒**，最大常驻内存 **4.145 GiB**；四个读取或统计工作线程使用单线程数值库，没有模型前向或参数更新。运行前实测 24 个可用 CPU 核、约 23.47 GiB WSL 内存及 RTX 5080 Laptop 16 GiB 显存，本诊断不使用 GPU 计算。E 盘在统计启动时剩余约 **39.807 GiB**，预计临时记录、缓冲及小产物峰值新增 **2.727 GiB**，高于安全余量的可用空间足够；运行中复查仍安全。临时排序文件在计算后自动释放，结束时 Windows E 盘实测余 **40.092 GiB**，未删除任何已有正式预测或检查点。

复现入口复用原评价程序，默认输出目录为 `runs/diagnostic_v1`；如该目录已经存在，应为新的完整复算指定另一输出目录，现有证据不会被覆盖：

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
.venv/bin/python -m src.evaluate --data-root /absolute/path/to/STU \
  --diagnose --output /path/to/diagnostic
```

`spec.json` 保存预先确定的比例、边界和来源；`frames.jsonl` 保存全部 10,943 帧的分组及少点检出信息；`summary.json` 保存全部分组覆盖、指标与实际工作阈值；`verification.json` 保存独立复算结果；`results.pdf` 显示完整分层结果，实际嵌入 SimSun 和 TimesNewRomanPSMT，并已检查渲染。诊断复用的当前帧读取由 `src/data.py` 的 `segment_for_window` 提供；权重、同分数处理、指定记录区间合并和诊断入口均在 `src/evaluate.py`。原有五帧构造继续调用同一冻结段读取逻辑。

下一阶段应优先检验共同覆盖充分、迁移差距较大的 20–99 点／10–20 米条件下的特征或生成分布差异，随后选择一个因素做受控对照。具体选择强度、几何形态还是背景，需要相应输入证据；本节不把其中任何因素认定为原因。少点和远距离仍是需要改善的已观察失败条件，但继续单纯优化合成汇总或启动策略不足以回答这些残余差距。本轮没有启动下一项对照或任何新训练。

## 全池正式训练

`AJAE-FullTrain-v1` 从 `runs/learn/initial.pt` 恢复原始模型、归一化状态和随机状态，重新创建 AdamW 与初始缩放值为 128 的动态损失缩放器。模型、5 厘米体素、全五帧类别平衡二分类损失和全部冻结数据保持原样。每轮以独立随机数生成器产生的排列无放回访问 3,080 个窗口，最多十轮。训练使用一个活动窗口、至多一个预取窗口及既有有界源帧缓存，不保存整池展开输入或中间网络特征。

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -u -m src.train --full \
  --data-root /absolute/path/to/STU --output runs/fulltrain_v1
```

前 200 次成功更新的学习率从 `3e-5` 线性升至 `3e-4`，第 1 次取起点，第 200 次取终点；溢出跳步不推进预热且不补抽窗口。每轮固定监控四条合成 201 中每个世界的首、中、末窗口，共 276 窗，以及原始正常 201 对应的 69 个不同时间窗口。中间取靠前者，末段取当前帧 620、650、681。检查随机种子沿用完整验证；评价模式和训练随机状态在检查后恢复。

监控 AP 使用百分数单位。只有严格超过参考最好 AP 的 0.1 个百分点才重置停滞计数；连续两轮未达到改善时，将下一轮学习率依次降至 `1e-4`、`3e-5`，保留历史参考值。只有完成至少四轮、最低学习率完整训练至少两轮且最近两轮均未改善，才按监控平台停止。十轮上限和训练加监控累计六小时上限分别记录，均不等于充分收敛。连续三次溢出、非有限损失或数值异常保留现场并停止，不自动改变配方重试。

`plan.json` 在开始前保存训练与监控窗口、全部排列、原始初始化、冻结数据和执行源码身份。每 500 步保存完整恢复状态，保留最近两版；每个完成训练与监控的整轮保存 `epoch_XX.pt`。`last.pt` 保存最后训练状态，可能处于半轮，不能直接充当最终候选。恢复使用同一命令追加 `--resume`，延续优化器、损失缩放、学习率、随机状态、下一窗口位置和累计耗时。周期检查点之后因异常退出丢失的工作会单独记录，重放不会重新打乱样本。监控恢复只清理本次输出中尚未提交完成记录的预测和指标尾部。

用户明确要求在当前整轮结束时停止，可在已保存的完整训练与监控状态上使用 `--resume --finish`，直接进入最终选模。该入口拒绝半轮训练或未完成监控的状态，不增加训练更新，也不更改原始计划；停止原因另记为 `user_requested_epoch_stop`，并保存用户指定的结束轮数。若最终评价中断，继续恢复同一选择状态，不重新训练。

监控最佳检查点与最后一个完成整轮监控的检查点进入完整验证，相同时只验证一次。每个新候选均评价 2,360 个合成窗口和 678 个原始正常窗口，保存全部五帧预测；最多两个新候选与既有 B 比较。同一评价范围内，先保留距离最高 AP 不超过 0.1 个百分点的候选，再依次比较更低的 FPR95、更低的正常点固定 0.5 高分占比和更早的检查点。完整比较写入 `comparison.json`，包括 92 个世界的配对指标、世界 AP 下四分位、沿用的 AP 低于 10% 世界描述，以及完整正常误报序列。正常连续局部误报另报告最差 21 个连续当前帧，属于描述性统计，不增加选模否决条件。

开始前复核 Windows E 盘与宿主机、WSL 内存。常驻内存目标 6 GiB，达到 8 GiB 停止；任一侧可用内存低于 4 GiB 停止继续加载。35 GiB 是初始磁盘预算，程序依据既有预测的真实点数、无损压缩最坏分数长度、十轮监控、两次完整验证、检查点和临时排序空间重新估算峰值，并保留 E 盘总容量 5% 与 10 GiB 中的较大者。资源检查按实际值持续执行，最终完整验证耗时另记。

这次训练同时改变访问覆盖、训练预算和学习率策略，只用初始化种子 23；结果不能单独归因于全池覆盖或五帧机制，也不能说明跨种子稳定性。201 用于开发调度和选模。在这次已结束的合成训练与选模阶段，真实异常数据没有参与性能评价；真实序列的启动约定随后在上文首次真实开发验证阶段确定。隐藏测试集继续封存。

正式启动的前 32 步均成功更新、无溢出，随后因 Windows 可用内存低于 4 GiB 保存状态并停止。根因是核验 B 的完整预测时积累了大量文件页缓存，训练进程本身常驻内存约 3.25 GiB。读取核验和预测写入现已释放无需复用的文件页缓存，不删除文件。随机访问还使用至多 256 MiB 的已验证稀疏变化记录缓存；非活动世界不保留展开帧，活动世界仍最多五帧。第 4、448、再次第 4 帧的真实窗口与原无缓存读取逐元素一致，逐点输出最大绝对差均为零。恢复保留原训练计划和已完成的 32 次更新，以 `--resume --updated-code` 记录此次经过等价回归的执行源码变更；后续源码不变时只需 `--resume`。累计训练与监控预算延续，不因资源修复或重启清零。

第 1,828 次成功更新后，为消除已实测的 CPU 体素排序瓶颈，在完整恢复位置再次暂停。体素分组改为按三维整数坐标排序后识别相邻相同网格，保持 `np.unique(axis=0)` 的字典顺序、逆映射及计数逐元素相同；均值累加仍按原点顺序，公式、精度和网络类未变。617,966 点真实窗口的三次 CPU 计时中，分组由约 0.308 秒降至 0.079–0.082 秒。真实窗口回归独立复算原 `np.unique` 网格、逆映射、计数和四通道均值，均要求逐元素相同；网络类与 B 的原源码语法结构也直接比较。恢复从第 1,829 个计划窗口继续，不重放已成功的更新。

本轮已按用户在第七轮期间追加的指令结束训练，并完成最终选模。原始十轮上限、学习率及平台规则仍保存在 `plan.json`；实际停止原因是 `user_requested_epoch_stop`，结束轮数为 7。第七轮监控仍有实质改善，学习率处于 `1e-4`，尚未进入 `3e-5` 阶段，也未达到原定平台、轮数或六小时预算上限。因此，本轮结果不能解释为已充分收敛。

七轮分别完整访问同一批 3,080 个窗口，共 21,560 次访问、21,559 次成功更新。每轮都保留 20 个纯正常窗口和 10 个仅含 1–4 个有效异常点的五帧窗口。独立逐项核对全部访问与原始排列，以及 128 个冻结世界的逐帧异常点数和全部可见点数，均一致；第八轮访问数为零。唯一溢出发生在第 21,018 次访问，即第七轮合成序列 5、片段 4、当前帧 116；该五帧窗口含 346 个异常点，损失缩放从 131,072 减至 65,536。后续更新正常，该窗口未重抽或补训。每轮损失均包括已访问窗口，正常类和总损失的分母为 3,080，异常类分母为 3,060。

| 轮数 | 成功更新 | 实际学习率 | 正常类训练损失 | 异常类训练损失 | 总训练损失 |
| --- | ---: | --- | ---: | ---: | ---: |
| 1 | 3,080 | 3e-5→3e-4 | 0.034923 | 0.085692 | 0.060061 |
| 2 | 3,080 | 3e-4 | 0.003525 | 0.011273 | 0.007385 |
| 3 | 3,080 | 3e-4 | 0.002210 | 0.006255 | 0.004223 |
| 4 | 3,080 | 1e-4 | 0.000701 | 0.000456 | 0.000582 |
| 5 | 3,080 | 1e-4 | 0.000522 | 0.000504 | 0.000513 |
| 6 | 3,080 | 1e-4 | 0.000400 | 0.000225 | 0.000313 |
| 7 | 3,079 | 1e-4 | 0.000358 | 0.000206 | 0.000283 |

以下为同一固定 345 窗监控范围的结果。AP、AUROC、FPR95 和高分比例均以百分数列示；正常分数中位数和第 95 百分位保持原始 0–1 分数。第三轮出现明显退化后，第四轮按原规则降低学习率；第五、六、七轮分别达到实质改善。七轮监控全部完成，每轮均为零优化器更新，模型参数和归一化缓冲区未变化。

| 轮数 | AP（%） | AUROC（%） | FPR95（%） | 正常分数中位数 | 正常分数第 95 百分位 | 正常点分数≥0.5（%） |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 95.960504 | 99.227102 | 0.143553 | 1.463704e-4 | 2.519128e-3 | 0.022454 |
| 2 | 92.076250 | 99.431716 | 0.197902 | 2.888537e-5 | 3.057677e-3 | 0.762173 |
| 3 | 80.603162 | 99.376021 | 0.708615 | 2.318448e-6 | 4.296097e-4 | 1.415712 |
| 4 | 94.360413 | 99.440432 | 0.166356 | 2.359843e-6 | 1.026574e-4 | 0.395209 |
| 5 | 97.064723 | 99.489759 | 0.065926 | 5.425910e-7 | 3.102012e-5 | 0.193081 |
| 6 | 97.264045 | 99.438247 | 0.038854 | 3.002199e-7 | 3.794574e-5 | 0.131203 |
| 7 | 97.490063 | 99.565071 | 0.042943 | 7.607027e-8 | 9.062712e-6 | 0.149963 |

第七轮同时是按预定排序规则确定的监控最优检查点和最后一个完整检查点，因此只进行一次新增完整验证。第七轮和 B 的完整评价均覆盖 2,360 个合成窗口与 678 个正常窗口；窗口、种子、点身份和评价点集合逐项一致。合成部分有 2,278 个合资格窗口，汇总 162,980,036 个正常点与 1,207,724 个异常点。另 57 窗没有官方范围内异常点、25 窗仅有 1–4 个，保留预测和不合资格原因，未填零或替换。官方汇总采用当前帧、2.5–50 米距离、忽略标签和严格 `TPR > 0.95` 规则，未平均逐窗 AP。

| 完整 201 候选 | AP（%） | AUROC（%） | FPR95（%） | 正常点分数≥0.5（%） | 世界 AP 下四分位（%） | 世界 AP 中位数（%） |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| B | 93.360878 | 98.394620 | 2.522800 | 0.073711 | 50.275946 | 94.124636 |
| 第七轮 | 97.161646 | 99.349523 | 0.046213 | 0.151258 | 77.748421 | 98.566802 |

第七轮完整 AP 比 B 提高 3.800769 个百分点，FPR95 降低 2.476587 个百分点，按原选模规则胜出。最终候选为 `runs/fulltrain_v1/epoch_07.pt`，SHA-256 为 `eb84e5383e9134968a3421749dd7e2f5b04248e631772eed0befb5486e44a7bb`。B 原检查点和完整预测继续只读保留；原始初始化身份及数据身份见原始 `plan.json`，本轮未改写资格证据。

92 个世界中，AP 有 69 个上升、23 个下降，逐世界 AP 变化的中位数为 +0.829077 个百分点；AUROC 有 76 个上升、16 个下降；FPR95 有 70 个降低、11 个相同、11 个升高。这些是配对描述，不是跨种子稳定性或统计显著性结论。世界 AP 下四分位从 50.275946% 提高到 77.748421%，但仍有 12 个世界 AP 低于 10%（B 为 15 个）。其中 11 个延续 B 的低 AP，4 个原低 AP 世界升至 10% 以上，序列 2、片段 17 新降至 10% 以下。该世界 AP 从 48.286067% 降至 6.571073%，FPR95 从 49.323593% 升至 55.075335%，是最大的 AP 退化。序列 2、片段 8 的 AP 仍仅 0.065657%。这 12 个低 AP 世界在合资格当前帧中累计仅有 147–2,179 个异常点；这是各世界的汇总点数，不能当作单窗点数或独立物理样本数。

完整正常序列覆盖当前帧 4–681，共 48,828,507 个官方范围内正常点。B 的高分点为 35,992 个，第七轮为 73,857 个，固定 0.5 高分比例增至约 2.052 倍。正常分数中位数由 1.081462e-5 降至 7.674656e-8，第 95 百分位由 1.372615e-4 降至 9.078449e-6；分数中央部分降低，同时极端高分尾部扩大。FPR95 与这一固定阈值比例使用不同的工作阈值和评价数据，不能互相替代。

| 正常当前帧范围 | B 高分点／总点数 | 第七轮高分点／总点数 | B 高分比例（%） | 第七轮高分比例（%） |
| --- | ---: | ---: | ---: | ---: |
| 324–344（第七轮最差连续 21 帧） | 5,886／1,785,227 | 16,818／1,785,227 | 0.329706 | 0.942065 |
| 406–426（B 最差连续 21 帧） | 7,294／1,465,917 | 2,932／1,465,917 | 0.497573 | 0.200011 |
| 397–417（既有局部观察范围） | 6,265／1,494,052 | 5,484／1,494,052 | 0.419329 | 0.367055 |

B 的最差单帧是 372，高分比例为 1.650785%；第七轮最差单帧是 373，为 1.566282%。单帧最大值降低没有消除全序列和最差连续 21 帧的误报退化。0.5 仅是固定观察阈值，没有在本轮被选成部署门槛。

第七轮完整合成验证的全五帧正常类、异常类及总损失分别为 0.006667、2.984021、1.489034；其逐窗分母分别为 2,360、2,350、2,360。异常类按全部异常点加权的损失均值为 0.334361，与逐窗等权均值具有不同含义。汇总 AP 较高不能覆盖少点世界的失败，也不能把训练损失的持续下降解释为普遍泛化改善。

累计训练与逐轮监控用时 12,162.208 秒，其中七次监控合计 1,271.457 秒；用户结束指令之后的完整验证单独用时 962.490 秒。训练步骤记录的加载、体素整理、等待预取、传输及训练计算时间分别合计 7,412.071、3,205.568、4,983.159、134.335、5,407.890 秒。加载和体素整理与计算存在并行，等待预取也是其中的等待部分，不能把这些阶段直接相加作为总墙钟时间。完整验证的加载、体素整理、传输、前向及评分保存时间分别合计 339.102、443.261、18.713、750.332、801.041 秒，也存在并行。

训练资源采样中的常驻内存最大值为 4.127 GiB，GPU 已预留内存最大值为 11.547 GiB；这些记录不能当作逐时刻全部内存的精确峰值。初次资源中断的 Windows 可用内存最低采样值为 2.377 GiB，已按前文所述修复缓存并延续状态。最终产物逻辑大小约 19.471 GiB，任务结束时重新查询的 Windows E 盘物理剩余空间为 50,503,000,064 字节，仍高于 24,247,532,954 字节安全余量；逻辑文件大小与宿主盘实际占用分别报告。

本轮保留 7 个完整轮次检查点、最近两个周期恢复状态、2,415 份监控五帧预测和 3,038 份完整验证五帧预测。全部 116 个当前帧记录文件的偏移、点数和内容均与逐窗记录一致。另从真实输入重新读取正常、无异常、少异常和低 AP 等六个代表窗口，核对预测点身份、当前帧及距离过滤，并与 scikit-learn 独立复算结果比较，均一致。完整评价确认零优化器更新，参数及缓冲区未改变。完整配对结果、每个世界的点数、正常逐帧序列和最终分数定义保存于 `comparison.json`；逐窗损失、失败原因及预测位置保存于 `validation/epoch_07/results.jsonl`。

当前代码的完整测试为 65 项通过、2 项因未设置完整数据测试环境变量而跳过；体素与缓存输入的真实 GPU 等价测试已在修改时另行启用并通过。结束入口还验证了拒绝半轮训练、未完成监控及不一致轮次状态，并确认恢复最终评价不会重新启动训练。测试只支持对应实现行为；科学结果以上述真实训练、完整验证和输入至指标的核对为依据。

最终推理方式仍是五个有序因果扫描联合输入、5 厘米体素、原模型评价路径；分数为逐点 logits 的 sigmoid，不校准、不融合重叠窗口，只用当前帧分数评价。本轮仅用初始化种子 23，201 已参与开发调度及选模，不构成独立最终测试。真实异常和隐藏测试未读取、未评价。后续仍需先确定真实序列开头不足五帧的处理及可比评价范围，再开展真实异常评价；本轮不再恢复训练或追加调参。

<details>
<summary>92 个世界的完整配对指标</summary>

世界编号为“合成序列编号：片段编号”，均从零开始。异常点数是合资格当前帧中的汇总点数，两模型的正常点、异常点及合资格窗口集合完全相同。所有指标使用百分数单位，低 AP 仍如实保留。

| 世界 | 合资格窗口 | 异常点 | B AP | 第七轮 AP | B AUROC | 第七轮 AUROC | B FPR95 | 第七轮 FPR95 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 0:0 | 24 | 13255 | 99.965652 | 99.993048 | 99.999128 | 99.999958 | 0.000000 | 0.000000 |
| 0:1 | 21 | 203 | 0.019184 | 0.941530 | 61.048487 | 89.229155 | 84.876593 | 44.495310 |
| 0:2 | 24 | 19156 | 94.736460 | 99.991422 | 98.676616 | 99.999926 | 1.186517 | 0.000086 |
| 0:3 | 24 | 13618 | 71.980197 | 98.176295 | 93.278939 | 99.765928 | 42.805421 | 0.009285 |
| 0:4 | 17 | 9145 | 99.995561 | 99.998939 | 99.999979 | 99.999995 | 0.000000 | 0.000000 |
| 0:5 | 24 | 4469 | 94.880468 | 99.097369 | 98.956039 | 99.998467 | 0.068397 | 0.009439 |
| 0:6 | 24 | 22606 | 99.885780 | 99.997520 | 99.996379 | 99.999979 | 0.001508 | 0.000075 |
| 0:7 | 24 | 10404 | 99.464041 | 99.826075 | 99.998350 | 99.999396 | 0.007420 | 0.003191 |
| 0:8 | 24 | 3444 | 91.401003 | 95.623434 | 99.985834 | 99.993130 | 0.074392 | 0.043516 |
| 0:9 | 23 | 57522 | 99.998524 | 99.998765 | 99.999962 | 99.999968 | 0.000226 | 0.000000 |
| 0:10 | 23 | 336 | 7.538115 | 1.557927 | 96.222360 | 99.053307 | 23.167028 | 2.464666 |
| 0:11 | 24 | 4811 | 18.751850 | 26.581612 | 88.846884 | 99.468359 | 58.878872 | 1.208375 |
| 0:12 | 24 | 75490 | 99.817825 | 99.763293 | 99.993759 | 99.974132 | 0.024313 | 0.013080 |
| 0:13 | 24 | 19696 | 47.376965 | 84.371457 | 93.516314 | 99.465807 | 50.322718 | 0.673281 |
| 0:14 | 24 | 11033 | 94.715869 | 99.054991 | 99.913964 | 99.995711 | 0.152973 | 0.020734 |
| 0:15 | 24 | 1280 | 16.694604 | 88.748801 | 98.457154 | 99.991254 | 10.832179 | 0.028979 |
| 0:16 | 21 | 147 | 0.798992 | 8.057688 | 84.933498 | 99.431165 | 69.839943 | 1.218298 |
| 0:17 | 24 | 7467 | 99.982915 | 99.980976 | 99.999886 | 99.999874 | 0.000723 | 0.000633 |
| 0:18 | 24 | 1849 | 84.344798 | 99.782888 | 96.576778 | 99.999640 | 28.303310 | 0.001200 |
| 0:19 | 24 | 24317 | 99.872783 | 99.999649 | 99.974147 | 99.999993 | 0.000000 | 0.000000 |
| 0:20 | 24 | 42330 | 91.141669 | 98.401805 | 95.657686 | 99.398624 | 42.658740 | 0.013512 |
| 0:21 | 24 | 5170 | 74.204919 | 78.726694 | 86.751223 | 93.254137 | 79.899244 | 47.758617 |
| 0:22 | 57 | 882 | 1.236691 | 2.931128 | 29.407829 | 58.410837 | 98.354234 | 96.287458 |
| 1:0 | 14 | 29273 | 99.993361 | 99.997282 | 99.998655 | 99.999896 | 0.000000 | 0.000000 |
| 1:1 | 22 | 476 | 77.924353 | 74.813599 | 98.723508 | 99.734307 | 11.115683 | 0.129044 |
| 1:2 | 24 | 8273 | 89.336326 | 99.473200 | 97.853231 | 99.973291 | 12.077336 | 0.001076 |
| 1:3 | 24 | 6122 | 99.926624 | 99.989856 | 99.999241 | 99.999974 | 0.000000 | 0.000000 |
| 1:4 | 24 | 17877 | 98.652305 | 99.972424 | 99.752851 | 99.999348 | 0.001787 | 0.000037 |
| 1:5 | 24 | 2859 | 87.629890 | 89.388538 | 99.790359 | 99.985858 | 0.092558 | 0.065708 |
| 1:6 | 24 | 8311 | 99.801994 | 99.994354 | 99.993850 | 99.999982 | 0.001013 | 0.000038 |
| 1:7 | 24 | 5739 | 99.558748 | 99.844406 | 99.995471 | 99.999646 | 0.001963 | 0.001037 |
| 1:8 | 24 | 17911 | 76.898165 | 94.419990 | 91.081738 | 98.966474 | 74.000526 | 0.304271 |
| 1:9 | 24 | 2272 | 92.768948 | 88.219437 | 99.992532 | 99.797610 | 0.037623 | 0.120637 |
| 1:10 | 24 | 2321 | 99.602131 | 98.558793 | 99.999527 | 99.997804 | 0.002271 | 0.006580 |
| 1:11 | 24 | 1936 | 24.680193 | 27.448503 | 98.157403 | 99.768378 | 7.911195 | 0.859653 |
| 1:12 | 24 | 9976 | 91.449798 | 99.105227 | 99.116925 | 99.996544 | 0.631574 | 0.012850 |
| 1:13 | 24 | 614 | 0.395767 | 1.380852 | 86.330215 | 97.192752 | 50.828971 | 14.946600 |
| 1:14 | 24 | 38170 | 54.820051 | 98.007686 | 95.714810 | 99.929408 | 22.649468 | 0.102437 |
| 1:15 | 24 | 57694 | 99.998013 | 99.999262 | 99.999925 | 99.999974 | 0.000066 | 0.000000 |
| 1:16 | 18 | 1558 | 99.196415 | 97.945327 | 99.996653 | 99.997522 | 0.004694 | 0.004694 |
| 1:17 | 24 | 70323 | 99.999865 | 99.999735 | 99.999991 | 99.999984 | 0.000000 | 0.000000 |
| 1:18 | 24 | 10190 | 98.596631 | 99.775857 | 99.841696 | 99.986736 | 0.003605 | 0.001287 |
| 1:19 | 24 | 590 | 76.016285 | 73.680833 | 99.096442 | 99.528175 | 2.027095 | 1.377512 |
| 1:20 | 24 | 2298 | 99.761898 | 98.126994 | 99.994731 | 99.822004 | 0.000335 | 0.009554 |
| 1:21 | 24 | 4692 | 66.639079 | 67.826339 | 83.626194 | 89.631954 | 81.258243 | 65.546376 |
| 1:22 | 60 | 1525 | 2.203670 | 5.412625 | 31.657218 | 59.327140 | 98.192155 | 95.054008 |
| 2:0 | 24 | 25714 | 95.302160 | 99.873387 | 99.230277 | 99.992461 | 0.737945 | 0.000671 |
| 2:1 | 16 | 31645 | 99.999986 | 99.999675 | 100.000000 | 99.999993 | 0.000000 | 0.000000 |
| 2:2 | 24 | 297 | 0.028999 | 56.337338 | 63.000712 | 95.874003 | 84.038587 | 30.762607 |
| 2:3 | 24 | 11313 | 99.149918 | 99.990826 | 99.901976 | 99.999961 | 0.000562 | 0.000161 |
| 2:4 | 24 | 9854 | 96.209441 | 99.940463 | 99.058618 | 99.999770 | 0.023828 | 0.000742 |
| 2:5 | 22 | 308 | 3.248107 | 3.244487 | 90.573104 | 99.191020 | 49.526738 | 4.169717 |
| 2:6 | 24 | 6189 | 80.164442 | 98.882439 | 95.457633 | 99.966268 | 36.432690 | 0.004795 |
| 2:7 | 24 | 2399 | 98.952284 | 99.406385 | 99.999188 | 99.999445 | 0.004735 | 0.003477 |
| 2:8 | 22 | 220 | 0.069612 | 0.065657 | 81.565574 | 86.510093 | 56.113183 | 46.216787 |
| 2:9 | 24 | 28486 | 99.998796 | 99.999539 | 99.999985 | 99.999994 | 0.000000 | 0.000000 |
| 2:10 | 24 | 4145 | 99.388247 | 97.287534 | 99.997848 | 99.994238 | 0.004359 | 0.031953 |
| 2:11 | 24 | 13514 | 98.197900 | 98.764931 | 99.982838 | 99.994471 | 0.057466 | 0.013490 |
| 2:12 | 24 | 849 | 59.104951 | 81.651838 | 98.231853 | 99.987187 | 11.250916 | 0.062455 |
| 2:13 | 24 | 14588 | 98.645942 | 98.057263 | 99.989149 | 99.987299 | 0.069230 | 0.070636 |
| 2:14 | 24 | 353 | 0.312537 | 1.402692 | 89.087535 | 99.028701 | 44.745895 | 2.536121 |
| 2:15 | 24 | 1407 | 10.724952 | 56.388639 | 97.842455 | 99.794666 | 11.153135 | 0.897777 |
| 2:16 | 24 | 8785 | 99.313442 | 98.574812 | 99.952116 | 99.991325 | 0.008062 | 0.031849 |
| 2:17 | 24 | 1363 | 48.286067 | 6.571073 | 91.247862 | 84.842042 | 49.323593 | 55.075335 |
| 2:18 | 24 | 30724 | 82.850126 | 99.919653 | 96.368382 | 99.989844 | 25.902744 | 0.001381 |
| 2:19 | 24 | 17944 | 99.998025 | 99.999265 | 99.999969 | 99.999989 | 0.000000 | 0.000000 |
| 2:20 | 19 | 26188 | 99.988852 | 99.994912 | 99.999319 | 99.999705 | 0.003607 | 0.001683 |
| 2:21 | 24 | 8751 | 38.308472 | 45.895783 | 64.657233 | 84.063103 | 92.835749 | 67.173835 |
| 2:22 | 59 | 2179 | 2.930926 | 7.231931 | 26.855114 | 59.550039 | 98.943638 | 95.215702 |
| 3:0 | 24 | 660 | 41.948682 | 84.167869 | 88.317053 | 99.908176 | 60.009344 | 0.042681 |
| 3:1 | 24 | 13332 | 99.978755 | 99.931176 | 99.999818 | 99.999579 | 0.000185 | 0.001707 |
| 3:2 | 24 | 1149 | 50.939240 | 84.786792 | 84.356106 | 98.342677 | 68.949303 | 11.712779 |
| 3:3 | 24 | 12234 | 92.084690 | 96.811150 | 97.970850 | 99.377483 | 9.130377 | 0.009762 |
| 3:4 | 24 | 3492 | 86.801572 | 98.722957 | 96.561328 | 99.917258 | 27.729087 | 0.003296 |
| 3:5 | 24 | 25595 | 99.991211 | 99.935058 | 99.999916 | 99.999406 | 0.000000 | 0.002195 |
| 3:6 | 24 | 10614 | 99.907329 | 99.971033 | 99.999625 | 99.999881 | 0.001764 | 0.000000 |
| 3:7 | 24 | 17192 | 99.953044 | 99.989352 | 99.999766 | 99.999933 | 0.000818 | 0.000112 |
| 3:8 | 24 | 3782 | 95.983249 | 96.800494 | 99.990056 | 99.995528 | 0.038848 | 0.017279 |
| 3:9 | 24 | 8375 | 99.257072 | 99.991937 | 99.876678 | 99.999971 | 0.003326 | 0.000000 |
| 3:10 | 24 | 14572 | 99.974042 | 99.792643 | 99.999821 | 99.998675 | 0.000047 | 0.004567 |
| 3:11 | 24 | 3267 | 93.533403 | 92.495278 | 99.988381 | 99.986135 | 0.061161 | 0.047548 |
| 3:12 | 24 | 8379 | 60.581259 | 87.315337 | 98.725126 | 99.826272 | 6.805880 | 0.493200 |
| 3:13 | 24 | 491 | 0.406973 | 2.809296 | 87.816382 | 99.339707 | 57.213579 | 1.390634 |
| 3:14 | 24 | 77718 | 99.850655 | 99.897867 | 99.993380 | 99.997048 | 0.030867 | 0.011767 |
| 3:15 | 24 | 24649 | 99.965476 | 99.984697 | 99.999454 | 99.999806 | 0.001801 | 0.000579 |
| 3:16 | 24 | 21557 | 99.989686 | 99.918127 | 99.999516 | 99.999104 | 0.000637 | 0.004139 |
| 3:17 | 24 | 455 | 8.507666 | 18.204048 | 84.090311 | 96.812243 | 58.537066 | 17.804270 |
| 3:18 | 24 | 1084 | 62.043519 | 96.940434 | 95.950943 | 99.866967 | 27.564944 | 0.006851 |
| 3:19 | 24 | 26558 | 99.993217 | 99.999940 | 99.999842 | 99.999999 | 0.000173 | 0.000000 |
| 3:20 | 24 | 30151 | 99.999571 | 99.861561 | 99.999978 | 99.974507 | 0.000000 | 0.000000 |
| 3:21 | 9 | 53 | 4.171640 | 10.575945 | 60.113758 | 90.765626 | 94.483668 | 47.074436 |
| 3:22 | 55 | 5520 | 6.337815 | 12.064655 | 17.044623 | 48.911055 | 99.698713 | 98.645343 |

</details>

## 五帧异常分割模型

`src/model.py` 实现唯一的首版模型：九通道联合体素输入、官方语义分割 LitePT-S 编码器与轻量解码器、`81→32→1` 逐点异常头，隐藏层使用 GELU。共有 12,729,089 个可训练参数，其中九通道骨干 12,726,432 个、异常头 2,657 个。模型全部随机初始化，不下载或加载外部权重，没有独立预训练阶段。

`vendor/litept/` 仅引入 [LitePT 官方仓库](https://github.com/prs-eth/LitePT/tree/f0cc7692b81518124b96c856e79346fd19f40bec) 提交 `f0cc7692b81518124b96c856e79346fd19f40bec` 的独立 `litept/`、`libs/pointrope/` 实现及 MIT 许可证。骨干只把 PointROPE 的导入改为包内相对路径；结构参数保留官方语义分割小模型默认值，输入通道从 4 改为 9，解码输出为 72 维。未引入官方数据加载器、训练框架、演示数据或检查点。未安装 PointROPE 专用扩展时，使用官方提供的纯 PyTorch 实现。

输入直接来自冻结的 `SceneWindow`，不重新配准，不裁点，不重新渲染。体素边长默认 `0.05` 米，以当前 LiDAR 原点为网格原点，按照 `floor(xyz / d)` 分组；整数网格再减去最小格号，以满足稀疏卷积的非负索引要求，物理坐标不平移。分格除法和均值累加使用双精度，网络输入保存为单精度。每个体素的九通道依次为坐标均值、原始强度均值和五次扫描命中标记。命中标记仅表示该次扫描在体素中存在返回，不代表可见性或异常判断。五帧共用一个场景编号，不把扫描来源拆成不同批次。

每个原始点按逆映射取得 72 维体素特征，再拼接三维体素内均值偏移除以体素边长、该点原始强度、五维相对扫描来源独热编码，得到 81 维输入。标签、标签有效性、绝对帧号、槽号、序列编号和代理编号不进入特征。同一体素内的正常、异常和忽略标签保持逐点独立，不生成体素标签。

模型的 `forward(window)` 返回原窗口顺序的 `M` 个未经过 sigmoid 的值，用于训练；`predict(window)` 返回已有的 `PredictionBatch`，保存全部点身份和 sigmoid 异常分数。分数尚未经概率校准。训练时只用 `labels.anomaly_target != -1` 排除官方忽略点，不使用 `current_mask` 限制训练监督。固定样本重复学习时，可将 `joint_voxelize` 的结果通过 `forward(window, inputs=...)` 复用；输入绑定到该窗口的只读点对象和体素大小，仍走同一个骨干与逐点预测实现。

```python
from pathlib import Path
from src.model import AJAE

window = train[0]  # train 来自上面的 FrozenWindowDataset，构造时已核验冻结池。
model = AJAE(voxel_size=0.05).cuda().eval()
prediction = model.predict(window)
prediction.save(Path("runs/window_000004.npz"), window=window)
online_scores = prediction.anomaly_score[prediction.online_mask]
```

先保存全部点预测，再提取当前帧在线分数；不同窗口中同一点的其他预测不参与在线分数融合。上述随机模型示例只验证接口，不产生有意义的异常检测结果。体素边长属于模型超参数，不改变 schema 34 的数据协议。

本机模型环境使用 `.venv`，复用已有 Python 3.13、PyTorch 2.12.0 / CUDA 13.0，编译器为 CUDA 13.2；不复制另一份 PyTorch。新增依赖安装在项目环境内，不修改基础环境。安装命令如下，其他机器必须选择与其 PyTorch、Python 和显卡匹配的扩展版本：

```bash
python -m venv --system-site-packages .venv
.venv/bin/python -m pip install --no-cache-dir \
  spconv-cu126==2.3.8 addict==2.4.0 colorhash==2.1.0 \
  timm==1.0.27 einops==0.8.2 ninja==1.13.0
.venv/bin/python -m pip install --no-cache-dir --no-deps \
  torch-scatter==2.1.2+pt212cu130 \
  -f https://data.pyg.org/whl/torch-2.12.0+cu130.html
MAX_JOBS=3 NVCC_THREADS=1 FLASH_ATTN_CUDA_ARCHS=120 \
  FLASH_ATTENTION_FORCE_BUILD=TRUE \
  .venv/bin/python -m pip install --no-cache-dir --no-deps \
  --no-build-isolation flash-attn==2.8.3.post1
```

FlashAttention 针对本机 `sm_120` 编译，保留官方骨干使用的注意力实现。编译前仍需检查 E 盘物理剩余空间；本机使用三个编译任务，避免编译器内存叠加挤占交换空间。PointROPE 当前使用官方 PyTorch 实现，不执行上游仅指定 `sm_90` 的专用扩展安装命令。

模型权重保持单精度，训练可由调用者启用自动混合精度。实际检查发现 `spconv 2.3.8` 的推理分支跳过了训练分支的权重自动类型转换，半精度特征与单精度权重混用会报错。因此 `AJAE.forward` 在 `eval()` 模式下关闭外层自动混合精度，按单精度调用骨干与预测头；官方注意力内部的半精度实现保持不变。该处理只限定执行精度，不修改官方网络结构，也不改变冻结坐标、点数或标签。

下列检查不启动优化器或长训练，不保存模型检查点。普通检查覆盖独立体素计算、标签隔离、官方骨干前向与反向、全点预测保存和读取；显式设置数据路径后，另以完整冻结窗口进行显存与全点输出检查，训练 206 只做一次反向，合成与正常 201 均仅前向：

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -m pytest -q -p no:cacheprovider
AJAE_STU_ROOT=/absolute/path/to/STU PYTHONDONTWRITEBYTECODE=1 \
  OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -m pytest -q -s -p no:cacheprovider \
  tests/test_model.py -k complete_frozen
```

本机已实际执行上述检查：普通测试 38 项通过，默认跳过的完整数据检查另行执行并通过；Ruff 和差异格式检查通过。所有输入特征的独立复算、混合标签逐点保留、有效历史点与当前点梯度、忽略点零损失梯度、全点预测保存与回读均通过对应实现检查。以下三个窗口的当前帧均为 4，没有裁点或改变体素边长：

| 输入视图 | 全窗口点数 | 联合体素数 | 当前帧点数 | 执行内容 | 峰值已分配／已预留显存（GiB） |
| --- | ---: | ---: | ---: | --- | ---: |
| 206 合成训练 | 617,966 | 396,386 | 124,140 | 前向与反向 | 5.33／5.92 |
| 201 合成验证 | 1,163,987 | 390,734 | 103,929 | 仅前向 | 1.56／3.05 |
| 201 原始正常 | 1,163,892 | 391,106 | 103,915 | 仅前向 | 1.56／3.05 |

显存由 PyTorch 统计，不包括桌面和驱动占用，也不是整个数据池或正式优化器训练的显存上界。训练检查使用半精度自动混合精度及损失缩放；验证使用上面说明的推理精度路径。修复后该次检查的单窗耗时分别为 6.93、1.53、0.96 秒，包含输入整理及对应梯度检查；首次训练窗口检查耗时为 28.45 秒，不能以其中任一单次耗时直接推算稳定训练吞吐。上述模型适配检查未执行优化器更新、模型选择或 STU 性能评价，只支持模型接口与所测窗口的可执行性。

## 固定八窗口学习诊断

本节记录 `src/train.py` 默认入口的历史八窗口诊断，只回答模型能否学习见过的合成异常。该诊断通过现有冻结入口读取 206 第 0 条合成序列，在片段 0、2、4、6、8、10、12、15 各取末尾五帧窗口，当前帧固定为 27、83、139、195、251、307、363、448。八个窗口互不重叠，完整保留所有可见点、有效标签和忽略点；不使用 201 观测、真实异常序列或隐藏测试数据。冻结入口仍核验两份池清单与文件身份，不因此把验证数据送入模型。

模型和异常头均随机初始化，种子 23。每轮独立打乱八个窗口且无放回访问，共 25 轮、200 个计划步骤，每步一个完整窗口，无梯度累积或增强。AdamW 固定学习率 `3e-4`、权重衰减 `1e-2`、`betas=(0.9,0.999)`、`eps=1e-8`；全模型梯度范数上限为 1。沿用半精度自动混合精度，损失缩放初值 128。先解除缩放并检查梯度，再裁剪、更新参数；实际调用优化器才计作成功更新。连续三次梯度溢出、显存不足或 30 分钟保护时限会停止并保存已有状态，不追加步骤或修改配置。

损失在五帧全部有效点上计算：正常点和异常点各自求二分类 logits 损失均值，两类各占一半；仅存在一类时使用该类完整均值。忽略点不参与分母和损失，但保留输入观测。该配方仅是本次学习诊断的起点。

检查时刻为 0、40、80、120、160、200 步。每个窗口每次检查均以 `23 + window_index` 重置 Python、NumPy、PyTorch CPU 和 CUDA 随机状态，前向后恢复训练随机状态；使用 `eval()` 和无梯度推理，并确认 BatchNorm 运行统计未改变。同一检查点连续前向两次，记录实际分数、损失和 AP 差异，不要求 GPU 跨运行逐位一致。八个联合体素输入只整理一次，复用前以首个真实完整窗口与原整理路径比较，容差为 `atol=1e-6, rtol=1e-5`。

全窗口分别记录正常类、异常类和总损失。当前帧 AP 直接沿用官方筛选：忽略语义 0，语义 2 为异常，其余非零语义为正常，只保留闭区间 2.5–50 米内的点；有效异常点少于 5 时记录不可评价。逐窗 AP 以百分数表示，汇总时对可评价窗口求均值和中位数，不把重复前向当作独立样本。分数分布同时明确记录全窗口有效点和当前帧官方评价点两种范围，各含正常点中位数、正常点第 95 百分位和异常点中位数。

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -u -m src.train \
  --data-root /absolute/path/to/STU --output runs/learn
```

已有输出目录会被拒绝，不覆盖此前证据。`runs/learn/metrics.jsonl` 记录固定配置、完整访问顺序、源数据身份、逐步更新与各检查点结果；`predictions/step_*/` 保存两次前向的全部点分数及身份，历史四帧不丢弃。`initial.pt` 保存初始状态，`final.pt` 同时保存模型、优化器、损失缩放器、随机状态、计划／成功步骤计数及后续访问位置。检查点只加载自行生成的可信文件；含 NumPy 随机状态时使用 `torch.load(..., weights_only=False)`。

本机已完成这一次固定配方实验：八个窗口共 5,009,637 个可见点，200 个计划步骤全部成功，每窗恰好访问 25 次，溢出跳步为 0，损失缩放保持 128。第一次更新使骨干 12,726,431 个参数元素、异常头全部 2,657 个参数元素发生变化。最终状态中全部 203 个优化器参数状态的步数均为 200。

以下全部是**训练子集上的拟合诊断**。损失是固定推理模式下八个全窗口的均值，AP 是八个当前帧的逐窗结果汇总；不是 201 验证或真实 STU 测试成绩。

| 计划步骤 | 正常类平均损失 | 异常类平均损失 | 总损失 | 当前帧 AP 均值（%） | 当前帧 AP 中位数（%） |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 0 | 0.663256 | 0.729808 | 0.696532 | 0.8807 | 0.6749 |
| 40 | 0.343440 | 0.908632 | 0.626036 | 20.1637 | 9.0452 |
| 80 | 0.117764 | 1.713136 | 0.915450 | 22.4118 | 4.6910 |
| 120 | 0.041174 | 1.292886 | 0.667030 | 67.1465 | 82.4132 |
| 160 | 0.015084 | 1.151241 | 0.583163 | 75.4649 | 84.3480 |
| 200 | 0.008283 | 0.247967 | 0.128125 | 97.5719 | 99.5007 |

| 当前帧 | 初始 AP（%） | 最终 AP（%） | 最终正常分数中位数 | 最终正常分数第 95 百分位 | 最终异常分数中位数 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 27 | 0.2154 | 99.3455 | 0.003608 | 0.031262 | 0.990420 |
| 83 | 1.1019 | 96.7668 | 0.003268 | 0.033467 | 0.965638 |
| 139 | 0.1714 | 100.0000 | 0.001612 | 0.028037 | 0.960714 |
| 195 | 0.1385 | 99.6560 | 0.002172 | 0.032558 | 0.982854 |
| 251 | 0.2777 | 86.3942 | 0.000860 | 0.024190 | 0.553563 |
| 307 | 1.4358 | 98.5806 | 0.002122 | 0.029901 | 0.994625 |
| 363 | 1.0721 | 99.9792 | 0.004852 | 0.036479 | 0.992729 |
| 448 | 2.6326 | 99.8532 | 0.002060 | 0.029372 | 0.993624 |

表中分数使用当前帧官方评价点范围。八个窗口均具备评价资格且 AP 均改善，正常类损失均下降，异常类损失有七个窗口下降。251 是例外：全窗口异常类损失由 0.737089 升至 1.118747，最终当前帧异常分数中位数为 0.553563，明显低于其余七窗的 0.960714–0.994625。因此结果支持当前模型和全窗口监督能够学习这些已见合成样本，但不能称所有窗口已经充分拟合。中间检查的异常类损失先升后降，也不能表述为训练与推理损失始终同步下降；尚未隔离归一化统计、内部随机机制或其他因素的贡献。

六个检查点共 48 对重复前向，实测逐点分数最大绝对差、AP 差和损失差均为 0；这是本次环境的实测结果，不是跨设备逐位复现承诺。全部 96 份预测已通过完整窗口身份回读，覆盖 60,115,644 条点分数记录；调用官方完整指标计算程序重算的 AP 与日志之差为 0，两种点范围的分位数也与保存预测一致。另在独立进程中分别恢复 `initial.pt` 和 `final.pt`，各重跑全部八窗，分数、AP 和总损失与对应保存结果的最大绝对差也均为 0，没有执行额外优化器更新。

从初始状态保存到最终状态保存共 117.03 秒，不含冻结数据载入和一次性输入整理；其中 200 个训练步骤合计 51.02 秒，首次 5.56 秒，其余步骤平均 0.228 秒。完整训练步骤包含优化器状态和八窗输入复用，其峰值已分配／已预留显存为 6.38／7.23 GiB。全部本次产物共 501,076,127 字节（约 477.9 MiB），不据此推算完整池的显存上界或训练时间。当前代码测试 45 项通过，未启用涉及 201 的完整数据测试；没有执行 201 模型选择、真实异常评价或额外训练。

本次证据不支持泛化能力、五帧优于单帧或超过 STU 基线的结论；也没有证据要求更换骨干、重抽异常世界或重建数据池。已见八窗的拟合分数不作为正式性能；独立固定 201 样本的后续检查见下节。

## 201 配对窗口零更新诊断

`src/evaluate.py` 对已有 `initial.pt` 和原定 200 步的 `final.pt` 做配对推理，不继续训练、不选择模型或校准分数。它只使用 201 第 0 条冻结合成序列，在每个片段的合法窗口中取时间顺序居中的一个，双中位时取靠前者；再读取原始正常 201 的相同五帧作为对照。23 个当前帧固定为 15、43、71、99、127、155、183、211、239、267、295、323、351、379、407、435、463、491、519、547、575、603、650，共 46 个完整窗口。这是临时诊断子集，不是完整 201 验证，也不更改原有数据角色或片段世界。

两个模型均严格加载指定检查点中的全部参数和运行统计，不以新随机模型代替初始状态。对每个窗口，两者采用相同的 `23 + segment_index` 检查随机状态，复用训练入口的 `fixed_check`，前向后恢复随机状态并保护 BatchNorm 统计。全程使用已有推理精度路径，关闭参数梯度，不创建优化器、不反向传播；运行结束逐项比较模型参数与缓冲区，并重新核对两份源检查点的 SHA-256。

合成视图的主要 AP、AUROC、FPR95 使用保留的官方评价类：先对当前帧应用距离、忽略标签和至少五个有效异常点的资格规则，再把合格帧的点合并计算指标。FPR95 也保留源码中严格 `TPR > 0.95` 的取值方式，不改成插值或其他约定。逐窗 AP 中位数和改善／持平／下降数量单独记录，不替代官方汇总指标。不合格窗口保留全部预测、点数及原因，AP 记为 `null`。两种视图均记录全窗口类别平衡损失，缺失类别记为 `null`，其余类别均值照常计算。

原始正常视图不使用异常帧资格规则，而是保留当前帧中闭区间 2.5–50 米内的有效正常点，记录分数中位数、第 95 百分位及 `score >= 0.5` 的点占比。汇总分位数来自全部所选正常点合并后的分布，不能平均逐窗分位数。0.5 仅为事先固定的观察阈值，不是校准概率或部署阈值；同时记录逐窗结果和高分点占比最高的五个窗口。

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -u -m src.evaluate \
  --data-root /absolute/path/to/STU \
  --checkpoints runs/learn --output runs/transfer
```

`runs/transfer/samples.json` 在推理前保存固定样本清单、检查点哈希、源世界身份和执行设置；`results.jsonl` 保存 46 个窗口的配对结果；`summary.json` 保存官方汇总、正常对照、零更新检查和资源记录；`predictions/` 保存 92 份完整窗口分数与点身份。输入按窗口整理一次，供两份模型共用，推理后释放；源帧读取沿用原有有界缓存。资源分别统计数据入口初始化、检查点加载、窗口读取、体素输入整理、模型前向，以及损失／指标计算和预测保存的耗时。已有输出目录或位于训练证据目录内的输出路径均被拒绝，不覆盖 `runs/learn`。本次不生成 PLY，不读取真实异常数据。

本机已完成该次诊断，46 个输入窗口共 26,270,654 个点，92 份预测保留 52,541,308 条带身份的分数记录。模型参数、运行缓冲区和检查点文件均未改变，参数更新次数为 0。两份源文件 SHA-256 为：

- `initial.pt`：`3df06db08460dffb7457f5508bce6aed2364f5210752694bb943afe08a5badac`
- `final.pt`：`f56a11bfed7d00455f7aaf50dfd48a7c16d0dbc5e613fca62ffeabd372fcee5c`

合成视图中有 22 个当前帧符合官方资格，汇总包含 1,632,740 个正常点和 15,222 个异常点。127 的当前帧在官方范围内有 113,137 个有效正常点、0 个异常点，因此 AP 为不可评价；它没有被删除或替换，全窗口分数和损失仍保留。

| 合成 201 诊断指标 | 初始模型 | 200 步模型 |
| --- | ---: | ---: |
| 可评价窗口数 | 22 / 23 | 22 / 23 |
| 官方汇总 AP（%） | 0.8787 | 10.4395 |
| 官方汇总 AUROC（%） | 50.0775 | 30.4528 |
| 官方汇总 FPR95（%） | 95.7255 | 98.2019 |
| 逐窗 AP 中位数（%） | 0.5445 | 0.9040 |
| 全窗口正常类损失均值 | 0.659799 | 0.017752 |
| 全窗口异常类损失均值 | 0.727350 | 4.809743 |
| 全窗口总损失均值 | 0.693574 | 2.413748 |

三项损失均按全部 23 个五帧窗口计算；所有窗口都有全窗口异常标签，这不等同于当前帧具备官方评价资格。下面保留全部逐窗结果；官方汇总直接合并合格点和分数后重算，没有对逐窗 AP 求均值或加权均值。

| 当前帧 | 有效正常点 | 有效异常点 | 初始 AP（%） | 最终 AP（%） |
| ---: | ---: | ---: | ---: | ---: |
| 15 | 93,548 | 508 | 0.4816 | 36.0794 |
| 43 | 90,007 | 9 | 0.0112 | 0.0796 |
| 71 | 96,554 | 616 | 0.6486 | 5.7410 |
| 99 | 104,469 | 545 | 0.4484 | 0.2918 |
| 127 | 113,137 | 0 | 不可评价 | 不可评价 |
| 155 | 115,078 | 139 | 0.1032 | 90.4031 |
| 183 | 109,772 | 788 | 0.6200 | 42.3385 |
| 211 | 112,510 | 483 | 0.3634 | 0.2266 |
| 239 | 109,043 | 166 | 0.1764 | 13.0046 |
| 267 | 97,221 | 3,067 | 2.9251 | 1.7035 |
| 295 | 89,971 | 17 | 0.0728 | 0.0129 |
| 323 | 85,658 | 201 | 0.4143 | 0.1203 |
| 351 | 80,766 | 3,233 | 3.6079 | 1.9718 |
| 379 | 77,655 | 1,022 | 0.9012 | 0.6919 |
| 407 | 70,791 | 414 | 0.7632 | 0.5793 |
| 435 | 66,320 | 47 | 0.0720 | 0.0403 |
| 463 | 52,924 | 5 | 0.0124 | 0.2300 |
| 491 | 46,353 | 327 | 0.6073 | 0.3746 |
| 519 | 47,994 | 60 | 0.1136 | 0.0713 |
| 547 | 50,959 | 1,146 | 2.0359 | 1.1162 |
| 575 | 24,392 | 2,219 | 8.1942 | 55.4073 |
| 603 | 10,308 | 195 | 2.2044 | 58.6111 |
| 650 | 447 | 15 | 8.6600 | 2.3501 |

22 个可评价窗口中，AP 改善 9 个、持平 0 个、下降 13 个。AP 的汇总改善与 AUROC、FPR95 的退化同时存在，不能只展示 AP。回读保存预测得到的合格异常点分数中位数由 0.480109 降至 0.001525，说明大量新异常也获得了低分；这与异常类损失升高一致，但不能据此确定是道路、代理世界、观测阶段或模型机制中的哪个因素导致。

原始正常视图保留全部 23 个当前帧，共 1,756,370 个范围内有效正常点，单独统计如下。没有使用合成标签或其他窗口分数改变正常对照。

| 原始正常 201 诊断指标 | 初始模型 | 200 步模型 |
| --- | ---: | ---: |
| 分数中位数 | 0.479495 | 0.010184 |
| 分数第 95 百分位 | 0.502301 | 0.046347 |
| 分数不低于 0.5 的点数 | 117,022 | 199 |
| 分数不低于 0.5 的点占比（%） | 6.6627 | 0.011330 |

最终模型高分点占比最高的五个正常窗口为：

| 当前帧 | 有效正常点 | 初始高分点占比（%） | 最终高分点数 | 最终高分点占比（%） | 最终分数第 95 百分位 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 435 | 66,367 | 6.2441 | 93 | 0.140130 | 0.040598 |
| 323 | 85,859 | 7.4704 | 71 | 0.082694 | 0.054274 |
| 295 | 89,988 | 7.5166 | 12 | 0.013335 | 0.049021 |
| 351 | 82,895 | 5.9376 | 11 | 0.013270 | 0.056868 |
| 407 | 71,071 | 5.6028 | 8 | 0.011256 | 0.043611 |

因此，本次所选正常观测在固定 0.5 阈值下未出现高分误报扩张，但不能据此宣称达到部署要求。合成异常只显示局部迁移，尚未显示普遍可靠的跨序列异常区分；模型只训练过八个世界，本次也没有隔离新道路与新异常世界的影响，不能直接否定 LitePT、五帧观测或冻结合成池。随机初始模型仅是学习前参照，既不是竞争性基线，也不支持与此前 206 训练 AP 直接相减来量化纯粹的泛化损失。

实际运行总耗时 105.37 秒：数据入口初始化 0.16 秒、检查点加载 3.93 秒、窗口读取 19.31 秒、体素输入整理及传输 16.20 秒、初始／最终模型前向分别 10.69／9.15 秒、损失与逐窗指标计算及预测保存 38.82 秒；其余为资源检查、最终汇总和状态核对等开销。峰值已分配／已预留显存为 1.67／3.58 GiB，进程峰值常驻内存为 2.47 GiB。输出共 254,264,965 字节（约 242.49 MiB），未生成额外 PLY。

运行后已按真实窗口回读全部 92 份预测，重新调用官方评价类计算逐窗与汇总指标，并独立重算正常点范围、分位数和固定阈值计数；所有结果与记录一致。模型文件和执行源码哈希核验一致，正式数据资格文件未重写。运行前的六项针对性测试覆盖样本选择、官方点与帧规则、点级汇总区别、严格 FPR95 约定、随机状态恢复及零更新保护；未运行涉及优化器更新的测试或追加训练。

## 相同预算下的训练覆盖对照

本次对照检验整体训练覆盖扩展是否比重复少量世界更有利于跨序列合成迁移。A 使用原第 0 条 206 合成序列的八个窗口，当前帧仍为 27、83、139、195、251、307、363、448；B 使用八条冻结合成序列、每条 16 个片段的最后一个合法窗口，共 128 个。B 同时扩大异常世界与正常背景片段的覆盖，因此不能把差异单独归因于异常形状多样性。数据、异常代理、标签、5 厘米体素、网络结构、全窗口类别平衡损失和优化配方均保持不变。

两组均严格加载原 `runs/learn/initial.pt` 的模型参数、归一化统计和随机状态，各自新建 AdamW 与初值 128 的损失缩放器。每组在运行前固定 1,280 个计划步骤，每轮无放回排列全部本组窗口；没有跳步时，A 每窗访问 160 次，B 每窗访问 10 次。成功更新、溢出跳步、处理点数和体素数分别记录，不补跳步，不为了平衡计算量而裁点。原 200 步模型不作为继续训练的起点。

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -u -m src.train --coverage \
  --data-root /absolute/path/to/STU \
  --initial runs/learn/initial.pt \
  --validation-samples runs/transfer/samples.json \
  --output runs/coverage --workers 4
```

训练继续使用同一个 `src/train.py` 优化循环，A、B 顺序运行。完整窗口和确定性的体素输入只缓存在主存，每步把当前一个窗口传入 GPU，骨干特征每次重新计算。实际八窗对照中，一线程与四线程准备的坐标、身份、标签、全部体素张量及随机状态完全一致，耗时分别为 9.19 和 4.43 秒，因此正式准备使用四线程、底层数值库单线程。每次准备新窗口前检查可用主存是否至少为 3 GiB；预计磁盘峰值预算为 5 GiB，包含 320 份全窗口预测、四份模型／优化器状态和写入缓冲，并沿用 E 盘安全余量检查。

在 640 步保存 `step_0640.pt`，在 1,280 步保存 `final.pt`，均包含优化器、损失缩放器、随机状态与访问位置。每组最终对自身全部训练窗口做一次固定推理检查，保留全点预测、逐窗两类损失及汇总；此检查不改变参数或归一化统计。两组训练结束后，读取同一步数的 A／B 状态成对评价，以减少验证输入的重复准备。640 步仅观察过程，1,280 步是预先指定的主要比较位置，不根据中间结果改变训练或选择检查点。

评价直接复用 `src/evaluate.py`，从原 `runs/transfer/samples.json` 读取并核对同一批 23 对 201 窗口、异常世界和检查种子。主要结果保留官方点级汇总 AP、AUROC、FPR95、逐窗 AP 中位数及 B 相对 A 的改善／持平／下降数量；正常对照仍使用固定阈值 0.5，另保留全窗口两类损失、分数分布和最差正常窗口。127 不满足异常评价资格时继续保留，完整五帧预测不丢弃历史点、不融合未来窗口。201 已参与研究决策，应称为固定开发验证视图，不是盲测集或完整 201 验证。

`runs/coverage/plan.json` 在训练前保存样本、访问顺序、初始模型和开发验证清单身份。`A/`、`B/` 各自保存训练日志、检查点、最终训练子集预测及汇总；`check_0640/`、`check_1280/` 保存配对开发验证的完整预测与结果；根目录 `summary.json` 汇总两组执行状态及两次比较。原学习与迁移实验的检查点、清单和结果文件只读保留，不覆盖；本轮不读取真实异常序列。

本机正式执行已完成，两组均成功更新 1,280 次，溢出跳步为 0。实际日志与训练前访问清单逐项一致，A 每窗访问 160 次，B 每窗访问 10 次；两组首次更新均实际改变骨干与异常头参数。主存缓存经设备传输后与直接整理输入的完整预测差为 0。两次开发验证均完成全部 46 个窗口，推理没有改变模型参数或归一化缓冲区。

| 合成 201 指标 | A：640 步 | B：640 步 | A：1,280 步 | B：1,280 步 |
| --- | ---: | ---: | ---: | ---: |
| 可评价窗口数 | 22 / 23 | 22 / 23 | 22 / 23 | 22 / 23 |
| 官方汇总 AP（%） | 15.7315 | 84.8042 | 26.3963 | 94.4234 |
| 官方汇总 AUROC（%） | 65.4973 | 98.4831 | 77.8835 | 98.3152 |
| 官方汇总 FPR95（%） | 78.4344 | 4.6368 | 72.4618 | 0.7556 |
| 逐窗 AP 中位数（%） | 2.6393 | 92.7368 | 4.0230 | 98.0985 |
| 全窗口正常类损失均值 | 0.001115 | 0.011879 | 0.003339 | 0.002598 |
| 全窗口异常类损失均值 | 8.347956 | 1.908679 | 11.843606 | 2.935769 |
| 全窗口总损失均值 | 4.174536 | 0.960279 | 5.923472 | 1.469183 |
| 官方合格异常点分数中位数 | 0.000092353 | 0.999517 | 0.000001006 | 0.999987 |

每列的官方汇总均包含同样的 1,632,740 个正常点和 15,222 个异常点；帧 127 的当前帧无合格异常点，始终记为不可评价，完整窗口预测与损失仍保留。两次检查中，B 相对 A 均为 21 个逐窗 AP 改善、0 个持平、1 个下降，另有 1 个不可评价窗口。以下为预先指定的 1,280 步逐窗比较，不以 640 步替换主结果。

| 当前帧 | A 的 AP（%） | B 的 AP（%） |
| ---: | ---: | ---: |
| 15 | 71.0894 | 99.9886 |
| 43 | 0.0139 | 0.0137 |
| 71 | 10.0523 | 99.9976 |
| 99 | 0.3993 | 79.4152 |
| 127 | 不可评价 | 不可评价 |
| 155 | 95.3416 | 100.0000 |
| 183 | 58.1924 | 99.9076 |
| 211 | 0.5184 | 99.9867 |
| 239 | 65.4839 | 98.5990 |
| 267 | 53.5143 | 99.9999 |
| 295 | 0.0856 | 7.0407 |
| 323 | 0.2092 | 1.1939 |
| 351 | 5.2926 | 99.9968 |
| 379 | 2.2928 | 80.3819 |
| 407 | 2.7535 | 97.5980 |
| 435 | 0.1993 | 22.7017 |
| 463 | 0.1200 | 0.1432 |
| 491 | 22.5502 | 99.9871 |
| 519 | 0.1109 | 100.0000 |
| 547 | 48.1272 | 99.9936 |
| 575 | 81.3117 | 90.9837 |
| 603 | 84.1015 | 93.5573 |
| 650 | 2.7007 | 2.7362 |

原始正常 201 每列均包含相同的 1,756,370 个有效当前帧点，固定阈值没有重新调整。

| 正常 201 指标 | A：640 步 | B：640 步 | A：1,280 步 | B：1,280 步 |
| --- | ---: | ---: | ---: | ---: |
| 分数中位数 | 0.000066323 | 0.001132 | 0.000000077253 | 0.000010650 |
| 分数第 95 百分位 | 0.000264936 | 0.006079 | 0.000003953635 | 0.000135844 |
| 分数不低于 0.5 的点数 | 456 | 3,082 | 1,211 | 999 |
| 分数不低于 0.5 的点占比（%） | 0.025963 | 0.175476 | 0.068949 | 0.056879 |

B 在 640 步时的高分占比高于 A，不能隐去这个过程结果；到主要比较位置，B 的总体高分占比低于 A，但正常分数中位数和第 95 百分位仍高于 A，且有 7 个窗口的高分占比局部增加。B 在 1,280 步时高分占比最高的五个正常窗口如下，不能把总体平均值当作所有道路位置都没有误报。

| 当前帧 | 有效正常点 | A 高分占比（%） | B 高分点数 | B 高分占比（%） | B 分数第 95 百分位 |
| ---: | ---: | ---: | ---: | ---: | ---: |
| 407 | 71,071 | 0.393972 | 438 | 0.616285 | 0.000163592 |
| 323 | 85,859 | 0.323787 | 265 | 0.308646 | 0.000147091 |
| 379 | 78,576 | 0.483608 | 84 | 0.106903 | 0.000147868 |
| 239 | 109,209 | 0.000000 | 80 | 0.073254 | 0.000125134 |
| 295 | 89,988 | 0.132240 | 37 | 0.041117 | 0.000134750 |

最终固定推理时，A 对自身八窗的正常／异常类损失均值为 0.000007380／0.083783，B 对自身 128 窗为 0.000152780／0.016719；对应异常类损失中位数为 0.058957／0.001224。这支持 B 已经学会其大部分训练样本，当前残余失败不能简单解释为 128 窗完全没有学动。两个训练子集不同，这些损失不能作为谁的训练成绩更优的公平比较。

主要结果支持：在同一初始化、优化配方、成功更新次数和固定开发验证视图下，整体训练覆盖扩展带来了超出单纯延长八窗训练的迁移收益。B 相对等预算 A 的 AP 高 68.0271 个百分点，改善覆盖多数窗口，AUROC 与 FPR95 同时更好。旧 200 步模型的 AP 为 10.4395%，A 延长至 1,280 步也提高到 26.3963%，说明更多训练本身有帮助，但不能解释 B 相对 A 的差异。

这仍有明确边界：B 的 43、295、323、435、463、650 等窗口表现仍差；全窗口异常类损失并未随 640 到 1,280 步单调降低，AUROC 也从 98.4831% 小幅降至 98.3152%。历史点、当前帧、范围筛选和窗口均值的统计范围不同，不能用较高汇总 AP 抹去这些失败。该实验只使用一个初始化，B 同时增加代理世界和背景片段覆盖，201 又已经参与研究决策；它不证明最终真实异常泛化、五帧优于单帧或超过 STU 基线，不据此开放真实异常序列。

| 实际训练资源 | A | B |
| --- | ---: | ---: |
| 处理点数 | 801,541,920 | 801,433,720 |
| 处理体素数 | 432,893,440 | 423,659,710 |
| 训练步骤合计耗时（秒，含传入 GPU） | 308.91 | 307.21 |
| 其中输入传输（秒） | 6.02 | 6.73 |
| 一次性输入准备（秒） | 4.80 | 67.99 |
| 本组总耗时（秒，含最终训练子集检查） | 345.54 | 479.57 |
| 训练峰值已分配／已预留显存（GiB） | 6.03 / 6.99 | 6.04 / 7.46 |

两次配对开发验证各耗时 100.78／101.03 秒，全部正式执行共 1,028.13 秒（约 17.14 分钟）。训练没有中断、重试、溢出跳步或显存不足。全过程的进程峰值常驻内存为 18.39 GiB；B 主存缓存完成后约有 2.5 GiB WSL 内存可用，WSL 交换空间占用未增加，但宿主机内存余量偏小，监测时 Windows 分页文件分配量约为 19 GiB。E 盘余量一度由约 34 GiB 降至约 24 GiB，不能忽略宿主机空间变化；用户随后授权删除已查看的全部 PLY，Windows 侧空间延迟回收，后续检查均满足原磁盘预算。手动空闲块回收因需要管理员密码未执行，没有关闭 WSL 或修改分页文件。

本轮共保存 320 份完整窗口预测：两次 A／B 配对开发验证 184 份，加最终训练子集检查 136 份；全部实验文件共 1,572,979,040 字节（约 1.46 GiB）。删除的 PLY 共 49,452,943,694 字节（约 46.06 GiB），仅移除可重新导出的可视化文件，不影响上述预测或冻结数据。

执行后已按实际冻结窗口回读全部 320 份预测，共 190,235,625 条带身份的点分数，核对完整行顺序、帧号、槽位、当前帧掩码、文件与内容哈希及全窗口分数统计。独立重新调用保留的官方程序计算逐窗和汇总指标，并单独重算正常点筛选、分位数及固定阈值计数，均与记录完全一致；两组训练及开发验证的损失汇总也与逐窗记录一致。两组 1,280 条训练记录与事先保存的访问顺序逐项相同，初始模型、开发验证清单和实际执行源码身份未变。以上核验没有追加模型前向、参数更新、数据生成或真实异常访问。

四份检查点的 203 个优化器参数状态均具有相应的 640／1,280 次更新计数，保存的访问位置、AdamW 参数及源学习检查点哈希核验一致。代码回归为 51 项通过、1 项未启用的完整冻结窗口 GPU 测试跳过；本轮真实训练和配对评价已另行实际执行，不能把测试通过本身当作迁移收益的证据。

## 完整 201 零更新开发验证

本轮固定 `runs/coverage/B/final.pt`，即扩大覆盖组原定 1,280 步结束时的状态，模型内容哈希为 `22678e6bf7ef40f66b35a8cd836993e2d1aff500090307353f6b86335ea5dce6`。同时加载该状态的归一化运行统计，不创建优化器，不反向传播，不校准分数，不追加训练，不比较其他检查点。模型、5 厘米体素、标签和冻结异常世界保持不变。

验证读取四条冻结合成 201 的全部合法窗口，每条 590 个，共 92 个世界、2,360 个窗口；合成窗口不跨片段。原始正常 201 完整读取 0–681 帧，仅运行一遍，输出 4–681 的 678 个窗口，保留开头重复槽位。总计 3,038 个窗口，全部保存五帧完整预测及点身份，在线指标只使用当前帧，不融合其他窗口的分数，不生成 PLY，也不读取真实异常序列。

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -u -m src.evaluate --full \
  --data-root /absolute/path/to/STU \
  --checkpoint runs/coverage/B/final.pt --output runs/validation
```

`src/evaluate.py` 的完整验证使用与局部配对检查相同的前向、类别平衡损失、官方点筛选和逐窗评价函数。合成检查种子固定为 `23 + 23 * sequence_index + segment_index`；正常窗口使用当前帧所在片段的 `23 + segment_index`。因此原第 0 条合成版本中 23 窗及其正常对照的种子保持不变。每次检查恢复随机状态并检查归一化缓冲区，结束时逐项检查模型参数与缓冲区没有改变。

输入处理按时间顺序进行，合成帧缓存最多五帧，原始源帧缓存最多 16 帧。一个后台线程最多准备下一个完整窗口，GPU 每次仅推理一个窗口，另一个线程最多保留一个待写结果。窗口释放后不保留历史点分数数组，不缓存骨干特征。预测 NPZ 采用一级无损压缩，科学内容哈希、点身份与分数不变；冻结数据池文件不重新压缩或改写。三个实际先导窗口的压缩耗时由九级的 0.25–0.55 秒降至 0.06–0.13 秒，文件大小增加约 0.5%–0.6%。

官方评价继续使用当前帧 2.5–50 米内非忽略点，异常点不足五个的窗口不进入合成指标汇总，但预测、损失、点数及不可评价原因全部保留。精确汇总使用磁盘暂存和原位排序：每个必要的当前帧评价记录保存原始 `float32` 分数位模式和二分类标签，不做分箱或取整；相等分数按同一阈值统计。分块累计精确率—召回率及 ROC 曲线，并保留官方默认删除共线 ROC 中间点后严格 `TPR > 0.95` 的行为。磁盘排序副本在一次汇总结束后释放，不保留无用途的临时文件。正常点的中位数、第 95 百分位及固定 0.5 阈值计数也从精确排序结果计算。

运行前，旧 23 窗真实预测的官方逐点计算与磁盘分块汇总得到完全相同的 AP 94.42336702020746%、AUROC 98.31521187709309%、FPR95 0.7556010142459914%；同一批 1,647,962 个点的指标计算分别耗时 0.502 和 0.076 秒。并列分数、跨分块并列分数、共线 ROC 点及严格召回门槛另有针对性检查。单线程和后台线程准备的完整坐标、标签、体素张量与随机状态一致；三个实际窗口的直接 GPU 输入与主存准备后传输的预测差均为零，其中第 15 帧与旧预测也完全一致。最大的开头正常窗口包含 1,163,892 个点，先导检查峰值显存约 1.60 GiB，没有裁点。

结果写入新的 `runs/validation/`：`samples.json` 保存完整样本、模型、冻结世界和执行身份；`results.jsonl` 保存全部逐窗结果及预测位置；`predictions/` 保存 3,038 份全窗口预测；`current/` 只保存必要的当前帧精确评价记录；`worlds.json` 保存 92 个世界的汇总与逐窗 AP；`summary.json` 保存完整结果、子范围和资源记录。旧学习、迁移和覆盖对照证据只读保留。

合成主结果按合格点汇总，并另列原 23 窗、第 0 条版本其余 567 窗、第 1–3 条版本的 1,770 窗，以及四条版本各自的结果。全窗口类别平衡损失保持原公式；异常损失额外按全部点、历史点、当前帧全部点及当前帧官方距离范围内的点分开，均直接由 logits 计算，分别保存点数、损失总和、按点均值和按窗均值。该距离范围统计不以当前帧是否达到五个异常点为前提。重复出现的历史观测按所在窗口分别统计，不假定重叠窗口相互独立。

正常 201 单独报告精确的全局和逐帧分数统计，并保留最差窗口与 397–417 帧的连续输出，不把纯正常数据混入合成 AP。201 已参与开发决策，本轮是完整开发验证，不是盲测；单一模型的完整验证也不能替代 A 与 B 在全量数据上的对照，不能推出五帧优于单帧或真实异常指标超过基线。

### 完整验证结果

本机正式执行已完成全部 3,038 个窗口，优化器更新数为零，模型参数、归一化缓冲区、源模型文件及旧样本清单均未改变。合成部分有 2,278 个合格窗口；另外 82 个窗口中，57 个没有官方范围内异常点，25 个仅有 1–4 个，均保留完整预测、标签计数、损失和不可评价原因。92 个世界均至少具有一个合格窗口。

| 合成评价范围 | 合格窗口 / 全部窗口 | AP（%） | AUROC（%） | FPR95（%） | 逐窗 AP 中位数（%） |
| --- | ---: | ---: | ---: | ---: | ---: |
| 全部合成池 | 2,278 / 2,360 | 93.3609 | 98.3946 | 2.5228 | 97.8908 |
| 原 23 窗 | 22 / 23 | 94.4234 | 98.3152 | 0.7556 | 98.0985 |
| 第 0 条版本其余窗口 | 548 / 567 | 93.5183 | 98.4470 | 2.0422 | 96.0330 |
| 第 1–3 条版本 | 1,708 / 1,770 | 93.2912 | 98.3752 | 2.7200 | 98.3170 |

完整合成主指标包含 162,980,036 个正常点和 1,207,724 个异常点。原 23 窗的完整预测、两类损失和指标与上轮完全相同。其余同世界窗口和其他合成版本的 AP 仍接近 93%，没有出现离开原选定时刻后整体成绩大幅下降的现象；但完整 FPR95 高于原 23 窗，不能只用 AP 接近来宣称所有评价条件均保持不变。

| 合成版本 | 合格窗口 / 全部窗口 | AP（%） | AUROC（%） | FPR95（%） | 合格异常点 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 0 | 570 / 590 | 93.5560 | 98.4412 | 1.9640 | 348,630 |
| 1 | 570 / 590 | 91.9402 | 98.4323 | 4.7188 | 301,000 |
| 2 | 570 / 590 | 90.8581 | 97.8638 | 11.8951 | 247,215 |
| 3 | 568 / 590 | 96.4513 | 98.7266 | 0.0510 | 310,879 |

四条版本均保持较高的点级 AP，但高召回位置上的误报差异较大，尤其第 2 条版本的 FPR95 达到 11.8951%。这些版本共享同一条 201 背景，不是四条独立道路。

### 逐世界结果与残余失败

以下每格为该世界的官方汇总 AP（%），括号内为合格窗口数 / 全部合法窗口数。片段编号从零开始，与冻结清单一致。

| 片段编号 | 合成版本 0 | 合成版本 1 | 合成版本 2 | 合成版本 3 |
| ---: | ---: | ---: | ---: | ---: |
| 0 | 99.9657（24/24） | 99.9934（14/24） | 95.3022（24/24） | 41.9487（24/24） |
| 1 | 0.0192（21/24） | 77.9244（22/24） | 100.0000（16/24） | 99.9788（24/24） |
| 2 | 94.7365（24/24） | 89.3363（24/24） | 0.0290（24/24） | 50.9392（24/24） |
| 3 | 71.9802（24/24） | 99.9266（24/24） | 99.1499（24/24） | 92.0847（24/24） |
| 4 | 99.9956（17/24） | 98.6523（24/24） | 96.2094（24/24） | 86.8016（24/24） |
| 5 | 94.8805（24/24） | 87.6299（24/24） | 3.2481（22/24） | 99.9912（24/24） |
| 6 | 99.8858（24/24） | 99.8020（24/24） | 80.1644（24/24） | 99.9073（24/24） |
| 7 | 99.4640（24/24） | 99.5587（24/24） | 98.9523（24/24） | 99.9530（24/24） |
| 8 | 91.4010（24/24） | 76.8982（24/24） | 0.0696（22/24） | 95.9832（24/24） |
| 9 | 99.9985（23/24） | 92.7689（24/24） | 99.9988（24/24） | 99.2571（24/24） |
| 10 | 7.5381（23/24） | 99.6021（24/24） | 99.3882（24/24） | 99.9740（24/24） |
| 11 | 18.7519（24/24） | 24.6802（24/24） | 98.1979（24/24） | 93.5334（24/24） |
| 12 | 99.8178（24/24） | 91.4498（24/24） | 59.1050（24/24） | 60.5813（24/24） |
| 13 | 47.3770（24/24） | 0.3958（24/24） | 98.6459（24/24） | 0.4070（24/24） |
| 14 | 94.7159（24/24） | 54.8201（24/24） | 0.3125（24/24） | 99.8507（24/24） |
| 15 | 16.6946（24/24） | 99.9980（24/24） | 10.7250（24/24） | 99.9655（24/24） |
| 16 | 0.7990（21/24） | 99.1964（18/24） | 99.3134（24/24） | 99.9897（24/24） |
| 17 | 99.9829（24/24） | 99.9999（24/24） | 48.2861（24/24） | 8.5077（24/24） |
| 18 | 84.3448（24/24） | 98.5966（24/24） | 82.8501（24/24） | 62.0435（24/24） |
| 19 | 99.8728（24/24） | 76.0163（24/24） | 99.9980（24/24） | 99.9932（24/24） |
| 20 | 91.1417（24/24） | 99.7619（24/24） | 99.9889（19/24） | 99.9996（24/24） |
| 21 | 74.2049（24/24） | 66.6391（24/24） | 38.3085（24/24） | 4.1716（9/24） |
| 22 | 1.2367（57/62） | 2.2037（60/62） | 2.9309（59/62） | 6.3378（55/62） |

92 个世界的 AP 中位数为 94.1246%，第 25 百分位为 50.2759%。作为描述性统计，有 15 个世界的 AP 低于 10%，占世界数的 16.30%，但这些世界只有 13,583 个合格异常点，占全部合格异常点约 1.12%。这里的 10% 仅用于呈现分布，不作为新的模型选择门槛。结果仍显示少点世界的失败容易被点级汇总弱化，不能把 93.36% AP 解释为每个世界都已可靠识别。

| 原检查时刻（版本 0） | 所在世界的全部输出时刻 | 合格窗口 | 世界 AP（%） | 逐窗 AP 中位数（%） |
| ---: | --- | ---: | ---: | ---: |
| 43 | 32–55 | 21 / 24 | 0.0192 | 0.0137 |
| 323 | 312–335 | 24 / 24 | 18.7519 | 16.5288 |
| 463 | 452–475 | 21 / 24 | 0.7990 | 0.1432 |
| 650 | 620–681 | 57 / 62 | 1.2367 | 1.7354 |

帧 43 所在世界在整个合格观测阶段都很差。323 所在世界并非所有时刻同样失败，例如 325 的 AP 为 90.98%，但多数时刻仍较差。463 所在世界在末端出现局部改善，例如 472 达到 68.75%，整体世界汇总仍低。650 所在世界在大部分连续观测中持续失败；末尾 677–681 无官方范围内异常点，记为不可评价而非零分。此外，最后一个 66 帧片段在四条合成版本中的世界 AP 分别为 1.24%、2.20%、2.93%、6.34%。这提示应保留共同观测条件下的失败，但尚不能据此认定是距离、背景、代理形状或体素混合造成的。

### 异常损失的范围

原有全窗口类别平衡损失仍按各窗口分别计算。完整合成池的正常类均值为 0.00318126，异常类均值为 3.34039484，总损失均值为 1.66471111；异常类均值只包括有异常点的 2,350 个窗口，其余 10 个无全窗口异常点的样本仍使用正常类损失。

| 异常点范围 | 原 23 窗点数 | 原 23 窗损失总和 | 原 23 窗按点均值 | 全合成池点数 | 全合成池损失总和 | 全合成池按点均值 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 五帧全部异常点 | 73,896 | 50,824.553758 | 0.687785 | 6,703,100 | 4,512,368.873370 | 0.673176 |
| 历史四帧异常点 | 58,674 | 40,428.287399 | 0.689032 | 5,292,319 | 3,580,697.981291 | 0.676584 |
| 当前帧全部异常点 | 15,222 | 10,396.266359 | 0.682976 | 1,410,781 | 931,670.892079 | 0.660394 |
| 当前帧官方距离范围内异常点 | 15,222 | 10,396.266359 | 0.682976 | 1,207,799 | 904,695.304459 | 0.749045 |

原 23 窗的异常类按窗均值仍为 2.93576865；历史点的按窗均值为 2.95714709，当前帧及当前帧官方范围内均为 2.59944025。当前帧这两种范围中的异常点完全相同。因此，原先较高的异常损失不能全部归因于历史点或范围外点。按窗均值与按点均值具有不同权重，不能将 2.9358 与 0.6878 当作同一种平均量比较。

完整合成池中，历史、当前帧、当前帧官方范围内的按窗均值分别为 3.33929270、3.34973772、3.32196228；按点均值同样没有显示历史点独自承担高损失。当前帧官方范围内损失统计的点数比正式指标多 75，这是 25 个异常点不足五个的窗口中的异常点：损失分项不施加帧资格门槛，正式 AP 仍跳过这些帧。上述计数是窗口中的观测次数，重复历史观测没有被解释成独立物理点。

### 完整正常序列

原始正常 201 的 678 个当前帧共包含 48,828,507 个范围内有效正常点。分数中位数为 0.0000108146，第 95 百分位为 0.000137262；35,992 个点的分数不低于固定阈值 0.5，占 0.073711%。此前 23 个正常窗口的占比为 0.056879%，两者是不同覆盖范围的描述，不是重新调整阈值后的比较。

| 当前帧 | 有效正常点 | 分数不低于 0.5 的点 | 占比（%） |
| ---: | ---: | ---: | ---: |
| 372 | 78,811 | 1,301 | 1.6508 |
| 373 | 78,913 | 1,237 | 1.5675 |
| 408 | 71,052 | 676 | 0.9514 |
| 371 | 78,641 | 677 | 0.8609 |
| 368 | 78,398 | 672 | 0.8572 |

407 附近的高分点并非只出现在一个时刻。下表保持原始连续在线输出，不融合窗口分数。

| 当前帧 | 分数不低于 0.5 的点 | 占比（%） |
| ---: | ---: | ---: |
| 397 | 57 | 0.0786 |
| 398 | 111 | 0.1538 |
| 399 | 333 | 0.4618 |
| 400 | 190 | 0.2636 |
| 401 | 158 | 0.2194 |
| 402 | 93 | 0.1296 |
| 403 | 107 | 0.1497 |
| 404 | 350 | 0.4898 |
| 405 | 122 | 0.1709 |
| 406 | 143 | 0.2008 |
| 407 | 438 | 0.6163 |
| 408 | 676 | 0.9514 |
| 409 | 544 | 0.7659 |
| 410 | 235 | 0.3314 |
| 411 | 318 | 0.4496 |
| 412 | 532 | 0.7536 |
| 413 | 412 | 0.5840 |
| 414 | 289 | 0.4111 |
| 415 | 408 | 0.5820 |
| 416 | 340 | 0.4864 |
| 417 | 409 | 0.5868 |

全局正常分数仍较低，但 368–374 和 407 附近存在连续局部误报，不能以全局 0.0737% 的占比代替逐帧检查。本轮没有给这些比例另设通过门槛，也未将 0.5 解释为校准概率或部署阈值。

### 执行资源与核验

正式运行共 1,717.13 秒，约 28.62 分钟，其中全部推理和保存完成于 1,697.23 秒，随后汇总约 19.90 秒。累计窗口读取时间为 336.71 秒，联合体素准备为 1,084.36 秒，输入传输为 18.16 秒，前向调用及同步为 1,462.33 秒，评分和写入为 1,425.52 秒。这些调用墙钟区间因输入预取、推理和写入并行而重叠，不能相加，也不能把前向区间视为纯 GPU 内核时间。

推理阶段进程主存峰值约 3.02 GiB，全程含磁盘排序为 4.12 GiB。PyTorch 峰值已分配显存为 1.77 GiB，峰值预留显存为 10.36 GiB；显卡监测曾约为 12,128 MiB，包含显示占用与其他缓存，不能把已分配张量峰值当作整卡使用量。没有显存不足、数值错误、中断或重试。

运行前 E 盘约有 70.31 GiB 剩余，32 GiB 的保守写入峰值预算包含全部预测、当前帧记录、排序副本和写入缓冲；安全余量约为 22.58 GiB。监测到的 E 盘余量一度约为 53.22 GiB，结束后的临时空间回收及宿主状态变化后约为 62.73 GiB。WSL 交换空间保持 91 MiB，但宿主分页文件分配量曾由 18 GiB 增到约 20 GiB，观察到的使用量达到约 2.38 GiB；因此不能宣称宿主完全没有分页活动。没有手动调整分页文件、重启 WSL 或清理原实验数据。

本轮保存 3,038 份全窗口预测，共 1,749,852,566 条带窗口及点身份的分数；完整结果目录为 10,595,800,578 字节，约 9.87 GiB。原 46 份合成／正常局部检查的完整预测与本轮对应预测逐元素完全相同，最大分数差为零。

执行后已从冻结数据重新构建全部 3,038 个窗口，回读预测并逐行核验身份、顺序、当前帧掩码、文件及内容哈希、评价记录、标签计数和损失范围的计数。每窗 AP 和全部 92 个世界指标用保留的官方程序独立复算一致；完整正常分布及阈值计数由回读分数独立重算一致。该回读核验耗时 331.53 秒，没有增加模型前向或参数更新。

另用独立的异常点阈值累计计算 AP、正负点排序关系计算 AUROC，并单独检查官方 ROC 共线点删除后的高召回位置，对完整池、三个子范围和四条合成版本共八个范围重新核验。最大指标差为 $8.53\times10^{-14}$ 个百分点，FPR95 一致；实际执行源码、模型和旧样本清单哈希也未变。独立秩统计核验耗时 10.08 秒，部分与预测回读并行。损失值来自正式前向的 logits，核验没有从饱和分数反推 logits。

完整代码回归为 58 项通过、1 项未启用的完整冻结窗口 GPU 测试跳过；本轮 3,038 个真实冻结窗口已另外实际执行。静态检查通过。单元测试支持实现正确性，科学判断依据上述实际输入、预测与独立指标核验。

这些结果支持将 B 保留为具有完整合成开发验证证据的工作基线：其迁移能力并不限于原 23 个时刻或第 0 条合成版本。但仍不能宣称全部异常世界可靠、正常道路没有连续误报，或已在真实异常上超过 STU 基线。当前结果没有支持“同世界其他时刻普遍崩溃”这一判断，也没有把时间覆盖不足、远距离、代理形状或网络结构确定为残余失败原因。后续单帧／五帧贡献及针对性改进应另行设计受控对照，本轮不启动。

## 运行时几何输入

`artifacts/calibration.pt` 由 206 全部 449 帧建立，射线参数来源为 `artifacts/calibration_source.npz`。206 支撑面池 `artifacts/training_206_support_pool.npz` 覆盖 0–448，锚点范围为 2–446。各文件由 `protocol.json` 中的哈希绑定。

201 的活动支撑面池必须覆盖完整 0–681，锚点范围为 2–679。首次建立时执行：

```bash
python src/prepare.py support-validation \
  --data-root /absolute/path/to/STU --processes <按实测资源确定>
```

程序写出 `artifacts/validation_201_support_pool.npz` 并打印文件 SHA-256，应与 `protocol.json` 对应字段一致。该文件覆盖 0–681，包含 1,210,186 个合格支撑记录和 640 个实际产生合格记录的锚点帧。

201 帧 0–3 的文件槽数分别为 393,216、393,216、291,328 和 262,144，它们包含协议文档记录的精确重复槽。复现时不得先行删除或去重；读取和渲染程序会校验 `xyzi`、标签和冻结的多对一射线布局。

## 先导生成

正式生成前，只选择一个训练片段写入 `runs/`，测量耗时、内存和稀疏文件大小：

```bash
python src/data.py generate --pool train \
  --data-root /absolute/path/to/STU \
  --sequence-indices 0 --segment-indices 0 \
  --output-directory runs/pilot
```

先导必须确认固定根种子可重复、未命中异常的原始槽位逐位不变、异常标签与新回波同步、片段内每帧只渲染一次、窗口引用相同的渲染帧。先导结果不能加入正式清单，检查结束后应从 `runs/pilot` 删除。

每段必须严格包含一个异常代理物体。生成端、冻结片段读取端和资格检查都执行这一要求。

物体放置采用 `PROTOCOL.md` 中的尾部优先规则。资格结果记录末帧可见性和物理回退情况，不因窗口没有异常回波而删除窗口。

## 正式池生成

正式生成前必须重新检查 CPU、内存、交换空间、GPU 占用、其他实验进程和 Windows E 盘实际剩余空间。若预计峰值新增超过 1 GiB，应从 Windows 侧执行：

```powershell
Get-Volume -DriveLetter E | Select-Object Size,SizeRemaining
```

生成期间须保留 E 盘总容量 5% 与 10 GiB 中较大的安全余量。当前实现按片段保存稀疏变化，不复制未变化的官方点，也不为重叠窗口重复保存点数组。

确认资源安全后，生成完整池。本轮实测资源允许使用 6 个进程，复现时应重新确定适用的进程数：

下面的生成与建立清单命令用于首次建池阶段。当前池已冻结，不再对正式路径运行这些命令；读取既有池使用上面的训练数据入口。若需复现原始生成文件，应使用原资格证据记录的生成实现，不能把当前消费接口的源码哈希替换进原清单。

```bash
python src/data.py generate --pool train \
  --data-root /absolute/path/to/STU --workers 6
python src/data.py manifest --pool train

python src/data.py generate --pool validation \
  --data-root /absolute/path/to/STU --workers 6
python src/data.py manifest --pool validation
```

中断后只能使用 `--resume` 复用已经存在且身份完全一致的正式片段。它不会更换根种子，也不会覆盖内容冲突的文件。训练池应包含 128 个片段世界和 3080 个窗口；合成验证池应包含 92 个片段世界和 2360 个窗口。

## 冻结完成条件

生成完整池后执行：

```bash
python src/qualify.py --data-root /absolute/path/to/STU
```

当前状态为 `frozen`，该命令默认只读复核，不重写 `artifacts/data/qualification.json`。如需另外保存结果，指定尚不存在的路径，例如 `--output runs/qualification.json`；指向冻结资格文件或任意已有文件时都会拒绝执行。复核重新检查已有世界；同种子确定性检查只在内存重放既定首段，不重抽种子、不挑选替代世界，也不写回数据池。两份冻结清单同样禁止覆盖。

需要逐窗口人工核查时，可在同一次检查中导出完整点云：

```bash
python src/qualify.py --data-root /absolute/path/to/STU \
  --ply-directory artifacts/ply
```

该选项导出训练池的 3080 个窗口和合成验证池的 2360 个窗口，不导出原始正常 201。路径为 `artifacts/ply/{train,validation}/sequence_000/window_000004.ply` 等，文件名中的帧号为当前帧。每个二进制 PLY 保留五帧全部可见点和当前帧 LiDAR 坐标；灰色 `(160,160,160)` 为正常，红色 `(255,0,0)` 为异常，蓝色 `(0,128,255)` 为官方忽略。颜色只用于核查，不能作为模型特征。

完整 PLY 约占 45.93 GiB，导出前必须按实际点数和 Windows E 盘剩余空间重新确认安全余量，运行期间持续监测。已有且内容完全一致的文件可复用，冲突文件不会被覆盖。PLY 不纳入 Git，也不作为模型输入；正式数据始终来自冻结的稀疏片段文件。

此前另有用户授权的真实异常目视样本：`artifacts/ply/real/125/window_000004.ply` 至 `window_000018.ply`，共 15 个文件、141,149,315 字节（134.61 MiB），完成查看后已按用户要求删除。选择协议列出的第一条真实序列 `val/125`，取最早连续 15 个当前帧均有可见异常的窗口，仅需读取第 0–18 帧点云和标签。导出复用 `STUSequence`、`WindowPartition(sequence, 4, 18)` 和 `src/qualify.py` 的 `_write_window_ply`，未修改数据读取或导出实现。坐标约定和配色与合成窗口相同，未下采样；每窗有 120–126 个异常点。15 个 PLY 均已回读并逐行核对坐标与官方真值颜色，未运行模型、计算性能指标或修改任何合成数据。删除可视化文件不改变该片段已经被人工查看的事实。

模型无关资格程序必须进一步核对所有正式片段、重建帧、窗口和原始 201 的 678 个在线输出。只有以下三个文件全部生成且通过独立检查后，才允许把其 SHA-256 写入协议：

```text
artifacts/data/train_manifest.json
artifacts/data/validation_manifest.json
artifacts/data/qualification.json
```

随后才可把协议状态改为 `frozen`，并同步设置 `data_pool_frozen=true`、`training_allowed=true`、`validation_tuning_allowed=true`。`real_anomaly_access_allowed` 仍保持 `false`，直到模型和选择规则另外完成冻结。
