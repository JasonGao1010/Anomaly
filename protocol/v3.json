{
  "format": "ajae-v3-learning",
  "base_config": "protocol/pretrain.json",
  "scope": "V3: point detail enters LitePT; observation-conditioned fusion of its five levels and one pointwise score. Unchanged paired binary population risk; no training until explicitly requested.",
  "warm_start": {"checkpoint": "results/keep/mean/1152.pt", "step": 1152},
  "selection_from": "protocol/micro.json",
  "replacements_from": "results/synthetic/manifest.json",
  "maximum_updates": 1024,
  "checkpoint_steps": [128, 256, 384, 512, 640, 768, 896, 1024],
  "model": {
    "backbone": "LitePT-S",
    "channels": 64,
    "relation_mode": "pyramid",
    "attention_heads": 4,
    "voxel_m": 0.05,
    "query_chunk": 2048,
    "checkpoint_fusion": true,
    "backbone_drop_path": 0.0,
    "backbone_shuffle_orders": false,
    "intensity_transform": "identity_raw_stu_no_clip",
    "backbone_batchnorm": "fixed_parent_running"
  },
  "training": {
    "seed": 20260916,
    "batch_frames": 2,
    "accumulation_steps": 4,
    "workers": 4,
    "learning_rate": 0.0001,
    "inherited_learning_rate": 0.000005,
    "backbone_learning_rate": 0.000002,
    "weight_decay": 0.01,
    "gradient_clip": 1.0,
    "save_every": 128,
    "sampling": "results/v3/populations.npz",
    "binary_view": "official_range_v3",
    "conditions": {"radius_m": 2.0, "minimum_neighbors": 8},
    "group_queries": {"normal": [4096, 2048, 2048], "raw": [2048, 1024, 1024], "weights": [0.5, 0.25, 0.25]},
    "anomaly_queries": 2048,
    "augmentation": "none_preserve_physical_sensor_conditions",
    "learning_rate_schedule": {"kind": "v3_adapt_joint", "freeze_updates": 128, "warmup_updates": 32, "decay_start": 160, "total_updates": 1024, "start_factor": 0.1, "end_factor": 0.1}
  },
  "loss": {"kind": "paired_binary_population", "coefficients": [0.5, 0.25, 0.25]},
  "coverage_mixture": 0.2,
  "diagnostic_from": "results/v2_diagnosis",
  "reference_directory": "results/v3/reference",
  "evaluation": {
    "synthetic_steps": [256, 512, 1024],
    "real_steps": [256, 512, 1024],
    "paired_normal_steps": [256, 512, 1024],
    "synthetic_splits": ["train", "validation"],
    "full_val19_steps": [256, 512, 1024],
    "normal201_steps": [256, 512, 1024],
    "full_synthetic_steps": [],
    "normalization_steps": [],
    "binary_view": "official_range_v3",
    "primary": "Same official complete val19 labels and pooled ranking. Compare AP, FPR95 and R@1%FPR jointly with parent1152; disclose AUROC and non-dominated tradeoffs. Fixed152/108 reuse these predictions."
  },
  "optimization_monitor": {"every_updates": 32, "gradient_alert": 1000.0, "retained_alerts": 3},
  "stop": "1024 optimizer updates,8192 world-frame requests,16384 scan views; evaluate256/512/1024 and stop. No automatic training during implementation.",
  "initialization": "Transfer only LitePT/context/point. Detail adapter final layer is zero; other projections, condition encoding, scale fusion and the 208-to-128-to-1 score head are new. Fixed parent BN buffers; parent final scores are not preserved. Historical nuScenes semantic pretraining is disclosed.",
  "risk": "0.5 anomaly population +0.25 post normal population +0.25 unique-source raw normal population in the original sensor orientation. Exact dynamic mixture probabilities and overlapping-query importance correction; no keep, tail, teacher or semantic auxiliary loss."
}
