# 单帧 STU 异常分割研究说明

当前研究对象是一张 STU 激光雷达扫描中的逐点异常。输入是当前整帧全部实际回波的坐标与原始强度，每个点的判断可以利用同一扫描的邻域和场景上下文；输出与原始回波槽位对应，分数越高表示越异常。输入单位不是脱离邻域的孤立点。

现有工作已完成真实几何画像、仅用正常来源建立的条件几何探针，以及合成数据的内容与插入前后核查。这些结果支持部分局部几何具有弱预测信息，也显示复杂正常结构、稀疏观测和几何解释存在限制。本轮已修正三项未通过的条件覆盖，最终240个训练世界、120个合成验证世界同时满足原有核心条件、30格各8／4世界配额及取样暴露要求。数据验收限于预先声明的条件与206／201两条正常来源，不代表所有可能组合均匀覆盖或检测效果已经成立。正式模型尚未设计或实现，模型设计继续暂停。已退出的旧模型原型不作为正式 V1 的默认架构或继续训练起点。

旧模型原型完成过一轮完整合成与真实评价；随后同结构辅助监督整体开启／关闭的 A、B 各完成一轮训练，配对评价尚未完成即按用户要求主动终止。旧训练、模型、离线监督及独立诊断图表已删除，历史事实仅保留在研究协议。没有辅助监督有益或有害的受控评价结论。

当前研究问题与数据边界在 [spec.json](spec.json)，由 [src/protocol.py](../src/protocol.py) 读取。[data.json](data.json) 保存现有数据生成和几何核查所需参数。其 `geometry` 中的 `sampling_scale`、`boundary`、`levels`、`sampling`、`surface` 服务于数据诊断，不规定新模型必须采用哪些输入或辅助任务。冻结清单内原有生成配置和来源身份保留，用于追溯当时的生成过程；当前世界目录与读取路径采用下述命名。

## 当前数据与解释范围

本次定向修正预先限定为24个训练提案和24个合成验证提案。训练侧在206的三个支撑区域（未平移网格的3/−3、5/−5、6/−7）分别提出块状、多接触物体，寻找远距至少30条物理表面射线的观测机会，并核查原稀疏正常搭配。验证侧在201的不同区域提出局部长0.065—0.16米、宽0.05—0.075米、高0.07—0.14米的小型块状物体，寻找至少5个近距观测机会中自然只有1—6条物理表面射线的合法位置。实际验收仍采用冻结信号渲染后的1—4条异常回波，不能将表面相交机会当成实际返回。小物体先按合法地面支撑提案，再通过同一几何检查确定30格背景资格。

预定预算允许物理提案失败；停止条件是在该批及固定测量结束后，若仍没有满足原要求的完整世界选择，就报告缺口，不自动追加批次。实际生成23个训练、24个验证世界，另1个训练提案因几何可靠性检查失败被拒绝。与原360世界共同选择后，保留其中17个训练、3个验证新世界，替换同数旧世界；删除47个未选完整世界及1个失败提案目录。原始扫描与所有保留世界的帧、信号、标签和顺序均未修改。

当前唯一完整入口为 `results/synthetic/manifest.json`，保留训练240个、合成验证120个完整世界，共107,760／81,840帧。旧世界与新世界共同参与选取，原80／40和原验证20世界均无额外保留配额。正常来源仍分别为206和201，完整世界及其全部源帧不跨两侧分配。全部世界实体集中在 `results/synthetic/train/` 和 `validation/`。当前样本所需的全部生成配置已合入主清单，旧批次目录及候选缓存已删除。

世界目录使用英文特征加类内序号：`<structure>_<height>_<background>_<number>`，例如 `branched_low_rough_1`。结构取 `branched`（多分支）、`contacts`（多接触）、`elongated`（细长）、`sheet`（薄片）、`solid`（其余块状）；局部高度取 `low`（不超过0.2米）、`raised`（超过0.2米）；正常背景取 `smooth`（平滑）、`rough`（起伏）、`sparse`（稀疏）。30个类别分别在训练侧编号1—8、验证侧编号1—4，序号依各侧主清单原有的类内顺序确定。

名称采用主清单最终分配的 `combination`，不重新分类。逐世界生成记录中的提案类别可能与最终分配类别不同，原始记录保持原样；目录改名不改变世界身份、帧文件名、内容、标签、划分、读取顺序或采样权重。覆盖记录及可视化示例使用当前目录名。

| 核查项 | 修正前 | 最终360世界 | 原要求 |
| --- | --- | --- | --- |
| 训练远距且至少20条异常射线 | 最大区域贡献53.3800% | 最大区域贡献30.3425%；23个重复父项、259个唯一源帧、734个世界帧、20,206条异常射线 | 区域贡献不超过50% |
| 训练稀疏正常与非极端异常 | 4个重复父项；补完既定测量后原池达到10个 | 18个重复父项、81个唯一源帧、236个世界帧、2,201条中央区间异常射线 | 至少6个父项，每父项至少5个不同源帧 |
| 验证近距1—4条异常射线 | 2个重复父项，最大父项贡献46.6667% | 4个重复父项，最大父项贡献31.4607%；85个唯一源帧、96个世界帧、267条异常射线 | 至少4个重复父项，最大父项贡献不超过35% |

上述射线数为观测累计，同一正常源帧在多个世界中的出现不是独立背景。训练稀疏正常与非极端异常组的最大区域贡献为49.9773%，仍接近50%上限；按原门槛通过，但不能据此称该子组远离集中风险。其余原定核心条件也在最终成员上复核通过，没有降低门槛或采用全池取样均衡替代子组检查。

先补完原360世界的11,049个既定缺失观测，再对47个新世界计算7,150个固定分层与触发稀疏轨迹观测；原始已测结果直接复用。最终几何缓存含43,886个观测（训练27,559、验证16,327），其范围与缺失数量保存在 `research/selection.json` 和 `research/summary.json`，不能推广为全189,600帧的几何有效率。最终取样预案对全部训练帧赋予正概率，最大父项、两套区域网格的预期异常回波贡献分别为6.0688%、17.7586%、19.0198%；这些是输入暴露检查，尚未实施训练。

主构造空间为五类实际结构、两档局部高度和三类原始正常背景，共30格。每格最终训练8个、验证4个世界。五类结构依次以多分支、多接触、薄片、细长、其余块状作互斥主类；原有非互斥物理描述仍分别报告。分类根据实际占据几何而非模板名，两种空间分辨率不一致时记为未确定。两档高度为物体局部高度不超过0.2米与超过0.2米，不解释为真实重力方向高度。三类背景为两项原始几何同时不超过206条件参考第10百分位的平滑正常、至少一项达到第90百分位的起伏正常，以及局部邻域不足8点的稀疏正常；201只应用206参考。

每世界至少需要一个实际扫描，包含5条距离范围内唯一异常射线，以及相距2米内至少5个原坐标、强度和正常标签不变的同类正常位置，才可计入对应背景格。一世界只分配到一格。每格训练至少6个独立几何父项，验证4个；优先在至少3个支撑区域分布，并报告重复观测、父项、区域和源帧集中度。一个世界的背景类型可以随观测变化，指定主格不会改变其余帧的内容或标签。

世界构造先根据原始正常几何选择空间分散的背景簇，再寻找合法放置；精确射线相交机会用于选择观察条件，完整渲染后的实际内容用于最终筛选。物理不合法的提案拒绝，不修改返回概率、不补点、不重复随机回波、不使用模型分数或val19异常分布。`data.json` 保留最后一批的数值配置：每父项最多3次形状尝试、192次支撑提案、48个局部合法支撑候选，每个生成世界上限16 MiB。当前世界对应的原始配置集中在主清单，逐世界定义保存实际形状与放置。

格内另外核查距离、可见支持和弱背景变化。近、中、远距离为[2.5,10)、[10,35)、[35,50]米，采用实际异常回波中位距离。支持分0、1—4、5—19和至少20条实际物理射线；零回波没有可见距离，范围外另报。201中4帧的重复文件槽位在物理覆盖中去重，官方评价仍保持原槽位定义。30格并不声称所有距离、点数、形态的笛卡尔组合都已被物理实现。

弱背景变化主定义已获用户确认：至少5条可见异常射线，同时被替换或消失的原背景射线不超过4条。新增命中、替换原回波、原回波消失分别统计，并报告射线重叠比例、方位与仰角跨度、射线格连通分量。这描述已观测背景变化，不是异常自身被背景遮住的比例。零回波与1—4回波不能计入可见弱背景变化证据。

| 核心内容 | 最低观测关系 |
| --- | --- |
| 同一几何的观测变化 | 同一固定世界在近、中、远均至少5个不同源帧；物体局部视线方向跨度至少30°，可见点所在八分区组合至少2种，再统计独立父项数 |
| 距离与支持交叉 | 近距1—4回波、远距至少20回波分别核查；低矮物体在近、中、远分别核查 |
| 实际物体结构 | 紧凑、细长、薄片、多分支及距局部最低支撑平面1厘米截面的多接触区域，使用实际几何与双分辨率一致性 |
| 弱背景变化 | 可见异常存在，同时已观测背景射线改动少 |
| 困难正常与非极端异常 | 三类原始正常与两量均在206条件参考第10—90百分位区间的异常，在相同距离与竖直射线分箱且相距2米内配对；每类至少跨2个距离段 |
| 保留正常点 | 坐标、强度与标签不变，但残差变化超过0.01米或法向变化量改变超过0.01弧度，每观测至少5点 |

这些核心项继续采用补充前的诊断标准：训练至少6个父项、3个支撑区域、30个不同源帧及世界帧、150条相关异常回波；验证至少4个父项、3个区域、20个源帧及世界帧、100条回波。每父项至少5个合格不同源帧，单父项相关回波贡献最多35%，单区域最多50%。区域按世界坐标10米网格及平移5米网格检查。未满足项如实报告，不从样本数量推断模型效果。

数量选择与原定数据覆盖验收均已完成：30格各8／4个，每个世界只分配一次。`spec.json` 中 `balanced_selection_completed=true`、`coverage_closure_completed=true` 和 `data_preparation_accepted=true` 分别记录这三个状态；实际观测、集中度与缺失见 `results/coverage/research/summary.json`。这些状态不表示模型设计或检测效果已完成，也不自动触发下一轮生成或训练。

详细几何固定选取每世界距离、回波数与弱背景变化联合分层中的3个等距源帧，另外包含已有正常见证帧及前后各2帧。邻域始终由完整扫描建立。用于确认原始背景类型的邻近正常位置全部查询并按实际位置去重；插入后的正常几何变化诊断最多按槽位等距选512点，异常查询覆盖全部唯一射线。困难配对每观测至少3个中央区间异常与5个同类正常。缺失与没有正常参考分别记录；所选扫描比例不扩展为全池几何比例。

对稀疏搭配增加有明确范围的重复观测检查：若原固定选样中某世界至少一帧同时具有3条范围内异常射线和5个相邻保留稀疏正常位置，就计算该世界所有范围内异常射线不少于3条的帧。触发仅使用原固定选样，补算结果不递归扩大世界集合；触发不使用异常几何是否落在正常参考中央区间。这样可以核验同一父项的5帧重复证据，同时保留原3帧分层观察。几何公式、分箱、点数及集中度门槛均不改变，已有逐帧结果直接复用。868候选世界中，该规则预先确定训练50、验证116个补查世界及10,447／21,280个新增观测。此范围是有触发条件的世界可见帧检查，其比例仍不能代表全池。

训练取样预案先给30个指定组合相同概率，再在格内将80%分配给已有距离、点数、弱背景变化状态，余下20%覆盖完整世界的全部帧。状态内部兼顾支撑区域和几何父项，并按平均异常点数平方根的倒数抑制单一大物体回波占比。零回波、范围外和几何不可计算帧均有非零概率。只检查真实输入帧与异常回波的预期贡献，不把这当成梯度平衡或训练效果。

### 上一轮等额保留与本轮定向修正

上一轮独立清单已删除，其被当前世界使用的生成配置已合入当前主清单。当时954个候选中选取240个训练和120个验证世界，每格严格8／4个。每个世界的组合资格来自实际结构、局部完整高度及同帧异常与保留正常位置；一世界只分配到一格。两侧各30格的代表扫描读取通过，全部189,600帧引用可解析。

| 实际结构 | 局部高度 | 原始正常背景 | 训练世界 | 验证世界 |
| --- | --- | --- | ---: | ---: |
| 其余块状 | 低矮 | 平滑正常 | 8 | 4 |
| 其余块状 | 低矮 | 起伏正常 | 8 | 4 |
| 其余块状 | 低矮 | 稀疏正常 | 8 | 4 |
| 其余块状 | 较高 | 平滑正常 | 8 | 4 |
| 其余块状 | 较高 | 起伏正常 | 8 | 4 |
| 其余块状 | 较高 | 稀疏正常 | 8 | 4 |
| 细长 | 低矮 | 平滑正常 | 8 | 4 |
| 细长 | 低矮 | 起伏正常 | 8 | 4 |
| 细长 | 低矮 | 稀疏正常 | 8 | 4 |
| 细长 | 较高 | 平滑正常 | 8 | 4 |
| 细长 | 较高 | 起伏正常 | 8 | 4 |
| 细长 | 较高 | 稀疏正常 | 8 | 4 |
| 薄片 | 低矮 | 平滑正常 | 8 | 4 |
| 薄片 | 低矮 | 起伏正常 | 8 | 4 |
| 薄片 | 低矮 | 稀疏正常 | 8 | 4 |
| 薄片 | 较高 | 平滑正常 | 8 | 4 |
| 薄片 | 较高 | 起伏正常 | 8 | 4 |
| 薄片 | 较高 | 稀疏正常 | 8 | 4 |
| 多分支 | 低矮 | 平滑正常 | 8 | 4 |
| 多分支 | 低矮 | 起伏正常 | 8 | 4 |
| 多分支 | 低矮 | 稀疏正常 | 8 | 4 |
| 多分支 | 较高 | 平滑正常 | 8 | 4 |
| 多分支 | 较高 | 起伏正常 | 8 | 4 |
| 多分支 | 较高 | 稀疏正常 | 8 | 4 |
| 多接触 | 低矮 | 平滑正常 | 8 | 4 |
| 多接触 | 低矮 | 起伏正常 | 8 | 4 |
| 多接触 | 低矮 | 稀疏正常 | 8 | 4 |
| 多接触 | 较高 | 平滑正常 | 8 | 4 |
| 多接触 | 较高 | 起伏正常 | 8 | 4 |
| 多接触 | 较高 | 稀疏正常 | 8 | 4 |
| 合计 |  |  | 240 | 120 |

训练保留107,760个世界帧，验证81,840个；其中零异常回波帧分别为52,928和44,244。取样预案对30格各分配1/30概率，所有训练帧均有正概率；未实施模型训练。按该预案计算的最大单父项、两套区域网格的异常回波贡献分别为7.3174%、10.7684%和15.9803%。这描述输入和回波的预期暴露，不是梯度或检测效果。

上一轮未满足的科学诊断按当时结果保留：训练远距至少20回波的最大区域贡献为53.3800%，高于50%；训练稀疏正常与非极端异常搭配只有4个父项达到5帧重复要求，少于6个；验证近距1—4回波只有2个重复父项，少于4个，最大父项贡献46.6667%，高于35%。30格数量均衡已完成，不因此宣布这些更细的条件交叉已全部充分。该次数量筛选没有完成科学验收。本轮补完原定11,049个缺失观测后，训练稀疏正常与非极端异常的重复父项达到10个，已通过原要求；其余两项经本轮定向补充和完整世界选择后也已通过，当前数值见上表，模型设计继续暂停。

此前删除了594个未选中的候选世界、40个历史开发世界和1,214个失败提案目录。工作区实际剩余360份世界定义；当前样本需要的原生成配置保留在主清单中，旧根清单和旧池独立分析文件已删除。旧池不再具有完整工作区复算条件。原始STU数据、真实几何缓存与当前360世界的合成核查结果继续保留。

筛选收口时，最终样本及引用来源共190,344个独立文件，内容803,658,002字节，文件和目录分配合计1,220,325,376字节。该轮删除和空间实测保存在 `spec.json` 的 `history.storage_and_removal`，属于当时的执行记录，不能当作当前宿主盘剩余空间。

该轮58项数据、射线、几何与官方评价回归通过；新增86个世界仅补算86张正常见证扫描，复用原54,038个观测。当时汇总仅使用最终360世界。此次先补齐原360世界的41,070个观测，再完成新世界测量；最终缓存只保留当前360世界的43,886个观测。

## 工作区结构与文件用途

当前代码只包含数据读取、物理生成、几何分析和评价工具，正式模型与训练代码尚未实现。下表按实际文件说明用途；带有数字占位的路径表示相同格式的逐帧文件，不将它们视为重复代码。旧路线仅保留协议中的历史事实，当前结果目录不再保存独立的旧报告。

`results/` 按用途分为五个目录：

| 目录 | 当前用途 |
| --- | --- |
| `synthetic/` | 当前合成样本实体、唯一主清单、共用标定及生成来源记录。 |
| `coverage/` | 当前合成覆盖结果、最终选择记录及原始正常对照。 |
| `geometry/` | 真实条件几何分析、正常参考、探针结果及可复用逐帧缓存。 |
| `profile/` | 真实val19画像的记录和表格，不含旧原型诊断。 |
| `view/` | 固定相机生成的四张当前示例JPG，不作为训练样本。 |

根目录、标定与协议文件：

| 文件 | 内容与使用者 |
| --- | --- |
| `AGENTS.md` | 协作、研究边界、执行授权、表达及资源使用规则。 |
| `.gitignore` | 排除运行环境、自动缓存和大体积结果；保留真实画像的轻量表格。 |
| `.gitattributes` | 定义已纳入版本控制的二进制文件如何通过 Git LFS 存储；不自动上传被忽略的结果。 |
| `pytest.ini` | 设置测试目录和项目导入路径。 |
| `assets/rays.npz` | 激光束方向、束原点及槽位对应所需的标定参数，另含拟合和留出核验记录；由渲染器读取。 |
| `protocol/spec.json` | 研究问题、当前状态、数据范围、点身份、标签和官方评价规则，另保留简短历史事实。 |
| `protocol/data.json` | 当前样本入口、物理生成参数、几何诊断定义、30组合配额、取样预案及固定可视化相机参数；不定义新模型监督。 |
| `protocol/README.md` | 研究说明、当前证据及其边界、历史结果和本功能表。 |

程序文件：

| 文件 | 具体功能与主要使用关系 |
| --- | --- |
| [src/__init__.py](../src/__init__.py) | 标识 Python 程序包；只有包说明，没有实验入口。 |
| [src/protocol.py](../src/protocol.py) | 读取并检查研究协议，限定正常来源和公开开发序列，提供统一数据与评价约定。 |
| [src/scene.py](../src/scene.py) | 读取原始扫描、标签、位姿和标定，保留文件槽位身份，区分零占位与实际回波；是数据读取的基础。 |
| [src/view.py](../src/view.py) | 读取任意现存合成单帧或允许范围内原始扫描的全部实际回波，用固定120°广角透视投影保持真实直线，结合检测真值颜色和原始强度明暗生成深色背景 JPG；重叠点全部参与颜色融合，复用单帧读取器及字体定义。 |
| [src/data.py](../src/data.py) | 保存、还原合成单帧差量，按清单读取完整世界，保存与恢复逐点预测身份，读取已声明的帧取样概率；另提供原子写入和宿主磁盘查询。 |
| [src/render.py](../src/render.py) | 定义连续物体几何、材质和固定世界，处理射线标定、返回信号、合法放置、碰撞及前后景竞争，输出一帧物理渲染结果。 |
| [src/generate.py](../src/generate.py) | 提出受约束的形状与放置，检查正常背景搭配，渲染全部源帧并保存世界和差量清单；当前默认入口遇到已冻结池直接返回。 |
| [src/profile.py](../src/profile.py) | 计算真实扫描的点、帧和可见实例画像；其中 `observed_geometry` 从完整当前扫描计算无标签局部几何，供真实分析、生成和合成核查复用。 |
| [src/probes.py](../src/probes.py) | 用206正常数据拟合经验参考，计算无条件和条件化的几何稀有度，做描述性匹配与分组诊断；不含神经网络。 |
| [src/geometry.py](../src/geometry.py) | 提取并缓存当前帧几何，运行正常参考探针对照、精确分数计数和指标汇总；另保留覆盖检查使用的采样尺度、边界、删减观测和参考表面计算。后者部分依赖标签，不能作为无标签模型能力。 |
| [src/profile_report.py](../src/profile_report.py) | 汇总真实画像和几何探针结果，输出表格、统计图、误报案例及原始正常案例；由画像和几何分析程序调用。 |
| [src/coverage.py](../src/coverage.py) | 统计当前世界内容、物理射线与弱背景变化，核查困难正常和非极端异常、插入前后正常点，完成30格世界选择及帧取样概率计算；保留通用采样与表面诊断，原40世界专用活动入口已移除。 |
| [src/evaluate.py](../src/evaluate.py) | 按原始点身份和官方点集汇总预测，精确处理同分排序，计算 AP、FPR95、低误报召回及错误贡献；是项目统一评价入口。 |
| [vendor/stu/compute_point_level_ood.py](../vendor/stu/compute_point_level_ood.py) | STU 官方点级指标类，供项目评价与回归对照使用；指标实现保持不变，上游专用命令行包装已移除。 |
| [vendor/stu/LICENSE](../vendor/stu/LICENSE) | 官方评价代码的 MIT 许可证与来源署名。 |

测试文件：

| 文件 | 检查的关键行为 |
| --- | --- |
| [tests/test_scene.py](../tests/test_scene.py) | 原始槽位、标签与预测身份，差量还原、物理遮挡、物体几何和合法结构。 |
| [tests/test_view.py](../tests/test_view.py) | 透视投影的直线共线性、视场与固定相对位姿、重叠点全量融合、统一强度深浅，以及合成真值、强度和零占位处理。 |
| [tests/test_evaluate.py](../tests/test_evaluate.py) | 官方筛选、同分处理、全局指标、工作阈值及误差归属的一致性。 |
| [tests/test_profile.py](../tests/test_profile.py) | 真实画像口径、局部几何与缺失、正常表面支持、内容选择、30格配额和稀疏正常搭配；文件覆盖画像及其直接共用的数据诊断。 |
| [tests/test_probes.py](../tests/test_probes.py) | 正常参考边界、双尾计分、缺失参考、描述性匹配、精确计数和分组阈值。 |

真实画像位于 `results/profile/`，共108个文件。下表中的 `<序列>` 为19条公开开发序列；其8659帧描述范围包含官方不评价的帧。

| 相对路径 | 数量 | 内容与范围 |
| --- | ---: | --- |
| `records/spec.json` | 1 | 该次真实画像的输入范围与统计参数快照。 |
| `records/summary.json` | 1 | 汇总所有序列的分布、帧、实例和总数，供表格生成使用。 |
| `records/<序列>/frames.jsonl` | 19 | 每个原始帧的回波、标签、距离、强度和状态统计。 |
| `records/<序列>/instances.jsonl` | 19 | 当前帧可见实例的尺寸、形态及地面支持记录；不是完整物体真值。 |
| `records/<序列>/anomaly.npz` | 19 | 异常点身份和诊断量；含依赖真值的最近正常点、同实例邻域等字段。 |
| `records/<序列>/histograms.npz` | 19 | 连续分布的取值、计数和权重，供可靠聚合与分位数计算使用。 |
| `records/<序列>/summary.json` | 19 | 单序列计数、分布、统计定义及执行记录。 |
| `tables/method.md` | 1 | 真实画像的方法、分母、缺失含义和可复算入口。 |
| `tables/index.csv` | 1 | 其余表格的行数与来源索引。 |
| `tables/totals.csv` | 1 | 全部真实画像及官方范围的总量。 |
| `tables/sequences.csv` | 1 | 逐序列计数与官方可评价范围。 |
| `tables/frames.csv` | 1 | 逐帧计数、距离、强度、异常占比等。 |
| `tables/instances.csv` | 1 | 可见实例尺寸、形态和可靠地面关系。 |
| `tables/continuous.csv` | 1 | 连续量的均值、分位数及缺失比例。 |
| `tables/categorical.csv` | 1 | 类别、状态及其计数和占比。 |
| `tables/bins.csv` | 1 | 已定义分箱内的分布比例。 |
| `tables/count_distance.csv` | 1 | 异常回波数与距离的联合分布。 |
| `tables/reliability.csv` | 1 | 地面估计成功、缺失及拒绝原因计数。 |

上述真实画像均不依赖旧模型。旧原型的18份诊断文件已删除，历史指标与退出原因仅保留在研究协议。

真实条件几何分析位于 `results/geometry/`，共3181个文件。训练正常参考为206，正常迁移检查为201，真实异常区分在已用于开发的val19上观察。

| 相对路径 | 数量 | 内容与使用者 |
| --- | ---: | --- |
| `configuration.json` | 1 | 特征提取的数据、参数、固定抽样与执行范围。 |
| `extraction.json` | 1 | 实际提取帧数、耗时和存储记录。 |
| `execution.json` | 1 | 探针评分、汇总阶段的执行记录。 |
| `reference.json` | 1 | 206正常参考的来源、分箱、支持数和分布元数据；重建参考时核对。 |
| `thresholds.json` | 1 | 从206正常参考确定的固定工作阈值。 |
| `summary.json` | 1 | 各对照、共同有效点集、逐序列和去一序列结果的主汇总。 |
| `matched.json` | 1 | 带抽样权重的条件匹配描述，不能作为完整官方成绩。 |
| `features/train/206/<帧>.npy` | 449 | 固定正常查询点的特征、身份及有效标志，用于拟合参考。 |
| `features/train/201/<帧>.npy` | 682 | 对应正常迁移检查的特征缓存，包括空查询结果。 |
| `features/val/<序列>/<帧>.npy` | 1960 | 官方可评价真实帧的特征、标签分组和有效标志。 |
| `samples/<序列>.npy` | 19 | 描述性匹配和分组所用样本，含序列、点身份及抽样权重。 |
| `counts/keys.json` | 1 | 精确分数计数中的对照名称与序列索引。 |
| `counts/pooled.npz` | 1 | 合并后的精确分数计数，供总体指标和去一序列比较使用。 |
| `counts/<划分>/<序列>/groups.npz` | 21 | 两条正常来源及19条开发序列的精确同分计数。 |
| `counts/<划分>/<序列>/summary.json` | 21 | 对应序列的计数分母、覆盖和阈值记录。 |
| `tables/metrics.csv` | 1 | 总体、逐序列的探针 AP、FPR95、低误报召回等。 |
| `tables/coverage.csv` | 1 | 各点集和分组的几何及参考覆盖率。 |
| `tables/normal_transfer.csv` | 1 | 固定206阈值在正常来源上的迁移表现。 |
| `tables/confusion.csv` | 1 | 按正常语义类别统计的探针误报。 |
| `tables/frames.csv` | 1 | 全局固定阈值下的逐帧正常、异常和错误计数。 |
| `tables/subgroups.csv` | 1 | 既有困难分组的覆盖及检测统计，保留抽样权重。 |
| `tables/sampling_increment.csv` | 1 | 在同一有效点集上比较加入邻距尺度条件的变化。 |
| `tables/candidates.json` | 1 | 按固定规则建立的误报案例候选索引；不是合成候选世界。 |
| `tables/cases.csv` | 1 | 最终四个几何误报案例的局部统计。 |
| `overview.png` | 1 | 几何探针对照总体结果图。 |
| `sequences.png` | 1 | 逐序列改善与差异图。 |
| `coverage.png` | 1 | 几何可计算性与正常参考覆盖图。 |
| `features.png` | 1 | 各单量几何探针的比较图。 |
| `plots.pdf` | 1 | 上述四张统计图的合并文档。 |
| `cases.json` | 1 | 四个几何案例的身份、规则、稀有度与图表记录。 |
| `case_1.png` 至 `case_4.png` | 4 | 几何稀有度误报的四张案例图。 |
| `cases.pdf` | 1 | 四张几何案例的合并文档。 |

合成覆盖核查位于 `results/coverage/`，共16个文件。`research/` 保存当前360世界的测量及最终选择记录；`geometry/` 保存原始正常案例。旧池报告和过期提案记录已删除。

| 相对路径 | 数量 | 内容与范围 |
| --- | ---: | --- |
| `research/inventory.json` | 1 | 当前360世界的物理几何、身份、来源及实体路径。 |
| `research/observations.csv` | 1 | 全部189,600世界帧的射线数、新增命中、背景替换与消失及空间分布。 |
| `research/selection.json` | 1 | 当前43,886个几何观测的固定选样及触发的稀疏轨迹检查范围。 |
| `research/geometry.json` | 1 | 上述观测的几何测量，按世界与源帧身份复用；不代表全池局部几何普查。 |
| `research/summary.json` | 1 | 当前360世界的30格覆盖、核心条件、集中度和缺失。 |
| `research/sampling.npz` | 1 | 107,760个训练世界帧的取样概率，按世界身份和源帧读取；尚未训练。 |
| `geometry/normal_cases.json` | 1 | 原始206／201正常案例的身份、选择规则和统计。 |
| `geometry/normal_1.png` 至 `normal_6.png` | 6 | 原始正常结构的六张案例图。 |
| `geometry/normal_cases.pdf` | 1 | 六个正常案例的合并文档。 |
| `geometry/tables/normal.csv` | 1 | 原始正常缓存中两项几何量的条件分布与覆盖。 |
| `research/balance.json` | 1 | 最终240／120世界的选择规则、候选与裁决记录；候选路径是执行时的位置，当前实体由主清单定位。 |

合成样本位于 `results/synthetic/`，共190,323个文件。唯一完整入口是根目录的 `manifest.json`；实体直接按训练和验证划分存放，每个世界只有一份。

| 路径 | 数量 | 实际内容与使用者 |
| --- | ---: | --- |
| `manifest.json` | 1 | 当前240／120世界的成员、顺序、组合、来源和相对路径；读取器统一从此进入。每个成员的 `configuration_identity` 对应顶层配置或内嵌的 `generation_configs`。 |
| `train/<世界>/world.json` | 240 | 训练世界的固定物体形状、尺寸、放置、朝向、材质、种子和206来源。 |
| `train/<世界>/manifest.json` | 240 | 各世界全部449帧的顺序、回波和遮挡计数、物理检查及正常参照记录。 |
| `train/<世界>/frames/<帧>.npz` | 107760 | 训练单帧无损差量；与原始206扫描一起还原完整点云和标签。 |
| `validation/<世界>/world.json` | 120 | 合成验证世界定义，正常来源为201。 |
| `validation/<世界>/manifest.json` | 120 | 各验证世界全部682帧的顺序与逐帧记录。 |
| `validation/<世界>/frames/<帧>.npz` | 81840 | 验证单帧无损差量；与原始201扫描一起还原完整输入。 |
| `calibration.pt` | 1 | 共用射线、返回概率和强度标定，格式为 `stu-sensor-calibration`，没有神经网络权重；原10份文件逐字节相同，合并保留此份。 |
| `normal_reference.json` | 1 | 最近批次使用的正常背景候选位置、几何类别和身份，供受约束放置复用。 |

主清单的顶层配置描述最近批次，`generation_configs` 内保存当前世界实际使用的另外8种配置，以及原实现和标定身份。这9种配置的参数快照保持不变。旧池成员表、历史背景候选缓存、未采用的开发分布和旧运行记录已删除，不再为它们保留独立目录。

此前集中存放只移动世界目录、更新位置索引并合并相同标定。世界名称、身份、完整帧顺序、差量内容、标签、数据划分及科学验收条件保持不变。原始STU扫描位于仓库外，合成差量不能独立替代原始扫描。

此前集中存放时核验了全部189,600帧引用和360个世界的生成来源。190,320份世界定义、逐世界清单及帧文件的文件身份、大小和修改时间与移动前一致；覆盖数值、几何选样和取样概率也一致。四张示例JPG仅更新输入路径记录，解码后的全部像素保持一致。61项现有回归通过；默认生成入口仍直接报告已冻结，不启动生成。

本轮清除旧路线的46份文件和13个目录后，再次核对了全部帧引用、世界文件身份、9种生成配置和取样概率；当前覆盖文件内容未改变。训练和验证代表帧分别还原出原有20和3个插入回波，默认生成入口仍直接返回已冻结。没有重新计算科学结果，也没有启动生成或训练。

运行环境与版本管理：

| 路径 | 功能与处理边界 |
| --- | --- |
| `.venv/pyvenv.cfg` | Python 3.13环境定义，配置为同时使用外部Miniforge的系统包；当前环境并非完全自包含。 |
| `.venv/.gitignore` | 防止虚拟环境内容进入版本控制。 |
| `.venv/bin/`、`.venv/lib/`、`.venv/include/`、`.venv/lib64` | 解释器和工具入口、第三方依赖及链接；不属于AJAE自主实现，本轮保留环境。 |
| `.git/` | 提交历史、分支、索引、远端配置及Git LFS对象等；不作为实验数据清理。 |

项目内自动生成的 `__pycache__/` 和 `.pytest_cache/` 已清除；后续执行时可能重新出现，不是实验输入或科学证据。原始STU扫描位于仓库外，不在以上文件统计中。现有忽略规则保持不变，只有源代码、协议及真实画像的11份轻量表格纳入版本控制。

以下命令均从仓库根目录执行，用于说明现有入口，不代表需要重新运行已经完成的实验。

## 数据范围

开发诊断使用 STU 公开验证集全部 19 条异常序列：125、137、138、139、140、141、142、143、144、145、146、147、148、149、150、151、152、153、169。每条序列的全部原始帧都可以产生单帧预测。

当前正常数据源 `train/206` 的全部449帧用于240个训练世界，共107,760个世界帧；`train/201` 的全部682帧用于120个合成验证世界，共81,840个世界帧。两侧分别具有30种组合，每种8／4个世界，完整帧顺序及标签不变。入口为 `results/synthetic/manifest.json`，360个实际世界全部位于该目录下的 `train/` 和 `validation/`；没有另外保留的80／40世界。世界及其正常源序列整体分开，不能将同一世界的前后帧分到训练与验证两侧。隐藏测试集不在当前开发范围内，原始数据保留在仓库之外，不随代码提交。

公开验证集已经用于开发分析，后续在此集合观察的规律和模型差异均应保留开发数据的适用边界。几何条件与失误的统计差异不能单独证明因果机制。

目录结构为：

```text
STU/
  train/{201,206}/
  val/<sequence>/
    velodyne/<frame>.bin
    labels/<frame>.label
    calib.txt
    poses.txt
```

`spec.json` 保留已记录的原始压缩包摘要，供需要核对数据来源时使用。

## 单帧读取与输入身份

[STUSequence](../src/scene.py) 通过 `source_frame(frame_id)` 返回一个 `SourceFrame`。索引和迭代同样返回原始单帧。

`SourceFrame.xyzi` 保存完整原始文件槽位，形状为槽位数乘 4，数据类型为 `float32`。`real_slots` 按原始顺序列出坐标不全为零的实际回波槽位。零坐标占位槽继续保存在源数据中，构造研究输入时通过 `real_slots` 排除。强度保持原值，不能预设它严格位于 0 到 1 之间。

单帧可读的基本数组是：

```python
x = frame.xyzi[frame.real_slots]
```

语义标签、实例标签以及依赖标签计算的几何量仅用于监督或诊断，不能作为推理输入。用 `label_mode="forbidden"` 读取源数据时，读取器不打开标签文件。

源对象保留官方坐标及特征的原有计算能力，供数据核对和外部方法适配使用：`coordinates` 按发布位姿计算，`features` 包括原始强度及相对扫描坐标均值的距离。它们是辅助数据表达，不改变当前研究以单帧传感器坐标和强度为输入的任务定义。官方评价距离始终来自 `xyzi[:, :3]`，不能用世界坐标范数代替。

发布标签的低 16 位是语义，高 16 位是实例。原始打包标签完整保留。合成训练的正常监督依据 `normal_semantic_class_map` 中的有效类别，原始忽略类别保持忽略。异常监督只由渲染器的实际插入掩码定义，不能将正常源中的原始语义 2 直接解释成插入异常。

读取示例：

```bash
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m src.scene \
  --data-root /home/jasongao/Data/STU --partition val \
  --sequence 125 --frame 0 --labels required
```

## 固定前视广角图像

[src/view.py](../src/view.py) 接受一份实际帧文件，输出按检测真值着色的 JPG。合成 `.npz` 是差量，程序根据同一世界的清单定位原始206或201扫描，经 `FrozenFrame.load` 还原整帧并检查来源身份。也可直接输入允许范围内的原始 `velodyne/<帧>.bin`。当前完整样本入口仍为 `results/synthetic/manifest.json`；实际帧分别位于其 `train/` 和 `validation/` 子目录，可直接读取。

```bash
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m src.view \
  results/synthetic/train/contacts_raised_rough_3/frames/000238.npz \
  --output results/view/synthetic.jpg

PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m src.view \
  /home/jasongao/Data/STU/train/206/velodyne/000238.bin \
  --output results/view/original.jpg
```

不指定 `--output` 时，图片保存到 `results/view/`，名称包含来源、世界和帧号。合成帧所需原始数据根目录默认为 `/home/jasongao/Data/STU`，可用 `--data-root` 指定。该目录中的 JPG 是可重新生成的观察图，不是模型成绩或数据覆盖验收结果。

相机参数统一保存在 `data.json` 的 `visualization` 中。默认相机中心在LiDAR后方0.3米、下方0.6米，横向偏移为0，光轴始终朝LiDAR的正前方；按LiDAR的前、左、上坐标轴，位置为 `(-0.3, 0, -0.6)` 米。这个位置是模拟驾驶员观察点，未将发布数据中的单位标定矩阵解释成真实驾驶员位置。程序直接使用当前传感器坐标，不按物体或当前点云范围自动转向、居中或缩放。

相机画面为1920×1080像素，120°指水平视场，竖直视场约88.51°。当前采用保持真实直线的广角透视模型，对应常见行车视频去畸变后的几何外观；数学上使用无镜头畸变的针孔透视关系。焦距 `f = 1920 / (2 × tan(60°)) ≈ 554.26` 像素，横竖方向焦距相同。右、下、前相机坐标记为 `(X, Y, Z)`，分别对应LiDAR的负左轴、负上轴和前轴，像素坐标为 `u = f × X / Z + 960`、`v = f × Y / Z + 540`。相机平面及后方点在除法之前排除，画面边界固定。该投影保持空间直线在图像中的共线性，画面边缘可能出现广角透视拉伸。画面底部另附112像素说明栏，不计入视场。

蓝色表示正常监督标签0，橙红色表示异常标签1，绿色表示忽略标签−1。合成图直接使用 `FrozenFrame.anomaly_target`：实际插入回波为异常，原始有效正常类别为正常，其余保持忽略。原始正常扫描使用相同正常类别规则，公开开发扫描使用发布的异常真值。零坐标占位在相机平移前去除；不应用官方评价距离、异常点数或几何有效性筛选。因此，少回波、无异常以及异常位于前视范围外的帧都可以生成图片。

画面及说明栏使用深色背景，背景RGB为 `(12, 16, 23)`，说明文字为浅色。逐帧读取全部实际回波，不随机采样、不体素降采样、不按坐标去重，也不按距离、标签或强度剔除实际点。120°前视画面只容纳对应视场内的点；所有画内点均采用相同的1像素半径标记。每个像素累加所有覆盖它的点的颜色并取算术平均，远点、近点和坐标相同的不同回波全部贡献，不作深度剔除，不通过绘制顺序覆盖已有点。不同标签重叠时像素会呈混合色，不能将该像素解释为某一个点的独立标签。

颜色的明暗直接来自每点原始 `xyzi[:, 3]` 回波强度。所有帧和类别共用固定转换：记非负强度为 `J = max(I, 0)`，与背景的颜色对比系数为 `c = 0.4 + 0.6 × J / (J + 0.25)`，逐点颜色为 `(1 − c) × 背景色 + c × 真值类别基色`。强回波更亮，弱回波较暗；零或负强度仍保留基色与背景差值的40%，避免弱点融入深色背景。原始强度可能超过1，显示转换不在1处截断，不按每帧分位数、最小值或最大值重新拉伸。`visualization.intensity` 中的两个常数只控制显示，不修改样本强度。

说明栏报告整帧实际点数、画内点数及参与绘制的点数，并另外报告异常点数量；画内点数与绘制点数应相同。重叠点以融合方式参与同一个有限像素，不代表能够在 JPG 中逐个分辨。雷达扫描的覆盖边界不是物理直线，投影后仍可能有弧形轮廓和留白，不能为铺满矩形而拉伸扫描边界或补点。点云没有观测到的表面不进行补绘，图片是全部已观测点的投影，不是真实彩色照片或物理遮挡恢复。

JPG的 `EXIF ImageDescription` 保存输入身份、相机变换、投影参数、颜色、强度转换、逐类点数、重叠像素数及点对像素的贡献总数，便于确认跨帧设置一致及全部画内点参与绘制。文字实际使用宋体和Times New Roman并检查字形，JPG采用高质量、无色度降采样保存；有损压缩仍可能轻微改变边缘颜色。该工具不修改原始数据、冻结样本、划分或训练定义。

## 预测身份和存储

[src/data.py](../src/data.py) 中的 `FramePrediction` 使用以下字段：

```python
FramePrediction(
    partition,
    sequence_id,
    frame_id,
    source_slot,
    anomaly_score,
)
```

每个分数对应一个明确的原始文件槽位。保存和加载时都必须验证：

- 分区、序列号和帧号与传入的 `SourceFrame` 一致。
- 槽位无重复，且其集合恰好等于 `SourceFrame.real_slots`。
- 分数是有限的 `float32`；生产端显式决定精度转换，存储端不静默量化。
- 分数可以为负，也可以超过 1；评价使用原始数值的排序。

生产端可以采用任意点行顺序，但必须同时保存对应槽位。`restore(source)` 按槽位放回完整原始顺序，并将零坐标槽的分数填为 0。依赖数组行号而忽略槽位身份的结果不能进入评价。

`save(path, source)` 不覆盖已有预测；`load(path, source)` 不仅检查压缩包字段，也重新核对真实源扫描。当前存储格式是 `stu-frame-prediction`，不接受其他任务格式作为当前结果。

统一目录为：

```text
<predictions>/val/<sequence>/<frame:06d>.npz
```

后续模型分别使用自己的预测根目录；上述约定保留为通用数据与评价接口。旧原型的模型权重、完整预测及独立诊断图表均已删除。

## 官方评价和误差归属

[src/evaluate.py](../src/evaluate.py) 保留 [STU 官方评价器](../vendor/stu/compute_point_level_ood.py) 的点集和阈值定义：

- 点到当前传感器的距离满足 `2.5 <= r <= 50` 米。
- 语义 0 忽略，语义 2 为异常，其余有效语义为正常。
- 距离过滤后异常点少于 5 个的帧整体不进入正式指标。
- 所有合格帧的全部有效点合并计算指标，不平均逐帧或逐序列指标。

平均精确率 AP、排序区分指标 AUROC、以及高召回工作点的正常误报比例 FPR95 以百分数报告。FPR95 使用官方保留的 ROC 阈值节点中召回率严格大于 95% 的第一个节点。相同分数的点必须整体进入或离开检测集合。

低误报工作点使用完整官方点集上实际误报比例不超过 1% 时的最高召回率；召回率相同时选择最早达到该召回率的阈值。如果没有异常点能在该条件下检出，阈值记录为空，含义是拒绝全部点。子组统一使用这两个全局工作点。

指标实现保留精确的单精度排序，并采用磁盘临时数组就地排序和分块累计；不把分数量化到直方图。官方评价器保留为独立核对依据。

`APAttribution` 记录每个异常分数对应的全局精确率及所需误报比例。对异常点 `i`，全局同分组全部纳入后的精确率记为 `P_i`，异常点总数记为 `N_A`。其失分为 `(1-P_i)/N_A`，全部异常点的失分之和应为 `1-AP/100`。任意互斥分组可以据此汇总对全局 AP 的失分贡献。

所需误报比例按分数大于或等于该异常点分数的正常点数量除以全局正常点数量计算。它与 AP 失分都来自同一个全局排序。这些计算描述输出排序的错误归属，不能单独确定网络内部机制或错误的因果来源。

完整开发集评价示例：

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -m src.evaluate --data-root /home/jasongao/Data/STU \
  --predictions results/predictions/<model> --output results/evaluation/<model>
```

可用 `--sequence` 运行明确标记的开发子集。完整评价要求所选序列的每个原始帧均有符合身份约定的预测。临时排序文件使用结束后即释放。大体积评价必须根据当时 Windows E 盘空间保留至少 10 GB 余量。

## 单帧真实几何画像

[src/profile.py](../src/profile.py) 保留单帧统计算法，包括异常点数、距离、强度、最近正常点距离、正常邻域、同实例最近邻、可见实例尺寸、协方差形态和地面关系。几何量在当前扫描中计算，源点身份贯穿统计过程。

画像描述全部原始帧的实际回波，范围比官方指标点集更广。原始异常点数和官方范围内异常点数分别保存；符合官方帧门槛的计数另行汇总。忽略点和范围外点的描述性统计不能混入正式 AP 失分结论。

实例几何与地面估计保留原有可靠性规则。无可靠地面拟合时，高度保持缺失并报告原因。异常点实例标识为零时，不假定这些点属于同一个物体；点级和帧级异常统计继续保留它们。

[src/profile_report.py](../src/profile_report.py) 根据实际生成的逐帧记录、逐实例记录和分布累计结果导出 [统计表格](../results/profile/tables)。分位数来自完整加权经验分布，不平均各序列分位数。表格目录保存：

```text
frames.csv
instances.csv
sequences.csv
totals.csv
count_distance.csv
continuous.csv
categorical.csv
bins.csv
reliability.csv
index.csv
method.md
```

CSV 数值列与实现字段直接对应，空值表示缺失或不适用。CSV 本身不保存字体。详细统计定义由生成源写入 `method.md`，修改定义时应修改生成源并重新生成结果。

完整运行示例：

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -m src.profile --data-root /home/jasongao/Data/STU \
  --output results/profile --workers 12
```

进程数必须依据当时 CPU、内存和其他任务实测状态确定。`--output` 同时决定明细子目录 `records/` 和表格子目录 `tables/`。少量帧检查可使用 `--sequence` 和 `--limit`，并指定独立的 `--output`，避免将子集结果覆盖到完整画像位置。

已有完整明细时，在上述命令中加上 `--tables-only` 可重新汇总并生成表格，无需重新读取原始扫描。表格中的 `index.csv` 使用相对于表格目录的路径指向明细汇总文件。

## 固定世界与冻结单帧数据

[src/render.py](../src/render.py) 保留连续几何、形状族、激光射线、物体放置、地面支持、传感器标定和真实遮挡竞争。`render_frame(source, world, ray_grid, sensor)` 处理一个原始单帧；`render_frames` 逐帧流式调用。物体一旦放置，形状、尺寸、世界坐标位置、朝向和材质均不随帧变化。

[assets/rays.npz](../assets/rays.npz) 保存射线几何标定；`calibrated_ray_grid` 给出束方向、束原点及原始槽位与射线的对应。回波概率与强度规律由正常 `train/206` 的全部单帧标定；放置地面仅从一张当前正常扫描拟合。包围盒射线机会、真实物体表面交点与最终返回信号是三个不同阶段，不能互相替代。

[src/generate.py](../src/generate.py) 只使用正常源提出固定世界，检查支持、整条轨迹的距离和已观测障碍物碰撞，再执行完整渲染。每个已渲染帧写后重新还原，核对坐标、强度、打包标签、实际回波槽位、插入和遮挡掩码。物体遮挡原始回波却没有返回信号时，对应槽位及标签清零；没有异常回波的帧仍保留。既有物理检查只能约束扫描中已观测到的背景，不能声称掌握所有未观测表面。

当前360世界全部采用 `stu-frozen-frame` 无损差量格式，绑定原始扫描、标签、位姿和固定世界；未改变的背景引用原始正常扫描。`FrozenDataset` 按当前主清单读取，`FrozenFrame` 按原始槽位还原完整扫描。样本的生成参数与实现身份集中保存在同一主清单，物体实际尺寸、形状、放置和源帧身份仍由逐世界定义与帧文件保存。

数据生成入口为 `src.generate --config protocol/data.json --workers N`。当前输出 `results/synthetic/` 已冻结，默认入口直接报告240／120世界，不重新生成样本。当前世界只使用正常来源与预定内容条件，不使用val19异常的尺寸、比例或分布来选样。

此前曾有20／20和80／40世界等探索池，数量扩充没有自动消除远距、少回波和困难正常搭配的缺口，随后才按30组合和预先声明的科学条件完善为当前360世界。曾使用val19统计的旧开发池已退出；旧池样本、独立清单、历史提案缓存及旧覆盖报告均已删除。其历史判断仅作简短记录，不再具备完整工作区复算条件。

当前内容覆盖仍由 [src/coverage.py](../src/coverage.py) 处理；`results/coverage/research/` 保留全池射线库存、43,886个既有几何观测、当前汇总、取样概率和最终选择记录。只有当前完整世界及现存分析能够按相应入口复算，不能恢复已经删除候选的完整选择过程。

## 当前真实几何分析

本次分析回答四个问题：几何能否区分标签；条件化是否比原始几何更有用；这些量在哪些条件下失效；类似条件下哪些正常结构容易与异常混淆。本轮几何与条件特征只由当前扫描全部实际回波的坐标计算，强度未进入这些探针。标签用于原帧的官方评价资格判断，并在无标签特征计算之后用于正常拟合样本的资格判断和最终评价、描述。依赖真实异常标签计算的异常内部邻域、距异常边界或距已知正常点距离，不得充当待评价分数的输入。

正常参考仅从 `train/206` 拟合。条件分箱的规则、双尾评分规则、特征集合与组合方式先于本次验证评分确定；需要估计的尺度分箱边界和经验分布只用 `train/206` 正常点计算。`train/201` 用于检查固定参考在另一正常来源上的误报和覆盖，不重新拟合参考。val19 标签仅用于官方资格判断、固定分数的评价及条件分布描述，不能用于选择最有利的分箱、尾方向或组合，也不能用于拟合生成器的异常尺寸、距离和数量比例。

几何计算以同一当前扫描中的不同坐标位置建立局部邻域，保留原始回波槽位身份。候选证据包括局部表面残差、表面厚度、法向变化和协方差形态，并单列按局部采样尺度归一化的量。法向、平面和尺度均从当前无标签点云估计；局部表面残差描述可见邻域，不能解释为被遮挡正常表面的真实位移。局部采样尺度同时受观测和物体几何影响，是观测条件的代理量；加入它的条件比较不能直接证明纯传感器因素的因果作用。

具体邻域为两米内最多 32 个不同坐标的最近邻，排除查询位置自身，至少需要 8 个位置。邻域中位距离记为采样尺度。协方差按邻点数归一化，升序特征值记为 $\lambda_0,\lambda_1,\lambda_2$：表面厚度为 $\sqrt{\lambda_0}$，最小方差占比为 $\lambda_0/\sum_i\lambda_i$，线性度为 $(\lambda_2-\lambda_1)/\lambda_2$，平面度为 $(\lambda_1-\lambda_0)/\lambda_2$。查询点至邻域中心平面的绝对距离为局部表面残差。法向另要求 $\lambda_1>10^{-12}$ 且 $\lambda_1-\lambda_0\geq0.05\lambda_2$；法向变化为与至少 8 个有效邻点法向之间的不定向夹角均值，单位弧度。残差与厚度除以采样尺度的版本单列。没有可靠法向时，残差和法向变化缺失，仍可靠的协方差形态不随之清空。

距离采用固定边界 2.5、5、10、20、35、50 米。射线条件仅采用传感器坐标的竖直方向分量 `ray_z=z/r`，边界为 −1、−0.3、−0.15、−0.075、−0.025、0.025、0.1、0.3、1；它不是方位角或表面入射角。由于 $z=r\,\mathrm{ray}_z$，距离与竖直射线分量合用也包含传感器坐标下的高度信息，可能同时区分不同场景结构，不能将全部收益归因于消除了采样影响。采样尺度采用 `train/206` 正常点的五等分分位数，参考取值范围外的尺度保持无参考。条件箱内每个所用几何量均须有至少 256 个有效正常点、来自至少 20 帧；这只是样本支持要求，相邻 20 帧不代表 20 个独立场景，也不保证参考足以代表所有正常结构。

最小对照使用三类固定规则，分数越高表示在正常参考下越少见。双尾经验稀有度同时检查偏小与偏大两端，包含全部同分参考点，不按真实异常标签选择方向。正常参考取值范围之外的几何量取最高稀有度。这是经验排序分数，不是经过校准的异常概率或显著性检验概率。多特征组合统一取各特征稀有度的最大值。

| 对照 | 输入与正常参考 | 回答的问题 |
| --- | --- | --- |
| A：仅观测条件 | 距离、射线竖直方向等无标签条件；加入局部采样尺度的版本另列 | 仅凭观测条件的少见程度，能够产生多少异常区分 |
| B：无条件几何 | 同一组几何量在 `train/206` 正常点上的无条件双尾经验稀有度 | 几何本身相对于全部正常参考是否少见 |
| C：条件几何 | 同一组几何量在 `train/206` 正常条件分箱内的双尾经验稀有度；距离、距离加射线方向及加入采样尺度代理的条件版本分别报告 | 在可比条件的正常参考下，几何稀有度是否提供额外区分 |

B 与 C 使用完全相同的几何特征集合和组合规则。原始量与归一化量分别比较，不能把条件化与更换特征的效果合并。A 同时给出与 C 对应的条件集合，含射线竖直方向的结果作为补充。条件分箱须达到预先规定的正常样本数与跨帧支持数；正常参考不足或几何不可计算的点不填零分，不将缺测本身当作异常证据。

主 A/B/C 评价限制在相同有效覆盖交集，沿用官方距离、标签和原始帧资格规则，不因某方法覆盖不足重新筛选合格帧。分别报告正常与异常覆盖以及远距、少回波等条件下的缺测。交集内的 AP、FPR95 和固定误报率召回仅描述该交集；没有覆盖全部官方点时，不能报告为完整 STU 指标，也不能直接与旧原型的完整 val19 指标比较。正常迁移误报、单特征结果和条件差异共同用于解释组合分数，不能仅由一个总 AP 判断科学命题。

总体组合要求六项几何同时有效，可能排除稀疏或法向不可靠的结构；单量对照采用该量自己的共同有效点集。`returns` 分组指原合格帧内全部官方异常标签点数，是依赖真值的事后描述，不是推理条件，也不是单个物体的完整点数。所有相邻帧保持所属序列，逐一排除序列时直接从总体精确分数计数中减去该序列，不重新拟合、不把点随机拆开。

阈值有两种用途：排序诊断的“误报不超过 1% 时召回率”在各评价点集上读取固定分数的整条曲线；正常迁移与分组误报使用仅由 `train/206` 确定的固定阈值。前者是开发集排序能力的描述，不能当作由正常训练得到的可部署工作点。后者在 `train/206` 上达到的误报比例属于拟合样本内结果，需要由 `train/201` 检查迁移。

这些是开发分析。条件比较的正结果支持给定特征、正常来源与覆盖条件下存在可利用关联。单变量双尾分数只检测极端取值，可能漏掉正常分布中部的低密度区，以及只体现在多变量关系中的异常；负结果只能否定本次可计算特征与比较方案，不能单独否定所有可能的局部几何证据。合成世界的补充、监督选择和模型设计应依据具体证据决定；这里的探针结果不确定正式模型方案。

A 仅使用各条件边际稀有度的最大值，未建立条件的联合分布；C 超过 A 只能说明超过此明确基线。距离与射线方向也关联场景内容，C 超过 B 表明本次正常参考变换改善了区分，不能据此识别纯采样因素的因果作用。

**实际输入与复算入口。** 几何入口为 [src/geometry.py](../src/geometry.py)，无标签几何与正常参考的唯一实现分别位于 [src/profile.py](../src/profile.py) 的 `observed_geometry` 和 [src/probes.py](../src/probes.py)。完整运行读取 `train/206` 的 449 帧、`train/201` 的 682 帧，以及 val19 的全部 8,659 帧。正常侧每帧先在距离范围内均匀抽取至多 8,192 个真实槽位，再按有效正常标签筛选；固定随机种子为 `20260911`，每帧具有独立随机流。最终正常参考样本为 3,544,145 个点出现次数，正常迁移样本为 3,736,364 个。邻域始终来自整帧，与抽样和标签筛选分开。

val19 中 1,960 个原帧符合官方资格，保留全部 193,792,470 个正常点与 87,499 个异常点。特征缓存按原始槽位保存全部这些点，标签分组发生在无标签计算之后。距离条件有 5 个参考箱；距离与竖直射线分量有 37 个实际出现的参考箱，全部满足支持要求；加入邻距尺度后有 138 个实际出现的箱，其中 115 个满足全部六项几何的支持要求。尺度边界为 0.030862、0.095147、0.150313、0.233404、0.420635、1.968716 米，实际计算保留未取整值。

描述性条件分布保留全部官方异常点，每帧均匀抽取至多 2,048 个正常点，并按抽中概率倒数加权。其分位数、分布差异和固定阈值分组中的正常误报为抽样描述；同条件箱的比较不等于严格逐点匹配。主要排序指标、逐序列结果、逐一排除序列结果及主对照覆盖率则来自全部共同有效点的精确单精度分数计数，没有直方图量化。

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  .venv/bin/python -m src.geometry --stage all --workers 12
```

初次运行使用上面的入口。已有完整特征时用 `--stage analyze --workers 6` 复算固定参考和分析，复用已经完整完成的序列精确计数。输出位于 `results/geometry/`：`configuration.json` 与 `extraction.json` 记录输入和执行，`reference.json` 记录正常参考支持，`thresholds.json` 记录训练阈值，`tables/` 保存指标、覆盖、正常迁移、逐帧和误报表，`matched.json` 保存描述性条件分布，`plots.pdf` 和 `cases.pdf` 保存统计图与预定真实案例。这些无模型条件几何证据继续保留，已删除的旧原型诊断不属于它们。

**真实主对照结果。** 下表所有数值均为百分数，AP 为平均精确率，AUROC 衡量整体排序，末列为该共同点集的评价曲线上正常误报不超过 1% 时的最高召回率。这些是公开开发数据上的探针结果，均不代表完整 STU 方法成绩。

| 条件参考 | A 的 AP | B 的 AP | C 的 AP | C 的 AUROC | C 的低误报召回 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 距离 | 0.031549 | 0.040116 | 0.047157 | 53.523050 | 0.600326 |
| 距离与竖直射线分量 | 0.038803 | 0.040116 | 0.074426 | 66.089737 | 1.425052 |
| 再加入邻距尺度 | 0.036207 | 0.039986 | 0.072629 | 63.568025 | 1.981391 |

距离与射线方向组的 B 为 AUROC 47.012161%、低误报召回 0.483500%；C 的 AP 增益为 **0.034310 个百分点**，仍只有 0.074426% 的绝对 AP。该共同子集异常占比约为 0.044865%，也是常数分数的 AP；B 的六量最大值分数低于这个水平。因此，本轮支持部分可计算几何与标签有关，以及当前条件参考具有弱增益，不能宣称原始几何组合已能有效检测异常，更不能将相对提升幅度写成实际检测能力。

主距离与射线方向组的 C 相对 B，在 **19/19** 条序列上 AP 和 AUROC 均提高，低误报召回在 17 条提高、2 条下降。逐一排除任意序列后，AP 增益仍为 **0.020960–0.039267 个百分点**；移除 `125` 后 B/C 的 AP 为 0.029802%/0.050762%。加入邻距尺度的主组在 19/19 条序列上 AP、AUROC、低误报召回均高于同组 B；逐一排除序列后的 AP 增益为 0.020573–0.038852 个百分点。两组的 C 也在全部 19 条序列上超过各自 A 的 AP。`125` 放大了总体增益，但不是唯一来源。序列是本次相关观测的分组单位，并不保证 19 个独立背景或场地。

仅按距离条件化的增益较弱：AP 在 18/19 条序列上增加，但低误报召回仅 5 条增加、7 条下降、7 条相同。这说明“整体排序略有改善”与“低误报下更有用”需要分别判断。

**哪些几何量提供了证据。** 下面比较各单量在无条件正常参考 B 和距离与射线方向正常参考 C 下的表现。每行内部使用完全相同的有效点，不同几何量之间仍须检查各自覆盖。

| 单个几何量 | B 的 AP（%） | C 的 AP（%） | C 的 AUROC（%） | C 的低误报召回（%） |
| --- | ---: | ---: | ---: | ---: |
| 局部表面残差 | 0.043695 | 0.080188 | 66.214089 | 2.170419 |
| 表面厚度 | 0.033711 | 0.074415 | 68.464782 | 0.532024 |
| 法向变化 | 0.058429 | 0.116620 | 73.199763 | 4.177993 |
| 最小方差占比 | 0.051754 | 0.093542 | 70.471152 | 2.200178 |
| 线性度 | 0.044801 | 0.061810 | 58.567217 | 1.757397 |
| 平面度 | 0.042507 | 0.084171 | 64.382500 | 2.874076 |

法向变化的该行与主六量组合恰好具有相同的 192,608,108 个正常点和 86,453 个异常点。单量 C 的 AP 为 0.116620%、低误报召回 4.177993%，六量最大值组合却分别降至 0.074426% 和 1.425052%。这是当前融合规则的直接负面证据，不支持继续把“增加几何量”自动当作增强。单量结果用于开发诊断，本轮没有据此重新选择正式评分特征或调整方向。

将残差和厚度除以邻距尺度、其余量不变的无条件组合，在同一共同点集上把 AP 从 0.040116% 提高至 0.050583%，AUROC 从 47.012161% 提高至 54.513644%，低误报召回从 0.483500% 提高至 1.108117%。这只是该归一化变换的结果，与条件参考的效果分开解释。最小方差占比仅按距离条件化时，总体 AP 只增加约 0.000061 个百分点，17/19 条序列下降，去掉 `125` 后增益为负；不能概括为所有几何都受益于距离条件化。

描述性分布也显示可计算差异。例如，按异常点数最多而非分布差异大小选择的距离与射线方向箱为距离 [5,10) 米、竖直射线分量 [−0.3,−0.15)。其中保留 38,346 个异常点和 533,421 个抽样正常点，实际进入可靠法向变化分布的有效样本为 38,085 个异常点和 532,719 个抽样正常点。正常加权中位数为 0.036018 弧度，异常中位数为 0.337631 弧度。正方向分布排序量为 0.876037，即在本箱内随机抽取一个有效异常和一个按权重代表的正常点，异常法向变化较大的概率约为 87.60%，同值计半。这个量在看到真值分组后描述分布，不是正常训练分数的 AUROC，也没有用于改成单尾方法。

**覆盖与正常迁移。** 距离组、距离与射线方向组以及无条件归一化组，在 val19 上具有相同的共同覆盖：正常 **99.388850%**，异常 **98.804558%**。因此这些组中没有额外因条件参考不足丢失已具有全部六项可靠几何的点；未覆盖的 1,184,362 个正常点和 1,046 个异常点来自组合几何可靠性不足。加入邻距尺度后，覆盖降为正常 **99.345479%**、异常 **98.633127%**，在前者基础上另失去 84,050 个正常点和 150 个异常点。这些点保持无参考，不算检测出的异常。

| 距离（米） | 全部官方异常点 | 距离与射线方向组覆盖异常 | 加入邻距尺度后覆盖异常 |
| --- | ---: | ---: | ---: |
| [2.5,10) | 66,147 | 65,470（98.9765%） | 65,343（98.7845%） |
| [10,20) | 16,191 | 16,056（99.1662%） | 16,056（99.1662%） |
| [20,35) | 4,805 | 4,686（97.5234%） | 4,686（97.5234%） |
| [35,50] | 356 | 241（67.6966%） | 218（61.2360%） |

35–50 米异常仅占全部官方异常的 0.4069%；总体覆盖不能掩盖这一段的明显缺测。在仅由 `train/206` 决定的固定阈值下，距离与射线方向 C 在四个距离组分别检出 1,104、182、3、0 个异常点，加入邻距尺度的 C 分别检出 1,532、532、4、0 个；各组实际正常误报不同，不能据这些召回计数声称误报匹配后的距离内增益。现有证据主要适用于有可靠邻域的近中距离可见回波，不支持远距、弱遮挡或完整物体高度的有效性主张。

`train/201` 的共同正常覆盖约为 98.13%–98.15%。沿用 `train/206` 的固定阈值，无条件 B 的正常误报约为 1.6613%；距离 C 为 1.3309%，距离与射线方向 C 为 **1.0769%**，加入邻距尺度 C 为 **0.9802%**。后两者比 B 更接近训练时约 1% 的工作点，支持正常侧迁移有所改善，但不能由单个正常来源保证泛化。在 val19 上这两个 C 的固定阈值实际误报分别为 1.0427% 和 1.1867%，召回为 1.4910% 和 2.3962%；这与前述评价曲线上的误报匹配指标属于不同用途。

**真实正常误报。** 固定规则在两种条件 C 下各选最多两帧，优先不同序列，再按全帧正常误报数降序处理；每帧选独立误报坐标最多的一米网格，显示其中心周围六米立方体，不按外观更换样本。

| 案例 | 评分 | 全帧正常误报 | 显示区正常误报 | 显示区官方异常 | 中心至最近官方异常（米） |
| --- | --- | ---: | ---: | ---: | ---: |
| `150/000340` | 距离与射线方向 C | 2,999 | 980 | 0 | 35.28 |
| `125/000025` | 加入邻距尺度 C | 2,623 | 1,581 | 0 | 22.58 |
| `141/000612` | 距离与射线方向 C | 2,978 | 998 | 0 | 34.98 |
| `139/000429` | 加入邻距尺度 C | 2,252 | 644 | 0 | 31.92 |

实际点云显示，所选主要误报位置包括连续竖直片状正常表面和其上的条带式回波；周围也存在其他起伏结构。它们远离真实异常，不能统一归为异常边界混合。`150` 与 `141` 的可见布局很相近，本轮未核对是否为同一背景实体；四个案例不代表四种独立结构，也不是总体误报构成的随机样本。

双尾计分会把过小的几何量也列为少见。以 `125/000025` 显示区为例，在法向变化达到六量最大值的 689 个正常误报点中，632 个来自下尾；最小方差占比相应为 525/543，表面厚度为 185/197。`139/000429` 相应为 74/118、202/214、170/173。多个字段可能同时达到最大值，数量不可相加作因果份额。局部表面残差在四例中却以上尾为主，不能统称“小残差造成误报”。这些实测值说明，当前参考会把某些法向一致、较薄且近似平滑的正常片段列为稀有，正常参考下的少见性与异常标签仍有差距。

总体正常误报中的原始编码 `40` 占比为 97.42%/97.49%，但该编码同时占相应共同正常背景的大多数；该比例不能单独识别物体类别、定义地面真值或说明其单位点风险特别高。结构解释来自实际点云，原始编码只作为可追踪的分组记录。

**邻距尺度的独立变化。** 主表的两种条件组覆盖略有差别。读完主表后，本轮补充了一次明确标记的开发对照：从距离与射线方向 C 的既有精确计数中，仅减去不属于采样尺度共同覆盖的 84,050 个正常点和 150 个异常点。排除身份由已固定的几何有效性和参考支持决定，只对这些排除点重算原分数；不重估正常参考、不改条件箱、不改变评分规则。两种 C 随后都使用 192,524,058 个正常点和 86,303 个异常点。

| 同点指标（%） | 距离与射线方向 C | 再加入邻距尺度 C |
| --- | ---: | ---: |
| AP | 0.074254 | 0.072629 |
| AUROC | 66.081581 | 63.568025 |
| FPR95 | 80.421689 | 88.130267 |
| 误报不超过 1% 时召回率 | 1.411307 | 1.981391 |

追加邻距尺度后，逐序列 AP 为 11 升、8 降，而全部 19 次逐序列排除后的总体 AP 都下降；低误报召回在 19 条序列和全部逐序列排除结果中均提高。FPR95 在所有逐序列与逐序列排除结果中均变差。这支持低误报工作点上的局部收益，同时否定“本次邻距尺度条件化全面改善排序与高召回代价”的表述。完整结果见 `tables/sampling_increment.csv`；其中采样尺度 C 的全部 39 个总体／序列／去序列结果与原主表逐项一致。

**执行与验证。** 完整特征提取已执行，逐点身份与单线程／并行结果核验一致。评分曾因同时保留多个增长中的计数而出现内存压力，改为每八帧写入精确计数块、逐对照归并后完成；没有改变特征、随机流、参考、评分或评价条件。总体计数核对至覆盖表及完整官方分母，补充同点对照另核对排除身份。保留当前有效特征、精确计数和图表；四页统计与四页案例 PDF 已检查实际嵌入 SimSun、Times New Roman。

**本轮裁决与下一问题。** 可计算的法向变化、局部表面残差及部分形态量具有弱预测信息；距离与竖直射线分量的正常参考解释，在公开开发序列和正常迁移来源上提供了重复出现的改善。该结论超出了只依赖真值构造的诊断量，因为所有待评价几何与分数均可从当前帧及训练正常参考计算。

当前证据同时否定了六量最大值融合已能有效整合这些信息的判断，也不支持仅距离条件化对所有形态量普遍有益、加入邻距尺度全面改善、或远距困难条件已经解决。它没有否定所有局部几何，也不能独立证明传感器采样的因果机制、旧原型内部表示的根因或最终网络收益。

真实开发分析已完成，下节保留与当前数据仍相关的原始正常对照。当前不扩充 val19 手工探针，不以拟合开发集分布为补样目标，也不直接将两项几何量升级为网络模块。当前360世界的完成状态和解释范围见本文开头；正式 AJAE V1 的设计尚未开始。

## 原始正常结构对照

原始正常侧使用按帧固定抽样的206／201缓存，每个源帧只计一次。两项几何从完整当前扫描建立邻域，正常参考仅来自206，沿用既定距离、射线分箱和双尾规则。六个案例及对应正常统计保留在 `results/coverage/geometry/`，入口为 `src.profile_report.source_geometry_cases`。

**原始正常内容。** `206` 的 449 份缓存均非空，共 3,544,145 个正常点出现次数；`201` 的 682 份缓存中有 681 份非空，共 3,736,364 个，帧 681 的缓存为空。空缓存仅表示原固定抽样及标签筛选没有保留样本，不能据此认定原扫描为空。法向变化的可靠正常点分别为 3,496,293 和 3,667,130，局部残差为 3,498,423 和 3,670,674；`201` 其中另有 47 和 53 个可靠几何点没有条件参考。

在固定 `206` 条件参考下，`201` 法向变化下尾和上尾分别出现于 649、674 个源帧、各 36 个条件箱；残差两尾分别出现于 656、674 帧、各 37 个箱。这些计数只说明不同取值存在于多个观测条件，不能把同一背景跨帧出现解释成大量独立结构。`206` 自身约十分之一取值落在各尾，部分来自分位数定义，不能作为正常多样性充分的独立证据。

案例按两量同时较小、法向变化较大、残差较大的规则，每源固定三例后再查看原始点云，不因外观替换。六例从原始完整扫描精确复现两量、距离、射线竖直分量和邻距尺度；实际邻域也核对到相同槽位。下面仅描述可见几何，不按原始编码推定物体类别或完整尺寸。

| 原始来源／帧／槽位 | 距离（米） | 法向变化（弧度） | 残差（米） | 实际可见证据 |
| --- | ---: | ---: | ---: | --- |
| `206/270/22210` | 15.474 | 0.001665 | 0.00000259 | 连续、近水平的平滑片状回波，32 个支持位置 |
| `206/122/107690` | 7.350 | 1.528882 | 0.052993 | 起伏轮廓与近水平表面相邻，局部法向变化很大 |
| `206/406/46425` | 8.064 | 1.300240 | 1.087472 | 选定回波与空间上分散的邻域支持错开，不能统称表面粗糙 |
| `201/66/66878` | 32.852 | 0.00001633 | 0.000000281 | 稀疏、近乎共面的片段，仅 8 个支持位置，不能声称大片连续表面 |
| `201/302/49974` | 27.594 | 1.550578 | 0.058012 | 条带与片状回波相邻，19 个支持位置，法向变化很大 |
| `201/36/60616` | 6.651 | 0.179995 | 1.929650 | 32 个邻居集中在约 1.9 米外的另一片竖直表面，查询点与拟合表面不属于同一可见片段 |

六例均至少触发一个原固定单量探针。它们说明平滑正常与高变化正常在原始来源中存在，也暴露了估计解释的限制：邻点数量和法向特征值条件通过，不保证拟合表面与查询点属于同一物理表面。不能把大残差直接当成真实粗糙度，也不能由这六个极端案例推断整批误报的结构构成。六页案例 PDF 已查看，实际嵌入字体为 SimSun 与 Times New Roman。

## 已退出路线的历史结论

原40世界的代表扫描和两条完整轨迹曾观察到非极端异常、复杂正常、插入后标签仍正常但邻域发生变化的回波，以及远距少回波时的几何缺失。它们促成了后续数据设计，但不能代表当前360世界的全池比例。旧样本池报告和插入对照明细已经删除，当前覆盖判断以现存360世界的测量为依据。旧文档内容可由版本控制追溯，不声称旧实验仍可完整复算。

旧探索原型并非正式AJAE V1。其val19官方AP为6.532102%，FPR95为17.214532%，误报不超过1%时召回率为64.351593%；当时观察到同帧异常排序不足和复杂正常误报。旧模型、训练资产及18份独立诊断图表均已删除，只在协议中保留这些历史事实。

A/B辅助监督对照的状态为“训练完成、评价未完成，主动终止”，不记作实验失败。两组各完成一轮，均从共同起点使用修复后的计算路径；修复针对归一化前半精度投影溢出。没有完成的A/B评价不能支持辅助监督有效或有害的结论，也不构成正式AJAE V1的架构约束。

## 必要检查与当前边界

保留的检查针对原始槽位与标签对应、零坐标占位、官方距离与帧门槛、全局同分排序、严格高召回阈值、几何缺失、插入遮挡及不变正常点配对。人工构造数据只验证这些数学与接口含义，不产生正式检测成绩。

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -m pytest -q -p no:cacheprovider
```

文中工作进程数是既往执行或复算示例。新执行前须按当时 CPU、内存、其他任务及 Windows E 盘实际剩余空间确定并发；E盘固定保留至少10 GB，不以WSL逻辑容量代替宿主余量。原始数据、最终360世界及有效几何证据继续保留。当前任务已完成30组合等额选择、原定条件覆盖验收和多余样本删除；正式AJAE V1设计仍未开始。数量均衡、接口测试通过和局部几何探针的弱增益均不替代模型效果结论。
