# 单帧 STU 异常分割研究协议

当前研究对象是一张 STU 激光雷达扫描中的逐点异常。输入为当前扫描的坐标和原始回波强度，输出为同一扫描实际回波的异常分数，分数越高表示越异常。

当前主线为：固定世界的完整合成序列中，每一帧作为独立样本；在固定先导上验证监督计算、损失聚合与可见支持条件，再决定补算范围与模型实验。具体定义见 [v1.json](v1.json)。当前已完成同批 14 帧的损失聚合核对及 C3 可见支持 20 对 10 的比较，不训练、不重生成、不替换池。后续训练仍逐轮查看结果，训练期间不运行渲染器。

当前本地已冻结 32 个训练世界和 8 个合成验证世界，共 19,824 帧。数据生成与单帧还原已经实际执行，模型尚未实现或训练。合成与真实观测的分布仍有残差，尤其是训练源的逐帧距离变化，不能将冻结完成解释为完整轨迹分布已经匹配。

现存池的生成与选择使用了公开验证集的聚合标签统计。尚未核实这种信息使用是否符合正式比较条件；后续正式方案不以该拟合为前提，也不根据先导监督有效率自动重新选择世界。

V1 拟采用 LitePT-S，并研究边界距离监督、按局部采样间距归一化的几何关系，以及从当前扫描预测正常表面的位置。当前尚无已训练模型或真实验证成绩；这三个设计的有效性仍待实验验证。固定训练 10 轮的设定已撤销，后续逐轮查看结果。

机器可读定义在 [spec.json](spec.json)，读取和必要语义检查由 [src/protocol.py](../src/protocol.py) 完成。当前格式为 `stu-single-frame`，格式版本为 1。

## 工作区结构

同类内容集中保存，以下命令均从仓库根目录执行。

| 目录 | 内容 |
| --- | --- |
| `src/` | 单帧数据、辅助监督、评价、几何统计、物理渲染和冻结数据生成代码 |
| `tests/` | 对应的数据身份、评价和几何检查 |
| `protocol/` | 本说明与机器可读定义 |
| `assets/` | 可复用的射线几何标定数据 |
| `results/` | 当前结果；同一次几何统计的明细与表格共用一个目录 |
| `vendor/stu/` | 保持原样的官方评价器及许可证 |

几何统计的明细位于 `results/profile/records/`，表格和统计说明位于 `results/profile/tables/`。只有表格和说明纳入版本控制；明细、预测和其他运行结果保持本地保存。

## 数据范围

开发诊断使用 STU 公开验证集全部 19 条异常序列：125、137、138、139、140、141、142、143、144、145、146、147、148、149、150、151、152、153、169。每条序列的全部原始帧都可以产生单帧预测。

正常数据源 `train/206` 的全部 449 帧用于 32 个训练世界，共 14,368 个单帧样本；`train/201` 的全部 682 帧用于 8 个合成验证世界，共 5,456 个单帧样本。世界及其正常源序列整体分开，不能将同一世界的前后帧分到训练与验证两侧。隐藏测试集不在当前开发范围内，当前数据入口不会开放它。原始数据保留在仓库之外，不随代码提交。

公开验证集已经用于开发分析，后续在此集合观察的规律和模型差异均应保留开发数据的适用边界。几何条件与失误的统计差异不能单独证明因果机制。

目录结构为：

```text
STU/
  train/{201,206}/
  val/<sequence>/
    velodyne/<frame>.bin
    labels/<frame>.label
    calib.txt
    poses.txt
```

`spec.json` 保留已记录的原始压缩包摘要，供需要核对数据来源时使用。

## 单帧读取和模型输入

[STUSequence](../src/scene.py) 通过 `source_frame(frame_id)` 返回一个 `SourceFrame`。索引和迭代同样返回原始单帧。

`SourceFrame.xyzi` 保存完整原始文件槽位，形状为槽位数乘 4，数据类型为 `float32`。`real_slots` 按原始顺序列出坐标不全为零的实际回波槽位。零坐标占位槽继续保存在源数据中，进入模型时通过 `real_slots` 排除。强度保持原值，不能预设它严格位于 0 到 1 之间。

模型可读的基本数组是：

```python
x = frame.xyzi[frame.real_slots]
```

语义标签、实例标签以及依赖标签计算的几何量仅用于监督或诊断，不能作为推理输入。用 `label_mode="forbidden"` 读取源数据时，读取器不打开标签文件。

源对象保留官方坐标及特征的原有计算能力，供数据核对和外部方法适配使用：`coordinates` 按发布位姿计算，`features` 包括原始强度及相对扫描坐标均值的距离。它们是辅助数据表达，不改变当前模型以单帧传感器坐标和强度为输入的任务定义。官方评价距离始终来自 `xyzi[:, :3]`，不能用世界坐标范数代替。

发布标签的低 16 位是语义，高 16 位是实例。原始打包标签完整保留。合成训练的正常监督依据 `normal_semantic_class_map` 中的有效类别，原始忽略类别保持忽略。异常监督只由渲染器的实际插入掩码定义，不能将正常源中的原始语义 2 直接解释成插入异常。

读取示例：

```bash
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m src.scene \
  --data-root /home/jasongao/Data/STU --partition val \
  --sequence 125 --frame 0 --labels required
```

## 预测身份和存储

[src/data.py](../src/data.py) 中的 `FramePrediction` 使用以下字段：

```python
FramePrediction(
    partition,
    sequence_id,
    frame_id,
    source_slot,
    anomaly_score,
)
```

每个分数对应一个明确的原始文件槽位。保存和加载时都必须验证：

- 分区、序列号和帧号与传入的 `SourceFrame` 一致。
- 槽位无重复，且其集合恰好等于 `SourceFrame.real_slots`。
- 分数是有限的 `float32`；生产端显式决定精度转换，存储端不静默量化。
- 分数可以为负，也可以超过 1；评价使用原始数值的排序。

生产端可以采用任意点行顺序，但必须同时保存对应槽位。`restore(source)` 按槽位放回完整原始顺序，并将零坐标槽的分数填为 0。依赖数组行号而忽略槽位身份的结果不能进入评价。

`save(path, source)` 不覆盖已有预测；`load(path, source)` 不仅检查压缩包字段，也重新核对真实源扫描。当前存储格式是 `stu-frame-prediction`，不接受其他任务格式作为当前结果。

统一目录为：

```text
<predictions>/val/<sequence>/<frame:06d>.npz
```

各模型或消融实验分别使用自己的预测根目录。当前尚未导入可靠的单帧模型预测。

## 官方评价和误差归属

[src/evaluate.py](../src/evaluate.py) 保留 [STU 官方评价器](../vendor/stu/compute_point_level_ood.py) 的点集和阈值定义：

- 点到当前传感器的距离满足 `2.5 <= r <= 50` 米。
- 语义 0 忽略，语义 2 为异常，其余有效语义为正常。
- 距离过滤后异常点少于 5 个的帧整体不进入正式指标。
- 所有合格帧的全部有效点合并计算指标，不平均逐帧或逐序列指标。

平均精确率 AP、排序区分指标 AUROC、以及高召回工作点的正常误报比例 FPR95 以百分数报告。FPR95 使用官方保留的 ROC 阈值节点中召回率严格大于 95% 的第一个节点。相同分数的点必须整体进入或离开检测集合。

低误报工作点使用完整官方点集上实际误报比例不超过 1% 时的最高召回率；召回率相同时选择最早达到该召回率的阈值。如果没有异常点能在该条件下检出，阈值记录为空，含义是拒绝全部点。子组统一使用这两个全局工作点。

指标实现保留精确的单精度排序，并采用磁盘临时数组就地排序和分块累计；不把分数量化到直方图。官方评价器保留为独立核对依据。

`APAttribution` 记录每个异常分数对应的全局精确率及所需误报比例。对异常点 `i`，全局同分组全部纳入后的精确率记为 `P_i`，异常点总数记为 `N_A`。其失分为 `(1-P_i)/N_A`，全部异常点的失分之和应为 `1-AP/100`。任意互斥分组可以据此汇总对全局 AP 的失分贡献。

所需误报比例按分数大于或等于该异常点分数的正常点数量除以全局正常点数量计算。它与 AP 失分都来自同一个全局排序。当前模块提供这些通用计算，尚未实现图谱的条件匹配、序列重采样及贡献裁决。

完整开发集评价示例：

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -m src.evaluate --data-root /home/jasongao/Data/STU \
  --predictions results/predictions/<model> --output results/evaluation/<model>
```

可用 `--sequence` 运行明确标记的开发子集。完整评价要求所选序列的每个原始帧均有符合身份约定的预测。临时排序文件使用结束后即释放。大体积评价必须根据当时 Windows E 盘空间保留至少 10 GB 余量。

## 单帧真实几何画像

[src/profile.py](../src/profile.py) 保留单帧统计算法，包括异常点数、距离、强度、最近正常点距离、正常邻域、同实例最近邻、可见实例尺寸、协方差形态和地面关系。几何量在当前扫描中计算，源点身份贯穿统计过程。

画像描述全部原始帧的实际回波，范围比官方指标点集更广。原始异常点数和官方范围内异常点数分别保存；符合官方帧门槛的计数另行汇总。忽略点和范围外点的描述性统计不能混入正式 AP 失分结论。

实例几何与地面估计保留原有可靠性规则。无可靠地面拟合时，高度保持缺失并报告原因。异常点实例标识为零时，不假定这些点属于同一个物体；点级和帧级异常统计继续保留它们。

[src/profile_report.py](../src/profile_report.py) 根据实际生成的逐帧记录、逐实例记录和分布累计结果导出 [统计表格](../results/profile/tables)。分位数来自完整加权经验分布，不平均各序列分位数。表格目录保存：

```text
frames.csv
instances.csv
sequences.csv
totals.csv
count_distance.csv
continuous.csv
categorical.csv
bins.csv
reliability.csv
index.csv
method.md
```

CSV 数值列与实现字段直接对应，空值表示缺失或不适用。CSV 本身不保存字体。详细统计定义由生成源写入 `method.md`，修改定义时应修改生成源并重新生成结果。

完整运行示例：

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -m src.profile --data-root /home/jasongao/Data/STU \
  --output results/profile --workers 12
```

进程数必须依据当时 CPU、内存和其他任务实测状态确定。`--output` 同时决定明细子目录 `records/` 和表格子目录 `tables/`。少量帧检查可使用 `--sequence` 和 `--limit`，并指定独立的 `--output`，避免将子集结果覆盖到完整画像位置。

已有完整明细时，在上述命令中加上 `--tables-only` 可重新汇总并生成表格，无需重新读取原始扫描。表格中的 `index.csv` 使用相对于表格目录的路径指向明细汇总文件。

## 固定世界与冻结单帧数据

[src/render.py](../src/render.py) 保留连续几何、形状族、激光射线、物体放置、地面支持、传感器标定和真实遮挡竞争。`render_frame(source, world, ray_grid, sensor)` 的基本渲染对象是一个 `SourceFrame`；`render_frames` 只是逐个调用单帧渲染的流式工具。

[assets/rays.npz](../assets/rays.npz) 保存独立的射线几何标定数据。`calibrated_ray_grid` 读取该文件，计算束方向、束原点以及原始槽位到射线的对应关系。回波概率与强度规律重新由正常 `train/206` 的全部单帧标定；放置地面的拟合仅使用一张当前正常扫描。物体随后固定在世界坐标中，形状、尺寸、位置、朝向和材质均不随帧变化。

[src/generate.py](../src/generate.py) 提出候选世界，检查完整轨迹中的位置距离及全部帧的已观测障碍物碰撞，再通过 `render_frames` 渲染固定物体。每个已渲染帧写入后重新读取，逐元素核对坐标、强度、打包标签、实际回波槽位和插入、遮挡掩码。没有异常回波的帧仍然写入；物体遮挡原始回波却没有返回信号时，对应槽位及标签清零。

分布比较以公开验证集中同一序列内的非零实例标识为对象单位，使用其完整逐帧观测记录。未知实例的点不用于对象轨迹，数量单独报告。每个对象权重相同；各观测特征分组权重相同。比较内容包括点数与距离的联合分布、可见尺寸、形态、邻近点间距、背景点数和语义、强度差、高度，以及可见比例、连续可见帧数、逐帧变化和距离与点数的相关性。缺失几何量具有独立的统计位置，不能填为零。

每个候选世界都必须完成整条序列，具有至少一个满足官方点数门槛的异常观测，并满足参考轨迹范围。训练与合成验证分别从有限候选集合中选择 32 和 8 个世界，使全部特征组的平均分布差异尽量小。该选择只改变保留哪些完整世界，不修改物体、不修改回波，也不删除单帧。最终清单记录选择前后的逐项差异；有限候选的近似匹配不等于真实与合成分布一致。生成器使用公开验证画像，因此该集合明确属于开发资料，后续逐轮查看的成绩也不具备未见测试集的含义。参考轨迹按发布的实例标识组织，不额外假定所有真实实例在世界坐标中严格静止；合成世界则按照当前研究定义保持固定。

产物集中在 `results/synthetic/v1/`：

```text
manifest.json
reference.json
calibration.pt
train/world_000/{world.json,manifest.json,frames/000000.npz,...}
validation/world_000/{world.json,manifest.json,frames/000000.npz,...}
```

根清单只有在两个划分均完整生成后才标为 `frozen`。各世界清单包含从 0 到源序列末帧的完整身份和观测记录。每帧使用 `stu-frozen-frame` 无损差量格式，保存被改变的原始槽位及其坐标、强度、标签和掩码索引，并绑定原始扫描、标签、正常类别转换、位姿及固定世界的身份。没有改动的背景直接引用原始单帧，避免重复保存 32 份背景。该表示保留完整扫描，不能用来脱离对应原始 STU 文件单独加载。

本轮生成的实际汇总如下；完整逐帧记录和分项差异保存在本地 `results/synthetic/v1/manifest.json` 及各世界清单中。

| 划分 | 世界数 | 单帧数 | 无异常回波的帧 | 插入异常回波数 |
| --- | ---: | ---: | ---: | ---: |
| 训练 | 32 | 14,368 | 9,183 | 111,232 |
| 合成验证 | 8 | 5,456 | 3,642 | 34,951 |

训练候选中 114 个通过检查、14 个被拒绝；合成验证候选中 48 个通过、16 个被拒绝。最终选择将各分项总变差距离的平均值从训练候选集合的 0.1671 降到 0.1032，从合成验证候选集合的 0.0949 降到 0.0794。总变差距离在所采用的统计分箱内取值为 0 到 1；0 表示分箱概率相同，1 表示完全分离。此处每个特征包含缺失值分箱，平均数不能代替逐项判断。

训练集最明显的残差是 `range_step`，即相邻可见帧异常回波中位距离之差的绝对值，再取整条轨迹的中位数。该项总变差距离为 0.8354；32 个训练世界的该量中位数为 0.1710 米／帧，真实参考轨迹为 0.3861 米／帧。该差异与当前观测轨迹有关，不能仅由这个量断言车辆速度是唯一原因。只对具有可靠可见尺寸的帧比较高度分布时，训练集的条件总变差距离仍为 0.3474。当前数据是有限候选下的近似结果，这两项偏差不能被综合均值掩盖；本轮没有修改真实位姿、抽掉原始帧或重定义真实标签来消除它们。

[FrozenDataset](../src/data.py) 按清单加载一个单帧，验证原始输入未变化后还原全部原始槽位；其加载过程不导入或调用渲染器。`sample.source.xyzi[sample.source.real_slots]` 是模型可见的当前坐标和原始强度，`sample.anomaly_target` 为按原始槽位排列的监督：0 正常、1 插入异常、−1 忽略。地面标签和放置平面仅能用于后续辅助监督或诊断，不能成为推理特征。

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -m src.generate --workers 2 --pilot 4
```

先导运行沿正式候选种子产生完整世界，保留可直接复用的已冻结帧，但不宣布数据集完成。资源实测后，去掉 `--pilot` 并设置适当进程数，继续生成剩余候选、选择世界并写出完整清单。既有完整数据集不覆盖。写入过程限制每个候选及全部候选的空间占用，并持续查询 Windows E 盘的实际剩余空间。

当前渲染工具不作为真实画像或官方评价的依赖。合成测试用例仅验证实现和物理遮挡约定，不能作为真实数据上的科学结论。

## 单帧辅助监督先导

[src/supervision.py](../src/supervision.py) 提供可复用的 `ScanGeometry`、`boundary_targets`、`thinning_pair`、`sampling_targets`、`surface_targets` 和组合入口 `compute_supervision`。`ScanGeometry` 只读取当前视图的坐标与原始槽位；三项监督函数使用的检测标签、原始正常表面、插入/遮挡掩码与配对身份只用于训练，不能进入真实推理输入。

先导样本在辅助计算之前写入 `v1.json` 的 `supervision.pilot.samples`：训练侧 6 帧、合成验证侧 8 帧，依据既有清单和物体参数固定选取，包含无异常、少点、较密、远距、低矮及 `201` 特殊槽位情况。基础视图及两个规定的删减视图均使用完整当前扫描计算邻域。坐标去重只影响几何支持计数，原始点行和异常标签保持原样。

现有环境使用 NumPy、SciPy 和 Numba；束/列删减仅调用射线映射，不调用物理渲染。依据当时 CPU、内存和 Windows E 盘资源确定线程数后执行：

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  .venv/bin/python -m src.supervision --data-root /home/jasongao/Data/STU --workers 16
```

辅助结果单独保存在 `results/supervision/pilot/<split>/<world>/<frame>.npz`，同名 JSON 保存计数、失效原因、相位及几何核对。NPZ 的 `view_0/` 是基础视图，`view_1/`、`view_2/` 是两个删减级别；每个视图均保存完整输入回波的 `source_slot`，删减视图另存 `dense_row`。目标和掩码按这些点行排列；基础检测标签由冻结帧按槽位读取。NPZ 不复制原始背景，也不改写世界或冻结基础帧。

[先导汇总](../results/supervision/pilot/summary.json) 保存正常/异常有效点数、条件分组、C1 非饱和目标数、C2 保留率及保留点中的有效率、C3 失效原因和独立几何核对。失效原因编码为位集合，0 为有效；C1/C2 可多因并存，C3 记录计算中首先失败的条件。基础与删减视图分别计算尺度、C1 图和 C3 可见支持；同一查询位置的插入前平面只拟合一次。

本次已计算固定的 14 帧和 42 个视图，不据此估计全池覆盖率，也不以有效率作为通过门槛。稀疏、远距样本的辅助监督缺失和 C1 截断目标占比需要据实讨论；辅助无效不改变检测标签、不删除样本。当前停止在先导报告，尚未补算全量监督。

这批基础扫描包含 1,693,399 条正常回波和 556 条异常回波；556 条异常对应 554 个不同坐标。忽略标签回波仍进入邻域计算，但不计入下表正常/异常分母。半采样指列步长 2；四分之一采样指束、列步长均为 2，两者使用各自规定的随机相位。

| 监督及视图 | 正常有效／当前正常点 | 异常有效／当前异常点 |
| --- | ---: | ---: |
| C1 基础 | 140,890／1,693,399（8.32%） | 538／556（96.76%） |
| C1 半采样 | 86,419／846,637（10.21%） | 272／283（96.11%） |
| C1 四分之一采样 | 36,930／423,551（8.72%） | 137／149（91.95%） |
| C2 半采样 | 735,371／846,637（86.86%） | 269／283（95.05%） |
| C2 四分之一采样 | 336,682／423,551（79.49%） | 134／149（89.93%） |
| C3 基础 | 861,173／1,693,399（50.85%） | 547／556（98.38%） |
| C3 半采样 | 410,548／846,637（48.49%） | 272／283（96.11%） |
| C3 四分之一采样 | 188,823／423,551（44.58%） | 139／149（93.29%） |

C2 的异常保留率分别是 283／556＝50.90% 和 149／556＝26.80%；有效一致性点占基础异常点的比例分别为 269／556＝48.38% 和 134／556＝24.10%。不能只报告保留点中的 95.05% 和 89.93%。

下列条件可以重叠。低矮由世界参数中的物体局部完整高度不超过 0.2 米定义；远距由异常回波距离中位数在 35–50 米定义。C2 列依次列出“保留／基础；有效／保留”，原始点数分母不能省略。

| 条件 | 帧数 | 基础异常数 | C1 基础有效 | C2 半采样 | C2 四分之一采样 | C3 基础有效 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 1–4 点异常 | 5 | 13 | 0／13（0%） | 8／13；0／8 | 7／13；0／7 | 4／13（30.8%） |
| 5–19 点异常 | 3 | 27 | 22／27（81.5%） | 15／27；12／15 | 9／27；3／9 | 27／27（100%） |
| 至少 100 点异常 | 2 | 449 | 449／449（100%） | 227／449；224／227 | 116／449；114／116 | 449／449（100%） |
| 35–50 米远距 | 3 | 6 | 0／6（0%） | 3／6；0／3 | 3／6；0／3 | 0／6（0%） |
| 低矮物体 | 7 | 385 | 376／385（97.7%） | 195／385；190／195 | 103／385；92／103 | 381／385（99.0%） |

3 张无异常帧的 C1 均关闭，C2/C3 继续计算正常点监督。两个 `201` 特殊槽位样本均完成核对；其中 `validation/world_000/0` 的 3 条异常记录是同一位置的三个槽位，分别为 65078、196150、327222。它们在两个删减视图中均共同保留，几何支持只计一个位置；该异常位于 62.65 米，属于槽位核对样本，超出官方评价距离。

C1 的基础视图有 73 条可信交界边，异常边缘标签仍为 1；距离经同标签边传播。基础异常中 18 点无效：11 点尺度及局部支持不足，7 点没有可达交界。正常点主要因没有通向观测交界的同标签路径而无效。更突出的限制是饱和：正常有效点只有 163 个目标小于 1，异常有效点只有 144 个；两类合计的非饱和比例为 307／141,428＝0.217%。例如 `validation/world_003/310` 的槽位 105819，图距离为 0.363980 米，尺度为 0.173374 米，归一化目标为 0.699796，标签保持异常。大量饱和点对训练的影响仍待决定，不能由整体异常有效率代替判断。

C2 的异常失效主要来自删减后尺度或标签组支持不足，部分点失去规定的空间跨度；正常点最常见的失效原因也是尺度不足。对上述验证帧槽位 105820，半采样将尺度从 0.123980 米变为 0.196751 米，正常/异常局部支持分别从 24/67 个位置变为 12/33 个位置，仍符合一致性条件。1–4 点异常在这批样本的两个删减级别下均没有有效一致性点，需作为明确的覆盖缺口保留。

C3 基础异常的 9 个无效点均缺少至少 20 个插入前地面支持；远距代表点实际只有 0、7 或 9 个支持，特殊槽位异常没有局部地面支持。正常点主要因插入前支持不足（439,336 点）或查询位置在支持凸包外（318,171 点）而无效。低矮异常已经获得有效目标：例如 `validation/world_003/310` 槽位 105820 的查询高度为 −1.857585 米，插入前平面高度为 −2.000723 米，目标为 −0.143138 米；插入后仍有 1,474 个地面支持，半采样后为 740 个，目标位移保持原值。

C3 的最低可见支持数有可以单独讨论的实际例子。`train/world_015/285` 槽位 71191 在基础视图有 26 个可见支撑，目标 −0.066704 米；半采样保留该异常点，但可见支撑降至 13 个，因此现有掩码关闭。仅为解释失效原因，继续核对这 13 个支撑：矩阵满秩、平面误差 0.032387 米、水平协方差最小特征值 0.729152 平方米，查询点位于支撑凸包内，其余规定条件均满足。四分之一采样仅剩 6 个支撑，查询点已在凸包外；此时只放宽数量要求也不足以恢复有效性。这些附加几何诊断没有改变目标或掩码，也没有证明减少支持数仍可保持真实学习效果。

先导对全部交界路径做同标签与最短路径核对，逐点检查两个删减视图的对应关系；所有基础和保留异常回波的 C3 另用 NumPy 最小二乘与 SciPy 凸包独立复算。全套 26 项测试通过，`train/world_015/446` 的 50 个辅助数组在 1 与 16 个线程下逐元素相同。修正过一处报告写出错误：无异常清单没有距离字段，现按缺失值处理；监督公式和样本选择未改变。

上述结果是 `9a0ecb5` 的监督计算先导。它支持计算实现及该批样本的覆盖判断，没有确认三项辅助监督会改善真实分割结果；损失聚合与可见支持单变量比较见下一节。

## 损失聚合与可见支持对照

`supervision.loss` 定义当前采用的聚合规则；`src/supervision.py` 中的 `boundary_loss`、`sampling_loss`、`surface_loss` 是可求梯度的 PyTorch 实现。每次调用处理当前小批量的同一视图或同一删减级别：先合并该批点行，再按类别求均值。拼接多个帧时，C2 的 `dense_row` 必须加上各基础帧在批次中的点行偏移，使其仍指向同一帧、同一槽位的基础预测。

| 辅助项 | 点误差 | 聚合规则 |
| --- | --- | --- |
| C1 | 预测距离与既有距离目标的绝对误差 | 有效且目标小于 1 为交界带，等于 1 为饱和区；每个区域内先分别平均正常点、异常点，再对存在类别等权。两个区域均存在时，交界带占 0.75，饱和区占 0.25。 |
| C2 | 稀疏异常概率与停止梯度的对应基础异常概率之差的平方 | 在原有一致性有效掩码内，分别平均正常点、异常点，再对存在类别等权。 |
| C3 | 预测表面位移与既有目标的绝对误差 | 在正式默认可见支持至少 20 的有效掩码内，分别平均正常点、异常点，再对存在类别等权。 |

空类别跳过；C1 空区域跳过后，将剩余区域权重归一化。整项无有效点时返回可反向传播的零；先选有效点再计算误差，不用无效占位值补目标。若同时使用多个视图，同一辅助项对存在有效监督的视图/级别等权平均。三项辅助损失与基础检测损失之间的系数本轮未设定。3∶1 是初始区域权重，未证明最优；所有分组信息和点权重只用于损失，不进入推理输入。

同一批 14 帧分视图合并后，直接计算恒预测 1 的 C1 损失，并用新权重下的加权中位数求最优常数。这里没有训练网络。

| 视图 | 原逐点等权：恒预测 1 | 新聚合：恒预测 1 | 新聚合下最优常数 | 该常数的新损失 |
| --- | ---: | ---: | ---: | ---: |
| 基础 | 0.001094 | 0.378209 | 0.657314 | 0.287008 |
| 半采样 | 0.001911 | 0.351113 | 0.728553 | 0.281759 |
| 四分之一采样 | 0.003647 | 0.416506 | 0.562172 | 0.304636 |

原逐点等权下，三个视图的最优常数均为 1。新聚合中，交界带正常/异常组的总权重各为 0.375，饱和区两组各为 0.125；基础视图中 140,727 个饱和正常点只占 0.125。复制一遍全部饱和组后，新聚合的恒预测 1 损失保持不变，验证了饱和点数量不会继续压低该损失。上述数值只说明聚合性质，不是模型性能比较，也不能保证网络不会学成其他常数。

C2 半采样有效点中的异常比例为 0.03657%，四分之一采样为 0.03978%；C3 基础视图为 0.06348%。新聚合对存在的正常、异常类别均值各赋予 0.5 权重。它规定的是误差均值的系数，实际梯度大小仍由预测误差决定。C2 的配对、相位和有效条件没有改动，失去异常证据的点仍关闭一致性。

C3 已将数量参数分开：`minimum_reference_support_points=20` 同时约束插入前参考点和筛选后的拟合内点；`minimum_visible_support_points=20` 是正式默认。比较只把后者设为 10，满秩、误差、空间展开和凸包条件均不变。`surface_targets` 返回按可见支持下限索引的视图结果，默认取 `[20]`；比较时传入 `[20,10]`，共用一次插入前平面拟合和每个视图的后续几何检查。

运行命令如下，仍只读取原有 14 帧及其三个视图。比较没有重算或改写 C1/C2 目标；原辅助文件和冻结基础帧均保持原样。

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  .venv/bin/python -m src.supervision --compare --workers 16
```

结果单独保存于 [comparison/summary.json](../results/supervision/pilot/comparison/summary.json)，同目录的 `<split>/<world>/<frame>.npz` 保存下限 10 的完整 C3 数组及恢复点槽位，标记 `formal_default=false`。同名 JSON 保存逐帧类别计数和独立几何核对。旧版无效目标中的 0 只是占位；新恢复点的数值取自同一插入前参考平面。

| 视图 | 正常有效：20 → 10（新增） | 异常有效：20 → 10（新增） | 其中低矮异常：20 → 10（新增） |
| --- | ---: | ---: | ---: |
| 基础 | 861,173 → 861,339（166） | 547 → 547（0） | 381 → 381（0） |
| 半采样 | 410,548 → 426,325（15,777） | 272 → 277（5） | 190 → 193（3） |
| 四分之一采样 | 188,823 → 197,129（8,306） | 139 → 139（0） | 97 → 97（0） |

计数单位是实际文件回波行；低矮异常是异常的子集。三个视图不能相加后解释成不重复的物理点。正常新增行对应的不同坐标数分别为 166、10,770、6,136，差别来自 `201` 的重复槽位。

正常点恢复的逐帧来源如下；所有异常恢复均发生在半采样视图。

| 来源／世界／帧 | 正常新增：基础 | 正常新增：半采样 | 正常新增：四分之一采样 | 异常新增（其中低矮） |
| --- | ---: | ---: | ---: | ---: |
| 训练／world_016／385 | 0 | 640 | 685 | 0 |
| 训练／world_011／322 | 20 | 664 | 296 | 2（0） |
| 训练／world_015／285 | 146 | 1,190 | 446 | 3（3） |
| 训练／world_015／446 | 0 | 674 | 548 | 0 |
| 训练／world_010／210 | 0 | 1,118 | 501 | 0 |
| 训练／world_018／191 | 0 | 1,010 | 380 | 0 |
| 合成验证／world_004／116 | 0 | 493 | 565 | 0 |
| 合成验证／world_003／451 | 0 | 108 | 163 | 0 |
| 合成验证／world_003／260 | 0 | 630 | 312 | 0 |
| 合成验证／world_001／499 | 0 | 101 | 192 | 0 |
| 合成验证／world_003／189 | 0 | 234 | 264 | 0 |
| 合成验证／world_003／310 | 0 | 597 | 319 | 0 |
| 合成验证／world_003／3 | 0 | 3,230 | 1,520 | 0 |
| 合成验证／world_000／0 | 0 | 5,088 | 2,115 | 0 |

恢复的异常槽位为 `train/world_011/322` 的 68119、69151（均有 17 个可见支撑），以及低矮样本 `train/world_015/285` 的 71189、71191、71193（分别有 14、13、11 个支撑）。这些点均重新通过后续几何条件。例如槽位 71191 的参考点和内点均为 28，目标仍为 −0.066704 米；13 个可见支撑相对原平面的误差为 0.032387 米，水平协方差最小特征值为 0.729152 平方米，查询点在凸包内。

原来首先因可见支持少于 20 而失效的正常点，在下限 10 下仍有下列失效情况。表中记录首次失败条件；数量仍不足 10 时不宣称其他几何条件合格。

| 视图 | 原数量不足 20 | 恢复有效 | 仍不足 10 | 凸包不包含查询点 | 可见支持质量不合格 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 基础 | 171 | 166 | 0 | 0 | 5 |
| 半采样 | 17,292 | 15,777 | 383 | 882 | 250 |
| 四分之一采样 | 23,027 | 8,306 | 7,620 | 2,346 | 4,755 |

质量不合格进一步分为：基础视图 5 点水平展开不足；半采样 170 点平面误差过大、83 点水平展开不足，其中 3 点同时存在这两项问题；四分之一采样分别为 139 和 4,616 点。四分之一采样的 4 个原数量不足异常点仍少于 10 个支持，其中 3 个是低矮异常；原先因参考表面、凸包等条件失效的其余点没有因这次数量修改而转为有效。

默认 20 的 336 个 C3 数组与原先导逐元素一致，原本有效的目标值完全不变。新增和仍失效点另做了 184 次独立 NumPy/SciPy 几何核对；30 项测试通过，包括参考点不足 20、内点不足 20 时不得因比较下限 10 而获得有效目标，以及分组权重、空组、无效值和 C2 停止梯度检查。

这次比较确认：调整可见支持数量能够在半采样的两个训练帧中恢复少量异常监督，同时仍会被其他几何条件约束。有效率增加不能证明 10 更好；正式默认保持 20。远距组只有 3 帧、6 个异常点，继续作为后续检查缺口，不据此裁决远距机制失败或要求重生成世界。本轮停止在损失聚合规则和这一单变量对照，没有扩大先导、补算全池或训练模型。

## 必要验证与当前边界

关键检查对应数据和评价的实际风险：原始槽位错配、标签错配、零坐标槽处理、距离边界、少于 5 个异常点的帧过滤、全局同分排序、严格高召回阈值、AP 失分加总、地面估计缺失和前景遮挡。

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -m pytest -q -p no:cacheprovider
```

接口检查使用人工构造的分数时，只能支持存储、排序和评价实现的正确性，不产生模型性能证据。

数据集还原、辅助目标和损失聚合核对只支持相应实现及先导观测，不说明模型已经学会异常分割。当前损失规则已经明确，C3 默认仍为参考支持 20、可见支持 20。是否正式采用比较条件或进入模型实验仍待后续决定；边界、采样尺度和正常表面设计是否改善真实异常检测，须由模型输出与对应对照支持。
