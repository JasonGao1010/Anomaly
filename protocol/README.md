# 单帧 STU 异常分割研究协议

当前研究对象是一张 STU 激光雷达扫描中的逐点异常。输入为当前扫描的坐标和原始回波强度，输出为同一扫描实际回波的异常分数，分数越高表示越异常。

当前主线为：每个固定物体世界渲染完整正常源序列，每帧作为独立样本。已有内容检查已确认组合形态、合格远距与低矮远距的世界覆盖缺口；本轮已将 [v1.json](v1.json) 的生成方案落实到实际入口，完成 32 个固定候选世界、18,096 帧的试生成及 134 张完整扫描的监督检查。保留现有辅助监督、损失和 C3 默认 20，模型尚未训练；正式新池配额与冻结仍待确定。

历史开发池 `results/synthetic/v1/` 保留 32 个训练世界、8 个合成验证世界，共 19,824 帧。它已使用 val19 聚合标签统计生成与选样，不能作为未使用这些统计的新数据。新候选另存于 `results/synthetic/candidates/`，只使用正常来源、射线和预先规定的内容条件，不读取真实参考包络或拟合分布差异。

V1 拟采用 LitePT-S，并研究边界距离监督、按局部采样间距归一化的几何关系，以及从当前扫描预测正常表面的位置。当前尚无已训练模型或真实验证成绩；这三个设计的有效性仍待实验验证。固定训练 10 轮的设定已撤销，后续逐轮查看结果。

机器可读定义在 [spec.json](spec.json)，读取和必要语义检查由 [src/protocol.py](../src/protocol.py) 完成。当前格式为 `stu-single-frame`，格式版本为 1。

## 工作区结构

同类内容集中保存，以下命令均从仓库根目录执行。

| 目录 | 内容 |
| --- | --- |
| `src/` | 单帧数据、辅助监督、评价、几何统计、物理渲染和冻结数据生成代码 |
| `tests/` | 对应的数据身份、评价和几何检查 |
| `protocol/` | 本说明与机器可读定义 |
| `assets/` | 可复用的射线几何标定数据 |
| `results/` | 当前结果；同一次几何统计的明细与表格共用一个目录 |
| `vendor/stu/` | 保持原样的官方评价器及许可证 |

几何统计的明细位于 `results/profile/records/`，表格和统计说明位于 `results/profile/tables/`。只有表格和说明纳入版本控制；明细、预测和其他运行结果保持本地保存。

## 数据范围

开发诊断使用 STU 公开验证集全部 19 条异常序列：125、137、138、139、140、141、142、143、144、145、146、147、148、149、150、151、152、153、169。每条序列的全部原始帧都可以产生单帧预测。

正常数据源 `train/206` 的全部 449 帧用于训练世界；`train/201` 的全部 682 帧用于合成验证世界。本轮每侧预先规定 16 个候选，正式保留配额尚未确定。历史开发池每侧的世界数分别为 32 和 8。世界及其正常源序列整体分开，不能将同一世界的前后帧分到训练与验证两侧。隐藏测试集不在当前开发范围内，当前数据入口不会开放它。原始数据保留在仓库之外，不随代码提交。

公开验证集已经用于开发分析，后续在此集合观察的规律和模型差异均应保留开发数据的适用边界。几何条件与失误的统计差异不能单独证明因果机制。

目录结构为：

```text
STU/
  train/{201,206}/
  val/<sequence>/
    velodyne/<frame>.bin
    labels/<frame>.label
    calib.txt
    poses.txt
```

`spec.json` 保留已记录的原始压缩包摘要，供需要核对数据来源时使用。

## 单帧读取和模型输入

[STUSequence](../src/scene.py) 通过 `source_frame(frame_id)` 返回一个 `SourceFrame`。索引和迭代同样返回原始单帧。

`SourceFrame.xyzi` 保存完整原始文件槽位，形状为槽位数乘 4，数据类型为 `float32`。`real_slots` 按原始顺序列出坐标不全为零的实际回波槽位。零坐标占位槽继续保存在源数据中，进入模型时通过 `real_slots` 排除。强度保持原值，不能预设它严格位于 0 到 1 之间。

模型可读的基本数组是：

```python
x = frame.xyzi[frame.real_slots]
```

语义标签、实例标签以及依赖标签计算的几何量仅用于监督或诊断，不能作为推理输入。用 `label_mode="forbidden"` 读取源数据时，读取器不打开标签文件。

源对象保留官方坐标及特征的原有计算能力，供数据核对和外部方法适配使用：`coordinates` 按发布位姿计算，`features` 包括原始强度及相对扫描坐标均值的距离。它们是辅助数据表达，不改变当前模型以单帧传感器坐标和强度为输入的任务定义。官方评价距离始终来自 `xyzi[:, :3]`，不能用世界坐标范数代替。

发布标签的低 16 位是语义，高 16 位是实例。原始打包标签完整保留。合成训练的正常监督依据 `normal_semantic_class_map` 中的有效类别，原始忽略类别保持忽略。异常监督只由渲染器的实际插入掩码定义，不能将正常源中的原始语义 2 直接解释成插入异常。

读取示例：

```bash
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m src.scene \
  --data-root /home/jasongao/Data/STU --partition val \
  --sequence 125 --frame 0 --labels required
```

## 预测身份和存储

[src/data.py](../src/data.py) 中的 `FramePrediction` 使用以下字段：

```python
FramePrediction(
    partition,
    sequence_id,
    frame_id,
    source_slot,
    anomaly_score,
)
```

每个分数对应一个明确的原始文件槽位。保存和加载时都必须验证：

- 分区、序列号和帧号与传入的 `SourceFrame` 一致。
- 槽位无重复，且其集合恰好等于 `SourceFrame.real_slots`。
- 分数是有限的 `float32`；生产端显式决定精度转换，存储端不静默量化。
- 分数可以为负，也可以超过 1；评价使用原始数值的排序。

生产端可以采用任意点行顺序，但必须同时保存对应槽位。`restore(source)` 按槽位放回完整原始顺序，并将零坐标槽的分数填为 0。依赖数组行号而忽略槽位身份的结果不能进入评价。

`save(path, source)` 不覆盖已有预测；`load(path, source)` 不仅检查压缩包字段，也重新核对真实源扫描。当前存储格式是 `stu-frame-prediction`，不接受其他任务格式作为当前结果。

统一目录为：

```text
<predictions>/val/<sequence>/<frame:06d>.npz
```

各模型或消融实验分别使用自己的预测根目录。当前尚未导入可靠的单帧模型预测。

## 官方评价和误差归属

[src/evaluate.py](../src/evaluate.py) 保留 [STU 官方评价器](../vendor/stu/compute_point_level_ood.py) 的点集和阈值定义：

- 点到当前传感器的距离满足 `2.5 <= r <= 50` 米。
- 语义 0 忽略，语义 2 为异常，其余有效语义为正常。
- 距离过滤后异常点少于 5 个的帧整体不进入正式指标。
- 所有合格帧的全部有效点合并计算指标，不平均逐帧或逐序列指标。

平均精确率 AP、排序区分指标 AUROC、以及高召回工作点的正常误报比例 FPR95 以百分数报告。FPR95 使用官方保留的 ROC 阈值节点中召回率严格大于 95% 的第一个节点。相同分数的点必须整体进入或离开检测集合。

低误报工作点使用完整官方点集上实际误报比例不超过 1% 时的最高召回率；召回率相同时选择最早达到该召回率的阈值。如果没有异常点能在该条件下检出，阈值记录为空，含义是拒绝全部点。子组统一使用这两个全局工作点。

指标实现保留精确的单精度排序，并采用磁盘临时数组就地排序和分块累计；不把分数量化到直方图。官方评价器保留为独立核对依据。

`APAttribution` 记录每个异常分数对应的全局精确率及所需误报比例。对异常点 `i`，全局同分组全部纳入后的精确率记为 `P_i`，异常点总数记为 `N_A`。其失分为 `(1-P_i)/N_A`，全部异常点的失分之和应为 `1-AP/100`。任意互斥分组可以据此汇总对全局 AP 的失分贡献。

所需误报比例按分数大于或等于该异常点分数的正常点数量除以全局正常点数量计算。它与 AP 失分都来自同一个全局排序。当前模块提供这些通用计算，尚未实现图谱的条件匹配、序列重采样及贡献裁决。

完整开发集评价示例：

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -m src.evaluate --data-root /home/jasongao/Data/STU \
  --predictions results/predictions/<model> --output results/evaluation/<model>
```

可用 `--sequence` 运行明确标记的开发子集。完整评价要求所选序列的每个原始帧均有符合身份约定的预测。临时排序文件使用结束后即释放。大体积评价必须根据当时 Windows E 盘空间保留至少 10 GB 余量。

## 单帧真实几何画像

[src/profile.py](../src/profile.py) 保留单帧统计算法，包括异常点数、距离、强度、最近正常点距离、正常邻域、同实例最近邻、可见实例尺寸、协方差形态和地面关系。几何量在当前扫描中计算，源点身份贯穿统计过程。

画像描述全部原始帧的实际回波，范围比官方指标点集更广。原始异常点数和官方范围内异常点数分别保存；符合官方帧门槛的计数另行汇总。忽略点和范围外点的描述性统计不能混入正式 AP 失分结论。

实例几何与地面估计保留原有可靠性规则。无可靠地面拟合时，高度保持缺失并报告原因。异常点实例标识为零时，不假定这些点属于同一个物体；点级和帧级异常统计继续保留它们。

[src/profile_report.py](../src/profile_report.py) 根据实际生成的逐帧记录、逐实例记录和分布累计结果导出 [统计表格](../results/profile/tables)。分位数来自完整加权经验分布，不平均各序列分位数。表格目录保存：

```text
frames.csv
instances.csv
sequences.csv
totals.csv
count_distance.csv
continuous.csv
categorical.csv
bins.csv
reliability.csv
index.csv
method.md
```

CSV 数值列与实现字段直接对应，空值表示缺失或不适用。CSV 本身不保存字体。详细统计定义由生成源写入 `method.md`，修改定义时应修改生成源并重新生成结果。

完整运行示例：

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -m src.profile --data-root /home/jasongao/Data/STU \
  --output results/profile --workers 12
```

进程数必须依据当时 CPU、内存和其他任务实测状态确定。`--output` 同时决定明细子目录 `records/` 和表格子目录 `tables/`。少量帧检查可使用 `--sequence` 和 `--limit`，并指定独立的 `--output`，避免将子集结果覆盖到完整画像位置。

已有完整明细时，在上述命令中加上 `--tables-only` 可重新汇总并生成表格，无需重新读取原始扫描。表格中的 `index.csv` 使用相对于表格目录的路径指向明细汇总文件。

## 固定世界与冻结单帧数据

[src/render.py](../src/render.py) 保留连续几何、形状族、激光射线、物体放置、地面支持、传感器标定和真实遮挡竞争。`render_frame(source, world, ray_grid, sensor)` 的基本渲染对象是一个 `SourceFrame`；`render_frames` 只是逐个调用单帧渲染的流式工具。

[assets/rays.npz](../assets/rays.npz) 保存独立的射线几何标定数据。`calibrated_ray_grid` 读取该文件，计算束方向、束原点以及原始槽位到射线的对应关系。回波概率与强度规律重新由正常 `train/206` 的全部单帧标定；放置地面的拟合仅使用一张当前正常扫描。物体随后固定在世界坐标中，形状、尺寸、位置、朝向和材质均不随帧变化。

[src/generate.py](../src/generate.py) 提出候选世界，检查完整轨迹中的位置距离及全部帧的已观测障碍物碰撞，再通过 `render_frames` 渲染固定物体。每个已渲染帧写入后重新读取，逐元素核对坐标、强度、打包标签、实际回波槽位和插入、遮挡掩码。没有异常回波的帧仍然写入；物体遮挡原始回波却没有返回信号时，对应槽位及标签清零。

新候选按八类内容提出：单基元、扁平单基元、双部件接合、扁平弯折、远距稀疏、远距较大投影、低矮远距单基元、低矮远距组合。每类在两个正常来源上各有两个固定种子；类别参数、种子规则和支持搜索预算见 `v1.json:proposals`。每个物体的完整局部几何范围包含全部基元及偏移，统一用于尺寸、低矮条件和放置支撑；碰撞检查使用完整形状及其保守范围。组合部件必须连通，贴地后检查完整物体的下界与埋地比例。

放置只使用正常源数据。每个固定形状最多提出 128 个正常支持位置，收集至多 6 个满足局部平面、整物体投影支撑和背景类别的候选。远距类别在实际轨迹的约 35、42.5、50 米位置读取真实射线，计算未被原始回波遮挡的整物体包围盒交点，按预定投影机会数选择支持与朝向。包围盒交点只是候选提出的上界线索；最终异常回波由原有物理渲染器产生，不补点，也不保证删减后一致性有效。

完整渲染后按普通少点、合格远距少点、较密远距、低矮、低矮远距分别记录世界数与帧点贡献。世界选择按内容类别轮流取样，类别内优先选已选集合中较少的形态与背景搭配；实际帧数多少和辅助掩码有效率不参与排序。没有官方合格异常帧的物理世界可继续检查，但不进入正式选样；已合格世界里的无异常帧、极少点帧和辅助无效帧全部保留。

新产物集中在 `results/synthetic/candidates/`：

```text
manifest.json
calibration.pt
train/candidate_000/{world.json,manifest.json,frames/000000.npz,...}
validation/candidate_000/{world.json,manifest.json,frames/000000.npz,...}
```

默认命令完成候选后记录 `candidates_complete`，并给出按内容分层的选样顺序，尚不冻结正式训练池。根清单的 `planned_candidates` 在执行前记录全部种子；`generation` 保留每次尝试、完整观测与失败原因。物理碰撞失败的世界完成整条渲染后保留失败证据，不列入可用候选。每帧采用 `stu-frozen-frame` 无损差量格式，绑定原始扫描、标签、位姿和固定世界，保存变化槽位；未改变的背景引用原始正常扫描，没有删除正常结构。

历史开发池采用过 val19 参考轨迹包络和最小总变差选样，相关数值只说明该历史生成流程的行为。其完整配置与参考记录保留在 `results/synthetic/v1/`，生成入口已移除该路径。以下开发池结果仍可用于对照，不能解释为新候选结果。

历史开发池的实际汇总如下；完整逐帧记录和分项差异保存在本地 `results/synthetic/v1/manifest.json` 及各世界清单中。

| 划分 | 世界数 | 单帧数 | 无异常回波的帧 | 插入异常回波数 |
| --- | ---: | ---: | ---: | ---: |
| 训练 | 32 | 14,368 | 9,183 | 111,232 |
| 合成验证 | 8 | 5,456 | 3,642 | 34,951 |

训练候选中 114 个通过检查、14 个被拒绝；合成验证候选中 48 个通过、16 个被拒绝。最终选择将各分项总变差距离的平均值从训练候选集合的 0.1671 降到 0.1032，从合成验证候选集合的 0.0949 降到 0.0794。总变差距离在所采用的统计分箱内取值为 0 到 1；0 表示分箱概率相同，1 表示完全分离。此处每个特征包含缺失值分箱，平均数不能代替逐项判断。

训练集最明显的残差是 `range_step`，即相邻可见帧异常回波中位距离之差的绝对值，再取整条轨迹的中位数。该项总变差距离为 0.8354；32 个训练世界的该量中位数为 0.1710 米／帧，真实参考轨迹为 0.3861 米／帧。该差异与当前观测轨迹有关，不能仅由这个量断言车辆速度是唯一原因。只对具有可靠可见尺寸的帧比较高度分布时，训练集的条件总变差距离仍为 0.3474。当前数据是有限候选下的近似结果，这两项偏差不能被综合均值掩盖；本轮没有修改真实位姿、抽掉原始帧或重定义真实标签来消除它们。

[FrozenDataset](../src/data.py) 按清单加载一个单帧，验证原始输入未变化后还原全部原始槽位；其加载过程不导入或调用渲染器。`sample.source.xyzi[sample.source.real_slots]` 是模型可见的当前坐标和原始强度，`sample.anomaly_target` 为按原始槽位排列的监督：0 正常、1 插入异常、−1 忽略。地面标签和放置平面仅能用于后续辅助监督或诊断，不能成为推理特征。

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  .venv/bin/python -m src.generate --workers 16
```

进程数依据当时资源确定。默认执行所有预先规定的候选，完整数据集和不同生成输入的目录不覆盖；同一批中断可续算。写入仍使用有界流式格式，并核对 Windows E 盘实际剩余空间。候选实测后才可在同一入口用 `--select <训练配额> <合成验证配额>` 明确选择完整世界并冻结，本轮没有执行此项。`FrozenDataset` 的默认训练读取仍只接受 `frozen`，覆盖检查通过显式 `allow_candidates=True` 读取已完成候选。

当前渲染工具不作为真实画像或官方评价的依赖。合成测试用例仅验证实现和物理遮挡约定，不能作为真实数据上的科学结论。

## 开发池的单帧辅助监督先导

[src/supervision.py](../src/supervision.py) 提供可复用的 `ScanGeometry`、`boundary_targets`、`thinning_pair`、`sampling_targets`、`surface_targets` 和组合入口 `compute_supervision`。`ScanGeometry` 只读取当前视图的坐标与原始槽位；三项监督函数使用的检测标签、原始正常表面、插入/遮挡掩码与配对身份只用于训练，不能进入真实推理输入。

历史先导继续固定读取 `supervision.pilot.dataset_directory` 指定的旧开发池。先导样本在辅助计算之前写入 `v1.json` 的 `supervision.pilot.samples`：训练侧 6 帧、合成验证侧 8 帧，依据既有清单和物体参数固定选取，包含无异常、少点、较密、远距、低矮及 `201` 特殊槽位情况。基础视图及两个规定的删减视图均使用完整当前扫描计算邻域。坐标去重只影响几何支持计数，原始点行和异常标签保持原样。

现有环境使用 NumPy、SciPy 和 Numba；束/列删减仅调用射线映射，不调用物理渲染。依据当时 CPU、内存和 Windows E 盘资源确定线程数后执行：

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  .venv/bin/python -m src.supervision --data-root /home/jasongao/Data/STU --workers 16
```

辅助结果单独保存在 `results/supervision/pilot/<split>/<world>/<frame>.npz`，同名 JSON 保存计数、失效原因、相位及几何核对。NPZ 的 `view_0/` 是基础视图，`view_1/`、`view_2/` 是两个删减级别；每个视图均保存完整输入回波的 `source_slot`，删减视图另存 `dense_row`。目标和掩码按这些点行排列；基础检测标签由冻结帧按槽位读取。NPZ 不复制原始背景，也不改写世界或冻结基础帧。

[先导汇总](../results/supervision/pilot/summary.json) 保存正常/异常有效点数、条件分组、C1 非饱和目标数、C2 保留率及保留点中的有效率、C3 失效原因和独立几何核对。失效原因编码为位集合，0 为有效；C1/C2 可多因并存，C3 记录计算中首先失败的条件。基础与删减视图分别计算尺度、C1 图和 C3 可见支持；同一查询位置的插入前平面只拟合一次。

该次先导计算固定的 14 帧和 42 个视图，不据此估计全池覆盖率，也不以有效率作为通过门槛。稀疏、远距样本的辅助监督缺失和 C1 截断目标占比需要据实讨论；辅助无效不改变检测标签、不删除样本。先导当时停止在报告，未补算全量监督。

这批基础扫描包含 1,693,399 条正常回波和 556 条异常回波；556 条异常对应 554 个不同坐标。忽略标签回波仍进入邻域计算，但不计入下表正常/异常分母。半采样指列步长 2；四分之一采样指束、列步长均为 2，两者使用各自规定的随机相位。

| 监督及视图 | 正常有效／当前正常点 | 异常有效／当前异常点 |
| --- | ---: | ---: |
| C1 基础 | 140,890／1,693,399（8.32%） | 538／556（96.76%） |
| C1 半采样 | 86,419／846,637（10.21%） | 272／283（96.11%） |
| C1 四分之一采样 | 36,930／423,551（8.72%） | 137／149（91.95%） |
| C2 半采样 | 735,371／846,637（86.86%） | 269／283（95.05%） |
| C2 四分之一采样 | 336,682／423,551（79.49%） | 134／149（89.93%） |
| C3 基础 | 861,173／1,693,399（50.85%） | 547／556（98.38%） |
| C3 半采样 | 410,548／846,637（48.49%） | 272／283（96.11%） |
| C3 四分之一采样 | 188,823／423,551（44.58%） | 139／149（93.29%） |

C2 的异常保留率分别是 283／556＝50.90% 和 149／556＝26.80%；有效一致性点占基础异常点的比例分别为 269／556＝48.38% 和 134／556＝24.10%。不能只报告保留点中的 95.05% 和 89.93%。

下列条件可以重叠。低矮由世界参数中的物体局部完整高度不超过 0.2 米定义；远距由异常回波距离中位数在 35–50 米定义。C2 列依次列出“保留／基础；有效／保留”，原始点数分母不能省略。

| 条件 | 帧数 | 基础异常数 | C1 基础有效 | C2 半采样 | C2 四分之一采样 | C3 基础有效 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 1–4 点异常 | 5 | 13 | 0／13（0%） | 8／13；0／8 | 7／13；0／7 | 4／13（30.8%） |
| 5–19 点异常 | 3 | 27 | 22／27（81.5%） | 15／27；12／15 | 9／27；3／9 | 27／27（100%） |
| 至少 100 点异常 | 2 | 449 | 449／449（100%） | 227／449；224／227 | 116／449；114／116 | 449／449（100%） |
| 35–50 米远距 | 3 | 6 | 0／6（0%） | 3／6；0／3 | 3／6；0／3 | 0／6（0%） |
| 低矮物体 | 7 | 385 | 376／385（97.7%） | 195／385；190／195 | 103／385；92／103 | 381／385（99.0%） |

3 张无异常帧的 C1 均关闭，C2/C3 继续计算正常点监督。两个 `201` 特殊槽位样本均完成核对；其中 `validation/world_000/0` 的 3 条异常记录是同一位置的三个槽位，分别为 65078、196150、327222。它们在两个删减视图中均共同保留，几何支持只计一个位置；该异常位于 62.65 米，属于槽位核对样本，超出官方评价距离。

C1 的基础视图有 73 条可信交界边，异常边缘标签仍为 1；距离经同标签边传播。基础异常中 18 点无效：11 点尺度及局部支持不足，7 点没有可达交界。正常点主要因没有通向观测交界的同标签路径而无效。更突出的限制是饱和：正常有效点只有 163 个目标小于 1，异常有效点只有 144 个；两类合计的非饱和比例为 307／141,428＝0.217%。例如 `validation/world_003/310` 的槽位 105819，图距离为 0.363980 米，尺度为 0.173374 米，归一化目标为 0.699796，标签保持异常。大量饱和点对训练的影响仍待决定，不能由整体异常有效率代替判断。

C2 的异常失效主要来自删减后尺度或标签组支持不足，部分点失去规定的空间跨度；正常点最常见的失效原因也是尺度不足。对上述验证帧槽位 105820，半采样将尺度从 0.123980 米变为 0.196751 米，正常/异常局部支持分别从 24/67 个位置变为 12/33 个位置，仍符合一致性条件。1–4 点异常在这批样本的两个删减级别下均没有有效一致性点，需作为明确的覆盖缺口保留。

C3 基础异常的 9 个无效点均缺少至少 20 个插入前地面支持；远距代表点实际只有 0、7 或 9 个支持，特殊槽位异常没有局部地面支持。正常点主要因插入前支持不足（439,336 点）或查询位置在支持凸包外（318,171 点）而无效。低矮异常已经获得有效目标：例如 `validation/world_003/310` 槽位 105820 的查询高度为 −1.857585 米，插入前平面高度为 −2.000723 米，目标为 −0.143138 米；插入后仍有 1,474 个地面支持，半采样后为 740 个，目标位移保持原值。

C3 的最低可见支持数有可以单独讨论的实际例子。`train/world_015/285` 槽位 71191 在基础视图有 26 个可见支撑，目标 −0.066704 米；半采样保留该异常点，但可见支撑降至 13 个，因此现有掩码关闭。仅为解释失效原因，继续核对这 13 个支撑：矩阵满秩、平面误差 0.032387 米、水平协方差最小特征值 0.729152 平方米，查询点位于支撑凸包内，其余规定条件均满足。四分之一采样仅剩 6 个支撑，查询点已在凸包外；此时只放宽数量要求也不足以恢复有效性。这些附加几何诊断没有改变目标或掩码，也没有证明减少支持数仍可保持真实学习效果。

先导对全部交界路径做同标签与最短路径核对，逐点检查两个删减视图的对应关系；所有基础和保留异常回波的 C3 另用 NumPy 最小二乘与 SciPy 凸包独立复算。全套 26 项测试通过，`train/world_015/446` 的 50 个辅助数组在 1 与 16 个线程下逐元素相同。修正过一处报告写出错误：无异常清单没有距离字段，现按缺失值处理；监督公式和样本选择未改变。

上述结果是 `9a0ecb5` 的监督计算先导。它支持计算实现及该批样本的覆盖判断，没有确认三项辅助监督会改善真实分割结果；损失聚合与可见支持单变量比较见下一节。

## 损失聚合与可见支持对照

`supervision.loss` 定义当前采用的聚合规则；`src/supervision.py` 中的 `boundary_loss`、`sampling_loss`、`surface_loss` 是可求梯度的 PyTorch 实现。每次调用处理当前小批量的同一视图或同一删减级别：先合并该批点行，再按类别求均值。拼接多个帧时，C2 的 `dense_row` 必须加上各基础帧在批次中的点行偏移，使其仍指向同一帧、同一槽位的基础预测。

| 辅助项 | 点误差 | 聚合规则 |
| --- | --- | --- |
| C1 | 预测距离与既有距离目标的绝对误差 | 有效且目标小于 1 为交界带，等于 1 为饱和区；每个区域内先分别平均正常点、异常点，再对存在类别等权。两个区域均存在时，交界带占 0.75，饱和区占 0.25。 |
| C2 | 稀疏异常概率与停止梯度的对应基础异常概率之差的平方 | 在原有一致性有效掩码内，分别平均正常点、异常点，再对存在类别等权。 |
| C3 | 预测表面位移与既有目标的绝对误差 | 在正式默认可见支持至少 20 的有效掩码内，分别平均正常点、异常点，再对存在类别等权。 |

空类别跳过；C1 空区域跳过后，将剩余区域权重归一化。整项无有效点时返回可反向传播的零；先选有效点再计算误差，不用无效占位值补目标。若同时使用多个视图，同一辅助项对存在有效监督的视图/级别等权平均。三项辅助损失与基础检测损失之间的系数本轮未设定。3∶1 是初始区域权重，未证明最优；所有分组信息和点权重只用于损失，不进入推理输入。

同一批 14 帧分视图合并后，直接计算恒预测 1 的 C1 损失，并用新权重下的加权中位数求最优常数。这里没有训练网络。

| 视图 | 原逐点等权：恒预测 1 | 新聚合：恒预测 1 | 新聚合下最优常数 | 该常数的新损失 |
| --- | ---: | ---: | ---: | ---: |
| 基础 | 0.001094 | 0.378209 | 0.657314 | 0.287008 |
| 半采样 | 0.001911 | 0.351113 | 0.728553 | 0.281759 |
| 四分之一采样 | 0.003647 | 0.416506 | 0.562172 | 0.304636 |

原逐点等权下，三个视图的最优常数均为 1。新聚合中，交界带正常/异常组的总权重各为 0.375，饱和区两组各为 0.125；基础视图中 140,727 个饱和正常点只占 0.125。复制一遍全部饱和组后，新聚合的恒预测 1 损失保持不变，验证了饱和点数量不会继续压低该损失。上述数值只说明聚合性质，不是模型性能比较，也不能保证网络不会学成其他常数。

C2 半采样有效点中的异常比例为 0.03657%，四分之一采样为 0.03978%；C3 基础视图为 0.06348%。新聚合对存在的正常、异常类别均值各赋予 0.5 权重。它规定的是误差均值的系数，实际梯度大小仍由预测误差决定。C2 的配对、相位和有效条件没有改动，失去异常证据的点仍关闭一致性。

C3 已将数量参数分开：`minimum_reference_support_points=20` 同时约束插入前参考点和筛选后的拟合内点；`minimum_visible_support_points=20` 是正式默认。比较只把后者设为 10，满秩、误差、空间展开和凸包条件均不变。`surface_targets` 返回按可见支持下限索引的视图结果，默认取 `[20]`；比较时传入 `[20,10]`，共用一次插入前平面拟合和每个视图的后续几何检查。

运行命令如下，仍只读取原有 14 帧及其三个视图。比较没有重算或改写 C1/C2 目标；原辅助文件和冻结基础帧均保持原样。

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  .venv/bin/python -m src.supervision --compare --workers 16
```

结果单独保存于 [comparison/summary.json](../results/supervision/pilot/comparison/summary.json)，同目录的 `<split>/<world>/<frame>.npz` 保存下限 10 的完整 C3 数组及恢复点槽位，标记 `formal_default=false`。同名 JSON 保存逐帧类别计数和独立几何核对。旧版无效目标中的 0 只是占位；新恢复点的数值取自同一插入前参考平面。

| 视图 | 正常有效：20 → 10（新增） | 异常有效：20 → 10（新增） | 其中低矮异常：20 → 10（新增） |
| --- | ---: | ---: | ---: |
| 基础 | 861,173 → 861,339（166） | 547 → 547（0） | 381 → 381（0） |
| 半采样 | 410,548 → 426,325（15,777） | 272 → 277（5） | 190 → 193（3） |
| 四分之一采样 | 188,823 → 197,129（8,306） | 139 → 139（0） | 97 → 97（0） |

计数单位是实际文件回波行；低矮异常是异常的子集。三个视图不能相加后解释成不重复的物理点。正常新增行对应的不同坐标数分别为 166、10,770、6,136，差别来自 `201` 的重复槽位。

正常点恢复的逐帧来源如下；所有异常恢复均发生在半采样视图。

| 来源／世界／帧 | 正常新增：基础 | 正常新增：半采样 | 正常新增：四分之一采样 | 异常新增（其中低矮） |
| --- | ---: | ---: | ---: | ---: |
| 训练／world_016／385 | 0 | 640 | 685 | 0 |
| 训练／world_011／322 | 20 | 664 | 296 | 2（0） |
| 训练／world_015／285 | 146 | 1,190 | 446 | 3（3） |
| 训练／world_015／446 | 0 | 674 | 548 | 0 |
| 训练／world_010／210 | 0 | 1,118 | 501 | 0 |
| 训练／world_018／191 | 0 | 1,010 | 380 | 0 |
| 合成验证／world_004／116 | 0 | 493 | 565 | 0 |
| 合成验证／world_003／451 | 0 | 108 | 163 | 0 |
| 合成验证／world_003／260 | 0 | 630 | 312 | 0 |
| 合成验证／world_001／499 | 0 | 101 | 192 | 0 |
| 合成验证／world_003／189 | 0 | 234 | 264 | 0 |
| 合成验证／world_003／310 | 0 | 597 | 319 | 0 |
| 合成验证／world_003／3 | 0 | 3,230 | 1,520 | 0 |
| 合成验证／world_000／0 | 0 | 5,088 | 2,115 | 0 |

恢复的异常槽位为 `train/world_011/322` 的 68119、69151（均有 17 个可见支撑），以及低矮样本 `train/world_015/285` 的 71189、71191、71193（分别有 14、13、11 个支撑）。这些点均重新通过后续几何条件。例如槽位 71191 的参考点和内点均为 28，目标仍为 −0.066704 米；13 个可见支撑相对原平面的误差为 0.032387 米，水平协方差最小特征值为 0.729152 平方米，查询点在凸包内。

原来首先因可见支持少于 20 而失效的正常点，在下限 10 下仍有下列失效情况。表中记录首次失败条件；数量仍不足 10 时不宣称其他几何条件合格。

| 视图 | 原数量不足 20 | 恢复有效 | 仍不足 10 | 凸包不包含查询点 | 可见支持质量不合格 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 基础 | 171 | 166 | 0 | 0 | 5 |
| 半采样 | 17,292 | 15,777 | 383 | 882 | 250 |
| 四分之一采样 | 23,027 | 8,306 | 7,620 | 2,346 | 4,755 |

质量不合格进一步分为：基础视图 5 点水平展开不足；半采样 170 点平面误差过大、83 点水平展开不足，其中 3 点同时存在这两项问题；四分之一采样分别为 139 和 4,616 点。四分之一采样的 4 个原数量不足异常点仍少于 10 个支持，其中 3 个是低矮异常；原先因参考表面、凸包等条件失效的其余点没有因这次数量修改而转为有效。

默认 20 的 336 个 C3 数组与原先导逐元素一致，原本有效的目标值完全不变。新增和仍失效点另做了 184 次独立 NumPy/SciPy 几何核对；30 项测试通过，包括参考点不足 20、内点不足 20 时不得因比较下限 10 而获得有效目标，以及分组权重、空组、无效值和 C2 停止梯度检查。

这次比较确认：调整可见支持数量能够在半采样的两个训练帧中恢复少量异常监督，同时仍会被其他几何条件约束。有效率增加不能证明 10 更好；正式默认保持 20。远距组只有 3 帧、6 个异常点，继续作为后续检查缺口，不据此裁决远距机制失败或要求重生成世界。本轮停止在损失聚合规则和这一单变量对照，没有扩大先导、补算全池或训练模型。

## 开发池的训练内容检查与生成依据

这次检查确认了内容缺口，也保留了有价值的现有观测。不能继续把监督计算正确解释为样本质量已经合格；同样，不能把所有低矮背景描述为缺少正常参照。

[src/coverage.py](../src/coverage.py) 首先读取冻结根清单选中的 40 个世界及其 19,824 帧观测记录，不包含未选候选。训练侧为 14,368 帧、111,232 个异常回波，其中官方距离内为 110,124 个；合成验证侧为 5,456 帧、34,951 个异常回波，其中官方距离内为 34,665 个。无异常帧分别为 9,183 和 3,642。各世界的完整维度、指数、材质、放置语义、倾角及观测背景分类均保存在本地 [结果明细](../results/coverage/v1.json) 的 `worlds` 中；`inventory` 按条件保存帧数、回波数和每个世界的贡献。

“合格”指传感器距离 [2.5,50] 米内至少有 5 个异常回波；“少点”指该范围内 5–19 个；“远距”指清单中异常回波中位距离在 [35,50] 米；“低矮”沿用物体局部完整高度不超过 0.2 米的定义。训练与合成验证中的所有合格帧分别为 2,693 和 943。点数是回波观测数，不是独立物体数；多个世界反复使用同一条正常轨迹，背景也不能按合成帧数视为独立。

局部检查先固定样本：每世界取异常点数最多的最早一帧；合格少点帧按 `(in_range,frame)` 排序取中间一帧；加入全部合格远距帧后去重。得到训练 122 帧、合成验证 18 帧，共 140 帧、40 个世界、129 个不同原始扫描身份，其中包括全部 61 帧合格远距观测。具体身份在 `selection` 中，先于监督计算写出。邻域均使用完整扫描；基础视图计算 C1，两级删减计算 C2，三视图计算默认参考支持 20、可见支持 20 的 C3。辅助数组只在内存中计算，没有改写世界、基础帧或原 14 帧先导产物。

| 目标 | 训练侧全池内容 | 合成验证侧全池内容 | 针对性检查与生成判断 |
| --- | --- | --- | --- |
| C1：边界 | 32 个单基元世界；全池异常点的 51.08% 来自贡献最多的 5 个世界。物体完整长、宽、高分别为 0.204–1.395、0.104–0.952、0.062–0.725 米；有 5 个高长比小于 0.1 的扁平物体。 | 8 个单基元世界；最多的一个世界贡献 25.30% 异常点。长、宽、高分别为 0.410–0.992、0.185–0.567、0.081–0.623 米；有 1 个高长比小于 0.1 的扁平物体。 | 检查中训练 31/32、合成验证 8/8 世界具有交界。非饱和异常边界目标分别为 1,324 和 327 个。已有边界并非只来自一两个世界；但全部交界正常端均为语义 40/48/49，全部物体均无多部件组合。后续补组合轮廓与接触关系，不把已有扁平形态误称为缺失，也不声称组合形状已经证明能改善真实迁移。 |
| C2：稀疏 | 合格少点：1,428 帧、13,750 点、32 世界。远距：474 帧、1,158 点、19 世界；其中合格远距仅 59 帧、435 点、3 世界，82.53% 的点来自 `world_017`。 | 合格少点：513 帧、5,014 点、8 世界。远距：247 帧、468 点、8 世界；其中合格远距仅 2 帧、11 点、1 世界。 | 全部合格远距帧中，半采样的有效异常一致性仅训练侧 30 点、7 帧、1 世界；四分之一采样为零，合成验证两级均为零。普通少点观测与实际远距观测的覆盖明显不同。后续须按实际距离、投影尺度、朝向和遮挡提出不同世界，并将合格远距列为独立内容条件，不能靠增加同一物体的连续帧补齐。 |
| C3：低矮 | 2,831 个有异常观测的低矮帧、62,630 点、20 世界；70.22% 的点来自最多的 5 世界。合格低矮帧有 1,480 个；合格低矮远距仅 15 帧、76 点、2 世界。 | 765 个有异常观测的低矮帧、13,619 点、4 世界；最多的一个世界贡献 36.18%。合格低矮帧有 390 个；合格低矮远距为零。 | 所检低矮帧中，基础 C3 有效异常点为训练 1,679/1,809、合成验证 617/617；异常附近具有表面监督的正常凸起参照分别见于 11/20 和 3/4 个低矮世界。全部合格远距异常点均因原始参考支撑不足 20 而无效。后续要补距离和背景搭配；现有正常参照应保留，不能把可见支持 20→10 当作上述缺口的解决办法。 |

表中远距行的点数指这些帧的异常回波总数。逐点核对后，训练合格远距帧中实际位于 [35,50] 米的异常点为 433 个，其余 2 个略低于 35 米；合成验证为 11 个。世界成员与官方帧资格并未据此改变。三个训练世界的合格远距贡献分别为 `world_017` 的 44 帧、359 点，`world_020` 的 10 帧、51 点，以及 `world_007` 的 5 帧、25 点；合成验证全部来自 `world_001` 的第 585、590 帧。

C1 在检查样本中产生训练 1,054 条、合成验证 181 条交界边，分布于 63 和 12 帧。训练正常／异常有效点为 1,908,058／2,526，其中非饱和点为 1,851／1,324；合成验证正常／异常有效点为 413,108／767，其中非饱和点为 395／327。训练非饱和异常点贡献最多的一个、五个世界占 9.21%、39.05%；这不支持“有效边界普遍只集中在极少世界”，但只适用于这批检查。单看各世界最密帧，训练 28/32、合成验证 5/8 有交界；单看每世界的少点代表帧，则为 27/32、7/8。最密观测并不保证一定存在正式定义中的可观测交界。

全部 40 个物体仍是单一闭合超二次曲面，指数改变了形态，扁平样本也确实存在，但没有多基元接合、弯折或多部件外轮廓。训练放置语义为 28 个道路点、3 个语义 48 点和 1 个语义 49 点；合成验证为 3 个道路点和 5 个语义 48 点。记录的放置平面相对世界 z 轴的倾角分别为 0.05–5.37°、1.28–3.74°，不作为精确重力坡度。训练交界正常端有 894 条来自语义 40、142 条来自 48、18 条来自 49；合成验证为 59、122、0 条。现有证据说明接触语义范围有限，并未证明其中每个局部几何都相同，也未验证真实复杂轮廓的迁移。

C2 的异常点保留率与保留后的有效率须分开解释。在全部合格远距训练帧中，半采样保留 213/435 点（48.97%），其中 30/213 点（14.08%）有效；这 30 点全部属于 `world_017`。四分之一采样保留 100/435 点（22.99%），有效为 0/100。合成验证半采样保留 3/11 点（27.27%），有效 0/3；四分之一保留 2/11 点（18.18%），有效 0/2。训练半采样失效点中，176 个没有可靠局部尺度，133 个未保留足够的标签组支持，70 个空间展开不足；原因允许重叠。四分之一采样的 100 个保留异常点全部没有可靠局部尺度。不能把这些计数直接解释为某个阈值不合理，也不能要求已经失去证据的视图复制异常判断。

每世界少点代表帧提供了不同证据：训练半采样保留 132/285 点（46.32%），99/132 有效（75.00%），覆盖 26 世界；四分之一保留 71/285（24.91%），23/71 有效（32.39%），覆盖 8 世界。合成验证分别保留 33/68（48.53%）、16/68（23.53%），有效 30/33（90.91%）、0/16，覆盖 7、0 世界。较密帧的两级配对则在训练 32、31 世界和合成验证 8、8 世界提供异常一致性。这支持保留原 C2 规则，同时把真实远距内容不足与单纯删减稀疏区分开来；它不证明普通少点样本已足以代表所有稀疏情况。

C3 的所检低矮异常点在基础／半采样／四分之一视图中的有效数为训练 1,679／839／419，分母为 1,809／906／452；合成验证为 617／308／154，分母相同。训练全部 435 个合格远距异常点及合成验证全部 11 个均在插入前参考点不足 20 时退出。独立读取这些点的原始正常扫描，2 米水平邻域中的不同参考位置为训练 0–18 个，合成验证均为 2 个。例如 `train/world_007/173` 的槽位 78801 有 6 个参考位置，`validation/world_001/585` 的槽位 79884 有 2 个。此处尚未执行参考表面后续几何检查，不能声称把参考下限改小就能获得可靠目标；本轮不改变参考支持条件。

正常参照的定义是：原检测标签为正常，距插入异常实际回波不超过 2 米，C3 有效，且当前点比估计正常表面高 5–20 厘米。这个范围在检查前指定，仅用于观察“凸起仍为正常”的样本，不是路缘识别器或新的异常阈值。所检低矮帧中，训练有 2,376 个这样的正常回波，来自 19 帧、11 世界；合成验证有 154 个，来自 5 帧、3 世界。贡献最多的 5 个训练世界占 87.21%，其中语义 48 有 1,568 点，语义 40 有 173 点，另外还保留车辆、建筑、围栏等正常类别的表面附近回波。若不限制在异常附近，所检低矮世界的完整当前扫描均含此类正常参照。因此，本次否定“所有低矮世界都完全没有合法凸起参照”的概括，但不能据此宣布路缘、复杂坡面或困难负例已经充分覆盖。

几何核对给出了可定位的正负例。同一低矮世界 `train/world_012` 的第 393 帧，异常槽位 109846 的表面位移为 −0.059386 米，参考内点／可见支持为 278／244；附近正常车辆语义槽位 108832 的位移为 −0.182251 米，参考内点／可见支持为 252／217。后者相对表面的凸起更高，但检测标签仍为正常。在 `train/world_002/10` 中，异常槽位 91557 与附近正常语义 48 槽位 93603 的位移分别为 −0.098578、−0.071182 米，两者目标都有效。`checks[*].probes` 保存了 116 次独立 NumPy/SciPy 核对的坐标、平面系数、支撑数及残差，核对差异小于 10⁻⁶ 米。这些是监督几何证据，没有模型输出。

该次检查形成 `v1.json:generation_plan` 的修订依据：保留已有简单形态和正常参照，补充连通组合形状以及距离、朝向和背景搭配；把合格远距、合格少点、低矮合格远距分别作为按世界报告和选择的内容条件。原入口仅要求每世界任意一帧合格，再按照 val19 统计选择世界，不能保证上述交叉条件。当时确定的后续任务是移除参考包络和总变差拟合，实际生成符合来源条件的新候选。上述检查对应 `64f92fe`；新入口及候选实测见后文，开发池本身未改变。

据此区分处理位置：缺少组合形态、远距合格观测只来自很少世界、合成验证低矮远距为空，属于内容与选样问题；远距原始参考支持不足和删减后证据丢失属于观测与监督可用性问题，当前不能据此裁定应放宽阈值；现有点数集中则仍可由后续按世界抽样控制，但不能替代前两类处理。材质尚未发现独立缺口，本轮不同时修改。最终迁移价值仍须由自己的 V1 训练和真实验证回答。

复算命令如下。该次实测使用 24 核 CPU、约 20 GiB 可用内存、6 个进程各 3 个几何线程；CPU 路径复用现有精确算法，GPU 未参与。检查耗时 270.79 秒，最大单进程常驻内存约 0.70 GiB，输出约 2.01 MB。运行前后 Windows E 盘均约有 87.98 GB 可用，保留了 10 GB 安全余量。

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 \
  MKL_NUM_THREADS=1 NUMBA_NUM_THREADS=3 \
  .venv/bin/python -m src.coverage --dataset results/synthetic/v1 \
  --output results/coverage/v1.json --workers 6 --threads 3
```

世界清单汇总和具体样本选择先于辅助计算完成；一次准备性读取修正了根清单中帧数配置的路径，没有改变样本或观测口径。与原先导唯一重叠的 `validation/world_003/260` 在两类点、三视图的所有共同统计上完全一致。C1/C2/C3 定义、全部损失规则和历史生成参数与本轮开始时逐项相同。本轮 32 项测试通过，同时检查了官方距离过滤后计数、世界贡献不能按帧冒充，以及选择不依赖辅助有效性。

## 新内容条件下的完整世界试生成

本轮已实际完成训练侧 16 个世界、7,184 帧，以及合成验证侧 16 个世界、10,912 帧。32 个固定候选均完成整条源序列，通过现有物理检查，并至少具有一张距离过滤后异常点数达到官方门槛的帧。候选保存在 [候选根清单](../results/synthetic/candidates/manifest.json)，状态为 `candidates_complete`；没有覆盖旧开发池，也没有确定正式训练配额或冻结新池。代码、单帧还原和内容检查已执行，迁移效果尚无训练证据。

候选条件在渲染前写入 `v1.json:proposals`：下表每类在正常 `train/206` 和 `train/201` 上各用两个种子。种子由 `SeedSequence([2062026, source_sequence, index]).generate_state(1)[0]` 唯一确定，实际 32 个种子先写入根清单的 `planned_candidates`。每个种子只产生一个固定形状和材质，最多检查 128 个正常支持位置，收集至多 6 个通过几何与背景条件的支持；预算执行后没有替换种子或追加候选。所有类别每侧都完成了 2/2 个物理世界；表中数量专指这些世界是否实际产生合格远距观测，不是类别名称所保证的结果。

| 候选编号 | 形状与提出目的 | 预定背景 | 训练合格远距世界 | 合成验证合格远距世界 |
| --- | --- | --- | ---: | ---: |
| `000–001` | 普通单基元 | 地面 | 0/2 | 1/2 |
| `002–003` | 扁平单基元 | 结构背景 | 0/2 | 1/2 |
| `004–005` | 两部件接合 | 结构背景 | 0/2 | 2/2 |
| `006–007` | 扁平弯折组合 | 地面 | 0/2 | 1/2 |
| `008–009` | 远距稀疏单基元 | 地面 | 1/2 | 1/2 |
| `010–011` | 双支腿与横梁组合，提出较大远距投影 | 结构背景 | 2/2 | 2/2 |
| `012–013` | 低矮远距单基元 | 地面 | 0/2 | 2/2 |
| `014–015` | 低矮远距弯折组合 | 结构背景 | 2/2 | 2/2 |

地面与结构背景是插入前、支持位置 3 米内的描述：出现至少两种支持语义，或至少三个已知正常非地面回波时记为结构背景，其余为地面。没有删除或平整原始背景。每侧最终各有 8 个地面和 8 个结构背景世界，但这个分类不等同于邻近异常处具有可靠正常凸起参照。材质分位和粗糙度范围未变；正常 `train/206` 的既有标定在核对全部 449 帧及射线输入一致后复用。生成和选样入口已移除 val19 参考包络与总变差拟合；旧池的世界没有被复用为新候选。

**组合形状与放置。** 32 个物体中，16 个是单基元，4 个是两部件接合，8 个是弯折组合，4 个是双支腿与横梁组合。全部组合通过连续连通性检查。长宽高按整个物体在局部坐标系中的连续几何外界计算，包含所有部件和偏移，外界保守留出 10⁻⁶ 米。训练物体的完整高度为 0.0845–0.7377 米，合成验证为 0.1069–0.6920 米，每侧均有 8 个低矮物体。

`validation/candidate_005` 的第一部件高度为 0.1541 米，完整物体高度为 0.2568 米，因此没有被计入低矮组。`train/candidate_010` 在局部高度 −0.22395 米处，中心查询位于物体外，两个支腿中心查询位于物体内，确认横梁下方保留空隙并形成两处支撑。32 个物体标准与加密下界计算的最大差为 1.065×10⁻⁸ 米，埋地比例均为零；放置后的局部竖轴与支持法向、整物体最低点与支持平面的代数核对误差均不超过 2.23×10⁻¹⁶。全部源帧通过既有的已观测非地面障碍物深穿透检查。这些结果支持当前几何和物理检查条件下的正确性，不等同于获知扫描未观测区域的全部真实表面。

支持搜索保留了失败记录：训练／合成验证分别出现 333／175 次平面质量或完整投影支持不合格、412／174 次轨迹距离不合格、284／236 次背景类别不合格；训练另有 4 次后续局部碰撞或贴地拒绝、2 次参考点不足，合成验证有 85 次指定距离内无可用地面点。这些是提出阶段的原因记录；后续局部检查与初始支持检查不是同一个计数阶段。没有一个最终物理候选因上述过程被伪造为成功。

**实际内容覆盖。** 下表来自全部 32 个完整世界的已有逐帧观测，列内依次为不同世界数、帧数、异常回波数。点数统计所列帧的全部异常回波；帧资格仍按 [2.5,50] 米内至少 5 点决定。

| 内容 | 训练：世界／帧／异常回波 | 合成验证：世界／帧／异常回波 | 最多一个世界的点贡献：训练／合成验证 |
| --- | ---: | ---: | ---: |
| 全部观测 | 16／7,184／282,400 | 16／10,912／249,738 | 18.90%／22.56% |
| 合格少点，所有距离 | 15／711／7,535 | 16／1,367／13,360 | 16.19%／11.10% |
| 合格远距，35–50 米 | 5／197／1,733 | 12／319／2,380 | 41.03%／37.48% |
| 远距且距离过滤后至少 20 点 | 0／0／0 | 0／0／0 | — |
| 合格低矮，所有距离 | 8／1,081／100,429 | 8／1,493／112,762 | 42.49%／22.25% |
| 合格低矮远距 | 2／41／224 | 6／99／545 | 77.68%／38.90% |

全体距离内异常回波分别为 280,527 和 247,524；合格远距帧的距离内回波分别为 1,729 和 2,378。无异常回波的 3,791 和 5,900 帧全部保留。全体异常点最多的五个世界贡献分别为 68.37%、60.79%；低矮组为 90.69%、80.84%。完整每世界贡献与集中度见 [覆盖结果](../results/coverage/candidates.json) 的 `inventory`。同一物体的相邻帧和多个世界复用的正常背景仍有关联，不能按这些帧数声称具有同等数量的独立形态。

旧开发池合格远距为训练 3 世界、合成验证 1 世界，新候选分别为 5 和 12；低矮远距为旧池 2／0 世界、新候选 2／6 世界。训练低矮远距的世界数没有增加，新增的两世界均为弯折组合，`candidate_015` 仍贡献该组 77.68% 的点。旧池与新候选的世界预算不同，此处比较用于定位内容缺口，不是等预算生成优劣实验。

远距专门提出的 16 个世界中，12 个产生了合格远距帧。训练 `candidate_009、012、013` 在本轮支持预算内的三个远距检查位置均没有可用包围盒射线机会，最终也没有远距回波。合成验证 `candidate_008` 曾有最多 8 条包围盒机会，但实际没有达到合格远距门槛，说明包围盒机会不能代替物体表面交点和传感器实际回波。四个双支腿组合均产生远距观测，但整批远距帧最多只有训练 18 点、合成验证 16 点；较大投影提出尚未实现“至少 20 点”的内容条件。本轮没有据结果放大物体、删掉困难帧或追加尝试。

**监督与正常参照。** 在读取上述观测后、计算任何辅助目标前，按既定规则选定训练 53 帧、合成验证 81 帧，覆盖全部 32 个世界。每世界取最密帧、全距离合格少点中位帧；远距按 5–19 点和至少 20 点分别选最多三个距离排序代表，再保留一个远距 1–4 点代表，去重后共 134 帧。此批包含 56 帧合格远距，并非全部 516 帧；以下有效数只适用于固定检查样本。

基础视图包含训练正常／异常 6,244,020／6,517 点，合成验证 7,098,344／7,633 点。各项有效监督如下，每个单元格依次为正常点数／异常点数。C2 数量是删减后保留点中的有效数，C3 各视图重新检查可见支持，全部阈值和损失保持原定义。

| 监督 | 训练有效正常／异常 | 合成验证有效正常／异常 |
| --- | ---: | ---: |
| C1 基础有效距离 | 730,254／5,498 | 570,065／5,468 |
| C1 其中非饱和距离 | 943／711 | 1,453／1,117 |
| C2 半采样一致性 | 2,790,658／2,915 | 3,085,248／3,519 |
| C2 四分之一采样一致性 | 1,311,571／1,360 | 1,409,335／1,554 |
| C3 基础表面 | 2,038,152／5,532 | 3,983,394／6,434 |
| C3 半采样表面 | 966,433／2,767 | 1,933,454／3,166 |
| C3 四分之一采样表面 | 440,024／1,371 | 914,693／1,553 |

C1 交界覆盖训练 15/16、合成验证 16/16 世界；其中组合物体为 7/8、8/8 世界。训练单基元／接合／弯折／双支腿物体提供的非饱和异常目标为 272／74／289／76，合成验证为 558／180／311／68。训练 `candidate_005` 的所检帧没有有效交界，不能将有组合轮廓解释为每个视角都有有效边界。非饱和异常目标最多五世界的贡献为 57.24% 和 59.98%。交界正常端语义为训练 40 类 350 条、48 类 29 条、80 类 2 条，合成验证为 40 类 151 条、48 类 265 条；训练 `candidate_011/171` 的两个正常端属于 80 类，说明本批交界已有少量非地面正常结构，但绝大多数仍来自地面。

C2 必须区分保留与有效。下表只统计所检合格远距帧：训练 18 帧、154 个异常点、5 世界；合成验证 38 帧、244 点、12 世界。

| 删减与来源 | 异常点保留率 | 保留异常点中的一致性有效率 | 有效异常一致性世界 |
| --- | ---: | ---: | ---: |
| 半采样，训练 | 75/154＝48.70% | 14/75＝18.67% | 3 |
| 半采样，合成验证 | 124/244＝50.82% | 11/124＝8.87% | 3 |
| 四分之一采样，训练 | 37/154＝24.03% | 0/37 | 0 |
| 四分之一采样，合成验证 | 61/244＝25.00% | 0/61 | 0 |

训练有效半采样异常点来自 `candidate_008、010、011`，分别为 4、4、6 点；合成验证来自 `candidate_009、010、011`，分别为 2、6、3 点。例如训练 `candidate_008/349` 基础 9 点、保留 4 点，4 点均有效。远距半采样中训练 61/75、合成验证 107/124 个保留异常点没有可靠局部尺度；标签组支持丢失分别为 53、102 点，原因允许重叠。四分之一采样的全部 98 个保留异常点均缺少可靠尺度。新候选已经提供更多不同世界的远距检测与部分一致性观测，仍不能据此声称远距一致性机制充分可用。

对全距离少点代表帧，训练半采样保留 86/168、其中 48/86 有效，覆盖 11 世界；合成验证保留 67/134、其中 14/67 有效，覆盖 7 世界。四分之一采样分别保留 37/168、40/134，有效 5/37、0/40，覆盖 2、0 世界。这组包含远距代表，不能把其结果替代专门的远距表。较密的近中距观测则使两级有效异常一致性覆盖每侧全部 16 个世界。

C3 在所检合格低矮观测中，基础有效异常数为训练 2,857/2,904、合成验证 3,359/3,476，均覆盖全部 8 个低矮世界。但所检合格远距异常的基础 C3 均无效：训练 153/154 点、合成验证 244/244 点在插入前参考支撑不足 20 时退出；训练另 1 点具有 22 个参考位置，随后因可见支持不足而退出。参考数量范围为训练 0–22、合成验证 0–18。所检低矮远距的 31 个训练点和 100 个合成验证点均无三项已检查辅助，全部保留检测标签。全体所检远距点中，仅有检测监督的点分别为 132/154、208/244；这里指基础 C1、基础 C3 及两级 C2 对应点的并集之外，不外推其他未计算视图。

正常凸起参照仍存在，但局部搭配不均衡。所有低矮世界的所检完整扫描都含 C3 有效、凸起 5–20 厘米的正常点；限制在异常回波 2 米内后，训练仅为 240 点、4/8 世界，合成验证为 567 点、1/8 世界，验证侧全部来自 `candidate_003`。预定的结构背景分类并不能保证此项局部参照覆盖，后续世界搭配应明确保留这一缺口，不能把全扫描其他位置的正常结构冒充异常邻近的参照。

几何核对复用了原独立表面计算，共 66 个查询与批量结果相差小于 10⁻⁶ 米。例如训练 `candidate_002/309` 的异常槽位 83412 位移为 −0.031771 米，参考内点／可见支持为 168／158；同帧正常槽位 81378 位移为 −0.071854 米，内点／可见支持为 117／107。正常点相对表面更高，检测标签仍为正常。合成验证 `candidate_003/444` 的异常槽位 123219 位移为 −0.134664 米，正常语义 48 槽位 110930 位移为 −0.070615 米，均由各查询位置插入前的正常扫描拟合得到。正常结构及其标签没有被改为异常或从背景删除。

覆盖检查复算命令如下，结果包含完整清单汇总、134 个具体样本、逐类有效数、失败原因和几何核对；辅助数组仅在内存中计算。

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 \
  MKL_NUM_THREADS=1 NUMBA_NUM_THREADS=2 \
  .venv/bin/python -m src.coverage --workers 12 --threads 2 --far-limit 3
```

运行前实测 24 核 CPU、约 20 GiB 可用内存，无其他实验占用。生成使用 16 个单线程进程，完整候选计算耗时 221.37 秒，最大单进程常驻内存约 0.88 GiB；保存产物约 65 MB。134 帧覆盖检查使用 12 进程各 2 几何线程，耗时 184.62 秒，最大单进程约 0.71 GiB，结果约 2.16 MB。期间无交换空间占用；Windows E 盘生成前约 87.97 GB、检查后约 87.90 GB，满足 10 GB 余量。生成的全部帧写后还原与渲染结果逐元素一致，新增候选访问也验证了完整世界身份与帧数。34 项测试通过；三项监督、损失、随机相位总种子和正常标定配置与 `64f92fe` 逐项相同。

本轮停止在已生成候选与内容检查。已确认组合形状和部分远距／低矮远距提出可实现；未实现的条件是训练低矮远距世界多样性增加、至少 20 点的远距观测，以及更分散的低矮验证邻近正常参照。世界选样逻辑已按内容和形态背景给出预览顺序，尚未确定正式配额，也没有根据辅助有效率排序。下一次数据决策可据此确定哪些条件直接保留、哪些仍需有界补充；远距删减后的证据缺失应另作 C2 机制依据。C3 默认仍为 20，所有检测标签有效的困难帧继续保留，迁移价值仍须由 V1 训练与真实验证回答。

## 必要验证与当前边界

关键检查对应数据和评价的实际风险：原始槽位错配、标签错配、零坐标槽处理、距离边界、少于 5 个异常点的帧过滤、全局同分排序、严格高召回阈值、AP 失分加总、地面估计缺失和前景遮挡。

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -m pytest -q -p no:cacheprovider
```

接口检查使用人工构造的分数时，只能支持存储、排序和评价实现的正确性，不产生模型性能证据。

数据还原、辅助目标和损失核对只支持相应实现及所检观测。本轮已落实生成与选样修订，实际生成并检查了不使用 val19 数值拟合的新候选，正式世界配额和新池冻结仍未完成。当前损失规则和 C3 参考支持 20、可见支持 20 保持不变；边界、采样尺度和正常表面设计是否改善真实异常检测，仍须由模型输出与对应对照支持。
