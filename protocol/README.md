# 单帧 STU 异常分割研究协议

当前研究对象是一张 STU 激光雷达扫描中的逐点异常。输入为当前扫描的坐标和原始回波强度，输出为同一扫描实际回波的异常分数，分数越高表示越异常。

当前同一 V1 结构的辅助监督整体开启／关闭对照已完成 A（原联合监督）和 B（同结构仅检测监督）各一轮训练。两组均从保存的共同训练前状态开始，遍历相同的 8,980 个基础帧，各完成 2,245 次成功更新，均无跳过。配对评价在 A 合成验证阶段按用户要求暂停，最后一次进度记录为 6,200／13,640 帧，尚无完整评价指标；B 评价和比较未启动。两组训练检查点保留，本轮未追加训练或修改模型、损失、数据。具体对照定义与执行记录见本文末尾。

当前核心问题是：仅用当前扫描可计算的信息，局部几何能否区分异常标签与复杂正常结构，以及考虑观测条件后能否比无条件几何和仅观测条件提供更稳定的区分。这两个问题需要分别验证。真实分析已获得初步、有限支持，现有合成数据中也已找到固定几何探针会漏检的异常和会误报的正常；当前检验既有辅助监督是否帮助检测模型利用这些正反例。边界、采样归一化几何和正常表面监督只是 V1 的三项候选实现；法向变化和局部表面残差也尚未确定为新的监督目标或网络模块。

已完成的真实分析仅用正常来源 `train/206` 建立条件与几何的参考分布，用 `train/201` 检查正常分布跨来源迁移，再在 val19 上评价预先确定的 A/B/C 简单分数和描述条件几何差异。val19 异常标签不参与拟合、分箱、分数方向选择或组合权重选择。已完成的合成核查沿用这些计算与参考，先检查原始 `206/201`，再检查插入后的世界；不扩充 val19 探针，不将开发分布作为生成器优化目标。隐藏测试集不参与开发，当前证据与裁决见本文末尾。

已有首轮实验数据保持冻结：每个固定物体世界渲染完整正常源序列，每帧作为独立样本。保留全部 32 个基础世界并补充 8 个世界，训练与合成验证各 20 个，共 22,620 帧。训练合格低矮远距由 2 个世界增至 6 个，合成验证新增 4 个低矮异常与邻近正常凸起的有效搭配；新增世界完成了 41 张固定完整扫描的监督检查。V1 已完成正式一轮训练、全部合成验证和真实 val19 评价，以及既有预测的合成误报分析、真实排序和局部失败归因。第二轮训练及模型修改仍暂停。

历史开发池 `results/synthetic/v1/` 保留 32 个训练世界、8 个合成验证世界，共 19,824 帧。它已使用 val19 聚合标签统计生成与选样，继续作为隔离的开发对照。当前实验入口为 `results/synthetic/experiment/`，按原顺序引用 `results/synthetic/candidates/` 中的 32 个基础世界，再追加成功补充；不复制、重渲染或淘汰基础世界。当前 40 个世界的生成和选样只使用正常来源、射线和预先规定的内容条件，不读取 val19 参考包络或拟合分布差异。

V1 使用 LitePT-S 提供场景上下文，以完整的逐点正常／异常检测为主任务；边界距离、采样归一化几何和正常表面预测提供三项增强。关闭增强分支后，场景编码、逐点恢复和检测头仍然可独立前向和反向传播。当前基准为 `results/v1/epoch1.pt`，真实 AP 为 6.532102%，FPR95 为 17.214532%；第一轮结果及后续只读诊断见本文末尾。隔离短检查的权重未用于正式训练，三项增强的作用尚无受控对照证据。固定训练 10 轮的设定已撤销，不自动进入第二轮。

当前研究问题、数据边界与状态在 [spec.json](spec.json)，读取和必要语义检查由 [src/protocol.py](../src/protocol.py) 完成。当前格式为 `stu-single-frame`，格式版本为 1。[v1.json](v1.json) 保留已经执行的 V1 实验配置，其中实施前的状态和授权文字属于历史记录，不代表当前研究顺序。

## 工作区结构

同类内容集中保存，以下命令均从仓库根目录执行。

| 目录 | 内容 |
| --- | --- |
| `src/` | 单帧模型、训练、数据、辅助监督、评价、几何统计、物理渲染和冻结数据生成代码 |
| `tests/` | 对应的数据身份、评价和几何检查 |
| `protocol/` | 本说明与机器可读定义 |
| `assets/` | 可复用的射线几何标定数据 |
| `results/` | 当前结果；同一次几何统计的明细与表格共用一个目录 |
| `vendor/stu/` | 保持原样的官方评价器及许可证 |
| `vendor/litept/` | 作者发布的独立 LitePT-S 编码器、位置编码及 MIT 许可证 |

几何统计的明细位于 `results/profile/records/`，表格和统计说明位于 `results/profile/tables/`。只有表格和说明纳入版本控制；明细、预测和其他运行结果保持本地保存。

## 数据范围

开发诊断使用 STU 公开验证集全部 19 条异常序列：125、137、138、139、140、141、142、143、144、145、146、147、148、149、150、151、152、153、169。每条序列的全部原始帧都可以产生单帧预测。

正常数据源 `train/206` 的全部 449 帧用于 20 个训练世界，共 8,980 帧；`train/201` 的全部 682 帧用于 20 个合成验证世界，共 13,640 帧。每侧均由 16 个基础世界和 4 个成功补充组成。历史开发池每侧的世界数分别为 32 和 8。世界及其正常源序列整体分开，不能将同一世界的前后帧分到训练与验证两侧。隐藏测试集不在当前开发范围内，当前数据入口不会开放它。原始数据保留在仓库之外，不随代码提交。

公开验证集已经用于开发分析，后续在此集合观察的规律和模型差异均应保留开发数据的适用边界。几何条件与失误的统计差异不能单独证明因果机制。

目录结构为：

```text
STU/
  train/{201,206}/
  val/<sequence>/
    velodyne/<frame>.bin
    labels/<frame>.label
    calib.txt
    poses.txt
```

`spec.json` 保留已记录的原始压缩包摘要，供需要核对数据来源时使用。

## 单帧读取和模型输入

[STUSequence](../src/scene.py) 通过 `source_frame(frame_id)` 返回一个 `SourceFrame`。索引和迭代同样返回原始单帧。

`SourceFrame.xyzi` 保存完整原始文件槽位，形状为槽位数乘 4，数据类型为 `float32`。`real_slots` 按原始顺序列出坐标不全为零的实际回波槽位。零坐标占位槽继续保存在源数据中，进入模型时通过 `real_slots` 排除。强度保持原值，不能预设它严格位于 0 到 1 之间。

模型可读的基本数组是：

```python
x = frame.xyzi[frame.real_slots]
```

语义标签、实例标签以及依赖标签计算的几何量仅用于监督或诊断，不能作为推理输入。用 `label_mode="forbidden"` 读取源数据时，读取器不打开标签文件。

源对象保留官方坐标及特征的原有计算能力，供数据核对和外部方法适配使用：`coordinates` 按发布位姿计算，`features` 包括原始强度及相对扫描坐标均值的距离。它们是辅助数据表达，不改变当前模型以单帧传感器坐标和强度为输入的任务定义。官方评价距离始终来自 `xyzi[:, :3]`，不能用世界坐标范数代替。

发布标签的低 16 位是语义，高 16 位是实例。原始打包标签完整保留。合成训练的正常监督依据 `normal_semantic_class_map` 中的有效类别，原始忽略类别保持忽略。异常监督只由渲染器的实际插入掩码定义，不能将正常源中的原始语义 2 直接解释成插入异常。

读取示例：

```bash
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m src.scene \
  --data-root /home/jasongao/Data/STU --partition val \
  --sequence 125 --frame 0 --labels required
```

## 预测身份和存储

[src/data.py](../src/data.py) 中的 `FramePrediction` 使用以下字段：

```python
FramePrediction(
    partition,
    sequence_id,
    frame_id,
    source_slot,
    anomaly_score,
)
```

每个分数对应一个明确的原始文件槽位。保存和加载时都必须验证：

- 分区、序列号和帧号与传入的 `SourceFrame` 一致。
- 槽位无重复，且其集合恰好等于 `SourceFrame.real_slots`。
- 分数是有限的 `float32`；生产端显式决定精度转换，存储端不静默量化。
- 分数可以为负，也可以超过 1；评价使用原始数值的排序。

生产端可以采用任意点行顺序，但必须同时保存对应槽位。`restore(source)` 按槽位放回完整原始顺序，并将零坐标槽的分数填为 0。依赖数组行号而忽略槽位身份的结果不能进入评价。

`save(path, source)` 不覆盖已有预测；`load(path, source)` 不仅检查压缩包字段，也重新核对真实源扫描。当前存储格式是 `stu-frame-prediction`，不接受其他任务格式作为当前结果。

统一目录为：

```text
<predictions>/val/<sequence>/<frame:06d>.npz
```

各模型或消融实验分别使用自己的预测根目录。V1 的真实预测目录为 `results/v1/predictions/real/val/`；合成预测按世界分目录保存，另绑定实际检查点和实验根清单。第一轮训练、13,640 帧合成验证预测及 8,659 帧真实 val19 预测均已完成，对应评价结果分别在 `results/v1/synthetic.json` 和 `results/v1/real.json`。

## 官方评价和误差归属

[src/evaluate.py](../src/evaluate.py) 保留 [STU 官方评价器](../vendor/stu/compute_point_level_ood.py) 的点集和阈值定义：

- 点到当前传感器的距离满足 `2.5 <= r <= 50` 米。
- 语义 0 忽略，语义 2 为异常，其余有效语义为正常。
- 距离过滤后异常点少于 5 个的帧整体不进入正式指标。
- 所有合格帧的全部有效点合并计算指标，不平均逐帧或逐序列指标。

平均精确率 AP、排序区分指标 AUROC、以及高召回工作点的正常误报比例 FPR95 以百分数报告。FPR95 使用官方保留的 ROC 阈值节点中召回率严格大于 95% 的第一个节点。相同分数的点必须整体进入或离开检测集合。

低误报工作点使用完整官方点集上实际误报比例不超过 1% 时的最高召回率；召回率相同时选择最早达到该召回率的阈值。如果没有异常点能在该条件下检出，阈值记录为空，含义是拒绝全部点。子组统一使用这两个全局工作点。

指标实现保留精确的单精度排序，并采用磁盘临时数组就地排序和分块累计；不把分数量化到直方图。官方评价器保留为独立核对依据。

`APAttribution` 记录每个异常分数对应的全局精确率及所需误报比例。对异常点 `i`，全局同分组全部纳入后的精确率记为 `P_i`，异常点总数记为 `N_A`。其失分为 `(1-P_i)/N_A`，全部异常点的失分之和应为 `1-AP/100`。任意互斥分组可以据此汇总对全局 AP 的失分贡献。

所需误报比例按分数大于或等于该异常点分数的正常点数量除以全局正常点数量计算。它与 AP 失分都来自同一个全局排序。当前模块提供这些通用计算，尚未实现图谱的条件匹配、序列重采样及贡献裁决。

完整开发集评价示例：

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -m src.evaluate --data-root /home/jasongao/Data/STU \
  --predictions results/predictions/<model> --output results/evaluation/<model>
```

可用 `--sequence` 运行明确标记的开发子集。完整评价要求所选序列的每个原始帧均有符合身份约定的预测。临时排序文件使用结束后即释放。大体积评价必须根据当时 Windows E 盘空间保留至少 10 GB 余量。

## 单帧真实几何画像

[src/profile.py](../src/profile.py) 保留单帧统计算法，包括异常点数、距离、强度、最近正常点距离、正常邻域、同实例最近邻、可见实例尺寸、协方差形态和地面关系。几何量在当前扫描中计算，源点身份贯穿统计过程。

画像描述全部原始帧的实际回波，范围比官方指标点集更广。原始异常点数和官方范围内异常点数分别保存；符合官方帧门槛的计数另行汇总。忽略点和范围外点的描述性统计不能混入正式 AP 失分结论。

实例几何与地面估计保留原有可靠性规则。无可靠地面拟合时，高度保持缺失并报告原因。异常点实例标识为零时，不假定这些点属于同一个物体；点级和帧级异常统计继续保留它们。

[src/profile_report.py](../src/profile_report.py) 根据实际生成的逐帧记录、逐实例记录和分布累计结果导出 [统计表格](../results/profile/tables)。分位数来自完整加权经验分布，不平均各序列分位数。表格目录保存：

```text
frames.csv
instances.csv
sequences.csv
totals.csv
count_distance.csv
continuous.csv
categorical.csv
bins.csv
reliability.csv
index.csv
method.md
```

CSV 数值列与实现字段直接对应，空值表示缺失或不适用。CSV 本身不保存字体。详细统计定义由生成源写入 `method.md`，修改定义时应修改生成源并重新生成结果。

完整运行示例：

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -m src.profile --data-root /home/jasongao/Data/STU \
  --output results/profile --workers 12
```

进程数必须依据当时 CPU、内存和其他任务实测状态确定。`--output` 同时决定明细子目录 `records/` 和表格子目录 `tables/`。少量帧检查可使用 `--sequence` 和 `--limit`，并指定独立的 `--output`，避免将子集结果覆盖到完整画像位置。

已有完整明细时，在上述命令中加上 `--tables-only` 可重新汇总并生成表格，无需重新读取原始扫描。表格中的 `index.csv` 使用相对于表格目录的路径指向明细汇总文件。

## 固定世界与冻结单帧数据

[src/render.py](../src/render.py) 保留连续几何、形状族、激光射线、物体放置、地面支持、传感器标定和真实遮挡竞争。`render_frame(source, world, ray_grid, sensor)` 的基本渲染对象是一个 `SourceFrame`；`render_frames` 只是逐个调用单帧渲染的流式工具。

[assets/rays.npz](../assets/rays.npz) 保存独立的射线几何标定数据。`calibrated_ray_grid` 读取该文件，计算束方向、束原点以及原始槽位到射线的对应关系。回波概率与强度规律重新由正常 `train/206` 的全部单帧标定；放置地面的拟合仅使用一张当前正常扫描。物体随后固定在世界坐标中，形状、尺寸、位置、朝向和材质均不随帧变化。

[src/generate.py](../src/generate.py) 提出候选世界，检查完整轨迹中的位置距离及全部帧的已观测障碍物碰撞，再通过 `render_frames` 渲染固定物体。每个已渲染帧写入后重新读取，逐元素核对坐标、强度、打包标签、实际回波槽位和插入、遮挡掩码。没有异常回波的帧仍然写入；物体遮挡原始回波却没有返回信号时，对应槽位及标签清零。

基础 32 世界保留单基元、扁平单基元、两部件接合、弯折及双支腿与横梁等形态。当前入口只执行一次有界补充：训练侧为两个低矮单体、两个低矮弯折组合，目标是合格低矮远距；合成验证侧同样两个单体、两个组合，目标是邻近原生正常凸起参照。类别参数、种子和搜索预算见 `v1.json:proposals`。每个物体的完整局部几何范围包含全部基元及偏移，统一用于尺寸、低矮条件和放置支撑；碰撞检查使用完整形状及其保守范围。组合部件必须连通，贴地后检查完整物体的下界与埋地比例。

放置只使用正常源数据。每个种子固定一个形状和材质，最多提出 192 个支持位置，收集至多 12 个几何合格支持，每处检查三个预定朝向。远距候选从物体中心处于 35–50 米的真实轨迹中，取最接近 36、42、48 米的三个不同帧，复用渲染器的实际形状求交与原始背景遮挡竞争。分别记录包围盒机会、真实前景表面命中和固定信号的最终异常回波；只按真实表面命中排序。原生正常参照则从 `train/201` 的八张固定完整正常扫描中按既有 C3 条件查找，再在参照附近寻找合法支撑；选定参照必须在核对帧中免于遮挡并保持原坐标、强度和标签。

完整渲染后按普通少点、合格远距少点、较密远距、低矮、低矮远距分别记录世界数与帧点贡献。全部基础世界按原顺序保留；新增世界只有同时通过物理检查和预定内容目的才追加。八次尝试完成后按实际成功数冻结，不追加种子或按辅助有效率淘汰。远距至少 20 点只作探索性记录。入选世界里的无异常帧、极少点帧和辅助无效帧全部保留。

当前实验产物集中在 `results/synthetic/experiment/`，根清单引用原基础文件，新增文件如下：

```text
manifest.json
calibration.pt
normal_reference.json
train/supplement_000/{world.json,manifest.json,frames/000000.npz,...}
validation/supplement_000/{world.json,manifest.json,frames/000000.npz,...}
```

根清单的 `planned_candidates` 在执行前记录八个种子；`generation` 保留每次尝试、完整观测、内容核对与失败原因，`splits` 定义最终训练成员。状态为 `frozen`。物理碰撞失败的世界完成整条渲染后保留证据，不列入训练成员；内容目的未实现时同样不追加。每帧采用 `stu-frozen-frame` 无损差量格式，绑定原始扫描、标签、位姿和固定世界，保存变化槽位；未改变的背景引用原始正常扫描。`normal_reference.json` 仅保存绑定正常源身份的参照位置，供本批候选使用，不成为推理输入。

历史开发池采用过 val19 参考轨迹包络和最小总变差选样，相关数值只说明该历史生成流程的行为。其完整配置与参考记录保留在 `results/synthetic/v1/`，生成入口已移除该路径。以下开发池结果仍可用于对照，不能解释为新候选结果。

历史开发池的实际汇总如下；完整逐帧记录和分项差异保存在本地 `results/synthetic/v1/manifest.json` 及各世界清单中。

| 划分 | 世界数 | 单帧数 | 无异常回波的帧 | 插入异常回波数 |
| --- | ---: | ---: | ---: | ---: |
| 训练 | 32 | 14,368 | 9,183 | 111,232 |
| 合成验证 | 8 | 5,456 | 3,642 | 34,951 |

训练候选中 114 个通过检查、14 个被拒绝；合成验证候选中 48 个通过、16 个被拒绝。最终选择将各分项总变差距离的平均值从训练候选集合的 0.1671 降到 0.1032，从合成验证候选集合的 0.0949 降到 0.0794。总变差距离在所采用的统计分箱内取值为 0 到 1；0 表示分箱概率相同，1 表示完全分离。此处每个特征包含缺失值分箱，平均数不能代替逐项判断。

训练集最明显的残差是 `range_step`，即相邻可见帧异常回波中位距离之差的绝对值，再取整条轨迹的中位数。该项总变差距离为 0.8354；32 个训练世界的该量中位数为 0.1710 米／帧，真实参考轨迹为 0.3861 米／帧。该差异与当前观测轨迹有关，不能仅由这个量断言车辆速度是唯一原因。只对具有可靠可见尺寸的帧比较高度分布时，训练集的条件总变差距离仍为 0.3474。当前数据是有限候选下的近似结果，这两项偏差不能被综合均值掩盖；本轮没有修改真实位姿、抽掉原始帧或重定义真实标签来消除它们。

[FrozenDataset](../src/data.py) 按清单加载一个单帧，验证原始输入未变化后还原全部原始槽位；其加载过程不导入或调用渲染器。`sample.source.xyzi[sample.source.real_slots]` 是模型可见的当前坐标和原始强度，`sample.anomaly_target` 为按原始槽位排列的监督：0 正常、1 插入异常、−1 忽略。地面标签和放置平面仅能用于后续辅助监督或诊断，不能成为推理特征。

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  NUMBA_NUM_THREADS=1 .venv/bin/python -m src.generate --workers 8
```

上述命令已执行完成；对已冻结目录再次运行会拒绝覆盖。进程数依据当时资源确定，同一批中断可续算。写入使用有界流式格式，并核对 Windows E 盘实际剩余空间。当前入口在固定补充预算结束后直接冻结，原来的配额排序入口已移除。`FrozenDataset` 默认可直接读取当前实验池；读取基础候选目录本身进行历史核对时才需要 `allow_candidates=True`。

当前渲染工具不作为真实画像或官方评价的依赖。合成测试用例仅验证实现和物理遮挡约定，不能作为真实数据上的科学结论。

## 开发池的单帧辅助监督先导

[src/supervision.py](../src/supervision.py) 提供可复用的 `ScanGeometry`、`boundary_targets`、`thinning_pair`、`sampling_targets`、`surface_targets` 和组合入口 `compute_supervision`。`ScanGeometry` 只读取当前视图的坐标与原始槽位；三项监督函数使用的检测标签、原始正常表面、插入/遮挡掩码与配对身份只用于训练，不能进入真实推理输入。

历史先导继续固定读取 `supervision.pilot.dataset_directory` 指定的旧开发池。先导样本在辅助计算之前写入 `v1.json` 的 `supervision.pilot.samples`：训练侧 6 帧、合成验证侧 8 帧，依据既有清单和物体参数固定选取，包含无异常、少点、较密、远距、低矮及 `201` 特殊槽位情况。基础视图及两个规定的删减视图均使用完整当前扫描计算邻域。坐标去重只影响几何支持计数，原始点行和异常标签保持原样。

现有环境使用 NumPy、SciPy 和 Numba；束/列删减仅调用射线映射，不调用物理渲染。依据当时 CPU、内存和 Windows E 盘资源确定线程数后执行：

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  .venv/bin/python -m src.supervision --data-root /home/jasongao/Data/STU --workers 16
```

辅助结果单独保存在 `results/supervision/pilot/<split>/<world>/<frame>.npz`，同名 JSON 保存计数、失效原因、相位及几何核对。NPZ 的 `view_0/` 是基础视图，`view_1/`、`view_2/` 是两个删减级别；每个视图均保存完整输入回波的 `source_slot`，删减视图另存 `dense_row`。目标和掩码按这些点行排列；基础检测标签由冻结帧按槽位读取。NPZ 不复制原始背景，也不改写世界或冻结基础帧。

[先导汇总](../results/supervision/pilot/summary.json) 保存正常/异常有效点数、条件分组、C1 非饱和目标数、C2 保留率及保留点中的有效率、C3 失效原因和独立几何核对。失效原因编码为位集合，0 为有效；C1/C2 可多因并存，C3 记录计算中首先失败的条件。基础与删减视图分别计算尺度、C1 图和 C3 可见支持；同一查询位置的插入前平面只拟合一次。

该次先导计算固定的 14 帧和 42 个视图，不据此估计全池覆盖率，也不以有效率作为通过门槛。稀疏、远距样本的辅助监督缺失和 C1 截断目标占比需要据实讨论；辅助无效不改变检测标签、不删除样本。先导当时停止在报告，未补算全量监督。

这批基础扫描包含 1,693,399 条正常回波和 556 条异常回波；556 条异常对应 554 个不同坐标。忽略标签回波仍进入邻域计算，但不计入下表正常/异常分母。半采样指列步长 2；四分之一采样指束、列步长均为 2，两者使用各自规定的随机相位。

| 监督及视图 | 正常有效／当前正常点 | 异常有效／当前异常点 |
| --- | ---: | ---: |
| C1 基础 | 140,890／1,693,399（8.32%） | 538／556（96.76%） |
| C1 半采样 | 86,419／846,637（10.21%） | 272／283（96.11%） |
| C1 四分之一采样 | 36,930／423,551（8.72%） | 137／149（91.95%） |
| C2 半采样 | 735,371／846,637（86.86%） | 269／283（95.05%） |
| C2 四分之一采样 | 336,682／423,551（79.49%） | 134／149（89.93%） |
| C3 基础 | 861,173／1,693,399（50.85%） | 547／556（98.38%） |
| C3 半采样 | 410,548／846,637（48.49%） | 272／283（96.11%） |
| C3 四分之一采样 | 188,823／423,551（44.58%） | 139／149（93.29%） |

C2 的异常保留率分别是 283／556＝50.90% 和 149／556＝26.80%；有效一致性点占基础异常点的比例分别为 269／556＝48.38% 和 134／556＝24.10%。不能只报告保留点中的 95.05% 和 89.93%。

下列条件可以重叠。低矮由世界参数中的物体局部完整高度不超过 0.2 米定义；远距由异常回波距离中位数在 35–50 米定义。C2 列依次列出“保留／基础；有效／保留”，原始点数分母不能省略。

| 条件 | 帧数 | 基础异常数 | C1 基础有效 | C2 半采样 | C2 四分之一采样 | C3 基础有效 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 1–4 点异常 | 5 | 13 | 0／13（0%） | 8／13；0／8 | 7／13；0／7 | 4／13（30.8%） |
| 5–19 点异常 | 3 | 27 | 22／27（81.5%） | 15／27；12／15 | 9／27；3／9 | 27／27（100%） |
| 至少 100 点异常 | 2 | 449 | 449／449（100%） | 227／449；224／227 | 116／449；114／116 | 449／449（100%） |
| 35–50 米远距 | 3 | 6 | 0／6（0%） | 3／6；0／3 | 3／6；0／3 | 0／6（0%） |
| 低矮物体 | 7 | 385 | 376／385（97.7%） | 195／385；190／195 | 103／385；92／103 | 381／385（99.0%） |

3 张无异常帧的 C1 均关闭，C2/C3 继续计算正常点监督。两个 `201` 特殊槽位样本均完成核对；其中 `validation/world_000/0` 的 3 条异常记录是同一位置的三个槽位，分别为 65078、196150、327222。它们在两个删减视图中均共同保留，几何支持只计一个位置；该异常位于 62.65 米，属于槽位核对样本，超出官方评价距离。

C1 的基础视图有 73 条可信交界边，异常边缘标签仍为 1；距离经同标签边传播。基础异常中 18 点无效：11 点尺度及局部支持不足，7 点没有可达交界。正常点主要因没有通向观测交界的同标签路径而无效。更突出的限制是饱和：正常有效点只有 163 个目标小于 1，异常有效点只有 144 个；两类合计的非饱和比例为 307／141,428＝0.217%。例如 `validation/world_003/310` 的槽位 105819，图距离为 0.363980 米，尺度为 0.173374 米，归一化目标为 0.699796，标签保持异常。大量饱和点对训练的影响仍待决定，不能由整体异常有效率代替判断。

C2 的异常失效主要来自删减后尺度或标签组支持不足，部分点失去规定的空间跨度；正常点最常见的失效原因也是尺度不足。对上述验证帧槽位 105820，半采样将尺度从 0.123980 米变为 0.196751 米，正常/异常局部支持分别从 24/67 个位置变为 12/33 个位置，仍符合一致性条件。1–4 点异常在这批样本的两个删减级别下均没有有效一致性点，需作为明确的覆盖缺口保留。

C3 基础异常的 9 个无效点均缺少至少 20 个插入前地面支持；远距代表点实际只有 0、7 或 9 个支持，特殊槽位异常没有局部地面支持。正常点主要因插入前支持不足（439,336 点）或查询位置在支持凸包外（318,171 点）而无效。低矮异常已经获得有效目标：例如 `validation/world_003/310` 槽位 105820 的查询高度为 −1.857585 米，插入前平面高度为 −2.000723 米，目标为 −0.143138 米；插入后仍有 1,474 个地面支持，半采样后为 740 个，目标位移保持原值。

C3 的最低可见支持数有可以单独讨论的实际例子。`train/world_015/285` 槽位 71191 在基础视图有 26 个可见支撑，目标 −0.066704 米；半采样保留该异常点，但可见支撑降至 13 个，因此现有掩码关闭。仅为解释失效原因，继续核对这 13 个支撑：矩阵满秩、平面误差 0.032387 米、水平协方差最小特征值 0.729152 平方米，查询点位于支撑凸包内，其余规定条件均满足。四分之一采样仅剩 6 个支撑，查询点已在凸包外；此时只放宽数量要求也不足以恢复有效性。这些附加几何诊断没有改变目标或掩码，也没有证明减少支持数仍可保持真实学习效果。

先导对全部交界路径做同标签与最短路径核对，逐点检查两个删减视图的对应关系；所有基础和保留异常回波的 C3 另用 NumPy 最小二乘与 SciPy 凸包独立复算。全套 26 项测试通过，`train/world_015/446` 的 50 个辅助数组在 1 与 16 个线程下逐元素相同。修正过一处报告写出错误：无异常清单没有距离字段，现按缺失值处理；监督公式和样本选择未改变。

上述结果是 `9a0ecb5` 的监督计算先导。它支持计算实现及该批样本的覆盖判断，没有确认三项辅助监督会改善真实分割结果；损失聚合与可见支持单变量比较见下一节。

## 损失聚合与可见支持对照

`supervision.loss` 定义当前采用的聚合规则；`src/supervision.py` 中的 `boundary_loss`、`sampling_loss`、`surface_loss` 是可求梯度的 PyTorch 实现。每次调用处理当前小批量的同一视图或同一删减级别：先合并该批点行，再按类别求均值。拼接多个帧时，C2 的 `dense_row` 必须加上各基础帧在批次中的点行偏移，使其仍指向同一帧、同一槽位的基础预测。

| 辅助项 | 点误差 | 聚合规则 |
| --- | --- | --- |
| C1 | 预测距离与既有距离目标的绝对误差 | 有效且目标小于 1 为交界带，等于 1 为饱和区；每个区域内先分别平均正常点、异常点，再对存在类别等权。两个区域均存在时，交界带占 0.75，饱和区占 0.25。 |
| C2 | 稀疏异常概率与停止梯度的对应基础异常概率之差的平方 | 在原有一致性有效掩码内，分别平均正常点、异常点，再对存在类别等权。 |
| C3 | 预测表面位移与既有目标的绝对误差 | 在正式默认可见支持至少 20 的有效掩码内，分别平均正常点、异常点，再对存在类别等权。 |

空类别跳过；C1 空区域跳过后，将剩余区域权重归一化。整项无有效点时返回可反向传播的零；先选有效点再计算误差，不用无效占位值补目标。若同时使用多个视图，同一辅助项对存在有效监督的视图/级别等权平均。三项辅助损失与基础检测损失之间的系数本轮未设定。3∶1 是初始区域权重，未证明最优；所有分组信息和点权重只用于损失，不进入推理输入。

同一批 14 帧分视图合并后，直接计算恒预测 1 的 C1 损失，并用新权重下的加权中位数求最优常数。这里没有训练网络。

| 视图 | 原逐点等权：恒预测 1 | 新聚合：恒预测 1 | 新聚合下最优常数 | 该常数的新损失 |
| --- | ---: | ---: | ---: | ---: |
| 基础 | 0.001094 | 0.378209 | 0.657314 | 0.287008 |
| 半采样 | 0.001911 | 0.351113 | 0.728553 | 0.281759 |
| 四分之一采样 | 0.003647 | 0.416506 | 0.562172 | 0.304636 |

原逐点等权下，三个视图的最优常数均为 1。新聚合中，交界带正常/异常组的总权重各为 0.375，饱和区两组各为 0.125；基础视图中 140,727 个饱和正常点只占 0.125。复制一遍全部饱和组后，新聚合的恒预测 1 损失保持不变，验证了饱和点数量不会继续压低该损失。上述数值只说明聚合性质，不是模型性能比较，也不能保证网络不会学成其他常数。

C2 半采样有效点中的异常比例为 0.03657%，四分之一采样为 0.03978%；C3 基础视图为 0.06348%。新聚合对存在的正常、异常类别均值各赋予 0.5 权重。它规定的是误差均值的系数，实际梯度大小仍由预测误差决定。C2 的配对、相位和有效条件没有改动，失去异常证据的点仍关闭一致性。

C3 已将数量参数分开：`minimum_reference_support_points=20` 同时约束插入前参考点和筛选后的拟合内点；`minimum_visible_support_points=20` 是正式默认。比较只把后者设为 10，满秩、误差、空间展开和凸包条件均不变。`surface_targets` 返回按可见支持下限索引的视图结果，默认取 `[20]`；比较时传入 `[20,10]`，共用一次插入前平面拟合和每个视图的后续几何检查。

运行命令如下，仍只读取原有 14 帧及其三个视图。比较没有重算或改写 C1/C2 目标；原辅助文件和冻结基础帧均保持原样。

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  .venv/bin/python -m src.supervision --compare --workers 16
```

结果单独保存于 [comparison/summary.json](../results/supervision/pilot/comparison/summary.json)，同目录的 `<split>/<world>/<frame>.npz` 保存下限 10 的完整 C3 数组及恢复点槽位，标记 `formal_default=false`。同名 JSON 保存逐帧类别计数和独立几何核对。旧版无效目标中的 0 只是占位；新恢复点的数值取自同一插入前参考平面。

| 视图 | 正常有效：20 → 10（新增） | 异常有效：20 → 10（新增） | 其中低矮异常：20 → 10（新增） |
| --- | ---: | ---: | ---: |
| 基础 | 861,173 → 861,339（166） | 547 → 547（0） | 381 → 381（0） |
| 半采样 | 410,548 → 426,325（15,777） | 272 → 277（5） | 190 → 193（3） |
| 四分之一采样 | 188,823 → 197,129（8,306） | 139 → 139（0） | 97 → 97（0） |

计数单位是实际文件回波行；低矮异常是异常的子集。三个视图不能相加后解释成不重复的物理点。正常新增行对应的不同坐标数分别为 166、10,770、6,136，差别来自 `201` 的重复槽位。

正常点恢复的逐帧来源如下；所有异常恢复均发生在半采样视图。

| 来源／世界／帧 | 正常新增：基础 | 正常新增：半采样 | 正常新增：四分之一采样 | 异常新增（其中低矮） |
| --- | ---: | ---: | ---: | ---: |
| 训练／world_016／385 | 0 | 640 | 685 | 0 |
| 训练／world_011／322 | 20 | 664 | 296 | 2（0） |
| 训练／world_015／285 | 146 | 1,190 | 446 | 3（3） |
| 训练／world_015／446 | 0 | 674 | 548 | 0 |
| 训练／world_010／210 | 0 | 1,118 | 501 | 0 |
| 训练／world_018／191 | 0 | 1,010 | 380 | 0 |
| 合成验证／world_004／116 | 0 | 493 | 565 | 0 |
| 合成验证／world_003／451 | 0 | 108 | 163 | 0 |
| 合成验证／world_003／260 | 0 | 630 | 312 | 0 |
| 合成验证／world_001／499 | 0 | 101 | 192 | 0 |
| 合成验证／world_003／189 | 0 | 234 | 264 | 0 |
| 合成验证／world_003／310 | 0 | 597 | 319 | 0 |
| 合成验证／world_003／3 | 0 | 3,230 | 1,520 | 0 |
| 合成验证／world_000／0 | 0 | 5,088 | 2,115 | 0 |

恢复的异常槽位为 `train/world_011/322` 的 68119、69151（均有 17 个可见支撑），以及低矮样本 `train/world_015/285` 的 71189、71191、71193（分别有 14、13、11 个支撑）。这些点均重新通过后续几何条件。例如槽位 71191 的参考点和内点均为 28，目标仍为 −0.066704 米；13 个可见支撑相对原平面的误差为 0.032387 米，水平协方差最小特征值为 0.729152 平方米，查询点在凸包内。

原来首先因可见支持少于 20 而失效的正常点，在下限 10 下仍有下列失效情况。表中记录首次失败条件；数量仍不足 10 时不宣称其他几何条件合格。

| 视图 | 原数量不足 20 | 恢复有效 | 仍不足 10 | 凸包不包含查询点 | 可见支持质量不合格 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 基础 | 171 | 166 | 0 | 0 | 5 |
| 半采样 | 17,292 | 15,777 | 383 | 882 | 250 |
| 四分之一采样 | 23,027 | 8,306 | 7,620 | 2,346 | 4,755 |

质量不合格进一步分为：基础视图 5 点水平展开不足；半采样 170 点平面误差过大、83 点水平展开不足，其中 3 点同时存在这两项问题；四分之一采样分别为 139 和 4,616 点。四分之一采样的 4 个原数量不足异常点仍少于 10 个支持，其中 3 个是低矮异常；原先因参考表面、凸包等条件失效的其余点没有因这次数量修改而转为有效。

默认 20 的 336 个 C3 数组与原先导逐元素一致，原本有效的目标值完全不变。新增和仍失效点另做了 184 次独立 NumPy/SciPy 几何核对；30 项测试通过，包括参考点不足 20、内点不足 20 时不得因比较下限 10 而获得有效目标，以及分组权重、空组、无效值和 C2 停止梯度检查。

这次比较确认：调整可见支持数量能够在半采样的两个训练帧中恢复少量异常监督，同时仍会被其他几何条件约束。有效率增加不能证明 10 更好；正式默认保持 20。远距组只有 3 帧、6 个异常点，继续作为后续检查缺口，不据此裁决远距机制失败或要求重生成世界。本轮停止在损失聚合规则和这一单变量对照，没有扩大先导、补算全池或训练模型。

## 开发池的训练内容检查与生成依据

这次检查确认了内容缺口，也保留了有价值的现有观测。不能继续把监督计算正确解释为样本质量已经合格；同样，不能把所有低矮背景描述为缺少正常参照。

[src/coverage.py](../src/coverage.py) 首先读取冻结根清单选中的 40 个世界及其 19,824 帧观测记录，不包含未选候选。训练侧为 14,368 帧、111,232 个异常回波，其中官方距离内为 110,124 个；合成验证侧为 5,456 帧、34,951 个异常回波，其中官方距离内为 34,665 个。无异常帧分别为 9,183 和 3,642。各世界的完整维度、指数、材质、放置语义、倾角及观测背景分类均保存在本地 [结果明细](../results/coverage/v1.json) 的 `worlds` 中；`inventory` 按条件保存帧数、回波数和每个世界的贡献。

“合格”指传感器距离 [2.5,50] 米内至少有 5 个异常回波；“少点”指该范围内 5–19 个；“远距”指清单中异常回波中位距离在 [35,50] 米；“低矮”沿用物体局部完整高度不超过 0.2 米的定义。训练与合成验证中的所有合格帧分别为 2,693 和 943。点数是回波观测数，不是独立物体数；多个世界反复使用同一条正常轨迹，背景也不能按合成帧数视为独立。

局部检查先固定样本：每世界取异常点数最多的最早一帧；合格少点帧按 `(in_range,frame)` 排序取中间一帧；加入全部合格远距帧后去重。得到训练 122 帧、合成验证 18 帧，共 140 帧、40 个世界、129 个不同原始扫描身份，其中包括全部 61 帧合格远距观测。具体身份在 `selection` 中，先于监督计算写出。邻域均使用完整扫描；基础视图计算 C1，两级删减计算 C2，三视图计算默认参考支持 20、可见支持 20 的 C3。辅助数组只在内存中计算，没有改写世界、基础帧或原 14 帧先导产物。

| 目标 | 训练侧全池内容 | 合成验证侧全池内容 | 针对性检查与生成判断 |
| --- | --- | --- | --- |
| C1：边界 | 32 个单基元世界；全池异常点的 51.08% 来自贡献最多的 5 个世界。物体完整长、宽、高分别为 0.204–1.395、0.104–0.952、0.062–0.725 米；有 5 个高长比小于 0.1 的扁平物体。 | 8 个单基元世界；最多的一个世界贡献 25.30% 异常点。长、宽、高分别为 0.410–0.992、0.185–0.567、0.081–0.623 米；有 1 个高长比小于 0.1 的扁平物体。 | 检查中训练 31/32、合成验证 8/8 世界具有交界。非饱和异常边界目标分别为 1,324 和 327 个。已有边界并非只来自一两个世界；但全部交界正常端均为语义 40/48/49，全部物体均无多部件组合。后续补组合轮廓与接触关系，不把已有扁平形态误称为缺失，也不声称组合形状已经证明能改善真实迁移。 |
| C2：稀疏 | 合格少点：1,428 帧、13,750 点、32 世界。远距：474 帧、1,158 点、19 世界；其中合格远距仅 59 帧、435 点、3 世界，82.53% 的点来自 `world_017`。 | 合格少点：513 帧、5,014 点、8 世界。远距：247 帧、468 点、8 世界；其中合格远距仅 2 帧、11 点、1 世界。 | 全部合格远距帧中，半采样的有效异常一致性仅训练侧 30 点、7 帧、1 世界；四分之一采样为零，合成验证两级均为零。普通少点观测与实际远距观测的覆盖明显不同。后续须按实际距离、投影尺度、朝向和遮挡提出不同世界，并将合格远距列为独立内容条件，不能靠增加同一物体的连续帧补齐。 |
| C3：低矮 | 2,831 个有异常观测的低矮帧、62,630 点、20 世界；70.22% 的点来自最多的 5 世界。合格低矮帧有 1,480 个；合格低矮远距仅 15 帧、76 点、2 世界。 | 765 个有异常观测的低矮帧、13,619 点、4 世界；最多的一个世界贡献 36.18%。合格低矮帧有 390 个；合格低矮远距为零。 | 所检低矮帧中，基础 C3 有效异常点为训练 1,679/1,809、合成验证 617/617；异常附近具有表面监督的正常凸起参照分别见于 11/20 和 3/4 个低矮世界。全部合格远距异常点均因原始参考支撑不足 20 而无效。后续要补距离和背景搭配；现有正常参照应保留，不能把可见支持 20→10 当作上述缺口的解决办法。 |

表中远距行的点数指这些帧的异常回波总数。逐点核对后，训练合格远距帧中实际位于 [35,50] 米的异常点为 433 个，其余 2 个略低于 35 米；合成验证为 11 个。世界成员与官方帧资格并未据此改变。三个训练世界的合格远距贡献分别为 `world_017` 的 44 帧、359 点，`world_020` 的 10 帧、51 点，以及 `world_007` 的 5 帧、25 点；合成验证全部来自 `world_001` 的第 585、590 帧。

C1 在检查样本中产生训练 1,054 条、合成验证 181 条交界边，分布于 63 和 12 帧。训练正常／异常有效点为 1,908,058／2,526，其中非饱和点为 1,851／1,324；合成验证正常／异常有效点为 413,108／767，其中非饱和点为 395／327。训练非饱和异常点贡献最多的一个、五个世界占 9.21%、39.05%；这不支持“有效边界普遍只集中在极少世界”，但只适用于这批检查。单看各世界最密帧，训练 28/32、合成验证 5/8 有交界；单看每世界的少点代表帧，则为 27/32、7/8。最密观测并不保证一定存在正式定义中的可观测交界。

全部 40 个物体仍是单一闭合超二次曲面，指数改变了形态，扁平样本也确实存在，但没有多基元接合、弯折或多部件外轮廓。训练放置语义为 28 个道路点、3 个语义 48 点和 1 个语义 49 点；合成验证为 3 个道路点和 5 个语义 48 点。记录的放置平面相对世界 z 轴的倾角分别为 0.05–5.37°、1.28–3.74°，不作为精确重力坡度。训练交界正常端有 894 条来自语义 40、142 条来自 48、18 条来自 49；合成验证为 59、122、0 条。现有证据说明接触语义范围有限，并未证明其中每个局部几何都相同，也未验证真实复杂轮廓的迁移。

C2 的异常点保留率与保留后的有效率须分开解释。在全部合格远距训练帧中，半采样保留 213/435 点（48.97%），其中 30/213 点（14.08%）有效；这 30 点全部属于 `world_017`。四分之一采样保留 100/435 点（22.99%），有效为 0/100。合成验证半采样保留 3/11 点（27.27%），有效 0/3；四分之一保留 2/11 点（18.18%），有效 0/2。训练半采样失效点中，176 个没有可靠局部尺度，133 个未保留足够的标签组支持，70 个空间展开不足；原因允许重叠。四分之一采样的 100 个保留异常点全部没有可靠局部尺度。不能把这些计数直接解释为某个阈值不合理，也不能要求已经失去证据的视图复制异常判断。

每世界少点代表帧提供了不同证据：训练半采样保留 132/285 点（46.32%），99/132 有效（75.00%），覆盖 26 世界；四分之一保留 71/285（24.91%），23/71 有效（32.39%），覆盖 8 世界。合成验证分别保留 33/68（48.53%）、16/68（23.53%），有效 30/33（90.91%）、0/16，覆盖 7、0 世界。较密帧的两级配对则在训练 32、31 世界和合成验证 8、8 世界提供异常一致性。这支持保留原 C2 规则，同时把真实远距内容不足与单纯删减稀疏区分开来；它不证明普通少点样本已足以代表所有稀疏情况。

C3 的所检低矮异常点在基础／半采样／四分之一视图中的有效数为训练 1,679／839／419，分母为 1,809／906／452；合成验证为 617／308／154，分母相同。训练全部 435 个合格远距异常点及合成验证全部 11 个均在插入前参考点不足 20 时退出。独立读取这些点的原始正常扫描，2 米水平邻域中的不同参考位置为训练 0–18 个，合成验证均为 2 个。例如 `train/world_007/173` 的槽位 78801 有 6 个参考位置，`validation/world_001/585` 的槽位 79884 有 2 个。此处尚未执行参考表面后续几何检查，不能声称把参考下限改小就能获得可靠目标；本轮不改变参考支持条件。

正常参照的定义是：原检测标签为正常，距插入异常实际回波不超过 2 米，C3 有效，且当前点比估计正常表面高 5–20 厘米。这个范围在检查前指定，仅用于观察“凸起仍为正常”的样本，不是路缘识别器或新的异常阈值。所检低矮帧中，训练有 2,376 个这样的正常回波，来自 19 帧、11 世界；合成验证有 154 个，来自 5 帧、3 世界。贡献最多的 5 个训练世界占 87.21%，其中语义 48 有 1,568 点，语义 40 有 173 点，另外还保留车辆、建筑、围栏等正常类别的表面附近回波。若不限制在异常附近，所检低矮世界的完整当前扫描均含此类正常参照。因此，本次否定“所有低矮世界都完全没有合法凸起参照”的概括，但不能据此宣布路缘、复杂坡面或困难负例已经充分覆盖。

几何核对给出了可定位的正负例。同一低矮世界 `train/world_012` 的第 393 帧，异常槽位 109846 的表面位移为 −0.059386 米，参考内点／可见支持为 278／244；附近正常车辆语义槽位 108832 的位移为 −0.182251 米，参考内点／可见支持为 252／217。后者相对表面的凸起更高，但检测标签仍为正常。在 `train/world_002/10` 中，异常槽位 91557 与附近正常语义 48 槽位 93603 的位移分别为 −0.098578、−0.071182 米，两者目标都有效。`checks[*].probes` 保存了 116 次独立 NumPy/SciPy 核对的坐标、平面系数、支撑数及残差，核对差异小于 10⁻⁶ 米。这些是监督几何证据，没有模型输出。

该次检查形成 `v1.json:generation_plan` 的修订依据：保留已有简单形态和正常参照，补充连通组合形状以及距离、朝向和背景搭配；把合格远距、合格少点、低矮合格远距分别作为按世界报告和选择的内容条件。原入口仅要求每世界任意一帧合格，再按照 val19 统计选择世界，不能保证上述交叉条件。当时确定的后续任务是移除参考包络和总变差拟合，实际生成符合来源条件的新候选。上述检查对应 `64f92fe`；新入口及候选实测见后文，开发池本身未改变。

据此区分处理位置：缺少组合形态、远距合格观测只来自很少世界、合成验证低矮远距为空，属于内容与选样问题；远距原始参考支持不足和删减后证据丢失属于观测与监督可用性问题，当前不能据此裁定应放宽阈值；现有点数集中则仍可由后续按世界抽样控制，但不能替代前两类处理。材质尚未发现独立缺口，本轮不同时修改。最终迁移价值仍须由自己的 V1 训练和真实验证回答。

复算命令如下。该次实测使用 24 核 CPU、约 20 GiB 可用内存、6 个进程各 3 个几何线程；CPU 路径复用现有精确算法，GPU 未参与。检查耗时 270.79 秒，最大单进程常驻内存约 0.70 GiB，输出约 2.01 MB。运行前后 Windows E 盘均约有 87.98 GB 可用，保留了 10 GB 安全余量。

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 \
  MKL_NUM_THREADS=1 NUMBA_NUM_THREADS=3 \
  .venv/bin/python -m src.coverage --dataset results/synthetic/v1 \
  --output results/coverage/v1.json --workers 6 --threads 3
```

世界清单汇总和具体样本选择先于辅助计算完成；一次准备性读取修正了根清单中帧数配置的路径，没有改变样本或观测口径。与原先导唯一重叠的 `validation/world_003/260` 在两类点、三视图的所有共同统计上完全一致。C1/C2/C3 定义、全部损失规则和历史生成参数与本轮开始时逐项相同。本轮 32 项测试通过，同时检查了官方距离过滤后计数、世界贡献不能按帧冒充，以及选择不依赖辅助有效性。

## 基础世界试生成的已有证据

本节记录 `bc6c588` 已完成的基础试生成：训练侧 16 个世界、7,184 帧，合成验证侧 16 个世界、10,912 帧。32 个固定候选均完成整条源序列，通过现有物理检查，并至少具有一张距离过滤后异常点数达到官方门槛的帧。[基础根清单](../results/synthetic/candidates/manifest.json) 的原状态 `candidates_complete` 保持原样；全部世界现已被首轮实验根清单引用。以下计数仅指基础 32 世界，补充结果见下一节。

该批条件在渲染前写入当时的 `v1.json:proposals`，完整副本仍在基础根清单的 `configuration.proposals`：下表每类在正常 `train/206` 和 `train/201` 上各用两个种子。种子由 `SeedSequence([2062026, source_sequence, index]).generate_state(1)[0]` 唯一确定，实际 32 个种子先写入根清单的 `planned_candidates`。每个种子只产生一个固定形状和材质，最多检查 128 个正常支持位置，收集至多 6 个通过几何与背景条件的支持；预算执行后没有替换种子或追加候选。所有类别每侧都完成了 2/2 个物理世界；表中数量专指这些世界是否实际产生合格远距观测，不是类别名称所保证的结果。

| 候选编号 | 形状与提出目的 | 预定背景 | 训练合格远距世界 | 合成验证合格远距世界 |
| --- | --- | --- | ---: | ---: |
| `000–001` | 普通单基元 | 地面 | 0/2 | 1/2 |
| `002–003` | 扁平单基元 | 结构背景 | 0/2 | 1/2 |
| `004–005` | 两部件接合 | 结构背景 | 0/2 | 2/2 |
| `006–007` | 扁平弯折组合 | 地面 | 0/2 | 1/2 |
| `008–009` | 远距稀疏单基元 | 地面 | 1/2 | 1/2 |
| `010–011` | 双支腿与横梁组合，提出较大远距投影 | 结构背景 | 2/2 | 2/2 |
| `012–013` | 低矮远距单基元 | 地面 | 0/2 | 2/2 |
| `014–015` | 低矮远距弯折组合 | 结构背景 | 2/2 | 2/2 |

地面与结构背景是插入前、支持位置 3 米内的描述：出现至少两种支持语义，或至少三个已知正常非地面回波时记为结构背景，其余为地面。没有删除或平整原始背景。每侧最终各有 8 个地面和 8 个结构背景世界，但这个分类不等同于邻近异常处具有可靠正常凸起参照。材质分位和粗糙度范围未变；正常 `train/206` 的既有标定在核对全部 449 帧及射线输入一致后复用。生成和选样入口已移除 val19 参考包络与总变差拟合；旧池的世界没有被复用为新候选。

**组合形状与放置。** 32 个物体中，16 个是单基元，4 个是两部件接合，8 个是弯折组合，4 个是双支腿与横梁组合。全部组合通过连续连通性检查。长宽高按整个物体在局部坐标系中的连续几何外界计算，包含所有部件和偏移，外界保守留出 10⁻⁶ 米。训练物体的完整高度为 0.0845–0.7377 米，合成验证为 0.1069–0.6920 米，每侧均有 8 个低矮物体。

`validation/candidate_005` 的第一部件高度为 0.1541 米，完整物体高度为 0.2568 米，因此没有被计入低矮组。`train/candidate_010` 在局部高度 −0.22395 米处，中心查询位于物体外，两个支腿中心查询位于物体内，确认横梁下方保留空隙并形成两处支撑。32 个物体标准与加密下界计算的最大差为 1.065×10⁻⁸ 米，埋地比例均为零；放置后的局部竖轴与支持法向、整物体最低点与支持平面的代数核对误差均不超过 2.23×10⁻¹⁶。全部源帧通过既有的已观测非地面障碍物深穿透检查。这些结果支持当前几何和物理检查条件下的正确性，不等同于获知扫描未观测区域的全部真实表面。

支持搜索保留了失败记录：训练／合成验证分别出现 333／175 次平面质量或完整投影支持不合格、412／174 次轨迹距离不合格、284／236 次背景类别不合格；训练另有 4 次后续局部碰撞或贴地拒绝、2 次参考点不足，合成验证有 85 次指定距离内无可用地面点。这些是提出阶段的原因记录；后续局部检查与初始支持检查不是同一个计数阶段。没有一个最终物理候选因上述过程被伪造为成功。

**实际内容覆盖。** 下表来自全部 32 个完整世界的已有逐帧观测，列内依次为不同世界数、帧数、异常回波数。点数统计所列帧的全部异常回波；帧资格仍按 [2.5,50] 米内至少 5 点决定。

| 内容 | 训练：世界／帧／异常回波 | 合成验证：世界／帧／异常回波 | 最多一个世界的点贡献：训练／合成验证 |
| --- | ---: | ---: | ---: |
| 全部观测 | 16／7,184／282,400 | 16／10,912／249,738 | 18.90%／22.56% |
| 合格少点，所有距离 | 15／711／7,535 | 16／1,367／13,360 | 16.19%／11.10% |
| 合格远距，35–50 米 | 5／197／1,733 | 12／319／2,380 | 41.03%／37.48% |
| 远距且距离过滤后至少 20 点 | 0／0／0 | 0／0／0 | — |
| 合格低矮，所有距离 | 8／1,081／100,429 | 8／1,493／112,762 | 42.49%／22.25% |
| 合格低矮远距 | 2／41／224 | 6／99／545 | 77.68%／38.90% |

全体距离内异常回波分别为 280,527 和 247,524；合格远距帧的距离内回波分别为 1,729 和 2,378。无异常回波的 3,791 和 5,900 帧全部保留。全体异常点最多的五个世界贡献分别为 68.37%、60.79%；低矮组为 90.69%、80.84%。完整每世界贡献与集中度见 [覆盖结果](../results/coverage/candidates.json) 的 `inventory`。同一物体的相邻帧和多个世界复用的正常背景仍有关联，不能按这些帧数声称具有同等数量的独立形态。

旧开发池合格远距为训练 3 世界、合成验证 1 世界，新候选分别为 5 和 12；低矮远距为旧池 2／0 世界、新候选 2／6 世界。训练低矮远距的世界数没有增加，新增的两世界均为弯折组合，`candidate_015` 仍贡献该组 77.68% 的点。旧池与新候选的世界预算不同，此处比较用于定位内容缺口，不是等预算生成优劣实验。

远距专门提出的 16 个世界中，12 个产生了合格远距帧。训练 `candidate_009、012、013` 在本轮支持预算内的三个远距检查位置均没有可用包围盒射线机会，最终也没有远距回波。合成验证 `candidate_008` 曾有最多 8 条包围盒机会，但实际没有达到合格远距门槛，说明包围盒机会不能代替物体表面交点和传感器实际回波。四个双支腿组合均产生远距观测，但整批远距帧最多只有训练 18 点、合成验证 16 点；较大投影提出尚未实现“至少 20 点”的内容条件。本轮没有据结果放大物体、删掉困难帧或追加尝试。

**监督与正常参照。** 在读取上述观测后、计算任何辅助目标前，按既定规则选定训练 53 帧、合成验证 81 帧，覆盖全部 32 个世界。每世界取最密帧、全距离合格少点中位帧；远距按 5–19 点和至少 20 点分别选最多三个距离排序代表，再保留一个远距 1–4 点代表，去重后共 134 帧。此批包含 56 帧合格远距，并非全部 516 帧；以下有效数只适用于固定检查样本。

基础视图包含训练正常／异常 6,244,020／6,517 点，合成验证 7,098,344／7,633 点。各项有效监督如下，每个单元格依次为正常点数／异常点数。C2 数量是删减后保留点中的有效数，C3 各视图重新检查可见支持，全部阈值和损失保持原定义。

| 监督 | 训练有效正常／异常 | 合成验证有效正常／异常 |
| --- | ---: | ---: |
| C1 基础有效距离 | 730,254／5,498 | 570,065／5,468 |
| C1 其中非饱和距离 | 943／711 | 1,453／1,117 |
| C2 半采样一致性 | 2,790,658／2,915 | 3,085,248／3,519 |
| C2 四分之一采样一致性 | 1,311,571／1,360 | 1,409,335／1,554 |
| C3 基础表面 | 2,038,152／5,532 | 3,983,394／6,434 |
| C3 半采样表面 | 966,433／2,767 | 1,933,454／3,166 |
| C3 四分之一采样表面 | 440,024／1,371 | 914,693／1,553 |

C1 交界覆盖训练 15/16、合成验证 16/16 世界；其中组合物体为 7/8、8/8 世界。训练单基元／接合／弯折／双支腿物体提供的非饱和异常目标为 272／74／289／76，合成验证为 558／180／311／68。训练 `candidate_005` 的所检帧没有有效交界，不能将有组合轮廓解释为每个视角都有有效边界。非饱和异常目标最多五世界的贡献为 57.24% 和 59.98%。交界正常端语义为训练 40 类 350 条、48 类 29 条、80 类 2 条，合成验证为 40 类 151 条、48 类 265 条；训练 `candidate_011/171` 的两个正常端属于 80 类，说明本批交界已有少量非地面正常结构，但绝大多数仍来自地面。

C2 必须区分保留与有效。下表只统计所检合格远距帧：训练 18 帧、154 个异常点、5 世界；合成验证 38 帧、244 点、12 世界。

| 删减与来源 | 异常点保留率 | 保留异常点中的一致性有效率 | 有效异常一致性世界 |
| --- | ---: | ---: | ---: |
| 半采样，训练 | 75/154＝48.70% | 14/75＝18.67% | 3 |
| 半采样，合成验证 | 124/244＝50.82% | 11/124＝8.87% | 3 |
| 四分之一采样，训练 | 37/154＝24.03% | 0/37 | 0 |
| 四分之一采样，合成验证 | 61/244＝25.00% | 0/61 | 0 |

训练有效半采样异常点来自 `candidate_008、010、011`，分别为 4、4、6 点；合成验证来自 `candidate_009、010、011`，分别为 2、6、3 点。例如训练 `candidate_008/349` 基础 9 点、保留 4 点，4 点均有效。远距半采样中训练 61/75、合成验证 107/124 个保留异常点没有可靠局部尺度；标签组支持丢失分别为 53、102 点，原因允许重叠。四分之一采样的全部 98 个保留异常点均缺少可靠尺度。新候选已经提供更多不同世界的远距检测与部分一致性观测，仍不能据此声称远距一致性机制充分可用。

对全距离少点代表帧，训练半采样保留 86/168、其中 48/86 有效，覆盖 11 世界；合成验证保留 67/134、其中 14/67 有效，覆盖 7 世界。四分之一采样分别保留 37/168、40/134，有效 5/37、0/40，覆盖 2、0 世界。这组包含远距代表，不能把其结果替代专门的远距表。较密的近中距观测则使两级有效异常一致性覆盖每侧全部 16 个世界。

C3 在所检合格低矮观测中，基础有效异常数为训练 2,857/2,904、合成验证 3,359/3,476，均覆盖全部 8 个低矮世界。但所检合格远距异常的基础 C3 均无效：训练 153/154 点、合成验证 244/244 点在插入前参考支撑不足 20 时退出；训练另 1 点具有 22 个参考位置，随后因可见支持不足而退出。参考数量范围为训练 0–22、合成验证 0–18。所检低矮远距的 31 个训练点和 100 个合成验证点均无三项已检查辅助，全部保留检测标签。全体所检远距点中，仅有检测监督的点分别为 132/154、208/244；这里指基础 C1、基础 C3 及两级 C2 对应点的并集之外，不外推其他未计算视图。

正常凸起参照仍存在，但局部搭配不均衡。所有低矮世界的所检完整扫描都含 C3 有效、凸起 5–20 厘米的正常点；限制在异常回波 2 米内后，训练仅为 240 点、4/8 世界，合成验证为 567 点、1/8 世界，验证侧全部来自 `candidate_003`。预定的结构背景分类并不能保证此项局部参照覆盖，后续世界搭配应明确保留这一缺口，不能把全扫描其他位置的正常结构冒充异常邻近的参照。

几何核对复用了原独立表面计算，共 66 个查询与批量结果相差小于 10⁻⁶ 米。例如训练 `candidate_002/309` 的异常槽位 83412 位移为 −0.031771 米，参考内点／可见支持为 168／158；同帧正常槽位 81378 位移为 −0.071854 米，内点／可见支持为 117／107。正常点相对表面更高，检测标签仍为正常。合成验证 `candidate_003/444` 的异常槽位 123219 位移为 −0.134664 米，正常语义 48 槽位 110930 位移为 −0.070615 米，均由各查询位置插入前的正常扫描拟合得到。正常结构及其标签没有被改为异常或从背景删除。

覆盖检查复算命令如下，结果包含完整清单汇总、134 个具体样本、逐类有效数、失败原因和几何核对；辅助数组仅在内存中计算。

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 \
  MKL_NUM_THREADS=1 NUMBA_NUM_THREADS=2 \
  .venv/bin/python -m src.coverage --workers 12 --threads 2 --far-limit 3 \
  --dataset results/synthetic/candidates --output results/coverage/candidates.json
```

运行前实测 24 核 CPU、约 20 GiB 可用内存，无其他实验占用。生成使用 16 个单线程进程，完整候选计算耗时 221.37 秒，最大单进程常驻内存约 0.88 GiB；保存产物约 65 MB。134 帧覆盖检查使用 12 进程各 2 几何线程，耗时 184.62 秒，最大单进程约 0.71 GiB，结果约 2.16 MB。期间无交换空间占用；Windows E 盘生成前约 87.97 GB、检查后约 87.90 GB，满足 10 GB 余量。生成的全部帧写后还原与渲染结果逐元素一致，新增候选访问也验证了完整世界身份与帧数。34 项测试通过；三项监督、损失、随机相位总种子和正常标定配置与 `64f92fe` 逐项相同。

这批证据确认组合形状和部分远距／低矮远距提出可实现，同时定位了训练低矮远距世界少、验证局部正常参照集中的缺口。随后用户决定保留全部 32 世界，仅补这两项；远距至少 20 点降为探索性条件，远距删减后的证据缺失保留为 C2 适用边界。实际有界补充与冻结记录如下。

## 定向补充与首轮实验池冻结

本次按运行前确定的八次预算，实际完成 8 个新增完整世界、4,524 张单帧，全部通过现有物理检查并达到各自内容目的。训练四个世界各渲染正常 `train/206` 的全部 449 帧，合成验证四个世界各渲染 `train/201` 的全部 682 帧。[首轮实验根清单](../results/synthetic/experiment/manifest.json) 已标记 `frozen`：训练 20 世界、8,980 帧，合成验证 20 世界、13,640 帧。基础 32 世界身份和顺序逐项保持不变，新增成功世界按编号追加。历史 val19 拟合池没有进入该清单。

**运行前固定的提出条件。** 新种子由 `SeedSequence([2062026,1,source_sequence,index]).generate_state(1)[0]` 产生，与基础世界的种子空间分开；八个实际种子在任何候选运行前写入 `planned_candidates`。每个种子只产生一个形状与材质，最多尝试 192 个支持位置，收集至多 12 个几何合格支持，每个支持检查朝向偏移 −30°、0°、30°。训练物体完整局部长、宽、高分别在 1.4–1.8、1.0–1.5、0.16–0.195 米内按对数均匀采样；验证物体长、高为 0.8–1.2、0.08–0.18 米，单体宽 0.4–0.8 米、组合宽 0.6–0.9 米。材质、回波标定、物理碰撞和所有辅助条件保持原样。

远距支持与朝向以真实前景表面命中排序：先比较三个检查帧中至少五条不同射线命中的帧数，再比较各帧命中数截至 12 后的总和，同分保留先提出者。检查帧在实际轨迹中按物体中心距离最接近 36、42、48 米选择，限制在 35–50 米并去重。最终信号只记录和核对，不参与排序。完整渲染后，整个物体局部高度不超过 0.2 米，且至少一帧异常中位距离在 35–50 米、官方距离内异常回波不少于五个，才实现训练补充目的。

验证候选先在正常源固定帧 40、120、200、280、360、440、520、600 的完整扫描上计算原 C3，寻找标签有效、表面位移为 −0.20 至 −0.05 米的正常位置。保留传感器距离 5–18 米的不同位置，分别找到 851、2,141、130、349、114、1,087、244、7 个。每次提出选取半径 0.3 米内至少三个位置的原生参照簇，再寻找距所选中心为整物体投影半径加 0.15–0.65 米的合法地面支持。渲染后，预定核对帧必须具有至少五个距离内异常回波，并与至少三个距实际异常回波不超过 2 米的参照位置共同可见；这些参照仍须通过原 C3，受保护位置的原始坐标、强度、标签必须全部精确保留。上述数量是本次补充的初始内容条件，不是统计充分性或最优参数结论。

| 来源与新增编号 | 形态 | 固定种子 | 完整局部高度（米） | 完整渲染后的预定目的 |
| --- | --- | ---: | ---: | --- |
| 训练 `000` | 单基元 | 1828416539 | 0.1862 | 45 帧合格低矮远距，262 个异常回波 |
| 训练 `001` | 单基元 | 1987859863 | 0.1814 | 39 帧合格低矮远距，243 个异常回波 |
| 训练 `002` | 三部件弯折 | 3300434948 | 0.1635 | 56 帧合格低矮远距，361 个异常回波 |
| 训练 `003` | 三部件弯折 | 1498195472 | 0.1721 | 26 帧合格低矮远距，160 个异常回波 |
| 合成验证 `000` | 单基元 | 3895486994 | 0.1001 | 第 520 帧，41 个异常回波与 8 个受保护有效参照 |
| 合成验证 `001` | 单基元 | 2533967606 | 0.0964 | 第 520 帧，93 个异常回波与 32 个受保护有效参照 |
| 合成验证 `002` | 三部件弯折 | 117377475 | 0.1672 | 第 280 帧，165 个异常回波与 33 个受保护有效参照 |
| 合成验证 `003` | 三部件弯折 | 3674884339 | 0.1323 | 第 520 帧，180 个异常回波与 10 个受保护有效参照 |

全部新物体通过连通、贴地和完整轨迹已观测障碍物深穿透检查。八处选定支持的平面拟合误差为 0.00562–0.03619 米。支持提出阶段仍出现失败：训练／验证分别有 82／27 次平面或完整投影支撑不足、54／8 次轨迹距离不符，验证另有 10 次参照簇位置不足；后续局部碰撞或贴地拒绝为 3／1 次。全部记录保存在各 `world.json:generation.support_rejections`。最终没有世界级失败，未追加种子、改变条件或凑满配额。

**真实表面命中核对。** 训练 `supplement_002/180` 的包围盒机会、真实前景表面命中、最终异常回波依次为 13→9→9；`supplement_001/217` 为 6→0→0，说明即使单体也不能用包围盒直接代表可见表面。验证 `supplement_003/520` 为 249→181→180，显示表面相交与返回信号是不同阶段。所有选定放置的 16 个检查帧，其固定信号记录均与完整渲染后保存的异常总数、官方距离内点数相等。没有提高回波概率、挪动已生成的点或重抽信号。

**全池内容。** 下表读取最终 40 世界的全部 22,620 帧清单；列内依次为不同世界数、帧数、异常回波数。回波数包括所列帧的全部异常点，资格按官方距离过滤后计数决定。

| 内容 | 训练：世界／帧／异常回波 | 合成验证：世界／帧／异常回波 |
| --- | ---: | ---: |
| 全部观测 | 20／8,980／405,040 | 20／13,640／277,827 |
| 合格少点，所有距离 | 19／1,088／10,863 | 20／1,677／16,155 |
| 合格远距 | 9／363／2,759 | 13／320／2,385 |
| 合格低矮 | 12／1,957／221,922 | 12／2,090／139,792 |
| 合格低矮远距 | 6／207／1,250 | 7／100／550 |
| 远距且距离过滤后至少 20 点 | 0／0／0 | 0／0／0 |

训练合格低矮远距从基础 2 世界、41 帧、224 点增至 6 世界、207 帧、1,250 点；最多一个世界的点贡献由 77.68% 降为 28.88%。四个新增物体的形状、尺寸和位置各不相同，但前三个放置仍处于同一正常路段，不能视作四种独立道路背景。验证另自然出现一个低矮远距世界的一帧五点观测，这不是验证补充的选入条件。全池无异常帧为训练 4,323、验证 7,598；这些帧全部保留。全体官方距离内异常回波为 402,153、275,523；合格远距帧中的距离内异常回波为 2,755、2,383。每世界贡献与集中度见 [本次覆盖结果](../results/coverage/experiment.json) 的 `inventory`。

**新增世界的局部监督。** 只对八个成功补充按原最密、少点、远距代表规则及四个预定正常参照核对帧计算，共训练 24 帧、验证 17 帧。所有邻域使用完整原始或当前扫描，三视图辅助数组仅在内存中计算；基础 32 世界的 134 帧结果没有重算。以下数字不是全池辅助覆盖率。基础视图正常／异常点分别为训练 2,792,433／2,307、验证 891,289／1,329。

| 监督 | 新增训练检查：有效正常／异常 | 新增验证检查：有效正常／异常 |
| --- | ---: | ---: |
| C1 基础有效距离 | 169,282／1,577 | 95,003／1,128 |
| C1 其中非饱和距离 | 248／205 | 664／620 |
| C2 半采样一致性 | 1,234,692／1,000 | 351,973／395 |
| C2 四分之一采样一致性 | 582,517／459 | 152,846／197 |
| C3 基础表面 | 878,703／2,200 | 448,340／1,312 |
| C3 半采样表面 | 414,607／1,092 | 219,564／653 |
| C3 四分之一采样表面 | 187,982／551 | 104,643／329 |

这些有效异常监督均覆盖各侧全部四个新增世界。C1 交界边为训练 72 条、验证 309 条，边的两端按真实检测标签逐条核对。C2 中训练保留异常为半采样 1,155/2,307＝50.06%、四分之一 596/2,307＝25.83%，保留异常中的有效率分别为 1,000/1,155＝86.58%、459/596＝77.01%；验证相应为 663/1,329＝49.89%、333/1,329＝25.06%，有效率 395/663＝59.58%、197/333＝59.16%。近中距观测贡献了主要的有效一致性。

专看所检合格低矮远距，训练为 14 帧、92 个异常点、4 世界；验证为 1 帧、5 点、1 世界。训练半采样保留 46/92＝50.00%，其中 0/46 一致性有效；四分之一保留 35/92＝38.04%，其中 0/35 有效。验证半采样保留 2/5，其中 0/2 有效；四分之一没有保留异常点，因此保留点中的有效率不定义。固定射线删减不保证小样本恰好保留预定比例。训练半采样失效中 45/46 点缺少可靠尺度、40/46 点失去标签组支持；四分之一的 35 点均缺少可靠尺度，原因可重叠。没有放宽掩码来改变这些结果。

远距仍能提供部分其他监督：训练 C1 有效异常 10/92、其中 8 点非饱和，C3 为 6/92；验证 C1 为 3/5、C3 为 0/5。训练 C3 的 86 个无效点中，69 个参考支撑不足，17 个可见支撑不足；验证为 3 个参考支撑不足、1 个拟合内点不足、1 个可见支撑不足。所检远距中仅有检测监督的点为训练 76/92、验证 2/5，均正常保留。此处不要求三项掩码同时有效，也不能由新候选的两级 C2 均无效推翻基础池已经存在的半采样远距支持。

四个验证核对帧中的受保护参照合计 83 个，源帧与槽位没有重复，全部保留原数组并通过独立原 C3 几何计算。完整扫描批量复算另找到异常 2 米内有效正常凸起分别为 45、133、208、32 个，包括未用于提出的邻近正常位置；四帧中的全部 479 个异常点也获得基础 C3。整个新增验证检查共记录 789 个局部正常凸起观测，覆盖四个世界；与基础证据中的一个世界合计，已有局部见证覆盖 5 个低矮验证世界。这是已观察世界数的下界，不是全池穷尽统计。三项新增见证来自第 520 帧、一项来自第 280 帧，不能将四个搭配解释为四个独立正常场景，更不能把几何偏离直接命名为真实路缘或模型困难真值。

独立几何复算共检查 33 个查询，均与批量表面目标相差小于 10⁻⁶ 米。例如验证 `supplement_000/520` 的异常槽位 93764 表面位移为 −0.095984 米、参考内点／可见支持 575／558；附近正常语义 72 的槽位 89664 位移为 −0.170346 米、支持 360／343，检测标签仍为正常。这个搭配提供了“正常点也可比异常点更偏离局部正常表面”的具体参照，但还没有模型学习效果证据。

本次覆盖复算命令如下，完整清单与新增局部结果单独保存，不改写世界或基础帧。

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 \
  MKL_NUM_THREADS=1 NUMBA_NUM_THREADS=2 \
  .venv/bin/python -m src.coverage --new-only --far-limit 3 --workers 12 --threads 2
```

运行前实测 24 个可用 CPU 核、约 20 GiB 可用内存，无其他实验。正常参照查找与世界生成采用 8 个单线程进程，总耗时 111.49 秒，生成进程最大常驻内存约 0.85 GiB；新增完整产物约 29 MB。41 帧检查采用 12 进程、每进程 2 个几何线程，耗时 63.61 秒，进程 CPU 时间合计 853.21 秒，最大单进程常驻内存约 0.69 GiB；没有交换空间占用。Windows E 盘生成前约 87.90 GB、检查后约 87.87 GB，保持 10 GB 余量。

35 项测试通过，新增关键用例核对真实组合空隙、背景遮挡、固定信号与基础成员顺序。真实基础帧 `train/candidate_011/171`、`validation/candidate_003/0` 的提出计算还与既存物理渲染逐项核对，覆盖普通槽位及 `201` 的特殊槽位。全部 4,524 个新增帧写后还原均与渲染器的坐标、强度、打包标签、回波槽位及掩码逐元素相等。`render.py`、`data.py`、`supervision.py` 及协议的三项监督、损失与共同参数相对 `bc6c588` 保持不变。

当前已完成有界补充和首轮数据冻结。远距至少 20 点仍未实现，所检新低矮远距的一致性支持仍缺失，背景多样性仍受两条正常序列限制；这些限制随数据进入首轮实验，不再触发追加候选。下一项能改变研究判断的工作是实现 V1、准备所需监督并取得第一轮真实验证结果，按用户要求逐轮决定是否继续训练。

## 必要验证与当前边界

关键检查对应数据和评价的实际风险：原始槽位错配、标签错配、零坐标槽处理、距离边界、少于 5 个异常点的帧过滤、全局同分排序、严格高召回阈值、AP 失分加总、地面估计缺失和前景遮挡。

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 \
  .venv/bin/python -m pytest -q -p no:cacheprovider
```

接口检查使用人工构造的分数时，只能支持存储、排序和评价实现的正确性，不产生模型性能证据。

数据还原、辅助目标和损失核对只支持相应实现及所检观测。首轮实验池已完成八次有界补充并冻结为每侧 20 世界；当前损失规则和 C3 参考支持 20、可见支持 20 保持不变。模型已经实现，正式训练尚未执行；边界、采样尺度和正常表面设计是否改善真实异常检测，仍须由 V1 的模型输出与对应对照支持。

## V1 模型与训练实现

[模型代码](../src/model.py) 的输入只包含当前实际回波及由该扫描计算的无标签几何。LitePT-S 对 0.05 米体素内全部回波求均值，编码场景上下文，再按体素归属返回每个原始点。逐点恢复同时保留该点相对体素均值的三维偏移、原始强度及坐标；体素层不生成检测标签，也不进行标签投票。

局部几何分支在原有完整扫描邻域中，同时读取真实位移与按当前采样尺度归一化的位移，经过共享两层全连接变换，再汇合邻域均值和最大值。尺度无效时，归一化分量为零，真实几何和完整检测输入继续保留。32 通道局部几何与 72 通道场景上下文、逐点坐标强度和体素内偏移融合为 96 通道点特征。边界分支预测 0–1 无符号距离，表面分支预测以米为单位的有正负竖直位移。最终检测头读取这 96 通道特征和两项**预测值**，输出未经逐帧归一化的单精度异常分数。真值和标签可靠性掩码不能进入模型。

`model.enhancements` 分别控制三项增强。全部关闭时，局部增强和两项辅助预测退出计算，完整场景编码、逐点特征和直接检测头仍然存在。这个开关提供结构上可独立使用的基础检测器；本次不训练正式基础版对照。正式 V1 的三个开关均为 `true`，总参数量为 12,723,423。

编码器来自作者发布的 [LitePT 独立实现](https://github.com/prs-eth/LitePT/tree/436d04801c8151faebe66a1b2d368a9711e7e6aa)，没有载入预训练权重。来源、许可证和本机混合精度适配见 [编码器说明](../vendor/litept/README.md)。体素均值采用固定顺序分组求和；相同输入的重复推理须逐元素一致，才能在同一个源帧的多个世界间复用完全相同输入的预测。

短检查确实发现并处理了一项训练／推理差异。作者默认 BatchNorm 在训练时使用当前点云统计，在推理时改用历史滑动统计；同一组短检查权重下，训练远距帧的联合模型检测损失分别为 1.2907（历史统计）与 0.0420（当前统计），仅检测损失模型分别为 1.2773 与 0.0313。保持权重和样本不变的比较将主要差异定位到了统计切换。V1 因此明确设置 `model.backbone.norm_running_stats=false`，训练和推理均使用当前单帧的内部特征统计，保留可学习仿射参数。它不读取其他扫描，也不改变最终分数的全局比较规则；不能据此断言历史统计在其他训练规模或任务中一定无效。

[训练入口](../src/train.py) 将模型输入与监督数组分开传递。基础、半采样、四分之一采样分别前向与反向；只有一致性项读取停止梯度的基础分数。每个视图都计算有效正常点和异常点分别取均值的二分类损失，再对存在的类别等权。C1 保留交界带与饱和区 3∶1，以及区域内部类别等权；C2/C3 保留有效类别等权。一个基础样本的检测损失平均三个参与前向的视图；每个辅助项只平均存在有效正常或异常监督行的视图／配对，空视图不参与。有效但误差恰为零的视图仍计入分母；整项没有有效监督时为零。总损失为检测损失加三项各乘 0.1 的辅助损失。

`98d542e` 的跨视图代码曾固定除以 3 或 2，与已经规定的有效视图平均冲突；该问题影响实际反向和日志，不能只修改说明。现由 `loss_view_weights` 根据有效掩码及正常／异常标签计算权重，`view_losses` 将同一权重用于顺序反向、损失日志和 `check_summary`。例如仅基础视图具有 C1 时，跨视图结果保留其完整损失，不再额外除以三。四个基础样本的梯度累积及每视图内部权重不变。

第一版参数统一记录在 [v1.json](v1.json) 的 `model` 和 `training`，不是通过真实验证指标搜索得到：随机种子 2062028；AdamW；学习率 0.001；权重衰减 0.005；前 100 次更新从约十分之一学习率线性预热，此后恒定；每次一张基础扫描及对应两个视图，累积四个基础样本；梯度范数上限 1；半精度自动混合精度与梯度缩放，预测头和损失使用单精度。一次正式训练遍历预先打乱的全部 8,980 个基础样本，共 26,940 次独立单帧前向、2,245 次更新尝试。缩放器跳过的更新单独计数，不能把尝试数冒充成功更新数。

更新前先解除梯度缩放并计算总范数。范数有限时，使用 PyTorch 原来的裁剪计算；元素有限但总范数非有限时，在裁剪、权重衰减及 AdamW 更新前明确报错。若梯度元素本身非有限，则保留 AMP 跳步，记录 `success=false`、原因与降低后的缩放系数。这个分支区分依据 [PyTorch 的裁剪定义](https://docs.pytorch.org/docs/2.12/generated/torch.nn.utils.clip_grad_norm_.html) 和 [AMP 的更新约定](https://docs.pytorch.org/docs/2.12/notes/amp_examples.html)，没有改变学习率、裁剪上限或正常更新公式。本机 PyTorch 2.12／CUDA 修前复现中，两个有限的 10²⁰ 梯度得到非有限总范数，旧函数却报告成功，参数因权重衰减改变约 5.36×10⁻⁷；这是独立构造的执行反例，不是正式模型训练故障或科学效果结果。

本机实现检查使用 Python 3.13、PyTorch 2.12.0／CUDA 13.0、spconv 2.3.8、torch-scatter 2.1.2+pt212cu130、flash-attn 2.8.3.post1、NumPy 2.4.6、SciPy 1.17.1 和 Numba 0.65.1。设备为 RTX 5080 Laptop GPU，显存约 16 GB；最终隔离检查实测 PyTorch 峰值分配约 3.57 GB，包含完整 `201` 扫描及恢复接口检查。该数值是所检样本的资源记录，不是整轮训练的峰值保证。当前命令使用已经验证的 `.venv/bin/python`，正式执行时重新核对 CPU、内存、GPU 和 Windows E 盘资源。

离线监督使用 `results/supervision/experiment/`，不改写基础冻结差量。每份辅助文件绑定监督实现与配置、世界身份、完整正常源身份、冻结差量、视图及原始槽位。读取时校验基础回波与删减对应，训练中不运行渲染器。原始正常表面在同一源帧内只拟合一次；没有受插入、遮挡、别名变化及其支撑圆盘影响的查询复用该结果，其余查询仍按原函数重算。该复用已在训练普通帧、合成验证普通帧和 `201` 特殊槽位帧上，与未优化计算逐数组精确核对。

本次已完成全部 8,980 个训练样本和 13,640 个合成验证样本，共 22,620 份辅助文件、67,860 个独立视图。辅助文件与实验根清单逐一对应，没有缺失或额外成员，总计 15,274,782,930 字节。每份新文件写出后，均通过训练读取器检查世界、源帧、槽位和配对身份，并与刚计算的训练数组逐元素核对。收尾另读取两侧最后一帧，训练三视图为 123,321／61,687／30,844 点，合成验证为 123,963／61,978／31,014 点。完成状态及资源记录见 [监督清单](../results/supervision/experiment/manifest.json)；缓存保留本地，不随代码推送。

完整准备使用 20 个进程、每进程 1 个计算线程，耗时 6,463.92 秒，合计进程 CPU 时间 128,736.23 秒，平均约占用 19.92 个核心，最大单进程常驻内存约 0.84 GB。Windows E 盘运行前剩余 87.82 GB、结束时剩余 77.23 GB，保持 10 GB 安全余量。只保存后续训练所需的目标、掩码和对应，不保存全池邻接图、平面参数或失效原因数组，也没有重新做全池覆盖裁决。已执行命令如下：

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 \
  MKL_NUM_THREADS=1 NUMBA_NUM_THREADS=1 \
  .venv/bin/python -m src.supervision --full --jobs 20 --workers 1
```

默认命令仅执行固定小样本的隔离实现检查：

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  .venv/bin/python -m src.train --stage check
```

检查包括体素内偏移的逐点表达、无标签真实输入、三个辅助掩码关闭的异常点检测梯度、C2 仅在一致性项停止基础分支梯度、关闭全部增强后的基础检测前向与反向，以及固定两张训练扫描上的短学习运行。联合损失和仅检测损失各运行 64 次更新，使用相同初始参数；这不是泛化实验，所有优化后的权重均丢弃。详细记录保存为 `results/v1/check.json`。

修复后的最终代码已重新通过短检查；旧版本的通过记录没有作为新代码的证据。以下检测损失均为三种独立视图的平均值：

| 固定训练样本 | 初始检测损失 | 联合损失短运行后 | 仅检测损失短运行后 |
| --- | ---: | ---: | ---: |
| `supplement_002/37`，743 个基础异常回波 | 0.67635 | 0.03191 | 0.03008 |
| `supplement_001/207`，约 39 米、6 个基础异常回波 | 0.68234 | 0.03409 | 0.02764 |

两侧各有 64 次成功更新，均能区分固定样本中的正负点。该比较仅排除所检实现无法表达或学习检测目标的明显问题。这只是固定两帧的单次短运行，重复 CUDA 训练也不保证位级一致，不能据这组数字裁决损失方案的效果。联合运行的基础帧 C1 加权误差为 0.24327、0.08215，对应最优加权常数误差为 0.26310、0.28295；C3 加权误差为 0.16089、0.51731，对应常数误差为 0.58314、1.72007。C1/C3 输出具有逐点变化；这些数值只表明当前短检查中的目标能够被学习，不代表边界或低矮异常的真实迁移已经成立。

较密帧中的两个混合体素保留正常与异常的各自标签，两对点均输出不同分数。远距帧的半采样、四分之一采样各保留三个异常回波；它们的三个辅助掩码均关闭，但检测头与共享特征仍得到非零梯度。该帧的异常点没有直接 C3 表面监督，不能把其检测可学习性归功于直接表面监督。关闭全部增强后，120,673 个实际回波仍全部输出分数，检测头和编码器均获得检测梯度。

合成验证 `candidate_000/0` 只检查特殊槽位接口，没有参数更新：基础、半采样、四分之一采样分别保留 314,775、157,305、78,849 行，对应 104,925、52,435、26,283 个不同位置；三种视图重复推理逐元素相同。真实 `val/125/0` 在没有标签的输入接口下输出全部 125,446 个实际回波分数。`FramePrediction` 保存后分数精确不变。

[训练执行回归](../tests/test_train.py) 新增 10 项检查，全部 46 项测试通过。跨视图检查包括全部有效、只有一个 C1/C3 视图有效、只有一个 C2 配对有效、全部无效，以及有效但误差恰为零；还包含仅忽略标签行的掩码为真的情况。顺序反向与独立构造的参考目标、梯度和检查汇总一致，比较使用绝对容差 10⁻⁷、相对容差 10⁻⁶。更新检查确认普通有限梯度正常裁剪并更新 AdamW；有限大梯度的总范数溢出会在更新前报错，参数、AdamW 状态及缩放系数保持不变；`inf/NaN` 梯度均由 AMP 跳步，参数和 AdamW 动量不变、缩放系数减半。确定性 CPU 小模型按每组四个样本累积，保存恢复后的下一组身份、学习率、成功标记、参数、AdamW 状态、缩放器和全部随机状态均精确一致。

实际 V1 的恢复检查同样使用两组、每组四个基础样本；第一组作为共同起点，第二组分别连续执行和从新构造的模型恢复后执行。正式恢复与检查复用同一个 `restore_checkpoint`。初次比较在一个权重张量的元素上发现 1.19×10⁻⁵ 的差异，超过初设的 10⁻⁵ 绝对容差，当次检查失败，不能视作通过。随后从完全相同的参数、AdamW、缩放器和随机状态进行无保存的内存重放，以及磁盘恢复对照。内存对照使用独立复制的 AdamW 状态；最初共享可变引用的对照因快照被后续更新污染而作废，没有用于裁决。

有效对照中，内存重放自身的参数最大差异为 1.6195×10⁻⁴，AdamW 一阶动量为 1.5805×10⁻⁶；磁盘恢复分别为 1.2414×10⁻⁴、2.9975×10⁻⁶。四个参与样本的各项损失逐值相同，两种对照的起始状态与结束随机状态均精确一致。这个结果支持 CUDA 更新的非确定性已经足以触发原容差，不能单独由参数不逐位相同认定恢复实现错误。

据上述执行对照，最终检查先要求恢复的起始状态精确一致，再将下一次 CUDA 更新的参数绝对容差明确设为 2×10⁻⁴、AdamW 状态绝对容差设为 10⁻⁵，相对容差均为 10⁻⁴；损失与总范数使用绝对容差 10⁻⁶、相对容差 10⁻⁴。样本身份、学习率、缩放器、成功／跳步标记和全部随机状态仍精确比较。这些容差只用于实现检查，未修改训练参数或科学评价条件。最终重新执行的参数最大差异为 1.4816×10⁻⁴、AdamW 状态为 1.6689×10⁻⁶；连续与恢复后的第二次更新均成功，学习率均为 0.000118，缩放系数均为 1024，各损失相同，梯度总范数分别为 0.83206487、0.83204424，检查通过。完整记录在 `check.json` 的 `resumed_next_update`，不能据此宣称 CUDA 训练逐位可复现。

`4d87924` 的修复限于训练执行、针对性测试和说明；`model.py`、`supervision.py`、冻结世界及辅助缓存保持不变，缓存版本为 `8091f7d99c0b09459a8e7191b804abd6bfa9a6cd3a3c27717d8fbfc5c5d8f1cc`。该提交完成时尚未启动正式训练。下面记录用户复审通过后实际执行的一轮，代码、配置、数据与监督均沿用该版本。

**2026 年 9 月 10 日，用户复审通过并授权执行以下命令。完整训练与合成验证已完成；用户随后因合成结果不佳要求停止真实验证，进程已中断。当前保持停止。**

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  .venv/bin/python -m src.train --stage all
```

该命令的原定范围是一轮训练、全部 13,640 个合成验证基础帧及辅助误差、全部 8,659 张真实扫描的无标签推理和官方评价。入口确认已有 `check.json` 与当前代码、配置及依赖完全一致后，复用其通过记录；本次没有重跑隔离学习检查或回归测试。正式训练重新从记录的初始参数开始，没有使用隔离检查优化后的权重。

训练访问了全部 8,980 个基础帧，完成 26,940 次独立训练视图前向和 2,245 次更新尝试；2,245 次全部成功，AMP 跳步为零。训练耗时 3,890.67 秒，约 64 分 51 秒。轮末检查点与训练记录逐项核对：样本排列与种子 2062028 的排列精确一致，8,980 个索引无重复或遗漏，访问位置每次递增四；检查点标记第一轮完成，AdamW 各状态均记录 2,245 步，模型参数和全部损失、梯度范数记录均为有限值。512 次更新的裁剪前范数超过 1，仍按既定公式裁剪；没有非有限范数或身份保护触发。缩放系数按原规则从 1,024 增至 2,048。

下表为更新记录的描述性平均，各辅助项尚未乘总损失中的 0.1 系数。前后两个区间包含不同训练样本，不能据此宣称固定验证集误差改善。

| 损失 | 全部更新平均 | 前 100 次更新平均 | 末 100 次更新平均 |
| --- | ---: | ---: | ---: |
| 检测 | 0.084583 | 0.340017 | 0.042490 |
| C1 边界 | 0.066073 | 0.081192 | 0.049158 |
| C3 表面 | 0.151784 | 0.668171 | 0.091291 |
| C2 一致性 | 0.009515 | 0.008868 | 0.005528 |

合成验证完整评价 20 个世界、每世界 682 帧，共 13,640 个基础帧，耗时 2,391.95 秒，约 39 分 52 秒。预测文件与报告中的世界、帧身份逐一对应，没有缺失或额外成员。基础帧共输出 1,576,847,468 个实际回波的分数；指标使用其中 994,139,180 个有效正常点和 277,827 个异常点，忽略标签不参与。所有帧使用同一种未修改的单精度异常分数，合并后计算指标，没有逐帧归一化。完全相同当前输入的确定性预测复用 20,239 次，各世界的标签与误差仍分别统计。

| 合成验证指标 | 数值（%） |
| --- | ---: |
| AP | 4.720526 |
| AUROC | 98.156781 |
| FPR95 | 7.636772 |
| R@1%FPR | 73.063093 |

全局 R@1%FPR 对应原始分数阈值为 2.5658175945，检测到 202,989 个异常点，同时产生 9,941,369 个正常点误报。高 AUROC 并不代表高分段的误报已得到控制；当前合成 AP 较低，不能由训练损失下降认定检测质量已经合格。上述阈值只用于报告这一既定指标，没有用于重新训练或选择检查点。这些均为合成验证成绩，不是实测 STU 真实验证成绩。

辅助误差按正常与异常分别累计。C1 为归一化无符号距离的平均绝对误差，C3 为竖直位移的平均绝对误差，单位为米；C2 为有效配对预测概率差的均方误差。以下是各组点的误差统计，不等于按帧、类别及区域重新加权后的训练目标。

| 辅助目标与视图 | 正常有效点数 | 异常有效点数 | 正常误差 | 异常误差 |
| --- | ---: | ---: | ---: | ---: |
| C1 基础 | 43,410,408 | 216,067 | 0.103023 | 0.216254 |
| C1 半采样 | 28,833,500 | 112,065 | 0.116937 | 0.194495 |
| C1 四分之一采样 | 8,379,412 | 53,735 | 0.135784 | 0.199763 |
| C3 基础 | 527,540,579 | 240,244 | 0.125158 | 0.123766 |
| C3 半采样 | 255,036,626 | 117,326 | 0.121928 | 0.130413 |
| C3 四分之一采样 | 119,933,233 | 56,550 | 0.113457 | 0.125821 |
| C2 半采样配对 | 426,630,572 | 119,505 | 0.015106 | 0.043474 |
| C2 四分之一采样配对 | 192,722,457 | 52,683 | 0.027099 | 0.070847 |

C1 还单独核对有效且目标小于 1 的交界带，以免总体误差被饱和区掩盖：基础视图正常／异常分别为 96,662／92,843 点，平均绝对误差 0.193883／0.227754；半采样分别为 89,802／70,327 点，误差 0.200338／0.215370；四分之一采样分别为 53,013／39,365 点，误差 0.215888／0.207383。半采样和四分之一采样分别保留 138,870 和 69,470 个异常点，其中 119,505 和 52,683 个点的 C2 配对有效。所有辅助条件与 C3 支撑数 20 保持不变。

用户在获知合成指标后要求停止真实验证。收到指令后，向正在运行的进程组发送中断信号，并确认主进程及其加载进程退出。停止时真实侧只保存了 2,652 份无标签推理输出：序列 125 为 255 帧、137 为 560 帧、138 为 516 帧、139 为 542 帧、140 为 481 帧、141 为 298 帧。它们是未完成的部分预测，保留现场，不作为真实验证结果。程序尚未进入真实标签评价；`inference.json` 和 `real.json` 均不存在，没有计算真实 AP、AUROC、FPR95 或 R@1%FPR。未启动第二轮，也不自动恢复真实推理。

有效产物为 [轮末检查点](../results/v1/epoch1.pt)、[正式初始参数](../results/v1/initial.pt)、[运行配置](../results/v1/configuration.json)、[完整训练记录](../results/v1/training.json)、[合成验证记录](../results/v1/synthetic.json) 和 [完整合成逐点预测](../results/v1/predictions/synthetic/)。检查点包含优化器、梯度缩放器、样本顺序、访问位置和随机状态。合成预测共 12,637,231,184 字节；大体积产物及未完成的真实预测均留在本地，不随报告推送。开训前核对 24 个可用 CPU 核、约 21.8 GB 可用内存及 RTX 5080 Laptop GPU；E 盘剩余 77,191,516,160 字节，满足原定后续峰值预算及 10 GB 余量。运行期间持续读取宿主 E 盘空间，所记录最低值仍高于 64 GB，没有资源保护触发。

当前证据支持“一轮正式优化可以完整执行，并已获得完整合成验证结果”；合成检测质量仍有限。尚无真实验证成绩，也没有区分样本内容、模型连接、损失权重或训练充分程度对当前结果的影响，不能据这一轮直接裁决三项增强的独立作用。后续研究动作等待用户决定。

**2026 年 9 月 10 日，按用户后续要求完成第一轮合成预测的误报分析。误报在类别上集中于人行道、正常汽车和地形类表面，在距离上集中于近距离；筛选评价帧后问题仍然存在。** 这两项是分别统计的边际分布，尚未计算类别与距离的联合贡献。本次读取全部 13,640 份既有基础帧预测，未执行训练、模型前向、世界生成或辅助目标计算，未读取真实验证标签，也未恢复真实推理。训练实现仍为 `4d87924`，分析输入为 `1345faf` 记录的第一轮产物。

分析入口为 [diagnose.py](../src/diagnose.py)，直接调用 `FrozenDataset`、`FrozenFrame` 和 `FramePrediction`。预测目录绑定的 `epoch1.pt` 摘要为 `a92e0d316d7693f23948fc1e56d5caf10a1be614b63ea4a672476b1217e8eff5`，实验根清单摘要为 `19f56bfed809ebec51b63847236c7a314bde63adf54dec0aab395692f2dd2d93`；与现存文件一致。每个样本均保留世界身份、源帧及原始槽位。两个评价范围均使用 `FrozenFrame.anomaly_target`，没有把正常源中的原始语义 `2` 重新解释为合成异常。

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  .venv/bin/python -m src.diagnose --jobs 12
```

完整数值保存在 [两种范围的指标及逐世界帧计数](../results/v1/diagnosis/metrics.json) 和 [误报分组、原始身份统计及局部样例](../results/v1/diagnosis/groups.json)。原有 `synthetic.json` 和全部预测保持原样。后一个文件的 `scopes.*.groups` 为每个工作点提供原始语义、距离、帧类型、世界和源帧的正常点分母、误报数、组内误报率及误报贡献；`source_frames` 还保存逐源帧的槽位与坐标位置重复统计，以及具体原始槽位样例。

**评价对象与检测结果。** “全量合成”保留全部有效合成标签；“STU 条件合成诊断”增加传感器距离 2.5–50 米（含端点）及范围内至少 5 个合成异常点的帧条件。后者仍是合成结果，不是真实 STU 成绩。

| 评价范围 | 世界观测帧数 | 不同正常源帧数 | 世界数 | 正常点数 | 异常点数 | 异常占比（%） |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 全量合成 | 13,640 | 682 | 20 | 994,139,180 | 277,827 | 0.02793868 |
| STU 条件合成诊断 | 3,610 | 636 | 20 | 288,883,162 | 272,059 | 0.09408753 |

| 评价范围 | AP（%） | AUROC（%） | FPR95（%） | R@1%FPR（%） |
| --- | ---: | ---: | ---: | ---: |
| 全量合成 | 4.720526 | 98.156781 | 7.636772 | 73.063093 |
| STU 条件合成诊断 | 13.900228 | 98.125476 | 7.645598 | 71.734440 |

全量结果的原有指标字段、正常与异常点数、阈值及工作点计数均精确复现。筛选后保留 97.9239% 的异常点和 29.0586% 的正常点，异常占比随之增加；同时，源帧及世界观测的构成也改变。因此 AP 的差异不能全部归结为类别比例效应，也不表示模型发生了改进。两种范围的 AUROC、FPR95 相近；在较低误报率下，召回仍明显受限。

有一项需要纠正的具体判断：**本池全量指标中的有效正常点已经全部位于 2.5–50 米，范围外有效正常点及其误报均为零。** 本次没有额外删除这些正常点；正常分母从 994,139,180 减至 288,883,162，来自帧资格筛选。13,640 个观测中有 7,598 个无插入异常帧、2,432 个有异常但不满足范围内点数条件的帧、3,610 个合格帧。不能用“原成绩被范围外正常误报拖低”解释这一结果。

**统一阈值下的精确率和召回。** 每个范围、每个误报率上限只使用一个全局原始分数阈值，所有分组共用该阈值。复用 [现有精确排序与指标函数](../src/evaluate.py)，在一次完整同分归并中同时统计三个上限；同分点不拆分，也不插值得到不存在的工作点。下表的误报率是上限，实际值略低于上限，完整精度保存在指标文件中。

| 范围 | 误报率上限（%） | 原始分数阈值 | 检出异常数 TP | 正常误报数 FP | 精确率（%） | 召回率（%） |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 全量 | 1 | 2.56581759 | 202,989 | 9,941,369 | 2.001004 | 73.063093 |
| 全量 | 0.1 | 5.17012691 | 68,758 | 994,106 | 6.469125 | 24.748495 |
| 全量 | 0.01 | 6.22946739 | 12,726 | 99,413 | 11.348416 | 4.580548 |
| STU 条件 | 1 | 2.72370458 | 195,160 | 2,888,824 | 6.328178 | 71.734440 |
| STU 条件 | 0.1 | 5.18461704 | 67,326 | 288,882 | 18.900755 | 24.746838 |
| STU 条件 | 0.01 | 6.16189575 | 14,839 | 28,888 | 33.935555 | 5.454332 |

**高分正常点的类别。** 下表展示 0.1% 全局误报率工作点，组内误报率为该类误报数除以该类有效正常点数，误报贡献为该类误报数除以本范围全部误报数。原始编号分别统计，名称对应 [原始语义配置](https://raw.githubusercontent.com/PRBonn/semantic-kitti-api/master/config/semantic-kitti.yaml)。所有类别在全部三个工作点的数值均保存在 `groups.json`，此处没有按类别重新选择阈值。

| 全量合成的正常类别 | 正常点数 | 误报数 | 组内误报率（%） | 误报贡献（%） |
| --- | ---: | ---: | ---: | ---: |
| 人行道 `48` | 212,256,828 | 449,167 | 0.211615 | 45.1830 |
| 汽车 `10` | 7,670,380 | 175,807 | 2.292025 | 17.6849 |
| 地形 `72` | 42,390,371 | 154,944 | 0.365517 | 15.5863 |
| 道路 `40` | 178,390,309 | 123,718 | 0.069352 | 12.4452 |
| 围栏 `51` | 37,400,540 | 27,490 | 0.073502 | 2.7653 |
| 车道标线 `60` | 5,616,700 | 26,539 | 0.472502 | 2.6696 |
| 卡车 `18` | 8,415,840 | 24,777 | 0.294409 | 2.4924 |
| 植被 `70` | 322,105,100 | 7,378 | 0.002291 | 0.7422 |
| 停车区域 `44` | 8,234,780 | 2,323 | 0.028210 | 0.2337 |
| 人 `30` | 3,848,372 | 687 | 0.017852 | 0.0691 |
| 交通标志 `81` | 4,643,840 | 562 | 0.012102 | 0.0565 |
| 建筑 `50` | 141,516,640 | 409 | 0.000289 | 0.0411 |
| 杆 `80` | 5,961,300 | 257 | 0.004311 | 0.0259 |
| 树干 `71` | 15,660,280 | 48 | 0.000307 | 0.0048 |
| 自行车 `11` | 27,900 | 0 | 0 | 0 |

| STU 条件合成诊断的正常类别 | 正常点数 | 误报数 | 组内误报率（%） | 误报贡献（%） |
| --- | ---: | ---: | ---: | ---: |
| 人行道 `48` | 57,742,900 | 105,114 | 0.182038 | 36.3865 |
| 汽车 `10` | 3,424,798 | 77,767 | 2.270703 | 26.9200 |
| 地形 `72` | 12,243,573 | 49,140 | 0.401353 | 17.0104 |
| 道路 `40` | 53,069,826 | 29,283 | 0.055178 | 10.1367 |
| 卡车 `18` | 3,868,988 | 10,429 | 0.269554 | 3.6101 |
| 车道标线 `60` | 2,103,544 | 10,007 | 0.475721 | 3.4640 |
| 围栏 `51` | 9,161,842 | 4,323 | 0.047185 | 1.4965 |
| 植被 `70` | 92,103,355 | 1,423 | 0.001545 | 0.4926 |
| 停车区域 `44` | 3,662,545 | 944 | 0.025774 | 0.3268 |
| 人 `30` | 824,226 | 251 | 0.030453 | 0.0869 |
| 交通标志 `81` | 1,165,588 | 76 | 0.006520 | 0.0263 |
| 杆 `80` | 1,260,561 | 58 | 0.004601 | 0.0201 |
| 建筑 `50` | 44,670,327 | 55 | 0.000123 | 0.0190 |
| 树干 `71` | 3,576,717 | 12 | 0.000336 | 0.0042 |
| 自行车 `11` | 4,372 | 0 | 0 | 0 |

人行道、汽车和地形类分别合计贡献全量 78.4542%、STU 条件 80.3169% 的误报。汽车只占两种范围正常点的 0.7716% 和 1.1855%，却贡献 17.6849% 和 26.9200% 的误报；其较高组内误报率说明问题不能只归于地面点数量多。植被虽然数量很大，在这一高分段的误报贡献很小。进一步降到 0.01% 全局误报率时，人行道／地形／汽车仍分别贡献全量 60.9729%／25.9654%／8.2545%，STU 条件下为 43.1321%／33.8445%／17.3809%。

**误报距离与帧类型。** 下表仍为各范围自己的 0.1% 全局工作点。

| 范围 | 距离（米） | 正常点数 | 误报数 | 组内误报率（%） | 误报贡献（%） |
| --- | --- | ---: | ---: | ---: | ---: |
| 全量 | [2.5, 10) | 461,893,666 | 815,100 | 0.176469 | 81.9933 |
| 全量 | [10, 20) | 294,191,168 | 161,774 | 0.054989 | 16.2733 |
| 全量 | [20, 35) | 144,270,849 | 9,160 | 0.006349 | 0.9214 |
| 全量 | [35, 50] | 93,783,497 | 8,072 | 0.008607 | 0.8120 |
| STU 条件 | [2.5, 10) | 132,496,843 | 242,936 | 0.183352 | 84.0952 |
| STU 条件 | [10, 20) | 82,234,577 | 42,634 | 0.051844 | 14.7583 |
| STU 条件 | [20, 35) | 42,657,031 | 2,039 | 0.004780 | 0.7058 |
| STU 条件 | [35, 50] | 31,494,711 | 1,273 | 0.004042 | 0.4407 |

两种范围的 `<2.5` 米及 `>50` 米组正常分母和误报均为零，组内误报率无定义。0.01% 工作点的 2.5–10 米误报贡献进一步达到全量 92.1177%、STU 条件 91.7128%。这些结果将本轮的高分正常误报重点指向近距离；它们不评价远距异常漏检的严重程度。

| 全量范围中的帧类型 | 观测帧数 | 正常点数 | 0.1% 工作点误报数 | 组内误报率（%） | 误报贡献（%） |
| --- | ---: | ---: | ---: | ---: | ---: |
| 无插入异常 | 7,598 | 518,187,843 | 488,687 | 0.094307 | 49.1584 |
| 有异常但不满足 STU 帧条件 | 2,432 | 187,068,175 | 209,859 | 0.112183 | 21.1103 |
| 满足 STU 帧条件 | 3,610 | 288,883,162 | 295,560 | 0.102311 | 29.7312 |

此表三组共用全量阈值 5.17012691，合格帧的 295,560 个误报因而不同于该子集自己在阈值 5.18461704 下的 288,882 个误报。全量 1% 工作点中，无异常帧贡献 49.7740% 的误报，合格帧贡献 31.7839%；0.01% 工作点分别为 53.3300%、22.4367%。无异常帧本身产生大量误报，排除了“所有误报都必须由插入异常附近的特征混合引起”的解释；不能据此排除部分异常邻域还存在边界问题。

**重复背景与源帧集中程度。** 主指标仍按世界观测完整计数。另以 `(train/201, 源帧, 原始槽位)` 统计不同背景点，以 `(train/201, 源帧, 原始 xyz)` 合并 `201` 中重合的槽位。后者只合并同帧完全相同坐标，未将不同时间的点宣称为同一被跟踪表面。重复次数按不同世界计，同世界的重合槽位不增加位置重复次数。两种范围分别覆盖 49,720,042／49,699,774 个不同正常槽位，以及 49,140,080／49,119,812 个不同正常坐标位置。

| 范围 | 误报率上限（%） | 误报观测数 | 曾误报的不同源槽位 | 曾误报的不同同帧位置 | 有误报的源帧数 | 最大 10 个源帧贡献（%） |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 全量 | 1 | 9,941,369 | 585,212 | 575,744 | 658 | 5.7633 |
| 全量 | 0.1 | 994,106 | 61,236 | 60,407 | 595 | 11.3257 |
| 全量 | 0.01 | 99,413 | 6,569 | 6,538 | 379 | 30.6036 |
| STU 条件 | 1 | 2,888,824 | 505,231 | 496,783 | 636 | 7.7089 |
| STU 条件 | 0.1 | 288,882 | 55,091 | 54,310 | 594 | 11.7647 |
| STU 条件 | 0.01 | 28,888 | 7,125 | 7,087 | 410 | 17.2321 |

全量 0.1% 工作点的 994,106 次误报对应 61,236 个原始槽位，平均每个曾误报槽位贡献 16.234 次误报；其中 39,732 个槽位在全部 20 世界均误报，占这些槽位的 64.8834%，贡献 79.9351% 的误报观测。它们不能被解释为近百万个独立失败场景。另一方面，误报覆盖 595 个源帧，最大 10 帧只贡献 11.3257%，也不能把问题归结为几个坏帧。最高分段更集中，但仍覆盖数百帧。

以下给出 0.1% 工作点贡献最大的五个源帧，完整 682 帧及另外两个工作点可在 `groups.json` 复核。源帧分母包含该范围内全部世界的正常观测。

| 范围 | 源帧 | 正常点数 | 误报数 | 组内误报率（%） | 误报贡献（%） |
| --- | ---: | ---: | ---: | ---: | ---: |
| 全量 | 120 | 2,193,800 | 17,436 | 0.794785 | 1.7539 |
| 全量 | 118 | 2,170,551 | 13,282 | 0.611918 | 1.3361 |
| 全量 | 116 | 2,158,480 | 11,519 | 0.533663 | 1.1587 |
| 全量 | 119 | 2,176,514 | 11,196 | 0.514401 | 1.1262 |
| 全量 | 122 | 2,234,369 | 10,567 | 0.472930 | 1.0630 |
| STU 条件 | 379 | 863,814 | 4,413 | 0.510874 | 1.5276 |
| STU 条件 | 377 | 870,175 | 4,126 | 0.474157 | 1.4283 |
| STU 条件 | 397 | 724,439 | 4,030 | 0.556293 | 1.3950 |
| STU 条件 | 403 | 571,369 | 3,843 | 0.672595 | 1.3303 |
| STU 条件 | 402 | 501,730 | 3,269 | 0.651546 | 1.1316 |

具体原始槽位也能复核这种重复。例如在全量 0.1% 阈值下，`120/121575`（人行道）、`377/109243`（汽车）、`406/117466`（地形）的正常观测分母均为 20，误报数均为 20，组内误报率均为 100%，各贡献全量误报的 0.002012%。在 STU 条件阈值下，汽车槽位 `377/109243` 的分母／误报为 11／11，贡献 0.003808%；地形槽位 `406/117466` 为 5／5，贡献 0.001731%。这些是具体身份的示例，不能将“曾误报的不同槽位数／全部不同槽位数”替代原有按观测定义的误报率。

**局部点云核对。** 每种范围按 0.1% 工作点的语义误报贡献选前三类；在同源帧跨世界的误报位置并集中，选取一米格内位置较密的区域，再读取一个明确世界的实际预测。六米邻域仅用于显示，全部指标及分组均已在完整扫描上算完。六幅图均包含完整局部背景，蓝色为所检正常类别，红色为误报；显示时合并完全重合的坐标，统计仍保留原始槽位。

| 范围与图 | 世界／源帧／中心槽位 | 局部有效正常点 | 局部误报 | 六米内插入异常点 | 最近插入回波距离（米） |
| --- | --- | ---: | ---: | ---: | ---: |
| [全量人行道](../results/v1/diagnosis/case_1.png) | `candidate_009 / 120 / 121522` | 15,837 | 900 | 0 | 95.37 |
| [全量汽车](../results/v1/diagnosis/case_2.png) | `candidate_010 / 377 / 114361` | 12,407 | 362 | 0 | 49.07 |
| [全量地形](../results/v1/diagnosis/case_3.png) | `supplement_000 / 440 / 112397` | 7,763 | 142 | 0 | 28.75 |
| [STU 条件人行道](../results/v1/diagnosis/case_4.png) | `candidate_010 / 120 / 121522` | 15,837 | 864 | 0 | 41.13 |
| [STU 条件汽车](../results/v1/diagnosis/case_5.png) | `candidate_010 / 377 / 114361` | 12,407 | 357 | 0 | 49.07 |
| [STU 条件地形](../results/v1/diagnosis/case_6.png) | `candidate_011 / 406 / 110308` | 11,617 | 219 | 0 | 42.63 |

实际查看三个正交投影后，人行道样例显示一片连续、近水平的正常表面，误报沿靠道路的一侧形成带状区域；这里是正常表面之间的交界。汽车样例能看见两处车体轮廓，误报落在车身面片及端部。地形样例显示树旁低矮地表与道路、停车区域相邻，误报位于低表面的边侧。这些判读同时依据实际几何与原始语义；单靠点云不能进一步确定材质或每一处真实物体名称。六个局部均没有插入异常回波，最近的插入回波也在 28.75 米之外；这批例子支持正常结构本身的高分误报确实存在，不能据六例估计全部误报到异常的距离分布。

局部图同时保存同名 PDF，逐份核对均嵌入 SimSun 和 Times New Roman，已检查六幅实际渲染。新计算结果合计约 31.65 MB，留在本地结果目录；报告与分析代码随提交推送，大体积预测不上传。

**实现核对与本轮边界。** 多工作点扩展没有另写 AP 算法，也没有修改原单工作点结果。13 项相关测试通过，覆盖完整同分处理、三工作点与逐个工作点一致、STU 距离端点、合成异常身份、忽略标签及重复槽位统计。源帧 0、341、681 的 60 个世界观测还实际比较了串行和并行计算：排序记录逐字节一致、分组结果逐项一致。全量运行中，每种语义、距离、帧类型、世界和源帧划分的正常分母与误报之和，均与各范围的全局 TP／FP 计数对应；原有全量指标所有字段精确复现。

执行前核对 24 个可用 CPU 核、20.71 GB 可用内存及无其他实验，使用 12 个工作进程、数值库单线程；GPU 未参与。全量读取、两次精确排序、分组及六幅图约耗时 224.58 秒。宿主 E 盘起始剩余 70,126,473,216 字节，临时排序的保守峰值预算为 23,966,008,168 字节；运行中所核对最低余量为 56,760,274,944 字节，高于固定 10 GB 余量。临时记录自动释放后重新查询 E 盘，剩余 72,620,904,448 字节，确认宿主空间已实际回收。

本轮已经证实：评价对象会明显改变合成 AP，但采用 STU 条件后，高分正常误报仍限制召回；类别分组以人行道、正常汽车及地形表面为主，距离分组以近距离为主。误报包含跨世界反复出现的相同背景位置，同时广泛分布于正常源序列。不能将上述两项边际比例相乘来估计某个类别在某段距离的贡献。被排除的解释是“范围外有效正常点造成主要误报”和“所有误报都来自插入异常的局部交界”。这还不能确定训练负例是否不足、是否记忆了训练背景、联合损失是否干扰检测，或第一轮训练是否充分，也不能裁决 C1/C2/C3 的因果作用。

下一项能够增加关键证据的动作，是补齐同一个 `epoch1.pt` 的真实无标签推理并执行官方评价，观察上述正常结构问题是否延续到真实任务；不以合成 AP 达到某个数值为前提。**本轮到只读合成诊断结束，真实侧仍仅有原来的 2,652 份部分预测，没有真实 STU 指标；没有启动第二轮，也没有改变数据、模型或损失。**

**2026 年 9 月 10 日，用户明确授权后，已补齐同一个第一轮检查点的全部真实推理与官方评价。真实 val19 的 AP 为 6.532102%，AUROC 为 97.210719%，FPR95 为 17.214532%，R@1%FPR 为 64.351593%。第二轮训练继续暂停。** 这组结果补全了第一轮真实证据；真实检测仍同时存在正常误报和异常漏检，不能由合成辅助任务误差判断其根因。

本次执行沿用 `main@a70c104` 的现有入口，模型和训练实现仍为 `4d87924`，没有修改代码、模型、损失、协议配置、世界或辅助监督。只执行用户指定的两个阶段：

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  .venv/bin/python -m src.train --stage real && \
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  .venv/bin/python -m src.train --stage evaluate
```

恢复入口核对完整轮末状态、执行配置及预测绑定后，仅为缺失的 6,007 帧运行前向。原有 2,652 份预测保留，执行前后文件大小与修改时间一致。推理阶段沿用禁止读取标签的数据接口，输出未经逐帧归一化的单精度原始异常分数；之后的独立评价阶段才读取真实标签。两个命令均正常退出，没有启动 `--stage all` 或第二轮训练。

当前 [完整真实逐点预测](../results/v1/predictions/real/)、[推理完成记录](../results/v1/inference.json) 和 [真实官方评价及分组](../results/v1/real.json) 均已生成。8,659 份预测与 19 条规定序列的源帧逐一对应，没有缺失、重复或额外成员；评价器读取所有文件并核对原始槽位对实际回波的完整覆盖。真实预测共 7,916,640,938 字节，本次新增 5,469,065,010 字节。检查点及根清单绑定仍分别为 `a92e0d316d7693f23948fc1e56d5caf10a1be614b63ea4a672476b1217e8eff5` 和 `19f56bfed809ebec51b63847236c7a314bde63adf54dec0aab395692f2dd2d93`；执行前后 `epoch1.pt`、`training.json`、`configuration.json` 和 `protocol/v1.json` 的文件摘要均未改变。

**完整评价范围与指标。** 全部 8,659 帧均执行推理并被评价器读取；其中 1,960 帧满足距离 2.5–50 米内至少 5 个异常点的官方条件，来自全部 19 条序列。最终汇总 193,792,470 个有效正常点和 87,499 个异常点，异常占比为 0.04513050%。其他帧仍保留完整预测，但不进入官方点级汇总。这些是真实 STU 标签下的结果，与前面的两种合成诊断明确区分。

| 真实 val19 指标 | 数值（%） |
| --- | ---: |
| AP | 6.532102 |
| AUROC | 97.210719 |
| FPR95 | 17.214532 |
| R@1%FPR | 64.351593 |

全局 R@1%FPR 使用阈值 **1.0310213566**，实际误报率为 **0.99993720%**。该工作点检出 56,307 个异常点，漏检 31,192 个异常点，同时误报 1,937,803 个正常点，精确率为 **2.823666%**。按现有官方严格高召回约定得到的阈值为 **−7.4128994942**，召回率为 95.001086%，检出 83,125 个异常点，正常误报增至 33,360,467 个，对应 FPR95 为 17.214532%。这些阈值由固定检查点的全局分数按既有指标定义计算，没有用于改模型、调训练或逐帧改变分数。

AUROC 表明当前分数已有一定排序能力，但低误报率下仍漏掉约三分之一异常点；若提高到约 95% 召回，正常误报率则很高。因此，真实检测不足已经得到实际结果支持，同时仍不能把它解释为三个辅助机制之一已经被证伪。

**同一个全局工作点的序列诊断。** 以下各序列共用上述 1% FPR 全局阈值，各自的误报率不被固定为 1%；表中召回率也不是为每个序列重新选阈值得到的 R@1%FPR。帧数指全部保存的预测，合格帧数指进入官方指标的帧。

| 序列 | 预测帧数 | 合格帧数 | 正常点数 | 异常点数 | TP | FP | 召回率（%） | 组内误报率（%） |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 125 | 255 | 210 | 21,450,882 | 25,871 | 5,200 | 473,280 | 20.0997 | 2.2063 |
| 137 | 560 | 46 | 4,652,268 | 749 | 404 | 32,748 | 53.9386 | 0.7039 |
| 138 | 516 | 83 | 8,321,471 | 2,254 | 2,201 | 66,635 | 97.6486 | 0.8008 |
| 139 | 542 | 99 | 10,201,337 | 2,844 | 2,659 | 89,948 | 93.4951 | 0.8817 |
| 140 | 481 | 75 | 8,008,759 | 1,465 | 1,390 | 64,406 | 94.8805 | 0.8042 |
| 141 | 697 | 307 | 31,991,712 | 18,389 | 15,941 | 253,406 | 86.6877 | 0.7921 |
| 142 | 537 | 87 | 8,365,690 | 1,596 | 533 | 66,209 | 33.3960 | 0.7914 |
| 143 | 469 | 91 | 7,571,268 | 3,085 | 3,003 | 67,942 | 97.3420 | 0.8974 |
| 144 | 323 | 79 | 7,455,833 | 4,508 | 2,446 | 50,398 | 54.2591 | 0.6760 |
| 145 | 337 | 92 | 8,379,629 | 3,712 | 3,300 | 65,769 | 88.9009 | 0.7849 |
| 146 | 374 | 57 | 5,166,306 | 1,455 | 1,316 | 27,081 | 90.4467 | 0.5242 |
| 147 | 401 | 111 | 10,820,924 | 2,811 | 2,700 | 83,956 | 96.0512 | 0.7759 |
| 148 | 602 | 115 | 11,781,979 | 3,050 | 2,812 | 96,551 | 92.1967 | 0.8195 |
| 149 | 378 | 102 | 10,115,079 | 2,800 | 2,125 | 83,101 | 75.8929 | 0.8216 |
| 150 | 505 | 156 | 15,720,518 | 5,344 | 4,923 | 144,602 | 92.1220 | 0.9198 |
| 151 | 277 | 51 | 4,798,697 | 1,054 | 1,029 | 31,367 | 97.6281 | 0.6537 |
| 152 | 534 | 61 | 5,935,012 | 1,029 | 635 | 39,081 | 61.7104 | 0.6585 |
| 153 | 271 | 75 | 6,498,230 | 3,149 | 2,598 | 47,630 | 82.5024 | 0.7330 |
| 169 | 600 | 63 | 6,556,876 | 2,334 | 1,092 | 153,693 | 46.7866 | 2.3440 |

序列 `125` 的 25,871 个异常点中仅检出 5,200 个，召回率为 20.0997%，贡献全部漏检的 **66.2702%**，同时贡献 **24.4235%** 的误报。它是本工作点下首先需要理解的具体失败来源。其他序列并非一致失效，例如 `138`、`143`、`147`、`151` 的召回均超过 96%。这些差异支持进一步检查具体异常与背景，而不能直接归因于统一的权重、网络结构或训练轮数。

**同一阈值的距离诊断。** 各行分别按本组正常点及异常点计算误报率和召回率，误报贡献为本组 FP 除以全局 FP。

| 距离（米） | 正常点数 | 异常点数 | FP | TP | 组内误报率（%） | 召回率（%） | 误报贡献（%） |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| [2.5, 10) | 156,059,346 | 66,147 | 670,230 | 38,593 | 0.4295 | 58.3443 | 34.5871 |
| [10, 20) | 28,526,437 | 16,191 | 722,422 | 13,358 | 2.5325 | 82.5026 | 37.2805 |
| [20, 35) | 7,791,847 | 4,805 | 389,206 | 4,187 | 4.9950 | 87.1384 | 20.0849 |
| [35, 50] | 1,414,840 | 356 | 155,945 | 169 | 11.0221 | 47.4719 | 8.0475 |

在本工作点，距离较远的组具有更高的组内正常误报率；35–50 米组只有 356 个异常点，不能忽略其有限样本范围。2.5–10 米组包含 75.5974% 的异常点，贡献 88.3368% 的漏检。距离组的场景与序列构成不同，不能把这些差异解释为距离的单独因果作用，也没有计算类别与距离的联合贡献。

**正常标签编码诊断。** 本轮保留原评价入口输出的编码分组，不通过语义编码过滤模型分数。下表列出贡献较多的六组，全部编码在 `real.json` 的 `diagnostics.groups` 中。

| 正常原始编码 | 正常点数 | FP | 组内误报率（%） | 误报贡献（%） |
| --- | ---: | ---: | ---: | ---: |
| `40` | 188,179,854 | 1,891,642 | 1.005231 | 97.6179 |
| `50` | 3,953,975 | 25,606 | 0.647601 | 1.3214 |
| `48` | 259,333 | 11,512 | 4.439080 | 0.5941 |
| `70` | 905,135 | 6,768 | 0.747734 | 0.3493 |
| `10` | 296,404 | 1,371 | 0.462544 | 0.0708 |
| `30` | 13,403 | 893 | 6.662687 | 0.0461 |

编码 `40` 覆盖 **97.1038%** 的有效正常点，贡献 **97.6179%** 的误报，组内误报率接近全局误报率。因此其占据绝大多数误报首先对应极大的正常分母；本轮没有核对真实局部点云及这些编码的实际几何覆盖，不能仅凭编号就认定误报全部来自道路，也不能据此确认真实任务的主要结构与合成验证相同。部分编码的组内误报率较高，但规模很小，例如 `32` 仅有 5 个正常点，其中 2 个误报；不能由其 40% 比例指定研究方向。`72` 在本范围仅有 15 个正常点，也不足以判断真实地形类的检测能力。

上述真实分组使用 **1% FPR** 工作点，不能与上一轮重点展示的合成 **0.1% FPR** 类别贡献直接对照。本轮没有增加新的工作点、联合统计或局部分析，也没有把原合成 AP 13.90% 作为真实成绩或模型改进。

**实际核对与停止状态。** 完成后核对全部文件与 `real.json` 的序列／帧集合完全一致；1,960 个合格帧的逐帧正常与异常计数之和对应最终指标。序列及距离分组的正常数、异常数、TP 和 FP 分别精确合计到全局计数，正常编码分组的正常数和 FP 也精确合计。没有绑定、身份、非有限输出或资源保护错误，没有跳过缺失帧来完成评价。训练记录及检查点保持不变，这次没有产生新的参数更新。

执行前资源实测为 24 个可用 CPU 核、约 20.73 GB 可用内存、RTX 5080 Laptop GPU；按原配置使用 4 个加载进程、2 个 PyTorch 线程及数值库单线程。宿主 E 盘起始剩余 72,555,302,912 字节，剩余预测、评价排序及小文件的保守新增峰值预算为 15,811,663,240 字节，保留固定 10 GB 余量。运行中所记录最低 E 盘余量为 67,469,905,920 字节；评价临时文件释放后重新查询为 68,890,923,008 字节。推理约十分钟，评价约八分钟。大体积预测及检查点仍按现有忽略规则保留本地，本轮只提交报告。

第一轮现在具有完整训练记录、合成检测与误报分析、真实 val19 预测与官方指标。已确认真实检测也存在明显的精确率／召回不足，并定位到 `125` 对漏检的大量贡献；尚未确认其具体物体、背景及机制原因，也没有建立真实误报与合成三类结构的一一对应。下一项能改变修改优先级的证据，是理解这些具体真实漏检和高分正常点，而非直接增加负例权重或指定一个辅助模块有错。**本次评价任务完成后停止，第二轮训练未启动，模型、损失和数据保持原样。**

## 第一轮真实排序与局部失败归因

**2026 年 9 月 10 日，已直接读取本地完整真实预测，完成按序列、帧的 AP 失分和所需误报率归因，并检查六个预先按统计规则固定的真实局部案例。`125` 的主要漏检已经发生在同帧正常与异常的排序中；它贡献全局 AP 失分的 30.94%，同时占高召回阈值下残余漏检的 96.23%。其余序列仍贡献 69.06% 的 AP 失分，以及该高召回工作点 81.41% 的正常误报。** 因而应同时处理可见异常被排低、正常结构被排高的问题，不能把后续工作缩成调整 `125` 的阈值。

本次从 `8c0b002` 的现有产物出发，检查点仍是 `epoch1.pt`，预测分数仍为原始单精度数值。`src/diagnose.py --real` 复用 `FramePrediction`、`official_frame()`、`evaluate_frames()` 和 `APAttribution`；没有运行模型、训练、渲染器或辅助目标计算，没有改写 `real.json`、配置、检查点及预测，也没有恢复第二轮。执行命令为：

```bash
PYTHONDONTWRITEBYTECODE=1 OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  .venv/bin/python -m src.diagnose --real --jobs 6
```

结果单独保存在 [真实归因目录](../results/v1/attribution/)：

| 产物 | 内容与范围 |
| --- | --- |
| [sequences.csv](../results/v1/attribution/sequences.csv) | 19 条序列的 AP 失分、异常所需全局／同帧 FPR 分布、两个固定工作点的计数 |
| [frames.csv](../results/v1/attribution/frames.csv) | 8,659 帧的清单及归因；仅 `eligible=True` 的 1,960 帧进入官方统计，其他行保留过滤后点数、失分为零、所需 FPR 留空 |
| [joint.csv](../results/v1/attribution/joint.csv) | 19 条序列×4 个距离段，共 76 组；分别记录正常分母、FP、组内误报率、对全局 FP 的贡献及提高召回新增的 FP |
| [anomalies.npz](../results/v1/attribution/anomalies.npz) | 87,499 个异常点的序列、帧、原始槽位、原分数、完整同分精确率、全局／同帧所需 FPR、传感器距离 |
| [metrics.json](../results/v1/attribution/metrics.json)、[summary.json](../results/v1/attribution/summary.json) | 原指标复算及汇总定义；原 `real.json` 保留 |
| [cases.json](../results/v1/attribution/cases.json)、`case_1` 至 `case_6` 的 PNG／PDF | 固定选择规则、六例身份、几何核对数值、真实标注／原始分数／固定阈值图 |

**评价范围和加总关系。** 实际重新读取全部 8,659 份已存预测及相应源帧，核对完整原始回波槽位。距离 2.5–50 米、过滤后至少 5 个异常点的条件、忽略标签规则和完整同分处理全部沿用原评价器。得到的逐帧资格和正常／异常点数与 `real.json` 逐项相同；AP 6.53210197635271%、AUROC 97.21071856103805%、FPR95 17.214532122945748% 及两个工作点的阈值和计数也完全复现。

对每个异常点，以全局不低于其分数的完整同分组计算精确率和所需正常误报率。各序列、帧的失分按用户指定的 `100 / 87,499 × Σ(1 − 全局同分精确率)` 汇总，单位为百分点。实际总和为 **93.46789802364728**，与 `100 − AP = 93.46789802364729` 的差仅为双精度加总误差。下表的“失分”不是该序列单独评价的 AP，也不是对应物体造成错误的因果份额。所需 FPR 的分布在导出的逐点文件中为 0–1 比例，表中统一乘以 100；分位数仅描述分布，不生成新的正式阈值。

| 序列 | 异常点数 | AP 失分（百分点） | 全部失分占比（%） | 所需全局 FPR 中位数（%） | 所需全局 FPR 第 95 百分位（%） | 高召回阈值下仍漏检 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 125 | 25,871 | 28.919856 | 30.94 | 3.7501 | 25.8218 | 4,209 |
| 137 | 749 | 0.817568 | 0.87 | 0.9017 | 6.5098 | 3 |
| 138 | 2,254 | 2.293612 | 2.45 | 0.0750 | 0.7226 | 0 |
| 139 | 2,844 | 2.952058 | 3.16 | 0.1569 | 1.1214 | 0 |
| 140 | 1,465 | 1.523459 | 1.63 | 0.1859 | 1.0077 | 0 |
| 141 | 18,389 | 19.360646 | 20.71 | 0.2450 | 2.0393 | 41 |
| 142 | 1,596 | 1.770273 | 1.89 | 2.2850 | 11.0609 | 20 |
| 143 | 3,085 | 3.080130 | 3.30 | 0.0402 | 0.5919 | 1 |
| 144 | 4,508 | 4.942536 | 5.29 | 0.8786 | 6.0478 | 2 |
| 145 | 3,712 | 3.850066 | 4.12 | 0.1555 | 1.5703 | 1 |
| 146 | 1,455 | 1.494170 | 1.60 | 0.1028 | 1.2482 | 0 |
| 147 | 2,811 | 2.845516 | 3.04 | 0.0577 | 0.8112 | 0 |
| 148 | 3,050 | 3.120593 | 3.34 | 0.0864 | 1.2078 | 0 |
| 149 | 2,800 | 2.985325 | 3.19 | 0.4064 | 2.7068 | 1 |
| 150 | 5,344 | 5.459328 | 5.84 | 0.0636 | 1.3274 | 16 |
| 151 | 1,054 | 1.055891 | 1.13 | 0.0404 | 0.6991 | 0 |
| 152 | 1,029 | 1.113583 | 1.19 | 0.6219 | 6.3868 | 1 |
| 153 | 3,149 | 3.301911 | 3.53 | 0.2492 | 2.1827 | 0 |
| 169 | 2,334 | 2.581378 | 2.76 | 1.0743 | 9.9784 | 79 |
| 全部 | 87,499 | 93.467898 | 100.00 | 0.4542 | 17.2140 | 4,374 |

`125` 与 `141` 分别是 AP 失分最多的两条序列，但点数也影响归属：`125` 占异常点的 29.57%，占失分的 30.94%，不能沿用它在 1% FPR 工作点占漏检 66.27% 的数字。其他序列合计失分仍为 **64.548042 个百分点**。全局单帧最大失分来自 `141/000379`，而非 `125`；前十个失分帧合计仅 3.914802 个百分点，也不能把全局 AP 问题归结为两三个坏帧。

| 序列／帧 | 异常点数 | AP 失分（百分点） | 所需全局／同帧 FPR 中位数（%） | 原 1% FPR 阈值下 TP | 高召回阈值下仍漏检 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 141/000379 | 374 | 0.417820 | 1.9705／1.4780 | 121 | 25 |
| 141/000461 | 406 | 0.415888 | 0.1028／0.0264 | 405 | 0 |
| 141/000460 | 380 | 0.402395 | 0.3380／0.1641 | 323 | 0 |
| 141/000378 | 361 | 0.396726 | 0.7518／0.6808 | 226 | 0 |
| 125/000129 | 342 | 0.388830 | 11.4034／25.6872 | 0 | 124 |
| 125/000139 | 341 | 0.386142 | 6.0341／16.2409 | 10 | 41 |
| 125/000131 | 335 | 0.378529 | 6.0632／15.5393 | 30 | 61 |
| 125/000134 | 332 | 0.377970 | 18.6025／33.0740 | 0 | 180 |
| 141/000462 | 378 | 0.376715 | 0.0467／0.0038 | 378 | 0 |
| 125/000132 | 330 | 0.373788 | 9.1314／21.6857 | 8 | 78 |

例如 `141/000461` 检出 405/406 点，仍有较大 AP 失分。这是因为失分同时取决于异常数量及其在全局正常点中的排序；在 1% FPR 阈值下召回高，不能替代高精确率。

**高召回代价对应哪些异常。** 原全局阈值 `t1 = 1.0310213565826416` 检出 56,307 点、误报 1,937,803 点；原官方高召回阈值 `t95 = −7.412899494171143` 检出 83,125 点、误报 33,360,467 点。两者之间新增 **26,818 个 TP、31,422,664 个 FP**。这里保留官方 ROC 节点删减及严格超过 95% 召回的约定，没有把异常分数的普通分位数替代为 FPR95。

用“所需全局 FPR 大于 10%”描述这一低分尾部时，共有 7,793 个异常点，其中 **7,306 个来自 `125`（93.75%）**。其中已经在 `t95` 检出的 3,419 点，仍有 3,097 点来自 `125`（90.58%）；`t95` 以下剩余的 4,374 点中，4,209 点来自 `125`（96.23%），分布在其 97 帧，而不是单个异常帧。次多的残余漏检为 `169` 的 79 点、`141` 的 41 点及 `142` 的 20 点。

高召回阈值处唯一的异常点是 **`125/000135` 原始槽位 `130498`**，距离 4.649981 米，分数恰为 `t95`，所需全局 FPR 为 17.214532%，所需同帧 FPR 为 31.640333%。这定位了阈值附近的实际观测，不表示一个点是全部误报的因果来源。该尾部中漏检最多的帧为 `125/000134`（180 点），随后是 `125/000125`（135 点）、`125/000136`（134 点）、`125/000129` 和 `125/000152`（各 124 点）。所有帧的对应计数均在 `frames.csv` 中。

**正常误报的序列×距离联合结果。** 下列每行分别以本组正常点数为误报率分母，以同一工作点全局 FP 为贡献分母。完整 76 组在 `joint.csv`；所有组的正常数与两种 FP 分别精确合计到全局，没有用序列、距离的边际比例相乘。

| 序列／距离（米） | 正常点数 | `t1` 下 FP | 组内 FPR（%） | 全局 FP 贡献（%） | `t95` 下 FP | 组内 FPR（%） | 全局 FP 贡献（%） |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 125／[2.5,10) | 13,253,611 | 301,485 | 2.2747 | 15.56 | 4,482,557 | 33.8214 | 13.44 |
| 125／[10,20) | 6,721,245 | 135,069 | 2.0096 | 6.97 | 1,541,859 | 22.9401 | 4.62 |
| 141／[10,20) | 3,922,743 | 119,878 | 3.0560 | 6.19 | 1,002,555 | 25.5575 | 3.01 |
| 169／[10,20) | 2,295,946 | 79,755 | 3.4737 | 4.12 | 642,694 | 27.9926 | 1.93 |
| 141／[2.5,10) | 26,495,309 | 57,994 | 0.2189 | 2.99 | 3,703,048 | 13.9762 | 11.10 |
| 169／[2.5,10) | 3,231,088 | 53,733 | 1.6630 | 2.77 | 1,506,158 | 46.6146 | 4.51 |

`125` 的近距离组确实是最大单组误报来源，但在 `t95` 下，其他序列仍产生 **27,157,673 个 FP，占 81.41%**。从 `t1` 降到 `t95` 新接纳的正常点中，2.5–10 米段增加 23,732,428 个，占新增 FP 的 75.53%。这是当前全局排序为提高召回付出的代价，不能单凭远距离组内误报率较高就指定 C2 为主要修改对象。

**`125` 的同帧比较。** 同帧所需 FPR 使用该完整合格帧的全部官方有效正常点，按“不低于该异常分数”的数量计算；不在局部裁剪里重算，不排除同分正常点，不改变任何分数。`125` 的所需全局 FPR 中位数为 **3.750052%**，同帧中位数反而为 **8.733057%**；75.35% 的异常点具有更高的同帧所需 FPR。

| 同一个异常点的比较 | 异常点数 | 占 `125` 全部异常点（%） |
| --- | ---: | ---: |
| 全局与同帧所需 FPR 均大于 1% | 19,795 | 76.51 |
| 全局大于 1%，同帧不超过 1% | 876 | 3.39 |
| 全局不超过 1%，同帧大于 1% | 1,241 | 4.80 |
| 全局与同帧均不超过 1% | 3,959 | 15.30 |

原阈值漏掉的 20,671 个 `125` 异常点中，**19,795 个（95.76%）在同帧内也不能以不超过 1% 的正常误报率检出**。只有 876 个属于“同帧较好而全局超过 1%”的那一格。这否定了“主要是同帧已经分清、只有跨帧分数位置有问题”的解释；逐帧严格单调校准保留同帧排序，无法消除前一类问题。两种 FPR 的分母和背景组成不同，本比较仍不能认定 BatchNorm、某种校准方式或任何辅助分支是根因。

**六个固定真实案例。** 在首次统计开始前，已声明选择规则：`125` 取两个最大 AP 失分帧；其相对成功帧和其他序列成功帧先按原全局阈值的召回降序，再按异常点数降序、序列／帧号升序处理并列；两个正常簇来自全局 FP 最多的两个不同序列，各取跨帧最多独立误报位置的一米网格，网格并列按帧号和坐标排序。统计完成后，先保存六个身份，再画图查看；没有按外观或猜测更换案例。

图的三列分别显示真实异常标注、原始分数、`t1` 下的异常检出／漏检及正常误报，三行显示传感器坐标下的三个正交投影。横纵坐标仅减去显示中心，单位仍为米。原始分数统一使用 −15 至 15 的线性色标，同时标出 `t1` 和 `t95`；六例实际均没有分数超出该显示范围。异常案例显示全部官方异常回波的包围范围及每轴两米余量；正常案例显示锚点周围六米，并增加同一簇的局部放大。裁剪不进入任何排序统计。为使原始颜色清楚可读，移除了密集黑色描边；这次显示调整前后，六个案例身份和已有数值逐项相同。

| 案例与图片 | 原阈值 TP／异常点数 | 所需全局／同帧 FPR 中位数（%） | 实际局部核对 |
| --- | ---: | ---: | --- |
| [1：125/000129](../results/v1/attribution/case_1.png) | 0／342 | 11.4034／25.6872 | 342 个独立异常位置，距离 4.67–5.59 米，呈贴近连续表面的多分支凸起轮廓；全部漏检，覆盖主体而非仅边缘 |
| [2：125/000139](../results/v1/attribution/case_2.png) | 10／341 | 6.0341／16.2409 | 与前例相近的可见分叉轮廓，341 个独立位置；仅末端少量点检出，大部分主体仍低分 |
| [3：125/000166](../results/v1/attribution/case_3.png) | 24／24 | 0.2793／0.4861 | 仅一小簇紧凑回波，相对周围正常回波较隔离；属于相对成功观测，不能视为完整物体识别成功 |
| [4：141/000462](../results/v1/attribution/case_4.png) | 378／378 | 0.0467／0.0038 | 378 个独立位置形成较连贯的块状／片状凸起，原分数明显高于局部连续背景；仍有少量邻近正常误报 |
| [5：125/000111](../results/v1/attribution/case_5.png) | 5／213 | 9.7092／26.1375 | 主要误报在连续、缓变的近水平正常表面上形成宽条带；远离真实异常的所选一米网格也全部误报 |
| [6：141/000319](../results/v1/attribution/case_6.png) | 28／29；显示区内无异常 | 0.1832／0.4827 | 竖直结构旁一块紧凑片状正常回波被判高分，距最近真实异常约 16.90 米；不能解释为异常邻域边界混合 |

案例 1 的异常三轴范围约为 **0.794×0.912×0.632 米**，异常分数中位数为 **−6.6822**，附近正常点中位数为 **−6.7528**；正常分布还包含超过异常的高分点。案例 2 的范围约为 **0.783×0.882×0.647 米**，仅 10 点检出，剩余 331 点覆盖高度方向的整个可见范围。这两例已有成百独立回波和主体、分支轮廓，不能把失败概括为仅有一至四个点或只漏边缘。单帧回波仍不能证明整个物体无遮挡，也不足以单独证明所有辨别信息都充分。

案例 3 虽然 24 点全检出，但可见范围只有 **0.431×0.167×0.209 米**，每个异常点到最近官方有效正常回波的距离至少 1.042 米，中位数 1.384 米；前两例相应中位数约为 0.244、0.320 米。其成功对应不同的可见片段和背景关系，不能由点数更少反推“稀疏越容易”，也不能用这一个观测宣布 `125` 的主体已经学会。案例 4 与前两例距离、点数处于相近数量级，但表现不同：原分数中位数 5.6044，全部检出，局部 1,369 个有效正常点仍有 39 个误报。它支持模型能识别一部分真实凸起，但不能把差异因果归结为某一种轮廓或接触关系。

案例 5 选择的一米网格位于传感器坐标 `[3,4)×[−4,−3)×[−2,−1)`，其中 586 个实际回波均为有效正常点，且全部被误报。其误报范围约 **0.998×0.999×0.276 米**，分数中位数 **5.9502**，锚点距最近异常回波 4.292 米；六米显示区共 24,694 个有效正常点、3,311 个 FP。几何图显示这片高分主要沿连续表面展开，不能只用异常轮廓附近的混合解释。它们的原始编码虽均为 `40`，本次“连续近水平表面”的判断来自实际几何；没有把所有 `40` 自动认定为道路或用于估计地面真值。

案例 6 选择的网格中有 769 个回波，其中 86 点在官方距离范围外，不计 FP；683 个有效正常点中 424 点被误报。误报位置范围约 **0.268×0.494×0.298 米**，分数中位数 **4.8464**，424 点的原始编码均为 `50`。整格距离为 2.412–2.829 米，处于官方下限附近，因此图中结构的一部分正常回波不参与评价。所选紧凑片状结构的具体物体类别仍未知；没有从编码直接将其解释成建筑、车辆或某种设施。六米范围完全不含真实异常，所检 FP 与最近异常相距很远。

这些案例是预定的极端／相对成功样本，不是随机抽样，不能外推两种正常结构占全部误报的比例。`125` 的三张观测虽在同一序列、姿态变换后的邻近区域，本轮没有建立跨帧物体或表面点追踪，所列范围只描述每帧实际观测，不声称完整物体尺寸或证明遮挡原因。

**执行核对与证据边界。** 除全局精确复算外，序列与帧的 AP 失分之和、两种工作点的 TP／FP、序列×距离的正常数／FP 均核对到同一总体。38 张真实源帧的单线程与六线程读取，其坐标、标签、预测槽位、原始分数、评价资格和最终指标完全相同；六线程用时 1.58 秒，单线程 1.83 秒。这是读取等价性检查，不是新模型实验。完整诊断命令约 138.85 秒，其中全局复算约 101.52 秒；局部显示改进仅重新画同六例，没有重算全局或进行前向。相关评价／诊断测试共 14 项通过。六份 PDF 经字体检查仅包含嵌入的 SimSun 和 Times New Roman，最终图片逐一查看，没有字体回退。

执行前实测为 24 个可用 CPU 核、约 20.69 GB 可用内存；本次读取及排序使用 CPU，没有调用 GPU 模型或占用训练资源。宿主 E 盘开始剩余 68,892,364,800 字节；临时排序及输出新增峰值预算为 1,651,039,752 字节。运行所记录最低剩余为 68,629,630,976 字节，完整命令结束后为 68,885,417,984 字节，始终超过固定 10 GB 余量。临时排序文件已释放，归因表、逐点数据及六图共约十 MB，原始科学结果保留。

**归因证据的含义。** 目前的直接证据支持“可见异常与正常结构的输出分数在同一帧内排序不足”。它没有独立检验潜在特征表示，也尚未区分训练不充分、合成到真实的几何／背景迁移不足、共享表示及辅助监督竞争等原因。六个案例不能确定正常负例的总体覆盖。此前提出的三项辅助损失同时关闭对照没有执行，保留为后续候选问题；当前优先检验真实几何证据，不直接续训 V1。

## 当前真实几何分析

本次分析回答四个问题：几何能否区分标签；条件化是否比原始几何更有用；这些量在哪些条件下失效；类似条件下哪些正常结构容易与异常混淆。本轮几何与条件特征只由当前扫描全部实际回波的坐标计算，强度未进入这些探针。标签用于原帧的官方评价资格判断，并在无标签特征计算之后用于正常拟合样本的资格判断和最终评价、描述。依赖真实异常标签计算的异常内部邻域、距异常边界或距已知正常点距离，不得充当待评价分数的输入。

正常参考仅从 `train/206` 拟合。条件分箱的规则、双尾评分规则、特征集合与组合方式先于本次验证评分确定；需要估计的尺度分箱边界和经验分布只用 `train/206` 正常点计算。`train/201` 用于检查固定参考在另一正常来源上的误报和覆盖，不重新拟合参考。val19 标签仅用于官方资格判断、固定分数的评价及条件分布描述，不能用于选择最有利的分箱、尾方向或组合，也不能用于拟合生成器的异常尺寸、距离和数量比例。

几何计算以同一当前扫描中的不同坐标位置建立局部邻域，保留原始回波槽位身份。候选证据包括局部表面残差、表面厚度、法向变化和协方差形态，并单列按局部采样尺度归一化的量。法向、平面和尺度均从当前无标签点云估计；局部表面残差描述可见邻域，不能解释为被遮挡正常表面的真实位移。局部采样尺度同时受观测和物体几何影响，是观测条件的代理量；加入它的条件比较不能直接证明纯传感器因素的因果作用。

具体邻域为两米内最多 32 个不同坐标的最近邻，排除查询位置自身，至少需要 8 个位置。邻域中位距离记为采样尺度。协方差按邻点数归一化，升序特征值记为 $\lambda_0,\lambda_1,\lambda_2$：表面厚度为 $\sqrt{\lambda_0}$，最小方差占比为 $\lambda_0/\sum_i\lambda_i$，线性度为 $(\lambda_2-\lambda_1)/\lambda_2$，平面度为 $(\lambda_1-\lambda_0)/\lambda_2$。查询点至邻域中心平面的绝对距离为局部表面残差。法向另要求 $\lambda_1>10^{-12}$ 且 $\lambda_1-\lambda_0\geq0.05\lambda_2$；法向变化为与至少 8 个有效邻点法向之间的不定向夹角均值，单位弧度。残差与厚度除以采样尺度的版本单列。没有可靠法向时，残差和法向变化缺失，仍可靠的协方差形态不随之清空。

距离采用固定边界 2.5、5、10、20、35、50 米。射线条件仅采用传感器坐标的竖直方向分量 `ray_z=z/r`，边界为 −1、−0.3、−0.15、−0.075、−0.025、0.025、0.1、0.3、1；它不是方位角或表面入射角。由于 $z=r\,\mathrm{ray}_z$，距离与竖直射线分量合用也包含传感器坐标下的高度信息，可能同时区分不同场景结构，不能将全部收益归因于消除了采样影响。采样尺度采用 `train/206` 正常点的五等分分位数，参考取值范围外的尺度保持无参考。条件箱内每个所用几何量均须有至少 256 个有效正常点、来自至少 20 帧；这只是样本支持要求，相邻 20 帧不代表 20 个独立场景，也不保证参考足以代表所有正常结构。

最小对照使用三类固定规则，分数越高表示在正常参考下越少见。双尾经验稀有度同时检查偏小与偏大两端，包含全部同分参考点，不按真实异常标签选择方向。正常参考取值范围之外的几何量取最高稀有度。这是经验排序分数，不是经过校准的异常概率或显著性检验概率。多特征组合统一取各特征稀有度的最大值。

| 对照 | 输入与正常参考 | 回答的问题 |
| --- | --- | --- |
| A：仅观测条件 | 距离、射线竖直方向等无标签条件；加入局部采样尺度的版本另列 | 仅凭观测条件的少见程度，能够产生多少异常区分 |
| B：无条件几何 | 同一组几何量在 `train/206` 正常点上的无条件双尾经验稀有度 | 几何本身相对于全部正常参考是否少见 |
| C：条件几何 | 同一组几何量在 `train/206` 正常条件分箱内的双尾经验稀有度；距离、距离加射线方向及加入采样尺度代理的条件版本分别报告 | 在可比条件的正常参考下，几何稀有度是否提供额外区分 |

B 与 C 使用完全相同的几何特征集合和组合规则。原始量与归一化量分别比较，不能把条件化与更换特征的效果合并。A 同时给出与 C 对应的条件集合，含射线竖直方向的结果作为补充。条件分箱须达到预先规定的正常样本数与跨帧支持数；正常参考不足或几何不可计算的点不填零分，不将缺测本身当作异常证据。

主 A/B/C 评价限制在相同有效覆盖交集，沿用官方距离、标签和原始帧资格规则，不因某方法覆盖不足重新筛选合格帧。分别报告正常与异常覆盖以及远距、少回波等条件下的缺测。交集内的 AP、FPR95 和固定误报率召回仅描述该交集；没有覆盖全部官方点时，不能报告为完整 STU 指标，也不能直接与 V1 的完整 val19 指标比较。正常迁移误报、单特征结果和条件差异共同用于解释组合分数，不能仅由一个总 AP 判断科学命题。

总体组合要求六项几何同时有效，可能排除稀疏或法向不可靠的结构；单量对照采用该量自己的共同有效点集。`returns` 分组指原合格帧内全部官方异常标签点数，是依赖真值的事后描述，不是推理条件，也不是单个物体的完整点数。所有相邻帧保持所属序列，逐一排除序列时直接从总体精确分数计数中减去该序列，不重新拟合、不把点随机拆开。

阈值有两种用途：排序诊断的“误报不超过 1% 时召回率”在各评价点集上读取固定分数的整条曲线；正常迁移与分组误报使用仅由 `train/206` 确定的固定阈值。前者是开发集排序能力的描述，不能当作由正常训练得到的可部署工作点。后者在 `train/206` 上达到的误报比例属于拟合样本内结果，需要由 `train/201` 检查迁移。

这些是开发分析。条件比较的正结果支持给定特征、正常来源与覆盖条件下存在可利用关联。单变量双尾分数只检测极端取值，可能漏掉正常分布中部的低密度区，以及只体现在多变量关系中的异常；负结果只能否定本次可计算特征与比较方案，不能单独否定所有可能的局部几何证据。现有 V1 权重、预测、40 个世界和监督缓存保持其原有身份。合成世界的补充、监督选择和模型设计，应在这次真实规律分析之后依据具体证据决定。

A 仅使用各条件边际稀有度的最大值，未建立条件的联合分布；C 超过 A 只能说明超过此明确基线。距离与射线方向也关联场景内容，C 超过 B 表明本次正常参考变换改善了区分，不能据此识别纯采样因素的因果作用。

**实际输入与复算入口。** 几何入口为 [src/geometry.py](../src/geometry.py)，无标签几何与正常参考的唯一实现分别位于 [src/profile.py](../src/profile.py) 的 `observed_geometry` 和 [src/probes.py](../src/probes.py)。完整运行读取 `train/206` 的 449 帧、`train/201` 的 682 帧，以及 val19 的全部 8,659 帧。正常侧每帧先在距离范围内均匀抽取至多 8,192 个真实槽位，再按有效正常标签筛选；固定随机种子为 `20260911`，每帧具有独立随机流。最终正常参考样本为 3,544,145 个点出现次数，正常迁移样本为 3,736,364 个。邻域始终来自整帧，与抽样和标签筛选分开。

val19 中 1,960 个原帧符合官方资格，保留全部 193,792,470 个正常点与 87,499 个异常点。特征缓存按原始槽位保存全部这些点，标签分组发生在无标签计算之后。距离条件有 5 个参考箱；距离与竖直射线分量有 37 个实际出现的参考箱，全部满足支持要求；加入邻距尺度后有 138 个实际出现的箱，其中 115 个满足全部六项几何的支持要求。尺度边界为 0.030862、0.095147、0.150313、0.233404、0.420635、1.968716 米，实际计算保留未取整值。

描述性条件分布保留全部官方异常点，每帧均匀抽取至多 2,048 个正常点，并按抽中概率倒数加权。其分位数、分布差异和固定阈值分组中的正常误报为抽样描述；同条件箱的比较不等于严格逐点匹配。主要排序指标、逐序列结果、逐一排除序列结果及主对照覆盖率则来自全部共同有效点的精确单精度分数计数，没有直方图量化。

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  .venv/bin/python -m src.geometry --stage all --workers 12
```

初次运行使用上面的入口。已有完整特征时用 `--stage analyze --workers 6` 复算固定参考和分析，复用已经完整完成的序列精确计数。输出位于 `results/geometry/`：`configuration.json` 与 `extraction.json` 记录输入和执行，`reference.json` 记录正常参考支持，`thresholds.json` 记录训练阈值，`tables/` 保存指标、覆盖、正常迁移、逐帧和误报表，`matched.json` 保存描述性条件分布，`plots.pdf` 和 `cases.pdf` 保存统计图与预定真实案例。模型、预测、世界和既有 V1 结果均未改变。

**真实主对照结果。** 下表所有数值均为百分数，AP 为平均精确率，AUROC 衡量整体排序，末列为该共同点集的评价曲线上正常误报不超过 1% 时的最高召回率。这些是公开开发数据上的探针结果，均不代表完整 STU 方法成绩。

| 条件参考 | A 的 AP | B 的 AP | C 的 AP | C 的 AUROC | C 的低误报召回 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 距离 | 0.031549 | 0.040116 | 0.047157 | 53.523050 | 0.600326 |
| 距离与竖直射线分量 | 0.038803 | 0.040116 | 0.074426 | 66.089737 | 1.425052 |
| 再加入邻距尺度 | 0.036207 | 0.039986 | 0.072629 | 63.568025 | 1.981391 |

距离与射线方向组的 B 为 AUROC 47.012161%、低误报召回 0.483500%；C 的 AP 增益为 **0.034310 个百分点**，仍只有 0.074426% 的绝对 AP。该共同子集异常占比约为 0.044865%，也是常数分数的 AP；B 的六量最大值分数低于这个水平。因此，本轮支持部分可计算几何与标签有关，以及当前条件参考具有弱增益，不能宣称原始几何组合已能有效检测异常，更不能将相对提升幅度写成实际检测能力。

主距离与射线方向组的 C 相对 B，在 **19/19** 条序列上 AP 和 AUROC 均提高，低误报召回在 17 条提高、2 条下降。逐一排除任意序列后，AP 增益仍为 **0.020960–0.039267 个百分点**；移除 `125` 后 B/C 的 AP 为 0.029802%/0.050762%。加入邻距尺度的主组在 19/19 条序列上 AP、AUROC、低误报召回均高于同组 B；逐一排除序列后的 AP 增益为 0.020573–0.038852 个百分点。两组的 C 也在全部 19 条序列上超过各自 A 的 AP。`125` 放大了总体增益，但不是唯一来源。序列是本次相关观测的分组单位，并不保证 19 个独立背景或场地。

仅按距离条件化的增益较弱：AP 在 18/19 条序列上增加，但低误报召回仅 5 条增加、7 条下降、7 条相同。这说明“整体排序略有改善”与“低误报下更有用”需要分别判断。

**哪些几何量提供了证据。** 下面比较各单量在无条件正常参考 B 和距离与射线方向正常参考 C 下的表现。每行内部使用完全相同的有效点，不同几何量之间仍须检查各自覆盖。

| 单个几何量 | B 的 AP（%） | C 的 AP（%） | C 的 AUROC（%） | C 的低误报召回（%） |
| --- | ---: | ---: | ---: | ---: |
| 局部表面残差 | 0.043695 | 0.080188 | 66.214089 | 2.170419 |
| 表面厚度 | 0.033711 | 0.074415 | 68.464782 | 0.532024 |
| 法向变化 | 0.058429 | 0.116620 | 73.199763 | 4.177993 |
| 最小方差占比 | 0.051754 | 0.093542 | 70.471152 | 2.200178 |
| 线性度 | 0.044801 | 0.061810 | 58.567217 | 1.757397 |
| 平面度 | 0.042507 | 0.084171 | 64.382500 | 2.874076 |

法向变化的该行与主六量组合恰好具有相同的 192,608,108 个正常点和 86,453 个异常点。单量 C 的 AP 为 0.116620%、低误报召回 4.177993%，六量最大值组合却分别降至 0.074426% 和 1.425052%。这是当前融合规则的直接负面证据，不支持继续把“增加几何量”自动当作增强。单量结果用于开发诊断，本轮没有据此重新选择正式评分特征或调整方向。

将残差和厚度除以邻距尺度、其余量不变的无条件组合，在同一共同点集上把 AP 从 0.040116% 提高至 0.050583%，AUROC 从 47.012161% 提高至 54.513644%，低误报召回从 0.483500% 提高至 1.108117%。这只是该归一化变换的结果，与条件参考的效果分开解释。最小方差占比仅按距离条件化时，总体 AP 只增加约 0.000061 个百分点，17/19 条序列下降，去掉 `125` 后增益为负；不能概括为所有几何都受益于距离条件化。

描述性分布也显示可计算差异。例如，按异常点数最多而非分布差异大小选择的距离与射线方向箱为距离 [5,10) 米、竖直射线分量 [−0.3,−0.15)。其中保留 38,346 个异常点和 533,421 个抽样正常点，实际进入可靠法向变化分布的有效样本为 38,085 个异常点和 532,719 个抽样正常点。正常加权中位数为 0.036018 弧度，异常中位数为 0.337631 弧度。正方向分布排序量为 0.876037，即在本箱内随机抽取一个有效异常和一个按权重代表的正常点，异常法向变化较大的概率约为 87.60%，同值计半。这个量在看到真值分组后描述分布，不是正常训练分数的 AUROC，也没有用于改成单尾方法。

**覆盖与正常迁移。** 距离组、距离与射线方向组以及无条件归一化组，在 val19 上具有相同的共同覆盖：正常 **99.388850%**，异常 **98.804558%**。因此这些组中没有额外因条件参考不足丢失已具有全部六项可靠几何的点；未覆盖的 1,184,362 个正常点和 1,046 个异常点来自组合几何可靠性不足。加入邻距尺度后，覆盖降为正常 **99.345479%**、异常 **98.633127%**，在前者基础上另失去 84,050 个正常点和 150 个异常点。这些点保持无参考，不算检测出的异常。

| 距离（米） | 全部官方异常点 | 距离与射线方向组覆盖异常 | 加入邻距尺度后覆盖异常 |
| --- | ---: | ---: | ---: |
| [2.5,10) | 66,147 | 65,470（98.9765%） | 65,343（98.7845%） |
| [10,20) | 16,191 | 16,056（99.1662%） | 16,056（99.1662%） |
| [20,35) | 4,805 | 4,686（97.5234%） | 4,686（97.5234%） |
| [35,50] | 356 | 241（67.6966%） | 218（61.2360%） |

35–50 米异常仅占全部官方异常的 0.4069%；总体覆盖不能掩盖这一段的明显缺测。在仅由 `train/206` 决定的固定阈值下，距离与射线方向 C 在四个距离组分别检出 1,104、182、3、0 个异常点，加入邻距尺度的 C 分别检出 1,532、532、4、0 个；各组实际正常误报不同，不能据这些召回计数声称误报匹配后的距离内增益。现有证据主要适用于有可靠邻域的近中距离可见回波，不支持远距、弱遮挡或完整物体高度的有效性主张。

`train/201` 的共同正常覆盖约为 98.13%–98.15%。沿用 `train/206` 的固定阈值，无条件 B 的正常误报约为 1.6613%；距离 C 为 1.3309%，距离与射线方向 C 为 **1.0769%**，加入邻距尺度 C 为 **0.9802%**。后两者比 B 更接近训练时约 1% 的工作点，支持正常侧迁移有所改善，但不能由单个正常来源保证泛化。在 val19 上这两个 C 的固定阈值实际误报分别为 1.0427% 和 1.1867%，召回为 1.4910% 和 2.3962%；这与前述评价曲线上的误报匹配指标属于不同用途。

**真实正常误报。** 固定规则在两种条件 C 下各选最多两帧，优先不同序列，再按全帧正常误报数降序处理；每帧选独立误报坐标最多的一米网格，显示其中心周围六米立方体，不按外观更换样本。

| 案例 | 评分 | 全帧正常误报 | 显示区正常误报 | 显示区官方异常 | 中心至最近官方异常（米） |
| --- | --- | ---: | ---: | ---: | ---: |
| `150/000340` | 距离与射线方向 C | 2,999 | 980 | 0 | 35.28 |
| `125/000025` | 加入邻距尺度 C | 2,623 | 1,581 | 0 | 22.58 |
| `141/000612` | 距离与射线方向 C | 2,978 | 998 | 0 | 34.98 |
| `139/000429` | 加入邻距尺度 C | 2,252 | 644 | 0 | 31.92 |

实际点云显示，所选主要误报位置包括连续竖直片状正常表面和其上的条带式回波；周围也存在其他起伏结构。它们远离真实异常，不能统一归为异常边界混合。`150` 与 `141` 的可见布局很相近，本轮未核对是否为同一背景实体；四个案例不代表四种独立结构，也不是总体误报构成的随机样本。

双尾计分会把过小的几何量也列为少见。以 `125/000025` 显示区为例，在法向变化达到六量最大值的 689 个正常误报点中，632 个来自下尾；最小方差占比相应为 525/543，表面厚度为 185/197。`139/000429` 相应为 74/118、202/214、170/173。多个字段可能同时达到最大值，数量不可相加作因果份额。局部表面残差在四例中却以上尾为主，不能统称“小残差造成误报”。这些实测值说明，当前参考会把某些法向一致、较薄且近似平滑的正常片段列为稀有，正常参考下的少见性与异常标签仍有差距。

总体正常误报中的原始编码 `40` 占比为 97.42%/97.49%，但该编码同时占相应共同正常背景的大多数；该比例不能单独识别物体类别、定义地面真值或说明其单位点风险特别高。结构解释来自实际点云，原始编码只作为可追踪的分组记录。

**邻距尺度的独立变化。** 主表的两种条件组覆盖略有差别。读完主表后，本轮补充了一次明确标记的开发对照：从距离与射线方向 C 的既有精确计数中，仅减去不属于采样尺度共同覆盖的 84,050 个正常点和 150 个异常点。排除身份由已固定的几何有效性和参考支持决定，只对这些排除点重算原分数；不重估正常参考、不改条件箱、不改变评分规则。两种 C 随后都使用 192,524,058 个正常点和 86,303 个异常点。

| 同点指标（%） | 距离与射线方向 C | 再加入邻距尺度 C |
| --- | ---: | ---: |
| AP | 0.074254 | 0.072629 |
| AUROC | 66.081581 | 63.568025 |
| FPR95 | 80.421689 | 88.130267 |
| 误报不超过 1% 时召回率 | 1.411307 | 1.981391 |

追加邻距尺度后，逐序列 AP 为 11 升、8 降，而全部 19 次逐序列排除后的总体 AP 都下降；低误报召回在 19 条序列和全部逐序列排除结果中均提高。FPR95 在所有逐序列与逐序列排除结果中均变差。这支持低误报工作点上的局部收益，同时否定“本次邻距尺度条件化全面改善排序与高召回代价”的表述。完整结果见 `tables/sampling_increment.csv`；其中采样尺度 C 的全部 39 个总体／序列／去序列结果与原主表逐项一致。

**执行与验证。** 真实特征提取耗时 393.23 秒，写出 12,674,890,530 字节。评分最初两次因内存估计不足而中断：先是八个进程，随后六个进程仍同时保留不断增长的多个对照计数，导致交换空间开始使用。修正只涉及计算实现：每八帧写入精确计数块，再逐个对照归并，释放临时块；特征、随机流、正常参考、评分、阈值与评价条件保持一致，完整完成的序列保留。最终恢复运行的评分、总体和逐序列排除汇总、描述性分布及四页统计图耗时 **1,416.32 秒**；该时间不包含先前中断的尝试和已经复用的序列计算，不能解释成从零开始的总耗时。补充同点对照用时 46.50 秒，峰值内存 4.12 GB；同四例的上下尾核对与再生成用时 10.80 秒。

运行前实测 24 个可用 CPU 核、约 22 GB 可用内存及 RTX 5080 Laptop 的 16 GB 显存。本轮邻域计算与精确计数采用 CPU：提取 12 个进程，最终评分 6 个进程，数值库每进程一条线程。真实先导的单线程与多线程几何数组逐元素相同；归档合并回归保持分数位模式、点数和正例数一致。正式汇总将每个共同点集的计数核对至覆盖表，再核对至完整官方输入分母；同点补充对照额外核查排除点身份。最终有关几何、参考、精确评价和既有归因的 37 项检查通过，当前协议可正常加载。

宿主 E 盘初始剩余 43,980,787,712 字节；运行期间所查询到的低值约为 26.84 GB，最终主分析结束后为 30,145,548,288 字节，均高于固定 10 GB 余量。评分进程最终峰值约为 1.40–1.56 GB；此前已换出的系统页面仍可能存在，不能将正常吞吐误报成系统交换空间使用为零。临时计数块已释放，保留当前有效特征、精确计数和分析证据。四页统计 PDF 与四页案例 PDF 均核对到实际嵌入的 SimSun、Times New Roman；最终图片已查看。统计图只调整坐标显示范围与小数精度，计算结果不变。

**本轮裁决与下一问题。** 可计算的法向变化、局部表面残差及部分形态量具有弱预测信息；距离与竖直射线分量的正常参考解释，在公开开发序列和正常迁移来源上提供了重复出现的改善。该结论超出了只依赖真值构造的诊断量，因为所有待评价几何与分数均可从当前帧及训练正常参考计算。

当前证据同时否定了六量最大值融合已能有效整合这些信息的判断，也不支持仅距离条件化对所有形态量普遍有益、加入邻距尺度全面改善、或远距困难条件已经解决。它没有否定所有局部几何，也不能独立证明传感器采样的因果机制、V1 内部表示的根因或最终网络收益。

真实开发分析到此收口。下一项工作限定为现有 40 个合成世界的针对性核查：检查是否有几何接近正常的异常、原始正常来源是否含平滑片状与高变化结构、插入是否改变保留正常回波的邻域，以及同一固定物体沿轨迹的几何支持与缺失。沿用本轮两项候选量和距离与竖直射线条件箱，不将与 val19 分布接近作为生成器目标，不扩充开发集手工探针，也不直接把两项几何量升级为网络模块。核查只需识别一个最值得检验的设计因素；不以补齐全部数据缺口作为进入最小受控训练的前提。V1 历史配置与结果保留，训练仍暂停。

## 现有合成数据的针对性核查

本次只检查既有数据能否提供学习几何区别所需的正反例。原始正常侧先读取此前按帧固定抽样的 `206/201` 缓存，每个源帧只出现一次；随后在当前 40 个世界中复用既有选择规则，检查 155 张代表扫描。另按清单中 35—50 米可见帧数最多、世界名破同分的规则，每侧预选 `candidate_011` 的完整轨迹，共 449 和 682 帧。两部分重合帧只计算一次，合计 1,276 个世界帧、1,131 个唯一原始帧。代表扫描与两条轨迹分别汇总；代表扫描比例不代表全部 22,620 个世界帧的发生比例，世界数量不代表独立正常环境数量。

两项几何仍从各自完整扫描全部实际回波计算，未读取标签建立邻域。参考仅重建原 `train/206` 拟合并核对其元数据一致，保留原距离与竖直射线分箱、双尾规则和单量阈值：法向变化为 0.9899884462356567，局部表面残差为 0.9899904727935791。另用同一条件参考的第 10 至第 90 百分位作描述性中间区间；区间外不等同于达到异常阈值，区间内也不保证所有模型都无法区分。没有新拟合分数、组合两量、改用上尾或读取新的 val19 结果。

**原始正常内容。** `206` 的 449 份缓存均非空，共 3,544,145 个正常点出现次数；`201` 的 682 份缓存中有 681 份非空，共 3,736,364 个，帧 681 的缓存为空。空缓存仅表示原固定抽样及标签筛选没有保留样本，不能据此认定原扫描为空。法向变化的可靠正常点分别为 3,496,293 和 3,667,130，局部残差为 3,498,423 和 3,670,674；`201` 其中另有 47 和 53 个可靠几何点没有条件参考。

在固定 `206` 条件参考下，`201` 法向变化下尾和上尾分别出现于 649、674 个源帧、各 36 个条件箱；残差两尾分别出现于 656、674 帧、各 37 个箱。这些计数只说明不同取值存在于多个观测条件，不能把同一背景跨帧出现解释成大量独立结构。`206` 自身约十分之一取值落在各尾，部分来自分位数定义，不能作为正常多样性充分的独立证据。

案例按两量同时较小、法向变化较大、残差较大的规则，每源固定三例后再查看原始点云，不因外观替换。六例从原始完整扫描精确复现两量、距离、射线竖直分量和邻距尺度；实际邻域也核对到相同槽位。下面仅描述可见几何，不按原始编码推定物体类别或完整尺寸。

| 原始来源／帧／槽位 | 距离（米） | 法向变化（弧度） | 残差（米） | 实际可见证据 |
| --- | ---: | ---: | ---: | --- |
| `206/270/22210` | 15.474 | 0.001665 | 0.00000259 | 连续、近水平的平滑片状回波，32 个支持位置 |
| `206/122/107690` | 7.350 | 1.528882 | 0.052993 | 起伏轮廓与近水平表面相邻，局部法向变化很大 |
| `206/406/46425` | 8.064 | 1.300240 | 1.087472 | 选定回波与空间上分散的邻域支持错开，不能统称表面粗糙 |
| `201/66/66878` | 32.852 | 0.00001633 | 0.000000281 | 稀疏、近乎共面的片段，仅 8 个支持位置，不能声称大片连续表面 |
| `201/302/49974` | 27.594 | 1.550578 | 0.058012 | 条带与片状回波相邻，19 个支持位置，法向变化很大 |
| `201/36/60616` | 6.651 | 0.179995 | 1.929650 | 32 个邻居集中在约 1.9 米外的另一片竖直表面，查询点与拟合表面不属于同一可见片段 |

六例均至少触发一个原固定单量探针。它们说明平滑正常与高变化正常在原始来源中存在，也暴露了估计解释的限制：邻点数量和法向特征值条件通过，不保证拟合表面与查询点属于同一物理表面。不能把大残差直接当成真实粗糙度，也不能由这六个极端案例推断整批误报的结构构成。六页案例 PDF 已查看，实际嵌入字体为 SimSun 与 Times New Roman。

**异常并非只有极端几何。** 155 张代表扫描中有 17,648 个距离内异常回波。下表分母为每个量自己具有可靠几何与固定正常参考的点；本次各量的可靠几何点均找到条件参考，没有把缺失计为低分，也没有将不同量的有效点强制取交集。

| 来源／量 | 全部异常 | 有效且有参考 | 正常参考中间区间占比 | 固定双尾阈值命中 |
| --- | ---: | ---: | ---: | ---: |
| `206` 法向变化 | 8,755 | 8,680 | 75.357% | 60（0.691%） |
| `201` 法向变化 | 8,893 | 8,776 | 66.272% | 118（1.345%） |
| `206` 局部残差 | 8,755 | 8,686 | 82.616% | 35（0.403%） |
| `201` 局部残差 | 8,893 | 8,791 | 75.873% | 16（0.182%） |

两侧合并后，法向变化中间区间占比为 **70.789%**，残差为 **79.224%**。每个量的中间区间异常都出现在全部 40 个世界的预定代表观测中，不能将结果归为某一个特殊物体。两量分别的阈值命中中，下尾占 115/178 和 37/51；当前规则触发的少数异常也并非全靠几何偏大。这里没有统计“两量同时处于中间区间”的精确交集，也没有证明这些点对任意模型均困难。

距插入回波两米内、仍保留且标签正常的对照点，共有 141,437 个距离内回波出现次数。法向变化覆盖 140,812 个、误报 1,105 个（0.785%），残差覆盖 140,854 个、误报 1,034 个（0.734%）。对应误报分别出现在 38/40 和 40/40 个世界。结合异常侧的大量非极端取值，当前数据已经含有固定探针会漏检的正例和会误报的负例；这支持开展任务标签的可学习性对照，不等于已证明网络能够学会，也不是完整合成池或 STU 的方法指标。

**插入改变保留正常的邻域。** 代表扫描共核对 15,034,728 个保留有效正常回波出现次数，坐标、强度、标签以及前后距离和射线分量完全一致。被移除的 17,109 个原始回波另计，其中 16,701 个原为有效正常；没有把它们配成零值。变更位置包括插入后的新回波和插入前被移除的回波。

距任一变更位置半米内，前后均有可靠法向变化的正常回波中，73.448% 的值改变，平均绝对变化为 0.069067 弧度；残差相应为 39.384%、0.004618 米。这里的百分比分母是两次几何均有效的正常点，平均值包含未变化的零差值。正常标签并没有随邻域变化转成异常。

| 固定探针 | 两次均有参考的正常点 | 原不命中、插入后命中 | 原命中、插入后不命中 | 新命中中位于半米内 |
| --- | ---: | ---: | ---: | ---: |
| 法向变化 | 14,793,774 | 402 | 67 | 392 |
| 局部残差 | 14,804,625 | 187 | 104 | 177 |

这些是正常点的探针误报转换，不是 V1 预测变化。有效性转变另列：法向变化有 112 个从有效变缺失、272 个从缺失变有效；残差为 117 和 264。全部配对分母包含所有距离的保留正常点，固定方向参考只覆盖规定距离内的条件，不能直接以全部配对数计算范围内参考覆盖率。全部正常误报也不能只用新增减消失解释，因为两次各自覆盖范围略有变化。

法向变化在距变更位置二至四米内仍有 1,830 个单精度取值改变，但该范围前后均有效点的平均绝对变化仅为 $8.51\times10^{-5}$ 弧度，新触发阈值的点只有 1 个。残差在两米外完全不变；两量在四米外的值、有效性与命中均完全不变。这符合两层邻域的影响范围，不能把细微数值变化等同于强物理干扰。邻近点变化本身也不证明渲染操作有错；它确实为训练留下了“邻域改变、自身仍正常”的负例。

**完整轨迹保留稀疏与缺失。** `206` 固定世界的 449 帧包括 2 帧无物体回波、206 帧仅有距离外回波、1 帧有 1—4 个距离内回波、84 帧有 5—19 个、156 帧至少 20 个。`201` 的 682 帧相应为 259、106、2、126、189。这些状态全部保留，不用官方资格过滤掉困难观测。

按每帧物体回波的中位距离分组，在 35—50 米处，`206` 的 70 帧含 713 个距离内异常回波，法向变化与残差分别可靠覆盖 700（98.177%）和 707（99.158%）；`201` 的 95 帧含 894 个，相应为 760（85.011%）和 833（93.177%）。这些范围内的可靠几何均能找到原条件参考，因此缺失来自几何估计，不能归为参考覆盖不足。`201` 仅有 1—4 个距离内异常回波的两帧合计 5 点，法向变化仅 1 点、残差仅 3 点有效；样本很少，只能作为具体低支持反例。

正常对照也随观测变化。下表是同一世界沿轨迹的异常与其两米内保留正常回波的法向变化均值，各自仅用可靠值。均值按各组可靠回波出现次数汇总，回波较多的帧贡献较大；分组依旧是帧内物体回波的中位距离，不是逐点距离分箱，也不是同一物理表面点的跨帧追踪。

| 来源／观测距离 | 异常均值（弧度） | 附近正常均值（弧度） |
| --- | ---: | ---: |
| `206`，2.5—10 米 | 0.316171 | 0.095737 |
| `206`，35—50 米 | 0.181006 | 0.244001 |
| `201`，2.5—10 米 | 0.198489 | 0.080598 |
| `201`，35—50 米 | 0.477078 | 0.513641 |

异常的均值在近处更大，远处却没有保持该顺序。这是所选两个固定物体沿轨迹的描述，距离、视角、可见片段和正常背景同时变化，不能单独归因于采样或推广为所有世界的规律。远距可靠法向变化在两侧分别有 700 和 760 个，但固定阈值只命中 0 和 5 个异常；附近正常分别有 1,093 和 906 个可靠值，仍命中 5 和 18 个。因而，现有困难并不全来自算不出几何，可靠几何的任务解释也是问题。异常自身回波少与整帧邻域支持不足不是同一条件。

**本次设计判断。** 核查已找到可进入受控训练的问题，不继续扩充同类统计，也不要求先修复所有远距缺失。

| 核查问题 | 当前发现 | 对下一步的决定 |
| --- | --- | --- |
| 异常是否过于单一 | 全部 40 个世界均含所检单量非极端异常，固定稀有度会漏掉大量可靠异常 | 不以增大残差、法向变化或拟合 val19 分布为补数据目标 |
| 困难正常是否存在 | 原始 `206/201` 已含平滑片状、较大估计变化以及跨可见片段的邻域；固定探针会误报 | 优先检验证据使用方式，不能将正常内容缺失作为当前默认解释 |
| 插入怎样影响正常点 | 保留点邻域改变、新误报主要集中在变更位置半米内，标签仍为正常 | 受控训练须检查能否保住这些正常点，不能只报告异常检出改善 |
| 跨条件证据是否保留 | 所检远距既有几何可靠但被当前固定探针漏检的回波，也有低支持缺失；附近正常与异常的几何大小关系会变 | 保留全部困难点与缺失分母；当前无需先补齐远距内容才允许训练 |

下一次优先只检验一个因素：**现有辅助监督整体是否帮助主任务利用这些正反例**。候选对照保留同一 V1 网络、预测头、三视图、检测标签及类权重，也保留 20 个训练世界与 20 个合成验证世界的划分，训练仍仅使用 `206` 来源的 20 个世界。从相同起始状态使用相同样本顺序和更新预算，将三项既有辅助损失系数共同乘以 $\lambda$，比较 $\lambda=1$ 与 $\lambda=0$。这只区分联合辅助监督与仅检测监督，不能归因于某一分支；本次数据核查也没有证明辅助项有害。当前提出该因素，是因为所需正反例已经存在，而其对任务学习的实际作用仍缺受控证据。两项几何探针不新增为模型输入、模块或监督目标。

训练代码已核对：完整视图包含全部实际回波，原始语义映射有效的保留正常点进入正常检测损失，辅助可靠性掩码不会排除这些正常标签；体素上下文恢复为逐点输出。原始插入前扫描没有作为额外视图接受一致性训练。每视图对正常与异常分别平均，再对存在类别等权，因此小范围困难正常在正常类内部没有额外权重；但目前没有将本次集合与 V1 的逐点错误、实际梯度作配对，不能由点数少断言监督被淹没，也不据此直接重加权。V1 没有输入这些经验稀有度分数，探针失败不能写成模型正在复制该规则。

下一次训练判断应依据标签预测：异常排序与检出是否改善，原生复杂正常和插入影响附近的保留正常是否误报，`201` 合成验证能否迁移。完整评价与本次代表观测诊断分别报告，最终若使用 val19，仍属公开开发证据。该候选对照本次未启动，V1 历史权重、预测、配置及结果均未改变。

**复算与核验。** 原始正常入口为 `src.profile_report.source_geometry_cases`，插入核查入口如下。正常来源输出位于 `results/coverage/geometry/`，插入输出位于其 `inserted/` 子目录；`selection.json` 保存预选身份，`frames.csv`、`features.csv`、`changes.csv` 分别记录帧状态、两量分布及配对变化，`summary.json` 按代表观测和完整轨迹分别汇总。以上明细按现有忽略规则本地保留，核心数值与结论在本文保存。

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 \
  .venv/bin/python -m src.coverage --geometry --workers 12 --threads 1
```

本次两帧先导核验身份、原始槽位、异常数及邻域影响范围后，使用 12 个进程、每进程一条数值库线程，原始几何按源帧复用。插入核查完整耗时 287.65 秒，工作进程 CPU 累计 3,264.27 秒，最高单进程内存 455,979,008 字节；实际监测的工作进程交换占用为零。宿主 E 盘剩余从 30,124,511,232 到 30,117,367,808 字节，超过固定 10 GB 余量；插入明细仅约 3.76 MB。

实际输出为 1,276 个帧记录、10,208 个特征分组记录、12,760 个配对变化记录，身份、分组分解及缺失分母均已核验。原始正常第一次执行暴露了空缓存帧处理遗漏，修正为按文件身份保留空帧后完成同一选样规则，未改旧缓存或参考。新增必要回归检查了原固定单量规则与缺失语义、完整轨迹保留零点及少回波；相关参考检查共 11 项通过，协议可加载。真实几何候选、数据核查证据与模型学习效果继续分开表述；本次完成的是数据核查，训练因素的收益仍未知。


## 辅助监督的成对训练

本次实验检验：相同网络和训练条件下，额外学习现有三项辅助目标是否比只学习正常／异常标签更有利于最终检测。两组均保留局部采样归一化几何、边界与表面预测，以及这些预测到检测头的梯度路径；只改变辅助损失整体系数：

$$\mathcal L(\lambda)=\mathcal L_{\mathrm{det}}+\lambda\sum_{k=1}^{3}w_k\mathcal L_{C_k}.$$

A 为原联合监督，$\lambda=1$；B 为同结构仅检测监督，$\lambda=0$。原三项权重各为 0.1。网络、全部预测头、完整／半采样／四分之一采样三个视图、检测标签与类别权重、20 个训练世界和 20 个合成验证世界的划分均保持不变。两组种子均为 `2062028`，训练只用 `206` 的 8,980 个世界帧，每组一轮，按相同排列遍历并每四帧累积一次更新。B 不能解释为没有几何的模型。

**共同起点。** 历史 `results/v1/initial.pt` 只有模型和配置，没有保存正式训练前的优化器、缩放器和完整随机状态。当前构造的 220 个状态张量、12,723,427 个元素与历史初始权重逐元素相同，8,980 项排列也与历史轮末保存的顺序一致；记录的实现和主要包版本一致，但历史没有记录随机深度所依赖的 `timm` 版本及完整运行环境。因此，本次不把历史 A 直接用作成对 A，也不让 B 从已训练的 `epoch1.pt` 继续。

本次保存 `results/paired/initial.pt`，包含模型、空优化器、初始缩放器、Python／NumPy／PyTorch CPU 与 CUDA 随机状态，以及共同样本排列。A、B 各自恢复该状态，逐项核对一致；作为初始状态的检查点必须尚未访问训练样本、没有更新及历史记录。两组配置比较只允许 `auxiliary_scale` 不同。共同初始状态并不保证后续非确定性 CUDA 运算逐位复现；同种子也不保证跨环境结果相同，见 [PyTorch 官方复现说明](https://docs.pytorch.org/docs/2.14/notes/randomness.html)。本次在同一设备及环境执行，两组共同使用下述数值修正，仍保留已确认的浮点复现限制。

**评价与判断。** 主结果使用全部 val19 官方点集，报告 AP、FPR95、正常误报不超过 1% 时的最高召回率；不排除几何不可计算的点。两组在各自曲线上确定相同误报要求对应的阈值，不共用数值阈值。合成验证沿用全部有效标签及附加官方筛选两个范围，解释沿用已有语义、距离、复杂正常案例、插入回波两米内的保留正常和 `125` 全部合格帧的同帧排序，不按新输出重新选择案例。共同检测损失可以比较，两个不同定义的总损失不能用于判优。

A 若 AP 更高且 FPR95 更低，支持该辅助组合在此预算下有帮助；B 若同时更好，支持其在此预算下拖累检测，不代表几何表示无用。两指标一好一坏则报告取舍，差异很小或两组都很差则保留训练充分程度与学习方式的未知。本次只有一轮、一个配对种子，不据此宣布长期有效或有害，不自动只为较好的组延长训练。本实验直接检验辅助监督，不能直接验证传感器条件化，因为两组均保留条件几何结构。

**训练前核验。** 与本次变量直接相关的 13 项回归全部通过：辅助系数只改变相应梯度项，共同起点完整恢复，已训练检查点不能冒充训练前状态。另在真实 `206/supplement_002/37` 的三个既有视图上执行一次仅检测监督的反向传播，分别含 120,673／60,415／30,125 个实际回波。边界、表面、边界表示、局部几何表示和检测头的梯度范数分别为 0.00067429、0.00233978、0.00992118、0.02858729、0.047085，均有限且非零；没有执行优化器更新，输入和模型状态保持一致。这直接核实了 B 保留检测梯度到各分支的路径，不证明正式检测成绩会改善。

**数值故障与重新配对。** 原数值路径下的 A 未完成一轮：第一次在第 7,365 个访问样本 `candidate_000/257` 遇到非有限损失，最后完整检查点为 7,200 帧、1,800 次成功更新。该帧输入与目标全部有限，手动重放至该位置未复现；随后按原配置恢复又在第 7,373 个样本 `candidate_013/92` 失败。带故障前随机状态捕获的原训练函数再次复现了后一个故障。现场模型参数全部有限，基础视图 120,237 个点的三个输出却全部为 NaN。重新构造的 10 个输入张量、5 个目标张量与现场逐字节相同；局部八维邻点输入最大绝对值仅为 7.002109，不支持原始几何输入过大或缓存改变数据的解释。

按同一模型和故障前随机状态重放，最早的非有限值出现在 `backbone.dec.dec3.up.proj.0`：输入全有限，最大绝对值为 6,941.185547；半精度线性投影产生 5,852 个正无穷，随后原归一化产生 8,054 个 NaN，并向后传播。同输入、同参数的单精度投影全部有限，最大绝对值为 65,718.445313；接续原归一化和激活后最大绝对值降至 3.195784。半精度最大有限值为 65,504，但舍入成无穷的边界约为 65,520，不能把所有超过 65,504 的单精度值数目直接当作无穷数目，也不能宣称两种算术逐点相同。

据此仅修改 `GridUnpooling` 的数值计算：两条既有线性投影、归一化和激活连续保持单精度，在分支完成后恢复原混合精度输出类型，再按原索引相加。参数、网络结构、公式、标签、三视图及损失权重均不变。独立溢出回归通过；故障样本完整三视图的 A/B 前向及反向也通过，全部 12,723,423 个梯度元素有限。两臂在同模型、同随机状态下的三视图输出逐元素相同；B 的边界、表面、局部边特征和几何汇聚梯度范数分别为 0.000211485、0.002321739、0.014191004、0.016789048，仍均非零。该检查没有执行优化器更新。

旧的未完成尝试及故障快照位于 `results/paired/failed/`，其中 `failure.pt` 明确标为仅诊断使用；`activation.json` 记录首次非有限激活。其原始路径字段记录当时的保存位置，不代表当前正式起点。成对实验的 A、B 均使用修正后的数值路径，从同一完整训练前状态开始，各自完成一轮，没有从旧 7,200 帧检查点接续不同数值路径。新旧训练前模型、优化器、缩放器、全部随机状态与样本排列已逐项核对完全一致；配置记录仅解码器源码发生上述修正。这一失败只确定了具体数值故障，不构成辅助监督有益或有害的证据。

**A 完成后暂停。** 数值修正后的 A 从共同训练前状态重新完成一轮，实际遍历 8,980 个基础帧、26,940 个视图，尝试更新 2,245 次且全部成功，跳过 0 次，耗时 3,826.013 秒（约 63 分 46 秒，不含此前失败和诊断时间）。`results/paired/joint/training.json` 与 `epoch1.pt` 的访问数和更新数一致，检查点标记 `complete=true`，全部模型状态张量有限。该轮经过两处此前故障样本位置并完整结束，支持这次 A 执行已消除所捕获的溢出中断；不能据此保证所有未来训练均无数值问题。

上轮按用户“完成 A 之后先暂停”的要求，在 A 完成记录保存后停止成对执行进程。当时确认训练及加载进程退出，B 目录尚不存在，两组评价均未启动。该暂停发生在 B 的任何训练更新之前；以下记录本次用户要求“继续完成 B”后的实际执行。

**B 从共同起点完成。** 恢复前按训练入口的实际初始化顺序核对运行配置，当前源码、模型与监督定义、数据身份、包版本、GPU 与驱动、数值精度设置及线程环境均与 A 记录一致。现有成对入口读取 A 的完成检查点后直接返回，再将 B 的辅助损失整体系数设为 0，恢复 `results/paired/initial.pt` 中的模型、优化器、梯度缩放器和四类随机状态。B 的 `start.json` 记录零访问、完整恢复精确一致，8,980 项样本顺序与 A 相同；两组执行配置仅 `auxiliary_scale` 为 1／0 的差别。全部网络分支、三个视图和检测梯度路径继续保留，本次未修改训练实现。

B 一次连续执行完成 8,980 个基础帧、26,940 个视图，2,245 次尝试更新全部成功，跳过 0 次；进程正常退出。`results/paired/detection/training.json` 记录总耗时 3,907.459 秒（约 65 分 7 秒，包含本轮检查点保存和完成前资源读取），末次进度行的 3,906.041 秒早于最终完成记录。轮末 `epoch1.pt` 标记 `complete=true`，访问数、更新数及完整排列与训练记录和 A 一致，全部模型状态张量有限。两组模型状态键与形状相同；A 的轮末检查点和训练记录大小、修改时间均与 B 启动前一致，没有重训或覆盖 A。

两组各一轮训练完成时，训练及加载进程退出，GPU 无计算进程，当时合成验证与 val19 评价均未启动。后续评价执行与暂停情况见本节末尾；当前仍没有可比较的 AP、FPR95 或低误报召回，不能据此判断辅助监督有益或有害。下一项能够改变该判断的工作是按既定范围完成两组评价；不比较不同定义的总训练损失，也不自动追加训练预算。

**资源与执行。** 执行前实测 24 核 CPU、RTX 5080 Laptop 的 16 GB 显存、约 22.83 GB 可用内存，未发现其他训练争抢。固定前 48 个样本的 CPU 加载核验中，4／8 个加载进程分别为 6.660／11.401 基础帧每秒，全部输入与监督数组一致；4 个进程已经超过历史训练 2.308 帧每秒的需求，因此两组保留 4 个加载进程、单样本预取、主进程两条 PyTorch 线程。

历史单臂完整预测实际占约 20.55 GB；本次改为逐臂流式精确评价，合成有效分数流和一份原位排序文件顺序使用，保留必要的小结果与异常逐点解释记录，不复制旧监督和全量预测。预计同时新增峰值不超过 15 GB；起始 Windows E 盘剩余约 30.12 GB，始终保留固定 10 GB。训练、评价和科学裁决的完成状态分别记录，不将运行成功当作辅助监督有效。

数值修正后重新配对前，再次确认 24 个可用物理核、约 22.35 GB 可用内存、约 15.78 GB 空闲显存，GPU 没有其他计算进程；Windows E 盘剩余 29,662,810,112 字节。已有失败快照保留在本地，仍满足上述峰值预算和固定余量。

A 全轮记录的 PyTorch 峰值显存分配为 4,346,668,032 字节；完成时可用内存为 19,510,767,616 字节，Windows E 盘剩余 29,741,060,096 字节，高于固定 10 GB 余量。系统原有交换空间占用单独记录，不能将其视为本实验进程的交换量。

B 启动前重新实测 24 个可用物理核、21,977,800,704 字节可用内存、GPU 空闲显存 14,429 MiB，无其他计算进程；Windows E 盘剩余 29,700,554,752 字节。单轮检查点及待写副本预算为 1 GB，本次不启动评价，保留原四个加载进程和主进程两条 PyTorch 线程。运行中检查点记录的可用内存约 17.1—17.9 GB，GPU 利用率抽查约 62%—75%，实验进程交换占用均为零。全轮峰值显存分配为 4,337,630,208 字节；完成时可用内存为 18,753,933,312 字节，Windows E 盘剩余 29,597,859,840 字节，高于固定余量。

紧凑评价在历史真实预测 `125/129`、`125/139` 的 212,481 个官方点上核验：写入与重读后的原槽位、标签和单精度分数逐位相同；AP、AUROC、FPR95、各误报工作点及对应正常／异常命中数与原完整评价完全一致。同帧所需误报率也与逐异常直接统计“正常分数大于等于该异常分数”的结果逐项一致。该核验只确认存储与聚合等价，未用这两帧选择新参数或替代正式评价。

以下为本次使用的成对训练入口：核验并跳过已完成的 A，再从共同起点执行 B。两组现均已完成，无需再次执行；该入口不启动评价。

```bash
PYTHONDONTWRITEBYTECODE=1 OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 NUMEXPR_NUM_THREADS=1 \
  .venv/bin/python -m src.train --stage paired
```

**配对评价首次执行与暂停。** 用户随后授权依次评价 A、B 并比较。本次沿用既有 `src.diagnose --paired-run` 和 `--compare` 入口，模型、损失、数据及评价实现均未修改。启动前核对两组执行环境与保存配置完全一致；命令额外保留训练时记录的 `NUMEXPR_NUM_THREADS=1`，以满足原入口的环境检查。实测 24 个物理核、22,006,210,560 字节可用内存、GPU 空闲显存 14,042 MiB，无其他计算任务；Windows E 盘剩余 29,593,534,464 字节，满足 13 GB 临时文件预算与固定 10 GB 余量。

A 的合成验证开始后，用户要求暂停。立即向该评价进程发送中断信号，程序在合成视图误差累计过程中以 `KeyboardInterrupt` 退出；这是用户要求的停止，不是数值故障。最后一次完整进度日志为 6,200／13,640 帧、已复用 9,470 次前向、耗时 1,011.572 秒，临时文件为 8,047,220,247 字节。这是最后日志位置，不能当作中断瞬间的精确处理数。A 尚未完成全量指标汇总，也未进入原始正常案例与真实 val19 评价；后续 B 和比较命令未启动。

退出后确认评价及加载进程全部结束、GPU 无计算进程，两组训练检查点及完整训练记录保留。两组均无 `evaluation.json` 或 `anomalies.npz`，也无 `comparison.json`。临时分数文件随退出释放，Windows E 盘重新实测剩余 31,069,601,792 字节。A 仅留下用于绑定检查点和数据身份的 `predictions/manifest.json`，其中没有中途预测或评价结果。本入口未保存可恢复的中途评价进度，因此后续恢复需从 A 的评价重新开始，再完整评价 B 并比较；两组均无需重新训练。当前保持暂停，不依据这次部分执行作科学结论。
