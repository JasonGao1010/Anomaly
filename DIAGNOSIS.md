# 面向双指标目标的系统诊断

本报告针对已完成训练的 AJAE-NRE，目标为真实开发集 AP 至少 80%、FPR95 不超过 1%。本轮实际读取了本机检查点、正式 v2 数据、历史完整预测与训练日志，完成同点失分归属、193 个固定窗口的内部前向和 8 个训练窗口的零更新梯度检查。没有训练更新、修改模型或损失、重建数据、重选旧候选，也没有访问隐藏测试。

**主要结论：当前瓶颈集中于困难真实异常的排序及其正常支持路径。后半程进一步拟合训练数据，却使第 125 序列的一批近距离、较多点异常越来越被正常组接纳。** 125 贡献最终全局 AP 失分的 68.072%，125 与 142 合计贡献 1% 误报工作点漏检的 92.109%。这些现象不能用起始补帧、远距离少点、整窗正常缺覆盖或初始体素普遍丢点解释。

下一版建议优先检验一项结构变化：**将最终异常排序与正常类别支持的直接加和关系分开，让异常分支拥有独立的逐点表示与输出；正常原型和语义继续作为辅助训练目标。** 首轮继续使用冻结 v2 数据和匹配训练预算，另将监控覆盖及选择标准对应到两个目标。这里提出的是可检验的方案，不是已证明有效的改进；本轮到诊断报告为止。

需保留一项数值限制：本次部分前向与历史预测超过原数值容差。已有完整预测的指标可以原样复算；同一进程内的缓存、调用顺序和分块检查一致。超过容差的跨运行差异仍未定位，详细范围见第八节。内部路径结论来自两个检查点使用同一本次执行路径的比较，不能声称逐位复现了历史前向。

## 一、材料、身份与统计含义

主要模型为 `runs/train/nre/visit_14240.pt` 与 `visit_28480.pt`。前者完成 14,240 次有效访问、14,236 次更新；后者完成 28,480 次访问、28,470 次更新。第 7,120、21,360 节点仅读取参数、日志和已经修正的监控。原第 28,480 节点的选模身份保持不变。

| 对象 | 本轮使用范围 |
| --- | --- |
| 中期完整预测 | `runs/eval/nre/interim_14240/`，1,960 个官方合格当前帧 |
| 最终完整预测 | `runs/eval/nre/real/`，19 条真实序列、全部 8,659 帧 |
| 历史正常配对 | 第 14,240 节点的修正监控与最终完整预测共有的 152 个无异常窗口 |
| 正式训练材料 | 206 的 32 个世界、14,240 个合法窗口；两次遍历仍是同一批世界 |
| 正式合成验证 | 201 的 8 个世界、5,424 个合法窗口 |
| 原始正常验证 | 正常 201；内部探针为 16 个完整窗口 |
| 已有真实画像 | `profiles/real/` 及其逐点汇总，直接复用五帧与邻域统计 |

两个主要检查点的摘要分别为：

```text
14240: 1620e9a45b10623a64a2a2aeb2986a8c11686860cdfbd935603d67edceeac68f
28480: a7d71bec7257f4956c6112b0e255b3573ac935a4c0cad70a51da35a4890f57f0
```

全局指标按序列、当前帧、原始槽位对应到同一批点：193,792,470 个正常点、87,499 个异常点。历史窗口中的同一扫描预测没有替代当前输出。官方距离范围为 2.5–50 米，忽略原始语义 0，当前帧范围内异常点至少五个才进入正式指标。启动帧保留原规则。本报告中的正式分数都是原始 logit，固定正常高分条件为分数不低于 0。

AP 使用完整并列分数组的精确率，未用梯形积分；全局失分按每个异常点在全局排序中的精确率归属：

$$D_G=\frac{1}{87499}\sum_{p\in\mathcal A\cap G}[1-\operatorname{Prec}(s_p)],\qquad \sum_GD_G=1-\mathrm{AP}.$$

序列、点数与距离、可见模式、历史支持各自构成一套分区，四套加总均与全局失分一致。不同分区不能互相相加。失分归属描述排序位置，不是因果贡献或修复后的可回收 AP。该计算直接复用原评价器的排序与并列分数处理；定义参见 [Scikit-learn 的 AP 文档](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.average_precision_score.html)。

低误报工作点在全部实际阈值中取 FPR 不超过 1% 时的最高召回，不插值拆分同分点。官方高召回工作点保留原程序的共线 ROC 节点删除方式，取首次召回**严格超过** 95% 的节点。随后将各模型的一对全局阈值应用到所有序列，不逐序列另选阈值。阈值包含所有分数不低于它的点；参见 [ROC 曲线定义](https://scikit-learn.org/stable/modules/generated/sklearn.metrics.roc_curve.html)。

## 二、距离两个目标还有多少

| 同一官方点集 | 中期 14,240 | 最终 28,480 | 最终减中期 |
| --- | ---: | ---: | ---: |
| AP（%） | 65.860280 | 66.223547 | +0.363267 个百分点 |
| AUROC（%） | 99.208663 | 98.832748 | −0.375915 个百分点 |
| FPR95（%） | 4.250239 | 10.220041 | +5.969802 个百分点 |
| 1% 误报限制下召回率（%） | 93.053635 | 86.965565 | −6.088070 个百分点 |

最终 AP 距离 80% 还差 13.776453 个百分点，FPR95 高于目标 9.220041 个百分点。AP 略有提高同时高召回性能退化，说明两个目标需要共同检查，不能由一个指标代替另一个。两份完整历史指标均被原样复算，没有用本轮新探针结果覆盖它们。

| 模型与工作点 | 原始分数阈值 | TP | FN | FP | TN |
| --- | ---: | ---: | ---: | ---: | ---: |
| 中期，低误报 | −4.386591 | 81,421 | 6,078 | 1,935,729 | 191,856,741 |
| 最终，低误报 | −4.958472 | 76,094 | 11,405 | 1,937,329 | 191,855,141 |
| 中期，官方高召回 | −5.014358 | 83,125 | 4,374 | 8,236,644 | 185,555,826 |
| 最终，官方高召回 | −6.152641 | 83,125 | 4,374 | 19,805,670 | 173,986,800 |

TP、FN 分别为检出的异常与漏检异常；FP、TN 分别为误报正常与正确排除的正常。两个低误报工作点的实际 FPR 分别为 0.998867%、0.999693%；两个高召回工作点均为 95.001086% 召回。精确值见 `global.json`，曲线见图 1。

![图 1：原始完整预测的 PR 曲线与低误报 ROC。图形连接实际阈值供阅读，指标计算不使用插值。](reports/curves.pdf){width=100%}

### 失分与误报的主要来源

最终总失分为 33.776453 个百分点。125 的 210 个合格帧、25,871 个异常点贡献 22.992240 个百分点，较中期增加 6.001746 个百分点。其余序列合计失分反而下降约 6.365013 个百分点，因此全局 AP 只小幅提高。

| 重点范围 | 合格帧／异常点 | AP 失分，中期→最终（百分点） | 低误报漏检，中期→最终 |
| --- | ---: | ---: | ---: |
| 125 全部 | 210／25,871 | 16.990494 → 22.992240 | 3,431 → 8,975 |
| 125 第 133–165 帧 | 33／8,373 | 6.646525 → 9.092853 | 1,443 → 5,287 |
| 141 全部 | 307／18,389 | 5.065396 → 2.365633 | 188 → 117 |
| 142 全部 | 87／1,596 | 1.808076 → 1.803490 | 1,546 → 1,530 |
| 144 全部 | 79／4,508 | 1.482672 → 1.447653 | 165 → 178 |

125 的低误报漏检增加 5,544 个，超过全部序列净增加的 5,327 个；其余序列净减少 217 个。125 与 142 最终共漏检 10,505 个，占全体 11,405 个的 92.109%。142 仍值得保留，但就 80% AP 目标的失分范围而言，125 的优先级更高。

主要失分条件是当前异常点数 100–499、异常距离中位数 2.5–10 米：247 帧、45,876 个异常点，最终贡献 22.620366 个 AP 失分百分点，低误报漏检 8,494 个。持续五帧可见的 `11111` 组包含 1,897 个合格帧、83,985 个异常点，贡献 33.160284 个失分百分点和 11,315 个漏检。这里的点数是当前帧点数，不能改称完整物体大小。

为了达到严格超过 95% 的召回，最终模型的正常误报分散在多条序列：125 为 4,218,393 个，141 为 3,045,104 个，169 为 1,673,172 个，150 为 1,488,272 个；其余序列合计 9,380,729 个。**漏检集中与误报分散同时存在。** 高召回误报增多部分来自工作阈值下降，不能仅凭这些数量声称每条序列的正常分数都上升。完整 19 序列表和点数距离分解见附表。

![图 2：第 125 序列的原始连续观测。阴影是预先指定的第 133–165 帧，失分与漏检使用完整全局排序及工作点；没有合格异常的时刻不伪填 AP。](reports/sequence125.pdf){width=95%}

### 正常阶段、少点阶段和历史残留

最终模型在 5,114 个全距离无异常回波帧中，有 366,706,288 个官方范围内有效正常点；分数不低于 0 的点为 383,314 个，占 0.104529%。将上述真实全局低误报阈值用于这些帧，误报为 5,964,923 个，占 1.626621%；高召回阈值下为 46,697,158 个，占 12.734213%。这些帧没有进入官方 AP，但属于连续运行需要承担的误报。

双方已有修正后预测的 152 个正常窗口共 10,805,176 个正常点，零阈值误报从 23,920 个降至 7,647 个，即 0.221375% 降至 0.070772%。不能将这项配对改善扩大成中期模型全部正常阶段的结论。

正常阶段按完整五帧进一步分开：687 个当前已无异常、历史仍有异常的窗口，正常点 68,883,189 个，零阈值误报 22,601 个，占 0.032811%；4,355 个整窗无异常窗口，正常点 295,329,185 个，误报 349,110 个，占 0.118210%。还有 72 个启动正常帧单列。此处没有显示“只要历史有异常就普遍残留更高误报”，但场景构成不同，不能据此推断历史具有因果保护作用。

连续正常阶段中，169 第 0–260 帧有 47,299／24,289,847 个正常点高分，142 第 442–536 帧为 40,043／9,705,018，141 第 641–696 帧为 28,659／5,714,980。重点探针按这类累计数量与连续范围选择，没有只挑最大比例。

1–4 点异常阶段共 1,120 帧、2,595 个范围内异常点。零阈值检出 1,538 个，466 帧没有任何异常点被检出；低误报全局阈值检出 2,315 个、漏检 280 个；高召回阈值检出 2,515 个、漏检 80 个。同期正常点为 105,865,316 个，三个阈值的误报分别为 41,816、1,087,521、13,283,467 个。少点统计始终没有混入正式 AP。

## 三、正式 v2 的监督与覆盖

### 没有在受检消费链中发现错误监督

64 个训练探针包含 38,719,408 个正常、3,323 个异常、1,452,885 个忽略输入点；32 个合成验证探针包含 14,875,045 个正常、2,273 个异常、5,114,878 个忽略输入点。以上是五帧窗口中的观测次数，包含重复历史，不是独立原始点数量。

实际读取检查对应了未改变槽的坐标、强度和标签，新代理回波的异常身份、无细语义类别，以及没有返回的遮挡槽从输入中消失。探针中分别有 4 个、3 个不返回遮挡槽；这是实际覆盖量，不能声称检查了大量复杂遮挡样本。原始忽略回波保留输入，正常但无有效细语义的回波进入 `other_normal=19`。忽略点在三个输出端对四项损失的直接梯度均为零；它们仍可通过邻域表示间接影响其他点，这符合原设计。

固定探针在各合成世界内时间分散，没有形成重叠窗口对。因此另对训练世界 0 的 113／114 窗和验证世界 0 的 429／430 窗只读核对，分别共享的四个源扫描在实际坐标、位姿、槽位和标签上完全相同，没有额外模型前向。五帧构造和标签同步同时沿用已有窗口入口的有效检查。此次未发现会推翻训练或评价有效性的标签、点身份错误；此结论限于实际检查链与已有可靠检查，不等于全数据逐点人工标注复审。

### 覆盖缺口与已有覆盖要分开

| 条件 | 206 训练 | 201 合成验证 | 真实完整五帧 |
| --- | ---: | ---: | ---: |
| 全部合法窗口 | 14,240／32 世界 | 5,424／8 世界 | 8,583／19 序列 |
| 整窗无异常 | 7,234（50.801%） | 2,704（49.853%） | 4,355（50.740%） |
| 持续五帧可见 | 4,747 | 1,750 | 2,856 |
| 100–499 点、2.5–10 米 | 395 窗／14 世界／88,979 异常点 | 77 窗／4 世界／14,637 异常点 | 247 窗／14 序列／45,876 异常点 |
| 20–99 点、10–20 米 | 477 窗／18 世界／21,159 异常点 | 162 窗／5 世界／5,880 异常点 | 303 窗／14 序列／10,546 异常点 |

整窗正常比例的旧 v1 缺口已被当前 v2 改造覆盖，不能继续用旧数据解释本次失败。主要失败的点数距离条件在训练中有样本，固定训练探针也能学好；这里优先面对的是相近粗条件下的迁移问题。训练背景仍只有原始 206，验证背景仍只有 201，32 个世界不等于 32 条独立道路。

邻近背景方面仍有实测差异。对当前官方合格异常点，距同一扫描最近正常回波不超过 0.25 米的比例为训练 26.346%、合成验证 34.712%、真实 53.086%；在 100–499 点、2.5–10 米组内分别为 21.206%、35.438%、45.159%。距离计算先保留实际观测，再查询最近的存活正常回波，没有重新建立另一套模型体素。原真实逐点资料直接复用。

以“每帧异常点到正常点的最近距离中位数不超过 0.25 米”追加描述同一重点组，覆盖为训练 47 窗／7 世界／6,513 异常点，合成验证 35 窗／3 世界／6,131 异常点，真实 139 窗／14 序列／21,821 异常点。它提示背景接近条件的频率及多样性仍不一致，**不等于缺少全部此类样本**。

这也不是 125 的完整解释。125 完整五帧合格区间中，最近距离中位数大于 0.5 米的 45 帧仍贡献 8.752671 个失分百分点、4,439 个低误报漏检；142 则更多集中在很贴近背景的观测。不能把一项总体分布差异直接定为共同根因。本轮不因此自动增加生成参数或重建数据。

### 正常知识的实际范围

全部正式训练世界的正常组监督次数，与冻结的 `protocols/nre/labels.json` 独立复算一致。组 6、9 在训练中没有正监督；201 的组 9 有 345 个源帧、411,739 个不同原始正常点，在八个世界和重叠窗口中累计 16,469,560 次监督观测。它没有被错误改标为其他组，而是原训练头没有该组。

组 1 仅有 596 个不同原始正常点，分布于 118 个源帧，却累计 95,360 次五帧世界观测；权重已经为上限 4。类似地，组 2、3、4、5、7、11、18 的权重也达到 4。不同原始点通过所有世界的存活槽位并集计数，不能将多个世界计数的最大值冒充严格并集。这些事实说明正常知识受到单一道路来源和重复观测限制，不支持继续仅靠增加相同窗口访问次数补充知识。

在固定训练探针当前可见正常点上，语义平均交并比从 65.506% 提高至 75.201%；原始正常 201 的 16 个探针为 22.234% 至 22.717%。两者覆盖和类别构成不同，不能作严格因果配对，但训练拟合与跨道路细语义泛化的差距很明显。201 中无训练监督的组 9 保留在混淆矩阵真值行中，不能因为模型不输出它就删去其计分影响。

正常 201 探针的零阈值误报主要变化也不是单向的：组 0 为 1,318／8,163 降至 219／8,163，组 10 为 786／249,223 增至 1,067／249,223。这是具有细语义真值的正常 201；真实异常验证仅可报告“模型最支持的正常组”，不能把该组当作真实物体类别。

## 四、沿同一批点定位内部变化

探针在正式前向前按指定规则固定并去重：训练 64、合成验证 32、正常 201 为 16、真实基础 57、真实重点 24，共 193 个窗口，没有类型补缺。完整五帧及全部实际输入点都参与推理。基础探针按世界、序列和时间选择；125 的 12 个重点窗、142 的 6 个重点窗及 6 个连续误报重点窗用于定位已知错误，二者不混称代表性测试样本。

每个窗口保存两模型的全输入点分数、原始槽位和源帧，保存实际逐层映射；高维特征每窗至多固定抽取 256 个正常、256 个异常、64 个忽略点。抽样只影响特征描述，不裁剪输入或最终分数。逐窗分位数保留在 `scores.csv`；下表是当前官方范围内异常点的点加权均值，没有施加至少五点的帧资格，所以训练侧包含 710 个异常点，而探针 AP 使用其中 676 个合格帧异常点。

| 范围 | 异常点 | 最终分数，中期→最终 | 基础拒识，中期→最终 | 修正，中期→最终 |
| --- | ---: | ---: | ---: | ---: |
| 训练基础探针 | 710 | 9.316 → 10.528 | 8.032 → 9.069 | 1.283 → 1.459 |
| 合成验证基础探针 | 433 | 6.582 → 7.078 | 5.938 → 6.521 | 0.643 → 0.557 |
| 真实基础探针 | 1,020 | 8.203 → 4.928 | 7.596 → 4.932 | 0.607 → −0.005 |
| 125 全部探针 | 3,041 | 4.840 → −4.983 | 5.492 → −2.984 | −0.652 → −1.999 |

代码字段 `support` 是基础拒识分数，越大越异常；`normal_logits` 才是各正常组支持，越大越支持正常。125 同点最终分数平均下降 9.822918，其中基础拒识下降 8.475792、修正下降 1.347127，前者约占平均变化幅度的 86.286%。这是精确前向加和分解，不能将这个比例解释成训练原因的因果比例。

125 的最大正常组支持均值从 −3.914527 增至 5.670062。最终最支持组 0 的异常点从 63 个增至 1,787 个；最支持 `other_normal` 的比例为 37.652% 至 37.323%，没有出现由该兜底组独自扩张解释全部退化的证据。组号含义来自训练标签映射，不能由真实合并标签确认这些异常物体实际是什么。

按各模型自己的全局低误报工作点，同一批 125 探针点中，53 个持续漏检、1,549 个由检出变漏检、13 个由漏检变检出、1,426 个始终检出。由检出变漏检的点，平均总分下降 9.260434，基础下降 8.223231，修正下降 1.037203。真实基础探针对应转坏 200 个、转好 6 个；训练探针没有转坏点，仅一个转好点。完整窗口和当前范围的四类配对变化都保存在 `paired.csv`。

125 异常修正不高于 −1.98 的比例由 23.216% 增至 99.540%。将基础分数加上实际修正后，中期有 66 个、最终有 1,602 个异常点跨到各自全局低误报阈值以下。**这不意味着去掉修正就能达到 1% 误报。** 同时去掉正常点修正会改变正常端排序和工作阈值；重点真实探针中，最终基础分数 AP 为 4.168933%，最终加和分数 AP 为 4.167319%，两者都弱。因此当前证据不支持把“推理时关掉修正”当成解决方案。

![图 3：同一批探针的基础拒识、修正、前向分解及实际层特征。上排按当前范围内异常点加权；右下是固定特征样本的逐窗中位数。两类平均方式不混用。](reports/paths.pdf){width=100%}

### 原型使用集中，但不能写成原型普遍重合

最终 18 个正常组中，同组不同原型最大余弦相似度的最大值为 0.831728，中位数 0.259546；跨组最大相似度的总体最大值为 0.573358。它们没有普遍接近 1，不能称为所有原型向量塌缩到一个中心。

实际使用则高度集中。完整第二遍训练日志按**正常真值组**统计组内胜出原型，18 组中 16 组的一个原型承担超过 99% 的正常点观测，第一遍为 11 组。固定训练探针按**预测最支持组**统计，最终 17 个有响应组中 15 组超过 99%。两种统计范围不同，均提示名义上每组四原型并未形成广泛均衡分工；不能仅凭不均衡判定错误，也不能直接推出应增加原型数。

接受基准整体下降。组 0 的中期与最终值为 0.403278、0.378658；组 19 为 0.363797、0.335439。固定其他量时，降低基准会提高正常支持。但是组 0 的这一基准差除以温度 0.07 仅约 0.352，远小于 125 最大正常支持均值约 9.585 的变化。点特征、原型和胜出组也在共同变化，不能把退化单独归给接受基准，或只通过手调它解决。

### 实际下采样和浅深特征

每个原始点在初始体素、各编码层和最终解码层均有有效映射，两检查点的几何映射完全一致。没有发现丢点；观察到的是部分异常与正常点共享深层单元。

| 五帧探针范围 | 第一编码层：混合异常单元／异常单元 | 最深编码层：混合异常单元／异常单元 |
| --- | ---: | ---: |
| 训练 | 13／1,820 | 58／101 |
| 125 | 162／5,034 | 39／82 |
| 142 | 75／481 | 34／38 |
| 全部真实探针 | 301／7,858 | 156／237 |

混合指该单元同时存在异常与正常成员。`layers.csv` 还保留异常、正常、忽略三类占全部成员的比例，并分别按全部异常点与当前异常点汇总。上述各列是单元观测次数，重叠窗口仍会重复计数。

固定特征样本的可区分程度定义为两类特征中心的欧氏距离，除以两类内部平方距离均值的均方根。训练探针第一编码层的逐窗中位数从 0.771 至 0.788，最深层从 1.763 至 2.675，最终解码层从 3.981 至 5.304；125 分别为 0.766 至 0.859、1.243 至 1.109、2.565 至 1.722。125 没有出现浅层整体变差，而深层和解码表示的这项分离描述下降；它支持继续检查下游表示与拒识边界，但不能据此宣布五帧有害或所有真实异常特征都被吞没。

按第一融合线性层分块计算输入投影的范数，125 异常样本的逐窗均值再取中位数，浅层项为 4.390 至 5.017，深层项为 11.716 至 11.948，逐点细节项为 0.584 至 0.687。浅层路径确实存在贡献。范数不是独立输出贡献，后续归一化、非线性及向量抵消仍会改变作用，不能据此给各层分配因果百分比。

正常 201 中有可靠非零实例号、当前与至少一历史扫描各有五点的可移动类别，共得到 109 条实例×窗口记录。自车配准后，各扫描观测点中位中心到当前中位中心的最大距离，是观测散布，不是真实速度。散布小于 0.25 米、0.25–1 米、至少 1 米的当前点分别为 11,811、10,153、54 个，最终零阈值误报分别为 219、1、0 个。这里未显示散布越大误报越多；最后一组仅三条记录且有遮挡、可见部分变化等混杂，不能排除真实动态物体问题。

## 五、训练是在继续学会，还是继续失去迁移

### 同一批窗口给出的答案

以下 AP 仅属于固定探针的合格当前帧；未统一不同域的异常占比，也不是完整池成绩。重点探针根据旧错误选择，尤其不能作为独立泛化估计。

| 范围 | 合格窗／异常点 | AP，中期→最终（%） | FPR95，中期→最终（%） |
| --- | ---: | ---: | ---: |
| 训练基础 | 17／676 | 97.286446 → 99.944534 | 0.009076 → 0.000048 |
| 合成验证基础 | 24／433 | 42.429228 → 38.727609 | 19.4204 → 61.2361 |
| 真实基础 | 38／1,020 | 67.139745 → 60.705151 | 0.145514 → 6.517672 |
| 真实重点 | 17／2,926 | 66.489376 → 4.167319 | 9.048650 → 23.978560 |

训练探针当前异常点的逻辑损失按点均值由 0.092124 降至 0.000104；真实基础探针由 0.244558 增至 1.585741。训练正常点损失由 0.001937 降至 0.000939，真实基础正常点由 0.007446 降至 0.002781。这支持“训练拟合继续改善，部分真实异常的相对区分变差”，不支持所有点都因优化失败而一起退化。

在 100–499 点、2.5–10 米条件内，固定训练探针只有两世界、两窗、239 个异常点，AP 从 99.838659% 至 100%；真实基础对应两序列、两窗、335 个异常点，从 90.760628% 至 35.626395%。这与全量失分定位方向一致，但窗口少、几何和背景未配对，不能称为隔离所有混杂的条件因果实验。合成验证该组只有一世界、一窗，AP 从 78.997654% 至 54.879322%；不宜把问题全部解释为真实材质未知。

不同世界及有数据的点数距离组保存在 `condition_metrics.csv`。完整最终合成池 AP 仍为已有的 73.149212%，没有被这批时间抽取探针的 AP 替换。训练及合成验证世界数少，重复时刻会放大特定观测的权重，这是解释局部统计的必要边界。

### 有效训练轨迹与监督强调

训练日志按有效访问位置恢复，故障前回退的 29 次访问保留为旧运行证据，在本轮统计中由恢复后的有效访问替代，未重复累计。最终共 28,480 次访问、28,470 次成功更新，十次溢出跳步，第二遍全部使用修复后的解码投影路径。各遍学习率仍是既定计划，没有由诊断调整。

| 有效遍历 | 第一遍 | 第二遍 |
| --- | ---: | ---: |
| 访问／成功更新 | 14,240／14,236 | 14,240／14,234 |
| 裁剪更新／成功更新 | 4,094／14,236（28.758%） | 1,162／14,234（8.164%） |
| 总损失逐访问均值 | 0.393541 | 0.112795 |
| 检测项均值 | 0.295478 | 0.088375 |
| 基础支持项均值 | 0.143624 | 0.042135 |
| 语义项均值 | 0.099399 | 0.023763 |
| 困难正常项均值 | 0.190621 | 0.020057 |
| 整体修正接近边界比例的均值 | 89.250% | 99.919% |

四个分项在本表中未乘最终系数；检测、基础、语义的系数分别为 1、0.25、0.5，困难正常项在最初 1,000 次成功更新内从 0 增至 0.1。第一遍总损失不能简单用 0.1 乘整遍尾部均值重构。各千次区间还保存梯度范数、缩放、分位数、接受基准与原型使用。训练期窗口与参数同步变化，因此这些均值只描述过程；固定探针才用于同输入比较。

按**全五帧、全范围**异常点数分组，1–4 点访问有 2,772 次，发生六次溢出；5–19 点有 4,350 次、两次溢出；20–99 点有 3,564 次、两次溢出。零异常的 14,468 次和至少 100 点的 3,326 次没有溢出。十次中的四次来自世界 21。少点梯度数值风险有集中性，但它们只占全部访问的 0.035%，且主要训练探针继续改善；现有证据不能将 125 的整体排序变化归给这些跳步。

检测与基础支持项按类别出现比例归一化：正常出现比例为 1，异常为 0.491994382。正常点多不等于其梯度必然占主导，名义类别平衡也不等于实际梯度相等。最终固定训练探针的困难正常集合共 387,226 次入选，涉及 301,506 个不同原始正常点；一个原始点在本批探针中最多入选五次。这个统计通过源帧与原始槽位跨世界去重，确认了重复强调，但不代表它在全训练中一共只出现五次。

### 零更新梯度没有证明某项辅助损失普遍有害

反向诊断仅使用八个预定 206 窗口：六个含异常、两个整窗正常。两个检查点各八次前向图，四项损失乘实际系数后分别求梯度，共 64 份梯度；同一图复用，不调用优化器。计算沿用原训练混合精度、单精度头和损失，并使用检查点损失缩放后将返回梯度还原。临时模型的归一化统计在每窗后恢复，正式参数的 `.grad` 不积累，随机状态恢复。该接口返回梯度而不累积到叶子张量的通常用法见 [PyTorch 文档](https://docs.pytorch.org/docs/2.12/generated/torch.autograd.grad.html)。

共同参数块包括输入嵌入、编码器、解码器、融合层、原型、接受基准及修正分支。不连接、零范数的组合保留空余弦，没有强行填零。下表展示最终节点同一融合参数块上的实际比值和方向；比值先在同窗计算，没有用两组中位数相除。

| 训练世界／当前帧 | 类型 | 困难正常／检测梯度范数 | 语义／检测梯度范数 | 检测与困难正常梯度余弦 |
| --- | --- | ---: | ---: | ---: |
| 0／113 | 正常 | 19.431 | 15.949 | +0.989 |
| 8／55 | 异常 | 0.005 | 0.004 | −0.354 |
| 11／68 | 异常 | 9.196 | 5.956 | −0.071 |
| 19／147 | 异常 | 0.073 | 0.050 | −0.884 |
| 20／95 | 异常 | 20.873 | 22.789 | +0.887 |
| 22／155 | 异常 | 2.990 | 9.767 | −0.192 |
| 29／196 | 异常 | 0.311 | 0.097 | −0.957 |
| 31／268 | 正常 | 16.556 | 62.293 | +0.937 |

融合层检测与困难正常余弦的中位数从中期 +0.608 变为最终 −0.132；最终编码器对应中位数仍为 +0.523。方向随层、窗口和困难程度变化，强负余弦有时对应很小的尾部梯度。**没有观测到足以据此直接删除某项损失的持续、统一冲突。** 所有受检梯度有限，忽略点没有直接监督梯度。201 和真实验证没有参与反向计算。

## 六、监控既有覆盖不足，也有目标取舍

原 152 个真实合格监控帧包括 4,964 个异常点，占全部异常点的 5.673%。125 在监控中占异常点的 20.790%，在全部评价中占 29.567%。第 133–165 帧的 33 个退化帧仅有一个进入监控，含 283 个异常点；该区间全部 8,373 个异常点最终贡献 9.092853 个 AP 失分百分点、5,287 个低误报漏检，占全体漏检的 46.357%。

监控确实低估了关键连续区间的覆盖。同时，四次修正监控本身已经出现 AP 与高召回性能的取舍：

| 访问节点 | 修正监控 AP（%） | 修正监控 FPR95（%） |
| --- | ---: | ---: |
| 7,120 | 53.874663 | 18.061221 |
| 14,240 | 63.890063 | 7.940882 |
| 21,360 | 68.071732 | 16.117430 |
| 28,480 | 71.575610 | 10.374513 |

AP 优先规则选择最终节点符合原约定，不是选模实现错误。但这项规则没有保证同时接近 1% FPR95。即使补足 125 监控帧，只要选择目标仍只优先 AP，这种取舍仍可能发生。本轮没有追溯改写选择结果，也没有把中期全量 AP 放进四个监控 AP 排名。

## 七、证据等级与唯一主改进建议

| 问题 | 证据与范围 | 证据等级 | 与双目标的关系 | 建议修改 | 风险与未排除解释 |
| --- | --- | --- | --- | --- | --- |
| 125 排序退化，142 持续低分 | 原始完整预测；125 占 68.072% AP 失分，两序列占 92.109% 低误报漏检 | 确定现象 | 同时限制 AP 与低误报召回 | 优先保护这类异常相对正常端的排序 | 失分归属不等于因果贡献 |
| 困难异常越来越被正常组高支持接纳 | 125 同点基础分数平均下降 8.476，修正下降 1.347；训练同点改善 | 确定现象；正常边界迁移不足是有支持的机制解释 | 为检出这些点，正常误报扩散到多条序列 | 优先检验独立异常表示和输出 | 原型、语义或数据的训练原因尚未区分 |
| 正常语义与原型的有效覆盖有限 | 单一训练道路；无监督组；第二遍 16／18 组单原型使用超过 99% | 确定现象 | 可能限制未知结构边界的泛化 | 保留辅助正常知识，暂不增加原型或强制均衡 | 未完成对应训练干预 |
| 监控不足以约束高召回退化 | 关键 33 帧只覆盖一帧；AP 优先仍选中较高 FPR95 的模型 | 确定现象 | 直接影响候选选择与目标一致性 | 固定补充关键区间并同时使用 AP、低误报召回 | 属于开发反馈，不能充当独立测试 |
| 本次与历史前向超过容差 | 136／191 个共有参考比较超限；同进程检查一致 | 确定复现差异，来源仍未定位 | 限制极小分数变化和精确历史内部量的解释 | 保留原始证据与失败状态 | 不能放宽容差冒充一致 |

当前没有定位到会否定正式数据标签或评价公式的确定错误。全量失分与覆盖属于实测事实；正常支持与拒识目标之间的迁移问题有多项证据支持，但训练原因尚未被干预识别。背景、几何、强度、特征与优化目标共同变化仍可能提供替代解释。

**推荐下一版只建立一个“正常知识辅助、独立异常排序”的主模型，继续使用当前冻结 v2 数据。** 它集中改变最终评分头，不同时重建 v3 或更换骨干。

1. **改变最终异常输出的约束。** 继续使用完整五帧、5 厘米体素、36 维浅层、72 维解码和 9 维逐点输入。在这个共同输入后，正常分支保留现有融合、原型、接受基准及正常语义训练；异常分支使用自己的轻量逐点投影和单个无界 logit。它替换目前的有界修正输出，最终异常分数直接由该分支给出，不再被强制写成正常拒识加一个幅度不超过 2 的修正。检测损失和困难正常项监督该异常输出，基础支持与语义项继续监督正常分支。第一轮保持既有四项系数、采样、训练次数与骨干初始化规则，以避免把一批新变量混在一起。
2. **保留训练材料，固定新的开发判断。** 本次主要粗条件已有训练覆盖且能拟合，因此该项模型对照不要求新样本。保持原 464 窗监控内容，补入 125 第 133–165 帧中未覆盖的 32 个窗口，形成同一份固定开发视图；旧候选与新候选都在该视图评价，原实验成绩不覆盖。新增连续区间单列描述，不能把有意增加的重点窗视图称为对完整真实分布的无偏估计。
3. **将有效性明确写成两个目标。** 下一轮预先规定 AP、FPR95 与 1% 误报下召回率共同用于判断，保留不被其他候选在 AP 与 FPR95 上同时优于的候选。若有达到 FPR95 不超过 1% 的候选，在这些候选中按 AP 选择；若没有，先以 1% 误报下召回率选择，AP 用于同召回的比较，同时如实标记双目标未达成。全量评价时，AP 超过当前最终 66.223547%、FPR95 低于当前中期 4.250239%，才表述为超过已有两种取舍的双指标开发收益；达到 80%／1% 才表述为完成目标。正常阶段、142 和少点帧仍须完整报告。

这项设计的依据是当前困难异常沿正常支持路径被接纳，而幅度有限的同表征修正未恢复区分。它并非因为梯度表“证明了语义损失有害”，也不是把推理时删除修正当成训练消融。独立分支可能降低对正常结构的利用、增加误报，或者仍学到相同的训练域线索；共享骨干也仍允许辅助目标影响异常表示。若受控训练后 125 的低误报漏检、完整 AP 和 FPR95 没有共同改善，应否定这项具体改进判断，而不是追加轮数宣称尚未收敛。

暂不优先改动强度通道、原型数量、初始体素大小、五帧输入或困难正常项系数。本轮没有相应训练干预证据。背景接近和道路多样性是保留的数据限制，后续如果评分头对照不能改善，再由残余错误决定需要补什么观测；本次没有把所有分布差异同时变成必做生成任务。

## 八、数值边界、执行情况和可复算入口

### 已通过的范围与未通过的范围

四个检查点各 251 个状态张量、其中 26 个归一化状态张量均有限。两个主要模型分别在一个正常 201 和一个真实共同窗口上，完成冷缓存、混合精度生成表后评价、诊断开关、32,768 与 65,536 点分块、正常和真实交替调用检查，共 20 个比较记录、24 次前向。全部比较的最大绝对差为 0，模型参数和缓冲区不变。

但与本轮之前保存的完整预测比较，191 个有参考的模型×窗口中，136 个至少一个分数超过原来的绝对容差 $10^{-6}$ 加相对容差 $10^{-5}$。最大全窗口绝对差为 0.0460023，最大逐帧 AP 变化为 0.0387717 个百分点。所有共有探针在两个全局工作点上的异常分类变化均为零；正常点在低误报、高召回阈值下分别变化 53、217 次，其中中期为 4、11，最终为 49、206。

针对首先触发差异的合成窗口，另外检查了原评价入口、参数是否需要梯度、原加载顺序、正常窗口预热和扩大位置编码表前缀；它们没有解释差异。检查点、当前槽位和体素数量相同。没有降低容差，没有把失败标成通过，也没有在新旧分数中选较好的一个。检查输出保存在 `checks/` 与 `reproduction.csv`。PyTorch 对不同执行环境与非确定性算法的复现边界有明确说明，但这一通用说明**不能替代本次差异的具体原因**；参见 [PyTorch 2.12 复现说明](https://docs.pytorch.org/docs/2.12/notes/randomness.html)。

原始全量 AP 及本报告全局失分、工作点计数来自已保存预测，并已按原始槽位复算一致。因此没有证据需要推翻这部分结果。内部前向比较使用统一的本次路径，125 约 9.823 的平均分数变化远大于已观察的参考差异；但有限探针不能给未检查窗口提供差异上界，也不能把 0.0388 个百分点当成普适误差范围。本报告不依据这一级小差异评价改进。

### 本轮实际执行及修正

执行顺序为读取原始完整预测并定位失分、补充正式 v2 关键覆盖、固定探针、数值先导、完成两模型内部前向、整理完整训练日志、训练域零更新梯度，最后汇总报告。样本选择没有根据本次内部结果反复替换。

诊断程序在执行中修正了元数据标量读取、诊断 JSON 写入及 NumPy 整数序列化问题；这些问题属于新增统计程序，没有改变原模型或数据。首次训练探针在序列化阶段失败后重试，首个合成验证探针在检查历史预测差异时停止后按保留超限记录的方式继续，两次涉及四次常规前向重试。失败产物保留在 `checks/serialization_failure/`、`checks/saved_reproduction/`。完成数是 193 个唯一探针、386 份常规模型输出，**386 不包含重试、数值检查及零更新梯度前向，不能称为本轮总前向次数**。没有通过增加独立探针突破 193 窗范围。

原始正常点去重统计在汇总前改为所有世界实际存活槽位的并集，并重新完成两侧有界覆盖统计；冻结训练监督次数仍完全对应。初次验证读取曾触及 8 GiB 进程主存限制，在已完整保存 72 个探针后停止；将验证源扫描缓存从整序列缩至五帧后继续，没有重做已完成探针。恢复后结束采样的进程主存约 3.27 GB，预留显存约 4.04 GB；这两个结束采样值不冒充整个诊断峰值。

运行前与过程中均从 Windows 查询 E 盘，使用实际 10 GB 保留约束；排序使用有界临时文件，预测逐窗保存，不导出 PLY。CPU 点云及统计按任务使用六线程，数值库单线程；模型前向顺序执行，未启动其他正式训练争抢显卡。所有正式检查点、原始预测与冻结样本保持原样。

本轮对原评价器只增加完整并列阈值的观察回调和官方高召回点的计数输出，没有另写一套 AP 公式。评价相关 27 项测试通过，包括并列分数、共线节点与失分守恒；代码静态检查通过。这些检查支撑实现，科学结论仍依赖上述真实数据和预测。

### 产物与复算方式

完整数值证据集中在 `runs/diagnostics/nre/system/`，报告源文档为仓库中的 `DIAGNOSIS.md`。小体积报告和三份图件发布在 `reports/`；本地诊断目录中的 `report.pdf` 及图件名称指向同一份文件，不重复保存。中文字体使用宋体，英文使用 Times New Roman；图件与报告均检查实际 PDF 嵌入字体。

| 文件 | 内容与统计单位 |
| --- | --- |
| `global.json`、`errors.csv`、`frames.csv` | 两模型完整工作点；各互斥分组失分；全部 8,659 帧状态计数，缺少中期预测的格子为空；465 个异常全在范围外的帧只保留状态与点数 |
| `curve_mid.csv`、`curve_late.csv` | 完整异常分数跳点及额外实际阈值；精确指标仍使用全部并列分数组 |
| `probes.csv`、`probes.json` | 193 个窗口的世界、源帧、随机种子、基础／重点身份与选择原因 |
| `coverage.csv`、`coverage_groups.csv`、`normal_groups.csv` | 正式 v2 与复用真实画像的关键条件、世界覆盖、原始点与窗口观测次数 |
| `semantics.csv`、`detail_notes.json` | 受检实际回波的语义检查与相邻窗口来源检查；缺失和解释边界 |
| `scores.csv`、`paired.csv`、`probe_metrics.csv` | 各范围分数、修正分位数、阈值跨越、同点变化及探针指标 |
| `parameters.csv`、`prototype_similarity.csv`、`usage.csv` | 四节点原型／接受基准，以及探针最支持组的原型使用 |
| `layers.csv`、`fusion.csv` | 实际各层映射的成员混合与有限特征描述 |
| `confusion.csv`、`normal_class_errors.csv` | 正常细语义混淆；原始正常 201 按真值组的误报数量与分母 |
| `false_positive_groups.csv`、`movable_instances.csv` | 探针误报点的模型支持组；正常 201 有实例号的观测散布 |
| `training.csv`、`training_usage.csv`、`overflows.csv` | 有效训练轨迹、真值正常组原型使用及十次跳步 |
| `losses.csv`、`tail_repeat_*.csv`、`gradient_*.csv` | 固定训练损失、重复困难正常原始点、实际加权梯度 |
| `monitor_coverage.csv`、`condition_metrics.csv` | 监控覆盖和主要区间；基础探针逐世界及条件成绩 |
| `numerics.csv`、`reproduction.csv`、`checks/` | 同进程检查、跨运行容差失败及保留证据 |
| `predictions/`、`gradients/` | 两模型所有常规探针和训练模式梯度前向的完整输入点分数 |
| `mappings/`、`features/` | 逐点真实层映射；有固定身份的有限高维特征样本 |
| `curves.pdf`、`sequence125.pdf`、`paths.pdf` | 三份必要图件 |

CSV 中空值表示不适用或没有有效观测，实测零保留为零；五位可见模式需按文本读取。各分位数保留其原统计范围，没有平均逐窗分位数来冒充全部点分位数。表格不依赖真实细语义猜测，也没有添加不可观测物理属性。

本地复算入口为 `python -m src.diagnose`。依次使用 `scores`、`coverage`、`select`、`numerics`、`probes`、`summarize`、`history`、`gradients`、`details`、`report`；`errors` 复用既有全局排序摘要重新做失分归属。默认数据路径为 `/home/jasongao/Data/STU`，也可由 `--data-root` 指定。`probes` 可以按已经完整提交的窗口恢复，正式模型与数据不写回。报告和图件可单独由 `report` 阶段重建，不需要模型前向。

本报告属于真实开发集上的回顾性诊断。世界与相邻帧相关，重点探针存在主动选择，数据条件没有完全匹配；没有将点数当独立样本数做显著性检验。已经确认失分位置、内部前向变化、训练覆盖与监控取舍；尚未唯一识别训练原因，也没有证明建议模型能够提高成绩。下一项真正改变判断的工作是固定材料和预算的单个评分头训练对照，本轮没有启动。

## 附表：全部序列的全局失分与工作点错误

每列均是同一模型全局阈值下的贡献，不是逐序列最佳阈值。箭头左为中期、右为最终。AP 失分的单位为百分点。

| 序列 | 合格帧 | 异常点 | AP 失分 | 低误报漏检 | 高召回误报 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 125 | 210 | 25,871 | 16.990 → 22.992 | 3,431 → 8,975 | 1,698,692 → 4,218,393 |
| 137 | 46 | 749 | 0.641 → 0.391 | 298 → 124 | 114,570 → 275,761 |
| 138 | 83 | 2,254 | 0.392 → 0.164 | 3 → 8 | 281,965 → 661,228 |
| 139 | 99 | 2,844 | 0.682 → 0.703 | 7 → 49 | 366,475 → 906,889 |
| 140 | 75 | 1,465 | 0.211 → 0.070 | 1 → 0 | 234,829 → 572,157 |
| 141 | 307 | 18,389 | 5.065 → 2.366 | 188 → 117 | 1,120,563 → 3,045,104 |
| 142 | 87 | 1,596 | 1.808 → 1.803 | 1,546 → 1,530 | 263,803 → 639,698 |
| 143 | 91 | 3,085 | 0.727 → 0.585 | 122 → 191 | 327,149 → 746,715 |
| 144 | 79 | 4,508 | 1.483 → 1.448 | 165 → 178 | 242,753 → 513,519 |
| 145 | 92 | 3,712 | 0.866 → 0.456 | 0 → 0 | 300,983 → 657,306 |
| 146 | 57 | 1,455 | 0.410 → 0.214 | 25 → 11 | 190,190 → 430,398 |
| 147 | 111 | 2,811 | 0.371 → 0.155 | 46 → 8 | 334,386 → 797,481 |
| 148 | 115 | 3,050 | 0.453 → 0.163 | 1 → 6 | 380,955 → 908,883 |
| 149 | 102 | 2,800 | 0.552 → 0.224 | 6 → 0 | 337,866 → 807,604 |
| 150 | 156 | 5,344 | 1.078 → 0.548 | 13 → 15 | 581,881 → 1,488,272 |
| 151 | 51 | 1,054 | 0.175 → 0.069 | 3 → 0 | 189,373 → 380,358 |
| 152 | 61 | 1,029 | 0.462 → 0.233 | 83 → 44 | 182,999 → 409,546 |
| 153 | 75 | 3,149 | 0.443 → 0.199 | 4 → 1 | 273,156 → 673,186 |
| 169 | 63 | 2,334 | 1.329 → 0.994 | 136 → 148 | 814,056 → 1,673,172 |
| 合计 | 1,960 | 87,499 | 34.140 → 33.776 | 6,078 → 11,405 | 8,236,644 → 19,805,670 |

点数采用当前帧官方范围内数量，距离为这些异常点的距离中位数。距离左闭右开，末档包含 50 米。下表包括全部真实官方合格帧，因此 20–99 点、10–20 米比完整五帧画像多四个启动帧。

| 异常点数／距离（米） | 帧／序列数 | 异常点 | AP 失分，中期→最终 | 低误报漏检，中期→最终 |
| --- | ---: | ---: | ---: | ---: |
| 5–19／2.5–10 | 56／11 | 749 | 0.591929 → 0.502511 | 409 → 363 |
| 5–19／10–20 | 455／18 | 5,055 | 1.586903 → 1.105663 | 411 → 442 |
| 5–19／20–35 | 410／16 | 3,544 | 1.582596 → 1.173191 | 62 → 140 |
| 5–19／35–50 | 54／4 | 357 | 0.275022 → 0.303238 | 33 → 48 |
| 20–99／2.5–10 | 371／19 | 19,394 | 5.467085 → 3.584355 | 1,346 → 1,058 |
| 20–99／10–20 | 307／14 | 10,647 | 4.060574 → 4.083909 | 435 → 825 |
| 20–99／20–35 | 55／1 | 1,261 | 0.656405 → 0.370268 | 3 → 34 |
| 20–99／35–50 | 0／0 | 0 | 无观测 | 无观测 |
| 100–499／2.5–10 | 247／14 | 45,876 | 19.803096 → 22.620366 | 3,377 → 8,494 |
| 100–499／10–20 | 5／1 | 616 | 0.116111 → 0.032951 | 2 → 1 |
| 100–499／20–35 | 0／0 | 0 | 无观测 | 无观测 |
| 100–499／35–50 | 0／0 | 0 | 无观测 | 无观测 |
| 至少 500／2.5–10 | 0／0 | 0 | 无观测 | 无观测 |
| 至少 500／10–20 | 0／0 | 0 | 无观测 | 无观测 |
| 至少 500／20–35 | 0／0 | 0 | 无观测 | 无观测 |
| 至少 500／35–50 | 0／0 | 0 | 无观测 | 无观测 |
