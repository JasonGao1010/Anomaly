# AJAE V4 总方案（冻结版）

**版本：AJAE-V4-F240-R1　｜　冻结日期：2026-09-18**

**主线：复用现有 240 个 206 合成世界，以全部合格完整扫描建立基础分割能力，再在同一训练池上学习采样特征与位置上下文的条件交互。全程保持原始传感器朝向。**

| 项目 | 冻结决定 |
| --- | --- |
| 任务 | 完整单帧 LiDAR 输入，原始真实回波逐点正常／异常分割 |
| 数据 | 现有 240 个 206 合成世界；每个世界的全部合格帧进入同一训练池 |
| 帧筛选 | 原始点三维距离 2.5–50 米、标签有效；有效异常点整帧合计至少 5 点 |
| 输入视角 | 原始传感器坐标与朝向；保留完整扫描；关闭全部几何与强度数据增强 |
| 模型 | LitePT-S 共享骨干＋64 维逐点细节＋五尺度、四头条件交互 |
| 监督 | 两阶段均使用同一个类别平衡二分类损失，正常与异常各占 1/2 |
| 训练 | 第一阶段 50 轮；第二阶段 20 轮；每轮完整遍历同一训练池 |
| 有效批量 | 8 帧；固定微批次 1 帧、累积 8 次，末批按实际帧数计算 |
| 选模 | 真实 STU 官方验证集 AP 优先；同一检查点报告 AP、AUROC、FPR95 |
| 重复实验 | 随机种子 0、1、2；种子 0 为主运行，报告三次结果与均值、标准差 |

## 第一部分　开题

### 1.1 研究问题与方法定位

STU 的早期基线实验发现，未知障碍会被高置信度地判为正常结构，小物体及远距观测也更难识别。V4 关注**未知障碍与正常结构的混淆，以及提高异常检出时的正常误报**。[STU 论文](https://arxiv.org/html/2505.02148v1)

研究假设是：当局部回波不足以稳定区分类别时，当前扫描中的位置与周围结构可能提供额外依据。由采样特征和位置共同决定如何读取上下文，有机会提高逐点判别能力。低矮、稀疏、远距和背景变化程度用于描述数据与分析结果，不分别设立专项优化目标。

已有方法已涉及采样一致性、位置编码与上下文利用。因此，V4 的研究主张落在**条件交互是否改善实际混淆，以及是否优于相同信息下的常规融合**，不宣称首次使用“采样＋上下文”。局部观测是否容易混淆、上下文是否有效，均由模型结果和对照实验判断，不作为人工筛选样本的前置条件。

### 1.2 研究目标

模型仅使用当前完整扫描，为每个真实回波输出一个异常分数，以可比设置下超越 COVAL 为目标，重点提高 STU 官方 AP、AUROC，降低 FPR95，并控制完整推理延迟和显存。

训练和评价采用相同的距离、标签及整帧筛选规则；这些规则不作为在线推理的准入条件。实际推理始终执行，不需要知道当前帧的真实异常点数。

### 1.3 方法依据与执行路线

| 参考 | 借鉴内容 | 本版落实 |
| --- | --- | --- |
| [LiON](https://arxiv.org/html/2309.10230v3)、[REAL](https://arxiv.org/html/2207.01452v1) | 合成异常暴露、直接学习已知与未知的区分 | 复用已有合成观测，端到端学习正常／异常分割 |
| [NDP](https://arxiv.org/html/2604.09232v2)、[LIDO](https://arxiv.org/html/2604.23604v1) | 异常评分偏差与特征区分能力 | 使用直接二分类监督，联合检验异常漏检和正常误报 |
| [COVAL](https://icml.cc/virtual/2026/poster/65016) | 利用物理观测和遮挡结构，训练后单帧推理 | 保留完整真实背景与既有遮挡结果，通过共享骨干学习空间关系 |

执行路线固定为：**同一数据池上的基础分割训练 → 继承完整模型并加入条件交互 → 真实验证集选模 → 固定对照与最终测试。** 不复制 COVAL 的反事实重建任务，也不把本版训练参数写成其论文设置。

## 第二部分　数据

### 2.1 固定训练池

训练源仅为已有 **240 个 206 合成世界**。一个“世界”是一个已有合成版本，包含其保存的逐帧完整扫描、标签和生成记录；240 个世界不等于 240 帧，也不等于 240 个独立真实场景。

对每个世界的全部保存帧执行第 2.2 节筛选，得到唯一训练清单 $D$，其大小记为 $N=|D|$。两个阶段均使用整个 $D$；每轮每个合格样本读取一次，跨轮重复学习。

本版不新增几何、位置或合成版本，不重新划定物体最小观测帧数，不按短片段截掉合格帧。原有三类关系、正常参照、点数与距离统计仅用于离线分析，不决定训练准入、抽样配额或损失。取消基础池／针对性池划分及其混合比例。

### 2.2 与官方一致的筛选

先将既有标签按其原有映射转换为统一三值：**忽略 0、正常 1、异常 2**。原始语义类别编号不能直接当成这三个值。以下统计均基于读取后的真实原始回波、传感器坐标及统一标签，在体素化和数值归一化之前计算。

设 $p_i$ 为真实回波的三维坐标，$y_i$ 为统一标签，则：

$$
m_i=\mathbf 1\!\left[2.5\leq\|p_i\|_2\leq50\right]
\mathbf 1[y_i\neq0],
\qquad
n_A=\sum_i m_i\mathbf 1[y_i=2].
$$

- **整帧准入：** $n_A\geq5$ 时保留，$n_A<5$ 时跳过整帧，包括零异常纯正常帧。
- **点级监督：** 合格帧内所有 $m_i=1$ 的正常与异常点均参与损失。
- **物体不另设门槛：** 两个异常分别有 3 点和 20 点时，整帧保留，两者均参与监督。
- **无额外筛选：** 不按高度、距离子区间、物体点数、背景变化、局部形态或人工上下文判断删除合格帧。

采用固定版本的 [STU 官方逐点评价实现](https://github.com/kumuji/stu_dataset/blob/8f0f09c2ca4bf7b665e0ae5919b4092ddae140a2/compute_point_level_ood.py)。正常与异常点的统计范围、距离端点和整帧规则与该实现对齐。

### 2.3 完整输入与原始朝向

**“正向学习”固定解释为保留 206 原有传感器坐标轴和观测朝向。** 不按异常所在方位重定向，不随机旋转、镜像、平移、缩放、抖动或混合扫描；也不裁成仅车前扇区。侧方、后方及其他真实回波均保留在完整扫描内。

模型输入包含全部真实回波的三维坐标和既有强度值。范围外及忽略标签点保留为上下文，但不产生监督。射线空槽位不视为点；其索引可供输出回填使用。

同一个样本在不同轮次具有相同的点、坐标、强度与标签。仅改变帧的读取顺序。体素化使用全部真实点进行聚合，并保留逆映射，不随机抽取每个体素中的一个点代替全部点。

### 2.4 清单、去重与观测复用

样本唯一标识为“合成世界 ID＋原始帧号”。同一标识的重复文件只保留一个入口；不同世界中的同一原始帧仍是不同训练样本，全部按规则保留。

固定清单记录世界 ID、206 原始帧号、点云和标签来源、真实点数、有效正常／异常点数及内容校验值。所有训练运行与对照共用此清单及其校验值，训练过程中不增删条目。

旧报告记录了 40,152 个合格扫描，可作为重建清单的核对数；**最终 $N$ 由上述固定池和官方规则机械确定**，不将旧表中的对象观测次数、配对数或重复路径直接当作 $N$。

物体世界位置、已有遮挡结果、背景回波和强度全部沿用保存结果。训练不重新渲染，不修改传感器响应或补设物理参数。

### 2.5 数据一致性

读取时核查点与标签一一对应、坐标单位为米、坐标系一致、数值有限及真实回波与空槽位分离。损坏或无法对齐的数据须报错处理，不静默删帧或猜测标签。

世界 ID、帧号、物体身份、正常参照、轨迹、位姿、生成类别及标签均不进入模型特征。位姿只用于必要的数据解码和离线核查，推理不累积多帧。

### 2.6 训练与评价隔离

240 个世界的全部合格帧用于训练，不从 206 内随机划出“独立验证集”。这些世界共享同一背景，增加的是合成配置与观测样本数量。

使用真实 **STU 官方验证集**逐轮评价和选模，验证数据不加入训练、不用于生成或筛选合成样本。最终测试集仅在训练及选模规则固定后评价。已有其他合成序列不加入本版训练或主选模流程。

## 第三部分　模型

### 3.1 总体结构

冻结结构为 **逐点细节支路＋共享 LitePT-S 编解码器＋条件交互分割头**。只运行一个骨干，不使用额外语义模型、物体检测器、手工异常规则或跨帧模块。

第一阶段训练细节支路、骨干和基础分割头；第二阶段继承全部参数，增加一次五尺度条件交互，所有参数共同更新。两个阶段都直接学习同一正常／异常任务。

### 3.2 输入编码与逐点细节

坐标使用米，强度 $I_i$ 使用既有保存值，不做按帧归一化或按正常／异常身份区分的变换。基础体素边长 $h=0.05$ 米，以传感器原点定义网格；稀疏算子所需的整数索引平移不改变连续坐标与朝向。

以点坐标、强度与体素内偏移组成七维输入：

$$
u_i=\left[p_i/50,\ I_i,\ (p_i-v_i)/h\right],
\qquad
 e_i=\operatorname{MLP}_{7\to64\to64}(u_i),
$$

其中 $v_i$ 为该点所在基础体素的几何中心。新增 MLP 除分割头外，隐藏层统一使用 LayerNorm（$\epsilon=10^{-5}$）与 GELU，末层为线性层；分割头隐藏层仅使用 GELU，与第 3.5 节公式一致。不额外加入 BatchNorm 或 Dropout，骨干原有结构保留。

细节进入骨干采用残差适配：每个体素的平均坐标与平均强度组成四维基础特征，经原有骨干嵌入；同时将该体素全部 $e_i$ 的均值与最大值拼接，经 **128→64→36** 的适配器加到 36 维嵌入输出。适配器末层权重与偏置初始化为零，以保留预训练入口并逐步接纳逐点细节。

全部 $e_i$ 另行保留。预测与损失始终回到真实原始点，不以体素多数标签合并正常与异常。

### 3.3 骨干与两类表示

采用官方 **LitePT-S nuScenes** 结构和语义预训练检查点。保留兼容的四维入口、编码器与解码器权重，移除原语义分类层；新增细节支路、适配器及二分类头从头初始化。全部骨干参数参与训练。

| 配置 | 冻结值 |
| --- | --- |
| 编码层通道 | 36、72、144、252、504 |
| 编码层深度 | 2、2、2、6、2 |
| 层间步幅 | 2、2、2、2；对应网格尺度 5、10、20、40、80 厘米 |
| 局部与上下文计算 | 前三层稀疏卷积，后两层序列化窗口注意力 |
| 编码头数 | 2、4、8、14、28；注意力窗口上限 1024 |
| 解码器 | 通道 72、72、144、252，额外块深度全为 0；细尺度输出 72 维 |
| 骨干正则化 | DropPath 最大值 0.3；注意力与投影 Dropout 均为 0 |
| 序列化 | 固定使用 z、z-trans、hilbert、hilbert-trans 的既定顺序；关闭顺序随机打乱 |
| 其余骨干设置 | 沿用下文固定提交的官方 nuScenes 配置 |

上述序列化只决定计算排列，不旋转或镜像点云坐标。骨干依据为 [LitePT 官方配置](https://github.com/prs-eth/LitePT/blob/436d04801c8151faebe66a1b2d368a9711e7e6aa/configs/nuscenes/semseg-litept-small-v1m1.py)。

- **采样表示 $s_i$：** 拼接 $e_i$ 的 64 维特征与第一级编码输出映射回该点的 36 维特征，经 **100→64→64** MLP 获得。
- **基础上下文 $c_i$：** 将解码器 72 维特征映射回原始点，经 **72→64** 线性层获得。
- **基础分割：** 拼接 $s_i,c_i$，通过 **128→64→1**、中间激活为 GELU 的 MLP 输出异常 logit。

第一级编码输出须取自解码前，避免将解码后的 72 维特征误当作上述 36 维特征。采样与上下文表示共享骨干，含有信息重叠，不宣称实现了两类因素的完全解耦。

### 3.4 五尺度条件交互

第二阶段新增 **一层、四头、总宽度 64** 的跨尺度注意力，每头 16 维。每点读取五个编码层中对应祖先体素的特征，使用骨干已生成的层级映射，无需重新搜索邻域。

1. **查询。** 将传感器距离与单位射线方向 $(\|p_i\|_2/50,\ p_i/\|p_i\|_2)$ 经 **4→32→64** MLP 编码，与 $s_i$ 拼接后线性映射 **128→64**，得到查询 $q_i$。
2. **键和值。** 五层编码特征分别从 36、72、144、252、504 维线性投影至 64 维，加入可学习的尺度嵌入并经 LayerNorm，再由共享线性层生成键和值。该步骤按体素计算一次并复用。
3. **相对位置。** 以点到该层体素代表坐标的相对位置除以该层网格边长，经共享 **3→16→4** MLP 生成四个注意力偏置；代表坐标使用骨干该层保留的实际几何坐标。
4. **交互输出。** 每个头在五个尺度上进行 softmax 加权，拼接后经 **64→64** 线性投影得到 $r_i$。新增注意力 Dropout 为 0，不叠加第二层交互或额外前馈块。

每头的权重为：

$$
\alpha_{i\ell}^{(a)}=
\operatorname{softmax}_{\ell}
\left(
\frac{\langle q_i^{(a)},k_{i\ell}^{(a)}\rangle}{\sqrt{16}}
+b_{i\ell}^{(a)}
\right),
\qquad \ell=1,\ldots,5.
$$

该模块让局部采样表示、传感器位置和周围结构共同决定上下文的使用方式。它不读取对象点数、参照曲线或人为上下文标签。

### 3.5 统一分割头与训练目标

两阶段使用同一个基础分割头。第二阶段仅扩展交互特征的输入：

$$
z_i=w^{\mathsf T}\operatorname{GELU}
\left(W_s s_i+W_c c_i+W_r r_i+b\right)+b_0.
$$

第一阶段无 $W_r r_i$ 项；其余参数来自基础 **128→64→1** 头。第二阶段继承它们，并将新增 $W_r\in\mathbb R^{64\times64}$ 初始化为零，因而在推理模式下，阶段切换的初始预测保持一致。随后全部模块共同更新。

**两个阶段只使用类别平衡的二分类损失：**

$$
\mathcal L_1=\mathcal L_2=\mathcal L_{\mathrm{seg}}
=\frac{1}{2|A|}\sum_{i\in A}\operatorname{softplus}(-z_i)
+\frac{1}{2|B|}\sum_{i\in B}\operatorname{softplus}(z_i).
$$

$A,B$ 分别为整个有效训练批次中、满足第 2.2 节掩码的异常与正常点集。每类内部按有效点数取均值，两类等权；若批次缺少某一类，仅对存在的类别计算均值，不发生除零。

不添加人工配对排序、对比损失、点数预测、曲线拟合或额外重建任务。联合学习由条件交互结构和完整扫描的正常／异常监督实现。损失平衡只作用于训练，官方评价保留原始点分布。

### 3.6 推理与效率

推理为一次完整扫描前向，输出原始点顺序下的异常 logit；分数越高越异常。不做逐帧分数归一化、阈值后处理、测试时增强或多模型集成。

体素键值投影复用，每点只与五个尺度交互，避免全点两两注意力。逐点编码与评分按最多 **65,536 点／块**处理，块之间不裁掉上下文，骨干只运行一次。训练时可对逐点块使用梯度重计算控制激活显存。

输出散射回原始存储顺序。若原始文件包含空射线槽位，空槽得分固定为 0 并保持忽略；所有真实点必须具有预测值。

运行效率在相同硬件、完整输入、批量 1 下测量，计入体素化、骨干、层级映射、交互和逐点输出，报告平均延迟、P95 延迟及峰值显存；磁盘读取单独统计。

## 第四部分　训练

### 4.1 两阶段流程

| 阶段 | 数据 | 模型与优化 |
| --- | --- | --- |
| 第一阶段：基础分割 | 全部 $D$，完整遍历 50 轮 | 加载 LitePT-S nuScenes 预训练权重，联合训练细节支路、骨干与基础分割头 |
| 第二阶段：条件交互 | 同一个 $D$，完整遍历 20 轮 | 继承第一阶段最佳检查点的全部参数和归一化状态，加入第 3.4 节交互模块；新建优化器与调度器，全部参数继续训练 |

两阶段采用相同完整点输入、监督掩码和分割损失。第二阶段不更换样本分布，不按旧参照关系重新选取帧，也不追加针对性数据。

### 4.2 遍历、批量与方向

每轮对固定清单进行一次由种子控制的无放回随机排列，所有条目恰好读取一次。打乱的是帧顺序；每个输入仍是原始朝向、原始完整扫描，不使用时序输入。

全局有效批量为 8，固定微批次 1 帧，单卡累积 8 个微批次后更新一次；多卡合计仍为 8 帧。末批不足 8 帧时按实际帧数与有效点数更新，不丢弃，也不重复填充样本。分布式读取同样避免采样器自动补齐产生额外重复。

在前向前，根据掩码统计整个有效批次的 $|A|,|B|$；每个微批次使用对应全局分母累计损失与梯度，多卡同步计数并正确处理梯度平均系数。骨干原有归一化按固定微批次设置正常训练；新增分支使用 LayerNorm。

**数据增强全部关闭：** 旋转、镜像、平移、缩放、坐标扰动、强度扰动、随机删点、随机裁剪、体素随机择点、跨扫描拼接均不启用。骨干 DropPath 仅作网络正则化，不改变输入观测。

### 4.3 冻结优化参数

| 设置 | 第一阶段 | 第二阶段 |
| --- | --- | --- |
| 优化器 | AdamW | AdamW，重置优化状态 |
| 已加载参数峰值学习率 | 预训练骨干全部兼容参数：$2\times10^{-4}$ | 第一阶段全部参数：$2\times10^{-5}$ |
| 新增参数峰值学习率 | 细节支路、残差适配、特征投影、基础分割头：$2\times10^{-3}$ | 条件交互模块及 $W_r$：$2\times10^{-4}$ |
| 动量与数值项 | $\beta=(0.9,0.999)$，$\epsilon=10^{-8}$ | 同左 |
| 权重衰减 | 0.005；偏置与归一化参数不衰减 | 同左 |
| 学习率调度 | 前 5% 参数更新线性预热，其后余弦衰减 | 本阶段重新计算预热与衰减 |
| 起止学习率 | 从峰值的 10% 预热至峰值，最后降至峰值的 1% | 同左 |
| 训练轮数 | 50 | 20 |
| 梯度裁剪 | 全局 L2 范数上限 1.0 | 同左 |
| 数值精度 | 支持时使用 BF16；否则 FP16＋动态梯度缩放；损失用 FP32，点计数用 int64；指标沿用官方数值实现 | 同左 |

设 $S=\lceil N/8\rceil$，则：

$$
U_1=50S,\qquad U_2=20S.
$$

第 $j$ 阶段预热步数为 $W_j=\lceil0.05U_j\rceil$。阶段首步使用峰值的 10%，第 $W_j$ 步达到峰值，此后余弦下降，最后一步到峰值的 1%。每个有效批次占一个计划调度步；混合精度溢出导致跳过参数更新时，记录该事件，该批次仍计入固定读取与调度预算，另行报告实际成功更新数。

50＋20 轮为固定执行预算，不开展学习率、轮数、混合比例或损失权重搜索，不因训练损失仍下降而追加轮数。优化形式参考 [LitePT 配置](https://github.com/prs-eth/LitePT/blob/436d04801c8151faebe66a1b2d368a9711e7e6aa/configs/nuscenes/semseg-litept-small-v1m1.py)与 [NDP 微调设置](https://arxiv.org/html/2604.09232v2#S6)；表中数值为 V4 的固定实验选择。

### 4.4 验证与检查点选择

每轮结束后，在完整真实 STU 官方验证集上执行一次无增强评价。使用原始点、官方标签映射和筛选规则，跨所有合格扫描累计有效点后统一计算指标，不逐帧或逐物体平均 AP。

固定选模顺序：**AP 更高优先；AP 完全相同时 FPR95 更低优先；再相同时 AUROC 更高优先；仍相同时选择更早检查点。** 使用未四舍五入的指标比较，三个指标必须来自同一检查点。FPR95 直接沿用固定官方脚本的实现，包括其 TPR 阈值处理。

第一阶段保存最佳与最后检查点，最佳检查点启动第二阶段。第二阶段训练前评估一次零初始化交互的模型，作为“第 0 轮”一并参与选模；随后完成全部 20 轮。若训练后没有检查点优于第 0 轮，保留该结果，并如实记录交互未获得验证收益。

三个种子各自按相同规则选模。种子 0 为固定主结果；同时报告种子 0、1、2 的独立指标及均值、总体标准差，不按测试表现挑种子，不集成三个模型。测试集不用于修改数据、参数或选模顺序。

### 4.5 固定方法对照

为区分额外训练量、更多上下文输入与条件交互的作用，固定以下两项必要对照。它们与主方法共用各自种子的第一阶段最佳检查点、第二阶段数据顺序、20 轮预算、分割损失和选模规则。

| 对照 | 固定改变 | 检验问题 |
| --- | --- | --- |
| 基础模型继续训练 | 不新增交互模块，原模型按第二阶段继承参数学习率继续训练 | 收益是否仅来自额外 20 轮训练 |
| 相同信息的常规融合 | 保留五尺度投影、尺度嵌入和位置编码；以拼接 MLP 代替注意力交互 | 条件交互是否优于直接使用同样的信息 |

常规融合输入为 $s_i$（64）、传感器位置编码（64）、五个尺度特征（320）和五组归一化相对位置（15），合计 **463 维**，通过 **463→64→64** MLP 产生 $r_i$；同样以零初始化 $W_r$ 接入原分割头。新增参数使用第二阶段新增参数学习率，报告其与主模型的参数量和计算开销差异。

对照为固定研究验证，不用于看到结果后重定义主方法。与 COVAL、NDP、LIDO 等已发表结果比较时，明确数据、预训练、骨干及计算预算差异；只有相同设置的复现实验才能归因于机制本身。

### 4.6 冻结与复现记录

执行前生成固定训练清单及校验值，记录已有 240 个世界的来源版本；保留所有合格帧，不以历史统计表代替当前文件索引。参数冻结不依赖某个预填的 $N$，$N$ 仅决定上述机械计算的更新数。

固定外部实现：

- **STU 评价：** 提交 8f0f09c2ca4bf7b665e0ae5919b4092ddae140a2。
- **LitePT 代码与配置：** 提交 436d04801c8151faebe66a1b2d368a9711e7e6aa。
- **预训练权重：** 官方模型库的 [nuScenes LitePT-S 最佳检查点](https://huggingface.co/prs-eth/LitePT/blob/a8e76e92efbb2061639f5c683968bc5d248ee002/nuscenes-semseg-litept-small-v1m1/model/model_best.pth)，模型库版本固定为 a8e76e92efbb2061639f5c683968bc5d248ee002，下载后另记录文件 SHA-256；不得静默换为其他数据集或从头训练。

保存数据清单与校验值、代码及依赖版本、权重加载明细、随机种子、完整训练配置、模型及优化器状态、调度位置和随机数状态。日志记录分割损失、实际更新数、有效点数、学习率、官方验证指标、耗时及峰值显存。

实现验收覆盖：官方掩码与整帧计数一致、完整回波均有输出、原始朝向保持、空槽不入网、阶段切换推理结果一致、梯度累积权重正确。数据错误或实现缺陷可以修复并记录；更换数据池、网络、损失或训练超参数须另立版本，不覆盖 F240-R1 的实验定义。
