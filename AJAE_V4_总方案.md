# AJAE V4 数据构建与训练验证

**当前执行版本：AJAE-V4-N1。更新日期：2026-09-21。**

**保留的诊断基线：原 C 权重＋FP32 旋转位置编码，正式完整验证 AP 78.06939%、FPR95 0.09832%、AUROC 99.95952%。当前最高 AP 候选为 81.63721%，来自 40 步局部加权诊断；此前 80.53251% 素材组作为未加权对照继续保留。** [基线正式评价](results/train/native/errors/precision_eval.json)、[未加权素材组](results/train/native/transfer/material/0/conditional/result.json)与[局部加权结果](results/train/native/transfer/local/0/conditional/result.json)分别保存。这次干预使可信训练局部、其他观测和两段真实道路的排序表现均改善，支持局部监督的迁移价值；尚不将该权重固化为正式训练规则，也不把全部剩余失分归因为监督不足。位置精度修正相对原 C 提高 AP 1.80989 个百分点。模型计算路径复查已覆盖全部 8,145 条现有训练／有效验证扫描的体素归并，并在 40 个完整扫描上核对实际下采样、注意力还原、解码恢复及条件交互索引；在这些检查范围内，未发现第二项错误归并或索引错配。

当前主线要求同时取得任务指标收益与跨场景、跨物体的泛化证据。固定修正后的 C 继续用于诊断，逐点分析用于提出一般性问题，暂停由单个验证案例直接决定正式配方。第 1,000 步 C 的权重及原结果 **AP 76.25949%、FPR95 0.13002%、AUROC 99.95131%**继续保留，权重位于 `results/train/native/branches/lr/0/conditional/best.pt`。此前逐点比较内部特征，并通过唯一改变位置编码运算精度的完整对照，确认了一项具体的信息损失：FP32 输入仍被自动混合精度将位置角度乘法降为 BF16。修正该运算后，配对诊断 AP 从 **76.25381% 提高到 78.06973%**，FPR95 降至 **0.09857%**，AUROC 提高到 **99.95955%**，全部权重保持不变。[完整数值对照](results/train/native/errors/precision.json)与[逐点特征值](results/train/native/errors/feature_values.json)保存原始证据；[异常逐项表](results/train/native/errors/attribution.csv)及[正常结构表](results/train/native/errors/normal_cases.csv)增加各案例的干预结果、支持解释、排除解释与仍缺证据。该结果确认了一个计算机制及其排序影响，尚未完成剩余失分在覆盖、训练使用及错误关联之间的逐项区分。

此前 27 个重点案例与通用困难取样对照没有完成上述归因。原取样终点 AP 为 75.79741%，困难取样为 73.94485%，均未超过 C，不再继续尝试取样比例。此前展开 N141:75、N125:30 的连续局部，检索范围达到两项合计 4.80802 个失分百分点，并完成 125 同几何未选观测检查及固定表示读出比较；这些证据的边界继续保留。141 的条件性拟合仍未启动，因为素材相关性尚未确认。位置精度修正一轮没有训练更新，没有改变训练清单、取样、损失或模型容量；正式推理中的旋转位置编码运算已恢复到 FP32，其他混合精度运算保持原设置。原 75.90827% 及历史 68.65256% 模型仍保留；下文区分各轮有效证据，P1／P2 为历史路线。

## 数据用途与正式方法的证据要求

**val19 已经反复参与模型选择、案例分析和相关训练素材选择，所有现有分数均属于开发结果。** 验证点没有参与梯度计算仍是事实，但不能据此排除模型选择对验证集的适应。80.53251% 与本次 81.63721% 都不能单独证明方法对独立场景和未知物体的泛化。模型选择本身可能过拟合有限验证样本，是已有研究明确区分于训练集过拟合的问题。[Cawley 与 Talbot，2010](https://www.jmlr.org/papers/v11/cawley10a.html)

本轮按实际清单与已有运行记录重新确认数据用途，没有重命名旧数据以制造“未见”状态。

| 数据 | 已有使用与暴露 | 后续用途及边界 |
| --- | --- | --- |
| 当前 6,185 条训练记录 | 已在异常任务中遍历；nuScenes 涉及 628 场景、46 日志，STU 为 206 | 训练与训练来源的机制检查；从中另取帧不自动成为未见背景 |
| 现有 350 条内部检查记录 | 162 条来自 4 个隔离日志的正常扫描；188 条来自 `proxy-012` 至 `proxy-014`，仍共享 206 背景。至少已由 `m500`、`m1547`、`b1547` 三个模型推理并用于分析 | 内部开发集；分别检查任务训练外正常背景与同背景新几何，不能作为最终未触碰留出集，也不足以检验跨背景异常识别 |
| val19 | 已反复用于开发和选模，官方规则下本轮包含 1,960 个有效扫描 | 保留历史开发结果；后续只在预先约定的重要节点评价 |
| 最终留出检查集 | 尚无符合新要求、用途和历史暴露已核清的独立清单 | 尚未建立；不能宣称已具备独立泛化证据 |

4 个检查日志与当前异常任务训练的 46 个日志没有重叠，但它们均属于 nuScenes 官方训练划分。固定版本的 [LitePT 预训练配置](https://github.com/prs-eth/LitePT/blob/436d04801c8151faebe66a1b2d368a9711e7e6aa/configs/nuscenes/semseg-litept-small-v1m1.py)明确使用 `split="train"`，[读取实现](https://github.com/prs-eth/LitePT/blob/436d04801c8151faebe66a1b2d368a9711e7e6aa/datasets/nuscenes.py)加载训练信息文件中的记录。因此，按公开训练配方应将这些背景视为预训练已暴露；作者没有公开本权重逐扫描访问清单，不能确认每帧实际访问次数，也不能把这项缺失反过来当作“未见”的证据。后续必须分别报告异常任务未见与预训练未见。

现有官方训练池的日志已经进入异常任务训练或内部开发，从其中换几个帧不能构造新的独立日志留出集。新实验开始前，应先确定训练、内部开发和最终留出的来源身份，并按完整采集日志及源几何划分；同一源物体的所有尺寸变体、渲染和轨迹观测归于同一侧。真正未用于开发的新来源才有资格承担最终留出检查。若重新划分的是已被当前权重学习的来源，就必须从不含该异常任务暴露的初始化重新训练，并如实保留人工作为开发资料使用及语义预训练接触的限制。本轮没有擅自改分组、重训或读取独立测试结果。

样本建设继续由任务定义和传感器观测规律决定，增加独立背景、源几何和多类可信正常困难结构，并让正常与异常都覆盖距离、方向、遮挡和响应变化。不得依据 val19 对象复刻形状、尺寸、位置或背景关系；6006 的 24 点只保留为本次诊断身份，不能成为正式方法的常驻特殊规则。正式训练应完整遍历确定后的合格训练池，同时分别报告独立扫描、独立采集日志、源几何及扫描访问次数；本次 40 步仅是机制诊断，不冒充完整训练预算。

模型的待验证假设是：局部采样存在歧义时，结合观测条件和周围结构可以改善正常与未知异常的区分。条件交互用于检验采样信息能否帮助选择上下文；其必要性必须在相同输入信息、数据及预算下，与不使用采样信息调节交互的重新训练对照比较。关闭已训练模块和查看注意力图不足以完成该验证。当前合理动机及开发收益不构成理论证明或全局最优架构证据。

每项正式修改采用以下四项证据要求。本轮只明确这些要求与现有缺口，不启动新的搜索或扩大训练预算。

| 要求 | 对局部监督候选的具体含义 |
| --- | --- |
| 任务依据 | 在训练来源核验正常标签和传感器观测，检查少量困难点在现有目标下是否持续未拟合；不按 val19 的相似程度决定纳入 |
| 普适机制 | 用同一可执行规则选择和加权多种可信困难结构，规则不得含特定记录、点编号或验证对象信息；是否由监督分配造成仍作为待检验假设 |
| 独立对照 | 内部开发用于日常选择；对照保持输入、初始化和预算一致，隔离采集日志及源几何，检查多个种子；最终留出承担阶段确认，val19 不承担日常搜索 |
| 完整记录 | 运行前记录候选学习率、损失权重、梯度尺度检查、遍历次数、预算和选模规则；保留失败结果及全部搜索成本，经验参数如实称为经验选择 |

已完成局部诊断仅使用预先确定的权重 1.0 和 40 步，没有额外搜索权重或根据中间 AP 选停。今后的搜索范围和预算需要在对应独立数据用途明确后预先确定，不能事后由 val19 最优结果倒写。论文应报告数据划分、超参数如何选择、波动来源与计算投入，这与 [NeurIPS 实验报告规范](https://neurips.cc/public/guides/PaperChecklist)一致。最终测试应在数据版本、模型、训练配方、权重选择规则及推理流程确定后执行；正式报告区分开发、阶段留出和独立测试结果。

## 当前双域数据

nuScenes 来自 628 个官方训练场景、46 个采集日志，每场景均匀选择八个关键帧；内部检查集对应的四个日志整体排除。按用户确定的比例，每场景四个原始正常观测、四个异常生成候选。八个候选没有有效异常回波，剔除后保留 5,016 个扫描。STU 只使用 206，保留 449 个原始正常扫描，并在既有 240 个合成世界中各取三个观测代表。选择依据为点数、距离、遮挡量和局部形态；先取接近中位观测者，再取与已选观测差异较大者。

| 来源 | 扫描数 | 每遍有效正常点 | 每遍有效异常点 |
| --- | ---: | ---: | ---: |
| nuScenes 原始正常 | 2,512 | 59,301,266 | 0 |
| nuScenes 合成异常 | 2,504 | 58,707,942 | 447,724 |
| STU 原始正常 | 449 | 54,631,044 | 0 |
| STU 合成异常 | 720 | 87,368,607 | 68,426 |
| 合计 | 6,185 | 260,008,859 | 516,150 |

[固定训练清单](results/data/native/train.json)与[逐条真实读取统计](results/data/native/census.json)保存输入身份和计数。扫描数最多的来源不必贡献最多有效点；损失仍按类别归一化，没有另加域权重。沿用已核验的标签转换，其他点保留为上下文并忽略监督；总有效异常为 1–4 的扫描剔除，合格扫描中单个物体的 1–4 点保留。没有单列针对性配额，也没有加入此前的边缘候选池或 STU 验证素材。本节描述固定基础清单，后文困难对照只改变其中记录的访问顺序与频率。

nuScenes 生成使用原生 32 线列顺序。邻近扫描位姿近似恢复扫描内运动，重复的运动补偿零深度坐标修正射线起点；已返回点使用实测方向，缺失回波方向由当前扫描的线束与列模式拟合。物体在每个场景的世界坐标中固定，四个异常帧联合观测同一组物体。接地依据原始地面支持、足迹范围和精细几何最低点，穿插检查使用八个选定扫描的已观测非地面点；联合最近表面决定遮挡，未产生物体回波时仍遮掉背后回波。

训练读取沿用原始非零坐标记录，并保留忽略点作为上下文；运动补偿后的零深度槽位在射线生成与遮挡判断中另行识别。因此，元数据中的输入非零槽位数量不能直接当作物理返回数量。这些零深度槽位不进入有效正常／异常点计数。

几何来源有 239 组既有程序形状参数，234 组通过当前接地与几何检查，实际放置使用 233 组。参数组数不自动等于去除所有对称与同构后的独立几何数。第一版尚未加入真实实例衍生几何或可信正常物体重渲染，不能声称已排除所有合成痕迹。

传感器响应从 46 个训练日志各一个扫描的平面内部观测拟合，包含 512,635 次观测机会和 508,543 次返回，按线束、距离、入射角统计返回率及强度分位数；支持不足的格子使用明确记录的汇总分布。[响应记录](results/data/native/response.json)保留来源和支持量。这是基于实测数据的近似响应，不能视为任意材质和未观测表面的真实物理标定。原生运动补偿建模参考 [NVIDIA NCore 的 nuScenes 转换说明](https://nvidia.github.io/ncore/conversions/nuscenes/nuscenes.html)。

在正式训练前已检查全部清单的真实读取与标签计数，并对六个场景的 24 个异常扫描增加射线求交精度进行独立复算。可见性及标签一致，未改动背景逐元素一致，坐标／强度最大绝对差为 4.7684×10⁻⁷。[复算记录](results/data/native/inspection.json)限定了检查范围；它验证数值求交与背景保留，不能单独证明真实传感器准确性。[实际回波图](results/data/native/examples.png)显示三个未按模型分数挑选的例子，局部窗口仅用于查看。对应 PDF 已确认嵌入宋体与 Times New Roman。

## 当前模型与配对训练目标

完整单帧仍输入同一个 LitePT-S 骨干。逐点坐标、强度与体素内位置提供采样细节；体素内汇总形成查询表示，五层编码特征和解码特征提供六个上下文。两层交叉注意力依据当前查询及可学习相对位置选择上下文，逐层更新统一的体素表示；恢复到每个点时再加入逐点细节与体素内位置，单个学习得到的评分头输出异常分数。共有 12,880,513 个参数。交互在体素层完成，同体素点共享上下文查询，逐点输出再保留细节差异；不能把它描述为每个原始点独立执行全场景注意力。

两组均重新初始化新增参数与优化器，加载 [LitePT 官方 nuScenes 预训练权重](https://huggingface.co/prs-eth/LitePT/blob/main/nuscenes-semseg-litept-small-v1m1/model/model_best.pth)的骨干部分，移除语义分类头。种子为 0，训练清单各完整排列两遍，顺序固定相同，共 12,370 帧次、1,547 次更新，最后一次更新含两帧。每两个完整扫描保留计算图，四对累积成通常的八帧有效批量；两组均采用此执行方式。没有整帧旋转或裁剪。

基础组使用按有效批次类别分别求均值的二元交叉熵损失。指标组使用

\[
L=L_{\mathrm{BCE}}+r(u)\left(L_{\mathrm{AP}}+0.1L_{\mathrm{AUC}}+0.1L_{95}\right),
\qquad r(u)=\min(1,\max(0,(u-0.1)/0.1)).
\]

进度为当前计划更新除以 1,547。前 10% 只使用分类损失，10%–20% 线性增加排序部分，之后保持完整权重。排序在原始异常分数上以单精度计算，平滑温度为 1。AP 使用平滑排名中的异常精确率平均值；AUROC 使用异常与正常分数差的平滑成对损失；高召回误报项先求训练异常点的平滑召回为 0.95 的阈值，再计算该阈值下的加权正常越阈概率。

每对扫描均匀选取至多 256 个异常锚点，全部异常点参与 AP 比较和召回阈值求解。正常点取最高分 512 个及其余正常点中无放回随机抽取 3,584 个，不足时全部保留；最高分点权重为 1，其余样本按候选数量除以抽取数量加权。AUROC 使用相同的均匀异常锚点与加权正常点；高召回误报使用全部异常点和加权正常点。两种正常均值均除以真实正常总数，AP 中的加权比值仍是近似。纯正常对只贡献分类损失，排序项按有效批次中的扫描对数量求平均，最后两帧只形成一对。

阈值通过单精度二分求根获得，反向使用隐式求导。若 \(q_p=\sigma((s_p-t)/\tau)\)，则

\[
\frac{\partial t}{\partial s_p}=\frac{q_p(1-q_p)}{\sum_{j\in P}q_j(1-q_j)}.
\]

因此正常点误报项既能降低高分正常点，也能通过阈值路径提高低分异常点。抽样使用由种子、更新编号及对内位置确定的独立随机数生成器，不消耗网络随机数流。每 25 步记录四项损失、渐增系数、抽样规模、纳入权重、阈值与实际平滑召回。

AP 代理的依据为 [Smooth-AP](https://www.robots.ox.ac.uk/~vgg/research/smooth-ap/)，其原始任务并非 STU。上述高召回损失是本实验自定义代理；[Cotter 等人的约束优化工作](https://proceedings.mlr.press/v98/cotter19a.html)提供相关方法背景，并未验证这里的具体式子。两帧排序不能等同于全验证集 AP；[ROADMAP](https://arxiv.org/abs/2110.01445)讨论了批次近似与不可分解排序指标之间的差距。梯度累积不会自动把四对改成一次八帧或全数据排序。

优化器沿用从公开权重开始训练时的 AdamW 设置：骨干峰值学习率 2×10⁻⁴，新增部分 2×10⁻³，前 78 步从峰值的 10% 预热，其后余弦下降，第 1,547 步达到峰值的 1%；权重衰减 0.005，梯度范数上限 1。两组设置完全一致。这不是从 R2 小学习率续训。分别在第 500、1,000、1,500、1,547 步完整评价同一 STU 验证集，AP 优先选模，同时报告固定终点、FPR95、AUROC 与实际耗时；任何训练代理阈值均不用于正式评价。

生成入口为 `python -m src.render --native --output results/data/native --workers 8`，合并清单入口为 `python -m src.data native --output results/data`。正式配对命令如下，使用已有 CUDA Python 环境，单卡顺序执行：

```bash
OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 /home/jasongao/Study/AJAE/.venv/bin/python -m src.train --recipe native --objective bce --train-manifest results/data/native/train.json --initial assets/nuscenes.pth --output results/train/native/bce --workers 4 --threads 2 --save-every 100
OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 /home/jasongao/Study/AJAE/.venv/bin/python -m src.train --recipe native --objective metrics --train-manifest results/data/native/train.json --initial assets/nuscenes.pth --output results/train/native/metrics --workers 4 --threads 2 --save-every 100
```

## 初始两组损失的实际结果

两组各执行 1,547 次成功更新，数值溢出均为零。每组恰好访问 6,185 条记录各两次，共 12,370 帧次，累计监督 520,017,718 个正常点次、1,032,300 个异常点次。四个验证区间的来源计数逐项相同；完整配置逐项比较只有 `objective` 不同，完整取样记录相同。每次评价均包含同一 1,960 个合格扫描和 193,879,969 个有效点。文件名 `epoch1.json` 至 `epoch4.json` 对应四个验证区间，数据本身只遍历两次。

| 更新次数 | BCE AP（%） | 组合 AP（%） | BCE FPR95（%） | 组合 FPR95（%） | BCE AUROC（%） | 组合 AUROC（%） |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 500 | 50.46899 | 75.90827 | 1.74513 | 0.23584 | 99.64591 | 99.91930 |
| 1,000 | 68.42421 | 71.14096 | 0.21331 | 0.21987 | 99.82610 | 99.91993 |
| 1,500 | 69.96067 | 71.57002 | 0.13183 | 0.16815 | 99.93239 | 99.93797 |
| 1,547，固定终点 | 70.17733 | 71.97329 | 0.12384 | 0.16406 | 99.93452 | 99.93956 |

固定终点比较时，组合组的 AP 高 1.79595 个百分点，FPR95 高 0.04022 个百分点，AUROC 高 0.00503 个百分点。按 AP 选模时，BCE 选择第 1,547 步，组合组选择第 500 步；组合组的 AP 高 5.73093 个百分点，FPR95 高 0.11200 个百分点，AUROC 低 0.01523 个百分点。两种比较都没有实现三项指标同步优于 BCE，不能因为加入了三个指标代理就声称三者均已改善。

初始两组对照当时选中 `results/train/native/metrics/0/conditional/best.pt`，本机保留其完整状态和最后状态。该权重实际经历 500 次更新、4,000 帧次；整轮实验仍执行了 1,547 次更新、12,370 帧次并进行了四次完整验证，选模成本不能省略。相对历史 68.65256% AP 最佳模型，所选组合模型的 AP 高 7.25570 个百分点，FPR95 低 0.33804 个百分点，AUROC 高 0.07877 个百分点；这项比较同时改变了数据、模型、初始化与损失，不能用它孤立解释任何一个因素。

| 实际耗时 | BCE | 组合损失 |
| --- | ---: | ---: |
| 训练循环，分钟 | 27.17 | 28.26 |
| 四次完整验证，分钟 | 12.03 | 12.71 |
| 全部区间含保存等开销，分钟 | 39.58 | 41.34 |

两个实验顺序运行，总计约 80.9 分钟。组合组的实测训练循环耗时约多 4.0%，包括保存等开销的区间总耗时约多 4.4%；这是本机本次运行的观察值，包含系统和内核预热差异。两组张量峰值显存约 9.99 GB，排序损失未增加训练中的完整扫描前向次数。

阈值的隐式梯度已通过独立双精度数值差分核对；共同平移全部分数时排序梯度和为零，两个独立扫描的计算图都能收到排序梯度，忽略点不参与损失。真实双帧反向传播包括清单中点数最多的一对 STU 扫描。训练中逐对检查平滑召回误差，定期日志记录的 164 个有效排序对中，最大误差为 1.66894×10⁻⁷；另有 56 个纯正常对按规则仅使用分类监督。日志中的对数只是每 25 步记录的样本数，不是全程排序调用总数。

配对有一个实际限制：在前 10% 仅使用 BCE 的阶段，两组的数值轨迹已经不同。例如，第 150 步损失分别为 0.49418616 与 0.28819019。最终检查点中的 Python、NumPy、PyTorch CPU 和 CUDA 随机状态均一致，配置与取样也一致，但这不意味着模型参数或逐步浮点运算相同。当前骨干调用 FlashAttention 时沿用默认的非确定性反向计算；本轮没有在中途更换实现，也没有单独排除所有可能的数值差异来源。[PyTorch 的复现说明](https://docs.pytorch.org/docs/2.12/notes/randomness.html)也区分了随机数控制和非确定性算法。该实验只有一个种子，不能估计训练方差，不能以此宣称统计显著或稳定复现。

这次初始运行显示：在相同数据、初始化构造、输入顺序及预算下，组合组取得了更高的观测 AP，最高值出现在第 500 步，其后没有维持该峰值。这是该次运行的曲线，后面的重放未复现同样的回落。上下文交互的独立作用、真实实例衍生几何、可信正常物体重渲染及独立测试表现仍未得到这些实验的证据。

[完整比较与核对记录](results/train/native/comparison.json)、[BCE 结果](results/train/native/bce/0/conditional/result.json)、[组合损失结果](results/train/native/metrics/0/conditional/result.json)保留原始精度指标、取样核对、检查点选择、随机状态核对和时间统计。没有将训练代理阈值用于正式评价，也没有将验证点送入损失。

## 固定检查点的排序与泛化诊断

本次重新推理组合第 500 步、组合第 1,547 步和 BCE 第 1,547 步，使用同一完整验证集：1,960 个扫描、193,792,470 个正常点、87,499 个异常点。每个预测保留扫描身份和原始点槽位。正式指标仍直接调用固定版本的官方实现；曲线分析另外按所有不同分数的实际取值分组，保留并列分数，不使用近似直方图，其 AP、FPR95、AUROC 与本次官方计算均在 10⁻⁹ 个百分点内一致。

重新推理的 AP 分别为 75.90754%、71.99035%、70.17770%，与原记录的差分别为 −0.00073、+0.01706、+0.00037 个百分点。原记录没有被覆盖。本节逐点比较全部使用本次保存的预测；这些小幅推理差异与后文训练重放差异是不同层面的现象。

比较阈值时，对每个模型重新寻找使全局召回率严格超过目标值的第一个完整分数组，分类条件为分数不低于该阈值，与官方 FPR95 的严格大于 0.95 规则一致。因为有并列分数，实际召回率不一定完全相等，结果中同时保存目标和实际召回率，未随机拆开并列分数。

| 目标召回率 | 组合 500 步误报点 | 组合 1,547 步误报点 | BCE 1,547 步误报点 |
| --- | ---: | ---: | ---: |
| 50% | 5,973 | 8,928 | 9,391 |
| 75% | 41,553 | 46,283 | 34,582 |
| 90% | 156,969 | 137,735 | 112,857 |
| 95% | 456,895 | 319,302 | 240,568 |
| 99% | 3,032,979 | 2,025,288 | 1,573,566 |

组合末步相对第 500 步，在召回率 0–25%、25%–50%、50%–75% 三段分别损失 2.04945、1.05980、1.27863 个 AP 百分点，共损失 4.38788；75%–100% 共增加 0.47069，净变化为 −3.91719。前段精度下降与高召回误报下降可以同时存在，AUROC 也不要求前段精度同步改善。每个召回区间按官方阶梯式 AP 积分拆分，区间贡献之和等于总体 AP。

在约 50% 召回率处，组合末步新增 5,651 个误报并修复 2,696 个误报，新增 5,239 个漏检并修复 5,172 个漏检；在约 95% 召回率处，对应为新增／修复误报 139,474／277,067，新增／修复漏检 1,430／1,439。误报和漏检的身份均来自同一扫描、同一点槽位，不按两个模型的原始分数升降判定好坏。

正常结构按原始语义标注分组。约 50% 召回率处，建筑标签的误报由 426 增至 3,872；约 95% 召回率处，道路标签的误报由 417,227 降至 279,687。抽查新增误报数最多的四个不同序列扫描，新增误报点到最近标注异常的距离中位数约为 4–26 米，不能沿用旧模型“候选几乎都在异常边缘”的解释。这里依据的是点标签和空间距离，未把建筑标签直接当成已人工确认的物理类别，也未认证全部标签无误。[空间检查](results/train/native/diagnostics/spatial.json)保存四例的原始槽位、类别计数和距离。

异常点按序列、原始实例号、距离和单帧实例回波数统计。实例号零单独列为未分配身份。总体 AP 中的逐点精度贡献，损失较大的标注身份包括 143:3、125:1、141:4 和 169:1；5–10 米异常点的贡献减少 4.31501 个百分点，其他距离合计有补偿。这些是对同一个全局 AP 的可加贡献，不是分别计算的物体 AP，不能据此单独判定某个物体内部排序的变化。125 在约 95% 全局召回处的漏检由 2,749 降至 2,301；142 的漏检由 501 降至 485，总体 AP 贡献反而增加 0.05726 个百分点。因此，不能继承旧模型对 125、142 的失败结论。

另在查看分数前，每个训练来源均匀选择 32 条记录，共固定 128 条；内部检查使用全部 162 个留出日志正常扫描及三个留出几何的 188 个扫描。内部检查仍来自官方训练来源；它们分别检查异常任务训练之外的正常背景，以及相同 206 背景上的新几何，不代表跨场景异常泛化，也不保证语义预训练未见过这些 nuScenes 日志。

| 同一固定子集中的 AP（%） | 组合 500 步 | 组合 1,547 步 | BCE 1,547 步 |
| --- | ---: | ---: | ---: |
| 128 条训练扫描 | 96.46720 | 98.80528 | 97.91824 |
| 188 条 206 留出几何扫描 | 65.21253 | 24.23949 | 23.22956 |
| 留出几何 proxy-012 | 80.38587 | 89.10849 | 75.76978 |
| 留出几何 proxy-013 | 0.68519 | 2.77178 | 1.95212 |
| 留出几何 proxy-014 | 78.19511 | 21.22562 | 22.83805 |

固定训练子集继续改善而部分留出几何退化，支持进一步检查泛化；三个留出几何的变化并不一致，退化主要集中于 proxy-014。不同子集的类别比例和难度不同，不能直接用其 AP 差当泛化差距；可信证据是同一子集上三个检查点的变化。纯正常 nuScenes 检查没有异常点，未为其虚构 AP 或 FPR95，而是记录各模型在自身 STU 95% 召回阈值处的误报：38,627、21,693、20,801 点，分母均为 4,188,321 个正常点。

[曲线与子集统计](results/train/native/diagnostics/curves.json)、[完整 PR 曲线](results/train/native/diagnostics/pr.pdf)、[分组统计](results/train/native/diagnostics/groups.csv)、[新增与修复错误](results/train/native/diagnostics/changes.csv)和[逐点定位例子](results/train/native/diagnostics/points.csv)提供复核入口。图中 `m500`、`m1547`、`b1547` 分别表示上述三个检查点，`replay`、`ap`、`lr` 表示下述 A、B、C。每个不同分数对应的 PR 台阶另存于相应 `*_pr.csv`，未下采样这些曲线。

## 梯度检查和同状态重放

固定使用原取样序列第 501、626、751、876 次更新的四个八帧批次，分别在三个检查点上计算梯度，不更新参数。每个批次前恢复该检查点的参数及缓冲区，并恢复共同的随机状态。除了最终分数，也测量条件交互输出的 64 维体素表示上的梯度；后者位于逐点评分头之前。

每个检查点的 16 个双帧组中有 13 个同时含正常与异常。对这些组，AP 与 AUC、FPR95 在分数和该共享表示处均未观察到负的梯度余弦。以下为共享表示梯度范数的中位数，已经乘以实际的损失权重与四组累积系数：

| 检查点 | BCE | AP | AUC 辅助项 | FPR95 辅助项 |
| --- | ---: | ---: | ---: | ---: |
| 组合 500 步 | 4.103×10⁻⁴ | 1.589×10⁻⁴ | 1.104×10⁻⁶ | 2.350×10⁻⁷ |
| 组合 1,547 步 | 4.178×10⁻⁵ | 1.771×10⁻⁴ | 1.688×10⁻⁷ | 4.822×10⁻¹² |
| BCE 1,547 步，以组合目标诊断 | 1.579×10⁻⁴ | 3.622×10⁻⁴ | 3.879×10⁻⁷ | 8.488×10⁻⁷ |

这些固定批次不支持“辅助项经常与 AP 对抗”的解释，也说明权重为 0.1 并不表示贡献为 AP 的十分之一。这只测量选定表示处的局部梯度，不能推出所有共享参数梯度均同向，也不能单凭范数认定辅助项无用。

以平滑比较值不大于 0.001 或不小于 0.999 定义饱和，组合 500／1,547 步的加权正常—异常比较饱和比例中位数为 99.374%／99.992%；最高分正常点部分也有 98.306%／99.607%。大量训练点已经容易区分，这是需要解释的训练状态，不自动等于实现错误。完整取样序列在排序启用后含 5,569 个双帧组，其中 1,268 个没有异常，占 22.7689%，按原规则仅贡献分类监督。[梯度记录](results/train/native/diagnostics/gradients.json)保存逐组数值及来源信息。

两次从完全相同第 500 步状态开始的八步重放，均使用第 501–508 步的 64 帧次、原优化器和调度。最终 PyTorch CPU／CUDA 随机状态相同，但参数最大绝对差为 0.00687154、均方根差为 8.25220×10⁻⁶；固定八帧预测的最大／平均绝对差为 0.59375／0.0128586。[重放记录](results/train/native/diagnostics/numeric.json)量化了数值敏感性，但没有估计完整验证 AP 的方差，也未隔离唯一的非确定性来源。

## 三个短分支的实际结果与下一项判断

三个分支均恢复原组合第 500 步的模型、优化器、随机状态以及第 4,000 帧次之后的取样位置，继续第 501–1,000 步。没有重新预热或重置优化器；学习率和损失渐增进度仍使用原 1,547 步总调度。A 保留原设置；B 仅把 AUC、FPR95 权重设为零；C 保留组合损失，仅把剩余学习率乘以 0.3。三个执行配置的源代码与依赖完全相同，B、C 相对 A 的设置差分别只有损失权重、学习率系数及分支名称。

每支新增 500 次成功更新和 4,000 帧次，无溢出；完整输入顺序、批内位置和来源点数相同。每支累计监督 168,783,373 个正常点次、338,277 个异常点次。三支最终 Python、NumPy、PyTorch CPU 与 CUDA 随机状态相同。每支仅在终点做一次完整验证，允许保留原 75.90827% 起点。

| 设置 | 固定终点 AP（%） | FPR95（%） | AUROC（%） | 允许保留起点的最佳 AP（%） | 裁剪次数／500 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 原组合 500 步，保留基准 | 75.90827 | 0.23584 | 99.91930 | 75.90827 | — |
| A：原设置重放 | 76.12569 | 0.13346 | 99.93757 | 76.12569 | 195 |
| B：BCE＋AP | 73.55010 | 0.14829 | 99.92111 | 75.90827 | 194 |
| C：剩余学习率乘 0.3 | **76.25949** | **0.13002** | **99.95131** | **76.25949** | 174 |

B 保留的起点仍是组合损失训练出来的模型，不能称为 BCE＋AP 独立训练的成绩。三个分支的训练、验证与区间保存共耗时 38.28 分钟，另有初始化和本轮诊断开销；训练循环分别为 554.06、561.43、557.23 秒，完整验证分别约 201.48、192.62、207.15 秒，张量峰值显存均约 9.51 GB。当前 C 模型的训练路径为原 4,000 帧次加本支 4,000 帧次，共 8,000 帧次；本轮三个分支实际支出 12,000 帧次，另有两次八步数值探针的 128 帧次，不能只报告被选中模型的成本。

最重要的结果是 A 的 AP 比原运行第 1,000 步的 71.14096% 高 4.98473 个百分点。其 20 个定期日志位置的类别计数、学习率、排序渐增系数与抽样数量规则都和历史记录一致。原来的回落没有在这次同状态重放中复现，不能据此稳定归因于辅助项或学习率。4.98473 是一次实际重放差异，不是置信区间，也不能当作通用的噪声阈值。

B 相对 A 的 AP 低 2.57558 个百分点，本轮不支持删除辅助项。C 比 A 高 0.13381，比原保留基准高 0.35123 个百分点，作为本轮数值最佳保留，但不足以认定较低学习率具有稳定收益。C 相对 A 在召回率 0–50% 增加约 1.39964 个 AP 百分点，在 50%–95% 减少约 1.35051，95%–100% 又增加约 0.08467。约 50% 召回处误报由 A 的 8,254 减至 C 的 5,793；75% 处反而由 34,116 增至 44,190；95% 处由 258,629 减至 251,965。当前结果不代表 C 在所有工作点均优于 A。

该轮曾将同状态数值重放列为首要后续工作，用户随后明确调整了优先级：固定 C，先解释其剩余错误，重放作为并行质量检查。当前仍保留组合目标、数据和模型，不据单次 0.13381 个百分点的差距认定较低学习率具有稳定收益。下面的困难取样对照回答既有训练观测的使用方式是否能改善 C；从公开预训练权重开始及增加独立种子的完整验证仍待执行。

[短分支总比较](results/train/native/branches/comparison.json)、[A](results/train/native/branches/replay/0/conditional/result.json)、[B](results/train/native/branches/ap/0/conditional/result.json)、[C](results/train/native/branches/lr/0/conditional/result.json)保留原始精度、时间及选模记录。`src/diagnose.py` 提供固定预测、曲线／错误分析、梯度与短重放入口；`src/train.py --branch replay|ap|lr` 在原训练入口内恢复这三个分支，`--score-path` 保存正式评价所用的原始分数，未增加推理分支或后处理。恢复逻辑已用独立的一步计算核对优化器步数、参数、随机状态、取样偏移和原调度；25 项相关训练检查通过。这些实现检查支撑上述真实实验，不替代泛化或因果证据。

## 历史重点案例、训练检索与困难取样对照

本节记录此前的 27 个重点案例及其后的取样实验，保留原实验的有效边界。它没有穷尽全部失分案例，不能替代下一节的完整排序清单，也没有建立数据或模型修改的充分原因证据。

本轮先用 C 正式评价时保存的逐点分数，在完整 1,960 个合格扫描中提取 2,993 个按标注分组的异常观测，以及 3,809 个包含中段召回误报的正常局部单元。异常身份由序列和原始标注实例号共同确定，不将它直接解释为经过独立核验的物理轨迹；其中一个未分配实例号的异常点单独统计，仍参与完整评价与 AP 分解。正常单元采用传感器坐标中边长 0.75 米的格子，只服务于定位和查看，模型输入始终为完整扫描。

[主要样本表](results/train/native/errors/cases.csv)列出 27 项：125、169、143 各三个主要漏检观测及两个同标注的成功参照；六个不同序列的高分正常局部单元；六个同扫描、相近距离的低分正常参照。[完整对象记录](results/train/native/errors/cases.json)保存原始点槽位、点数、距离、形态、强度、周围正常点和各召回工作点的错误数。[对照图](results/train/native/errors/cases.png)显示错误、成功参照和训练检索结果；灰色周围点仅在图中稀疏显示，推理输入未被裁剪。图中训练点沿用 C 的验证阈值仅作诊断展示，不是训练集正式评价。

| C 的固定观测 | 异常点数 | 距离中位数（米） | 约 75% 全局召回处漏检 | 同标注的成功参照 |
| --- | ---: | ---: | ---: | --- |
| 125／127，实例 1 | 316 | 5.24 | 316 | 125／156：250 点，4.38 米，漏检 0 |
| 169／365，实例 1 | 191 | 5.92 | 187 | 169／353：209 点，5.27 米，漏检 12 |
| 143／271，实例 3 | 110 | 5.51 | 110 | 143／275：158 点，4.71 米，漏检 0 |

这些失败包含近距离、多回波观测，同一标注在另一些观测下能够被识别；成功与失败同时变化了姿态、回波展布和周围关系，尚不能单独归因于交互模块。C 在约 75% 召回处的 21,847 个漏检中，125 占 14,578 个，距传感器不足十米者占 78.10%，至少一百回波的异常物体观测占 66.50%。按异常点所属对象分摊固定排序中相对 100% AP 的缺口，125／实例 1 占 13.22713 个百分点，169／实例 1 占 1.76251，143／实例 3 占 1.36942；这些是描述性分解，不是可以直接获得的提升，也不是相对 COVAL 的差距分解。

正常误报查询位于约 2.65–2.71 米。保持 C 的排序不变，把每个异常点精确率中正常点所占的部分按原始语义编码分摊，编码 40 对应 20.54732、编码 50 对应 2.51311 个 AP 百分点；全部类别之和为 100−76.25949。此前沿用类别表将它们称为道路、建筑标签，容易使读者把编码直接理解为已确认的物理结构。本轮查到，125 的 21,450,882 个正常点中有 21,450,506 个为编码 40，不能据此声称该序列几乎全是道路，更不能直接推出应该补道路素材。当前保留原始编码用于追溯，结构解释另查实际几何。

训练检索使用 C 对全部 6,185 条固定训练记录执行完整扫描推理，评分及候选提取耗时 398.70 秒。异常检索保留每个合成物体的实际回波；正常检索保留每帧两个高分格子，并补查 2,961 个原始正常扫描在上述误报所在传感器位置的实际观测。描述量包括回波数、距离、三个方向的空间展布、局部形态、强度分位数、垂直跨度及周围两米的正常点数量。各维度由训练观测的中位数和四分位间距缩放后计算距离，分别检索 nuScenes 场景及 STU 几何世界，正常 STU 观测全部只计一个 206 背景。

检索找到了部分粗略相近而 C 已能识别的合成异常。例如，125／127 对应的 nuScenes 记录 3835 有 95 个异常点、距离 5.61 米、异常分数中位数 33.75，而真实失败观测为 316 点、5.24 米、中位数 −0.20557；STU 记录 5139 有 393 点、5.68 米、中位数 28.625，但展布更接近条状。169／365 的相邻训练观测中也存在已被正确赋予高分的物体。143／271 检索到 STU 记录 5140：50 点、7.02 米、中位数 3.35938，是一项相近形态的低分训练候选。它进入本轮困难池，访问次数由对照的 1 次增加到 2 次，仍未修复下表对应真实观测。

这些结果显示部分合成近邻获得高分而真实查询获得低分，但素材等价尚未证实，不能据此认定模型见过相同情况、已经学会，或已经学错。完整几何缺口、材质与采样响应差异、背景关系和表示不足仍待区分。检索距离是粗描述的接近程度；跨传感器强度未作材料反射率标定，正常候选库也没有穷尽所有正常结构。全部固定训练点的合并 AP 为 98.41848%，但训练与验证的异常比例、标签组成和来源不同，不能把两个 AP 的差直接视为单一泛化缺口。[检索记录](results/train/native/errors/matches.json)和[位置覆盖检查](results/train/native/errors/coverage.json)保留这些适用边界。

困难池完全由训练分数确定。分别在 nuScenes 正常、nuScenes 异常、STU 正常、STU 异常四组中排序，正常帧使用最高分 512 个正常点的平均分类损失；异常帧将该量与全部异常点平均分类损失的组内百分位取平均。每组选择最困难的 20%，共 1,238 条记录。它是通用的分类困难取样，没有用验证点参加损失，也没有给上述检索邻居另设固定配额；不能把它解释为直接按验证 AP 梯度选择样本。

两支均从 C 的完整第 1,000 步状态继续至第 1,500 步，继承优化器历史量、随机状态、原取样位置和学习率调度，维持 C 的 0.3 倍后续学习率，不重新预热。各执行 500 次成功更新、4,000 帧次，只在终点完整验证。困难组逐批轮换替换一对扫描，共 1,000 个输入位置；其余 3,000 个位置逐项复用，替换位置的来源和正常／异常帧类型保持相同。两支四类帧次分别为 nuScenes 异常 1,615、正常 1,616、STU 异常 475、正常 294。

| 模型 | 固定终点 AP（%） | FPR95（%） | AUROC（%） | 允许保留 C 起点的最佳 AP（%） |
| --- | ---: | ---: | ---: | ---: |
| C，第 1,000 步 | **76.25949** | **0.13002** | **99.95131** | **76.25949** |
| 原取样，续训 500 步 | 75.79741 | 0.14525 | 99.95099 | **76.25949** |
| 困难取样 25%，续训 500 步 | 73.94485 | 0.16906 | 99.94725 | **76.25949** |

困难组比原取样低 1.85256 个 AP 百分点，比 C 低 2.31464；原取样比 C 低 0.46208。两支保留的最佳权重均逐参数等于 C，C 文件本身未被改写。困难组在各召回区间的 AP 贡献都低于对照，50%–75% 和 75%–90% 分别少 0.80120、0.62544 个百分点。在各自重新确定的约 75%、90%、95% 全局召回处，困难组比对照分别多 8,958、28,097、46,152 个正常点误报。并列分数按官方规则整体保留，表中“约”表示实际召回率有微小差别。

| 固定样本的错误数 | C | 原取样终点 | 困难取样终点 |
| --- | ---: | ---: | ---: |
| 125／126–128，共 931 个异常点，约 75% 召回处漏检 | 931 | 931 | 931 |
| 169／365，191 个异常点，同一工作点漏检 | 187 | 187 | 188 |
| 143／271，110 个异常点，同一工作点漏检 | 110 | 110 | 110 |
| 143／271，484 个正常点，同一工作点误报 | 402 | 454 | 482 |
| 147／239，446 个正常点，同一工作点误报 | 14 | 100 | 362 |

固定样本表显示主要失败没有被修复，部分正常结构的排序还变差了；它与全局指标的方向一致。全序列并非每项都同方向，例如 169 的约 75% 召回漏检数从 C 的 2,003 降为对照 1,984、困难组 1,993，但不足以抵消其他排序损失。因此，本轮不支持继续增加这一困难取样配方的预算，也不支持把观察到的结果概括为所有困难样本方法均无效。

困难池记录的实际访问量从 806 增至 1,618，其中困难正常帧从 374 增至 766；困难正常帧与异常帧共同参加双帧排序的次数从 181 增至 393。两支均有 461／2,000 对没有异常，只提供分类监督。输入来源配额相同，但困难替换使异常监督点次从 320,039 增至 350,263，正常点次从 169,322,897 变为 169,338,285；相应点数变化不能直接等同于损失权重变化。两支触发梯度裁剪的更新数分别为 124、140，数值溢出均为零。

训练循环分别耗时 549.86、570.95 秒，完整验证分别为 195.92、185.68 秒，连同区间保存总计 25.29 分钟；另有训练池评分、初始化与离线诊断开销。张量峰值显存分别约 9.62、9.70 GB。执行前已核对实际 CPU、显存、内存、进程及 Windows E 盘，单卡顺序训练，运行期间复查磁盘余量。完整配置只有分支选择不同；恢复状态和严格取样有对应的 27 项训练检查，曲线复算与官方指标误差不超过 10⁻⁹ 个百分点。该轮仍只有一个种子，已有数值敏感性限制因果解释，未将它转为继续定位真实失分的阻塞条件。

该轮检索的训练清单不等于所有可用训练来源。原有 STU 合成池包含 40,152 条合格观测，当前选入 720 条；还有 **39,432 条未选入，其中 11,208 条至少含一百个异常点**。困难取样轮次只核验了既有清单和成员关系，没有对未选观测重新评分。后文记录本轮围绕 125 定向检查其中 534 条的结果；其余仍未据此认定有用或无用。它们共享 206 背景，不能增加独立 STU 场景数量。

[配对结果及逐点样本变化](results/train/native/mining/comparison.json)、[原取样结果](results/train/native/mining/control/0/conditional/result.json)、[困难取样结果](results/train/native/mining/hard/0/conditional/result.json)、[困难池](results/train/native/errors/pool.json)、[训练评分记录](results/train/native/errors/train.json)保留实际输入和结果。`src/diagnose.py` 的 `cases`、`mine`、`coverage`、`match`、`mining-result` 分别完成定位、训练评分、位置支持、检索和结果比较；`src/train.py --branch control|hard` 沿用原训练入口，必须提供 `--hard-pool` 并指向上述 C 起点。原始模型和逐点数组仍只保留在本地，提交内容为程序、实验记录及可查看的分析产物。

## C 的完整排序失分与逐项原因证据

上一轮解决的第一项缺口是：此前有全局指标和少数重点观测，却没有把每一项失分连接到可追踪的案例。本节说明仍然有效的完整计数与素材检索基础，新增的逐项原因检查见下一节。分析固定使用 C 保存的完整验证预测，包含 1,960 个合格扫描、193,792,470 个正常点和 87,499 个异常点。保留全部并列分数和正式评价点，包括未分配实例号的一个异常点；没有只选某个召回率的误报，也没有因合并重复观测而减少评价点数。

设全部异常点数为 \(P\)，\(T(s)\)、\(F(s)\) 分别是分数不低于 \(s\) 的异常、正常点数。每个异常点 \(p\) 分摊的失分为

\[
\ell_p=\frac{100}{P}\frac{F(s_p)}{T(s_p)+F(s_p)}.
\]

全部 \(\ell_p\) 的和为 **23.7405070985 个 AP 百分点**，与 \(100-76.2594929015\) 的数值残差为 \(-1.42\times10^{-14}\)。再把 \(F(s_p)\) 按正常结构候选的实际点数拆开，就得到“异常案例—正常结构候选”的完整失分关系。它说明当前排序的缺口由哪些点对分摊，不表示删去某类错误就能得到同等提升，也不是相对某篇论文的差距归因。异常表和正常表是同一缺口的两个边际视角，不能把两个总数相加。

异常侧按序列及原始实例号合并为 **31 个标注案例、2,993 个物体观测**，每项保存连续帧段、点数、原始记录位置和分数分布。原始实例号尚未全部认证为独立物理轨迹，例如 143／实例 3 的世界坐标中心跨度约 11.46 米，不能未经检查便当作固定物体的重复观测。正常侧用原始位姿把真实回波放到世界坐标中，在同一原始语义及几何方向类别内，合并相邻的 0.5 米网格，得到 **27,126 个连通表面候选**，其中 16,707 个分摊正失分，10,419 个当前分摊为零。水平、竖直、倾斜及非平面／支撑不足四类由附近实际回波的几何估计区分，原始语义编码不直接当作物理类别。

这一步完整保留了所有点，却没有产生正常物体身份真值。较大的连续地面候选可能跨越很长的轨迹，细小候选可能是同一结构的碎片。每个序列另检查四对相邻扫描的正常点配准距离；多数序列的距离中位数约为数厘米，但 125 的部分帧对达到约 0.14–0.21 米，且位姿补偿并非总比不补偿更好。因此，正常案例仍称为几何候选，不能声称已经人工确认 27,126 个独立结构。合并方式影响案例解释，不影响完整排序的 AP 总分摊。全部 160,021,290 个当前零失分正常点及 56 个零失分异常点也有记录，没有从分析范围中删除。

[全量分摊记录](results/train/native/errors/ledger.json)保存对象、表面、逐帧身份及所有对侧贡献；[完整关系表](results/train/native/errors/relations.csv)保存非零的异常—正常案例关系。以下按整条排序的失分排序，而非沿用旧模型或单个工作点的案例优先级。

| 异常标注案例 | 固定排序失分（AP 百分点） | 占全部缺口（%） | 当前证据指向的核查重点 |
| --- | ---: | ---: | --- |
| 125／实例 1 | 13.22713 | 55.72 | 训练近邻多为高分，但实际形态、响应与背景关系尚未证实等价；同一扫描内也有错序 |
| 141／实例 4 | 1.80110 | 7.59 | 此前重点案例遗漏；部分训练近邻本身低分，应先核对其可信性和相关性 |
| 169／实例 1 | 1.76251 | 7.42 | 真实观测分类和排序均较困难；高分合成近邻不足以区分覆盖缺口与泛化问题 |
| 143／实例 3 | 1.36942 | 5.77 | 实际局部邻接关系与检索近邻有差异；同帧错序明显，先核查观测关系及身份 |
| 150／实例 4 | 0.67862 | 2.86 | 检查跨观测分布及排序，不能只依据较小分类损失判断学好 |
| 142／实例 3 | 0.63248 | 2.66 | 真实观测错序率高；训练候选难度也不一致，需要确认相关素材 |
| 141／实例 5 | 0.49957 | 2.10 | 近邻训练候选平均分类损失较大，但尚无训练不足的独立证据 |
| 139／实例 7 | 0.40680 | 1.71 | 分类损失较小仍分摊 AP 缺口，需比较真正排在前面的正常结构 |

前四项合计 **18.16016 个百分点，占缺口 76.49%**。其他 23 项没有被省略，均列在[异常逐项表](results/train/native/errors/attribution.csv)中。正常侧前 9、23、62 个候选分别累计覆盖约 50%、80%、90% 的正常侧分摊；这表示失分集中程度，不证明只需补几个结构就能解决问题。[正常逐项表](results/train/native/errors/normal_cases.csv)保留全部 27,126 项及其零失分参照。

| 全局召回率区间（%） | 相对理想精确率的失分（AP 百分点） |
| --- | ---: |
| 0–25 | 0.57217 |
| 25–50 | 1.56860 |
| 50–75 | 6.41806 |
| 75–90 | 7.47070 |
| 90–95 | 3.40020 |
| 95–99 | 3.34406 |
| 99–100 | 0.96671 |

50%–90% 区间分摊 13.88876 个百分点，占全部缺口 58.50%；90%–100% 的尾段也保留在分析中。跨越区间边界的并列分数仅按召回长度分配贡献，正式评价仍整体处理并列分数，没有人为打破并列。[完整排序图](results/train/native/errors/loss.pdf)同时给出各区间的案例组成和累计失分覆盖。

另一项诊断逐个异常点计算“正常点排在它前面的比例”，并列计半，分别使用本扫描正常点和其他扫描正常点作分母，再按该案例实际异常点数平均。125 的同帧／跨帧错序比例为 **0.08159%／0.06775%**，143 为 **0.16953%／0.05234%**；它们在同一扫描内就有问题，不能把全部错误统一解释为跨扫描分数偏移。169 为 0.33343%／0.41228%，141／实例 4 为 0.00381%／0.00520%。两种正常点总体的结构组成不同，这些数值提供排查线索，尚未通过控制比较确认分数偏移的原因，也不能代替 AP。

素材检索现覆盖全部异常观测，而非仅对 27 个展示案例查询。训练侧实际测量 **49,043 项观测描述**，覆盖全部 6,185 条当前训练记录，包括 6,079 个合成物体观测、每帧原有高分正常片段及各距离带的高点数正常参照。每项同时比较三维展布、点间距离、局部表面变化、回波间距、原始强度和周围两米关系，共 36 个描述量；学习分数不参与检索距离。查询分别寻找不同 STU 合成世界及不同 nuScenes 场景中的近邻，正常 STU 素材始终只计一个 206 背景。

描述的尺度只由训练素材确定。表面局部估计最多使用 128 个确定性选出的实际点；这只是描述计算，不改变完整扫描输入。强度没有跨传感器反射率标定，描述近邻不是几何、材料或上下文等价的认证，也不能根据最近的三个候选认定整个素材池缺少某类情况。当前未选入的 39,432 条 STU 合成观测中，本轮定向检查了 534 条，仍有 38,898 条未重新评分；不能把固定清单的缺口直接解释为所有训练来源都没有相应素材。

[实际回波对照图](results/train/native/errors/materials.pdf)展示前四项失分最大的观测、最近 STU 候选和最近 nuScenes 候选，统一每行的物理比例与 C 分数颜色。125／133 有 327 个异常点，分数中位数 −0.902；最近 STU 记录 5340 有 81 点、中位数 28.000，但回波形态和周围关系仍有差异。141／354 的 101 点中位数为 0.252，最近 STU 记录 5080 的 43 点中位数为 −3.109，这一候选本身也困难。169／352 与 143／312 最近的 STU 候选均为记录 5466，不能因为同一候选接近多个查询就认定它充分覆盖了两个物体。图中灰色环境点仅为显示而稀疏选择，彩色目标点全部显示；没有裁剪网络输入。

每项素材保存 C 的分类损失、完整参考池内的错序比例、同帧错序比例和实际训练访问次数。纯正常帧没有同帧异常参照，该项明确为空值，不填成零错误。训练与验证分别使用自己的完整点总体计算参考排名，不能混用数值阈值或把不同总体的比例差直接解释为原因。[训练素材记录](results/train/native/errors/train_profiles.json)、[查询素材记录](results/train/native/errors/val_profiles.json)和[近邻连接](results/train/native/errors/material_links.json)通过记录索引、原始实例／局部网格与素材编号逐项追溯；[机器可读归因表](results/train/native/errors/attribution.json)保存每项汇总。

训练排名的复算使用全部 260,008,859 个正常点和 516,150 个异常点，合并 AP 仍为 98.41847882%，与此前保存结果一致。记录 5340 的上述物体，在本扫描内没有正常点排在它前面，完整训练池内的错序比例约 0.000365%；记录 5080 的对应物体，同帧错序约 2.18970%，完整训练池内约 2.84490%。这说明两个近邻在 C 下的学习表现确实不同，而不只是原始分数的量纲差异。前者还需验证素材与真实观测是否相关，后者还需区分素材质量、标签、可学习性和优化不足，不能直接合并成一种原因。

截至上一轮素材与读出检查，尚无足够证据将具体失分归为“样本覆盖不足”“样本有了但没学够”或“样本有了但学错了”。随后开展的数值对照确认了位置表示的计算缺陷及其完整排序影响，见下文。历史 23.74051 个百分点仍未在上述原因之间作出互斥分解；这不等于没有识别出具体机制。找到高分训练近邻不能认定学错；候选也低分不能单凭一次或两次访问认定训练不足；未经素材相关性确认，也不能宣称已经见过同类观测。

还需区分描述覆盖与原因确认。异常侧 2,993 个观测均已计算描述并检索，未描述的异常侧失分为零。正常侧上一轮为每个正失分候选选择一个最大失分局部，仅代表 **2.66202 个百分点**。本轮展开两个主要候选后，正常局部的描述及近邻检索范围增至 **7.39120 个百分点，占正常侧缺口 31.13%**；其余 **16.34931 个百分点，占 68.87%**，已进入完整计数，但尚未取得相应局部的描述和训练近邻。描述及近邻检索不等于逐个人工核实，更不等于原因确认。

完整分摊通过原 `src.diagnose` 入口的 `account`、`materials`、`attribute` 生成。真实点计数和逐分数直方图均与原完整评价一致；包含并列分数的逐点对照验证了两侧分摊、召回区间和错序概率，两个必要的分析检查通过。上一轮使用六个 CPU 进程、复用保存预测；本轮新增的 GPU 诊断范围另列如下，未重算或替换完整排序依据。

## 此前高影响案例的素材与固定表示检查

本轮将“缺少相应观测”“已有候选尚未学好”和“融合使信息难以使用”拆成可分别检查的问题。所有检查保留 C，真实验证点只用于诊断，线性读出仅使用训练来源标签拟合。开始时写入[逐项检查定义](results/train/native/errors/focus.json)，分别固定样本、模型、描述尺度或读出容量，并记录唯一改变的对象。没有将检查样本加入正式训练，也没有启动完整训练。

| 对象 | 固定内容 | 本轮改变及判断条件 |
| --- | --- | --- |
| 125／实例 1 | C、三个已有合成世界的物体及位置、原始射线和标签、训练描述尺度 | 从已选代表帧扩为同世界全部既有观测；只有真实形态、响应和关系确实更相关，才能支持代表观测选择有缺口 |
| 141／实例 4 | C、285 个真实观测、相同训练素材及描述尺度 | 比较全描述、仅几何、去掉采样条件三种检索；检索不稳定时，不能由低分候选认定真实案例训练不足 |
| 两个正常候选 | 原完整排序、正常点身份、世界位姿 | 拆为 0.75 米世界网格及连续正失分帧段；全部片段计数，对各自累计至少 90% 的片段计算每个观测的描述 |
| 正常局部的表示 | C、完整输入扫描、点身份、训练和检查来源、训练限定的标准化及线性目标 | 最终比较同样六尺度上下文加融合前／后状态，均为 448 维输入、449 个读出参数；结果只用于判断信息是否容易读出 |

**正常候选的范围已经展开。** 每个片段包含同一世界网格连续出现正失分的帧，片段内该网格的全部正常点均保留，包括当前分摊为零的点。每项全部片段的点数与失分都与原表复算一致。原始位姿误差仍影响片段的物理身份，网格及帧段不是人工认证的独立物体。

| 正常候选 | 整项失分 | 全部片段数 | 已描述片段／观测数 | 已描述失分 | 其余失分 |
| --- | ---: | ---: | ---: | ---: | ---: |
| N141:75 | 2.67368 | 36,515 | 110／5,045 | 2.40692 | 0.26676 |
| N125:30 | 2.66734 | 29,895 | 334／21,711 | 2.40110 | 0.26624 |
| 合计 | 5.34102 | 66,410 | 444／26,756 | 4.80802 | 0.53300 |

失分单位均为 AP 百分点；两项原代表局部合计只有 0.07885 个百分点。N141:75 最大片段为网格 `40_-222_-15` 的 314–380 帧，分摊 0.41018 个百分点；N125:30 最大片段为 `6_-6_-3` 的 87–168 帧，分摊 0.35947 个百分点。[片段表](results/train/native/errors/fragments.csv)列出全部片段及是否取得逐观测描述，[实际局部图](results/train/native/errors/fragments.pdf)展示每项前两个片段的最高失分帧及 STU 正常近邻，不能用图中的单帧代替整个片段。

N141:75 的 377 帧局部与训练记录 5059 都有较大正常分类损失，分别为 6.690、3.842；但实际回波的平面／条带构成及周围关系仍有差异，尚不能称为可靠对应。相邻另一片段检索到记录 5797，训练正常分类损失仅 0.0234。N125:30 的 135 帧两个高分局部检索到记录 5757、6006，后两者分类损失为 0.2765、0.5435，真实局部为 15.4041、14.8979；图中回波排列也不相同。因此，不能用同一种“没学够”解释两个父候选内的全部错误，也不能把候选学习较好直接写成学错了。

**125 的同几何观测检查没有支持优先增加这几种几何的重复训练。** 按原近邻关系的查询失分量选择三个 STU 世界，分别包含 `branched_raised_smooth_5`、`solid_raised_smooth_5`、`solid_raised_rough_3`。三者共有 543 条合格观测，当前清单选入九条，本轮对其余 534 条执行完整扫描推理，已选九条复用保存预测。物体、位置及物理生成均未改变，没有按新分数选择待评分帧。[逐观测结果](results/train/native/errors/observations.json)记录每条原始索引、异常点数、分数、分类损失和同帧／完整训练正常池内的错序比例。

210 个真实查询在较大未选池中均找到总描述距离更小的候选，但增大池本身就会降低最近距离。以查询的完整 AP 分摊加权，已选与未选最近候选的分类损失分别为 0.0000805、0.0006564，多数已被 C 识别；未选池也存在少数困难观测，不能称为全部学好。最重要的 125／133 有 327 个异常点，C 分数中位数为 −0.902；未选旧记录 18307、同世界第 356 帧有 366 点，中位数为 23.125。总距离从 0.9011 降为 0.7450，主要来自采样条件变近；几何距离却从 0.3772 增为 0.5348，响应距离也未改善。这个实际例子不支持用点数接近认定覆盖成立。下一项应核验独立形态、响应及相邻结构，确认缺什么后再选择或补充素材。

**141 的低分通用近邻仍不足以启动拟合。** 全描述的单一最近邻为记录 5080 的查询分摊 0.32028 个百分点；去掉采样条件后为 0.33332，仅使用几何描述后为零。这些数值是各检索条件下的查询分摊范围，不能相加；与旧表“前三个近邻任意一个包含 5080”的计数也不同。其余全描述最近候选的失分加权分类损失约为 0.05696，多数较低；仅几何检索仍会找到低分素材，例如记录 5287，不能声称所有替代候选均已学好。

[实际观测对照](results/train/native/errors/relevance.pdf)保留真实 141／354、355 与全描述、仅几何近邻的原始回波。5080 的细条状局部与真实观测的曲面／面状返回尚未确认为同类有效信息；检索变化也不能反过来证明它毫不相关。当前决定是暂不依据 5080 加频或拟合。原计划最多 40 次更新、四个确认相关训练扫描的拟合检查仍未执行：相关性确认后才固定 C 起点、只改变这些样本的优化次数，同时观察未用于拟合的同世界观测及保留几何／日志。训练样本单独改善仅说明局部拟合成功；独立相关观测也改善才支持增加其训练使用，仍不能宣称真实 141 已解决。

**表示检查采用相同上下文和相同读出容量。** 第一项参考比较使用浅层采样查询与最终融合状态，各 64 维。检查实现时确认，前者不包含骨干的六尺度上下文，所以这一比较只能说明采样查询本身是否足够，不能判断骨干信息是否在融合中丢失。随后在再次提取前明确补充：两侧都保留完全相同的六个 64 维上下文向量，再分别接融合前或后的 64 维逐点状态。两侧各有 449 个线性读出参数，数据、抽点、标签、正则化和求解器一致；上下文坐标及标准化系数逐元素相同。

读出拟合使用 111 个训练来源完整扫描，其中包括每个来源／正常异常组均匀抽取的记录，以及最多 16 个不同扫描的正常近邻；共 228,069 个正常点、6,654 个异常点。检查使用四个保留 nuScenes 单元及三个保留 STU 几何的 56 个扫描，共 114,688 个正常点、481 个异常点；三个 STU 几何仍共享 206 背景。真实诊断子集为 36 个完整扫描，共抽取 74,870 个正常点、4,448 个异常点，包含两个父候选各八个高影响片段的峰值局部，以及 125、141 的均匀观测。每帧至多随机抽 2,048 个正常点和 512 个异常点，额外完整保留指定正常局部；全扫描先进入 C，抽点只减少读出诊断规模。

目标为类别各占一半的逻辑分类损失，加权重平方的 0.0005 倍；仅由训练点确定逐坐标均值及标准差。四组读出均从零初始化，使用同一 L-BFGS 求解设置，最多 500 次迭代，没有用检查或验证结果调参。实际 64 维参考求解为 231、89 次迭代，相同上下文两组为 150、188 次，均满足求解器收敛条件；C 参数未更新。

| 同一点集上的 AP（%） | 原 C | 融合前状态＋六尺度上下文 | 融合后状态＋相同上下文 |
| --- | ---: | ---: | ---: |
| 读出拟合点 | 99.29114 | 99.81730 | 99.83059 |
| 保留来源检查点 | 57.52714 | 61.57913 | 66.31814 |
| 真实诊断子集 | 90.68923 | 88.35327 | 89.39765 |

这些是人为抽取点集的 AP，类别比例不同于完整验证，**不能作为新的 STU 最佳结果**。相同上下文比较中，融合后状态的 AP 在检查点与真实诊断子集分别高 4.73901、1.04438 个百分点，暂未支持优先削弱融合。但两组都绕过了原模型对上下文的压缩，因此该比较也不能证明原融合没有损失信息。对照不同维数的读出还改变容量，不能用宽读出与 64 维读出的差值作为融合因果收益。

指定的 16 个正常局部共 1,167 点，只分摊原完整排序的 **0.28717 个百分点**，不是上述已描述的 4.80802 个百分点。各读出分别按真实诊断子集约 75% 召回确定阈值，原 C、融合前加上下文、融合后加上下文在这些点上的误报分别为 **422、594、547**。N141:75 的 826 点分别为 249、395、363 个误报，N125:30 的 341 点分别为 173、199、184 个误报；各自覆盖原完整排序的 0.15466、0.13251 个百分点。融合后的同容量读出有所改善，但没有胜过原 C，不能据此直接换评分头或改融合。其余指标、逐局部误报、训练素材对应和抽样身份均见[读出记录](results/train/native/errors/features.json)及[逐项检查结果](results/train/native/errors/review.json)。

本轮因此保留 C：125 暂不增加这三个世界的重复观测；141 暂不以 5080 为依据拟合；N141:75 优先确认记录 5059 的实际相关性及其自身正常误报，N125:30 优先确认高分局部与 5757、6006 等低分训练素材的形态及周围关系差异。真实对应确认后，才由独立观测结果决定增加样本使用或补充缺失条件。当前没有足以单独决定全量训练的数据或模型改动。

本轮通过原诊断入口的 `focus`、`observe`、`features` 和更新后的 `materials`、`attribute` 完成计算。三个世界新增推理约 59.68 秒，最终同上下文特征提取及四组读出拟合约 59.01 秒，另有首轮参考提取、素材描述和图表生成耗时。运行使用已有 Python 环境、单卡顺序推理、四个加载进程、六个描述计算进程；任务前后实际查询 Windows E 盘余量，结束前约 42.59 GB，未接近 10 GB 安全余量。C 的参数保持逐元素不变；最终 79,318 个真实诊断点的 C 分数与原正式缓存逐元素相同，完整 AP 分摊继续使用原正式预测。该子集的一致性不代表已排除所有 GPU 数值波动。生成表格时的一次局部变量重名已修正并重新生成；末次检查曾误用缺少 `torch_scatter` 的系统 Python，随后改回项目已验证环境，两个分析检查通过，没有改动依赖、预测或失分公式。

## 逐点特征检查与位置编码数值干预

本轮要区分的是：实际错误是否与模型计算中丢失位置差异有关。每次输入同一个完整扫描，保留 C 权重、逐点标签、原始槽位、体素归属、邻域及其他混合精度运算，只改变旋转位置编码是否在自动混合精度范围内执行。这里的旋转位置编码将体素坐标转为角度，再旋转注意力的查询与键向量，使它们的内积包含相对位置。单个特征通道没有预先指定的“异常语义”；数值检查必须通过固定输入的对照与最终排序联系起来。

代码原本将查询和键转为 FP32，但角度计算中的 `einsum` 在 CUDA 自动混合精度范围内仍返回 BF16。随后转回 FP32，只保留了已经舍入的角度。完整真实扫描对照显示，浅层逐点特征、嵌入、前三个骨干层及第一次注意力的位置编码前向量逐元素相同；全部 193,879,969 个有效评价点所对应的首次特征差异，都出现在第四个骨干层第一次旋转查询向量。初始条件查询仍完全相同，改变的是后续空间表示及其融合结果。因此，这条证据指向位置计算，不能改写成“浅层特征无效”或“条件融合已经学错”。

检查涵盖 38 组表示，包括每个注意力块的位置编码前向量与旋转后查询、键，五个骨干层、解码表示、初始条件查询、六尺度上下文、两次融合状态、逐点评分输入及评分隐藏状态。共享体素表示只比较一次，再通过真实归属关系追踪到每个原始点。共逐元素比较 418,073,703,996 个特征值对；数值采用流式检查，重点失败点及成功参照保存完整向量，其余点保存首次差异位置及对应分数。该范围不包含程序中每一个临时算术张量。

例如，125 第 133 帧、原始槽位 130513 的第一次旋转查询第 217 号通道，由 −2.8331623 变为 −1.8520417；同一位置编码前向量不变。141 第 354 帧、槽位 87575 的第 112 号通道，由 0.7266667 变为 1.1994755。通道从零开始编号，完整向量和成功参照见[逐点记录](results/train/native/errors/feature_values.json)。这些数值本身不表示判断正确，实际作用由下面的完整排序对照确定。

| 完整 STU 验证配置 | AP（%） | FPR95（%） | AUROC（%） |
| --- | ---: | ---: | ---: |
| 保留的历史 C | 76.25949 | 0.13002 | 99.95131 |
| 当次原计算，带特征记录 | 76.25381 | 0.13006 | 99.95130 |
| 仅修正位置编码，带特征记录 | 78.06973 | 0.09857 | 99.95955 |
| 原 C 权重＋修正，正式评价入口 | 78.06939 | 0.09832 | 99.95952 |

两组都使用 1,960 个完整合格扫描、193,792,470 个正常点及 87,499 个异常点。固定官方实现独立复算了两组全部预测，三个指标与按完整并列分数计算的结果在 10⁻¹⁰ 内一致。配对 AP 提高 **1.81592 个百分点**；其中约 50%–90% 召回区间贡献 1.35186 个百分点。各组分别确定召回阈值，不共享数值阈值；并列分数使实际召回率不能总是完全相等。

| 案例及视角 | 当次原计算分摊失分 | 修正后分摊失分 | 失分减少（百分点） | 支持到的判断 |
| --- | ---: | ---: | ---: | --- |
| P125:1，异常 | 13.22902 | 11.76397 | +1.46505 | 该案例的完整排序明显响应位置精度修正；大部分剩余失分仍未解释 |
| P141:4，异常 | 1.80227 | 1.66803 | +0.13424 | 本次正向干预成立，不能据此确认 5080 的素材相关性或判定没学够 |
| P169:1，异常 | 1.76265 | 1.62362 | +0.13904 | 存在位置计算影响，剩余观测覆盖及学习原因待查 |
| P143:3，异常 | 1.37057 | 1.32985 | +0.04072 | 全排序略有改善，但并非每个召回工作点都改善 |
| N141:75，正常 | 2.67327 | 2.57286 | +0.10041 | 整个候选有所改善，不能以一个高分局部代表全部观测 |
| N125:30，正常 | 2.66627 | 2.92559 | −0.25932 | 该正常候选恶化，位置精度修正未解决其主要错误 |

31 个异常案例中，19 项分摊减少、12 项增加；减少合计 1.90363、增加合计 0.08771 个百分点，净变化与配对 AP 一致。正常侧减少合计 2.36740、增加合计 0.55148 个百分点，净变化也与 AP 一致。**两个视角不能相加。** 分摊变化同时包含本案例及其他点改变全局排名的影响，不能全部解释为本案例点本身被修复，也不能把有正向响应案例的全部原失分算作已经解释。

这项最小对照支持：原位置角度的数值舍入损失了区分信息，并对固定 C 的预测与完整排序产生可测影响。新增样本、改变监督标签和增加训练次数都不能解释本次收益，因为它们没有发生。该证据没有排除覆盖不足、训练使用不足或泛化问题继续影响剩余错误；也没有证明网络学到了某种错误的语义关联。修正后的配对排序仍距 100% AP 有 **21.93027 个百分点**，不能宣布所有错误已经完成归因。

实现直接修改[原注意力计算](vendor/litept/model.py)，在旋转查询和键的现有调用范围内关闭自动混合精度，随后继续使用原注意力精度。没有增加推理分支、手工评分规则或模型参数。[诊断入口](src/probe.py)显式复现原算术与修正算术，两组每扫描均清空位置查找缓存，避免缓存隐藏干预。原始 C 权重始终逐元素不变，原预测与原指标独立保留。

数值质量检查保留了不利结果。首个真实扫描的原计算重放，所有被检查特征及分数逐元素一致；但当次完整原计算与历史缓存有 15,175,242 个分数不完全相同，最大差 0.375，AP 差 −0.00568 个百分点。正式修正调用与带特征记录的诊断调用，在预先选定的十扫描复查中也未达到逐点完全一致：85,826 个分数有差异，最大差 0.25。因此，正式调用另行完成完整验证，不能把诊断指标直接冒称为正式入口结果。这些执行差异没有在本轮被进一步归因。

新增的 GPU 回归直接运行实际注意力模块，并将旋转结果与独立逐轴 FP32 公式比较。原实现有 1,076／1,152 个被测数值不符，最大绝对误差 3.39280；修正后通过同一检查及有限梯度检查，原有位置公式与缓存边界回归也通过。此前仅在 CPU 上测试未复现这项 CUDA 自动混合精度问题，不能把 CPU 通过当作 GPU 正确的依据。完整逐特征配对用时约 540.51 秒，实际峰值分配显存约 4.54 GB；没有进行训练更新。

正式评价入口随后处理相同的全部扫描与评价点，得到 AP 78.06938761%、FPR95 0.09831858%、AUROC 99.95952235%，用时约 249.28 秒；原权重再次核验为逐元素不变。正式入口与配对诊断修正组的 AP 差为 −0.00034 个百分点。当前使用正式入口结果描述可交付配置，逐项数值干预效果仍使用其当次配对结果，不混用两个运行的分数。

后续原因检查继续从修正后仍高影响的 P125:1 与恶化的 N125:30 开始，保留此前可信训练素材、独立观测与成功参照。位置精度问题先由明确的实现修正处理；剩余的覆盖、训练使用与错误关联判断继续要求逐项对照，不以特征数值数量或检索命中数代替证据。

## 体素归并与模型计算路径复查

追加检查针对用户提出的 5 厘米体素及类似实现错误。CPU 部分读取全部 6,185 条当前训练记录与 1,960 条合格验证记录，包含作为上下文保留的忽略点，共 553,459,293 个实际回波。对原始 FP32 坐标先转为 FP64，再以精确整数 20 相乘并取下整，独立确定 5 厘米格子；该参考不复用实现中的除以 0.05。体素均值用独立按编号累加复算，逐个检查点索引、聚合分段、原始方向及四次下采样的传感器原点归属。

所有扫描的错误格子数、点次序错误、聚合分段错误和层级归属错误均为零；体素均值与独立参考逐元素一致。归一化逐点偏移的最大差为 2.98×10⁻⁸，属于计算顺序带来的 FP32 表示误差。若改用直接 FP32 除法划格，五组数据会有下表所示的边界归属差异，当前 FP64 实现没有采用该路径。这说明应保留现有高精度划格，而非将这些假设路径中的差异报告成当前错误。

| 数据来源 | 扫描数 | 假设直接 FP32 划格时归属不同的点次 | 正常、异常混合体素数 | 混合体素中的异常点次 |
| --- | ---: | ---: | ---: | ---: |
| STU 完整有效验证 | 1,960 | 2,062 | 708 | 805 |
| nuScenes 异常训练 | 2,504 | 784 | 183 | 193 |
| STU 异常训练 | 720 | 1,152 | 120 | 145 |
| nuScenes 正常训练 | 2,512 | 800 | 0 | 0 |
| STU 正常训练 | 449 | 710 | 0 | 0 |

验证中 805 个共享体素的异常点约占全部异常点 0.92001%；同体素另有 831 个正常点。所有检查数据中，没有发现四维输入坐标与强度完全相同、却在同一扫描中带有相反有效标签的点。**混合体素确实存在，但当前实现没有给它们合并标签，也没有删除逐点特征和偏移。** 均值与最大值聚合仍会压缩集合信息；索引正确不代表 5 厘米粒度最优，也不能把这些点次比例当作体素化对 AP 的影响或其上界。本轮没有改变体素尺寸。

GPU 部分预先均匀选择两域四类训练记录、验证扫描、重点失败及成功参照，并加入各来源混合异常点最多的扫描，共运行 40 个完整扫描。检查实际执行中的每一级整数格子归属，按独立索引累加复算坐标均值，按独立最大值归并复算池化特征，并核对注意力填充后的还原、解码器恢复的原体素顺序、六尺度上下文索引及坐标。所有离散索引和最大值归并检查完全一致；坐标均值最大差 7.63×10⁻⁶ 米，逐点细节均值最大差 6.69×10⁻⁸，均在预先设定的 FP32 计算容差内。解码尺度确实回到原 5 厘米体素，未出现误用深层索引。

这些运行检查支持：当前体素归并及已检查的多尺度还原路径没有发生所怀疑的索引或数值语义错误。5 厘米指立方体边长，对角线约 8.66 厘米；全部数据中原始点距其体素均值最远约 6.22 厘米，未超出格内几何范围。逐点偏移与原始坐标仍保留。更深层坐标采用子体素代表点的均值，代表占用体素分布，并非按所有原始回波数重新加权的质心；这是当前计算定义，不能仅因两者不同判定为错误。[完整逐扫描记录](results/train/native/errors/model_check.json)保存输入范围、真实误差及混合体素计数。检查用时约 71.84 秒，CPU 几何复算使用 12 个工作进程，GPU 逐层检查峰值分配显存约 1.38 GB，C 参数保持不变。

补充的边界回归确实发现了另一个辅助接口错误：空间编码的逆向转换函数实际返回坐标和批次共四项，外层却只接收三项，调用时直接报错。已在[原逆向接口](vendor/litept/serialization/default.py)修正返回值接收，并通过四种空间编码在 16 位坐标边界及多批次下的完整往返检查。**实际训练和推理只调用正向编码，未调用这个逆向接口，因此它不能解释既有 AP 失分，也不改变上述正式预测。** 该问题与运行中的位置精度缺陷分别记录。

实际稀疏嵌入模块另用单位卷积核检查中心及沿 x 方向的邻点响应：在原 `kernel_size=5, padding=1` 设置和自动混合精度环境下，输出与独立邻点查表完全一致，坐标顺序未变。该卷积只在原有占用体素位置输出，不能单凭填充值看似小于核半径便判定它错误。体素回归也加入正负 0.7 米边界的相邻浮点数，确保未来改动不会将边界附近的真实不同格子误并。

本轮对活动的条件交互模型还核对了混合精度范围、上下文轴、逐点输出的原始顺序及梯度通路。历史已停用的交互分支没有作为当前模型验证。位置精度缺陷已经修正；追加发现的逆向接口错误不在实际前向路径中，在其余已检查的活动路径上未发现第二项同类错误。该结论不等于全部模型近似都不影响结果。更小体素、不同聚合方式及最后评分层的精度属于需要独立对照的建模或数值选择，不能凭本轮检查直接改动并宣称收益。

## 修正后模型的素材使用对照

本轮沿用已有案例身份与修正后的 C，重点检查 P125:1、N125:30，没有重建全量案例清单。完整排序以普通正式评价保存的 `precision_deployed.npy` 为基线，P125:1 分摊 11.76390 个失分百分点，N125:30 分摊 2.92268 个百分点。这与上一节带特征钩子的配对运行有细小数值差异；两套预测分别使用，未混算。

首先核验训练素材的真实身份，再重算学习表现。N125 检索候选 5757 的目标局部含 45 个地形点和 5 个人行道点，不能直接用来证明道路素材已经覆盖；记录 6006 的目标局部则为 24 个原始道路标签点。6006 的强度第 10、50、90 百分位为 0.24554、0.31014、0.37460，真实 125 第 135 帧两处道路局部的范围与之重叠，但局部形态和周围关系并不完全相同。6006 局部的平面拟合残差约 0.00906 米，真实两处约为 0.04030、0.04520 米，不能仅凭类别与强度接近就宣布覆盖等价。

P125 原先对应的三个训练世界共 543 个现存观测已全部用修正后的 C 重新推理，包括九个已选代表帧。已选观测有 415 个异常点，平均分类损失为 0.000274；未选观测有 42,731 个异常点，平均损失为 0.002274，其中 27 点的分数低于零。该结果仍不支持优先增加这三个几何的重复观测。它只支持“这些候选大多已识别而真实 125 仍失败”，尚未证明真实物体的形态、响应及背景关系已经充分覆盖。

为了直接检验正常素材使用，本轮做了两支各 40 次更新的短对照。两支均从 C 第 1,000 步的完整状态恢复，继承优化器、初始随机状态及原第 1,001–1,040 步学习率，不重新预热。数据池、损失、完整扫描、体素大小、模型参数规模与 FP32 位置编码保持一致。原顺序组沿用原取样；素材组只把其中 31 个 STU 正常输入位置换成完整扫描记录 6006，其余 289 帧次及位置逐项复用。配置比较仅有分支名称不同，实际扫描替换另保存在取样记录中。素材组有效正常点数比对照少 124,579 点次，约 0.80%；异常点数完全相同，分类损失继续按类别归一化。

两支均完成一次包含 1,960 帧、193,879,969 个有效点的正式验证。排序复算与官方三项指标的差异均小于 10⁻⁸。表内单位为百分比。

| 配置 | AP | FPR95 | AUROC |
| --- | ---: | ---: | ---: |
| 修正后的 C，第 1,000 步 | 78.06939 | 0.09832 | 99.95952 |
| 原顺序，第 1,040 步 | 80.00223 | 0.08987 | 99.96537 |
| 仅替换上述正常扫描，第 1,040 步 | 80.53251 | 0.08573 | 99.96670 |

原顺序短训相对基线增加 AP 1.93284 个百分点；素材组相对原顺序增加 0.53028 个百分点，相对基线增加 2.46312 个百分点。两支终点均超过起点，因此允许保留起点的最佳结果与终点相同。最高 AP 权重单独保存在 `results/train/native/transfer/material/0/conditional/best.pt`，对照与原 C 均保留。这是单次短对照，尚未估计重复训练波动；初始随机状态与每组排序种子相同，扫描大小改变仍可能使后续随机深度掩码不同。

**全局收益没有验证预先提出的局部原因。** 重复素材后，6006 目标局部的分类损失没有下降。原本选出的两段真实道路错误也没有相对对照改善。两段分别是 `N125:30/6_-6_-3/87` 与 `N125:30/5_-5_-3/90`，覆盖 82、79 个连续观测，共 8,419 点，在基线分摊 0.87795 个失分百分点，占 N125:30 的约 30.04%。因此，这里比较的是连续片段，而非仅用第 135 帧代表全部道路。

| 检查对象与量 | 修正 C | 原顺序 40 步 | 素材组 40 步 |
| --- | ---: | ---: | ---: |
| 6006 道路局部，24 点平均分类损失 | 0.791767 | 0.982049 | 0.975397 |
| 上述局部以零分分类的错误点数 | 11 | 12 | 12 |
| 同一道路区域其他观测，556 点平均分类损失 | 0.033463 | 0.038913 | 0.037026 |
| 两段真实道路的 AP 失分百分点 | 0.877954 | 0.753421 | 0.770654 |
| 两段道路在约 75% 全局召回处的误报点数 | 1,871 | 1,671 | 1,680 |
| 全部 N125:30 的 AP 失分百分点 | 2.922679 | 2.651088 | 2.636165 |
| P125:1 的 AP 失分百分点 | 11.763896 | 11.267182 | 11.001595 |
| P125:1 在约 75% 全局召回处的漏检点数 | 13,800 | 14,379 | 14,400 |

各模型均以自身完整排序确定召回阈值；同分数点成组纳入，实际召回略有差异。分类损失与零分错误用于检查训练局部是否拟合，不作为跨模型排序改善的替代指标。P125 的 AP 分摊下降，而固定召回处漏检没有同步减少，也说明全局排序分摊不能直接解释成对象所有观测的检出改善。正常与异常两侧的失分仍是重叠视角，不能相加。

其他观测按原始 STU 帧号排除两支新增训练涉及的全部帧。预选 12 个观测中，九个仍看到同一道路区域，共 556 点；它们可能在 C 之前已被使用，因此只用于本次更新之外的观测检查，不代表新几何或新背景泛化。5757 仅做身份核验，没有加入额外重复。

对 6006 的监督通路另做了固定 C 的损失复算，未增加参数更新。24 点均得到正常标签，损失对分数的分类梯度方向正确；31 次输入对应 744 点次，其中 315 点次进入排序抽样，312 点次处于没有异常的双帧组、只获得分类监督。其余 117 点次未被排序抽到，但仍参与分类损失。检查未发现目标点被忽略，或损失到分数这一段的梯度中断、方向错误。该结果是固定分数下的实现核验，没有追踪实际更新中分数到共享参数的梯度；不能单独证明监督过弱或共享参数发生冲突。

上述素材使用对照支持保存更高 AP 的模型，**不支持将 N125 归因为“现有素材没学够并已通过重复训练解决”**，也未确认其主要属于覆盖不足或错误关联。6006 重复方案的全局收益暂不推广为主训练配方。该轮据此提出下一项拟合检验：固定素材组输入、C 完整起点和 40 步预算，唯一改变为在原组合损失上增加 1.0 倍这 24 个训练正常点的局部均值分类损失，再检查其他观测、真实道路及完整 AP。该项在随后获得授权，执行结果见下一节。

[完整素材与失分记录](results/train/native/errors/transfer.json)保存预先确定的对照、各帧结果、梯度通路核验及逐项“支持、排除、缺失证据、修改”说明；原[异常表](results/train/native/errors/attribution.csv)、[正常表](results/train/native/errors/normal_cases.csv)已补入这两项的最新证据，其他案例未新增原因结论。两支实际训练分别用时约 90.14、86.89 秒，完整评价约 255.35、253.68 秒；两支合计约 11.43 分钟，不含素材重算。新增训练与取样逻辑复用现有入口，四项必要回归通过；正式收益依据上述真实训练和完整评价，不依据回归测试数量。

## 局部监督的拟合与迁移诊断

本次从修正后的 C 第 1,000 步完整状态重新开始，未从素材组第 1,040 步继续。320 帧次及全部批内位置与未加权素材组逐项一致，其中 31 帧次为完整扫描 6006；优化器状态、初始随机状态、原第 1,001–1,040 步学习率曲线、组合损失及模型结构均保持一致。训练完成后的 Python、NumPy、PyTorch 与 CUDA 随机状态也与未加权组一致。训练与验证数据清单、标签、体素大小和位置编码精度修正均未改变。

唯一新增训练项为 1.0 倍目标道路点的平均二元分类损失。目标仍是记录 6006 中预选的 24 个可信正常点，以原始扫描点编号固定：89,577–89,588 和 90,576–90,587。一个八帧有效批次出现多次 6006 时，先合并其中全部目标点次再求均值；没有 6006 时，该项为零，避免微批次或重复次数意外改变权重。40 个批次中 22 个含该局部，共监督 744 点次。其他有效点继续参与原分类与排序损失，忽略点仍只提供上下文，验证点未进入损失。

[逐步记录](results/train/native/transfer/local/0/conditional/local.json)保存每次优化器更新前后全部 24 个分数、训练前向分数、梯度范数及实际参数变化。更新前后都在相同的训练前向后运行统计量上执行固定推理，记录过程恢复各模块状态和随机状态，并核验没有改变模型缓冲量。因此，同一步的前后差异可用于检查实际参数更新；相邻两步之间还包含正常训练对批归一化运行统计量的更新，不能把两者混同。

本次实际记录的起始局部损失为 0.784512，结束为 0.000976；零分分类错误从 11/24 降至 0/24。第 1,004 步首次出现局部监督，该步损失从 0.802252 降至 0.741200；第 1,007 步更新后 24 点均已低于零，最终分数范围为 −8.125 至 −6.125。40 次更新全部成功，32 次使该局部损失下降，8 次上升；12 次触发原有梯度裁剪。原缓存中 C 的独立素材读数为 0.791767，与本次启动读数有小幅差异；它们作为不同运行分别保留，未拼接成逐元素完全相同的轨迹。启动检查中，同一状态的两次记录完全一致。

两组终点均完成 1,960 帧、193,879,969 个有效点的正式 STU 验证。保存的逐点分数独立重算三项指标，与官方结果的差异小于 10⁻⁸。下表指标单位均为百分比。

| 配置 | AP | FPR95 | AUROC |
| --- | ---: | ---: | ---: |
| 固定诊断起点 C | 78.06939 | 0.09832 | 99.95952 |
| 未加权素材组，40 步 | 80.53251 | 0.08573 | 99.96670 |
| 局部加权组，40 步 | **81.63721** | **0.08027** | 99.96431 |

相对未加权素材组，AP 增加 1.10469 个百分点，FPR95 降低 0.00546 个百分点，AUROC 降低 0.00239 个百分点。终点超过起点，允许保留起点时的最佳结果也为第 1,040 步。新候选保存于 `results/train/native/transfer/local/0/conditional/best.pt`，原 80.53251% 与诊断起点权重均保留。极高召回处仍存在代价：各自达到约 99% 全局召回时，误报由 821,668 点增加至 915,205 点；不能将 AP、FPR95 改善表述为整条排序均改善。

迁移检查复用此前预选且未参与本次更新的观测与真实片段，没有根据本轮结果重新选择案例。下表召回阈值由各模型自身的完整验证排序分别确定。

| 对象及检查量 | 未加权素材组 | 局部加权组 |
| --- | ---: | ---: |
| 训练局部 24 点的平均分类损失 | 0.975397 | 0.000976 |
| 其他 9 个观测、556 点的平均分类损失 | 0.037026 | 0.000631 |
| 上述其他观测在约 95% 全局召回下的误报 | 12 | 0 |
| 真实道路片段 `6_-6_-3/87` 的 AP 失分百分点 | 0.429245 | 0.365437 |
| 真实道路片段 `5_-5_-3/90` 的 AP 失分百分点 | 0.341408 | 0.291987 |
| 两片段共 8,419 点，在约 75% 全局召回下的误报 | 1,680 | 1,397 |
| 全部 N125:30 的 AP 失分百分点 | 2.636165 | 2.259834 |
| P125:1 的 AP 失分百分点 | 11.001595 | 10.149511 |
| P125:1 在约 75% 全局召回下的漏检 | 14,400 | 13,881 |

九个其他观测的局部分类损失逐项下降；它们属于同一道路区域的不同观测，可能在 C 之前已被使用，不能当作全新背景泛化。两段真实道路的误报分别为 910→767、770→630，排序失分合计减少 0.113230 个百分点。未加权与加权的实际召回分别为 75.03057% 和 75.03400%，阈值分别为 5.875 与 6.84375；没有共用一个数值阈值。AP 失分分摊仍受全局排名变化影响，正常侧与异常侧不能相加，更不能把全局 1.10469 个百分点收益全部归于这两段道路。

**本次支持的原因判断是：现有模型能够拟合这处可信训练局部，加强局部监督能够改善其拟合，并在预选其他观测和真实道路片段上出现迁移收益。** 它排除了“这 24 点在当前表示下完全不可拟合”作为优先解释，也说明这次改善不需要增加几何、背景或更新次数。该判断限于本次局部干预；val19 已参与案例与素材选择，因此这些真实片段的改善属于开发证据，不是独立跨场景、跨物体泛化检验。尚不能把 N125 的全部剩余失分归为监督不足，也不能由 P125 排序改善认定异常几何覆盖已经充分。监督项引起的共享参数变化、运行统计量及全局排序变化尚未进一步分解，单次结果没有给出重复性区间。

对应动作是保留新的开发候选及未加权对照，把可信正常局部的监督分配作为一般机制假设；不立即固化权重 1.0、不同时改变几何规模、损失其他项或交互结构。下一轮训练前先落实上节的数据用途；然后预先固定只依赖训练来源标签和误报的统一局部规则，在多种结构上复核同起点、同输入、同预算的对照，并增加随机种子。日常选择只使用内部开发集，val19 仅在约定节点评价。该扩展尚未执行，也没有把现有检查数据改称为未触碰留出集。

本次训练及逐步记录用时约 99.43 秒，完整验证约 238.89 秒，随后素材与排序核查约 32.35 秒。七项针对损失归一化、点身份、记录过程及原续训状态的必要回归通过；科学判断依据上述实际更新、独立观测与正式完整评价。[完整证据](results/train/native/errors/transfer.json)中的 `local_comparison` 记录配对一致性和逐步结果，[正常结构逐项表](results/train/native/errors/normal_cases.csv)与[异常逐项表](results/train/native/errors/attribution.csv)同步更新相应案例，其他案例的未决原因没有被本次结果覆盖。

## 异常任务训练量的核实边界

NDP 的公开 `ap74.ckpt` 直接记录 `epoch=6`、`global_step=791`，即第七轮、791 次更新，批量为 8。结合公开生成与读取流程，每轮推算为 898 个扫描、113 个批次，末批两帧，因此七轮约 6,286 帧次，完整十轮约 8,980 帧次。后两个数是依据代码与公开数据规模的推算，作者没有提供逐条训练清单；在线正常实例粘贴也会增加观测变化。[公开权重](https://huggingface.co/343GltySprk/NDP-OOD/blob/main/ap74.ckpt)、[代码](https://github.com/343gltysprk/ndp)。

COVAL 的 MinkNet 设置为五轮、批量八，每个样本构造两个视图。每轮样本数尚不能核实，故只能条件性写作每轮 N 个样本对应五轮 5N 对、10N 个视图输入，不能代入 449。[原始论文](https://openreview.net/pdf?id=Hly2QYcuLW)。历史本项目最佳模型继承的异常任务访问量为 40,152×8＋2,000×8＝337,216 帧次，语义预训练另计；当前新模型每组为 12,370 帧次。不能将此前的性能差距简单归因于送入扫描太少，也不能宣称已与 COVAL 的训练量对齐。

## 历史路线及其已完成证据

正常背景扩展对照已完成，当前按 AP 优先选中第 2,000 步模型：**AP 68.65256%、FPR95 0.57388%、AUROC 99.84052%**。相对原 68.20276% 基准，AP 提高 **0.44981 个百分点**，FPR95 上升 **0.14250 个百分点**，AUROC 下降 **0.02396 个百分点**。本轮从相同 R2 权重和新建优化器出发，保持同样的 2,000 步调度，逐项保留 14,000 帧次共有输入，只替换 2,000 个 nuScenes 正常位置。结果支持该背景替换方案在本次固定预算下提高 AP，未改善高召回误报。原 68.20276%、67.70225% 和 P1 权重均独立保留。针对性数据、边缘候选和交互模块本轮未改变。完整研究方向仍是：局部观测可能混淆，而完整扫描保留的周围结构可能提供额外判别信息。

## 研究对象与数据层次

输入为原始传感器坐标下的一次完整扫描，包含真实回波的三维坐标和强度；输出为原始点顺序下的异常分数。训练使用正常／异常点监督，保留范围外和忽略标签点作为上下文。局部窗口只用于构造候选与分析，不裁剪模型输入，也不给样本标注“上下文有用”。

数据建设分为三层。基础数据覆盖不同背景、异常几何、合法放置和自然观测条件，并保留可信正常扫描。针对性数据以真实正常结构的局部回波为参照，选择观测描述接近的异常几何和位置。后续困难样本补充依据训练来源中的高分正常点、低分异常点和局部表示混淆，增加新的背景、独立几何与观测条件，继续混入基础数据。

广覆盖用于减少背景和形状依赖；针对性构造为利用周围结构提供候选。二者的用途不同，不要求每个样本都局部难以区分，也不要求先证明上下文有效才启动训练。

## 训练来源与标签

STU 背景仅使用 **206**；201 暂不进入训练。[官方配置](https://github.com/kumuji/stu_dataset/blob/main/Mask4Former3D/conf/semantic-kitti.yaml)将 206 列入训练项，201 位于注释掉的验证项。本地文件夹名称不改变该用途。

nuScenes 只从官方训练列表选择场景。首批选择 20 个不同采集日志中的各一个场景，预先划分为 16 个训练场景和 4 个内部检查场景；对应 642 与 162 个原生带标签关键帧。逐点标签来自官方 lidarseg 发布包，标签通过 sample_data token 与原始扫描一一对应。保留原检测类别文件，32 类逐点类别表单独保存。

逐点类别转换记录在 [labels.json](results/data/labels.json)，训练和内部检查来源记录在 [split.json](results/data/split.json)。统一标签为忽略 0、正常 1、异常 2；模型损失使用 -1、0、1，分别代表忽略、正常、异常。

- STU 保留官方逐点评价转换：原始语义 0 忽略，2 异常，其余已知原始语义为正常。206 的全部 449 个原始扫描没有原生异常。
- nuScenes 将官方 16 类分割任务涵盖的 20 个原始类别作为首批已知正常类别，包含行人、常见车辆、交通锥与围挡、地面、建筑结构和植被。其余 12 个原始类别暂不监督，其中包括噪声、`static.other`、杂物、部分少见车辆和行人子类。
- 忽略点仍保留在输入中；不将未确定类别静默当成正常。类别表是本项目的首批监督定义，不能把所有官方物体类别自动解释为本任务正常类别。[nuScenes 官方标注说明](https://github.com/nutonomy/nuscenes-devkit/blob/master/docs/instructions_lidarseg.md)
- nuScenes 强度按固定常数 255 转换，与 [LitePT 官方 nuScenes 输入实现](https://github.com/prs-eth/LitePT/blob/436d04801c8151faebe66a1b2d368a9711e7e6aa/datasets/nuscenes.py)一致；STU 强度保持原值。不按帧统计强度进行归一化，不旋转或变换坐标轴。

训练监督限于真实回波、有效标签及三维距离含边界的 2.5–50 米。可信零异常帧只要有有效正常点即可参加训练；异常总数为 1–4 的帧剔除，总数不少于 5 的帧保留。合格帧内不按单个物体另设点数门槛。官方评价仍跳过异常总数不足 5 的扫描，包括零异常帧。

## 首批针对性合成

先定义 15 个独立几何身份，12 个用于训练、3 个用于内部检查，再进行生成。每个几何是独立抽取尺寸和指数的单体超二次曲面，来自条状、薄片状、块状三个基础外形；它们作为独立放置的未知障碍代理，不赋予未经确认的真实异常类别。此批尚未加入真实实例变体和可信正常物体重渲染。

构造只使用 206 训练来源。每个几何检查至多 32 个已有训练放置位置，在至多 6 个合法且可见的候选中选择描述差异最小者。正常参照保留同一原始语义、半径 0.35 米内的局部点，异常也在半径 0.35 米内测量。描述包括点数、距离中位数、三个主方向的空间展布、线性程度、平面程度及强度分位数。所用差异是候选排序量，不能直接解释为模型难度。

异常固定在世界坐标中。原始 449 帧分别使用其位姿、128 线射线及由 206 拟合的距离／入射角／线束响应重新观测。接地复用已核对的支持平面并检查新几何的接触；新物体足迹不得超过原位置的已核对足迹。穿插检查覆盖全部 449 帧的已观测非地面回波，包括运动物体；这不代表未观测表面也已得到完整几何认证。合成使用联合最近表面遮挡、回波概率和强度响应，物体遮挡背景但未产生返回时仍移除相应背景回波。

每个世界保存形状、材质、固定姿态、种子、正常参照点、候选描述、剔除原因和有效帧。每帧保存原始帧身份及被替换的原始射线槽位、合成坐标／强度／标签、植入槽位和被遮挡槽位。原始扫描与差量共同精确恢复完整合成扫描，未改动背景逐元素保留。

首批结果如下。扫描数按整帧有效异常总数不少于 5 计算，重复观察同一物体不增加独立几何数。

| 数据 | 训练 | 内部检查 |
| --- | ---: | ---: |
| nuScenes 原始正常场景 | 16 | 4 |
| nuScenes 原始正常扫描 | 642 | 162 |
| 新合成独立几何 | 12 | 3 |
| 新合成完整扫描 | 1,008 | 188 |
| 原始 206 正常扫描 | 449 | — |

当前正常参照实际涵盖道路、卡车、围栏、建筑与地形，其中道路参照占 9/15。此分布仍偏向地面结构，不能代表广泛的正常结构混淆。内部检查几何与新训练几何独立，但仍共享 206 背景，不能作为独立背景泛化验证。nuScenes 场景按采集日志隔离；由于模型已有 nuScenes 语义预训练，这些场景仅作本次数据检查，不声称从未被预训练见过。

## 已完成的首轮训练

起点为历史 R2 完整 STU 验证最好的基础模型：`results/train/r2/0/base/best.pt`，AP 为 62.57595777404128。已经完成的注意力分支未优于该起点。P1 保持此基础模型的全部结构和参数规模，只继续学习正常／异常分割。

当前模型使用 LitePT-S 共享骨干、逐点细节编码和基础分割头。完整扫描只前向一次，所有原始真实点获得预测；体素化保留逆映射，忽略点不参与损失。无需双路输入、原始—合成特征重建或配对一致性损失。

每次更新固定混合 8 帧，微批次为 1 帧，通过梯度累积按整个有效批次的正常／异常有效点总数计算类别平衡损失。

| 来源 | 可用训练扫描 | 每次更新帧数 | 500 次更新读取帧次 |
| --- | ---: | ---: | ---: |
| 既有 240 个 206 合成世界 | 40,152 | 4 | 2,000 |
| 新针对性 206 合成 | 1,008 | 2 | 1,000 |
| nuScenes 正常 | 642 | 1 | 500 |
| 原始 206 正常 | 449 | 1 | 500 |
| 合计 | 42,251 | 8 | 4,000 |

各来源内部按种子控制的无放回排列取样，耗尽后重新排列；每批再打乱帧次序。种子固定为 0。来源、标签、参照、位姿、几何身份等元数据不进入模型特征。每一完整扫描保留原始朝向，关闭旋转、镜像、平移、缩放、抖动、删点、裁剪和跨扫描拼接。

使用新建 AdamW 优化器，全部模型参数峰值学习率为 0.00002，动量为 0.9／0.999，数值项 0.00000001。权重衰减为 0.005，偏置和归一化参数不衰减；全局梯度范数上限为 1。前 25 次更新从峰值学习率的 10% 线性预热到峰值，随后余弦衰减，第 500 次达到峰值的 1%。使用 BF16 计算，稀疏卷积和损失保持现有实现的数值精度；BF16 非有限梯度直接报错，不静默把跳过更新计为成功。

只在完成 500 次更新后执行一次完整 STU 验证。起点复用同一验证清单已经完成的结果，不重复评价。官方实现与样本集合不变，跨全部合格扫描累计有效点后统一计算 AP、FPR95 和 AUROC，不按帧平均。

同时报告第 500 次更新的最终指标和起点指标。AP 更高优先，完全相同时依次比较更低 FPR95、更高 AUROC，再保留更早检查点；若先导未改善，保留原模型并如实报告最终指标。此次结果只用于数据投入判断，不支持对特定数据层或上下文交互的独立归因。

## 已完成续训与旧数据对照的设置

续训从 `results/train/p1/0/pilot/best.pt` 出发，起点完整验证 AP 为 64.35851028490791。继承全部模型参数、AdamW 动量、数值缩放状态和随机数状态；保持同一训练清单、有效批量 8、微批次 1、原始朝向、类别平衡损失及 4＋2＋1＋1 的来源比例。

新建一个完整的 1,500 步学习率调度段。第 1 步学习率为 0.000002，第 75 步升至 0.00002，其后余弦衰减，第 1,500 步降至 0.0000002。第 500、1,000 步的验证不重启调度，也不从中间最佳模型重新开始优化；优化始终沿最后状态继续，选模另行保留 AP 最佳检查点，包括 64.35851% 的起点。

训练种子仍为 0，来源内抽样使用独立的新段排列，段编号为已完成的 500 次更新。各来源内部无放回取样，耗尽后重新排列。新段总共读取 12,000 帧次：6,000 帧既有基础数据、3,000 帧针对性数据、1,500 帧 nuScenes 正常数据、1,500 帧原始 206 正常数据。三个验证区间共享这个连续序列，不在区间边界重新抽取同一批样本。

旧数据对照从首轮相同的 `results/train/r2/0/base/best.pt` 出发，起点 AP 为 62.57595777404128。只抽取既有 40,152 个合成扫描，每次更新 8 帧，共 500 次更新、4,000 个无重复扫描；新建 AdamW 优化器，种子、批量、损失、精度、预热及余弦调度与首轮 P1 保持一致。对照只在第 500 步结束后完整验证，起点同样参与最佳模型选择。

同预算比较是“P1 混合配方 500 步”与“旧数据 500 步”。P2 的额外 1,500 步用于检查继续训练趋势，不能拿它与 500 步对照的差异直接归因于新数据。即使同预算比较支持混合配方，也仍不能分离针对性异常、多场景正常和正常帧监督各自的贡献。

本轮使用现有单张显卡依次完成评分、续训和旧数据对照。候选复查与续训同时推进，两个训练任务不并行争用显卡。检查点、日志与原子替换副本的额外峰值按 1.5 GB 估算，启动前及保存过程中检查 Windows E 盘，保留至少 10 GB 安全余量。

困难样本评分固定使用 P1 最佳模型，在查看分数前确定 355 个训练来源扫描：每个既有合成世界一个扫描，共 240 个；每个新训练几何按异常点数选低、中、高三个观测，共 36 个；每个 nuScenes 训练场景三个扫描，共 48 个；原始 206 轨迹上均匀选取 31 个扫描。内部检查几何、内部检查场景和 STU 验证对象均不进入此次候选挖掘。

每帧同时保存最高分正常点和最低分异常点附近半径 0.35 米内的同类回波，记录原始射线槽位、分数、原始语义、来源及坐标。正常候选在同一来源组内合并相近世界位置或同一 nuScenes 场景；异常候选在同一来源组内合并同一几何或世界。最终保留 32 个高分正常候选和 16 个低分异常候选；这些是候选观测数，不是已经确认的独立物体数。

[候选记录](results/train/p2/diagnostics/candidates.json)和[空间复查表](results/train/p2/diagnostics/review.csv)用于下一轮数据设计，不改变当前标签、抽样或损失。入选的 16 个合成扫描高分正常候选中，有 15 个距离异常点不超过 0.35 米，提示需要检查边缘附近的误报；该空间关系没有说明误报原因，也不能直接解释 STU 验证 FPR95 的变化。

[候选查看图](results/train/p2/diagnostics/candidates.png)显示四个实际例子：灰色为周围回波，彩色为局部同类点的异常分数，红圈标出候选中心。图中局部点数不等于整帧异常点数；局部只有 1–4 点仍可能来自合格训练帧。中文字体为宋体，英文与数字为 Times New Roman，PDF 已检查实际嵌入字体。

## 首轮结果

首轮 500 次更新已全部完成，成功更新 500 次，数值溢出 0 次。共读取 4,000 帧次，累计监督 437,315,946 个正常点和 273,000 个异常点；这些是带重复观测的训练访问次数，不是独立点数。训练约 17.9 分钟，完整验证 223.1 秒，二者合计约 21.6 分钟；模型激活与参数的实测峰值显存为 4.05 GB。

| 完整 STU 验证指标（%） | 起点 | 第 500 次更新 | 变化（百分点） |
| --- | ---: | ---: | ---: |
| AP，越高越好 | 62.57596 | 64.35851 | +1.78255 |
| FPR95，越低越好 | 0.47303 | 0.60386 | +0.13083 |
| AUROC，越高越好 | 99.86368 | 99.85106 | -0.01262 |

两次评价使用相同的 1,960 个合格扫描和 193,879,969 个有效点。按照 AP 优先规则，第 500 次更新的模型已被选中；[最终结果](results/train/p1/0/pilot/result.json)与[完整验证记录](results/train/p1/0/pilot/epoch1.json)保存原始精度数值。

首轮结果显示这套训练流程提高了 AP，同时高召回处的误报比例上升，AUROC 略降。它只有一个种子，且同时改变了正常背景、针对性异常和正常帧监督，不能独立归因于其中某一项，也没有验证上下文交互的因果作用。随后完成的续训及对照见下文。困难样本补充同时检查训练来源中的高分正常结构和低分异常，不直接搬用 STU 验证对象，也不因 FPR95 上升就单独加重正常分数抑制。

## 续训结果

保持 4＋2＋1＋1 的 1,500 次新增更新已全部完成，成功更新 1,500 次、数值溢出 0 次。三个验证区间各读取 4,000 帧次，合计 12,000 帧次、8,099 个不同扫描记录；累计监督 1,312,833,672 个正常点和 899,759 个异常点，仍是带重复观测的访问次数。三个区间及完整验证合计 3,821.94 秒，约 63.7 分钟。

| 本轮新增更新 | AP（%） | FPR95（%） | AUROC（%） | 是否替换最佳模型 |
| --- | ---: | ---: | ---: | --- |
| 0，首轮 P1 起点 | 64.35851 | 0.60386 | 99.85106 | 保留 |
| 500 | 63.42496 | 0.85078 | 99.80564 | 否 |
| 1,000 | 62.89491 | 0.79487 | 99.81111 | 否 |
| 1,500 | 63.44493 | 0.63316 | 99.83937 | 否 |

三次验证均为同一 1,960 个合格扫描和 193,879,969 个有效点。最终 AP 相对本轮起点下降 0.91358 个百分点，FPR95 上升 0.02931 个百分点。最后一次 AP 比第 1,000 步有所回升，但本次固定配方和调度段在 1,500 步预算内没有提高最佳 AP。不能据此断言所有续训调度均无效，也不能归因于某一类新增数据。

完整记录为 [500 步](results/train/p2/0/pilot/epoch1.json)、[1,000 步](results/train/p2/0/pilot/epoch2.json)、[1,500 步](results/train/p2/0/pilot/epoch3.json)与[最终结果](results/train/p2/0/pilot/result.json)。`best_update=0`，最佳权重与 P1 最佳权重逐参数相同；最终优化器的所有参数状态均为 2,000 步，即继承的 500 步加本轮 1,500 步。只保存 AP 最佳权重与最后恢复状态，各中间区间保留完整验证记录。

## 同预算旧数据对照

旧数据对照已完成 500 次更新，成功 500 次、溢出 0 次。共读取 4,000 个不同的既有合成扫描，累计监督 484,863,917 个正常点和 481,187 个异常点。训练约 18.9 分钟，完整验证约 210.2 秒，二者合计 1,345.85 秒，约 22.4 分钟。读取内容全部属于既有基础数据，不含新针对性扫描、nuScenes 正常扫描或原始 206 正常帧。

| 条件 | AP（%） | FPR95（%） | AUROC（%） |
| --- | ---: | ---: | ---: |
| 共同 R2 起点 | 62.57596 | 0.47303 | 99.86368 |
| 仅旧数据，第 500 步终点 | 61.71996 | 0.54568 | 99.84740 |
| 混合配方 P1，第 500 步终点 | 64.35851 | 0.60386 | 99.85106 |
| 混合终点减旧数据终点，百分点 | +2.63855 | +0.05818 | +0.00366 |

两次 500 步实验的起始权重相同，种子、批量、优化器参数、精度、损失和学习率调度一致，使用同一完整验证集。该比较支持混合配方在这个起点、种子与 500 步预算下优于仅用旧数据的 AP；旧数据终点反而比共同起点低 0.85600 个百分点。因此，首轮 AP 的提高不能仅凭本次旧数据对照解释为普通继续训练的收益。但混合配方的 FPR95 比旧数据终点高 0.05818 个百分点，高召回误报仍是未解决的问题。

两组都允许保留起点。旧数据组因终点 AP 较低而保留 R2 起点；按相同选模规则比较，混合组的最佳 AP 高 1.78255 个百分点。2.63855 个百分点是两个第 500 步终点之间的差异，不能把它写成两组已选最佳模型的差异。

对照的[配置](results/train/old/0/pilot/config.json)、[完整验证](results/train/old/0/pilot/epoch1.json)和[结果](results/train/old/0/pilot/result.json)保留原始精度数值。实际检查点确认旧数据优化器从零累计到 500 步。两项实验均已完成，合计训练及验证约 86.1 分钟；本轮未启动此前取消的三分支长程实验。

这是一个种子的短程验证结果，不是跨种子稳定性的证据。混合组同时增加了针对性异常、多场景正常和原始正常帧监督；仅旧数据与混合配方的比较不能分离它们各自的贡献，也没有检验上下文交互机制。

## 续训结论的范围

P2 续训结束时保留了 `results/train/p1/0/pilot/best.pt`，随后进入本轮严格配对对照。续训没有收益只说明本次设置不值得继续加预算，不能说明模型或数据已经达到上限。

## 严格配对的六加一加一对照

本轮从共同 R2 权重出发，新建 AdamW 优化器，训练 500 步并完整验证一次。P1 的原始取样函数由提交 `c73395f` 中的实现独立重建，使用相同训练清单、种子 0 和 500 步预算。每一步直接保留四帧旧合成、一个 nuScenes 正常扫描和一个原始 206 正常扫描的身份及批内位置，只替换两个针对性扫描。补入的 1,000 个旧合成扫描由独立随机序列无放回抽取，排除 P1 已使用的 2,000 个旧合成扫描。

| 内容 | 每次更新 | 500 步总帧次 | 与 P1 的关系 |
| --- | ---: | ---: | --- |
| 共有的旧合成扫描 | 4 | 2,000 | 同一步、同批内位置、同扫描 |
| 替代针对性扫描的旧合成 | 2 | 1,000 | 仅替换原针对性位置，互不重复 |
| nuScenes 正常扫描 | 1 | 500 | 同一步、同批内位置、同扫描 |
| 原始 206 正常扫描 | 1 | 500 | 同一步、同批内位置、同扫描 |

全部 500 步均完成逐项取样核对，共有 3,000 帧次完全一致，只有 1,000 帧次发生上述替换。不是仅通过相同种子假定配对。[取样记录](results/train/paired/0/pilot/sampling.json)保存两条完整的 4,000 帧次序列和训练清单身份；[配置](results/train/paired/0/pilot/config.json)记录共同起点及优化设置。峰值学习率、前 25 步预热、末尾降至峰值 1%、批量、精度和损失均与 P1 一致。每帧仍完整输入并保持原始朝向。

同时报告第 500 步固定终点和允许保留起点的最佳结果，AP 优先，其次才是 FPR95、AUROC。对照完成后按 AP 的方向和幅度决定是否继续投入针对性数据；单个种子的细小差异只视为尚未看到明确增益，不据此声称稳定优势或统计显著性，也不事后把小数差距包装成确定胜负。

500 次更新已全部完成，成功更新 500 次、溢出 0 次。共读取 4,000 帧次，累计监督 436,651,623 个正常点和 395,139 个异常点；这些是训练访问次数。训练约 952.9 秒，完整验证约 174.0 秒，总计 1,127.06 秒，约 18.8 分钟。验证仍使用与 P1 相同的 1,960 个扫描、193,879,969 个有效点及官方逐点评价方式。

| 条件 | AP（%） | FPR95（%） | AUROC（%） |
| --- | ---: | ---: | ---: |
| 共同 R2 起点 | 62.57596 | 0.47303 | 99.86368 |
| P1：4＋2＋1＋1，第 500 步 | 64.35851 | 0.60386 | 99.85106 |
| 严格配对：6＋1＋1，第 500 步 | 67.70225 | 0.37411 | 99.87865 |
| 六加一加一减 P1，百分点 | +3.34374 | -0.22975 | +0.02759 |

两组的第 500 步都优于共同起点，因此允许保留起点时，两组最佳结果均等于各自固定终点，上表差异不变。该短程对照选中 `results/train/paired/0/pilot/best.pt`；`best_update=500`，最佳权重与最后状态逐参数一致，优化器实际累计 500 步。该权重与 P1 文件继续保留。[完整验证](results/train/paired/0/pilot/epoch1.json)与[最终结果](results/train/paired/0/pilot/result.json)保存原始精度数值。

本次配对比较中，移除现有两帧针对性扫描并补入旧合成后，AP、FPR95 和 AUROC 均改善。当前针对性数据以每批两帧的方式加入，没有显示相对于额外旧合成扫描的增量价值。这支持优先采用 6＋1＋1，并暂停扩大现有针对性配方。结果仍限于这个种子、初始化、数据分布与 500 步预算，不构成所有针对性构造均无效的结论。

## 实际被替换样本的分布

本轮进一步核查两组在被替换位置上的实际 1,000 个扫描，未扩大到新的训练或生产。几何计数依据局部形状参数去重，排除材质与世界姿态；几何参数不同不保证局部观测容易区分。

| 被替换位置上的样本 | P1 针对性扫描 | 六加一加一补入的旧合成 |
| --- | ---: | ---: |
| 不同扫描数 | 1,000 | 1,000 |
| 几何参数组数 | 12 | 224 |
| 世界实例数 | 12 | 225 |
| 每帧有效异常点数中位数 | 17 | 36 |
| 每帧仅有 5–9 个有效异常点的扫描数 | 285 | 189 |
| 每帧异常距离中位数的中位数（米） | 10.19 | 14.12 |
| 每帧异常距离中位数的第九十分位（米） | 18.66 | 36.08 |
| 异常点总访问数 | 25,361 | 147,500 |

两组仍都来自 206 背景。225 个旧世界实例不是 225 个独立背景，也不能直接当作 225 个独立几何；去重后的形状参数组为 224。全部 2,000 个差量扫描的有效异常点数均与训练记录一致，非空插入回波没有正常标签。该项确认记录与标签的一致性，不能独立证明每种代理形状的真实类别含义。

[分布记录](results/train/paired/diagnostics/distribution.json)显示当前针对性分支集中于较少几何、较少回波和较窄的距离覆盖。局部相似性不能以明显缩窄几何和观测覆盖为代价。这些因素同时变化，本轮不能确定哪一个造成 AP 差异；现有损失分别按整个有效批次的正常点数与异常点数归一化，不能将异常点总数更多直接解释为收益来源。边缘核查也未发现足以解释本轮差距的局部错标或遮挡遗漏。当前先验证 6＋1＋1 的训练潜力，针对性重设计后移，不同时修改模型、损失和交互模块。

## 六加一加一的完整调度

本轮只延长训练预算及对应学习率调度：共同 R2 权重、新建 AdamW 优化器、种子 0、固定现有数据池和每批六个旧合成、一个 nuScenes 正常、一个原始 206 正常扫描。总预算为 2,000 次更新，前 100 步从峰值的 10% 预热至 `2e-5`，之后按原有余弦公式降至第 2,000 步的 `2e-7`。在第 500、1,000、1,500、2,000 步分别完整验证，按 AP 优先规则选模，允许保留 R2 起点。

前 500 步直接复用已完成对照的全部 4,000 帧次身份和批内位置。后 1,500 步使用同一数据池、6＋1＋1 配额及固定的取样段 1；每个来源按随机排列无放回读取，耗尽后再开始下一轮排列。两段之间允许再次访问同一扫描，这属于增加训练步数带来的重复观测。全程共 16,000 帧次，包含旧合成 12,000、nuScenes 正常 2,000、原始 206 正常 2,000；对应不同记录数分别为 11,325、642、449。没有加入针对性扫描或已筛出的边缘困难候选。

本轮输出单独保存在 `results/train/main/0/pilot/`，不改写 `results/train/paired/0/pilot/best.pt`。新建优化器的累计更新应为本轮 2,000 次，不继承先前 500 步状态。每 100 步保存可恢复状态并复查 Windows E 盘；正式启动时没有其他 CUDA 计算任务，采用四个数据读取进程与两个 PyTorch 计算线程，数值库各一个线程。

第 500 步处于长调度中，不能把这一点与先前已经降至最终学习率的短调度终点等同解释。预算完成后同时报告固定 2,000 步终点、本轮允许保留起点的最佳结果，以及与保留的 67.70225% 模型的比较。若最佳 AP 超过该值，保留新最佳，再决定背景扩展的投入；若未超过，停止单纯增加步数，下一项只扩大 nuScenes 正常背景的独立场景并作短对照。此处预先记录决策条件，不依据中途指标修改数据、损失、调度或交互模块。

2,000 次更新现已全部完成，成功更新 2,000 次、数值溢出 0 次。四次验证均使用相同的 1,960 个扫描、193,879,969 个有效点和官方逐点评价实现。

| 模型与验证点 | AP（%） | FPR95（%） | AUROC（%） |
| --- | ---: | ---: | ---: |
| 共同 R2 起点 | 62.57596 | 0.47303 | 99.86368 |
| 保留的短程对照，第 500 步 | 67.70225 | 0.37411 | 99.87865 |
| 本轮第 500 步 | 64.89110 | 0.42920 | 99.86652 |
| 本轮第 1,000 步 | 67.31483 | 0.46156 | 99.85556 |
| 本轮第 1,500 步 | 67.55835 | 0.48272 | 99.85212 |
| 本轮第 2,000 步 | **68.20276** | **0.43137** | **99.86449** |

本轮固定 2,000 步终点与允许保留起点的最佳结果相同，`best_update=2000`。相对短程对照，AP 提高 0.50050 个百分点，FPR95 上升 0.05727 个百分点，AUROC 下降 0.01416 个百分点；相对 R2，AP 提高 5.62680 个百分点。按预先确定的 AP 优先规则，该轮选中 `results/train/main/0/pilot/best.pt`，现作为背景扩展的保留基准。最佳与最后模型逐参数相同，两个保存状态中的优化器均实际累计 2,000 次更新，最终学习率为 `2e-7`。原 67.70225% 模型和 P1 文件经核对未被改写。

全程读取 16,000 帧次、12,416 个不同扫描记录，累计监督正常点 1,747,149,206 次、异常点 1,607,101 次。四个区间的点数均由实际取样序列独立复算，与真实训练报告完全一致；前 500 步的点数也与短程对照完全一致。训练约 45.08 分钟，四次完整验证合计约 11.40 分钟，总计 3,389.03 秒，约 **56.48 分钟**。模型张量的峰值分配显存约 4.06 GB，另有 CUDA 与稀疏运算库占用。运行期间无交换内存增长，结束时 E 盘仍有约 54.26 GB。

结果支持这套固定配方在该次完整长调度下取得更高 AP，因此执行“保留新最佳，再决定背景扩展投入”的分支。0.50050 个百分点来自单个种子、同一验证集上的预定四次检查，尚未估计重复训练波动，不能声称统计显著性或保证继续增加步数仍有收益。FPR95 和 AUROC 相对短程模型回退，不能表述为三项指标继续同时改善。该轮没有改变数据池、增加边缘候选或重做针对性样本。随后授权的背景扩展对照见下文。

[训练配置](results/train/main/0/pilot/config.json)、[实际取样](results/train/main/0/pilot/sampling.json)、[500 步](results/train/main/0/pilot/epoch1.json)、[1,000 步](results/train/main/0/pilot/epoch2.json)、[1,500 步](results/train/main/0/pilot/epoch3.json)、[2,000 步](results/train/main/0/pilot/epoch4.json)及[最终结果](results/train/main/0/pilot/result.json)保留原始精度数值。

## 固定预算的正常背景扩展

本轮使用 nuScenes 官方训练划分的全部可用场景，排除内部检查集所在的全部采集日志。官方训练划分有 700 个场景，4 个隔离日志涉及其中 72 个场景，剩余 **628 个场景、46 个采集日志**。场景与采集日志是不同层次，628 个场景不能表述为 628 次独立采集。

每个场景先按原生关键帧时间排序，再用包含两端的八个等距序号选帧，序号采用四舍五入。选帧不查看类别点数或模型分数。共选出 **5,024 帧**，逐帧读取原始扫描与通过 sample_data token 对应的 lidarseg 标签，全部存在且合格，没有剔除扫描。通过独立的 sample 表复核，628 个场景的选帧身份均与时间间隔规则一致。标签转换与原池完全相同，未确定类别和范围外点仍作为上下文输入并忽略监督。[扩展正常池](results/data/background.json)保存来源、时间、标签路径、隔离日志和每帧有效正常点数，原始标签转换与划分文件不改写。

训练直接读取上一轮 `results/train/main/0/pilot/sampling.json` 的实际序列。全部 **14,000 帧次**共有输入保持同一步、同批内位置、同扫描记录，其中旧合成 12,000 帧次、原始 STU 206 正常 2,000 帧次。仅将另外 **2,000 个 nuScenes 位置**替换为扩展池输入。场景按随机排列逐轮各抽一帧，同一场景先使用未抽过的帧；本轮 2,000 帧全部不重复，628 个场景全部覆盖，其中 512 个场景各抽三帧、116 个各抽四帧，覆盖 46 个采集日志。旧配方的 2,000 次 nuScenes 输入只有 642 个不同帧，来自 16 个场景和 16 个日志。

| 训练来源 | 两轮相同的帧次 | 原配方有效正常点访问数 | 扩展后有效正常点访问数 | 两轮有效异常点访问数 |
| --- | ---: | ---: | ---: | ---: |
| 旧合成 | 12,000 | 1,455,188,725 | 1,455,188,725 | 1,607,101 |
| 原始 STU 206 正常 | 2,000 | 243,326,142 | 243,326,142 | 0 |
| nuScenes 正常 | 2,000 | 48,634,339 | 47,136,946 | 0 |

这些是训练访问次数，含重复输入，不是独立点数。扩展后的 nuScenes 正常点访问数减少 1,497,393，不能把场景增加理解为监督点数或该来源权重增加。损失仍分别按整个有效批次的正常、异常点数归一化；正常类内部各来源的相对贡献会随实际有效点数变化，因此本轮评价的是整个背景替换方案的收益，不能单独归因于场景数量。

起点、种子 0、新建 AdamW 优化器、峰值学习率 `2e-5`、100 步预热、最终学习率 `2e-7`、每 500 步完整 STU 验证、AP 优先选模、完整单帧和原始朝向均与上一轮一致。输出独立保存在 `results/train/background/0/pilot/`。本轮不加入针对性扫描、边缘困难候选或交互模块，不扩大旧合成池。若超过保留的 68.20276%，再决定完整数据建设投入；若未超过，保留原基准，停止给该扩展配置追加步数，下一项转向异常覆盖与评分能力。该条件在训练前确定，不根据中途指标改变本轮设置。

本轮成功完成全部 2,000 次更新，数值溢出与跳步均为零。四次评价均覆盖相同的 1,960 个 STU 扫描和 193,879,969 个有效点，使用相同的官方评价实现。

| 更新步数 | 原背景配方 AP（%） | 扩展背景 AP（%） | AP 差值（百分点） | 扩展背景 FPR95（%） | 扩展背景 AUROC（%） |
| --- | ---: | ---: | ---: | ---: | ---: |
| 500 | 64.89110 | 66.19149 | +1.30039 | 0.50117 | 99.85141 |
| 1,000 | 67.31483 | 68.33163 | +1.01679 | 0.55289 | 99.84165 |
| 1,500 | 67.55835 | 68.56185 | +1.00351 | 0.57091 | 99.83654 |
| 2,000 | 68.20276 | **68.65256** | **+0.44981** | 0.57388 | 99.84052 |

固定 2,000 步终点与允许保留 R2 起点的最佳结果一致，`best_update=2000`；当前采用 `results/train/background/0/pilot/best.pt`。最佳与最后模型逐参数相同，保存的优化器均实际累计 2,000 次更新。原 68.20276% 模型、67.70225% 模型及 P1 文件经核对没有被改写。

实际训练共 16,000 帧次、13,774 个不同扫描记录，累计有效正常点访问数为 1,745,651,813，异常点访问数为 1,607,101。四个区间的各来源计数和总点数都从实际取样序列独立复算，与训练读取结果一致；所有共有扫描在同一步、同批内位置逐项一致。nuScenes 的 2,000 次输入确为 2,000 个不同帧，场景访问次数与上述三／四帧配额一致。训练约 45.22 分钟，四次完整验证合计约 11.22 分钟，总计 **3,386.49 秒，约 56.44 分钟**，与原配方的 56.48 分钟接近。模型张量峰值分配显存约 4.06 GB；结束时 E 盘剩余约 54.21 GB，运行中交换内存未增长。

四个预定检查点的 AP 均高于原配方相同步数，最终提高 0.44981 个百分点，满足预先规定的保留新最佳条件。相对保留基准，最终 FPR95 增加 0.14250 个百分点、AUROC 降低 0.02396 个百分点，因此不能表述为三项指标同时改善。四个检查点来自同一次训练和同一验证集，并非四次独立重复；单个种子的结果尚不能说明增益在重复训练中稳定。当前证据支持继续评估完整数据建设的投入，但没有检验上下文交互机制，也不能把收益唯一归因于场景数量或额外监督权重。

[配置](results/train/background/0/pilot/config.json)、[逐步取样与来源计数](results/train/background/0/pilot/sampling.json)、[500 步](results/train/background/0/pilot/epoch1.json)、[1,000 步](results/train/background/0/pilot/epoch2.json)、[1,500 步](results/train/background/0/pilot/epoch3.json)、[2,000 步](results/train/background/0/pilot/epoch4.json)与[最终结果](results/train/background/0/pilot/result.json)保留原始精度数值。

## 边缘候选核查

本轮对前次选出的 16 个合成扫描高分正常候选逐一核查，包含旧合成 8 个、针对性合成 8 个。其中 15 个距离异常点不超过 0.35 米。使用原始 206 扫描、保存的点差量、固定物体几何、位姿和校准射线恢复每个候选；检查中心 0.35 米邻域内的原始及合成回波，并加入中心射线附近的三乘三束线／方位邻域，共复算 946 条候选局部射线。

16 个中心点的原始坐标、强度和正常标签均保持不变，正常局部回波没有被合成差量改写。两档求交精度分别使用 96／384 个初始采样、8／10 层自适应细分及 24／32 次二分；局部可见性判断一致，未发现应被遮挡但仍保留的背景点，也未发现插入点的前景归属或异常标签不一致。局部正常点没有落入物体内部。合成回波坐标与更细射线复算的最大距离误差为 0.0000007204 米；这是数值一致性误差，不是实际传感器测量精度。

体素检查采用模型实际的原点固定五厘米网格，并与 `prepare_scan` 的原始点到体素归属逐项核对；随后按骨干的四次二倍网格合并检查父单元。

| 空间单元边长 | 中心与有效异常点共享单元的候选数 |
| --- | ---: |
| 5 厘米 | 1／16 |
| 10 厘米 | 7／16 |
| 20 厘米 | 8／16 |
| 40 厘米 | 15／16 |
| 80 厘米 | 15／16 |

最细网格混合的例子是旧合成第 268 帧、槽位 103987，同一体素包含一个正常点和一个异常点。模型在体素内聚合输入和特征，但逐点细节与逐点损失标签仍保留，未把整个混合体素改成单一类别。因此，不能把共享体素直接等同于标签污染，也不能认定它已解释高分原因。其余 15 个候选在最细体素中没有异常点，较粗单元共享说明周围异常可能进入其上下文表示，但仍只是空间关系。

[逐项核查记录](results/train/paired/diagnostics/edges.json)、[汇总表](results/train/paired/diagnostics/edges.csv)和[实际回波图](results/train/paired/diagnostics/edges.png)保存这些结果。图中绿色线框为中心点所属的五厘米体素，灰色为正常回波、红色为异常回波、蓝圈为高分正常中心；中文宋体和英文 Times New Roman 已检查实际渲染与 PDF 嵌入。

这 16 个观察通过了本次标签与局部可见性核查，可保留为可信的高分正常候选，暂不加入训练。检查范围限于这些候选及其局部射线，不是对全部合成数据的物理认证；这些经过极值筛选的候选也不能用于估计整体误报率，不能直接解释 STU 验证 FPR95。

## 完整数据方向

完整方向要求两个传感器域都有正常与异常观测。nuScenes 正常监督现已覆盖 628 个官方训练场景；其异常生成仍需以原生观测模型进行小批适配，检查采样与遮挡后扩展，不将 32 线点簇直接贴入 128 线扫描。此前约 100 个场景的先导建议已被本轮正常背景扩展覆盖，不再作为当前正常池的规模目标。

恢复数据扩展时，优先增加新的场景、独立几何、合法位置及可信正常物体的同流程重渲染，再增加同一固定物体的重复帧。补充样本的依据来自训练来源的模型错误，不把 STU 验证对象转为训练材料。上下文受限与完整模型、不同交互方式的必要比较，放在数据稳定生成且收益值得追踪之后。

历史 R1／R2 定义及其结果保留各自版本身份；此前取消的三分支长程实验不再由当前训练入口启动。本文件描述沿用 P1 数据的配对对照、已完成续训证据及主配方的完整调度，历史方案可由版本控制查阅。

## 当前文件与复现入口

- [标签转换](results/data/labels.json)、[来源划分](results/data/split.json)、[正常扫描](results/data/normal.json)、[针对性世界索引](results/data/targeted.json)与各世界 `world.json`。
- [数据读取与训练池构建](src/data.py)、[物理生成与候选描述](src/render.py)、[短程训练与续训入口](src/train.py)、[官方评价及困难候选入口](src/evaluate.py)。
- [新数据实际读取核验](results/data/inspection.json)，以及 [206 原始观测统计](results/206/report.md)。
- [针对性扫描及其局部参照图](results/data/candidates.png)和 [nuScenes 正常监督示例](results/data/normal.png)。图片显示真实数据，50 米显示窗口仅用于查看；训练保留完整扫描。对应 PDF 嵌入 Times New Roman 字体。
- [先导资源与计时记录](results/train/p1/preflight.json)及 `results/train/p1/0/pilot/` 下的训练配置、完整验证与结果。
- [续训结果](results/train/p2/0/pilot/result.json)、[旧数据对照结果](results/train/old/0/pilot/result.json)与[困难候选查看图](results/train/p2/diagnostics/candidates.png)。
- [严格配对结果](results/train/paired/0/pilot/result.json)、[逐步取样](results/train/paired/0/pilot/sampling.json)、[替换样本分布](results/train/paired/diagnostics/distribution.json)与[边缘核查](results/train/paired/diagnostics/edges.json)。
- [主配方完整调度结果](results/train/main/0/pilot/result.json)、[训练配置](results/train/main/0/pilot/config.json)与[实际取样](results/train/main/0/pilot/sampling.json)。
- [扩展正常池](results/data/background.json)与 `results/train/background/0/pilot/` 下的配置、逐步取样、各来源监督点数和完整验证记录。

当前数据清单和历史结果已存在。扩展池由 `python -m src.data expand --output results/data --workers 8` 生成；使用下述同一 Python 环境和数值库线程限制。背景对照命令如下；如需复跑应指定新的输出位置，以保留有效证据：

```bash
OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 /home/jasongao/Study/AJAE/.venv/bin/python -m src.train --normal-manifest results/data/background.json --output results/train/background --initial results/train/r2/0/base/best.pt --updates 2000 --eval-every 500 --recipe background --optimizer-state reset --sampling-segment 0 --workers 4 --threads 2 --save-every 100
```

候选评分入口为 `python -m src.evaluate mine --checkpoint results/train/p1/0/pilot/best.pt --output results/train/p2/diagnostics --workers 4`，使用同一 Python 环境与线程配置。评分占用显卡时不同时运行训练。

中断后使用同一配置加 `--resume`；不覆盖旧训练。原始数据、差量、训练清单和检查点遵守仓库忽略规则，只提交代码、来源记录和可复查结果。数据原件留在本机既有数据目录。
