% Compile from the repository root with pdfLaTeX, BibTeX, pdfLaTeX, pdfLaTeX.
% ICLR style files: https://media.iclr.cc/Conferences/ICLR2027/iclr-2027-style-files.zip
\documentclass{article}
\usepackage{vendor/iclr2027/iclr2027_conference,times}
\usepackage{amsmath,amssymb,booktabs,array,graphicx,xcolor,placeins,xurl}
\usepackage{hyperref}
\hypersetup{colorlinks=true,linkcolor=black,citecolor=black,urlcolor=black,
  pdftitle={Contextual Normal Return Fields for Point-Level LiDAR Anomaly Segmentation},
  pdfauthor={Anonymous authors}}
\urlstyle{same}
\newcommand{\R}{\mathbb{R}}
\newcommand{\given}{\,\vert\,}
\newcommand{\blank}{\phantom{00.00}}
\newcommand{\figureblank}[2]{%
  \fbox{\parbox[c][#1][c]{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{%
    \centering\color{gray}\textit{#2}}}}

\title{Contextual Normal Return Fields for\\Point-Level LiDAR Anomaly Segmentation}
\author{Anonymous authors}

\begin{document}
\maketitle
% Modern fancyhdr needs the template's header outside the title's local group.
\lhead{Under review as a conference paper at ICLR 2027}

\begin{abstract}
Point-level LiDAR anomaly segmentation must identify objects outside a normal semantic set while retaining normal objects whose sparse returns may look unfamiliar. We introduce contextual normal return fields to learn the observations supported by the surrounding scene and use these alternatives in the anomaly decision. A context encoder withholds a target angular block and predicts multiple three-dimensional return-rate fields. Each field provides a shared spatial explanation for the block's rays, and analytic integration converts it into normalized return-distance distributions. A block-level likelihood trains each explanation against a collection of normal returns before marginalizing the hypothesis identity. A point-level decoder then combines the support of the complete hypotheses with actual-observation features to produce one anomaly score. Original nuScenes scans supervise normal prediction, while scans containing auxiliary anomalies and inserted normal objects supervise discrimination. Both insertion roles use the same observation procedure, and grouped supervision gives inserted normal objects an explicit share of the classification objective. The evaluation examines this learning mechanism through source-domain development, controlled component comparisons, and AP, AUROC, and FPR95 on STU.
% Add measured findings after the source and target evaluations are complete.
\end{abstract}

\section{Introduction}
\label{sec:intro}

An unfamiliar road object can occupy only a handful of LiDAR returns. A distant pedestrian can be equally sparse and poorly resolved. Point-level anomaly segmentation must distinguish these cases according to their semantic identity while accounting for changes in range, sampling density, and scene layout. STU formalizes this task with point annotations for road objects outside a specified normal semantic set \citep{stu}.

Auxiliary unknown objects provide examples from which a model can learn an anomaly decision. Open-set LiDAR methods obtain such supervision through transformed or inserted objects \citep{real,lion} and connect it to learned anomaly scores \citep{rel,ndp}. Normal semantic prototypes and reconstructed features provide additional references for interpreting unfamiliar observations \citep{lido,doodle}. These approaches motivate a complementary learning problem: infer which normal observations the surrounding scene supports, then compare an actual return with those predictions. A context-dependent reference can supply evidence beyond resemblance to the finite set of auxiliary objects used for training.

Constructing this reference requires specifying both the information available to the predictor and the relationship among its outputs. We withhold a target angular block and predict from the measurements in surrounding blocks. The predictor therefore learns to infer the target observations from context. Neighboring LiDAR rays then query a common three-dimensional field, which ties their distance predictions to the same spatial parameters. Several candidate fields represent alternative normal arrangements when the surrounding scene admits more than one continuation.

We formulate these predictions as \emph{contextual normal return fields}. Each hypothesis is a spatial return-rate field whose analytic integral along a ray yields a normalized distribution over return distance. During normal learning, the likelihood of all reliable normal returns in a block is computed under each candidate field before the hypothesis identity is marginalized. This order of operations trains a shared explanation of the block. At inference, a point-level readout retains the likelihood and prior of each complete hypothesis, allowing the anomaly decision to use both agreement among explanations and differences in their support for the measured return.

The final decision also needs the identity of the observed object. A normal vehicle placed on a road can differ from the structure predicted by its surroundings. Likelihood measures support under a learned distribution, whereas the anomaly label describes semantic membership \citep{likelihood,perfectdensity}. We therefore combine the normal-hypothesis readout with an encoder of the actual scan and learn a single point-level score. Normal prediction and discriminative supervision jointly train this mechanism, following the broader use of complementary normal and anomaly objectives \citep{densehybrid,draem}.

The training observations support this distinction directly. Original nuScenes scans provide normal-field targets, and modified versions provide point-level detection labels. Auxiliary anomalies and normal cars, pedestrians, bicycles, and motorcycles undergo the same road-placement and observation procedure. The inserted normal objects retain their normal labels and receive an explicit share of the classification loss. The decoder thus learns normal retention alongside anomaly detection under the changes introduced by insertion.

Our contribution is an end-to-end anomaly segmentation mechanism built around a contextual normal reference: shared spatial fields define the predictions, a block-level likelihood trains their common explanations, and a point-level readout makes those explanations available to discrimination. The experimental design tests the contribution of this mechanism under fixed source supervision, examines normal-object retention and sparse observations, and evaluates transfer from nuScenes to STU using one score for all three official point-level metrics.

\section{Related Work}
\label{sec:related}

\paragraph{Point-level unknown detection.}
REAL learns unknown responses alongside known semantic classes, and LiON uses diverse synthetic objects to train point-wise abstention \citep{real,lion}. Relative energy learning and neural distribution priors further connect anomaly supervision to learned segmentation scores \citep{rel,ndp}. LIDO constructs complementary prototype, entropy, and contrastive evidence from normal training data \citep{lido}. COVAL studies visibility interventions for LiDAR anomaly learning \citep{coval}. These methods establish semantic representation, auxiliary anomalies, and observation structure as useful sources of supervision. Our method develops an explicit conditional normal-return representation that a point-level decoder reads at inference.

\paragraph{Normal prediction for anomaly segmentation.}
Image resynthesis and inpainting provide normal references for road anomaly detection \citep{erasing}. DRAEM couples reconstruction with discriminative anomaly segmentation, while DenseHybrid combines normal-density and outlier-discrimination objectives \citep{draem,densehybrid}. InTra excludes the target image patch before predicting it from its neighborhood, and UniAD restricts reconstruction pathways to address input copying \citep{intra,uniad}. In three dimensions, DOODLE augments LiDAR anomaly scores with diffusion-based feature reconstruction, and GPFlow models normal feature distributions with Gaussian prototypes \citep{doodle,gpflow}. We predict distributions of measured distances from shared spatial hypotheses and retain the support of each hypothesis in the anomaly readout.

\paragraph{Conditional prediction and geometric representation learning.}
Blind-spot denoising predicts a distribution from surrounding observations and combines it with the measured target \citep{blindspot,pn2v}. Conditional neural processes learn context-dependent prediction rules with uncertainty over queried outputs \citep{cnp}. In point clouds, masked reconstruction learns geometric representations \citep{pointmae}; UniPAD and Ponder connect three-dimensional fields to rendered observations \citep{unipad,ponder}. Probabilistic LiDAR fields also connect ray attenuation with return distributions \citep{plink}. Our formulation brings shared field prediction into the anomaly decision itself through a learned comparison with observed points.

\paragraph{Source-domain geometry and data construction.}
Scene completion, sensor-aware augmentation, and density variation support cross-domain LiDAR perception \citep{complete,idam,lidardg}. Mix3D and PolarMix vary the surroundings of real point-cloud instances while preserving their labels \citep{mix3d,polarmix}. We use source observations to construct both auxiliary anomalies and normal insertion controls, and organize the learning problem around their point-level semantic labels. This connects the training construction directly to the normal-reference mechanism.

\section{Contextual Normal Return Fields}
\label{sec:method}

\subsection{Point-Level Prediction from Observations and Normal Context}

Let $X=\{(x_i,I_i)\}_{i=1}^{n}$ denote one LiDAR scan, where $x_i\in\R^3$ is a return coordinate and $I_i$ its intensity. Each return has a direction $d_i$ and distance $r_i$ relative to the scan reference origin. The network produces a scalar $a_i\in\R$ whose ordering represents anomaly evidence. Training labels assign reliable normal points to zero, auxiliary anomaly points to one, and ambiguous points to an ignored set.

The model contains an actual-observation encoder and a normal-context predictor with separate parameters. The observation encoder uses LitePT-S \citep{litept} with a point-detail pathway to obtain $f_i\in\R^{64}$. It integrates multi-scale scene features, raw-point detail, and the point's position within its voxel. The normal predictor partitions directions into angular blocks at three scales. For each target block $B$, it reads the surrounding blocks $C_B$ and predicts a distributional reference for the target rays. The two pathways meet in the point-level readout shown in Figure~\ref{fig:method}.

\begin{figure}[t]
\centering
\includegraphics[width=.9\linewidth]{figures/method.pdf}
\caption{The pointwise anomaly prediction network. One scan supplies two independently parameterized branches. The observation encoder combines LitePT-S, point detail, and multi-scale fusion into $f_i\in\mathbb R^{64}$. The normal predictor excludes the target block from its context and decodes four spatial hypotheses, each containing eight Gaussian components, with separate prior weights. Its parameters are shared across $1^\circ$, $2^\circ$, and $4^\circ$ angular grids. Ray evaluation converts the fields into return densities and passes the priors to the readout. Point features and measured range guide attention over the hypotheses. The readout concatenates $f_i$, three 32-dimensional attention outputs, and three marginal log densities into 163 channels. The point head produces one anomaly score $a_i$ per input point. Arrows show forward dependencies; standard operations are grouped within their modules. Flames mark optimized parameters, including the pretrained backbone. The real nuScenes scan uses orange to show auxiliary anomaly labels.}
\label{fig:method}
\end{figure}

The normal predictor uses a separate pointwise encoder and pools its features independently within each angular block. For a target block, the hypothesis decoder reads the other 24 block representations in its $5\times5$ neighborhood. Learned hypothesis queries attend to these representations together with the fixed block location and scale. Target measurements are withheld before any cross-block interaction. At each scale, the predicted field parameters serve all target rays in that block.

\subsection{Shared Spatial Hypotheses and Return Distributions}
\label{sec:fields}

For a target block $B$, the context decoder produces $K$ normal hypotheses with weights $\pi_{Bk}$. Each hypothesis is a positive spatial rate field formed by $M$ anisotropic Gaussian components:
\begin{align}
\pi_{Bk}&=\operatorname{softmax}(b_B)_k,\\
\lambda_{Bk}(x)
&=\sum_{m=1}^{M}a_{Bkm}
\exp\!\left[-\frac12(x-c_{Bkm})^\top
\Sigma_{Bkm}^{-1}(x-c_{Bkm})\right].
\label{eq:field}
\end{align}
The network predicts the centers $c_{Bkm}$, positive amplitudes $a_{Bkm}$, and full positive-definite covariances $\Sigma_{Bkm}$. The rate has units of inverse distance. Components within a hypothesis jointly describe one spatial arrangement, and the mixture represents alternative arrangements supported by the context.

A ray queries this representation through $x_i(r)=o_i+rd_i$, with unit direction $d_i$. We model the distance of a recorded return within the interval $[L,U]$. Define the accumulated rate and its normalization by
\begin{equation}
H_{ik}(r)=\int_L^r\lambda_{Bk}(o_i+td_i)\,dt,
\qquad
Z_{ik}=1-\exp[-H_{ik}(U)].
\label{eq:integral}
\end{equation}
The conditional return-distance density is
\begin{equation}
p_{ik}(r\given C_B,d_i,\text{recorded return})
=\frac{\lambda_{Bk}(o_i+rd_i)\exp[-H_{ik}(r)]}{Z_{ik}},
\qquad L\leq r\leq U.
\label{eq:density}
\end{equation}
The exponential term couples the rate at distance $r$ to the accumulated rate at shorter distances. Normalization conditions the prediction on a recorded return in the interval. All target rays in $B$ use the same $\lambda_{Bk}$, so a change to a spatial component affects several distance predictions through their ray geometry.

Restricting a three-dimensional Gaussian to a ray gives a one-dimensional Gaussian response. Consequently, $H_{ik}$ is evaluated analytically through Gaussian cumulative probabilities. This provides differentiable distance densities without discretizing each ray into depth samples. Appendix~\ref{app:integral} gives the derivation.

\subsection{Learning a Shared Normal Explanation}
\label{sec:normal}

Let $\mathcal I_B$ index the reliably labeled normal returns within $[L,U]$ in block $B$ of an original scan. Write their distances as $R_B=\{r_i\}_{i\in\mathcal I_B}$ and directions as $Q_B=\{d_i\}_{i\in\mathcal I_B}$. The normal-learning objective assigns a common hypothesis to these returns:
\begin{equation}
P(R_B\given C_B,Q_B,N)
=\sum_{k=1}^{K}\pi_{Bk}\prod_{i\in\mathcal I_B}p_{ik}(r_i),
\label{eq:joint}
\end{equation}
where $N$ denotes normal supervision. Each candidate field is evaluated against the same normal returns before the hypothesis identity is marginalized. The block loss is
\begin{equation}
\mathcal L_B=-\frac{1}{|\mathcal I_B|}
\log\sum_{k=1}^{K}\pi_{Bk}
\exp\!\left[\sum_{i\in\mathcal I_B}\log p_{ik}(r_i)\right].
\label{eq:normal}
\end{equation}
We average this loss over blocks containing normal targets, then over scales and reference scans. Each block contributes its joint negative log-likelihood normalized by its number of normal returns, and the outer average weights populated blocks equally. A shared hypothesis couples the learning signals from their rays through one spatial field. Section~\ref{sec:experiments} specifies a comparison with independent per-point hypothesis mixing.

\subsection{Hypothesis Compatibility with the Observed Point}
\label{sec:readout}

The point-level decoder compares each actual return with the complete hypotheses predicted for its block. For scale $s$, a shared map encodes the hypothesis log density at the observed distance and its context-derived log prior:
\begin{equation}
v_{ik}^s=\psi\!\left[
\log p_{ik}^s(r_i),\ \log\pi_{Bk}^s
\right]+\delta_s,
\qquad q_i=W_qf_i+\rho(r_i/U).
\label{eq:tokens}
\end{equation}
Here $\delta_s$ is a learned scale embedding, and the query $q_i$ combines observed point features with measured distance. Each density is computed from all Gaussian components of its spatial hypothesis through Equation~\ref{eq:density}. The decoder attends to the resulting hypothesis representations:
\begin{equation}
e_i^s=\operatorname{Attn}_{k}
\big(q_i,\operatorname{key}(v_{ik}^s),\operatorname{value}(v_{ik}^s)\big).
\label{eq:across}
\end{equation}
The 64-dimensional hypothesis representation is split into 32-dimensional keys and values. Four attention heads aggregate evidence across the $K$ hypotheses. The query selects useful evidence for the point-level decision, while the field parameters and their priors are predicted from the surrounding blocks.

We additionally provide the point's marginal normal log density,
\begin{equation}
\ell_i^s=\log\sum_k\pi_{Bk}^s p_{ik}^s(r_i),
\qquad
a_i=g\!\left[f_i,e_i^1,e_i^2,e_i^3,\ell_i^1,\ell_i^2,\ell_i^3\right].
\label{eq:score}
\end{equation}
The absolute density complements the attention-weighted features when all hypotheses assign little support to the return. The output $a_i$ is the point-level anomaly score. The normal objective in Equation~\ref{eq:joint} shapes the shared representation during training; Equation~\ref{eq:score} specifies its point-wise use during inference.

\subsection{Joint Optimization}

Detection supervision and normal prediction train the full model together. Within each effective batch, we average binary cross-entropy separately over anomaly points, inserted normal points, and other reliable normal points. Their respective weights are $1/2$, $1/4$, and $1/4$. Group averaging preserves the contribution of inserted normal objects even when background points dominate the scan. With this classification objective $\mathcal L_{\mathrm{BCE}}$, we optimize
\begin{equation}
\mathcal L=\mathcal L_{\mathrm{BCE}}
+\alpha(t)\big(
\widetilde{\mathcal L}_{\mathrm{AP}}
+0.1\widetilde{\mathcal L}_{\mathrm{AUC}}
+0.1\widetilde{\mathcal L}_{\mathrm{FPR95}}\big)
+0.1\mathcal L_{\mathrm{normal}}.
\label{eq:loss}
\end{equation}
The ranking terms operate on the same $a_i$ across an effective batch of scans. They address the ordering of anomaly points, overall positive-negative separation, and the normal tail at high anomaly recall. Their weight $\alpha(t)$ increases from zero to one between 10\% and 20\% of the update budget. Appendix~\ref{app:loss} defines the surrogates and their sampling. Detection gradients pass through the compatibility decoder and the return densities into the normal predictor, while original normal scans provide the likelihood supervision.

\section{Source Training Observations}
\label{sec:data}

The two learning objectives use paired views of the same source scene. An original scan supplies reliable normal returns for field prediction. An observation containing an inserted object supplies the point labels for discrimination. Auxiliary anomalies and normal objects share the insertion procedure, so the training labels teach the decoder which inserted objects should be retained as normal.

\paragraph{Backgrounds and semantic supervision.}
We construct training and development observations from the official nuScenes training and validation partitions \citep{nuscenes}. They contain 700 and 150 scenes, respectively. A source point receives normal supervision when its annotated meaning agrees with the target normal set. Ambiguous source categories receive ignored labels. Camera observations support donor verification and local label refinement; model inputs are LiDAR coordinates and intensity.

\paragraph{Auxiliary anomalies and normal controls.}
The auxiliary object collection contains 40 verified instances, divided into 27 training and 13 development donors. They include bags, refuse containers, carts, toolboxes, and suitcases. These nuScenes objects supply auxiliary anomaly supervision for evaluation on STU's separately defined target anomaly set \citep{stu}. Normal donors are adult pedestrians, bicycles, motorcycles, and cars. Auxiliary donors receive anomaly supervision after insertion, while inserted normal objects retain normal supervision.

\paragraph{Placement and recorded observations.}
Each placement fixes one original-size donor at a world pose supported by measured road points. Independent placements cover an unrestricted base placement and requested observation ranges of 20--30, 30--40, and 40--50\,m. Vehicle motion then determines the sequence of viewing distances and directions. For each receiving scan, its recorded ray directions intersect the donor's measured open surface. A closer supported intersection replaces the corresponding return. Observations are retained within the views supported by the measured donor surface. Background points in uncertain occlusion regions retain their coordinates with ignored supervision.

\begin{figure}[t]
\centering
% Regenerate with figures/draw.py; training scene-0042, receiving frame 3 (0-based),
% token 955bce79c1aa4f7d80e8060d5c255a49; anomaly_r3 and control placements.
\includegraphics[width=\linewidth]{figures/data.pdf}
\caption{Realized source observations from one nuScenes training scan. Columns show the original, an auxiliary toolbox insertion (11 returns), and a normal motorcycle insertion (22 returns). Top and bottom rows show horizontal and height projections of the same crop, with equal distance scales on both axes. The insertions use independent world poses and the same observation procedure. Crosses mark background returns retained with ignored supervision in uncertain occlusion regions.}
\label{fig:data}
\end{figure}

\paragraph{Paired learning records.}
Every modified observation is linked to its unmodified scan. The normal-field targets always come from this original, including when the inserted object is normal. All versions of a scene remain in its original partition, and donor instance identities are partitioned with their source scenes. Table~\ref{tab:data} summarizes the resulting records. Each original background scan appears once in the reading index.

\begin{table}[t]
\centering
\caption{nuScenes source data. Modified records share their original scene and scan identities; the first two rows describe the underlying background observations.}
\label{tab:data}
\begin{tabular}{lrr}
\toprule
 & Training & Development \\
\midrule
Background scenes & 700 & 150 \\
Original scans & 28,130 & 6,019 \\
Auxiliary anomaly donors & 27 & 13 \\
Anomaly insertion records & 23,795 & 4,699 \\
Normal insertion records & 20,895 & 4,861 \\
Total reading records & 72,820 & 15,579 \\
\bottomrule
\end{tabular}
\end{table}

\section{Experiments}
\label{sec:experiments}

\subsection{Evaluation and Implementation}

\paragraph{Evaluation.}
The target benchmark is STU, with its public 19-sequence anomaly validation partition and 51-sequence hidden anomaly test partition \citep{stu}. The point-level evaluation uses valid labels in the 2.5--50\,m interval and aggregates eligible scans containing at least five anomaly points. We report average precision (AP), area under the receiver operating characteristic curve (AUROC), and the false-positive rate at 95\% anomaly recall (FPR95). All three metrics use the same checkpoint and unnormalized point score. Source development uses the nuScenes partition described in Section~\ref{sec:data}. Normal insertion observations additionally support an analysis of normal-point scores and false alarms.

\paragraph{Training.}
The observation backbone is initialized with official nuScenes LitePT-S weights; all added modules are initialized from scratch. All parameters are optimized jointly using AdamW, with peak learning rates of $2\times10^{-4}$ for the backbone and $2\times10^{-3}$ for the added modules. The training protocol specifies 2,500 updates with eight scans per effective batch, corresponding to 20,000 scan visits. Full source-development evaluation is scheduled at updates 1,250 and 2,500. Source AP selects one checkpoint, with endpoint results reported separately in Table~\ref{tab:source}. STU observations are excluded from optimization and checkpoint selection in this source-trained protocol. The model uses 5\,cm voxels, angular scales of $1^\circ$, $2^\circ$, and $4^\circ$, four normal hypotheses, and eight Gaussian components per hypothesis. Computation uses FP32. Appendix~\ref{app:implementation} supplies the remaining details.

\subsection{Benchmark Comparison}

The target comparison in Table~\ref{tab:stu} evaluates the complete model against LiDAR anomaly methods on the public validation and hidden-test partitions separately. AP measures precision across anomaly recall levels, AUROC summarizes positive-negative ordering, and FPR95 focuses on normal-point rejection at high anomaly recall. Their joint reporting characterizes detection and normal retention using one score. Appendix~\ref{app:resources} specifies the training resources for each compared model.

\begin{table}[t]
\centering
\caption{Point-level anomaly segmentation on STU. Values are percentages. Higher AP and AUROC and lower FPR95 are better. Each metric triplet uses one checkpoint and the same point scores.}
\label{tab:stu}
\setlength{\tabcolsep}{5pt}
\begin{tabular}{lrrrrrr}
\toprule
 & \multicolumn{3}{c}{Validation} & \multicolumn{3}{c}{Hidden test} \\
\cmidrule(lr){2-4}\cmidrule(lr){5-7}
Method & AP $\uparrow$ & AUROC $\uparrow$ & FPR95 $\downarrow$ & AP $\uparrow$ & AUROC $\uparrow$ & FPR95 $\downarrow$ \\
\midrule
NDP \citep{ndp} & \blank & \blank & \blank & \blank & \blank & \blank \\
REL \citep{rel} & \blank & \blank & \blank & \blank & \blank & \blank \\
LIDO \citep{lido} & \blank & \blank & \blank & \blank & \blank & \blank \\
COVAL \citep{coval} & \blank & \blank & \blank & \blank & \blank & \blank \\
\midrule
Ours & \blank & \blank & \blank & \blank & \blank & \blank \\
\bottomrule
\end{tabular}
\end{table}

% Fill the table from verified, protocol-compatible evaluations and analyze
% detection and normal retention together. Do not infer target performance
% from source-development results.

\subsection{Contribution of the Normal Representation}

The component study uses a common source dataset, backbone initialization, optimization budget, and detection objective. A direct point classifier supplies the observation-only reference. A point-wise mixture variant replaces the block objective in Equation~\ref{eq:joint} with $\prod_i\sum_k\pi_k p_{ik}(r_i)$ while retaining the same field architecture and inference decoder. A marginal-density readout retains the observation feature $f_i$ and the three log densities $\ell_i^s$ in the final head. The complete model additionally reads the individual hypotheses through Equations~\ref{eq:tokens}--\ref{eq:across}. These comparisons separate the contribution of a normal reference, shared-hypothesis training, and hypothesis-specific evidence.

\begin{table}[t]
\centering
\caption{Controlled component comparison on STU validation. Each row specifies a separately trained variant under the common source training setup.}
\label{tab:ablation}
\begin{tabular}{lrrr}
\toprule
Variant & AP $\uparrow$ & AUROC $\uparrow$ & FPR95 $\downarrow$ \\
\midrule
Direct point classifier & \blank & \blank & \blank \\
Point-wise mixture objective & \blank & \blank & \blank \\
Marginal-density readout & \blank & \blank & \blank \\
Full model & \blank & \blank & \blank \\
\bottomrule
\end{tabular}
\end{table}

% Add component results and explain which comparisons support the mechanism.

\subsection{Normal Controls and Sparse Observations}

Normal insertion controls test whether the learned decision retains normal objects after road placement and changes in observation. Their score distributions in Figure~\ref{fig:analysis} complement anomaly measurements grouped by range and observed object point count. Range changes sampling density, while point count describes the evidence actually available for each object. Donor-wise and object-type summaries further show how individual source objects contribute to pooled performance. These analyses connect the point-level metrics to the observations on which the model succeeds or fails.

\begin{figure}[t]
\centering
% Use one completed checkpoint for all panels. Retain raw scores and the
% evaluation eligibility rule; report point/scan counts for every stratum.
% Normal-control distributions require their own inference on those records.
\figureblank{1.05in}{Normal-control scores and range/point-count analysis}
\caption{Normal retention and sparse anomaly observations: score distributions for inserted normal objects, range-stratified measurements, and measurements grouped by observed object point count.}
\label{fig:analysis}
\end{figure}

% Add normal-control, range, point-count, and donor-wise findings once measured.

\subsection{Qualitative Analysis}

The qualitative comparison pairs point-level predictions with the normal-return distributions used by the decoder (Figure~\ref{fig:qualitative}). Inputs, annotations, direct-classifier outputs, and full-model scores expose detection errors and retained normal objects in the same scenes. Selected rays relate the measured return to the support of the predicted hypotheses. Together, these views examine how the normal reference contributes to decisions at object boundaries and under sparse observations.

\begin{figure}[t]
\centering
% Pair input, labels, baseline, and full-model predictions on identical scans
% and crops. Use one shared score scale; include successes and errors. Extract
% return distributions from those same forward passes, never illustrative curves.
\figureblank{1.05in}{Point-level predictions and normal-return distributions}
\caption{Qualitative comparison on STU validation: input points, annotations, direct-classifier predictions, full-model scores, and normal-return distributions for selected rays.}
\label{fig:qualitative}
\end{figure}

% Populate with representative successes and errors, then add interpretation.

\FloatBarrier
\section{Conclusion}

We introduced contextual normal return fields for point-level LiDAR anomaly segmentation. Surrounding observations predict shared spatial hypotheses, analytic ray integration converts them into distance distributions, and a learned decoder combines their support with actual-observation features. Block-level normal learning and point-level discrimination train this mechanism jointly. Original scans provide normal targets, while auxiliary anomalies and normal insertion controls teach the final semantic decision. The resulting formulation connects context-based normal prediction directly to point-level anomaly segmentation.
% Add the empirical conclusion supported by benchmark and component results.

\section*{AI Use Statement}
We used OpenAI Codex to assist with literature search and synthesis, refinement of research hypotheses and the model formulation, mathematical derivations, training-data construction and annotation review, experimental design, implementation and debugging, and interpretation of development results. Codex also assisted with manuscript drafting and editing, reference organization, figure design and plotting code, and document formatting. Synthetic point clouds are produced by the nuScenes-based geometric procedure in Section~\ref{sec:data}; AI assistance included designing and implementing that procedure. Checks of the assisted implementation include numerical evaluation of return densities and gradients, metric recomputation, and verification of source labels and generated observations. The authors take responsibility for the final text, claims, code, and data artifacts.

\section*{Reproducibility Statement}
Section~\ref{sec:method} defines the model and learning objectives, and Section~\ref{sec:data} describes the source-data construction and partitioning. Appendices~\ref{app:integral}--\ref{app:implementation} provide the analytic derivation, loss definitions, model dimensions, optimization settings, and sampling procedure. Appendix~\ref{app:resources} records the training resources and observation coverage.

\clearpage
\bibliography{paper}
\bibliographystyle{vendor/iclr2027/iclr2027_conference}

\clearpage
\appendix

\section{Analytic Gaussian Integration Along a Ray}
\label{app:integral}

Consider one component with covariance $\Sigma=AA^\top$ and rate
\begin{equation}
\lambda_m(x)=a\exp\!\left[-\tfrac12(x-c)^\top\Sigma^{-1}(x-c)\right].
\end{equation}
For the ray $x(r)=o+rd$, define $v=A^{-1}d$ and $w=A^{-1}(o-c)$. Completing the square gives
\begin{align}
\mu&=-\frac{v^\top w}{v^\top v}, &
\tau&=(v^\top v)^{-1/2}, &
\log h&=\log a-\tfrac12\|w+\mu v\|^2,\\
\lambda_m(o+rd)&=h\exp\!\left[-\frac{(r-\mu)^2}{2\tau^2}\right].
\end{align}
The integrated component rate is therefore
\begin{equation}
\int_L^b\lambda_m(o+rd)\,dr
=h\tau\sqrt{2\pi}
\left[\Phi\!\left(\frac{b-\mu}{\tau}\right)
-\Phi\!\left(\frac{L-\mu}{\tau}\right)\right],
\end{equation}
where $\Phi$ is the standard Gaussian cumulative distribution function. Summing these terms over $m$ gives $H_{ik}(b)$. The conditional density integrates to one because
\begin{equation}
\int_L^U\lambda_{Bk}(o+rd)e^{-H_{ik}(r)}\,dr
=\left[-e^{-H_{ik}(r)}\right]_L^U
=1-e^{-H_{ik}(U)}.
\end{equation}
All field and density operations are differentiable with respect to the predicted parameters. We compute interval probabilities in the log domain, using stable tail and narrow-interval expressions. Point queries are processed in chunks of 2,048.

The implemented observation model uses the scan reference origin and conditions on a recorded return in $[2.5,50]$\,m. Its learned rate is a representation for normal distance prediction. The conditioning event specifies which recorded measurements enter the likelihood objective. For returns outside $[L,U]$, log-density inputs to the readout are set to zero; these returns remain available as context.

\section{Point-Level Training Objectives}
\label{app:loss}

Let $\mathcal P$ and $\mathcal N$ be anomaly and normal points across one effective batch. Partition $\mathcal N$ into inserted normal controls $\mathcal C$ and other reliable normal points $\mathcal B$. With all three groups present, classification supervision is
\begin{equation}
\mathcal L_{\mathrm{BCE}}=
\frac1{2|\mathcal P|}\sum_{p\in\mathcal P}\log(1+e^{-a_p})
+\frac1{4|\mathcal C|}\sum_{c\in\mathcal C}\log(1+e^{a_c})
+\frac1{4|\mathcal B|}\sum_{b\in\mathcal B}\log(1+e^{a_b}).
\end{equation}
For an absent group, its term is omitted and the remaining group weights are normalized to sum to one. The source batch construction supplies all three groups. Insertion identity defines the supervision groups and is excluded from the model input. Ignored points remain available as context and are excluded from these supervised sets.

\paragraph{Ranking samples.}
We uniformly sample up to 256 anomaly anchors into $\mathcal A\subseteq\mathcal P$. All anomaly scores remain available when computing positive ranks and the soft recall threshold. Up to 4,096 normal points form $\mathcal S_-$. When $|\mathcal N|>4,096$, this set contains the 512 highest-scoring normal points and 3,584 uniformly sampled points from the remainder. The former have weight one and the latter have weight $(|\mathcal N|-512)/3584$. Otherwise all normal points are retained with unit weights. Denote these weights by $w_n$.

\paragraph{Average-precision surrogate.}
With temperature $\tau=1$ and logistic function $\sigma$, the soft positive and negative ranks of anomaly anchor $p$ are
\begin{align}
R_p^+&=1+\sum_{q\in\mathcal P\setminus\{p\}}
\sigma\!\left(\frac{a_q-a_p}{\tau}\right), &
R_p^-&=\sum_{n\in\mathcal S_-}w_n
\sigma\!\left(\frac{a_n-a_p}{\tau}\right),\\
\widetilde{\mathcal L}_{\mathrm{AP}}
&=1-\frac1{|\mathcal A|}\sum_{p\in\mathcal A}
\frac{R_p^+}{R_p^++R_p^-}.
\end{align}

\paragraph{Pairwise separation.}
The AUROC-oriented term uses a smooth positive-negative ordering loss:
\begin{equation}
\widetilde{\mathcal L}_{\mathrm{AUC}}
=\frac1{|\mathcal A|\,|\mathcal N|}
\sum_{p\in\mathcal A}\sum_{n\in\mathcal S_-}w_n
\operatorname{softplus}\!\left(\frac{a_n-a_p}{\tau}\right).
\end{equation}

\paragraph{Normal tail at high recall.}
The soft threshold $\theta$ satisfies
\begin{equation}
\frac1{|\mathcal P|}\sum_{p\in\mathcal P}
\sigma\!\left(\frac{a_p-\theta}{\tau}\right)=0.95.
\end{equation}
We differentiate this threshold implicitly and optimize the weighted normal exceedance:
\begin{equation}
\widetilde{\mathcal L}_{\mathrm{FPR95}}
=\frac1{|\mathcal N|}\sum_{n\in\mathcal S_-}w_n
\sigma\!\left(\frac{a_n-\theta}{\tau}\right).
\end{equation}
For $u_p=(a_p-\theta)/\tau$, the threshold derivative is
$\partial\theta/\partial a_p=\sigma'(u_p)/\sum_q\sigma'(u_q)$.
Ranking terms are zero when either class is absent. The BCE term continues to supervise the present groups. The schedule is
\begin{equation}
\alpha(t)=\operatorname{clip}\!\left(\frac{t/T-0.1}{0.1},0,1\right),
\qquad T=2500.
\end{equation}

\section{Architecture and Optimization Details}
\label{app:implementation}

The model contains 13,208,930 trainable parameters. Table~\ref{tab:architecture} lists its dimensions. The $2^\circ$ and $4^\circ$ grids are nested coarsenings of the $1^\circ$ grid. A target block reads the 24 other blocks in its $5\times5$ neighborhood at the selected scale. Empty neighbors use an attention mask; an entirely empty neighborhood uses a learned context token. The context predictor uses LayerNorm and block-local pooling.

\begin{table}[ht]
\centering
\caption{Implemented model dimensions. The hypothesis map uses the same parameters across hypotheses and angular scales.}
\label{tab:architecture}
\begin{tabular}{>{\raggedright\arraybackslash}p{0.43\linewidth}>{\raggedright\arraybackslash}p{0.48\linewidth}}
\toprule
Component & Configuration \\
\midrule
Observation backbone & LitePT-S; 5\,cm voxel size \\
Actual point state & 64-dimensional scene, detail, and within-voxel terms summed \\
Context point encoder & $[x/U,y/U,z/U,I,d_x,d_y,d_z]$; $7\to64\to64$ \\
Block representation & Mean/max pooling; 128 dimensions \\
Hypothesis decoder & Four queries; two four-head cross-attention layers; feed-forward width 256 \\
Spatial fields & Four hypotheses, each with eight full-covariance Gaussian components \\
Hypothesis map $\psi$ & $2\to16\to64$; 32-dimensional keys and values \\
Compatibility attention & Four heads; eight dimensions per head \\
Final point head & $163\to128\to64\to1$ \\
\bottomrule
\end{tabular}
\end{table}

The context and field decoder share parameters across scales. Covariances use Cholesky factors, and positive parameterizations give positive amplitudes. Hypothesis queries are initialized separately. Field centers are initialized across the valid distance interval in a coordinate basis determined by the target angular block. The actual-point query combines the 64-dimensional point state projected to 32 dimensions with a $1\to16\to32$ distance encoding. Hidden layers in the final head use normalization and GELU.

Within each context-attention layer, keys and values are projected once per independent angular block and then gathered for the original 24-neighbor attention. This reuses the same projections while preserving the attention parameters and target-block exclusion.

AdamW uses $\beta=(0.9,0.999)$, $\epsilon=10^{-8}$, and weight decay $0.005$, with zero decay for biases and normalization parameters. Learning rates warm up for the first 5\% of updates from 0.1 of their peak value, then follow cosine decay to 0.01 of that value. The gradient norm is clipped at one. The fixed source run uses random seed zero and the preconstructed observations directly. Ties in checkpoint AP are broken by lower FPR95 and then higher AUROC.

Each effective batch contains three observations with at least five anomaly points, one with one to four anomaly points, two with observed normal insertions, and two original normal scans. Within each insertion role, measured range bands are visited in proportion to their record counts in that role pool throughout training. Within a range band, ordering interleaves donor instances, receiving scenes, point-count bins, placement versions, and observation segments. Temporally separated frames precede adjacent frames. Original scans are interleaved by scene. Anomaly ranges use the median supervised-point distance; normal controls use the median band of their supervised inserted-point histogram. Records are reused only after a complete traversal of their role pool and never within the same effective batch.

The full scan supplies context to both encoders. During training, the compatibility readout and point head query only supervised points. Original normal references query ray densities only at their reliable normal targets and require only the normal predictor. Scores from all eight scans are collected before computing the joint detection objective. Cached score gradients are then replayed through per-scan forward computations, with the normal-reference loss added to the same parameter update. Normalization state advances once per training observation. This arrangement retains the effective-batch ranking objective while processing the backbone one scan at a time. Inference returns a score for every observed point.

\section{Training Resources and Observation Coverage}
\label{app:resources}

Table~\ref{tab:resources} records training resources alongside the target comparison. The model uses official nuScenes backbone initialization and nuScenes auxiliary objects. The designated source run uses nuScenes for optimization, development evaluation, and checkpoint selection. STU validation is reported as a public development partition, and the hidden partition supplies the separate test evaluation.

\begin{table}[ht]
\centering
\caption{Training and development resources for the checkpoints in the benchmark comparison.}
\label{tab:resources}
\begin{tabular}{>{\raggedright\arraybackslash}p{0.13\linewidth}>{\raggedright\arraybackslash}p{0.29\linewidth}>{\raggedright\arraybackslash}p{0.22\linewidth}>{\raggedright\arraybackslash}p{0.23\linewidth}}
\toprule
Method & Backbone training sources & Auxiliary anomalies & Target-domain use \\
\midrule
NDP & & & \\
REL & & & \\
LIDO & & & \\
COVAL & & & \\
Ours & nuScenes & nuScenes donors & Public validation reporting \\
\bottomrule
\end{tabular}
\end{table}

The source background partitions contain disjoint acquisition logs, and donor instance identities follow the source partition. The 40 auxiliary donors originate from 21 training and 12 development scenes. Their single-observation point counts range from 8 to 601. Normal donors come from adult pedestrians, bicycles, motorcycles, and cars. Original debris and pushable/pullable labels remain ignored; verified donor surfaces receive auxiliary anomaly labels after insertion. Reliable normal supervision includes compatible source labels and 4,752 manually refined building points across six source frames.

Every placement uses the donor at its measured size and a fixed world pose. A support point lies at least one meter inside the convex hull of local measured road points, and collision checks use scene observations. The receiving scan supplies the queried directions. Surface intersections replace recorded slots when they are nearer and supported by donor visibility. All independent placement versions remain with the same original scene. Table~\ref{tab:coverage} gives counts derived from the resulting observations.

\begin{table}[ht]
\centering
\caption{Observed source coverage. Range statistics use the saved returns. The five-point condition follows the point-level evaluation eligibility rule.}
\label{tab:coverage}
\begin{tabular}{lrr}
\toprule
 & Training & Development \\
\midrule
Successful auxiliary placements & 1,828 & 366 \\
Successful normal placements & 1,542 & 359 \\
Scans with at least five anomaly points & 10,486 & 1,796 \\
Scans with one to four anomaly points & 10,180 & 1,985 \\
Auxiliary anomaly points in range & 235,996 & 52,341 \\
At least five anomaly points beyond 30\,m & 494 & 223 \\
At least five inserted normal points beyond 30\,m & 575 & 129 \\
\bottomrule
\end{tabular}
\end{table}

Among synthesized auxiliary anomaly returns within the 2.5--50\,m evaluation interval, refuse containers contribute 44.7\% of training points and 86.8\% of development points. Donor-wise and type-wise summaries therefore accompany pooled point-level evaluation, together with range and point-count strata. The original scans, insertion records, and donor identities define the units for these summaries.

\section{Additional Results}
\label{app:results}

\begin{table}[ht]
\centering
\caption{Source-development evaluation and computational measurements. Time per scan includes both model pathways for the full model.}
\label{tab:source}
\begin{tabular}{lrrrr}
\toprule
Model & AP $\uparrow$ & AUROC $\uparrow$ & FPR95 $\downarrow$ & Time per scan \\
\midrule
Direct point classifier & \blank & \blank & \blank & \blank \\
Full model, source-selected & \blank & \blank & \blank & \blank \\
Full model, update 2,500 & \blank & \blank & \blank & \blank \\
\bottomrule
\end{tabular}
\end{table}

% Add source-development results and specify the hardware and timing procedure.

\end{document}
