# AJAE 数据与五帧监督协议

## 协议地位

本文件与仓库根目录的 `protocol.json` 共同定义 schema 34，是数据角色、五帧窗口、点级标签、合成异常世界和冻结数据池的唯一活动协议。

在正式训练池、合成验证池和模型无关资格检查全部生成并由内容哈希绑定以前，schema 34 的状态为 `qualification_pending`，训练和调参均不得开始。完成以后才把状态改为 `frozen`。

## 研究目标与资源约束

研究目标是在 STU 官方点级异常指标上取得更好的结果，并尽可能超过最强可比基线。五帧完整观测和人工合成异常监督是当前路线；是否提高真实异常指标必须由最终实验检验，数据池规模和资格检查通过均不能替代效果证据。

全部工作以本机 16 GB 显存的 RTX 5080 Laptop 为资源边界，不进行预训练，不额外引入数据集，并控制正式训练耗时。已淘汰的 STU 预训练模型不再作为本课题的模型入口或依赖。后续模型、联合表示、损失和训练策略的选择必须服从本文件的数据与监督定义。

## 数据角色

`train/206` 的 0–448 共 449 帧是唯一允许影响参数更新的数据源。正式训练只消费由这 449 帧生成并冻结的合成序列。其他序列的样本、标签、统计量和模型输出均不得进入梯度计算、优化器状态、归一化统计或任何具有训练效果的更新。

`train/201` 的 0–681 共 682 帧是唯一模型验证、超参数选择和模型选择数据源，任何 201 数据均不得产生参数更新。原始正常 201 直接形成 678 个因果窗口，在线输出帧为 4–681。已知重复的 0–3 帧保留，并在报告中说明；它们不再构成单独的排除区或隐藏确认区。逐位检查确认：帧 0 和 1 各含三份相同的 131,072 槽扫描，帧 3 含两份，帧 2 由前 29,184 槽前缀、一份完整扫描和同一完整扫描组成。协议保留这些文件槽的独立 `source_slot`，同时冻结其多对一校准射线关系，不删除、去重或伪造缺失点。

19 条 `val` 真实异常序列保持封存。只有模型结构、训练配方、超参数、阈值规则和模型选择规则全部固定以后，才允许进行最终真实异常检验。`test` 继续作为隐藏测试集封存。

## 五帧样本与坐标

对当前时刻 $t$，一个基本样本固定为

$$
\mathcal W_t=\{X_{t-4},X_{t-3},X_{t-2},X_{t-1},X_t\}.
$$

五帧共同构成一次完整观测。窗口内全部可见回波进入模型，模型必须按输入行顺序为全部 $M$ 个可见回波输出 $M$ 个异常分数。所有具有有效二分类真值的点共同计算训练损失。`current_mask` 只用于从完整输出中提取 $X_t$ 的在线结果，不能作为训练损失掩码。

窗口坐标统一表达在当前帧 LiDAR 坐标系中。历史帧 $i<t$ 使用

$$
T_{t\leftarrow i}=T_{W\leftarrow t}^{-1}T_{W\leftarrow i}
$$

进行刚体配准。历史坐标回归采用绝对误差 $10^{-6}$ 米、相对误差 $10^{-5}$ 的常规浮点容差，位姿刚体检查采用 $10^{-3}$ 量级容差。当前帧 $i=t$ 不经过矩阵求逆或自乘，直接复制原始 `float32` 坐标，并要求 `np.array_equal` 成立。

## SceneWindow 数据接口

活动接口至少包含以下信息：

``` python
SceneWindow(
    observation_sequence_id,  # 原始序列或冻结合成序列身份
    frame_ids,                # (t-4, t-3, t-2, t-1, t)
    points.coordinates,       # float32[M, 3]，当前帧 LiDAR 坐标
    points.features,          # float32[M, 1]，原始强度；后续表示由 Part 2 定义
    points.source_frame,      # int32[M]
    points.source_slot,       # int32[M]
    current_mask,             # bool[M]
    labels.anomaly_target,    # int8[M]，-1/0/1
)
```

点的稳定身份固定为

``` text
(observation_sequence_id, source_frame, source_slot)
```

原始 201 的序列身份为 `train/201`；冻结合成序列使用 `synthetic/train/000`、`synthetic/validation/000` 等身份。数组行号、体素号和射线映射均不能替代点身份。射线映射只在合成渲染器内部使用，不作为 `SceneWindow` 点身份字段对外暴露。

## 三态点级标签

`anomaly_target` 的取值固定为：

| 数值 | 含义     | 是否进入二分类损失 |
|-----:|----------|--------------------|
|   -1 | 官方忽略 | 否                 |
|    0 | 正常     | 是                 |
|    1 | 异常     | 是                 |

按 STU 官方点级异常评价语义，原始语义 0 为忽略，原始语义 2 为异常，其他非零原始语义为正常。合成异常代理实际产生的可见回波标为异常；未被替换的有效正常回波保持正常；原始语义 0 保持忽略。标签、标签有效性掩码、异常世界参数和对象身份只用于监督、资格检查与追踪，禁止作为模型输入特征。

## 206 正式训练池

206 先分成 16 个连续、互不重叠且完整覆盖 0–448 的片段。前 15 段各 28 帧，最后一段为 420–448 共 29 帧。每个片段独立采样一个不可变异常世界，世界中严格只有一个异常代理物体，其形状、材质、位置和姿态在整段内保持固定；不再随机抽取物体数量。一条合成序列的每一原始帧只渲染一次，然后才从片段内切五帧窗口。窗口不得跨片段。

每条合成序列产生 $449-4\times16=385$ 个窗口。正式训练池固定为 8 条合成序列，共 128 个异常世界和 3080 个训练窗口。种子为

``` text
34100000 + 1000 * synthetic_sequence_index + segment_index
```

其中合成序列索引为 0–7，片段索引为 0–15。协议冻结后禁止替换根种子。

## 尾部优先放置

异常代理从片段第一帧起就存在于固定世界位置，不能在尾部突然生成、移动或改变物理属性。“尾部”指物体的空间位置优先靠近片段末帧的 LiDAR 位置。

程序按支撑点到末帧 LiDAR 的距离，优先选择最近四分之一的合格支撑点，再按固定种子随机排序；没有通过物理放置检查的位置时，才扩大至片段全部合格支撑点。方位、左右位置和绝对距离不固定。候选放置先使用与正式渲染一致的射线、遮挡和回波规则检查末帧可见性，不写出候选观测文件。确定物体以后，整段正式观测仍每帧只渲染一次。

末帧可见候选在 48 次确定性形状和放置尝试内仍无法取得时，允许使用支撑点来源帧可见的物理位置，并在世界生成记录中写入 `support_visible_fallback`；常规情况记录为 `terminal_visible`。回退仍沿用同一根种子和候选随机流，不替换正式种子，不放宽支撑、穿透或遮挡条件。

停车片段和缺少近处合格支撑面的片段保持原始轨迹与几何，不强求“很远到近距离”。资格结果逐段记录首次与末次可见帧、末帧异常回波数、物体中心首末距离和没有异常回波的五帧窗口数量。所有无异常回波窗口仍保留为正常监督样本，原始忽略标签保持忽略。

## 201 固定合成验证视图

201 的每个片段也严格只含一个异常代理物体，整个片段共享其固定物理属性。

201 的合成验证视图采用相同的“先世界、后整段观测、最后切窗口”规则。完整支撑面检查显示，643–679 没有通过既有物理放置规则的合格支撑面；若把 653–681 单独作为短片段，无法生成异常世界。因此 682 帧固定分为 23 段：前 22 段各 28 帧，最后一段为 616–681 共 66 帧。最后一段仍使用 618–642 的合格支撑面在世界坐标中放置同一个固定异常世界，完整覆盖尾部，不使用忽略点或非地面物体伪造支撑。每条合成验证序列产生 590 个窗口。正式验证池固定为 4 条合成序列，共 92 个异常世界和 2360 个窗口。种子为

``` text
34200000 + 1000 * synthetic_sequence_index + segment_index
```

其中合成序列索引为 0–3，片段索引为 0–22。模型选择同时观察固定合成 201 的异常指标和原始正常 201 的误报、分数分布与稳定性，但 201 永远不参与参数更新。

## 生成顺序与冻结存储

每个片段严格执行以下顺序：

``` text
固定根种子与片段边界
        ↓
采样严格包含一个异常代理的完整世界
        ↓
按时间顺序将片段内每帧渲染一次
        ↓
保存逐帧回波与三态标签的冻结身份
        ↓
引用这些已渲染帧切出片段内五帧窗口
```

渲染器只允许改写被异常代理实际替换的文件槽位。未命中异常的原始 `xyzi` 和打包标签必须逐位保持不变。冻结文件因此只保存变化槽位、新 `xyzi`、新标签和异常对象身份；其余内容从经过逐帧内容哈希绑定的官方 STU 原始文件恢复。每个片段保存异常世界参数、不含种子的物理世界内容哈希、生成报告、根种子、边界、原始帧身份、渲染后帧身份、窗口身份、科学内容哈希和文件哈希。不同种子的多样性必须由物理世界内容哈希不同证明，不能由种子字段本身证明。

调试先导只能写入 `runs/`，不得进入正式清单。正式池一旦完成并写入清单，模型只能读取清单中列出的文件及哈希。

## 预测保存与在线评价

每个窗口必须保存全部点的预测记录：

``` text
observation_sequence_id
window_current_frame
source_frame
source_slot
anomaly_score
```

当前在线评价只选择 `source_frame == window_current_frame` 的记录。同一真实帧作为历史帧出现在其他窗口时得到的分数必须原样保存，但不能参与在线主指标，也不能进行均值、最大值或其他跨窗口融合。离线融合属于未来独立研究问题。

逐窗口预测文件格式固定为 `ajae-complete-window-point-prediction`。读写端必须保存全部行并校验内容哈希；不提供“只保存当前帧”的活动写入接口。

## 冻结资格

资格检查与模型完全无关。它必须核对原始帧完整性、片段无遗漏无重叠、每段严格一个固定异常代理、正式种子唯一性、同种子确定性、不同种子世界身份不同、每帧只渲染一次、所有窗口均位于一个片段、每个可见回波在窗口中恰好出现一次、点身份和标签逐行同步、当前帧逐位复制、历史帧配准容差、重叠窗口复用相同渲染帧，以及原始 201 恰好形成 678 个唯一在线输出。

只有训练池清单、验证池清单和资格结果全部写入 `protocol.json` 并通过哈希校验后，才能把状态从 `qualification_pending` 改为 `frozen`。这一步完成前，只能说协议实现已准备或先导已通过，不能声称正式数据池已经冻结。
