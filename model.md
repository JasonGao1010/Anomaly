# 冻结正常感知后的正常实例支持

当前主线为 `AJAE-instance-support`：官方 nuScenes LitePT-S 与原16类语义头完全冻结，后接模块对每个原始点输出异常分数。STU206 是正常训练主体，nuScenes 只补充经过 STU 类别规范核定的正常知识，STU201 决定模型与校准。val19 只作最终重复评价，不参与参数或检查点选择。当前运行目录为 `results/train/instance/`；正常训练和开发诊断已经完成，完整 val19 评价正在运行，三项超过 LIDO 的目标尚未实现。总体重新设计发生在用户获知上一版本评价失败之后，因此本次属于重复评价，不能称为未见过结果的首次独立测试。

## 已确认的问题与本次决定

旧类别条件重建版本的完整评价为 AP 0.4077170639%、AUROC 89.4657569867%、FPR95 51.3258182839%；193,879,969 个有效点的独立复算与官方结果完全一致，见 `results/train/class/verification.json`。固定类别先验版本的正常201类别等权正确率仅由55.3619%变为55.9518%，远距类别等权正确率由47.2239%降为41.7770%。正常拟合和分类改善不足以支持异常排序的改善。

旧联合密度把正常预测体积和特征偏离共同计入分数，其中在所有类别间相同的项会从类别比较中消失，却仍影响异常排名；因此，优化类别竞争不能直接约束这些评分成分。本次取消条件重建与密度体积，直接学习完整观测之间的正常实例距离。该变化不保证未知点一定远离正常实例，最终效果仍由完整异常评价确定。

真实输入核查还发现旧间距监督的不可识别部分：正常201第0帧的314,739个实际记录只有104,913个唯一完整XYZI，每个点恰好重复三次。按原顺序去除重复后，骨干输入的体素坐标和代表点逐值不变，但2,048个查询点的第八邻居间距变为去重视图的中位0.40468倍。第1帧同样三重，第2帧两到三重，第3帧两重。这些帧处于正常校准区块；它们证明间距混入文件重复计数，不能单独解释整个异常评价失败。本次不再预测或评分这一间距，也不改变原始评价点和官方统计规则。

冻结特征恢复没有发现错位：正常201第64帧中，独立官方前向及层输出恢复与当前252维特征、16类输出、查询索引和缓存逐值一致。103,357个实际点对应95,313个5厘米体素，语义输出也恰好有95,313种，没有额外的输出塌缩。

正常监督不足仍然存在：停车区域的84个明确训练点全部来自3个nuScenes场景，STU206没有该细类训练点；道路候选集合中的停车可能性不能充作停车细类真值。旧平方根逆频率加权后，停车明确监督总质量只有0.042634%，建筑约为其299倍。本次使每个来源内的真实候选集合具有相等监督质量，记录缺乏独立锚点的样本；不把增加权重表述成增加了目标域知识。

用户要求重新设计后，`results/train/prior/` 的类别先验小改训练已主动停止。该运行没有最终模型或异常评价，不代表当前主线完成。

## 输入、距离与正常支持

冻结网络为每个原始点提供早层36维、中层144维、深层72维特征，拼为同一个252维向量。固定坐标变换仅使用STU206全部缓存正常点计算中心、标准差及完整相关矩阵，相关矩阵加0.01单位阵。nuScenes特征使用同一目标坐标；这保证同一尺度依据，不宣称消除了传感器域差异。

后接模块学习一个252×252矩阵 $T$，共有63,504个可训练参数。每次更新后把其奇异值投影到 $[0.5,2]$，在浮点误差范围内保持所有原有方向，避免通过整体坍塌或无限拉伸完成分类。记固定白化后的向量为 $x$，则

\[
d_T(x,m)=\|(x-m)T\|_2^2,\qquad A(x)=\min_{m\in\mathcal M} d_T(x,m).
\]

参考集合 $\mathcal M$ 保存16,384个真实正常点：80%来自STU206、20%来自nuScenes，按实际正常候选集合和四个距离段分配，再优先覆盖不同采集帧。它们保留真实点身份与完整252维特征。每一个支持解释必须来自同一个真实点，不能分别从不同参考点拼接早、中、深层解释；不使用自由移动的类别均值或可学习的局部接受半径。

全部可信正常点，包括粗正常候选集合，都可以提供正常支持。类别监督只使用具有唯一细类标签的参考点；粗正常点不能冒充多个确定细类。

## 学习目标与 STU 主体

训练缓存包含4,720,640个可信正常点，来自STU206全部449帧和nuScenes的1,407帧、700个训练场景。STU206的训练损失总质量固定为80%，nuScenes为20%；各来源内按实际候选集合逆频率加权。每轮遍历全部有效缓存点，原始骨干不重复提取特征。

对正常候选类别 $c$，使用该细类真实参考点的最近距离 $E_c$。对真实允许类别集合 $S$，训练目标为

\[
-\log\frac{\sum_{c\in S}\exp(-E_c/\tau)}{\sum_c\exp(-E_c/\tau)}.
\]

温度 $\tau$ 固定为真实训练点到独立同类参考点距离的中位数。来源样本排除同一nuScenes场景的参考；STU206排除相差不足16帧的参考，以减少近重复观测造成的虚假容易。若允许集合中的类别缺少独立细类参考，该查询跳过类别损失并计数，不能把它的候选集合偷偷缩小。它仍是可信正常点，可以保留在正常参考集合中。其他正常类别只参与类别比较，始终不标成异常。

nuScenes的正常标签沿用已经核定的STU19候选集合：含义明确且兼容者保留，无法可靠对应者忽略。特征坐标、主要训练质量、主要参考覆盖及最终正常开发判断均来自STU。传感器束数、强度和姿态差异不会因为标签对应而自动消失；当前没有把32线观测伪造为128线观测。

学习率0.001，AdamW不加权重衰减，余弦下降，批量1,024，最多12轮。逐轮在STU201奇数64帧区块的644,238个正常缓存点上计算细类召回和交叉熵；优先类别等权召回，其次交叉熵。相同参考集合下的单位矩阵是训练前参照，学习没有改善时保留这一参照并如实报告。连续三轮无改善且至少四轮后停止。源域开发点不参与本次选型。

STU201偶数64帧区块只拟合正常分数参考；既有类别及连续距离校准仍依据独立奇数区块的正常尾部偏差选择。距离由真实XYZ计算，间距不再参与评分。校准可以修正正常分数尺度，不作为异常能力的替代证据。

本次实际完成7轮，共1,752.74秒，选中第4轮；第5至7轮未改善，触发原定停止规则。644,238个STU201开发点上的类别等权召回从单位矩阵参照的40.5414%提高到47.7521%，远距类别等权召回从39.3796%提高到41.4760%。这些是后接模块的正常细类诊断，不能当作冻结16类语义头的改善或异常三项指标。选择的距离校准带宽为0.5。

同一完整开发集的最近正常实例来源核查显示：637,041点由STU206实例提供最近支持，7,197点由nuScenes实例提供最近支持，目标来源占98.8829%。这确认实际推理由STU参考主导；该比例不证明辅助数据已经有效迁移。停车区域5,911个开发点的细类召回仍为0，当前源域补充没有补齐这一目标细类知识。详见 `results/train/instance/normal_mechanism.json`。两域原始输入各抽五帧的检查也显示强度分布不同：STU206中位数0.19714，nuScenes原始中位数8，固定除255后约0.03137；当前统一单位及特征尺度，没有声称统一了传感器分布。

## 当前验证与结果边界

23项关键测试已通过，覆盖独立完整向量最近距离、细类与粗标签边界、同场景及相邻帧排除、矩阵奇异值约束、梯度、缓存失效、保存重载、分块一致性和既有分数校准。一个测试修正了旧报错字符串匹配；投影测试按FP32分解重建误差使用1e-5奇异值容差，实际数值仍保持正下界。1,024个真实正常查询的损失及梯度有限，前向与反向约0.257秒，峰值显存约8.23GB。正式持续训练吞吐另由逐轮记录给出。

最终检查点的官方感知模块240个状态张量与原始权重逐值相同，全部感知参数保持冻结。32个真实正常查询的部署距离与独立CPU双精度直接差分计算相比，最大绝对误差0.000426、最大相对误差8.07e-7，见 `inference_check.json`。完整正常诊断初次执行暴露了对照模型未按所选距离带宽建立校准缓冲区的加载错误，已在原诊断入口修复，并对644,238点重新完整执行；最终模型权重与评分公式未改动。

最近邻正常支持已有研究基础：[Out-of-Distribution Detection with Deep Nearest Neighbors](https://proceedings.mlr.press/v162/sun22d/sun22d.pdf)讨论了特征距离、表示学习和正常参考集合；[A Simple Fix to Mahalanobis Distance](https://arxiv.org/abs/2106.09022)说明与判别无关的特征变化可能干扰分布距离。当前的具体研究对象是冻结点云多层特征的统一、受约束实例支持，不能把最近邻或线性度量本身宣称为首次创新。

[LIDO原文表2](https://arxiv.org/html/2604.23604)的val19目标为AP 27.53%、AUROC 95.05%、FPR95 34.86%。LIDO使用SemanticKITTI训练，骨干及训练来源与本项目不同。当前完成标准仍是独立核验的完整val19三项超过这些数值；正常开发提高和实现测试通过均不替代这一标准。

权威代码为 `src/normal.py` 的 `InstanceSupport`、`src/train.py --normal`、`src/model.py` 的冻结感知，以及 `src/evaluate.py`。历史失败指标保留在相应运行目录，历史模型需要其记录的代码版本；既有论文初稿不能用于当前方法与结果主张。
