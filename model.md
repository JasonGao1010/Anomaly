当前主线：由留出观测检验驱动的正常语义感知

本轮方法版本为 `AJAE-normal-evidence`，结构标识为 `multimode48_blind_density`，评分标识为 `joint_density_logtail`。模型接收单帧 LiDAR 点云，同时输出正常类别和连续未知分数。类别判断与未知分数使用同一套联合能量，语义支持和观测相容性直接参加正常类别竞争。

本文给出本轮实现的数学定义与数据范围。新版已完成最小真实数据实现核验，尚未启动正式训练，也没有新版正常分割或异常评价结果。实现位于 `src/model.py` 的 `NormalHypothesis`、`src/normal.py` 的 `SemanticHypotheses` 和 `src/train.py` 的正常数据入口。旧 `results/train/normal` 来自不同的方法与评分公式，不能作为本版本的已训练模型。

研究问题与数据

模型要回答两个相连的问题：哪个正常类别最能解释一个点，以及这个最优解释本身得到多少支持？前者比较正常类别，后者保留所有正常解释都较弱的可能性。训练仅使用可信正常类别监督，不合成未知物体，不构造异常训练目标，也不继承旧合成异常实验或旧正常假设实验的权重。

所有实际回波进入骨干和上下文构建，不依赖目标检测器、道路中央规则或异常候选筛选。输出保持原始点身份，读取时去掉的空槽在输出时恢复。推理不使用标签、相邻帧或现场参数更新。“正常”指已知语义类别：道路中央的汽车、远距离的稀疏行人仍可属于正常类别。

`src.evaluate infer` 将逐槽未知分数保存为文本；加上 `--semantic-output` 时，同时保存逐槽的 `*.semantic.npy`。类别数组使用 `int16`，实际回波的 0–18 对应 `NORMAL_CLASSES` 的顺序，空槽为 −1。类别来自完整联合能量，与开发时的最终类别定义相同。

- nuScenes 数据来自 700 个训练场景、28,130 帧，源域开发数据来自独立的 150 个场景、6,019 帧。场景、日志、点云令牌和文件路径无交集。
- STU 206 的 449 帧用于目标域正常训练，STU 201 的 682 帧用于正常开发与最终正常参考统计。本轮不运行 STU val19。
- nuScenes 原始 32 类依照 `NUSCENES_NORMAL_SETS` 提供可信正常监督。粗标签给出允许的细类别集合，例如植被对应植被或树干；未可靠确定的细类不被伪造为确定标签。
- 跨数据集定义差异必须保留：nuScenes 的人可能对应行人或车旁骑行者，可行驶地面可能对应道路、停车、人行道或其他地面，地形可能包含路缘或矮灌木。摩托原类包含轻型三轮车，警用车辆包含自行车，不能直接套用同名细类。完整允许集合在 `NUSCENES_NORMAL_SETS`；原始规范和采用理由在 `assets/normal.json`。
- 原始自行车标签与唯一实例框、官方有骑手属性共同确定骑自行车者，覆盖 1,586 个训练帧、74 个场景；摩托有骑手仍保留其他车辆或骑摩托车者的集合。恢复既有 4 帧、1,328 个建筑细标注。依据三个训练场景原图和投影新增 328 个明确停车点，逐点保留原始槽位与来源，不向相邻帧自动扩散。这消除了停车类零明确监督，不等于停车条件覆盖充分。
- STU 精细正常类别依照 `STU_NORMAL_SEMANTICS` 转换。原始 0、1、2、52、99 不提供精细正常监督；正常训练或开发记录若含原始异常标签 2，读取即报错。
- 精细语义监督与官方异常评价用途不同。STU 官方异常评价将除原始 0、2 外的点作为正常，不能用训练时的忽略规则删减官方正常评价点。
- 可信正常监督限定在 2.5–50 米，其余实际点保留在输入中。nuScenes 强度除以 255，STU 保持原始强度。

准确的数据表述是“仅使用可信正常类别监督，未构造异常训练目标”。含义不明确的点可以进入上下文，不参与正常损失；训练输入并非每个点都已被确定为 STU 正常细类。

多形态正常语义

骨干只运行一次。点云按 5 厘米体素聚合后输入 LitePT-S，每个原始点另外保留坐标、强度与体素内偏移的细节特征。骨干解码特征与细节特征共同产生 48 维逐点表征。同一体素内的不同点仍可得到不同输出。

19 个正常类别各使用 4 个语义模式。模式由共享类别向量和可学习偏移构成，归一化后的中心范数统一为 48 的平方根。点表征与中心的平方欧氏距离除以 48，得到同单位的距离 \(d\)。每类语义不相容程度为

\[
s_{i,c}=-\log\left[\frac{1}{4}\sum_{m=1}^{4}
\exp\left(-d_{i,c,m}/0.2\right)\right].
\]

多个模式容纳同类对象的不同有效形态。类内模式支持先平均，再取负对数；所有类别的模式数、中心范数和温度相同。没有为某类单独学习可无限扩张的接受半径。语义模式编号与表面候选编号没有人为的一一对应关系。

目标区域排除的观测预测

独立的轻量分支先编码实际回波，再在互不重叠的 0.5 度角度单元内聚合。预测目标单元时，只读取两个间隔上的 16 个周围单元；目标单元全部回波均不进入其预测状态。独立编码与单元聚合发生在跨单元读取之前，完整语义骨干中的目标信息不回流到这条预测分支。

类别查询读取上下文，分别预测 19 类的正常回波分布。每类有 3 个表面候选，输出混合权重、对数距离均值和尺度。均值随目标射线在单元内的角度偏移作二次变化，一次与二次系数均由网络预测。预测参数在一个角度单元内共享，具体射线方向确定该射线上的均值。

记真实回波的对数距离为 \(z=\log r\)，上下文为 \(C\)，候选的均值、尺度、权重为 \(\mu,\sigma,w\)。尺度通过正值变换并加下限 0.001，混合权重在类内归一化。自由度为 3 的 Student-t 密度为

\[
p_c(z\mid C)=\sum_{k=1}^{3}w_{c,k}
\frac{2}{\pi\sqrt{3}\,\sigma_{c,k}}
\left(1+\frac{u_{c,k}^{2}}{3}\right)^{-2},
\qquad u_{c,k}=\frac{z-\mu_{c,k}}{\sigma_{c,k}}.
\]

预测对象是已有回波的对数距离，不是首次回波风险，也不把未记录射线当作空闲空间。该密度相对于对数距离定义，不能直接称为米制距离密度。

本版本移除旧方法用目标单元其他点更新候选后验的步骤。训练与推理均使用目标区域排除后的预测参数。完整语义分支仍读取目标点；两个分支不是统计独立的观测来源。

统一的类别与未知判断

联合能量使用保留尺度项的完整预测密度。尺度下限为 \(\sigma_{\min}=0.001\)，所有类别、分量共享密度上界 \(M=2/(\pi\sqrt{3}\sigma_{\min})\)，因此观测支持 \(p_c/M\) 不大于 1：

\[
g_{i,c}=\log M-\log p_c(z_i\mid C_i),
\qquad E_{i,c}=s_{i,c}+g_{i,c}.
\]

\(g\) 非负，公共常数不改变有上下文点的类别竞争或分数排序。每个混合分量的尺度因子同时保留在预测损失、类别竞争和未知评分中：在预测均值处，把全部尺度扩大十倍会增加 \(\log 10\) 的观测代价。不能在评分时只保留标准化残差而丢掉密度的尺度项，否则高权重宽分量可能过度接受陌生观测。完全没有上下文时，几何贡献置零，由语义支持决定类别和未知程度。该支持是密度相对共同上界的比值，不是异常概率。

正常类别为 \(\arg\min_c E_{i,c}\)，原始未知分数为 \(\min_c E_{i,c}\)。同一个类别必须同时提供语义和观测支持，模型不能把“语义像汽车、几何像道路”拼成正常解释。校准只对最终标量作共同单调变换，不改变类别选择，也不为不同类别另设接受范围。

正常数据上的端到端学习

以 \(Y_i\) 表示确定类别或允许类别集合。联合类别损失为

\[
\mathcal L_{\mathrm{joint},i}
=-\log\sum_{c\in Y_i}\operatorname{softmax}(-E_i)_c.
\]

几何相容性直接参加类别竞争，正常标签由此训练哪些观测证据有助于选对类别。全部可信正常点同时接受纯语义辅助监督，其形式相同，但使用 \(-s\) 代替 \(-E\)，让完整输入中的正常语义知识持续得到训练。

观测损失使用规范化的负对数似然。上下文类别信念先在允许集合内归一化，再对类条件密度求和：

\[
\mathcal L_{\mathrm{predict},i}
=-\log\sum_{c\in Y_i}\pi(c\mid C_i,Y_i)p_c(z_i\mid C_i),
\qquad \sum_{c\in Y_i}\pi(c\mid C_i,Y_i)=1.
\]

确定标签的类别权重为 1。粗标签只约束允许类别的合计支持，不把其中每类都当作确定真值。混合权重来自排除目标区域的上下文，不读取目标点语义后验。预测损失按点求值，不再强迫同一单元的所有点共用单一潜在类别和表面候选。

正常拉近项为允许类别语义支持的负对数平均，即
\(-\log[|Y_i|^{-1}\sum_{c\in Y_i}\exp(-s_{i,c})]\)。
它使用多模式支持，不把同类全部观测硬拉到一个中心。上下文类别损失采用允许类别概率和。总目标包含五项，系数分别为：联合类别 1、全部点纯语义 0.5、正常拉近 0.05、观测预测 0.2、上下文类别 0.2。

每帧最多选择 4,096 个可信正常查询点计算联合类别、拉近和观测目标。查询先覆盖各正常类别，再补充其余有效点，并随训练遍历变化。全部实际观测始终进入模型，纯语义辅助监督覆盖全部可信正常点。没有可用上下文的查询不参与观测似然和上下文类别损失，但仍接受正常分类与拉近监督。训练保留绕重力轴旋转；跨帧类别一致性损失及相邻帧读取已移除。

联合类别、纯语义和上下文类别损失按本帧出现的允许类别集合均衡平均；拉近与观测似然分别对符合各自条件的查询点平均。这些训练权重不改变官方评价的点级分母。

负对数似然是规范的观测分布评分，类别目标关注选对解释。二者共同优化不等于自动获得校准的联合概率，也不保证所有未知都与正常支持分离。方法效果需要同时核验正常分割与真实未知点识别。

初始化、训练与开发

只有 LitePT-S 骨干加载官方 nuScenes 预训练权重，其余模块随机初始化。默认数据预算保持 nuScenes 一次完整遍历，再对 206 训练八次遍历；目标阶段每四帧 206 配一帧 nuScenes 正常回放。实际完成情况由新运行记录，时间预算截断不能记为完成全量训练。

源阶段骨干、新模块峰值学习率分别为 0.0001、0.0008，目标阶段为 0.00003、0.0002。有效批量为两帧，逐帧累积梯度；计算保留 FP32，梯度裁剪阈值为 1，非有限梯度直接报错。恢复训练只能读取本版本自身兼容的检查点。

源域阶段开发先取独立 nuScenes 开发集各场景的中间帧，再按 `source_indices[::3]` 选择其中 50 个场景的 50 帧。目标域阶段开发使用 201 沿时间均匀选择的 68 帧。选模首先最大化固定真实类别集合上的平均交并比 `mean_iou_gt`，数值相同才比较正常损失；不会因模型增加某类预测而改变平均所包含的真实类别集合。并集非空类平均交并比仍单独报告。

最终开发在 201 全部 682 帧上报告正式推理类别的全点混淆矩阵、逐类交并比、真实点数、缺失类别和允许集合准确率，并记录相对同权重纯语义判断纠正及损害的点数。距离按 10、20、30、40 米分层，角度单元回波数按 1、3、7、15 分层。全点语义统计与最多 8,192 个查询点的观测统计分开；后者包括预测负对数似然、90% 预测区间覆盖及米制宽度、中位预测误差、语义模式责任和熵、观测模式先验与后验责任、尺度和均值分离。统计保留可累加的分子、分母，不把逐帧平均冒充逐点平均。

206 中没有骑自行车者和停车的明确细标注。目标阶段的源数据回放总量不变，一半回放位置轮转覆盖这些有可靠补充标签的源帧，其余位置遍历普通源帧。回放优先级只读取 206 训练标签和源训练补充标签，201 不参与制定优先级。STU 201 缺少摩托车、其他车辆、骑自行车者、骑摩托车者和其他地面的真实标签，报告明确保留缺类信息。201 没有目标异常，不能提供异常 AP、AUROC 或 FPR95。

独立对照与机制评价

`src.train --normal --variant` 提供三种训练方式。`joint` 是完整模型；`semantic` 从同一官方骨干与随机种子独立训练语义模型，训练和推理都跳过观测分支；`separate` 保留同样的结构、共享类别参数和观测预测损失，但分类监督只使用语义代价，推理仍使用两种证据之和。最后一种对照专门检验观测证据参与类别竞争的作用，没有把全部参数联系切断。完整模型临时去掉观测评分得到的 `semantic_only` 仅是内部诊断，不能代替独立语义基线。

三种方式使用相同初始化顺序、正常数据、数据访问顺序、旋转和查询抽样、学习率进度与计划更新预算。配置和阶段结果同时保存计划量、实际训练量及选择的更新位置。`--match-run` 核对已完成的同条件运行，`--baseline` 读取独立语义模型的完整 201 结果并比较逐类表现；到期截断不记作完成共同预算。每个扫描及其标签的实际内容参与输入身份。

正式异常比较由 `src.evaluate compare --semantic ... --joint ... --separate ... --manifest ... --output ...` 完成。同一帧仅准备一次，各模型顺序执行，保存原始帧与槽位、官方有效点标记和各模型精确逐点分数、类别、置信度。AP、AUROC、FPR95 使用官方点集与计算。另以独立语义模型类别置信度至少 0.9 的异常点定义固定困难子集，在正常误报率预算 1% 和 5% 下记录召回及逐点救回、损失。并列分数不拆分；选择各方法共同可达的相同误报点数，从而比较实际相同的误报率，同时保留各方法各自的曲线操作点。这些是评价曲线上的操作点，不是用异常标签调出的部署阈值。

最终正常参考统计

参考统计仅来自 201 的可信正常点，使用与最终推理相同的 \(\min_c E_{i,c}\)。每帧从可信正常点中均匀无放回保留最多 2,048 点，不按类别配额抽样，也不删掉无上下文点。全类别共享一维参考分布，不再使用旧的每类语义／几何双通道参考，也不从 206 或 nuScenes 补入缺失类别的参考阈值。

129 个分位点在 90% 至 99.99% 的上尾较密。相同分位数值合并后，在负对数尾部水平上作分段线性变换，并用有限的端点规则处理参考区间外分数。它表示正常参考偏离程度，不是异常概率，也不保证跨域误报率。201 未覆盖的类别或条件仍没有独立的对应验证证据。

校准是最终标量的共同单调变换，不能凭校准本身宣称提高 AP、AUROC 或 FPR95。重复分位点合并、退化参考分布与变换有限性已纳入本轮机制检查。评分公式改变后，参考统计必须重新拟合。

当前状态与历史边界

本版本已完成真实数据上的最小实现核验，正式训练尚未开始。三种训练方式分别使用完全一致的随机初始化和官方 nuScenes 骨干，在包含 34,720 个实际点的 nuScenes 停车补充帧和包含 123,181 个实际点的 206 帧上，各以 4,096 个查询点完成前向、反向和优化器更新，共六次。联合模型和取消联合分类监督的对照均在语义与预测参数上获得有限非零梯度；独立语义模型跳过预测计算。初始化权重逐字节一致。检查参数与临时文件已删除，没有继承这些检查产生的权重。

201 第 0 帧包含 314,739 个实际点、393,216 个原始槽位，其中 283,734 个点具备本协议的正常细类监督。该帧完整执行开发统计，4,096 个查询的预测区间均为有限值；开发类别与正式推理类别逐元素相同。仅用该帧正常点临时拟合参考分布后，检查点重载的全部输出逐元素一致，正式推理入口恢复槽位后的分数和类别也逐元素一致。这验证了同一真实点在开发、推理、保存与重载中的对应关系。

独立数值回归覆盖完整 Student-t 混合密度、尺度扩张代价、目标单元排除与梯度、联合分类梯度、三种对照初始化和作用路径、分布区间与统计加和、同误报率并列分数、官方评价点集及保存身份。预测区间与负对数似然另用 SciPy 独立复算。数据回归覆盖有骑手属性、重叠框保持粗标签、精确点补充标注以及同路径替换数据的拒绝。`tests/test_normal.py`、`tests/test_training.py`、`tests/test_evidence.py` 的 CPU 回归共 113 项通过，16 项 CUDA 条件测试在该 CPU 命令中跳过；上述真实点云检查另行使用 GPU 完成。

本次联合模型在 206 实现检查中的显存峰值为 6,577,112,064 字节；已经预热的 201 单帧正式推理入口耗时约 0.729 秒，峰值为 1,710,347,776 字节。这是具体单帧的实现测量，完整训练预算仍需正式运行记录。正常分割表现、真实未知识别收益和模式是否有效分工尚没有本版本的训练结果。

旧 `results/train/normal` 使用每类单中心、目标单元内后验核验与分量分位数融合。其 201 历史正常点准确率为 84.45495%，按当时评价点集和并集非空类别计算的平均交并比为 40.01745%。这些数字只属于旧方法，旧 `frozen.pt` 不能按本版结构解释，且历史结果不含异常 AP、AUROC 或 FPR95。

本方法使用 nuScenes，并增加 206 正常训练与 201 正常开发。比较官方异常指标时应说明训练数据条件；将收益归因于方法机制，需要同骨干、同正常数据和相近预算的对照。竞争力由正常逐类感知、三项官方异常指标及完整单帧成本共同判断。
