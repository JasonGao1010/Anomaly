# 冻结正常感知之后的类别条件观测核验

当前主线只训练正常感知模型之后的小模块。官方 nuScenes LitePT-S 骨干与16类分类头全部冻结，输入单帧点云，保留原有正常语义输出，新增模块输出逐点异常分数。训练使用 nuScenes 与 STU206 的可靠正常点；STU201 只提供正常开发与分数校准。val19 仅在正常选型结束后重复评价，不用于选择模型或参数。

## 要解决的具体问题

正常分类器会给每个点选择一个熟悉类别，但类别排名不能表示这个解释是否充分。后接模块同时检查深层表示有没有正常类别支持，以及这种表示能否解释早层细节和实际距离、疏密。前者覆盖表示本身偏离正常的情况，后者针对表示看似正常、观测关系却不相容的情况。

冻结网络为每个原始回波提供早层36维、中层144维和最终72维特征。距离是传感器到点的欧氏距离；疏密用同帧实际回波中第八个邻居的距离表示，两者取对数。分类网络仍看完整帧。后接预测器只读取72维深层特征，早层、中层及两个实测几何量作为被预测的观测，不直接输入预测器。深层特征本身已经看过当前点，因此本方法不属于遮挡当前点的上下文预测。

## 一条联合评分

记训练统计标准化后的深层表示为 $x$，其余180维特征与两个几何量为 $y$。固定的正常训练线性预测先得到 $B[1,x]$；其残差经过正常训练协方差的逆 Cholesky 变换，得到

\[
u=(y-B[1,x])W.
\]

线性预测采用系数0.01的岭惩罚，截距不惩罚；残差协方差加0.01单位阵。该坐标变换在所有候选间相同，开发集不重新拟合。它保留全部维度，减少相关通道重复计数。

小网络将72维深层特征依次变为128维、64维表示，加入19个正常类别各自的嵌入，再由同一个解码器预测 $q(u\mid x,c)$ 的条件混合高斯分布。各类别、分量具有自己的182维均值及混合权重；逐维尺度只由深层表示产生，在类别与分量间共享。尺度对数用 $3\tanh(t/3)$ 限制，完整负对数似然保留尺度体积项。每个类别可以提出不同的正常解释，但不能单独放大预测范围吞入其他类别。

深层正常支持由可靠19类标签的类均值和全局共享类内协方差建立，各已出现类别具有相同先验。唯一细类样本提供类别锚点；粗类别正常样本只在其允许且已经有锚点的类别内分配责任。共享协方差避免将不同类别的分布体积差异当成最宽松类别选择的依据。最终原始分数为

\[
s_c=\log\pi_c+\log p(x\mid c)+\log q(u\mid x,c),\qquad
A=-\operatorname{logsumexp}_c s_c.
\]

正常类别后验为 $\operatorname{softmax}(s)_c$，正常类别判断与异常分数使用同一组证据。观测项能够改变不同类别之间的相对支持。标准化与残差白化的雅可比为所有点共有的常数，排名时省略。分数表示联合正常支持不足，不是经过异常监督得到的异常概率。

独立辅助分类头已经删除。对于真实允许类别集合 $S$，记 $a=\operatorname{logsumexp}_c s_c$、$b=\operatorname{logsumexp}_{c\in S}s_c$，训练损失为 $-b/182+\lambda(a-b)$。第一项学习允许类别对真实观测的联合支持；第二项是同一联合类别后验的部分标签交叉熵。该目标结合生成与判别监督，不将其他正常类别改标为异常。官方16类感知输出仍然保持冻结；后接模型另有可解释的19类支持后验。

182维条件分布中只有两个几何量；完整似然没有使四组信息等权。因此，本方法是联合核验多层特征与几何，不能宣称距离和疏密已经主导评分。

## 正常训练、选择与校准

当前重新提取官方冻结特征，不复用历史训练权重。原始帧范围仍为STU206全部449帧与nuScenes 700个训练场景的1,407帧；开发使用STU201全部682帧及nuScenes 150个独立开发场景的150帧。新缓存位于 `results/train/normal/features/`，保留逐点正常类别集合。实际有效长度由缓存元数据记录，预分配尾部不参与计算。

骨干输入为完整帧；监督选择2.5–50米内、允许语义集合非空的真实正常点。训练帧预选最多4,096个目标点或2,048个源点，先保护罕见唯一细类及距离段，再从其余全部可信正常点补足预算；开发帧均匀抽取最多2,048点。允许集合为空的点继续忽略。唯一细类保存其类别编号，多候选点保存 `semantic=-1` 及完整布尔集合，禁止用集合的第一个类别伪造真值。

统计初始化保留按唯一类别和来源抽取的点，并对每一种粗类别集合最多增加12,000个真实正常点。标准化、线性预测和残差坐标使用全部这些正常样本。深层分布先由唯一细类初始化，随后执行最多20轮集合约束的正则化EM；责任只分给允许类别，唯一细类责任保持为1。协方差更新为加0.01单位阵的加权类内协方差，对应的训练目标包含精度矩阵迹惩罚，逐轮检查该目标不下降。粗类别不能凭空建立缺少唯一细类锚点的新类别。

小网络每轮覆盖全部有效缓存点。训练目标总权重仍按目标域80%、源域20%分配，每个域内按实际标注集合的频数平方根倒数加权。语义辅助监督对粗标签最大化整个允许集合的概率总和，不要求集合中所有细类同时正确。条件观测密度使用全部可信正常点。

四种候选为混合分量数1或4、联合后验判别权重0或0.1。训练至多40轮，学习率从0.001按余弦下降，AdamW权重衰减0.0001。批量以实际显存和吞吐预检确定，写入运行配置。以正常开发类别等权召回优先、真实类别联合负对数似然打平；连续六轮没有改善、且至少训练十轮时提前结束。所有候选共用样本与逐轮随机排列。

选择前先要求候选超过相同深层高斯读出的正常类别等权召回，并使边缘正常负对数似然不劣于同坐标下的类别无关模型参照。参照固定为 `results/train/normal/model/` 的正常开发结果307.441409；初始化的十个统计缓冲区必须逐值相同，才能比较这个密度数值。记录纠正的深层错误、破坏的深层正确判断及35米以上正常子群结果。val19不参与这些决定。

STU201按连续64帧分块。奇数块用于正常模型选择，偶数块用于拟合分数校准；两者没有点或帧交集。选择时仅对至少128个开发点的类别计算类平均似然，201未出现的类别才由nuScenes正常开发补充。少量或缺失类别继续报告，不冒充充分验证。

分数校准只作用于最终联合分数，按官方冻结16类预测分组。每组至少2,048个正常参考点，否则回退不带距离条件的全局分布。除仅类别参考外，比较连续对数距离条件：2.5–50米之间固定16个对数距离锚点，高斯核宽度为0.25、0.5或1。每个锚点估计11个加权分位点，查询时在相邻锚点间插值；有效样本不足2,048或局部分位点重复时回退该组原参考。原类别分位点本身重复时保留原有合并原子的类别校准。实际点间距不作为校准条件。

分位点转换为单调尾部尺度，分数超出参考范围时继续沿正斜率延伸。候选选择及是否启用，统一由201奇数块上至少2,048点的预测类在1%参考阈值下的平均绝对误报偏差决定；源域点和距离分层报告不参与这一裁决。正常校准改善不等于异常排序改善。

## 实现与当前证据

权威实现是 `src/normal.py` 的 `CrossEvidence` 与 `ScoreCalibration`，`src/model.py` 的 `FrozenPerception`、`FrozenSupport`，以及 `src/train.py --normal`。当前类别条件版本标识为 `AJAE-class-evidence`，正在进行正式训练前的公式与真实正常点检查。它复用已完成的4,720,640个正常训练点、1,620,350个正常开发点的冻结特征，不重训骨干。初始化、选型与校准分别记录在 `initialization.json`、`development.json`、`calibration.json`；逐轮记录为 `training.jsonl`。

`results/train/normal/model/` 是补齐正常样本后的类别无关参照，模型训练与选型用时245.28秒，选中四分量、无独立语义辅助损失、第19轮；该目录不能代表新增类别条件机制。公式审查发现其 $p(u\mid x)$ 在比较候选类别时严格抵消，因此它不能实现观测更新类别后验。真实正常探针显示，50,000个201开发点中，深层读出错误的29.10%可被浅层或中层的独立读出正确判断；该比例是诊断性可纠正范围，不是已实现收益。当前修改把类别条件引入观测分布，以直接检验这些补充信息是否被利用。

同一参照的正常校准还暴露了稀有类别回退问题：参考点不足的预测组借用了全局距离条件分布，201中部分稀有组的误报率升到8%–13%。新拟合已将这些组恢复为不带距离条件的全局单调校准。决定来自正常数据，未使用异常评价选型。

该类别无关参照已完成1,960帧、193,879,969有效点的重复评价：AP为0.4419350745%、AUROC为89.3525142438%、FPR95为52.7413283911%，仍未达到LIDO。独立逐点复算的AUROC、FPR95和阈值完全一致，AP仅有约1.1×10⁻¹⁶的浮点求和差异，见 `results/train/normal/model/verification.json`。类别条件结构与正常选型规则在该评价输出前已由正常数据确定。

类别条件实现的23项关键测试通过，包括独立SciPy混合似然、观测改变类别顺序、正确梯度、粗标签EM及稀有组校准回退。2,048个真实正常训练点（含907个粗标签点）的损失与全部参数梯度有限。实际GPU预检同时常驻完整训练与开发缓存：批量1,024、4,096、8,192分别约16.2万、21.1万、18.7万点/秒，峰值约7.10、8.05、9.30GB，故正式采用4,096。该短预检用于资源选择，不等同于持续训练吞吐或异常检测证据。

此前 `results/train/cross/` 使用过度筛选的唯一细类缓存，已完成但未达标。其四种候选最优正常开发类平均负对数似然为325.85691、327.62220、323.59172、326.14052，选中四分量、无语义辅助监督的第29轮。全部正常训练与选择耗时220.96秒，训练期显存峰值约6.36 GiB。最终异常推理跳过辅助分类头计算，原官方语义头照常输出。

选中模型的距离对数平均绝对误差为0.15920，间距对数误差为0.22390。校准参考包含668,912个201偶数块正常点；启用判断仅使用644,238个201奇数块正常点。充分观测预测类在1%参考阈值下的平均绝对误报偏差从0.01805降至0.00765，因此启用校准。原始帧重新提取的4,096个正常点特征与缓存、对应最终分数均逐元素一致；240个官方感知状态张量保持相同。这些正常开发结果不表示异常三项指标已经达标。

已完成独立SciPy混合密度、共享协方差与缺类先验、输入隔离、逆变换、校准尾部外推和保存重载的公式检查。真实正常缓存与原扫描的点编号、单义标签及几何量对应正确；每轮随机排列覆盖全部有效训练点。原正常网络冻结保证同一预处理下的原语义输出不受后接训练改变，尚不代表已验证STU正常语义达到最佳水平。

此前 `results/train/support/` 是已完成但未达标的直接正常特征混合模型：val19的AP为1.12048%、AUROC为90.59829%、FPR95为57.10631%，完整逐点复算与官方结果一致。该结果保留为失败证据，不作为当前模型效果。[LIDO原文表2](https://arxiv.org/html/2604.23604)对应公开数值为27.53%、95.05%、34.86%；双方骨干和训练来源不同。

上述唯一细类缓存版本已完成1,960帧、193,879,969个有效点的val19重复评价：AP为0.3401870897%、AUROC为86.4117726509%、FPR95为71.4511023055%，推理与官方汇总耗时686.56秒。三项均未超过LIDO，也均差于此前直接特征分布基线。正常似然和正常误报校准的改善没有转化为本次异常排序改善，该版本不能作为达标最终模型。权威结果见 `results/train/cross/val19/metrics.json`，逐点记录保持保存。既有论文初稿描述此前路线，尚未改写为本轮方法，不能用于当前结果主张。

连续距离校准在相同旧模型上的正常检验位于 `results/train/range/`。正常选型选择核宽度0.5，类别平均绝对误报偏差从0.00764581降至0.00569482；35–50米正常误报从5.7262%降至3.4958%，2.5–10米从0.9173%升至1.4588%。260个非校准状态张量与此前模型逐元素相同，512个真实正常点通过加载和有限分数检查。发现训练样本遗漏后，未对这个旧缓存版本再运行val19。

样本遗漏已通过真实数据确认：在从700个源训练场景中分散选取的32个场景里，2.5–50米实际回波共764,736点，其中127,197点有唯一正常细类、461,031点有多个且全部正常的候选、176,508点尚未核定。原规则排除了抽查已知正常点的78.38%。另16个源开发场景中，该比例为80.74%。这是所抽查帧的实际比例，不代表全数据总体估计；它证实正常覆盖不足，尚不能确定其对异常指标的贡献。

正常特征诊断显示，原16类头对201卡车的兼容识别率为4.91%，同一72维冻结特征的固定线性读出可达27.12%。在相同338,581个正常训练点、类别平衡及岭系数0.01下，201类别等权读出准确率从深72维的52.19%提升至全252维的55.89%；再加入两项实测几何为55.87%。这说明浅中层补充了一部分目标语义，不能说明所有类别或未知排序改善。固定增加到每类32个最近原型后，另一相同白化坐标下的正常类别等权准确率从58.16%下降至54.32%，因此未采用该容量扩展。

## 与已有机制的关系

[Reverse Distillation](https://arxiv.org/pdf/2201.10703)已采用固定教师、高层瓶颈和多层特征恢复来定位异常；[CFLOW-AD](https://arxiv.org/pdf/2107.12571)已在预训练特征后用条件概率模型学习正常分布。当前实现的具体安排是联合建模深层支持、跨层观测与实测几何，并保持原感知网络固定。概率链式分解本身不是新的理论贡献。进一步的方法主张需要证明两类证据的互补作用，而不能将高斯模型、混合预测和校准分别列为创新。

LIDO的正式推理同时使用类别原型余弦差异与分类熵的乘积、以及另一分支的范数分数，并将二者取平均；前一项还除以本帧最大值。作者固定版本的[正式推理代码](https://github.com/SiMoM0/LIDO/blob/bfc2113d1bb63b2c7e9f9ce54d288116aee20ef4/modules/user.py)与论文公式11–14相符。此前将其概括为仅使用范数不完整，不能据此设计或宣称复现LIDO。
