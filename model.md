当前主线：仅用真实正常数据学习正常假设，并逐点核验

本轮研究对象是单帧 LiDAR 点级异常分割。模型接收完整的实际回波，输出原始点顺序下的连续异常分数。训练不构造异常物体，不粘贴点云，不使用异常标签，也不继承此前合成异常实验的权重。官方 nuScenes LitePT-S 权重只初始化骨干，其余可学习部分从随机初始化开始。

当前实现位于 `src/model.py` 的 `NormalHypothesis`、`src/normal.py` 的 `SemanticHypotheses` 和 `src/train.py` 的正常数据训练入口。当前实验目录是 `results/train/normal`。本文件所述方法与旧的“正常场加合成异常二分类头”不同；旧论文草稿及既有合成异常实验不能作为本方法的结果或贡献证据。

模型要回答的具体问题是：能否找到同一种正常类别，使一个点的局部表征和它所在区域的实际回波同时得到合理解释？只像某种正常类别还不够，解释还要经受实际观测的检验。模型为所有实际点计算分数，不先用目标检测器、道路中央规则或异常热图筛选候选点。

输入与正常监督

- nuScenes 原始训练背景包含 700 个场景、28,130 帧；原始开发背景包含 150 个场景、6,019 帧。两者的场景、日志、点云令牌和文件路径没有交集。
- STU 206 的 449 帧用于目标域正常训练。STU 201 的 682 帧用于正常数据开发与最终正常参考统计。本轮不运行 STU val19。
- nuScenes 原始 32 类只通过 `NUSCENES_NORMAL_SETS` 中明确列出的正常类别提供监督。其余点保留在输入中，监督标签忽略。例如，建筑等混合在 `static.manmade` 中的点没有被擅自细分为建筑、杆件或围栏。
- 对不能细分的正常大类，监督给出可能类别的集合。例如，植被对应植被或树干，自行车对应自行车或骑自行车者。模型学习这些类别的概率和，不把粗标签伪装成确定的细标签。
- STU 的精细正常类别按 `STU_NORMAL_SEMANTICS` 转换；原始 0、1、2、52、99 不提供精细正常监督。读取正常训练或开发序列时若发现原始异常标签 2，会直接报错。
- 精细监督和异常评价是两种标签用途。STU 官方异常评价将除原始 0、2 外的点作为正常；评价代码仍执行该规则，不能用训练时的精细语义忽略规则改变官方评价点集。
- 所有实际回波进入模型。监督只使用 2.5–50 米范围内的可靠正常点。nuScenes 强度除以 255；STU 使用其原始强度。空槽不进入骨干，输出时恢复原始槽位。

观测表征

骨干只执行一次。点云按 5 厘米体素聚合输入 LitePT-S；每个原始点另保留坐标、强度和体素内偏移的细节表征。因此，同一体素中的点仍可获得不同输出。骨干解码特征与点细节共同产生 48 维表征。19 个正常类别各有一个可学习中心，中心范数固定为 48 的平方根。点到中心的距离是平方欧氏距离除以 48，语义分类使用该距离的负值。

平方距离同时保留方向和幅值的信息。所有中心使用相同的距离单位，最终也使用同一个正常距离参考。训练不足的类别不能通过单独扩大自己的正常半径，成为几乎任何点都能通过的解释。

正常解释的产生

另一条轻量分支直接编码实际回波，先在互不重叠的 0.5 度角度单元内独立聚合。预测某个单元时，只读取周围两个间隔上的 16 个单元，不读取目标单元自己的点特征。独立点编码和单元聚合发生在跨单元读取之前，因此目标区域的回波不能先经过全局骨干混合，再绕回自己的预测中。

这 19 个正常中心同时作为类别查询读取上下文。语义分类与正常几何损失共同更新同一组类别表征，两个分支不能各自学习一套互不关联的类别中心。两轮注意力更新让不同类别学习关注不同的正常观测线索。每个类别输出三个局部表面候选及其先验概率。每个候选给出随单元内角度二次变化的对数距离均值和 Student-t 分布尺度。角度偏移按单元大小归一化，所有一次和二次系数均由网络预测，不设置硬斜率上限。候选在同一单元内共享，不为每个点任意生成一套独立参数。

这里预测的是已有回波的条件距离分布。实现没有把未记录的射线当作空闲空间，也没有假定数据必然满足严格的首次回波模型。Student-t 分布的自由度为 3；尺度下限为 0.001，作用是避免正常密度训练退化为数值奇异的零宽度分布。

训练时，将同一单元内具有相同允许类别集合的正常点放在一起。先累加这些点在同一个类别、同一个表面候选下的对数似然，再对类别与候选求和。一个候选必须共同解释这些回波，不能让每个点各选一个最有利的表面来降低训练损失。

实际观测如何核验正常解释

推理时，完整点表征产生各正常类别的绝对支持度，即类别距离除以 0.2 后取负指数。这些支持度决定每个观测对各类别表面解释提供多少证据，类别之间不强制归一化。一个点若远离所有正常类别，它对所有正常表面的影响都相应减小，避免被迫分到某个正常类别后干扰其他点。对于一个待评分点，模型用同一单元内其他点的观测更新各表面候选的概率，再计算该点在更新后解释下的预测似然。被评分点自己的几何似然不参与它自己的候选更新。

这一步把语义判断和几何核验联系起来：语义支持度参与组织同类观测证据，几何解释再检验实际回波是否符合该正常类别。上下文生成的初始表面严格不读取目标单元；核验阶段会使用已观测到的目标单元信息。排除自身几何似然不意味着整个语义骨干也看不到该点。

最终，每个类别都有两项偏离程度：点表征离该正常类别有多远，以及其回波在共同核验的表面下有多不寻常。模型取两项偏离的较大者，再在正常类别之间取最小值。因而，必须存在同一个正常类别同时解释两类证据。

所有类别的语义距离使用同一个单调参考变换。由此，加入几何核验不会把已经具有较高语义异常分数的点重新降为低分；它能补充的是“语义看起来像正常，但实际结构解释不成立”的点。这个性质只说明额外证据可能增加区分能力，并不保证异常评价指标一定改善，因为正常点也可能预测不准。

真实跨帧监督

跨帧关系只在训练中使用。对相邻正常帧，利用原始位姿把点变换到同一坐标系。仅接受静态类别、距离小于 10 厘米、互为最近邻且正常类别集合相容的匹配，每帧最多使用 512 对。

这些匹配约束的是上下文产生的类别信念，而不是要求不同视角下的距离、斜率或整段几何状态保持不变。车辆和行人的运动不通过静态位姿强行匹配。模型没有补造缺失的背面。推理只需要当前单帧。

正常数据训练

本轮预算为 nuScenes 一次完整遍历，再在 206 上训练八次遍历。目标阶段每四帧 206 配一帧 nuScenes 正常回放，避免正常知识被单一场景迅速覆盖。实际完成帧数和可能的时间预算截断以 `stages.json` 为准。

每帧的语义监督覆盖全部可靠正常点；几何损失最多使用 4,096 个查询点，先给各正常类别分配查询，再补充其余有效点。查询随训练遍历变化。骨干始终读取完整实际点云，减少的是独立损失查询，不是场景上下文。正常训练采用绕重力轴的旋转，并同步修正用于跨帧对应的坐标关系。

总损失包含正常语义分类、正常中心距离约束、共享表面联合似然、上下文类别监督以及真实对应点的类别信念一致性。系数分别为 1、0.1、0.2、0.2、0.1。没有“所有点都是正常”的异常二分类损失，也没有为未知类别生成训练标签。

源阶段骨干与新模块峰值学习率分别为 0.0001 和 0.0008；目标阶段分别为 0.00003 和 0.0002。有效批量为两帧，逐帧累积梯度。计算保留 FP32，梯度裁剪阈值为 1；遇到非有限梯度直接报错，不能静默跳过更新。训练从官方骨干初始化；本轮自身检查点的恢复保留优化器与随机状态。

开发、参考统计和冻结

源阶段使用来自独立场景的正常样本检查学习情况。目标阶段使用 201 中沿时间均匀选择的 68 帧开发，按预先固定的正常损失选取模型。选定后在全部 682 帧上计算正常语义与预测指标。201 没有目标异常，不能由此得到有意义的异常 AP、AUROC 或 FPR95。

语义距离参考来自 201 可靠正常点到最近正常中心的距离，所有类别共用。几何参考优先使用 201 中相应类别的正常观测。201 缺失的类别依次使用明确注明的 206 正常参考和 nuScenes 正常参考；这些统计不得被描述为该类别已经经过独立目标域验证。最终 `calibration.json` 逐类记录来源、样本数量和参考分位数。

每项参考保存 129 个分位点，较密地覆盖 90% 至 99.99% 的上尾。分数在负对数尾部概率上插值，超过参考区间时保持线性延伸，避免把高分全部截为 1。该数值是正常参考偏离程度，不是经过异常样本校准的异常概率。参考统计必须与实际评分公式一致；更改评分公式后，通过训练入口的 `--calibration-only` 重新拟合参考，不更新已选模型权重。

参考统计请求部分输出时，几何核验仍使用该帧完整角度单元内的观测，不能因为抽样输出而改变候选后验。推理不需要标签、参考帧或现场参数优化。

当前实验的完成标志是：已选模型、正常开发结果、参考统计以及可重新加载的 `frozen.pt` 均存在并经过实际推理检查。本轮在运行 STU val19 之前结束。只有后续真正执行官方点级评价，才能判断是否达到不使用合成异常的 SOTA。现阶段不能把正常语义准确率、正常预测似然或理论上的补充信息直接写成异常分割提升。

作为性能参照，LIDO 原文第 5.1 节说明其 STU 模型只用 SemanticKITTI 训练。本方案使用 nuScenes，并增加 STU 206 正常训练与 201 正常开发，两者的训练数据条件不同。后续官方指标可以比较系统表现；若要把提升归因于本方法本身，还需要在相同数据条件下比较方法。原始来源：https://arxiv.org/html/2604.23604v1 。

本轮已经执行的正常开发结果

nuScenes 阶段完成 28,130 帧的一次遍历，选中第 14,065 次更新；206 阶段完成 3,592 次目标帧访问及 896 次 nuScenes 正常回放，共 2,244 次更新，按既定正常开发损失选中第 1,800 次更新。

所选模型已经完成 201 全部 682 帧的正常开发评价：可靠正常点的语义准确率为 84.45495%，按当前实现对并集非空类别计算的平均交并比为 40.01745%，正常开发目标值为 1.291180。平均交并比的类别处理及评价点范围以 `normal201.json` 与 `normal_development` 为准，不能直接与其他论文的语义指标比较。该结果不包含异常 AP、AUROC 或 FPR95，也不能证明几何核验已改善异常识别。

当前 `results/train/normal/frozen.pt` 已按 `absolute_support_logtail` 评分重新拟合正常参考并完成核验。核验确认其网络参数与所选第 1,800 次更新完全一致，参考分位数有限且单调；201 的第 0、341、681 帧通过正式单帧推理入口得到与原始槽位一一对应的有限分数，空槽保持为零。19 类几何参考中，14 类来自 201，摩托车、其他车辆、骑摩托车者、其他地面四类来自 206，骑自行车者来自 nuScenes 的粗类别参考。这三种参考来源必须区别表述。STU val19 未运行；当前冻结表示本轮训练与正常开发完成，不表示异常性能或实际纠错价值已经成立。
