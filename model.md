# 正常感知中的正常实例支持

当前研究目标是让点云感知在分清熟悉类别的同时，判断观测是否缺乏正常类别支持。当前实现名为 `AJAE-instance-support`，采用冻结感知特征、受约束距离学习及真实正常参考点。它是待验证的方法；三项超过LIDO的目标尚未实现。

## 当前数据与执行状态

按用户最新决定，后接模块使用STU206真实正常数据及`/home/jasongao/Study/AJAE-v3/samples`中的206合成数据训练，使用STU201真实正常数据及同一目录中的201合成数据开发。合成异常用于辅助监督和模型选择，因此当前流程不再属于仅正常数据学习。论文贡献仍定位于正常感知之后的模型模块。官方nuScenes LitePT-S预训练骨干及16类语义头保持冻结；nuScenes样本不进入后接模块训练、参考集合或开发，val19只用于最终重复评价。

仅STU正常数据训练于2026年9月26日07:02启动，已完成4轮并选择第1轮，总耗时441.12秒，输出位于`results/train/stu/`。在201奇数区块644,238个开发点上，出现真值类别的平均交并比为27.421580819%，点准确率为63.87546%。这些结果属于正常语义诊断，尚未证明真实异常识别能力。

07:59的旧抽样特征提取按用户要求取消，停止时尚未训练，相关元数据已删除。当前运行于08:16启动，输出位于`results/train/auxiliary/`，正在提取合成观测的冻结特征，计划训练最多4轮。初始模块为`results/train/stu/frozen.pt`，只继承已学距离变换$T$及固定温度，学习率为0.0002；重新建立的中心、白化矩阵、参考特征、参考类别、来源和帧编号均已在CPU上与原模型逐值核对一致。新的辅助训练效果尚未知，未开启新的val19评价。

当前训练选择1,920个206合成帧，异常点数1–4、5–20、21–100和大于100四档各480帧，覆盖237个物体场景。201合成开发选择80帧，四档各20帧，覆盖57个物体场景。选帧只按异常点数分档，以206或201为固定随机种子，不放回抽取，不另按距离或形状平衡。训练每帧保留全部有效异常点和最多512个有明确正常细类标签的点；开发缓存保留全部有效检测点，包括没有细类标签的正常点。官方式三项指标使用其中异常点数不少于5的60帧，全部80个含异常帧另作少点诊断。

`results/train/instance/`保存此前含nuScenes辅助样本的历史训练。该模型的val19评价于06:43按用户要求停止，最后报告1,350/1,960帧；没有完整指标，未完成的分数数组已删除，停止原因见`val19/status.json`。已有val19历史结果仍须披露，后续评价报告为重复评价。

## 任务依据与需要证明的能力

[STU原文](https://arxiv.org/html/2505.02148v1)第3.4、4、4.1节研究训练中没有得到目标异常直接监督时的真实未知障碍识别，并同时核查正常分割。官方正常分割比较使用全景质量等指标，本项目的点级正常语义诊断使用逐类交并比及平均交并比，两种数值不能直接横比。普通正常分割表现好仍可能高置信误认未知，因此正常语义能力和未知识别能力需要分别获得证据。

本项目需要说明：同一正常类别知识既能正确解释困难正常点，也能对真正缺乏支持的观测提高异常分数。正常语义训练有效、正常误报受控、未见异常得到识别是三个不同判断。当前合成异常用于训练损失和开发选型，正常分数校准仍使用201真实正常数据；这三种作用须分别披露。

## 输入与模型

冻结LitePT-S提供早层36维、中层144维、深层72维特征，拼成252维向量并恢复到原始点索引。同一体素可能共享特征，后接模块不能恢复上游已经抹去的点间差异。官方感知的16类输出仍然保留；后接模块的19类正常解释另行评价。

固定中心与白化变换只从STU206正常缓存点估计，完整相关矩阵加0.01单位阵。后接模块学习252×252矩阵，共63,504个参数。每步更新后将奇异值限制在[0.5,2]，保持全部特征方向。记白化特征为$x$，变换为$T$，正常参考集合为$\mathcal M$：

\[
 d_T(x,m)=\|(x-m)T\|_2^2,\qquad A(x)=\min_{m\in\mathcal M}d_T(x,m).
\]

当前正常参考集合保存16,384个STU206真实正常点，按类别、四个距离段和不同帧分配。合成点不进入参考集合。一个正常参考必须同时支持完整252维特征，不分别拼接不同点的浅、中、深层解释。没有可自由移动的类别均值或可学习接受半径。

按类别求最近参考距离$E_c$，最小距离类别给出正常解释，到全部可信正常参考的最近距离给出原始异常分数。STU201的独立正常区块用于类别及连续距离的分数校准；该分数并非异常概率。当前没有正常场重建、距离预测或疏密预测损失。

## 正常与辅助合成训练

真实正常数据只读取历史缓存中的STU前缀：206全部449帧的1,839,104个监督点，201的1,313,150个开发点。代码验证场景、帧编号、连续偏移和来源；源域后缀不加载到训练或开发。每轮遍历全部STU监督缓存点，每个真实正常类别获得相等损失质量。缓存点不是全部原始回波，报告需要保留这一范围；合成观测另从上述已选206、201帧读取。

训练用正常类别比较学习距离：正确正常类别的参考应比其他正常类别更接近。其他类别始终是正常类别，不充作异常。温度由独立同类正常参考距离确定，206中相差不足16帧的参考在训练和206诊断中排除。缺少独立同类参考的查询不伪造监督，记录跳过数量。

已完成的正常数据训练使用学习率0.001、AdamW无权重衰减、余弦下降、批量1,024，并按201奇数64帧区块的正常平均交并比及交叉熵选型。当前辅助训练从该轮已选模块继续，学习率0.0002，使用相同优化算法、批量及距离约束，优化器状态重新初始化，最多4轮。每个原始206正常批量仍计算类别交叉熵；另抽取128个合成视角，并在每个视角内各取一个正常点和异常点组成比较对，使选中视角具有相等抽样权重。

记除以正常数据固定温度后的正常、异常距离为$a_n,a_a$，辅助排序损失为$\operatorname{softplus}(1+a_n-a_a)$，要求正常点得到更近的正常参考，异常点远离全部正常解释。同时对合成场景中保留的正常点计算正常类别交叉熵。每批总损失为原始206正常类别交叉熵，加上$0.5\times(\text{辅助排序损失}+\text{合成场景正常类别交叉熵})$。所有项共同更新同一个距离变换，异常点不获得正常类别标签；缺少独立本类参考的正常点只跳过其类别交叉熵并记录数量，仍保留排序监督。

当前选型使用201合成开发的官方式三项指标，按AP、AUROC、负FPR95依次比较；候选模型还必须保持201真实正常开发平均交并比不低于初始已学模块的27.421580819%。该规则首先约束正常语义退化，再以合成异常表现选择模型，并不保证三个指标在每轮同时改善。val19不参与选择模型、参数或停止时刻。

201偶数区块拟合正常分数参考，奇数区块检查正常误报。完整201校准入口已经实现，将覆盖全部明确正常且在评价距离内的原始点，保持相同区块划分和原模型权重，并与既有校准在同一完整正常留出集上比较。该入口也累计19类混淆矩阵并保存逐点正常类别预测；单类别参考集合下，最近类别距离与最近正常参考距离相同，复用同一次距离计算。该入口尚未在完整真实数据上执行，不能称为已验证。

206缓存未覆盖所有正常细类。例如停车细类不能从不存在的训练标签中凭空学得。201中的这类点仍计入召回和交并比，零召回不能被删除；该类交叉熵报告为不可计算，并统计缺乏细类参考的点数。

当前仅STU参考集合的未经学习距离，在201开发缓存上的出现真值类别平均交并比为20.1595%，正常数据训练后的已选模型为27.4216%。独立CPU诊断用相同252维冻结特征和206监督拟合固定正则0.01的类别等权线性最小二乘分类器，在相同644,238个201开发点上得到34.1602%，点准确率71.7542%；若平均所有真值或预测中出现的类别，交并比为24.6713%。诊断耗时12.83秒，线性方程残差及系数见`results/train/stu/linear_probe.json`。该诊断的正常分类表现优于已完成的距离模型，说明仍有正常类别信息未被当前距离规则充分利用；它不构成特征能力上限，也没有提供异常识别效果证据。

此前正常数据阶段CPU回归为58项通过、12项CUDA相关测试跳过。回归发现通用推理入口读取旧模型不存在的`mode`属性，已改为与现有可选属性规则一致；该修复不改变当前冻结感知模型的输出。

## 历史证据与失效边界

旧类别条件重建版本的完整val19为AP 0.4077170639%、AUROC 89.4657569867%、FPR95 51.3258182839%，独立复算一致，见`results/train/class/verification.json`。正常分类有小幅改善没有转化为足够的异常排序能力。

旧疏密监督存在可识别性问题：201第0帧314,739个实际记录只有104,913个唯一XYZI，点重复三次。去除重复后骨干体素输入不变，第八邻居间距却明显改变。因此当前取消这一疏密监督，不修改官方评价的原始点计数。

冻结特征恢复在201第64帧独立检查过，252维特征、原16类输出及缓存逐值一致；没有发现额外的点序错位或输出塌缩。

此前辅助数据版本完成7轮、1,752.74秒，选第4轮。201类别等权召回由40.5414%提高到47.7521%，远距类别等权召回由39.3796%提高到41.4760%；这些数值不属于新的仅STU版本，也不是平均交并比。其637,041/644,238个开发点的最近参考来自206，证明目标数据主导，但停车召回仍为0，不能据此称正常分割很好。

该历史模型240个官方状态张量与原始权重相同。32个真实正常查询的部署距离与独立CPU双精度计算相比，最大绝对误差0.000426、相对误差8.07e-7。正常诊断的校准缓冲区加载错误已修复，并对644,238点重跑。25项测试覆盖距离、梯度、时间排除、细类边界、完整点身份、校准、仅STU读取及未见训练类别的交并比计数；这些检查不替代科学效果。

## 比较与当前结论

最近邻及距离学习已有研究基础，包括[Deep Nearest Neighbors](https://proceedings.mlr.press/v162/sun22d/sun22d.pdf)和[Relative Mahalanobis](https://arxiv.org/abs/2106.09022)。最近邻或线性变换本身不能宣称为新颖贡献。

[LIDO表2](https://arxiv.org/html/2604.23604)的目标为AP 27.53%、AUROC 95.05%、FPR95 34.86%。LIDO的训练来源与本项目不同。当前没有证据证明本方法达到这些数值，也没有证明当前冻结感知及后接模块已在STU正常语义上足够强。

下一项能够改变判断的工作是完成当前辅助监督训练，检查同一距离模块能否在保住201正常语义表现的同时改善201合成异常排序，再对选定模型进行val19最终重复评价。合成开发改善与真实未知异常泛化需要分别获得结果；当前没有新一轮训练完成或三项指标超过LIDO的证据。代码权威实现位于`src/normal.py`、`src/train.py --normal`、`src/model.py`和`src/evaluate.py`。历史论文初稿不能作为当前方法已经完成的证据。
