# 模型与训练说明

本项目采用观测约束的正常回波场与逐点兼容性分割网络。输入单帧 LiDAR，输出每个实际回波的异常分数。实际观测路径编码场景与点细节；正常路径根据目标区域之外的上下文预测三维回波场；兼容性模块将实测回波与预测分布共同送入一个逐点异常头。所有可学习模块联合训练。

本文件说明当前方法、nuScenes 数据、训练和评价入口。方法的新颖性与性能仍需文献比较和实验支持；相邻工作目录保存在 `literature.csv`。

**1．任务、贡献与设计判断**

目标是单帧、LiDAR 单模态的未知异常点级分割，优化 AP、AUROC 和 FPR95。正常集合包含道路、车辆、行人、路缘等协议内正常结构。道路中间、高出地面、点少、预测不准，均不能单独成为异常定义。少回波、低矮、远距和弱遮挡是整体方法可能自然获益的场景。

模型回答两个相互补充的问题：

- 实际点及其周围场景，在已学习的正常/异常判别空间中是否合理？
- 隐去待检验区域的实测回波后，周围信息支持哪些正常三维结构？这些结构通过实际射线能否解释待检验回波？

第二条路径提供一种受约束的表示与学习信号，不是在同一输入之外凭空增加信息。正常预测失败也不直接等于异常，最终判断仍由有正常与异常监督的逐点解码器学习。

拟主张的贡献：

> 用目标区域不可见的上下文预测共享正常三维回波场，通过明确的射线关系构成正常回波联合分布；将该分布与实际点之间的兼容性纳入端到端异常分割，使正常几何学习直接服务于逐点异常排序。

贡献对象是这个完整学习机制及其被验证的收益。高斯、射线积分、掩码预测、注意力、重建与判别联合训练本身均已有先例。

**2．确定的整体结构**

| 部分 | 实施配置 |
|---|---|
| 实际点编码 | LitePT-S，5 cm 体素；现有原始点 MLP、mean/max 聚合、六尺度条件融合与逐点回填 |
| 实际点状态 | 64 维场景状态＋64 维点细节投影＋64 维体素内位置编码，相加得到 64 维 $f_i$ |
| 正常上下文 | 1°、2°、4° 嵌套角度块；各目标块读取同层 5×5 邻域去除自身的 24 块 |
| 上下文编码 | 原始坐标、强度、单位方向共 7 维→64→64；仅块内 mean/max 池化，投影至 128 维 |
| 正常假设解码 | 四个 128 维查询，两层四头交叉注意力，前馈宽度 256；三尺度共享参数 |
| 正常回波场 | 每个假设八个各向异性三维高斯基函数；中心、完整协方差、幅度与假设权重均由网络预测 |
| 射线观测 | 解析计算沿射线的回波密度和累计量，构成归一化的条件距离分布 |
| 兼容性解码 | 每个完整假设的 log 条件密度与 log 先验经共享 2→16→64 映射，并加入尺度编码；实际点查询读取四个假设，每尺度输出 32 维 |
| 最终头 | $[f_i,e_i^{1},e_i^{2},e_i^{3},\ell_i^{1},\ell_i^{2},\ell_i^{3}]$，163→128→64→1 |
| 输出 | 每个真实点一个 FP32 logit；同一分数用于 AP、AUROC、FPR95 |
| 排序学习范围 | 默认一次更新八扫描共同计算，微批次仅用于调度；显存不足采用分数梯度缓存与重算 |

四个假设、八个基函数是容量设置，不是四类正常物体或八种异常模板。三个尺度是留出范围，不是异常尺寸规则。这些是确定实现所需的默认值，没有理论或实验保证它们最优。

**3．实际观测路径与严格的信息边界**

实际路径由 [src/model.py](src/model.py) 实现点细节和上下文编码。训练使用固定的官方 nuScenes LitePT-S 预训练权重初始化骨干，正常场、兼容性模块与异常头重新初始化。所有模块联合训练。

论文需披露官方预训练数据和权重。

对某尺度目标块 $B$，正常场参数只允许依赖块外上下文 $C_B$、固定网格位置和尺度。逐点 MLP 后先做各块独立池化，排除目标块，随后才运行上下文交叉注意力。不能先整帧混合特征，再遮掉最后一层目标 token。

禁止正常场预测器读取目标块的距离、强度、点数、三维近邻、体素、骨干特征或异常分数。禁止跨块 BatchNorm、全扫描统计或全局 token 将这些信息带入正常场。使用逐点/逐 token LayerNorm。

上下文输入为 $(x/U,y/U,z/U,I,d_x,d_y,d_z)$，$U$ 为协议距离上限；不增加人工距离方差、密度阈值、地面高度等描述子。mean/max 是对学习特征的标准池化。块位置与尺度经学习投影加入，空块使用缺失掩码；上下文全空时使用学习到的空上下文 token，不直接输出“正常”。

同层邻块中的点可能仍含异常；多尺度留出降低部分污染风险，但不能保证任何大小的异常都被完整遮住。尤其不能声称 4° 是未知物体大小的上界。

目标射线的方向和原点只进入观测计算；目标距离只在比较、正常辅助监督和最终异常判别时使用。最终判别器可以读取实际点；这是任务输入，不构成对正常预测的前向信息泄漏。

**4．正常三维回波场**

对每个目标块、每个尺度，网络输出 $K=4$ 个正常假设及权重：

$$
\pi_k=\operatorname{softmax}(b)_k,\qquad
\lambda_k(x)=\sum_{m=1}^{M}a_{km}
\exp\!\left[-\frac12(x-c_{km})^\top\Sigma_{km}^{-1}(x-c_{km})\right],
\quad M=8.
$$

$c$ 是三维中心，$\Sigma$ 是正定的完整三维协方差，$a>0$ 是幅度，单位为每米。采用 Cholesky 参数化保证正定，用正值参数化保证幅度为正；仅保留数值稳定所需的极小值。

中心、朝向、三个轴尺度与幅度都通过梯度学习，不预设平面、物体类别、道路高度或人工绝对/相对噪声公式。较扁、较长或较宽的核可组合表示不同结构；高斯尾部没有硬边界。所谓“正常”仍是任务中的正常场景，不是说正常数据必须服从单个高斯分布。

同一个假设内的多个核共同存在；不同假设表示不同正常解释。所有射线共享该块预测的同一组场参数，不能让每个目标点自行生成一套能解释自己的三维结构。

共享范围是同一块内的多条射线；不同块和不同留出尺度分别预测，不宣称已经得到跨整帧、跨视角或跨距离一致的全局地图。多个假设无需被人为强制成不同类别；若上下文足够明确，权重集中本身不构成错误。

初始化使用覆盖有效距离区间的较宽场，并用不同的查询初始化打破假设/核的对称性；不从目标点位置初始化中心。初始化尺度仅是优化起点，随后可学习。

$\lambda$ 是用于正常距离预测的有效回波率场。仅凭留出距离无法唯一恢复真实占据、反射率或完整场景；不把它包装成可辨识的物理世界模型。

**5．由射线生成正常观测分布**

单位方向射线为 $x_i(r)=o_i+r d_i$。以已有 STU 评价采用的有效距离区间 $[L,U]=[2.5,50]$ 米为例；该区间本身不表示 STU 进入新模型训练：

$$
H_{ik}(r)=\int_L^r\lambda_k(o_i+t d_i)\,\mathrm dt,\qquad
Z_{ik}=1-\exp[-H_{ik}(U)],
$$

$$
p_{ik}(r\mid\text{有效区间内有记录回波})=
\frac{\lambda_k(o_i+r d_i)\exp[-H_{ik}(r)]}{Z_{ik}},
\qquad L\le r\le U.
$$

这是一个归一化的距离密度：近处回波率影响远处被观测到的相对概率。同一几何场通过不同射线产生彼此关联的预测，而不是若干独立自由距离回归器。

该形式采用生存概率/首次事件的有效观测模型。真实设备可能记录最强回波而非理想首次回波，扫描运动也可能使射线原点近似有误，因此不能称为完整、精确的传感器仿真。仅有回波点、缺少可靠原始发射记录时，不监督丢失回波，不把空角度格当成自由空间，也不将 $Z$ 宣称为校准后的实际 ray-drop 概率。

区间是评价协议的有效范围，不是异常阈值。其他数据使用其固定协议边界，训练和评测必须一致；区间外或忽略标签的点可作为上下文，不因此被标为正常。若真实束原点不可用，明确使用扫描参考原点近似，不能虚构精确发射轨迹。

**解析计算，不做稠密射线步进。** 单个高斯沿射线仍为一维高斯响应。令 $\Sigma=AA^\top$、$v=A^{-1}d$、$w=A^{-1}(o-c)$：

$$
\mu=-\frac{v^\top w}{v^\top v},\qquad
\tau=(v^\top v)^{-1/2},\qquad
\log h=\log a-\frac12\|w+\mu v\|^2,
$$

$$
\lambda_m(r)=h\exp[-(r-\mu)^2/(2\tau^2)],
$$

$$
\int_L^b\lambda_m(r)\,\mathrm dr
=h\tau\sqrt{2\pi}
\left[\Phi\!\left(\frac{b-\mu}{\tau}\right)
-\Phi\!\left(\frac{L-\mu}{\tau}\right)\right].
$$

各核积分相加得到 $H$。这在上述有效回波率假设内支持重叠核，不需要硬求交、深度排序、最近表面选择或额外人工宽分量。解析高斯射线积分本身已有先例，不作为本工作的发明。

仅有条件回波位置监督时，幅度可能不可充分辨识；小累计量极限下分布接近沿射线的归一化场响应。因此创新主张依托“共享三维场对预测的约束”，不能仅靠公式中出现透射率就声称已经学会真实遮挡。

本方案直接定义有记录回波条件下的预测模型，$\pi_k$ 是这个条件模型的混合权重，不是完整物理生成过程的无条件场景先验；不能从当前条件密度反推未观测射线的真实占据或返回概率。

**6．共享正常解释与可学习逐点判别**

正常辅助任务使用一个假设共同解释目标块内的有效正常回波：

$$
P(R_B\mid C_B,Q_B,N)
=\sum_{k=1}^{K}\pi_k\prod_{i\in B_{\rm valid}}p_{ik}(r_i).
$$

实现顺序为：每个假设先累加各射线的 log 概率，再对假设做 logsumexp。不能先为每点挑选最佳假设再求和。辅助负对数似然按块内有效点数归一化，再在尺度和扫描间汇总；同一组完整概率参数同时用于辅助学习和判别。

使用全部有效正常目标回波，以分块累计控制显存。没有目标块内的后验重加权，也没有固定污染比例。推理的假设先验 $\pi_k$ 始终来自块外上下文。

逐点正常 log 密度为：

$$
\ell_i^s=\log\sum_k\pi_k^s p_{ik}^s(r_i).
$$

每个假设的全部八核共同确定该假设对实际回波的条件密度。兼容性模块直接编码完整假设的概率证据：

$$
v_{ik}^s=\psi\left[\log p_{ik}^s(r_i),\log\pi_k^s\right]+\delta_s,
\qquad \psi:\ 2\rightarrow16\rightarrow64.
$$

共享映射的隐藏层使用 LayerNorm 与 GELU；$\delta_s$ 是 64 维学习尺度编码。实际点状态经 64→32 投影，归一化实际距离 $r_i/U$ 经 1→16→32 编码，两者相加得到查询 $q_i\in\mathbb R^{32}$。

这里的 $p_{ik}$ 是第 5 节由同一假设全部八核共同确定、包含累计回波率和归一化项的条件首返回密度，不是某个核的独立响应。有效距离区间外的真实回波，其送入 $\psi$ 的 log 密度置为零；正常联合辅助计算仍使用原始概率值及既定有效点掩码，不改变监督目标。

将 $v_{ik}^s$ 分为 32 维键和值，再由同一查询读取四个假设：

$$
e_i^s=\operatorname{Attn}_{k=1,\ldots,4}
\left(q_i,\operatorname{key}(v_{ik}^s),
\operatorname{value}(v_{ik}^s)\right)
\in\mathbb R^{32}.
$$

假设注意力使用四头，每头八维；映射在所有假设和尺度间共享，不加入假设编号嵌入。尺度编码区分三个留出范围。核的几何参数参与解析密度计算，兼容性模块读取完整假设的密度与先验。注意力权重只是判别特征的汇聚系数，不是对 $\pi_k$ 的后验重估；实际点信息不会经此步骤返回正常场预测器。

最终头输入 64＋3×32＋3＝163 维，MLP 163→128→64→1，隐藏层采用标准归一化和 GELU。三个 $\ell$ 提供绝对兼容程度，避免仅靠归一化注意力在“全部解释都很差”时仍必须选出一个解释。它们不是手工异常分数或标签，最终头可结合语义、实际细节和学习到的分布特征决定如何使用。

不输出区域统一异常分数，不将块级联合 NLL 复制到每点；也不对最终分数做邻域平均、连通域过滤、道路硬掩码或按点数删除目标。

联合分布约束用于正常训练；推理提供的是逐点边缘密度与预测分布特征，最终头同时读取实际场景上下文。边缘密度高并不证明整块观测能由一个正常假设共同解释，也不证明该点一定正常。论文不能把当前逐点头描述为精确的区域联合异常检验。

**7．一个训练目标，直接服务三指标**

令 $s_i$ 为最终异常 logit，正常/异常标签分别为 0/1。使用：

$$
\mathcal L=
\mathcal L_{\rm grouped\ BCE}
+\lambda_{\rm rank}(t)
\left[
\widetilde{\mathcal L}_{\rm AP}
+0.1\widetilde{\mathcal L}_{\rm AUROC}
+0.1\widetilde{\mathcal L}_{\rm FPR95}
\right]
+0.1\mathcal L_{\rm normal}.
$$

分类监督按完整有效批次分为异常点 $A$、插入正常物体点 $C$、其余可靠正常点 $B$。每组先取组内平均交叉熵，再按下式组合：

$$
\mathcal L_{\rm grouped\ BCE}=\tfrac12\overline{\mathcal L}_A
+\tfrac14\overline{\mathcal L}_C+\tfrac14\overline{\mathcal L}_B.
$$

缺组时，仅将实际存在组的权重归一化；当前源数据批次配额保证三组都有监督。插入身份只决定训练损失的归组，不进入模型输入；输出仍为正常／异常二分类。BCE 和三个排序代理都优化同一个最终输出，不建立三个独立头，也不分别挑三个检查点报告最好数字。正常预测只是辅助约束。

$\lambda_{\rm rank}$ 在总进度 10%–20% 由 0 线性升至 1；正常辅助权重固定为 0.1。

| 指标 | 学习目标关注什么 | 结构上的配合 |
|---|---|---|
| AP ↑ | 异常点在排序前部的精确率与召回 | 逐点细节保留边界；兼容性特征提供可学习的额外判别依据 |
| FPR95 ↓ | 高召回位置仍被排到前面的正常点 | 正常预测与场景状态联合判断，允许正常边界和多种正常解释 |
| AUROC ↑ | 整体正常/异常点对的排序 | 同时覆盖普通正常点与困难正常点，避免只顾少数高分误报 |

**排序范围。** 一次参数更新内的完整有效批次共同排序，每批八扫描。对排序损失而言，微批次划分仅是计算调度，不能各自独立确定召回阈值或排序池。

在有效批次上最多取 256 个均匀抽样的异常锚点；正常点保留最高分 512 个，并从其余正常点抽取最多 3,584 个、用抽样权重补偿。AP 的正样本秩使用该有效批次全部异常点；AUROC 使用加权点对 softplus；FPR95 由该有效批次全部异常点确定一个软 95% 召回阈值，再计算加权正常软超阈率。温度沿用 1。无异常的完整有效批次仍训练正常 BCE，不能伪造排序目标。

动机可用简单反例说明：一组扫描的异常分数为 10、20 个正常点均为 9；另一组异常为 1、20 个正常点均为 0。每组单独评价都是 AP=1、AUROC=1、FPR95=0，合并后则为 AP=0.54545、AUROC=0.75、FPR95=0.5。因此，独立微批次排序不是更大批次排序的等价实现。此反例是数学说明，不是本项目真实失分归因；已有 BCE 也会提供跨帧分数约束。

修正后仍然是有限批次、有限样本和训练分布下的代理，不能称为全数据指标的精确或无偏估计，也不保证三个测试指标同步改善。随机正常点覆盖对 AUROC 很重要，不能为了困难负例挖掘全部删除。推理不做逐帧 min–max、分位数或其他按当前帧统计变化的分数归一化。

异常损失通过兼容性解码、log 密度和观测公式回传到正常场与正常上下文编码器；同时回传到实际点骨干。整个网络不设正常分支 stop-gradient，不要求先单独预训练预测器再通过门槛才进行检测。

$\mathcal L_{\rm normal}$ 仅使用可靠、未修改的真实正常扫描；合成异常用于最终判别，不被当作正常重建目标。完整 NLL 的归一化会惩罚一味扩宽分布，但不自动保证校准，也不能完全排除有限训练集上的过度尖锐拟合。

异常梯度可以改变正常场，辅助权重 0.1 也不是理论上的正常性保证；因此“端到端”与“始终是校准的纯正常分布”不能画等号。保持正常辅助目标、检查正常扫描上的覆盖与宽度，是解释该分支含义所需的诊断，不另设预测器预训练门槛。最终模型可能降低某些几何证据的使用权重，不能把这种可学习融合提前宣称为不会掉点的保证。

**8．当前训练数据与适用边界**

当前数据位于 `results/data/background`、`results/data/objects` 和 `results/data/sequence`。训练与源域开发只使用 nuScenes；STU 的公开标签规范与评价代码用于确定任务边界，数据生成不读取 STU 扫描、标签或预测来选择供体、位置或生成参数。训练使用 `sequence/train.json`，开发使用 `sequence/val.json`。

**8.1．背景、标签与供体**

背景保留官方训练分区的 700 个场景、28,130 帧和验证分区的 150 个场景、6,019 帧。相邻帧和同一场景的全部合成版本留在同一分区，采集日志与供体官方实例编号跨分区不相交。正常监督只使用与目标正常含义兼容的源类别及明确补充标注，其余点保留输入坐标、标签忽略。每份清单的 `mapping` 给出全部 32 个源类别的实际编号、决定和理由；任何原始类别都没有被整类转换成异常。

原始 `debris`、`pushable_pullable` 点仍被忽略。经相机与原始点云核定的物体，只在明确插入道路后成为辅助异常监督。STU 论文说明其目标异常与这两个源类别所含对象有意区分，因此不能把这些供体写成 STU 官方目标异常，也不能仅用“不是19类正常物体”推导异常标签。

辅助异常目录现有 40 个供体：训练 27 个、验证 13 个，来源分别为 21、12 个场景和 13、8 个采集日志。类型包括袋包、垃圾容器、搬运车、工具箱和旅行箱。单次实测点数为 8–601；物体数不等于独立几何类别数。此前训练 #37 与验证 #36 的罩顶垃圾桶，位置相距约0.52米、采集相隔6天，原图显示疑似同一实物，两者均保守排除。目录记录了原图、位置和判定依据，但不将疑似身份写成已确证身份，也不宣称剩余对象已被证明跨场景物理身份绝对独立。

`results/data/background/annotations.json` 只补充明确建筑区域：训练4帧1,328点，验证2帧3,424点，共4,752点。未传播到邻帧，也未把整个 `static.manmade` 类改成正常。全部点已独立投影回记录的原图区域，并复核原始标签与坐标。这是局部正常标签补充，不能代表建筑、杆件和围栏监督已全面补齐。

正常供体从成年行人、自行车、摩托车和汽车中提取，使用与辅助异常相同的实测表面构建、道路支撑、碰撞与可见性规则。相机用于供体与标签核定，不作为模型输入。

**8.2．数量规划与实际观测**

每个场景为辅助异常、正常对照分别预分配一个供体，按物体组别及实例使用次数分配，并限制单实例分配场景数。两种角色各安排四个独立版本：基础版本，以及20–30、30–40、40–50米三个定向版本。850个场景共安排6,800个放置任务。版本内部只放一个原尺寸物体，整段世界位置与姿态固定；各版本在原始场景中独立生成，不叠加物体。

定向版本的锚定帧必须实际产生所请求距离层的插入回波。后续帧由真实车辆运动形成不同距离和点数，覆盖统计使用保存点的实际距离，不能把请求数或物体中心距离当作有效观测数量。失败不更换容易的供体，不补射线、不补背面、不缩放物体来满足配额。原始帧只进入读取清单一次。

道路支撑汇集同一场景的实测道路点，保留实际点坐标，并检查当前帧道路是否一致。落点在局部实测道路平面凸包内至少一米；该条件同时用于异常和正常对照，不能当作已知车道中心线。所有关键帧执行占据与保守包围框碰撞检查。完整核验中，4,095个成功放置均有实测路面锚点，独立复算的最小内部距离为1.0007056827米。

观察只沿接收帧已有回波方向，与供体实测开放表面求最近交点。交点更近时才替换原槽位；范围外变化也保留为上下文。源观察未支持的背面或更近视角不生成。未知表面可能遮挡的原背景保留坐标、标签忽略。这仍是局部实测表面的观测近似，不是完整物体或完整发射过程仿真。

原始背景与合成版本通过原扫描编号关联，正常辅助目标读取未修改原始扫描的可靠正常点，最终判别读取改动后的扫描。辅助异常与正常对照共享生成流程和请求距离层，但位置独立，不称为严格几何匹配的因果对照。

**8.3．当前产出与独立核验**

| 项目 | 训练 | 验证 |
|---|---:|---:|
| 原始背景帧 | 28,130 | 6,019 |
| 辅助异常改动记录 | 23,795 | 4,699 |
| 正常对照改动记录 | 20,895 | 4,861 |
| 成功辅助异常放置版本 | 1,828 | 366 |
| 成功正常对照放置版本 | 1,542 | 359 |
| 至少五个异常点的帧 | 10,486 | 1,796 |
| 一至四个异常点的帧 | 10,180 | 1,985 |
| 范围内辅助异常点 | 235,996 | 52,341 |

当前训练清单为72,820条记录，验证清单为15,579条记录，合计88,399条。清单不是88,399个独立采集样本；其独立原始背景仍为850个场景、34,149帧，供体仍为40个实例。

30米外至少含五个异常点的观测，训练为494帧、112个接收场景、12个供体；验证为223帧、26个接收场景、6个供体。当前正常对照在相同距离条件下，训练为575帧、104个场景、68个供体，验证为129帧、21个场景、18个供体。相邻帧、多个放置版本和重复供体不能视为独立重复实验。

构建剔除了7,037条没有改变输入及有效监督的记录，以及68条重复的纯忽略标签改动。最终全部88,399条记录完成独立复算：原始点与标签对应、改动槽位、正常与异常监督计数、距离分布、评价资格及插入正常点计数一致；未发现有效观测重复，所有改动数组都有唯一索引引用。射线方向采用双精度独立归一化复算，最大差为6.765104410000333e-08，未放宽原2e-7容差。

`sequences.json` 的 `summary.observation_coverage` 分别记录两种角色在五个距离层的点数、有效帧、接收场景、供体实例，以及至少五点的覆盖；另按1–4、5–9、10–29、30–99、至少100个插入点统计帧数。`table.csv` 与 `table.html` 给出850个序列的四个版本，共3,400行；原时间轴长度会在版本之间重复展示，读取记录数只在基础版本计入原帧一次。

**8.4．评价解释与未消除的限制**

STU官方点级代码在2.5–50米内处理标签，只汇总至少五个异常点的帧，计算AP、AUROC和FPR95。1–4点帧保留训练与稀疏观测分析，不能混充符合该规则的评价样本。正常背景及正常插入对照需要单独分析误报；源域开发还需按供体类型和距离查看结果，避免仅凭总体点级分数判断数据充分性。

距离扩增没有创造新形状。训练、验证异常点中，垃圾容器仍分别占44.7%、86.8%；验证侧稠密物体会支配总体点级指标。源内实例分离也不能保证几何类别未见。nuScenes辅助异常与STU目标异常的类别和观测域不同，局部表面、强度插值及忽略的未知遮挡都保留了近似边界。上述事实不能用增加合成帧数消除；数据构建与实现核验不能证明三项SOTA，也不能证明模型没有记住供体形状。训练和开发结果仍需独立核定，数据覆盖本身不保证目标域表现。

**8.5．当前生成与读取入口**

生成使用一个新的空输出目录；原始nuScenes文件不被覆盖：

```bash
OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 \
/home/jasongao/Study/AJAE/.venv/bin/python -m src.data nuscenes \
  --objects results/data/objects/catalog.json \
  --normal-annotations results/data/background/annotations.json \
  --output <新的空输出目录> --workers 12
```

当前模型读取 `results/data/sequence/train.json` 和 `val.json`。训练索引已超过GitHub单文件上限，因此两份可再生成的读取索引与增量数组保留在本地；生成源、供体目录、补充标签、完整生成报告及CSV统计纳入版本控制。该存储安排不改变本地训练输入或标签。

公开边界依据：[STU原论文](https://arxiv.org/html/2505.02148v1)、[STU官方点级评价代码](https://github.com/kumuji/stu_dataset/blob/main/compute_point_level_ood.py)、[nuScenes逐点标注规范](https://github.com/nutonomy/nuscenes-devkit/blob/master/docs/instructions_lidarseg.md)。

**9．计算与数值实现**

完整模型有 13,208,930 个参数。全模型使用 FP32，包括骨干、协方差求解、射线密度、兼容性头和排序代理。射线计算按 2,048 点分块；骨干注意力按完整窗口分块并在反向传播时重算。当前训练使用 `--retain-activations`，保留单扫描的模型和正常场中间结果。

训练时，完整扫描继续提供实际点骨干与正常场的上下文，仅对标签为正常或异常的监督点计算末端兼容性与异常头。原始正常参照仅对可靠正常目标查询射线密度，保留同样的块内联合似然归约。推理仍为所有真实点输出分数。

正常场的每层交叉注意力先为每个独立角度块计算一次键和值，再按原来的 24 个邻块索引读取，避免同一个块在多个邻域中重复投影。注意力参数与公式不变，目标块仍在邻域交互前排除；各块独立的投影不会混合目标与上下文信息。

八扫描的检测损失使用分数梯度缓存：先在参数不变时收集全部监督点的分数，在完整有效批次上计算检测损失对分数的梯度，再按相同输入、随机状态和前向分组重放，用缓存梯度反传并加入正常辅助损失，最后统一更新参数。每个扫描反传后释放其激活；BatchNorm 的运行统计只推进一次。每次更新检查重放分数，绝对与相对容差均为 `1e-5`。

高斯区间概率使用对数累积概率差。负尾导数使用与原函数数学等价的缩放余误差函数表达，避免两个大负数相减后求指数；窄区间展开先使未选分支的输入安全，再计算等价多项式；大累计回波率只限制内部指数计算，保留归一化项本身及其导数。相关实现位于 `src/normal.py`，数学与梯度检查位于 `tests/test_normal.py`。

**10．当前训练配置**

| 项目 | 当前设置 |
|---|---|
| 训练清单 | `results/data/sequence/train.json` |
| nuScenes 开发清单 | `results/data/sequence/val.json` |
| 初始化 | `assets/nuscenes.pth`，官方 nuScenes LitePT-S 权重 |
| 预算 | 2,500 次参数更新，20,000 次扫描访问；不是完整遍历全部数据 |
| 随机种子 | 0 |
| 优化器 | AdamW；骨干峰值学习率 `2e-4`，新增模块 `2e-3`，权重衰减 `0.005`，梯度范数上限 1 |
| 学习率 | 前 5% 更新从峰值的 0.1 倍预热，随后余弦衰减至峰值的 0.01 倍 |
| 有效批次／前向分组 | 8／2 个扫描 |
| 数据加载／数值线程 | 4／2 |
| 检查点保存间隔 | 25 次更新 |
| 完整验证 | 第 1,250 和 2,500 次更新后，使用同一完整 nuScenes 开发集 |
| 每批组成 | 3 帧至少五异常点、1 帧一至四异常点、2 帧含有效插入正常点、2 帧原始正常背景 |
| 训练目录 | `results/train/hypothesis` |
| 恢复状态 | `results/train/hypothesis/0/field/last.pt` |
| 选定模型 | 按开发集 AP 选取的单一检查点 `results/train/hypothesis/0/field/best.pt` |
| 终点逐点分数 | `results/train/hypothesis/0/field/val2500.npy`，对应第 2,500 步，不一定对应选定模型 |

各插入角色内，距离层按该角色池中各层的记录容量比例均匀推进；每个距离层内再按供体实例、接收场景、点数区间、放置版本和观察区段交错，先访问时间上分离的帧。原始背景按场景交错。每个角色池完整遍历后才重用记录，同一有效批次不重复记录。异常按有效异常点的中位距离分层，正常插入按有效插入点距离直方图的中位层分层，均不使用物体中心距离替代实际观测。

按种子 0 核定的计划读取包含 20,000 条互异记录：7,500 条至少五异常点记录、2,500 条稀疏异常记录、5,000 条有效正常插入记录和 5,000 条原始背景记录，共覆盖 700 个背景场景、全部 27 个训练异常供体和 199 个正常供体，含 153,394 个异常点和 60,127 个插入正常点。这些是计划样本的读取统计，不是已完成训练的证据。

按 2.5–10、10–20、20–30、30–40、40–50 米统计，计划异常点数分别为 76,996、56,079、16,322、3,046、951；插入正常点数分别为 27,811、20,722、8,635、2,257、702。后半程保留 50.21% 的远距异常点观测。距离容量比例仅由源训练角色池确定，不根据开发集构成重新加权。

20,000 条记录占完整 72,820 条训练清单的 27.47%。当前角色配额排除其中 6,019 条没有有效异常点或有效插入正常点的合成记录；预算优先覆盖有判别监督的物体观测，不等于完整遍历全部数据。本轮从官方 nuScenes 骨干权重重新初始化，正常场、兼容性读取与异常头重新初始化，不继承旧 1,500 步训练权重；旧训练仍应计入总研究计算成本。

旧逐核模型的 1,500 步实际见到 34,076 个异常点、39,326 个插入正常点和 222,778,056 个正常点；插入正常点只占全部正常监督的 0.01765%，162 个有效批次没有异常点。这些记录支持加强正常物体对照的监督份额和固定异常批次配额，但不能单凭数量确定旧模型失分的原因，也不是新方案改善性能的证据。

本轮首先与旧运行在同一 nuScenes 开发集上的结果比较：AP 58.09939%、AUROC 97.54486%、FPR95 13.77040%。评价清单、有效点及官方指标规则保持相同；报告三个指标各自的变化，不能只凭 AP 上升就宣称三项共同改善。该比较检验源域进展，不能用 STU 上的公开分数给本自建开发集定义领先水平。

该轻量模型与新监督方案尚未完成正式训练。固定权重和随机状态、未更新参数的同一真实八帧检查，冷启动耗时 13.3193 秒，预热后两次为 10.4328 和 10.4363 秒，峰值显存为 7,872,232,960 字节。三次损失均为 2.231501579，分数重放最大绝对差为零，梯度有限；这些是实现与计算检查，不是训练性能结果。计时不含读盘、优化器更新、保存和验证，不能直接与旧运行的每步均值比较提速比例。

本轮开发目标是在与旧 1,500 步完全相同的 nuScenes 开发集及评价规则下，取得明确改善。第 1,250 和 2,500 步各执行一次完整验证，以 AP 为主指标选取一个检查点；AP 相同时依次优先较低 FPR95、较高 AUROC。同时保留终点结果，每个检查点的三项指标完整报告，不拼接不同检查点的最佳数值。

按预热后的单批计算时间估算，2,500 步约需 7.25 小时模型计算；两次完整验证按旧运行粗估共约 18.5 分钟，读盘、更新和保存仍需额外余量，不能保证整轮在八小时内完成。以下命令在 470 分钟发出中断信号，并最多等待十分钟退出，给整个运行设置八小时外层保护；若验证未完成，应保留恢复状态，不将中断记录当作完整评价。

```bash
mkdir -p results/train/hypothesis
set -o pipefail
OMP_NUM_THREADS=2 OPENBLAS_NUM_THREADS=2 MKL_NUM_THREADS=2 \
timeout --signal=INT --kill-after=10m 470m \
  /home/jasongao/Study/AJAE/.venv/bin/python -u -m src.train \
  --train-manifest results/data/sequence/train.json \
  --val-manifest results/data/sequence/val.json \
  --initial assets/nuscenes.pth \
  --output results/train/hypothesis \
  --updates 2500 --eval-every 1250 --retain-activations --workers 4 --threads 2 \
  --score-path results/train/hypothesis/0/field/val2500.npy \
  --save-every 25 --progress --resume \
  2>&1 | tee -a results/train/hypothesis/console.log
```

`--resume` 仅在本训练目录存在同配置检查点时恢复模型、优化器、随机状态和下一批位置，否则从官方骨干权重开始。旧目录 `results/train/sequence8h` 保留逐核读取模型的已完成结果供比较，其检查点不兼容当前轻量读取结构，不能用于恢复本轮模型，也不能作为本轮性能证据。

当前检查点记录 `src` 与 `vendor` 中全部 Python 源码的身份。后台训练期间保留这些文件，既支持正在执行的训练，也支持一致的恢复。已停止使用的分析入口不作为当前运行入口。

**11．评价入口与范围**

同一模型的同一个点级分数用于 AP、AUROC 和 FPR95。完整训练结束后，可用以下入口复算 nuScenes 开发指标：

```bash
/home/jasongao/Study/AJAE/.venv/bin/python -m src.evaluate validate \
  --checkpoint results/train/hypothesis/0/field/best.pt \
  --manifest results/data/sequence/val.json \
  --output results/train/hypothesis/nuscenes.json
```

模型、预处理和分数处理确定后，STU 评价使用同一入口，将 `--manifest` 指定为 `assets/val.json`，并使用独立输出文件。`assets/rays.npz` 保留 STU 射线读取所需的标定信息。STU val19 是开发验证集，不称为独立隐藏测试集；隐藏测试使用 `src.evaluate test`，需要已完成训练并选定的检查点。

当前训练及其结果只支持所实际执行的数据和评价范围。源域辅助异常不等于 STU 官方目标异常，源域开发指标也不能换算成 STU 的三项指标。原始 nuScenes、STU 数据与外部 Python 环境保留在原位置。
