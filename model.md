# 冻结点云感知后的共享特征证据

当前主线为`AJAE-feature-evidence`：在冻结的成熟点云感知模型之后，训练一个轻量模块，同时输出正常语义类别和逐点异常分数。当前201开发选定模型为`results/train/observations/frozen.pt`第13轮，已完成真实val19全部1,960帧、193,879,969个有效点的重复评价：AP为11.5841%、AUROC为98.6624%、FPR95为6.8909%。AUROC和FPR95超过LIDO表2，AP仍低于27.53%，三项同时超过的目标未完成。

后续工作只在206训练、201开发：按观测分配异常监督后完成24轮训练，选中第13轮；再补充真实正常物体的合成对照，发现大量正常插入点被误报。直接加入这些对照的联合训练、仅微调异常头，以及降低正常控制权重的训练均已完成；这些候选没有替代按预定AP优先规则更好的`observations`开发参考。训练与开发比较显示，对照监督可以拟合206正常插入点，但201仍有较高误报，并伴随普通背景误报上升。新增多视图正常控制、共同类别竞争及正常平方根权重的对比也已完成。依照同一201扩展开发集的AP优先规则，本轮仍选`observations`第13轮，于2026年9月26日10:44启动、10:51完成该模型的完整val19重复评价；模型、参数与阈值未依据val19重新选择。

## 输入、特征与输出

输入为单帧点云。官方nuScenes预训练LitePT-S骨干及原16类语义头保持冻结，提供早层36维、中层144维、深层72维特征，拼成252维向量并恢复到原始点顺序。nuScenes只提供官方预训练权重，后接模块的训练和开发样本均来自STU。当前输出的正常类别来自新模块的19类语义头；原16类头保持原权重。

记冻结特征为$f$，STU206真实正常点估计的中心为$\mu$，固定白化矩阵为$W$。共享特征和两个输出为

\[
x=(f-\mu)W,\qquad z=x+R(x),\qquad
\ell=zW_s^\top+b_s,\qquad A=zw_a+b_a.
\]

$R$为252→128→252的两层全连接网络，中间使用GELU，最后一层零初始化；因此训练起点保留初始化线性模型的输出。19维$\ell$用于正常语义分类，标量$A$直接作为异常分数，越大表示越倾向异常。两项监督共同更新同一共享残差。中心和白化矩阵不学习，参数与计算均使用FP32。当前选定模型包含69,952个可训练参数。

正常语义头由`results/train/stu/linear_probe.json`中的206正常类别线性拟合初始化；异常头由`results/train/auxiliary/linear_probe.json`中使用206真实正常和合成样本的二分类线性拟合初始化。二者均使用固定0.01正则的最小二乘拟合，转换到同一白化坐标；异常线性输出再作正斜率仿射变换作为初始原始分数。初始化后的201指标已核对能复现独立拟合结果。当前模块沿用206统计量，不继承旧最近邻模型学到的距离变换。

当前主线没有正常场重建、距离预测、疏密预测或最近邻检索。异常头直接接受合成异常监督；原始分数没有经过正常分位数校准，不能解释成已校准的真实异常概率。共享体素中的点可能拥有相同冻结特征，这一上游信息限制仍然存在。

## 数据与监督范围

真实正常训练数据为STU206全部449帧已有缓存中的1,839,104个点。合成数据来自`/home/jasongao/Study/AJAE-v3/samples`，训练只读取其中源序列206的`train`分区，开发只读取源序列201的`validation`分区。这些已保存的几何障碍观测通过原扫描及差分文件恢复；原始点槽位、源扫描、位姿、物体身份和恢复后的异常数量均保留核对。当前没有使用NDP生成数据。

在每个分区内，按每帧有效异常点数1–4、5–20、21–100、大于100四档固定随机抽样，不放回选取，不再按距离或形状平衡。206选择1,920帧，四档各480帧，覆盖237个物体场景；201选择80帧，四档各20帧，覆盖57个物体场景。抽样范围和监督点数如下。

当前选中237个训练场景的`world.json`中，异常形状全部为`procedural-csg`：分别有138、49、50个场景使用1、2、3个超二次实体组合，提供完整隐式几何，并非nuScenes实测异常供体。新增正常控制则来自STU真实物体的部分实测三角面。共同使用传感器响应、材质分布和遮挡计算，仍不能排除孔洞、缺失背面及法向估计等几何生成差别；当前没有定量证据证明这些差别导致了模型失败。

| 用途 | 帧范围 | 正常点 | 异常点 |
|---|---:|---:|---:|
| 206真实正常训练缓存 | 449帧 | 1,839,104 | 0 |
| 206合成训练缓存 | 1,920帧 | 983,040 | 219,804 |
| 201真实正常开发缓存 | 奇数64帧区块 | 644,238 | 0 |
| 201合成开发全部有效点 | 80帧 | 6,722,428 | 7,959 |
| 201合成官方式指标范围 | 其中60帧 | 5,200,330 | 7,914 |

合成训练每帧保留全部有效异常点及最多512个具有明确正常细类标签的点。训练缓存总计3,041,948点；“每轮遍历全量”指这些已选缓存点，不是合成库全部帧或原始扫描全部回波。201合成开发保留全部有效检测点，包括没有细类标签的正常点；主要三项指标按STU每帧至少5个有效异常点的规则使用60帧，其余1–4点帧与全部80帧结果另作诊断。

正常细类沿STU19类标注转换，异常点没有正常类别标签。异常二分类损失使用正常和合成异常标签，正常类别损失只使用有明确细类的正常点。201数据参与开发选型，val19不参与参数学习、模型选择或阈值调整。当前方法使用合成异常训练和开发，报告必须保留这一监督条件。

数据依据为`results/train/auxiliary/auxiliary_views.json`、`auxiliary.json`、`synthetic_development.json`及`synthetic_development/features.json`；当前训练范围与初始化来源记录在`results/train/readout/config.json`。

## 首轮训练与选择

每轮将3,041,948个训练缓存点随机排列并各使用一次，批量8,192。损失为正常语义交叉熵与异常二分类交叉熵之和：语义项使每个训练中出现的正常类别具有相等总权重，二分类项使正常、异常分别具有一半总权重。两个输出头及共享残差共同训练，骨干和统计量保持固定。

优化器为AdamW，初始学习率0.001、权重衰减0.0001，按6轮余弦下降。6轮均已完成，模块训练及其开发检查总耗时45.71秒；该时间复用了已经提取的冻结特征，不包括此前特征提取和线性初始化拟合。206合成训练特征提取记录为739.43秒，见`results/train/auxiliary/auxiliary.json`。

模型选择首先要求201真实正常开发平均交并比不低于初始化线性模型的34.1602%，再按201合成开发AP、AUROC、负FPR95依次比较。初始化线性模型本身也可被选择；本次第3轮优于初始化并被选中，保留了学到的残差。真实val19仅对这个已选模型进行最终重复评价。

## 首轮开发结果

下表区分已选第3轮结果与初始化线性模型。正常平均交并比按真值中出现的类别平均；训练点、正常开发点和合成开发点分别报告，不混为同一总体。

| 正常语义范围 | 平均交并比 | 点准确率 |
|---|---:|---:|
| 206真实正常训练缓存 | 71.0482% | 90.3405% |
| 201真实正常开发缓存 | 37.4239% | 75.5954% |
| 同一201范围的初始化线性模型 | 34.1602% | 71.7542% |

201开发若对真值或预测中出现的所有类别取平均交并比，则已选模型为27.0283%。206没有训练实例的停车等类别仍计入201真值出现类的指标；不能通过删除困难类别提高报告数值。以上点级语义指标也不能与STU论文的全景质量指标直接横比。正常结果依据为`results/train/readout/normal206.json`和`development.json`。

| 201合成异常范围 | AP | AUROC | FPR95 |
|---|---:|---:|---:|
| 官方式60帧，初始化线性模型 | 36.4481% | 98.9389% | 5.2637% |
| 官方式60帧，已选第3轮 | 59.0609% | 99.4725% | 2.3000% |
| 全部80个含异常帧，已选第3轮 | 53.5579% | 99.4286% | 2.5096% |

这些结果支持：在当前选定合成开发分布上，共享残差及联合监督比初始化线性模型获得更好的正常分类和异常排序。它们尚不能单独确定共享残差的独立贡献或方法新颖性。逐轮结果位于`results/train/readout/training.jsonl`，已选结果位于`result.json`；其中`evaluation_status`是训练结束时的快照，后续真实评价以`val19/metrics.json`和`val19/verification.json`为准。

固定第3轮模型在已有201合成开发点上进一步按异常点数分档复算，未训练、未改变模型或参数，也未使用val19。结果记录在`results/train/readout/synthetic_strata.json`。

| 每帧异常点数 | 帧数 | 异常点总数 | AP | AUROC | FPR95 |
|---|---:|---:|---:|---:|---:|
| 1–4 | 20 | 45 | 0.0485% | 94.6027% | 16.3500% |
| 5–20 | 20 | 211 | 0.5333% | 96.5000% | 15.9067% |
| 21–100 | 20 | 876 | 9.0519% | 97.8319% | 11.3552% |
| 大于100 | 20 | 6,827 | 81.1746% | 99.8023% | 0.8691% |

四档帧数相同，但大于100点档提供了全部80帧85.7771%的异常点，在主要60帧评价中占86.2648%；因此总体逐点评价更受这一档的异常分数排序影响。少点档的AP明显较低，总体AP不能代表各档均表现良好。各档异常占比不同也会影响AP，这不是控制其他变量后的点数因果比较；异常点数本身还不能唯一确定距离、物体大小、形状或遮挡。该诊断只描述201合成开发集，不能据此断言真实val19中哪种因素造成了误差。

## 当前开发与正常合成对照

训练缓存中，大于100点的合成观测占全部异常点的86.4952%，不超过20点的观测只占2.8357%。`observations`运行将异常监督改为每个观测等质量，四档各占异常项25%的系数总量；正常、异常仍各占二分类项一半，正常语义损失不变。它从同样的线性初始化出发，以0.001学习率完成24轮，201选中第13轮，总耗时140.85秒。原201主要60帧AP为61.4995%、AUROC为99.4104%、FPR95为2.4006%；正常201平均交并比为36.1738%。这是观测加权与延长训练共同改变后的结果，未分离二者的作用。依据为`results/train/observations/config.json`、`result.json`。

全80个合成开发观测的7,959个异常点中，只有6点与同帧正常点的完整252维特征完全相同，且全部属于大于100点档；按逐维最大误差不超过$10^{-6}$核查也没有增加。少点观测没有这种完全碰撞，因此当前证据不支持把少点失败归因于不可逆的同体素混合；这也不等于已经证明这些特征足以稳定区分异常。结果见`results/train/readout/feature_diagnostics.json`。

新增正常控制严格取自STU自己的真实正常实例：206供体用于206训练背景，201供体用于201开发背景。供体保留原尺寸和实测可见表面，依据原位置附近道路平面保存相对地面的高度，在目标道路上检查支撑和碰撞。它们经过共同的射线竞争、遮挡、206传感器返回概率和强度响应，以及1/3500强度量化；材质与随机流在几何尝试前固定。插入点保留真实正常语义，人工物体身份只作元数据。未将已有异常形状改标为正常，也未引入nuScenes控制样本。201已知重复文件槽在渲染后按原布局恢复。

| 正常控制范围 | 成功观测 | 背景帧 | 有效插入正常点 | 每观测点数中位数 | 点数范围 |
|---|---:|---:|---:|---:|---:|
| 206训练 | 512 | 449 | 82,484 | 60 | 1—1,688 |
| 201开发 | 76 | 76 | 44,660 | 453.5 | 6—2,331 |

206控制由汽车、卡车、其他车辆、行人和摩托车骑乘者构成；201控制由汽车、卡车、行人构成。201的44,660个文件槽对应44,310个独立回波。80个开发请求中，4个尾部帧缺少合法道路支撑或放置距离，实际保留76帧。训练控制的1—4、5—20、21—100、超过100点观测数分别为11、156、138、207；开发分别为0、5、16、55。点距离中位数分别为9.78米和7.95米，当前分布没有被宣称为均匀。生成耗时36.3秒，清单位于`results/data/controls/{train,validation}.json`。

正常控制整帧送入冻结骨干；训练缓存保留全部有效插入点及每帧最多512个原背景正常点，共344,628点，开发缓存保留全部5,947,046个有效正常点。两部分特征提取分别耗时135.09和40.96秒，合计176.05秒；`results/train/controls`磁盘占用约6.2 GiB。扩展开发评价将原主要60帧与全部76个正常控制观测合并，保留同一批7,914个异常点，将正常点增加到11,147,376个；原60帧指标也单独保留，避免把不同评价总体直接混比。

固定的`observations`第13轮在扩展开发集上AP降至39.1806%。其达到95%异常召回的阈值下，正常插入点误报率为41.9772%。这暴露了正常插入观测的误报问题，尚不能单凭该结果确定误报由哪一种合成痕迹造成。随后`contrast`将二分类损失系数质量分为异常0.5、正常插入点0.25、其余正常点0.25，继续让共享残差和两个头共同学习；每轮遍历3,386,576个缓存点，学习率0.001，完成24轮，选择第3轮，耗时242.21秒。

| 已选开发模型与评价范围 | AP | AUROC | FPR95 |
|---|---:|---:|---:|
| `readout`第3轮，扩展开发集 | 30.7645% | 99.3694% | 2.6598% |
| `observations`第13轮，原60帧 | 61.4995% | 99.4104% | 2.4006% |
| `observations`第13轮，扩展开发集 | 39.1806% | 99.3224% | 2.7366% |
| `contrast`第3轮，原60帧 | 41.6674% | 98.9865% | 5.4530% |
| `contrast`第3轮，扩展开发集 | 28.5920% | 98.9139% | 5.8218% |
| `balanced`第5轮，原60帧 | 47.0466% | 99.2078% | 3.6844% |
| `balanced`第5轮，扩展开发集 | 33.9735% | 99.1322% | 4.0227% |
| `competition`第8轮，扩展开发集 | 33.1859% | 99.1263% | 4.2123% |
| `multiview`第7轮，扩展开发集 | 37.8858% | 99.1723% | 3.7546% |
| `coverage`第12轮，扩展开发集 | 37.1202% | 99.0326% | 4.7242% |

`contrast`的正常201平均交并比为36.4635%。在其95%异常召回阈值下，全部正常控制点误报率为6.1443%，其中插入点为21.5204%；对应`observations`的两项值为3.0305%和41.9772%。正常插入点误报下降，同时原异常排序和扩展评价下降，因此当前这套控制加权与共享表示联合更新的用法被否定，不采用`contrast`作为最终模型；这不否定正常控制数据本身的作用。依据为`results/train/observations/normal_controls.json`和`results/train/contrast/result.json`。

`tail`从`observations`第13轮出发，冻结共享表示和语义头，只训练异常头的253个参数。它使用0.0002学习率完成8轮，在原二分类损失外加入权重0.1的排序项，比较异常与206训练正常点中分数最高的0.1%。201最终选择第零轮，所有微调更新被拒绝；保存模型的250个张量与`observations`逐值相同，扩展开发AP仍为39.1806%。该实验耗时93.18秒，没有得到新的有效模型。依据为`results/train/tail/config.json`和`result.json`。

正常插入点只占训练正常点的2.6047%，而`contrast`给它们分配了正常二分类监督系数的一半。`balanced`只将这一系数从总二分类项的0.25降至0.05，即从正常项的50%降至10%；其余正常项相应从0.25升至0.45，异常项仍为0.5。它保留相同数据、初始化、观测加权、0.001学习率与24轮预算，选中第5轮，耗时244.59秒。扩展AP从28.5920%提高到33.9735%，正常201平均交并比为36.5276%，正常插入点误报率为21.2002%；它仍未超过`observations`的扩展AP39.1806%，因此不替代当前开发参考。这里的比例是损失系数总量，不等于实测梯度贡献。依据为`results/train/balanced/config.json`和`result.json`。

`competition`与`balanced`使用相同训练数据、初始化参数、损失权重及24轮预算，只把二分类分数改为未知头输出减去19个正常类别输出的对数指数和，即$A=u-\log\sum_{c=1}^{19}\exp(\ell_c)$；该变化也使二分类损失直接更新正常类别头。201选择第8轮，扩展开发AP为33.1859%、AUROC为99.1263%、FPR95为4.2123%，正常平均交并比为35.0100%。它没有改善`balanced`的三项开发指标，不采用。依据为`results/train/competition/config.json`和`result.json`。

固定`observations`第13轮的尺度诊断不支持将全部252维特征统一归一化作为已确认的修复：201真实正常误报中只有6.16%超过206正常白化范数的99%分位；同类围栏的误报范数偏大，汽车则相反，远距离人行道的误报范数也低于正确正常点。已检出异常的范数中位数19.74高于漏检异常的16.56，直接去掉幅值还可能损失已有异常信息。这些是关联统计，未执行归一化干预，见`results/train/observations/scale_diagnostics.json`。

正常训练覆盖检查进一步定位到人行道：206原正常缓存中，人行道只有21,782个2.5—10米点，占该类28.48%；201对应182,577点、71.41%。在人行道贡献大量误报的同时，现有缓存的近距离覆盖偏少，不能用增加远距离或重复相同实例替代。206与201缓存的查询抽样规则不同，这些比例描述缓存监督范围，不代表原始场景的真实类别频率。全17类等权也不是针对这一缺口的稳妥替代：自行车、摩托车、其他车辆、摩托车骑乘者合计只占普通正常训练点0.5058%，等权后却会取得23.5294%的普通正常损失质量。该计算说明稀少、观测范围有限类别会被大幅放大，尚无性能收益证据；依据为`results/train/balanced/semantic_transfer.json`中的`training_coverage`。

针对正常实例观察变化，扩展后的206正常控制已生成1,280个放置观测，实际使用40个实例的117个实测视角，共121,619个有效插入正常点，清单位于`results/data/views/train.json`。其供体候选池包含44个实例、125个视角，候选数量与实际使用数量分开记录。GPU特征提取耗时327.01秒，`results/train/views/train/auxiliary.json`记录776,979个正常训练缓存点；201开发仍复用原正常控制数据。

使用这批扩展控制的`multiview`已完成24轮训练，耗时246.59秒，201选择第7轮。扩展开发AP为37.8858%、AUROC为99.1723%、FPR95为3.7546%，正常平均交并比为35.6683%，插入正常点误报率为20.2553%。相较`balanced`，三项异常指标均有改善，但AP仍低于`observations`的39.1806%，当前开发参考不变，依据为`results/train/multiview/result.json`。随后`coverage`保留相同数据、24轮预算和其他损失设置，只将普通正常类别的二分类监督总权重按其206缓存点数的平方根分配，仍合计0.45；插入正常与异常项仍分别为0.05、0.5，正常语义交叉熵不变。`coverage`已完成24轮，耗时244.85秒，201选择第12轮；扩展AP为37.1202%、AUROC为99.0326%、FPR95为4.7242%，正常平均交并比为34.7784%，插入正常点误报率为26.7689%。平方根权重未优于相同数据下的逐点权重，故不采用，见`results/train/coverage/config.json`和`result.json`。

此前`readout`第3轮也已用CPU在同一扩展201总体上公平复算，AP为30.7645%、AUROC为99.3694%、FPR95为2.6598%，正常插入点误报率为52.9355%，见`results/train/readout/normal_controls.json`。因此，`observations`的优势具体体现在预定主指标AP，并非三项指标同时优于所有旧模型。当前尚未达到约90%开发AP；本轮仅将201选定的`observations`第13轮送入val19评价。该模型训练时没有使用正常控制，新增控制仅参与其开发检查，不能把控制训练收益归到它身上。训练与开发的对应诊断见`results/train/contrast/train_transfer.json`。使用各模型已选201阈值，`observations`的206插入正常误报为48.2045%，加入对照的`contrast`降至2.0392%；后者在201仍为21.5204%。206合成背景误报从0.3819%升至2.0686%，201合成背景从2.4006%升至5.4530%。这些结果显示当前对照可以被拟合，但跨序列表现及普通背景存在明显折中，不支持仅用“训练不够”解释失败。206背景经过抽样、201使用完整有效点，因此跨分区差异还包含观测分布变化；该诊断没有单独识别具体几何或传感因素的因果作用。本轮已扩大正常实例的实测视角并检验普通正常类别权重，前者改善加入控制的候选，二者均未使候选超过当前开发参考。

## 真实val19完整评价

两次评价分别使用201开发选定的`readout`第3轮与`observations`第13轮，覆盖相同1,960帧、193,792,470个正常点和87,499个异常点，共193,879,969点。所有数值均为百分数。

| 方法 | AP ↑ | AUROC ↑ | FPR95 ↓ |
|---|---:|---:|---:|
| `readout`第3轮 | 3.3406126040 | 98.4293854967 | 6.6640375655 |
| `observations`第13轮，当前已选模型 | 11.5841359734 | 98.6624108602 | 6.8909173819 |
| LIDO表2验证集 | 27.53 | 95.05 | 34.86 |

LIDO数值已核对[论文表2原文](https://arxiv.org/html/2604.23604#S5.T2)。两个已评价模型均在AUROC和FPR95上超过该表数值，AP均未达到目标。当前模型相对`readout`的AP提高8.2435个百分点，AUROC提高0.2330个百分点，FPR95增加0.2269个百分点；它的AP仍低于LIDO 15.9459个百分点。当前模型训练使用合成异常，比较须保留与LIDO不同的监督条件，不能称为三项最优或同等监督条件下的全面超越。

当前输出位于`results/train/observations/val19/metrics.json`，本次推理与官方指标计算耗时463.77秒。全部有效评价点分数保存在`metrics.npy`，原始回波分数及槽位对应关系保存在`metrics_all.npy`、`val_points.npy`及`val.json`；没有追加同一结果的重复复算。此前`readout`输出位于`results/train/readout/val19/metrics.json`。独立程序按精确分数秩、完整同分组及官方ROC顶点规则复算，结果位于`results/train/readout/val19/verification.json`：AP绝对差为$4.44\times10^{-16}$，AUROC绝对差为$1.42\times10^{-14}$，FPR95差为0，验证一致。此次评价没有用于重新选择模型、参数或阈值。

## 已否定的读出设计与证据边界

旧`AJAE-instance-support`采用16,384个206正常参考和受约束最近距离。相同冻结特征下，正常线性诊断在201平均交并比达到34.1602%，而旧正常距离模型为27.4216%；加入相同合成数据后，旧距离模型第2轮的合成开发AP仅2.5264%、AUROC90.7843%、FPR95为40.0806%。同数据的异常线性诊断AP为36.4481%。这些直接证据表明，当前最近正常距离规则没有充分使用已经存在的类别和异常可分信息，因而降为失败对照。旧模型不再是当前部署主线，结果保留在`results/train/stu/result.json`、`results/train/auxiliary/result.json`及两份`linear_probe.json`。

更早版本曾进行val19评价，历史过程不能被抹去，因此当前val19属于重复评价；其数据仍不参与本轮选择。旧类别条件重建版本的完整结果及独立复算保留在`results/train/class/verification.json`。旧混合来源模型未完成的val19评价记录见`results/train/instance/val19/status.json`，没有完整指标，不能充当比较结果。

首轮轻量模块已经完成训练、开发选型和真实val19完整评价，三项同时超过LIDO的目标未完成。后续201开发确认了正常控制误报；共享表示联合训练、异常头微调和控制降权均未替代更好的开发参考，训练与开发的对照诊断进一步显示，当前主要困难包括正常物体观测的跨序列迁移及普通背景误报折中。冻结特征、共享残差和双输出头本身不构成新颖性结论，现有比较也没有分离各模块的独立贡献。模型实现位于`src/normal.py::FeatureEvidence`，训练位于`src/train.py::evidence_training`，正式载入、原始点序推理和官方评价位于`src/evaluate.py`。历史论文草稿不能作为当前方法已验证的依据。
