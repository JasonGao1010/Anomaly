# 冻结感知模型上的正常支持评分

当前实现为 `AJAE-frozen-support`。输入是一帧真实点云，官方 nuScenes 预训练 LitePT-S 提供固定的正常感知特征，新增统计模型判断每个点能否得到某个正常类别的支持。训练只拟合正常统计分布，不更新骨干、原有分类头及其归一化状态，不使用合成异常。此前的可学习盲区回波预测与联合梯度训练不属于本轮方法。

输入与逐点表征

每个 5 厘米体素选取距体素内坐标均值最近的真实点，平局按原始点编号决定，网格坐标减去本帧最小值。这是确定性的单视图输入；原始点身份通过体素对应关系保留。nuScenes 强度除以 255，STU 强度保持原值。全部实际回波进入骨干，标签只选择正常拟合点。

冻结网络分别提取第零编码层的 36 维特征、第二编码层的 144 维特征和解码层的 72 维特征，恢复到原始点后拼成 252 维向量 \(F_i\)。额外观测为真实点云中第八近邻的距离 \(d_{8,i}\)，查询包含自身，因此读取第九个近邻距离；重合点使用 0.001 米下限。最终拟合对象为

\[
 x_i=[F_i,\log\max(d_{8,i},0.001)]\in\mathbb R^{253}.
\]

候选模型使用距离 \(r_i=\|p_i\|\) 的对数作为条件。近邻间距仍属于被评分的观测，不能通过把稀疏程度只当条件而免除对它的检验。特征和近邻间距共同拟合完整协方差，不另将两个相关异常分数任意相加。

正常类别分布与评分

所有坐标的均值、标准差均由选定正常训练点计算。记标准化后的观测为 \(z\)，标准化对数距离为 \(t\)，条件基为 \(b(t)=(1,t,t^2)\)。每类先用带岭惩罚的回归拟合条件均值，再对残差拟合高斯混合：

\[
 p_c(z\mid t)=\sum_{k=1}^{K_c}\pi_{c,k}
 \mathcal N\!\left(z;B_c^{\mathsf T}b(t)+\mu_{c,k},
              e^{v_c(t)}\Sigma_c\right),\qquad
 v_c(t)=4\tanh\!\left(\beta_c^{\mathsf T}b(t)/4\right).
\]

同一类别的各分量共享完整协方差 \(\Sigma_c\)，不同类别分别拟合。标量条件方差在混合拟合后以正常负对数似然及系数正则拟合，保留完整体积惩罚。无距离条件的候选只拟合常数均值，并固定 \(v_c=0\)。这是一套分步统计拟合，不称为端到端梯度训练。

每类代价为 \(E_{i,c}=-\log p_c(z_i\mid t_i)\)，异常分数为

\[
 A_i=\min_{c\in\mathcal C_{\mathrm{fitted}}}E_{i,c}.
\]

一个点只要得到任意已拟合正常类别的充分支持，就有较低分数。各类不乘经验频率先验；统一标准化的雅可比是所有点、所有类别共有的常数，评分中省略。分数是正常分布代价，不是异常概率。

原网络分类头始终输出 nuScenes 的 **16 类**语义；统计模型按 STU 的 **19 类**建立分布。统计代价最小的类别仅用于正常开发诊断，不能当作原分类头已经获得了 STU 19 类分割能力。冻结权重保证该单视图输入下的原分类输出不因统计拟合改变，不等于已经验证 STU 正常语义质量。

训练与开发范围

本轮训练包含 STU206 的全部 449 帧，以及 nuScenes 700 个训练场景中的 1,407 帧。每个源场景在时间约三分之一和三分之二位置取帧，并保留已有可靠补充细标注所在帧。开发包含 STU201 的全部 682 帧和 nuScenes 150 个独立开发场景的 150 帧。

只使用 2.5–50 米内、允许类别集合恰好只有一类的可靠正常点拟合。粗类别集合和忽略点继续作为输入，但不被强行分配细类。训练帧每帧最多保留 4,096 个 STU 点或 2,048 个 nuScenes 点：先覆盖 19 类与四个距离段的组合，再从剩余可靠点均匀补足；距离分界为 10、20、35 米。开发每帧均匀抽取最多 2,048 个可靠细类点。所有抽样固定随机种子，完整帧仍进入骨干。

拟合时每类最多取 24,000 个目标域点；有目标点时补入不超过 \(\max(512,\lfloor n_{\mathrm{target}}/4\rfloor)\) 个源域点，无目标点时源域上限为 30,000。正常开发每类最多取 8,192 个 201 点，仅当该类没有 201 开发点时使用源域开发点。源域只能补充实际存在可靠细标注的类别，不能补出未观测类别。少于两个训练点的类别不拟合，其代价为正无穷；缺失的训练、开发类别分别如实报告。

正常选模与最终评价

预先固定五种候选：距离条件模型分别采用 1、4、8 个混合分量与 0.05 协方差正则；另比较 4 分量、0.01 正则，以及 4 分量、0.05 正则的无距离条件模型。样本较少时实际分量数降至不超过 \(\max(1,\lfloor n_c/32\rfloor)\)。五种候选共享同一次冻结特征提取；各类独立拟合，最多十个进程各用两个数值线程。

候选按正常开发中真实类别负对数似然的逐类等权均值选择，未覆盖类别不冒充已经验证。开发同时记录统计类别正确率与按距离分层的分数分位数。正常似然改善只支持正常分布拟合，不能直接推出异常排序改善。选定后保存完整固定网络与统计参数，再对 STU val19 作一次最终评价；val19 分数不用于选择候选。AP、AUROC、FPR95 由官方有效点及对应逐点分数计算。

正常选型结果

正式拟合已完成。五种候选的正常开发逐类平均负对数似然依次为 596.22、605.46、607.46、1552.02、256.67，因此选定四分量、0.05 正则、无显式距离条件的模型。条件化设计在本次正常开发中未改善拟合，不用于最终评分；这一结果不能概括为距离信息没有用途。

骨干提取了 4,435,245 个正常训练点和 1,588,931 个正常开发点，实际拟合使用其中 338,581 个训练点，选型使用 106,336 个开发点。训练覆盖全部 19 个统计类别；独立开发缺少摩托车、骑摩托车者和其他地面三类。特征提取耗时 866.78 秒，拟合与选型耗时 45.05 秒。最终权重中的骨干和分类头与官方文件逐项相同，真实正常帧重新提取的特征及分数与训练缓存一致。

最终评价

2026 年 9 月 26 日已完成 val19 一次最终评价，共 1,960 个合格帧、193,879,969 个有效点，其中异常点 87,499 个。AP 为 **1.1204796599%**，AUROC 为 **90.5982869004%**，FPR95 为 **57.1063101678%**。推理与官方指标计算共耗时 857.43 秒；正常特征提取、拟合选型、最终评价的记录耗时合计约 29.5 分钟，不含实现开发与初始化。

从完整逐点记录独立按分数排名复算，三项指标及 FPR95 阈值与官方实现完全相同。权威结果为 `results/train/support/val19/metrics.json`，独立复算见同目录 `verification.json`；`metrics.npy` 保存有效评价点分数，`metrics_all.npy` 保存合格帧全部实际回波分数，`val_points.npy` 与 `val.json` 保存原始点编号、标签和帧内区间。

[LIDO 原文表 2](https://arxiv.org/html/2604.23604) 报告的验证集 AP、AUROC、FPR95 分别为 27.53%、95.05%、34.86%。本轮三项均未超过这些公开数值，效果目标未达成；双方训练数据和骨干不同，这里仅比较公开指标。正常开发似然最优没有转化为足够的异常排序能力。此结果否定了将本轮统计评分器作为达标最终模型的判断，未证明性能差距分别由特征、分布模型或源目标差异贡献多少。最终评价结果没有用于重新选择候选或修改参数。

实现依据为 `src/model.py` 中的 `FrozenPerception`、`FrozenSupport`，`src/normal.py` 中的 `FeatureSupport`，以及 `src/train.py` 中的 `SupportScans` 和 `normal_main`；配置、正常选型和训练阶段结果分别见 `results/train/support/config.json`、`development.json`、`result.json`。既有论文初稿描述此前路线，尚未改写为本轮方法。
