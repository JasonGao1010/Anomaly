# 冻结正常感知之后的观测核验

当前主线只训练正常感知模型之后的小模块。官方 nuScenes LitePT-S 骨干与16类分类头全部冻结，输入单帧点云，保留原有正常语义输出，新增模块输出逐点异常分数。训练使用 nuScenes 与 STU206 的可靠正常点；STU201 只提供正常开发与分数校准。val19 仅在正常选型结束后重复评价，不用于选择模型或参数。

## 要解决的具体问题

正常分类器会给每个点选择一个熟悉类别，但类别排名不能表示这个解释是否充分。后接模块同时检查深层表示有没有正常类别支持，以及这种表示能否解释早层细节和实际距离、疏密。前者覆盖表示本身偏离正常的情况，后者针对表示看似正常、观测关系却不相容的情况。

冻结网络为每个原始回波提供早层36维、中层144维和最终72维特征。距离是传感器到点的欧氏距离；疏密用同帧实际回波中第八个邻居的距离表示，两者取对数。分类网络仍看完整帧。后接预测器只读取72维深层特征，早层、中层及两个实测几何量作为被预测的观测，不直接输入预测器。深层特征本身已经看过当前点，因此本方法不属于遮挡当前点的上下文预测。

## 一条联合评分

记训练统计标准化后的深层表示为 $x$，其余180维特征与两个几何量为 $y$。固定的正常训练线性预测先得到 $B[1,x]$；其残差经过正常训练协方差的逆 Cholesky 变换，得到

\[
u=(y-B[1,x])W.
\]

线性预测采用系数0.01的岭惩罚，截距不惩罚；残差协方差加0.01单位阵。该坐标变换在所有候选间相同，开发集不重新拟合。它保留全部维度，减少相关通道重复计数。

小网络将72维深层特征依次变为128维、64维表示，预测 $u$ 的条件混合高斯分布。每个混合分量有182维均值，共享逐维条件尺度；尺度对数用 $3\tanh(t/3)$ 限制。完整负对数似然包含尺度体积项，因此不能无代价扩大预测范围。比较一个或四个分量，以正常开发似然决定是否需要多个正常形态。

深层正常支持由可靠19类标签的类均值和全局共享类内协方差建立，各已出现类别具有相同先验。共享协方差避免将不同类别的分布体积差异当成最宽松类别选择的依据。最终原始分数为

\[
s=-\log p(x)-\log p(u\mid x).
\]

这是一个联合正常密度的分解，没有另行学习两项之间的融合权重。标准化与残差白化的雅可比为所有点共有的常数，排名时省略。分数表示正常支持不足，不是经过异常监督得到的异常概率。

64维表示上的19类分类头只用于辅助训练，比较其损失权重0与0.1。它不替代官方16类分类头，也不直接进入异常分数。只有它改善正常观测预测时，才将对应训练设置选为正式模型。不能据此宣称已经实现19个类别条件观测预测的逐类竞争。

182维条件分布中只有两个几何量；完整似然没有使四组信息等权。因此，本方法是联合核验多层特征与几何，不能宣称距离和疏密已经主导评分。

## 正常训练、选择与校准

复用已经逐点核对过的官方冻结特征，不复用历史训练权重。缓存对应STU206全部449帧与nuScenes 700个训练场景的1,407帧，共4,435,245个可靠正常点。开发缓存包含STU201全部682帧与nuScenes 150个独立开发场景的150帧，共1,588,931点。实际有效长度由缓存元数据记录，预分配尾部不参与计算。

骨干输入为完整帧；监督只选2.5–50米内、允许语义集合恰好为一类的真实正常点。训练帧预选最多4,096个目标点或2,048个源点，兼顾类别与距离段；开发帧均匀抽取最多2,048个可靠点。统计初始化使用已有338,581个按类别保留的正常训练点，小网络每轮覆盖全部443万缓存点。训练目标的总权重按目标域80%、源域20%分配，每个域内使用类别频数平方根倒数保护少量类别。

四种候选为混合分量数1或4、语义辅助权重0或0.1。训练至多40轮，学习率从0.001按余弦下降，批量16,384，AdamW权重衰减0.0001。连续六轮正常开发类平均似然没有改善、且至少训练十轮时提前结束；所有候选共用样本与逐轮随机排列。

STU201按连续64帧分块。奇数块用于正常模型选择，偶数块用于拟合分数校准；两者没有点或帧交集。选择时仅对至少128个开发点的类别计算类平均似然，201未出现的类别才由nuScenes正常开发补充。少量或缺失类别继续报告，不冒充充分验证。

分数校准只作用于最终联合分数，按官方冻结16类预测分组。每组至少2,048个正常参考点，否则回退全局分布。分位点转换为单调尾部尺度，区间外线性延伸，避免少样本经验尾概率产生拒绝分数上限。距离、疏密不作为校准分组条件。是否启用校准，由201奇数块上各充分观测预测类在1%参考阈值下的误报偏差决定；源域点不参与这一裁决。正常校准改善不等于异常排序改善。

## 实现与当前证据

权威实现是 `src/normal.py` 的 `CrossEvidence` 与 `ScoreCalibration`，`src/model.py` 的 `FrozenPerception`、`FrozenSupport`，以及 `src/train.py --normal`。当前运行产物写入 `results/train/cross/`。正常训练与选择及完整val19重复评价均已完成；当前方法未达到异常效果目标。初始化、选型与校准分别记录在 `initialization.json`、`development.json`、`calibration.json`；逐轮记录为 `training.jsonl`。四种候选的最优正常开发类平均负对数似然分别为325.85691、327.62220、323.59172、326.14052，选中四分量、无语义辅助监督的第29轮。全部正常训练与选择耗时220.96秒，训练期显存峰值约6.36 GiB。最终异常推理跳过辅助分类头的计算，原官方语义头照常输出。

选中模型的距离对数平均绝对误差为0.15920，间距对数误差为0.22390。校准参考包含668,912个201偶数块正常点；启用判断仅使用644,238个201奇数块正常点。充分观测预测类在1%参考阈值下的平均绝对误报偏差从0.01805降至0.00765，因此启用校准。原始帧重新提取的4,096个正常点特征与缓存、对应最终分数均逐元素一致；240个官方感知状态张量保持相同。这些正常开发结果不表示异常三项指标已经达标。

已完成独立SciPy混合密度、共享协方差与缺类先验、输入隔离、逆变换、校准尾部外推和保存重载的公式检查。真实正常缓存与原扫描的点编号、单义标签及几何量对应正确；每轮随机排列覆盖全部有效训练点。原正常网络冻结保证同一预处理下的原语义输出不受后接训练改变，尚不代表已验证STU正常语义达到最佳水平。

此前 `results/train/support/` 是已完成但未达标的直接正常特征混合模型：val19的AP为1.12048%、AUROC为90.59829%、FPR95为57.10631%，完整逐点复算与官方结果一致。该结果保留为失败证据，不作为当前模型效果。[LIDO原文表2](https://arxiv.org/html/2604.23604)对应公开数值为27.53%、95.05%、34.86%；双方骨干和训练来源不同。

当前新模块已完成1,960帧、193,879,969个有效点的val19重复评价：AP为0.3401870897%、AUROC为86.4117726509%、FPR95为71.4511023055%，推理与官方汇总耗时686.56秒。三项均未超过LIDO，也均差于此前直接特征分布基线。正常似然和正常误报校准的改善没有转化为本次异常排序改善，当前实现不能作为达标最终模型。权威结果见 `results/train/cross/val19/metrics.json`，逐点记录保持保存。既有论文初稿描述此前路线，尚未改写为本轮方法，不能用于当前结果主张。

## 与已有机制的关系

[Reverse Distillation](https://arxiv.org/pdf/2201.10703)已采用固定教师、高层瓶颈和多层特征恢复来定位异常；[CFLOW-AD](https://arxiv.org/pdf/2107.12571)已在预训练特征后用条件概率模型学习正常分布。当前实现的具体安排是联合建模深层支持、跨层观测与实测几何，并保持原感知网络固定。概率链式分解本身不是新的理论贡献。进一步的方法主张需要证明两类证据的互补作用，而不能将高斯模型、混合预测和校准分别列为创新。
