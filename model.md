# 冻结点云感知后的共享特征证据

当前主线为`AJAE-feature-evidence`：在冻结的成熟点云感知模型之后，训练一个轻量模块，同时输出正常语义类别和逐点异常分数。模块训练已完成6轮，依据STU201开发结果选择第3轮，权重位于`results/train/readout/frozen.pt`。真实val19正在进行最终重复评价，目前没有完整结果。开发集表现不代表真实val19表现，也尚未建立三项指标超过LIDO的结论。

## 输入、特征与输出

输入为单帧点云。官方nuScenes预训练LitePT-S骨干及原16类语义头保持冻结，提供早层36维、中层144维、深层72维特征，拼成252维向量并恢复到原始点顺序。nuScenes只提供官方预训练权重，后接模块的训练和开发样本均来自STU。当前输出的正常类别来自新模块的19类语义头；原16类头保持原权重。

记冻结特征为$f$，STU206真实正常点估计的中心为$\mu$，固定白化矩阵为$W$。共享特征和两个输出为

\[
x=(f-\mu)W,\qquad z=x+R(x),\qquad
\ell=zW_s^\top+b_s,\qquad A=zw_a+b_a.
\]

$R$为252→128→252的两层全连接网络，中间使用GELU，最后一层零初始化；因此训练起点保留初始化线性模型的输出。19维$\ell$用于正常语义分类，标量$A$直接作为异常分数，越大表示越倾向异常。两项监督共同更新同一共享残差。中心和白化矩阵不学习，参数与计算均使用FP32。当前选定模型包含69,952个可训练参数。

正常语义头由`results/train/stu/linear_probe.json`中的206正常类别线性拟合初始化；异常头由`results/train/auxiliary/linear_probe.json`中使用206真实正常和合成样本的二分类线性拟合初始化。二者均使用固定0.01正则的最小二乘拟合，转换到同一白化坐标；异常线性输出再作正斜率仿射变换作为初始原始分数。初始化后的201指标已核对能复现独立拟合结果。当前模块沿用206统计量，不继承旧最近邻模型学到的距离变换。

当前主线没有正常场重建、距离预测、疏密预测或最近邻检索。异常头直接接受合成异常监督；原始分数没有经过正常分位数校准，不能解释成已校准的真实异常概率。共享体素中的点可能拥有相同冻结特征，这一上游信息限制仍然存在。

## 数据与监督范围

真实正常训练数据为STU206全部449帧已有缓存中的1,839,104个点。合成数据来自`/home/jasongao/Study/AJAE-v3/samples`，训练只读取其中源序列206的`train`分区，开发只读取源序列201的`validation`分区。这些已保存的几何障碍观测通过原扫描及差分文件恢复；原始点槽位、源扫描、位姿、物体身份和恢复后的异常数量均保留核对。当前没有使用NDP生成数据。

在每个分区内，按每帧有效异常点数1–4、5–20、21–100、大于100四档固定随机抽样，不放回选取，不再按距离或形状平衡。206选择1,920帧，四档各480帧，覆盖237个物体场景；201选择80帧，四档各20帧，覆盖57个物体场景。抽样范围和监督点数如下。

| 用途 | 帧范围 | 正常点 | 异常点 |
|---|---:|---:|---:|
| 206真实正常训练缓存 | 449帧 | 1,839,104 | 0 |
| 206合成训练缓存 | 1,920帧 | 983,040 | 219,804 |
| 201真实正常开发缓存 | 奇数64帧区块 | 644,238 | 0 |
| 201合成开发全部有效点 | 80帧 | 6,722,428 | 7,959 |
| 201合成官方式指标范围 | 其中60帧 | 5,200,330 | 7,914 |

合成训练每帧保留全部有效异常点及最多512个具有明确正常细类标签的点。训练缓存总计3,041,948点；“每轮遍历全量”指这些已选缓存点，不是合成库全部帧或原始扫描全部回波。201合成开发保留全部有效检测点，包括没有细类标签的正常点；主要三项指标按STU每帧至少5个有效异常点的规则使用60帧，其余1–4点帧与全部80帧结果另作诊断。

正常细类沿STU19类标注转换，异常点没有正常类别标签。异常二分类损失使用正常和合成异常标签，正常类别损失只使用有明确细类的正常点。201数据参与开发选型，val19不参与参数学习、模型选择或阈值调整。当前方法使用合成异常训练和开发，报告必须保留这一监督条件。

数据依据为`results/train/auxiliary/auxiliary_views.json`、`auxiliary.json`、`synthetic_development.json`及`synthetic_development/features.json`；当前训练范围与初始化来源记录在`results/train/readout/config.json`。

## 训练与选择

每轮将3,041,948个训练缓存点随机排列并各使用一次，批量8,192。损失为正常语义交叉熵与异常二分类交叉熵之和：语义项使每个训练中出现的正常类别具有相等总权重，二分类项使正常、异常分别具有一半总权重。两个输出头及共享残差共同训练，骨干和统计量保持固定。

优化器为AdamW，初始学习率0.001、权重衰减0.0001，按6轮余弦下降。6轮均已完成，模块训练及其开发检查总耗时45.71秒；该时间复用了已经提取的冻结特征，不包括此前特征提取和线性初始化拟合。206合成训练特征提取记录为739.43秒，见`results/train/auxiliary/auxiliary.json`。

模型选择首先要求201真实正常开发平均交并比不低于初始化线性模型的34.1602%，再按201合成开发AP、AUROC、负FPR95依次比较。初始化线性模型本身也可被选择；本次第3轮优于初始化并被选中，保留了学到的残差。真实val19仅对这个已选模型进行最终重复评价。

## 已得到的结果

下表区分已选第3轮结果与初始化线性模型。正常平均交并比按真值中出现的类别平均；训练点、正常开发点和合成开发点分别报告，不混为同一总体。

| 正常语义范围 | 平均交并比 | 点准确率 |
|---|---:|---:|
| 206真实正常训练缓存 | 71.0482% | 90.3405% |
| 201真实正常开发缓存 | 37.4239% | 75.5954% |
| 同一201范围的初始化线性模型 | 34.1602% | 71.7542% |

201开发若对真值或预测中出现的所有类别取平均交并比，则已选模型为27.0283%。206没有训练实例的停车等类别仍计入201真值出现类的指标；不能通过删除困难类别提高报告数值。以上点级语义指标也不能与STU论文的全景质量指标直接横比。正常结果依据为`results/train/readout/normal206.json`和`development.json`。

| 201合成异常范围 | AP | AUROC | FPR95 |
|---|---:|---:|---:|
| 官方式60帧，初始化线性模型 | 36.4481% | 98.9389% | 5.2637% |
| 官方式60帧，已选第3轮 | 59.0609% | 99.4725% | 2.3000% |
| 全部80个含异常帧，已选第3轮 | 53.5579% | 99.4286% | 2.5096% |

这些结果支持：在当前选定合成开发分布上，共享残差及联合监督比初始化线性模型获得更好的正常分类和异常排序。它们尚不能单独确定共享残差的独立贡献、方法新颖性或真实未知障碍泛化能力。逐轮结果位于`results/train/readout/training.jsonl`，已选结果位于`result.json`；真实val19评价产物将位于`results/train/readout/val19/`，运行结束前不报告完整指标。

## 已否定的读出设计与证据边界

旧`AJAE-instance-support`采用16,384个206正常参考和受约束最近距离。相同冻结特征下，正常线性诊断在201平均交并比达到34.1602%，而旧正常距离模型为27.4216%；加入相同合成数据后，旧距离模型第2轮的合成开发AP仅2.5264%、AUROC90.7843%、FPR95为40.0806%。同数据的异常线性诊断AP为36.4481%。这些直接证据表明，当前最近正常距离规则没有充分使用已经存在的类别和异常可分信息，因而降为失败对照。旧模型不再是当前部署主线，结果保留在`results/train/stu/result.json`、`results/train/auxiliary/result.json`及两份`linear_probe.json`。

更早版本曾进行val19评价，历史过程不能被抹去，因此当前val19属于重复评价；其数据仍不参与本轮选择。旧类别条件重建版本的完整结果及独立复算保留在`results/train/class/verification.json`。旧混合来源模型未完成的val19评价记录见`results/train/instance/val19/status.json`，没有完整指标，不能充当比较结果。

当前实现与证据支持一个已完成训练、已在开发集选定的轻量监督模块。冻结特征、共享残差和双输出头本身不构成新颖性结论；真实异常收益及论文方法贡献还需要相应结果支撑。模型实现位于`src/normal.py::FeatureEvidence`，训练位于`src/train.py::evidence_training`，正式载入、原始点序推理和官方评价位于`src/evaluate.py`。历史论文草稿不能作为当前方法已验证的依据。
