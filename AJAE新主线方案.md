# AJAE 新主线方案

本文是 AJAE schema 32 的科学权威说明。schema 32 保留 schema 31 的五帧联合稠密点云主线，但版本化修正单帧对照、训练预算、R02 证据范围和正式确认总体。schema 31 的审查前状态固定在 Git 标签 `schema31-preR02-audit`；schema 30 的中心帧时序路线固定在 `schema30-history-baseline`。历史结果不能被重标为 schema 32 证据。

当前权威节点是 R02。24 个 D_select 片段尚未生成，R02 资格证据不存在；另 24 个同生成机制 D_confirm 片段只有源帧计划、种子和重试规则，`confirm.json` 仍为空且在 G2 前禁止渲染。当前没有生成 schema 32 训练 bank、没有启动训练、也没有 schema 32 性能结果。R02 数值阈值仍是占位值；生成入口会在阈值内容、结果盲冻结标志和阈值身份任一缺失时拒绝执行。

# 1. 研究问题与主张边界

AJAE 研究以下问题：能否先构造适合连续激光雷达观测的异常代理，再把五次连续扫描对齐并合并成一个联合稠密点云，从而弥补单次扫描对未知物体采样稀疏、形状残缺的问题，并提升 STU 官方点级分布外检测指标？

项目只保留三项可证伪主张。

1. 窗口级程序化异常代理能够提供可学习、且不依赖低层标签捷径的监督。
2. 在相同五帧输入、参数、初始化、训练样本、损失和评价规则下，跨扫描联合空间建模优于按扫描身份隔离的五帧建模。
3. 方法完全冻结后，合成监督在 19 条真实异常验证序列上的表现能够支持其向真实未知障碍物迁移。

本项目不主张运动未知物体检测、显式运动理解、对象跟踪、未来帧帮助当前帧，或以中心帧为目标的异常补全。程序化异常代理、联合密度指标和开发集结果也不能单独证明真实分布外泛化。

# 2. 五条不可变科学合同

## 2.1 五帧窗口是基本观测单位

合法窗口由起点 $s$ 唯一标识：

$$
\mathcal W_s=\{X_s,X_{s+1},X_{s+2},X_{s+3},X_{s+4}\}.
$$

窗口必须包含五个真实且连续的源帧。禁止补零、重复帧或复制扫描。训练世界、模型输入、损失累计和窗口级描述量都以完整五帧窗口为单位。

## 2.2 没有中心帧或主帧

五个成员接受同等监督并具有相同的输出资格。`window_start` 和 `frame_ids` 只标识数据来源；`source_frame`、`source_slot` 与 `source_ray` 只用于恢复点身份和融合。任何成员都不能因数组位置而获得额外预测权重。

## 2.3 主模型不接收帧位置或时间信息

模型不能读取帧编号、窗口位置、相对时间、绝对时间、时间嵌入或其可逆编码。扫描分组身份只允许在 B2 中阻止跨扫描运算，不能进入可学习特征。

## 2.4 完整模型执行联合空间计算

五帧先经位姿对齐，再作为一个点集共同体素化、共同建立空间邻域，并在解码时共同搜索邻点。B3 的运算不能按扫描身份分支。B2 与 B3 使用同一个模型实现，两者唯一主要差异是 B2 的体素、邻域和上采样键额外包含扫描分组。

## 2.5 五帧全部点输出并按原始身份融合

模型为窗口中每个可见回波输出一个异常 logit。一个原始点可能出现在多个重叠窗口中，其序列分数为所有合法出现位置的概率等权平均：

$$
S_p=\frac{1}{|\Omega(p)|}\sum_{\mathcal W\in\Omega(p)}\sigma\!\left(z_p^{(\mathcal W)}\right).
$$

融合键是带序列命名空间的原始 `frame-ray` 身份。不得只输出某个固定局部位置，不得以 logit 平均替代概率平均，也不得让任何窗口成员只提供上下文而不接受评价。

# 3. 对称窗口坐标系

设五个位姿 $(R_j,t_j)$ 都表示从各自传感器坐标到同一世界坐标的刚体变换。窗口平移原点取五个位移的算术平均：

$$
\bar t=\frac{1}{5}\sum_{j=0}^{4}t_j.
$$

令 $M=\sum_{j=0}^{4}R_j=U\Sigma V^\top$，旋转取 chordal mean 在 $SO(3)$ 上的投影：

$$
\bar R=U\operatorname{diag}\!\left(1,1,\det(UV^\top)\right)V^\top.
$$

源帧点 $x_j$ 的窗口坐标是：

$$
\widetilde x_j=\bar R^\top(R_jx_j+t_j-\bar t).
$$

该参考系由五个位姿共同决定。实现资格必须验证输入扫描顺序置换不改变参考系、全局刚体变换保持相对坐标、重复构造确定、旋转正交且行列式为 $+1$。

# 4. 窗口级反事实异常代理

## 4.1 训练样本

一个训练样本由五帧窗口身份和一次固定的 `WorldSpec` 构成。渲染任何一帧前，程序必须固定五个源帧、正常对照、异常代理、世界位置、朝向、尺度、材质和全部随机种子，然后用同一个 OS1-128 首次回波渲染器生成五次观测。一个实体不能跨越整条 449 帧序列后再产生大量高度重叠训练样本。

schema 7 的几何生成、支撑面估计、放置、碰撞检查、射线身份、首次回波、遮挡、强度与空射线机制继续有效。窗口化改造不得通过补点、复制扫描、重复一帧或补造底部回波来增加密度。

## 4.2 联合描述量

对实体在五帧中的实际可见回波集合 $P_0,\ldots,P_4$，定义：

$$
N_{\mathrm{pts}}^{(5)}=\sum_{j=0}^{4}|P_j|,
$$

$$
N_{\mathrm{vox}}^{(5)}=\left|\operatorname{Voxel}\!\left(\bigcup_{j=0}^{4}P_j\right)\right|,
$$

$$
G_{\mathrm{dens}}=\frac{N_{\mathrm{vox}}^{(5)}}{\max_j N_{\mathrm{vox}}^{(j)}},
\qquad
R_{\mathrm{dup}}=1-\frac{N_{\mathrm{vox}}^{(5)}}{N_{\mathrm{pts}}^{(5)}}.
$$

同时记录距离、遮挡、支撑面、可见扫描数、最低可见回波离地高度、强度和 beam 分布。可见扫描数只能描述完整窗口，不能要求指定位置必须可见或含有异常点。

## 4.3 正常对照与异常代理匹配

正常对照和异常代理必须共用渲染路径，并至少按联合回波数、联合空间体素数、稠密增益、距离、遮挡、放置表面和可见离地高度进行窗口级匹配。R02 依次执行三类低容量审查：完整且与扫描顺序无关的物理观测统计；实体级冻结 STU 特征与证据的均值、标准差、可靠度、强度与简单几何摘要；渲染正常对照与其模板绑定的 train/206 原始正常实例之间的多视角真实性。三类审查均按 `WorldSpec` 分组划分，并同时使用逻辑回归和最大深度为 3 的决策树。阈值只排除接近直接、接近饱和的可分性，但必须在生成 D_select 前冻结；任一审查达到阈值即使 R02 失败。

# 5. 模型

## 5.1 输入与输出

每个可见回波的输入分成三类。可学习内容特征是 128 维冻结 STU 特征、19 维正常类别证据、分配可靠度、无对象可靠度和强度。B1 的空间位置编码、相对位移、体素化、邻域和上采样只读取该帧原生传感器坐标；B2/B3 对应运算读取对称窗口坐标 $\widetilde x_p$。点身份和扫描分组只用于输出恢复与 B2 隔离。主模型输出每个输入回波的一个异常 logit。

空间坐标不得编码扫描位置或时间。帧位置、相对时间、绝对时间和时间嵌入全部禁止进入模型。扫描分组只控制 B2 的隔离运算。

## 5.2 分组规则

B3 的体素键为 $(k_x,k_y,k_z)$；B2 的体素键为 $(g,k_x,k_y,k_z)$，其中 $g$ 是扫描分组。邻域查询与三近邻上采样使用相同规则：B3 在完整联合点集搜索，B2 只在同组内搜索。

模型只保留一个权威实现 `JointWindowPointTransformer`。B2 与 B3 不能复制成两套网络，也不能保留 schema 30 的时间分支、时间差偏置、跨帧门或同时间解码作为影子路径。

## 5.3 显式密度

体素特征必须保留真实回波数量：

$$
h_v=\operatorname{MLP}\!\left[\operatorname{mean}(h_i),\operatorname{max}(h_i),\log(1+n_v)\right].
$$

空间邻域聚合还应包含 $\log(1+|\mathcal N(i)|)$，使固定邻居上限不会完全抹去局部密度。计数来自真实输入回波，不能由帧位置编码代替。

## 5.4 置换等变性

对五帧顺序的任意置换 $\pi$，恢复原始点顺序后必须满足：

$$
F_\theta(\pi\mathcal W)=\pi F_\theta(\mathcal W).
$$

该性质既要求网络不读取顺序，也要求体素化、邻域、解码和点身份恢复保持一致。

# 6. 最小实验矩阵

| 条件 | 输入与运算 | 科学问题 |
|---|---|---|
| B0 | 冻结 STU 官方单帧 MaxLogit | 不使用 AJAE 时表现如何 |
| B1 | 同一五帧训练世界拆成五次原生传感器坐标单帧前向；每个 frame-ray 只算一次并作恒等复用 | 新异常代理监督本身是否可学习 |
| B2 | 五帧使用对称窗口坐标共同进入同一网络，但体素、邻域和解码按扫描身份隔离 | 对齐坐标与五帧共同曝光是否产生改变 |
| B3 | 五帧对齐后共同体素化、建立邻域和解码 | 联合稠密化是否提供额外价值 |

B1、B2 和 B3 使用同一冻结训练 bank。bank 同时保存原生传感器坐标和对称窗口坐标；B1 只能读取前者，B2/B3 只能读取后者。B2 与 B3 使用相同点、参数量、初始化、样本顺序、损失、优化器和窗口概率融合规则。因此，B3−B2 才识别删除扫描边界后的联合稠密化作用；B3−B1 是完整方法相对原生单帧代理模型的性能差异，不能写成纯稠密化归因。

如需单独研究窗口融合，只能在主 Gate 之外比较 `canonical-occurrence` 与 `all-occurrence-mean`，且不能改变主模型定义。

# 7. 训练

## 7.1 固定窗口训练 bank

结果盲生成一份 `window_train_bank`。每个条目同时保存原生传感器坐标与对称窗口坐标，并包含五个源帧身份、完整 `WorldSpec`、五帧实际渲染点、稳定点身份、标签、窗口级密度描述量、渲染器身份和 STU 身份。每个渲染源帧还用实际 `xyzi`、位姿、可见槽位和打包标签的内容摘要绑定，STU 输出须另与实际坐标、特征和槽位输入精确匹配。

训练 bank 的协议身份只摘要决定 bank 内容的训练源、窗口、标签、渲染、STU 和距离有效域规则。R02 开发总体另用决定 24 个生成器内片段内容的开发源、窗口、标签、渲染和片段设计规则摘要。S01 的 6 个环面有符号距离场留出片段使用独立的冻结计划；冻结计划只确定未来输入，不授权提前渲染或观察。R05 只冻结优化配方时，训练 bank 与 24 个开发片段不会被误判为内容改变；模型检查点和正式评价仍绑定包含训练与评价规则的完整协议科学身份。

生成预计超过 1 GiB 的 bank 或缓存前，必须重新从 Windows 查询 E 盘剩余空间，并按 `AGENTS.md` 计算峰值占用和安全余量。

## 7.2 损失与更新单位

一个微批次是一个完整 `WindowWorld`。B1 分别前向五帧后汇总全部点，B2/B3一次前向完整窗口。这里的有效批次是一次优化器更新实际累计的全部窗口；程序先跨这些窗口汇总正负样本的损失和计数，再计算空类别安全平衡二元交叉熵：

$$
\mathcal L=
\frac{1}{2}\frac{\sum_{i:y_i=1}\ell_i}{N_+}
+\frac{1}{2}\frac{\sum_{i:y_i=0}\ell_i}{N_-}.
$$

若有效批次只有一个类别，只计算该类的均值。tiny 与 pilot 只使用优化器更新次数作为预算，不携带 epoch 字段；正式训练只使用 epoch 预算。每个记录同时保存优化器更新数、已见窗口数和等价 bank 遍历数。一个 epoch 是完整遍历一次冻结 bank；445 个窗口不能整除梯度累计数时，epoch 尾部的部分有效批次允许执行，并计为一次优化器更新。

## 7.3 训练模式与冻结顺序

训练入口只接受彼此互斥的 `tiny_overfit`、`pilot` 和 `formal` 模式。微型过拟合固定使用训练 bank 的前 8 个窗口、种子 2001、学习率 $10^{-4}$、梯度累计 1、恒定学习率、权重衰减 0，并以最多 500 次优化器更新为唯一预算；它不参与配方选择。正式种子固定为 0、1、2；先导种子与正式种子隔离，分别为 1001、1002。

8 个固定窗口的微型过拟合要求每个条件在最多 500 次更新内达到训练 AP 不低于 99% 或损失低于 0.02。128 个固定窗口用于结果盲筛查学习率 $\{3\times10^{-5},10^{-4},3\times10^{-4}\}$ 与梯度累计 $\{1,2\}$：50 次更新要求六个候选都形成有限、可审计结果，排除完全概率饱和后至少保留两组；200 次更新按开发集逐片段融合 AP 宏平均保留严格领先的两组；600 次更新改用种子 1002 选出一组，数值完全相同时按协议候选顺序裁决。随后用同一严格比较与协议顺序平局规则依次选择恒定学习率或 5% 预热余弦退火，再选择权重衰减 $\{0,10^{-5},10^{-4}\}$。只有 R05 冻结最终配方后，正式训练才允许开始。三个正式种子都进入 Gate 统计；真实数据上的最终方法结果盲固定为 B3 的种子 0，B1 参照固定为种子 0，不能根据正式结果事后挑选条件或种子。

# 8. 评价

真实序列评价遍历全部合法窗口起点。B2/B3为窗口五帧全部可见回波输出概率，并按 `(partition, sequence_id, source_frame, source_ray)` 汇总。至少出现在一个合法窗口中的点构成共同评价域；边界点的出现次数可以为 1 至 4，内部点最多为 5。

合成开发评价不能把来自不同 `WorldSpec` 的同名 frame-ray 合并。需要重叠窗口融合时，开发数据必须组织成一个固定 `DevelopmentClipWorld`：同一 `world_identity`、实体和随机规格先冻结，再渲染足以形成重叠五帧窗口的一段连续扫描。冻结定义必须为每个源帧保存实际渲染几何、位姿、可见槽位和语义的内容摘要，评价时逐帧核对。合成融合键额外包含 `world_identity`。不同 clip 或独立 `WindowWorld` 之间禁止融合。

合成数据分成两个独立的 24 片段总体。D_select 用于 R02、pilot 和检查点选择；D_confirm 与 D_select 的源帧、世界身份和种子完全分离，在所有 B1/B2/B3 正式检查点于 F01 选定以前只允许冻结计划。G2 才一次性生成并打开 D_confirm，G2/G3 的正式统计只读 D_confirm。另有 6 个环面有符号距离场片段只用于 S01 机制外移位检查，不能替代同生成机制 D_confirm。

正式点级指标沿用 STU 官方 AP、AUROC 和 FPR95 语义，距离域为闭区间 $[2.5,50.0]$ 米。除总体指标外，还必须按窗口出现次数 1 至 5 分层报告。评价器数值等价、密封验证/测试访问和点身份恢复的既有实现证据继续有效，但所有中心 $q=0$ 共同域、按位置校准和 B3/B4 分裂均失效。

检查点记录必须绑定 D_select 的实际 `dev.json`，并由逐片段完整标签与融合分数重算 AP。正式 G2/G3 评价必须绑定独立的 `confirm.json` 及其逐片段原始数组，不能把训练过程中的 D_select 指标复用为确认结果。所有三个条件和三个正式种子的检查点都在 F01 使用 D_select 选定，之后禁止重新训练或改选检查点。

S01 只有一个正式生成入口：它在验证已通过且与协议绑定的 G3 裁决后，按冻结计划位置 48--53 重建 `WorldSpec`、种子和重试规则，调用环面留出渲染器，并保存完整片段清单与逐窗口描述量。M01 的 B0、B1、B3 点阈值只能从被选中的正式原始证据派生，不能由调用者填入。V01 的序列记录必须由完整覆盖域上的逐点身份、坐标、原始语义和分数数组重新计算 AP、AUROC、FPR95 与正常安全；只有 M01 和通过的 V01 裁决同时绑定后，T01 才能解封。这些结构只是防止伪造或错接证据，不能替代任何尚未执行的科学实验。

# 9. 数据角色

- `train/206`：传感器渲染参数校准、异常代理生成、窗口训练 bank 和全部可学习参数的唯一来源。
- `train/201`：24 个 D_select、24 个密封 D_confirm 和 6 个 S01 留出片段的来源；三组源帧互不相交。D_select 用于资格与选择，D_confirm 只用于正式确认，S01 只解释机制外移位；该序列不产生模型梯度。
- 19 条公开真实异常验证序列：方法完全冻结后仅打开一次，用于真实分布外迁移确认。
- 51 条隐藏测试序列：最终一次测试；不参与设计、调参或停止条件修改。

验证集和测试集继续由密封访问机制保护。开发证据、先导实验和合成代理结果不能写成真实测试结论。

# 10. 决策链与停止条件

当前决策链为：

```text
R00 科学合同
  → R01 对称五帧窗口语义
  → R02 窗口级异常代理
  → R03 联合稠密模型资格
  → R04 训练体系资格
  → R05 训练配方冻结
  → F01 全部正式检查点训练与 D_select 选择
  → G2 异常代理监督有效性
  → G3 联合稠密化增益
  → S01 合成移位与机制
  → M01 点级方法冻结
  → V01 真实异常确认
  → T01 隐藏测试
```

G2 比较 B1 与 B0；G3 同时比较 B3 与 B1、B3 与 B2。G2/G3 的数值阈值、统计单位和安全裁决，以及 V01 的迁移条件，都必须在 R05 观察任何正式结果前冻结。这样，正式检查点到真实异常评价始终绑定同一科学身份；schema 30 的阈值不能静默继承。

以下情况停止对应路线：异常代理标签可由低层生成捷径恢复；B1 无法通过微型过拟合；B3 与 B2 的差异不再只由扫描分组边界构成；正式结果不满足 R05 预注册的 G2 或 G3 条件；冻结方法在 V01 不支持向真实异常迁移。停止结论必须限定到被否定的主张，不能用机械测试通过替代科学裁决。

# 11. 历史证据边界

继续有效的证据包括 schema 7 几何与覆盖、支撑面和放置、ray mapping、首次回波渲染、遮挡与强度、正常对照和异常代理共用渲染器、STU 官方权重与点证据恢复、E61 原始正常安全集合身份、点身份、官方评价器数值等价及密封数据访问。E61 的原始 frame/ray 掩码可以复用，但安全分数必须在 schema 32 的窗口覆盖域重算。

E26 的世界构造原语、E37 的身份一致性、E55 的 150 维非时间输入内容、E70 的空类别安全损失原语、E71 的概率平均原语和 E72 的 B0 定义可以作为实现材料复用，但必须在新窗口语义下重新资格。旧 B0 数值也必须在 schema 32 的评价域重算。

E45B-v2、E48 与 E76-C1 只保留为旧中心窗口分布的历史证据。它们不能证明 schema 32 的窗口级匹配、捷径安全或训练分布有效。

E56 的中心坐标、E57–E60 的中心开发世界、E63 的 $q=0$ 共同域、E64–E69 的时间分支资格、E73–E76、AJAE-F0-X/AJAE-F1-X、旧 B1–B5、`fast-v5` 以及所有中心帧 Gate 2/3 结果均不进入 schema 32 正式统计。schema 31 的审查前实现同样不能作为 schema 32 正式结果。

# 12. 当前完成状态

当前权威节点为 R02。R00 已完成科学合同统一与 schema 30 正式入口拒绝；R01 已完成五姿态对称参考系、全点身份恢复、扫描顺序置换、统一刚体变换、重复构造和旋转正交性的机械核验。两项完成状态分别证明合同一致和窗口实现符合定义，不构成异常代理有效性或模型性能证据。

R02 已具备阈值生成前硬锁、物理观测捷径、冻结 STU 捷径和正常对照真实性审查实现。当前 `dev.json` 的 D_select 与 `confirm.json` 的 D_confirm 均为 0 个片段；D_confirm 和 S01 只冻结计划，没有渲染或查看。R02 数值阈值仍为占位，因此 D_select 生成会被拒绝，R02 尚未通过。

联合稠密模型、双坐标训练 bank、互斥预算、B1 原生单帧推理、D_select 检查点选择与 D_confirm 正式评价路径已经完成机械实现。小型测试只能证明这些路径符合定义，不提供性能证据。当前没有生成 schema 32 训练 bank，也没有启动训练；R05 配方、F01 正式检查点、G2/G3 结果、S01 片段、M01 方法冻结、真实异常验证结果和隐藏测试结果均不存在。

# 13. 一句话定义

AJAE 利用同一反事实世界生成的高质量五帧异常代理，把五次连续扫描对齐为一个联合稠密点云，并为五帧全部可见回波学习异常分数。
