# AJAE 新主线方案

## 1. 当前结论

AJAE 当前只回答一个问题：把连续五帧激光雷达扫描对齐到当前帧后，冻结的 STU 能否直接从更稠密的观测中得到更好的异常证据，同时不明显破坏正常数据上的输出。

当前不训练新的 AJAE 网络。schema 32 中的 B0、B1、B2、B3 矩阵、对称窗口参考系、全窗口逐点输出、重叠窗口分数融合、移动正常目标专用损失和复杂裁决链均退出活动主线。它们不能作为 schema 33 的结果或前置成功证据。

新的实验只比较两种输入：

- 单帧 STU：当前扫描直接进入冻结 STU。
- 稠密 STU：连续五帧先按位姿建立同一几何关系，再按 STU 官方 `sweep=5` 的世界坐标和时间升序文件槽构成一张稠密伪扫描。冻结 STU 为五帧全部真实回波生成分数和语义类别；F2 与 F3 的主比较只读取当前扫描原有点对应的行。

若稠密 STU 在正常数据上稳定，并在少量合成异常上获得正向的点级平均精确率差值，后续优先采用这个无训练方案。若冻结 STU 不能适应稠密输入，再考虑在逐帧冻结 STU 特征之后训练一个小型异常头。

## 2. 在线问题与输出时刻

当前时刻为 (t)。系统已经接收前四帧和当前帧，窗口定义为

\[
\mathcal W_t=\{X_{t-4},X_{t-3},X_{t-2},X_{t-1},X_t\}.
\]

第 (i) 帧的激光雷达位姿记作 (T_{W\leftarrow i})。前四帧通过

\[
T_{t\leftarrow i}=T_{W\leftarrow t}^{-1}T_{W\leftarrow i}
\]

变换到当前扫描坐标。五帧可见回波合并为

\[
\widetilde X_t=\bigcup_{i=t-4}^{t}T_{t\leftarrow i}X_i.
\]

这里没有额外的中间参考空间。第 (t) 帧的特殊性只来自在线输出时刻：检测结果必须描述车辆当前坐标系中的障碍物。五帧都进入稠密输入，冻结 STU 的原始输出也覆盖五帧全部输入点。在线检测接口以及 F2、F3 的成对比较只选择 (X_t) 中原有可见点对应的输出行。程序保留全点输出和当前点行号，不能在模型返回时丢弃历史点分数。

窗口按当前帧逐帧推进：

```text
96--100  -> 输出 frame 100
97--101  -> 输出 frame 101
98--102  -> 输出 frame 102
```

因此一个当前时刻只对应一次正式输出，不再平均同一点在多个重叠窗口中的预测。

## 3. 两个冻结 STU 输入

### 3.1 单帧 STU

单帧条件沿用 STU 官方预处理、冻结权重和 MaxLogit 异常分数：

\[
X_t\longrightarrow \operatorname{STU}_{\mathrm{frozen}}\longrightarrow S_t^{\mathrm{single}}.
\]

它是唯一主基线，不再额外训练单帧 AJAE 网络。

### 3.2 稠密 STU

稠密条件把五帧对齐后的全部可见回波当成一张伪扫描：

\[
\widetilde X_t\longrightarrow \operatorname{STU}_{\mathrm{frozen}}\longrightarrow
S_{t-4:t}^{\mathrm{dense}}.
\]

评价只选取来源帧为 (t) 的行，得到

\[
S_t^{\mathrm{dense}}.
\]

两种条件因而在完全相同的当前帧点和标签上比较。过去四帧只改变 STU 可见的几何上下文，不扩大主指标所用的点集，也不产生重复的当前时刻交付结果。全点输出可用于核查，但不能与当前点比较域混为一谈。

F1 的几何表达继续把回波变换到当前扫描坐标，用于检查自车运动补偿和当前时刻场景。送入 STU 时则直接拼接每个 `SourceFrame` 已按官方公式得到的世界坐标，不再经历“源坐标到当前坐标、再回到世界坐标”的浮点往返。输入保留每个非空扫描的全部文件槽参与官方体素化，但模型输出只恢复真实回波。稠密伪扫描继续使用强度和整张合并扫描的中心距离通道。该变化必须在正常稳定性实验中接受检验，不能在看到结果后改写。

官方 `sweep=5` 整理路径会生成扫描时间编号，但当前 `Mask4Former3D` 的空间位置编码明确不读取该编号。AJAE 因而复用官方五帧空间输入语义，只检验增加空间观测密度是否有效，不检验显式时序编码。

## 4. 数据范围

### 4.1 正常开发数据

`train/201` 的原始帧 0--3 已确认存在内部精确重复，继续排除。帧 4--553 构成方法开发区间，共 550 帧和 546 个合法五帧窗口。F1、F2 和 F3 只能使用这个区间。

### 4.2 正常确认数据

`train/201` 的帧 554--681 预留为连续正常确认区间，共 128 帧。合法窗口的源帧起点为 554--677，对应在线输出帧 558--681，共 124 个输出。在方法以及可能出现的 F4 训练配方确定之前，不读取该区间的分数或指标。该区间在 C1 中只回答最终方法是否使正常世界明显失稳。

### 4.3 未来训练数据

只有 F2 或 F3 否定直接稠密 STU 时，`train/206` 才用于生成训练数据和更新参数。每个随机异常世界先在整段正常序列上固定，再连续渲染所有帧，随后切成五帧窗口。相邻窗口中的同一帧和同一异常物体必须保持完全相同。

若需要多个训练样本，生成多个世界位置、形状、尺度和材质不同的完整虚拟序列。训练时可从已经生成的窗口池中随机抽取一部分端点，而不改变世界、标签或优化器更新单位。

### 4.4 真实异常与隐藏测试

19 条公开真实异常序列只在方法、阈值和任何训练配方固定后打开，用于检验合成异常上的判断能否迁移到真实异常。51 条隐藏测试序列只在公开验证支持该方法后使用。

## 5. 递进实验与确认阶段

### 5.0 F0：实现语义检查

F0 除坐标变换、当前点身份和无训练路线检查外，还固定两个容易被一般形状测试漏掉的边界条件。第一，当过去四帧没有真实回波时，稠密输入必须在输入张量、分数和类别上精确退化为单帧输入。第二，当历史点和当前点落入同一个 5 厘米体素时，程序必须使用 STU 官方稀疏量化的逆映射，把该体素输出恢复到正确的当前点行。正常语义类别还必须与 STU 官方查询分配计算完全一致。

真实 F0 固定单帧 8、198、387 和五帧窗口 4--8、194--198、383--387。每个案例逐元素比较官方 `LidarDataset(sweep=1/5)` 与 AJAE 的世界坐标、强度、中心距离、稀疏体素、稀疏特征和逆映射，并比较全部真实回波及当前帧视图的 MaxLogit 与类别。AJAE 路径在相同随机状态下重复两次，确定性 CPU 路径要求逐点完全重复。正式证据保存在 `artifacts/f0_qualification.json`。

### 5.1 F1：几何稠密性

F1 不运行模型。它遍历正常开发区间的全部 546 个窗口，报告：

- 五帧可见回波数与当前单帧可见回波数之比；
- 在 0.05、0.10 和 0.20 米体素下的唯一体素数之比；
- 五帧输入中当前帧没有覆盖的新体素数量。

每个体素尺度都汇总唯一体素比例的中位数、第 5 百分位和第 95 百分位，并报告新增体素数的中位数，避免只保留逐窗明细而无法判断总体几何变化。

当前帧 8、198、387 和 553 另导出带扫描年龄字段的 PLY 点云，用于检查配准、重影和动态目标拖影。F1 是描述性实验，不凭点数增加直接宣称异常检测改善。

### 5.2 F2：正常稳定性

F2 在开发区间预先选定的 24 个当前帧上运行两种冻结 STU 输入。异常稳定性统计使用原始语义不等于 0、也不等于异常类 2，并且距离位于 2.5--50.0 米闭区间的当前点。正常语义准确率和平均交并比使用 STU 目标类别不等于忽略值 255、并且处于同一距离范围的当前点。两个掩码不能互换，因为原始语义中的有效正常点集合与 19 类语义评价集合不是同一个构念。

在完整 F2 前，程序先在当前帧 8、198、387 和 553 上核对 `normal_class` 是否逐点等于 STU 官方查询分配语义。此检查失败时，F2 的类别指标没有意义，必须停止而不能把字段存在当成语义正确。

当前参考 CUDA/MinkowskiEngine 环境未通过同一输入重复前向检查，因此 schema 33 暂时只授权确定性 CPU 路径执行 F2 和 F3。重新使用 GPU 前必须先在相同真实帧上同时通过重复前向和官方端到端等价检查，不能用运行速度换取不同条件间不可解释的随机差异。

稠密 STU 只有同时满足以下三项才视为正常稳定：

- MaxLogit 中位数相对单帧增加不超过 0.02；
- MaxLogit 第 95 百分位相对单帧增加不超过 0.05；
- 正常语义准确率下降不超过 0.02。

这些容差在真实 F2 输出产生前写入 schema 33。平均交并比用于解释类别层面的退化，不额外增加一个事后裁决条件。结果同时报告各帧中位数偏移和第 95 百分位偏移的最坏帧，只作诊断，不增加新门槛。

### 5.3 F3a/F3b：少量合成异常信号

F3 预先固定 16 个全局互不重叠的连续片段，每段 28 帧，起点依次为 4、38、72、106、140、174、208、242、276、310、344、378、412、446、480 和 514。交替选取其中 8 个作为 F3a，其余 8 个作为 F3b。每个片段的源起点和世界根种子均在运行前固定。支撑面锚点必须位于片段首尾各内缩 2 帧的范围，使锚点所需的前后各 2 帧估计上下文完整落在同一片段内。

一个根种子最多允许 48 次物理放置尝试，第 (k) 次尝试的种子严格为 `root_seed + 1000003 * k`；只有世界构造期间的物理放置失败可以进入下一次尝试。每个实体只在合法支撑行中，按世界与实体种子以及帧号、文件槽身份的确定性散列排序候选位置。同一种子保持完全相同的候选顺序，不同种子改变候选前缀。渲染开始后不得重采样世界，也不得因某个窗口看不到异常而替换根种子。

固定异常物体后连续渲染整段，并从第 5 帧开始形成 24 个候选在线输出。只有在 2.5--50.0 米评价范围内至少含 5 个异常点的当前帧才进入点级指标计算。一个世界若没有任何合格当前帧，结果仍记录该固定世界及不可评价原因，但不进入指标自助重采样。

两种条件在相同当前点、相同异常标签和相同距离范围上调用 STU 官方点级评价定义，报告平均精确率、受试者工作特征曲线下面积和 95% 真阳性率处的假阳性率。实现直接使用与官方一致的排序和阈值函数，重复分数按官方库的并列值规则处理。每个异常世界先得到一对指标，再以世界为配对单位执行 10,000 次自助重采样，主裁决量为

\[
\Delta AP=AP_{\mathrm{dense}}-AP_{\mathrm{single}}
\]

的 95% 区间。

若 8 个筛选世界全部可评价，且平均精确率差的均值和中位数都不大于 0，F3a 否定直接方法并进入 F4。其余情况均执行预先固定的 8 个 F3b 世界；这包括初筛区间下界大于 0 的情况，因为 8 个世界尚未达到最终正向证据量下限。最终按 16 个计划世界报告。只有至少 12 个计划世界可评价、\(\Delta AP\) 的 95% 区间下界大于 0 且 F2 通过，现有证据才支持直接采用稠密 STU。可评价世界不足 12 个时结论是合成可行性证据不足，并进入 F4，不能按少数易观察世界宣布直接方法成功。合成结果只支持合成开发分布上的可行性，不能替代真实异常验证。

### 5.4 F4：最小训练分支

F4 不是当前待执行实验。只要直接稠密 STU 未同时得到 F2 与 F3 支持，包括 F3 证据不足，才启动该分支。

第一候选方法保持 STU 的原生单帧输入：五帧分别通过同一个冻结 STU，再把逐点特征连同对齐到当前帧的几何送入一个小型异常头。只有这个小头已得到清楚定义、完成最小可学习性检查且仍不足以回答问题时，才讨论更大的新网络。

F1--F3 结束以前，不固定 F4 网络、损失、训练轮数或大规模训练样本量，因为这些选择取决于冻结 STU 失败的具体方式。

### 5.5 C1、V1 与 T1

机器状态显式记录方法分支。F1--F3 期间 `selected_method` 为空；F2 与 F3 支持直接方法后，它取值 `direct_dense_stu`，同时 `f4_required=false`。直接方法未得到支持时必须先令 `f4_required=true` 并进入 F4；F4 完成后 `selected_method=f4_small_head` 且 `f4_completed=true`。只有这两种已完成分支之一成立，程序才接受 C1 状态。

C1 只打开正常确认区间的 124 个在线输出，检查冻结后的最终方法是否在正常数据上失稳。C1 不含真实异常，也不允许据此重新选择方法或阈值。C1 通过后，V1 才打开 19 条公开真实异常序列，检验合成开发判断能否迁移到真实异常。V1 支持方法后才进入 T1 的 51 条隐藏测试序列。把 C1 与 V1 分开可以明确区分正常安全证据和真实异常有效性证据。

## 6. 评价与解释边界

合成异常主指标为点级平均精确率，AUROC 和 FPR95 作为辅助指标，三者均遵循项目内绑定字节哈希的 STU 官方点级评价程序。平均精确率与 AUROC 越高越好，FPR95 越低越好；结果文件显式记录该方向，避免把统一的 `dense-single` 差值看反。传感器标定文件同样绑定字节哈希。官方距离范围保持闭区间 2.5--50.0 米；单帧和稠密条件必须使用相同有效点掩码。

移动车辆、行人和骑行者仍按原始语义标记为正常，但在出现正向主结果以前不设计专用损失，也不设置早期停止条件。若 F2 与 F3 支持主方法，再单独报告移动正常子组，以判断稠密对齐是否因目标运动产生额外误报。

五帧配准只补偿自车运动。动态物体在当前坐标中可能形成拖影；冻结 STU 也可能因点密度、遮挡关系和扫描中心距离分布变化而失稳。这些都是 F1 和 F2 要直接暴露的现象，而不是预先用复杂模型掩盖的问题。

## 7. 当前真实状态

schema 33 已替代 schema 32 的活动实验定义。`contract_identity` 固定 F0--F3 的科学合同，阶段、完成声明及设备资格另由完整协议文件身份追踪。正式 F1--F3 只允许从已跟踪文件无修改的 Git 提交运行。当前状态顺序固定为 F1、F2、F3、F4、C1、V1、T1，命令行只允许执行与协议当前状态同名的实验。

F0 已在干净提交 `f0d32486a939da0dd7bf9fce2f1315b95aecec19` 上完成。三个单帧与三个五帧案例的官方输入、体素和逆映射均与 AJAE 一致，官方与 AJAE 以及 AJAE 两次 CPU 重复的 MaxLogit 最大绝对误差均为 0，类别不一致数均为 0。证据保存在 `artifacts/f0_qualification.json`。当前节点仍是待执行的 F1。

当前尚未在真实 `train/201` 数据上执行 F1、F2 或 F3；没有生成新的合成异常序列，没有训练 AJAE 参数，也没有真实异常或隐藏测试结果。因此现在能成立的结论仅限于研究问题和实现路线已经收束，不能声称五帧输入已经变稠密、冻结 STU 能够适应稠密输入或异常检测性能得到提升。
