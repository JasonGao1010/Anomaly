**AJAE 完整模型审定方案｜2026-09-23｜保留正常假设层次的兼容性解码**

**采用“观测约束的正常回波场与逐点兼容性分割网络”。** 输入单帧 LiDAR，输出每个真实回波的一个异常分数。实际观测路径保留已有骨干、多尺度上下文和原始点细节；正常路径由目标块之外的上下文预测三维回波场，经射线观测得到正常距离分布；兼容性解码器先读取每个正常假设内部的八个核，再结合该假设的条件距离概率和块外先验，读取四个完整假设。所有可学习模块联合训练。

本版替代上一版软表面元、固定宽参考分布、固定污染比例、块内参考点重加权和十二项统计证据的设计。后续开发只实现、训练这一完整结构，不安排 N1、四平面首版或普通条件模型的重训队列。

本文件是现行主模型的唯一架构说明，不声称已经找到全局最优模型。本次结构升级将原来的每尺度 32 核混合读取改为假设内、假设间两级注意力。骨干结构、目标块不可见的正常场、共享假设联合辅助似然、最终单一异常输出及三指标共同排序损失均保留。随后针对真实核验发现的重载差异，固定了骨干稀疏卷积的累加顺序，保持其权重与卷积语义。第 15 节保留修复前失败记录，第 16 节记录定位、修复及原容差下通过的真实回归。现行模型尚未开展正式训练，检测性能和收益尚未验证。

第 13、14 节保留升级前解码器的真实执行记录。其中 STU 206＋Perlin 单种子实验的 AP 75.690029%、FPR95 1.085961%、AUROC 99.803424% 属于旧解码器在 STU 开发验证集上的历史证据，不能作为新解码器的成绩，也不是仅用 nuScenes 训练的结果。现行训练范围限定为 nuScenes，允许源内合成异常；当前按步骤重建数据，已完成第 9.2 节正常背景划分和第 9.3 节真实异常候选物体选取，尚未启动正式训练。此前生成数据的执行记录保留原适用范围，不作为这次逐步构建已经完成的证据。

**本次结构调整的目的，是让逐点判别保留“同一假设内多个核共同构成结构、不同假设提供不同正常解释”的区别。** 两级注意力只学习判别特征，不修改正常假设先验，不增加独立检测头、人工异常阈值或后处理规则。其是否改善最终三指标需要真实训练后判断。

**1．任务、贡献与设计判断**

目标是单帧、LiDAR 单模态的未知异常点级分割，优化 AP、AUROC 和 FPR95。正常集合包含道路、车辆、行人、路缘等协议内正常结构。道路中间、高出地面、点少、预测不准，均不能单独成为异常定义。少回波、低矮、远距和弱遮挡是整体方法可能自然获益的场景。

模型回答两个相互补充的问题：

- 实际点及其周围场景，在已学习的正常/异常判别空间中是否合理？
- 隐去待检验区域的实测回波后，周围信息支持哪些正常三维结构？这些结构通过实际射线能否解释待检验回波？

第二条路径提供一种受约束的表示与学习信号，不是在同一输入之外凭空增加信息。正常预测失败也不直接等于异常，最终判断仍由有正常与异常监督的逐点解码器学习。

拟主张的贡献：

> 用目标区域不可见的上下文预测共享正常三维回波场，通过明确的射线关系构成正常回波联合分布；将该分布与实际点之间的兼容性纳入端到端异常分割，使正常几何学习直接服务于逐点异常排序。

贡献对象是这个完整学习机制及其被验证的收益。高斯、射线积分、掩码预测、注意力、重建与判别联合训练本身均已有先例。

**2．历史证据基础与结构选择**

最初方案核对的历史代码版本：[v4 / 79997c1aa91f479bbe111e717bfa206e2bb093f6](https://github.com/JasonGao1010/Anomaly/tree/79997c1aa91f479bbe111e717bfa206e2bb093f6)。本节保留其结构选择依据，以下历史指标不属于现行两级兼容性解码器。

现有四平面候选终点记录为 AP 81.50734%、FPR95 0.07806%、AUROC 99.96646%；同期 N1 为 80.08333%、0.08157%、99.97082%。候选全量重载结果尚未核实。它说明方向已有初步开发收益，不能证明新架构会进一步上涨，也不能说明三个指标已全面改善。

| 复审对象 | 审定处理 | 原因 |
|---|---|---|
| 已有实际点编码与六尺度融合 | 保留组件，随完整模型训练 | 已有任务内证据；不增加无依据的骨干替换 |
| 单平面或手工表面元求交、排序 | 替换为连续三维回波场的解析射线积分 | 支持有限厚度、不同朝向与重叠结构，减少硬分支 |
| 人工宽参考分布及固定污染比例 | 删除 | 缺少任务依据，还可能提供吸收异常的便捷通道 |
| 用目标块内其他回波修改正常假设权重 | 删除 | 正常参照始终由块外生成，避免块内异常互相“证明正常” |
| 十二项人工统计证据 | 替换为可学习的回波兼容性解码 | 不预先规定哪些残差、熵或阈值一定有用 |
| 以正常预测质量作为检测训练门槛 | 不采用 | 正常预测是辅助任务；最终依据是异常分割三指标 |

这些修改针对明确的表示和优化风险，尚不是已证实的性能改进。

**3．确定的整体结构**

| 部分 | 实施配置 |
|---|---|
| 实际点编码 | LitePT-S，5 cm 体素；现有原始点 MLP、mean/max 聚合、六尺度条件融合与逐点回填 |
| 实际点状态 | 64 维场景状态＋64 维点细节投影＋64 维体素内位置编码，相加得到 64 维 $f_i$ |
| 正常上下文 | 1°、2°、4° 嵌套角度块；各目标块读取同层 5×5 邻域去除自身的 24 块 |
| 上下文编码 | 原始坐标、强度、单位方向共 7 维→64→64；仅块内 mean/max 池化，投影至 128 维 |
| 正常假设解码 | 四个 128 维查询，两层四头交叉注意力，前馈宽度 256；三尺度共享参数 |
| 正常回波场 | 每个假设八个各向异性三维高斯基函数；中心、完整协方差、幅度与假设权重均由网络预测 |
| 射线观测 | 解析计算沿射线的回波密度和累计量，构成归一化的条件距离分布 |
| 兼容性解码 | 共享 3→16→64 映射编码核参数；实际点查询先在每个假设内读取八核，再将所得 32 维特征与该假设 log 条件密度、log 先验拼为 34 维，经共享 34→64→64 映射后读取四假设；每尺度输出 32 维 |
| 最终头 | $[f_i,e_i^{1},e_i^{2},e_i^{3},\ell_i^{1},\ell_i^{2},\ell_i^{3}]$，163→128→64→1 |
| 输出 | 每个真实点一个 FP32 logit；同一分数用于 AP、AUROC、FPR95 |
| 排序学习范围 | 默认一次更新八扫描共同计算，微批次仅用于调度；显存不足采用分数梯度缓存与重算 |

四个假设、八个基函数是容量设置，不是四类正常物体或八种异常模板。三个尺度是留出范围，不是异常尺寸规则。这些是确定实现所需的默认值，没有理论或实验保证它们最优。

**4．实际观测路径与严格的信息边界**

实际路径沿用 [src/model.py](https://github.com/JasonGao1010/Anomaly/blob/79997c1aa91f479bbe111e717bfa206e2bb093f6/src/model.py) 中已有效的点细节和上下文组件。新训练使用项目固定的官方 nuScenes LitePT-S 预训练权重初始化骨干，新分支与新异常头重新初始化。骨干继续可训练。

复用组件不意味着再次训练 N1，也不继承历史自制合成数据训练后的异常头权重。论文需披露官方预训练数据和权重。

对某尺度目标块 $B$，正常场参数只允许依赖块外上下文 $C_B$、固定网格位置和尺度。逐点 MLP 后先做各块独立池化，排除目标块，随后才运行上下文交叉注意力。不能先整帧混合特征，再遮掉最后一层目标 token。

禁止正常场预测器读取目标块的距离、强度、点数、三维近邻、体素、骨干特征或异常分数。禁止跨块 BatchNorm、全扫描统计或全局 token 将这些信息带入正常场。使用逐点/逐 token LayerNorm。

上下文输入为 $(x/U,y/U,z/U,I,d_x,d_y,d_z)$，$U$ 为协议距离上限；不增加人工距离方差、密度阈值、地面高度等描述子。mean/max 是对学习特征的标准池化。块位置与尺度经学习投影加入，空块使用缺失掩码；上下文全空时使用学习到的空上下文 token，不直接输出“正常”。

同层邻块中的点可能仍含异常；多尺度留出降低部分污染风险，但不能保证任何大小的异常都被完整遮住。尤其不能声称 4° 是未知物体大小的上界。

目标射线的方向和原点只进入观测计算；目标距离只在比较、正常辅助监督和最终异常判别时使用。最终判别器可以读取实际点；这是任务输入，不构成对正常预测的前向信息泄漏。

**5．正常三维回波场**

对每个目标块、每个尺度，网络输出 $K=4$ 个正常假设及权重：

$$
\pi_k=\operatorname{softmax}(b)_k,\qquad
\lambda_k(x)=\sum_{m=1}^{M}a_{km}
\exp\!\left[-\frac12(x-c_{km})^\top\Sigma_{km}^{-1}(x-c_{km})\right],
\quad M=8.
$$

$c$ 是三维中心，$\Sigma$ 是正定的完整三维协方差，$a>0$ 是幅度，单位为每米。采用 Cholesky 参数化保证正定，用正值参数化保证幅度为正；仅保留数值稳定所需的极小值。

中心、朝向、三个轴尺度与幅度都通过梯度学习，不预设平面、物体类别、道路高度或人工绝对/相对噪声公式。较扁、较长或较宽的核可组合表示不同结构；高斯尾部没有硬边界。所谓“正常”仍是任务中的正常场景，不是说正常数据必须服从单个高斯分布。

同一个假设内的多个核共同存在；不同假设表示不同正常解释。所有射线共享该块预测的同一组场参数，不能让每个目标点自行生成一套能解释自己的三维结构。

共享范围是同一块内的多条射线；不同块和不同留出尺度分别预测，不宣称已经得到跨整帧、跨视角或跨距离一致的全局地图。多个假设无需被人为强制成不同类别；若上下文足够明确，权重集中本身不构成错误。

初始化使用覆盖有效距离区间的较宽场，并用不同的查询初始化打破假设/核的对称性；不从目标点位置初始化中心。初始化尺度仅是优化起点，随后可学习。

$\lambda$ 是用于正常距离预测的有效回波率场。仅凭留出距离无法唯一恢复真实占据、反射率或完整场景；不把它包装成可辨识的物理世界模型。

**6．由射线生成正常观测分布**

单位方向射线为 $x_i(r)=o_i+r d_i$。以已有 STU 评价采用的有效距离区间 $[L,U]=[2.5,50]$ 米为例；该区间本身不表示 STU 进入新模型训练：

$$
H_{ik}(r)=\int_L^r\lambda_k(o_i+t d_i)\,\mathrm dt,\qquad
Z_{ik}=1-\exp[-H_{ik}(U)],
$$

$$
p_{ik}(r\mid\text{有效区间内有记录回波})=
\frac{\lambda_k(o_i+r d_i)\exp[-H_{ik}(r)]}{Z_{ik}},
\qquad L\le r\le U.
$$

这是一个归一化的距离密度：近处回波率影响远处被观测到的相对概率。同一几何场通过不同射线产生彼此关联的预测，而不是若干独立自由距离回归器。

该形式采用生存概率/首次事件的有效观测模型。真实设备可能记录最强回波而非理想首次回波，扫描运动也可能使射线原点近似有误，因此不能称为完整、精确的传感器仿真。仅有回波点、缺少可靠原始发射记录时，不监督丢失回波，不把空角度格当成自由空间，也不将 $Z$ 宣称为校准后的实际 ray-drop 概率。

区间是评价协议的有效范围，不是异常阈值。其他数据使用其固定协议边界，训练和评测必须一致；区间外或忽略标签的点可作为上下文，不因此被标为正常。若真实束原点不可用，明确使用扫描参考原点近似，不能虚构精确发射轨迹。

**解析计算，不做稠密射线步进。** 单个高斯沿射线仍为一维高斯响应。令 $\Sigma=AA^\top$、$v=A^{-1}d$、$w=A^{-1}(o-c)$：

$$
\mu=-\frac{v^\top w}{v^\top v},\qquad
\tau=(v^\top v)^{-1/2},\qquad
\log h=\log a-\frac12\|w+\mu v\|^2,
$$

$$
\lambda_m(r)=h\exp[-(r-\mu)^2/(2\tau^2)],
$$

$$
\int_L^b\lambda_m(r)\,\mathrm dr
=h\tau\sqrt{2\pi}
\left[\Phi\!\left(\frac{b-\mu}{\tau}\right)
-\Phi\!\left(\frac{L-\mu}{\tau}\right)\right].
$$

各核积分相加得到 $H$。这在上述有效回波率假设内支持重叠核，不需要硬求交、深度排序、最近表面选择或额外人工宽分量。解析高斯射线积分本身已有先例，不作为本工作的发明。

仅有条件回波位置监督时，幅度可能不可充分辨识；小累计量极限下分布接近沿射线的归一化场响应。因此创新主张依托“共享三维场对预测的约束”，不能仅靠公式中出现透射率就声称已经学会真实遮挡。

本方案直接定义有记录回波条件下的预测模型，$\pi_k$ 是这个条件模型的混合权重，不是完整物理生成过程的无条件场景先验；不能从当前条件密度反推未观测射线的真实占据或返回概率。

**7．共享正常解释与可学习逐点判别**

正常辅助任务使用一个假设共同解释目标块内的有效正常回波：

$$
P(R_B\mid C_B,Q_B,N)
=\sum_{k=1}^{K}\pi_k\prod_{i\in B_{\rm valid}}p_{ik}(r_i).
$$

实现顺序为：每个假设先累加各射线的 log 概率，再对假设做 logsumexp。不能先为每点挑选最佳假设再求和。辅助负对数似然按块内有效点数归一化，再在尺度和扫描间汇总；同一组完整概率参数同时用于辅助学习和判别。

默认使用全部有效正常目标回波，以分块累计控制显存；删除“每块最多 32 个参考点”。没有目标块内的后验重加权，也没有固定污染比例。推理的假设先验 $\pi_k$ 始终来自块外上下文。

逐点正常 log 密度为：

$$
\ell_i^s=\log\sum_k\pi_k^s p_{ik}^s(r_i).
$$

为保留假设内部结构，每个核先提供三个射线参数，经共享映射得到核表示：

$$
t_{ikm}^s=\phi
\left(\mu_{ikm}/U,\ \log(\tau_{ikm}/U),\
\log(h_{ikm}U)\right)+\delta_s.
$$

核映射 $\phi$ 为 3→16→64，$\delta_s$ 是 64 维学习尺度编码；相加后分成用于注意力匹配的 32 维键和用于汇聚的 32 维值。实际点状态经 64→32 投影，归一化实际距离 $r_i/U$ 经 1→16→32 编码，两者相加得到查询 $q_i\in\mathbb R^{32}$。同一个查询先分别读取各假设的八个核：

$$
u_{ik}^s=\operatorname{Attn}_{m=1,\ldots,8}
\left(q_i,\operatorname{key}(t_{ikm}^s),
\operatorname{value}(t_{ikm}^s)\right)
\in\mathbb R^{32}.
$$

随后，将每个假设的汇聚特征、该假设对实际距离的完整条件 log 密度和块外预测的 log 先验拼接，经过共享映射：

$$
v_{ik}^s=\psi\left[u_{ik}^s,\log p_{ik}^s(r_i),\log\pi_k^s\right],
\qquad \psi:\ 34\rightarrow64\rightarrow64.
$$

这里的 $p_{ik}$ 是第 6 节由同一假设全部八核共同确定、包含累计回波率和归一化项的条件首返回密度，不是某个核的独立响应。有效距离区间外的真实回波，其送入 $\psi$ 的 log 密度置为零；正常联合辅助计算仍使用原始概率值及既定有效点掩码，不改变监督目标。

将 $v_{ik}^s$ 分为 32 维键和值，再由同一查询读取四个假设：

$$
e_i^s=\operatorname{Attn}_{k=1,\ldots,4}
\left(q_i,\operatorname{key}(v_{ik}^s),
\operatorname{value}(v_{ik}^s)\right)
\in\mathbb R^{32}.
$$

两级注意力均使用四头，每头八维；同类映射在所有核、假设和尺度间共享，不加入核编号或假设编号嵌入。尺度编码仍用于区分三个留出范围。第一层在同一正常解释内部汇聚核信息，第二层比较完整正常解释，避免先把四个假设的 32 个核混成一个集合。第二层注意力权重只是判别特征的汇聚系数，不是对 $\pi_k$ 的后验重估；实际点信息不会经此步骤返回正常场预测器。

最终头输入 64＋3×32＋3＝163 维，MLP 163→128→64→1，隐藏层采用标准归一化和 GELU。三个 $\ell$ 提供绝对兼容程度，避免仅靠归一化注意力在“全部解释都很差”时仍必须选出一个解释。它们不是手工异常分数或标签，最终头可结合语义、实际细节和学习到的分布特征决定如何使用。

不输出区域统一异常分数，不将块级联合 NLL 复制到每点；也不对最终分数做邻域平均、连通域过滤、道路硬掩码或按点数删除目标。

联合分布约束用于正常训练；推理提供的是逐点边缘密度与预测分布特征，最终头同时读取实际场景上下文。边缘密度高并不证明整块观测能由一个正常假设共同解释，也不证明该点一定正常。论文不能把当前逐点头描述为精确的区域联合异常检验。

**8．一个训练目标，直接服务三指标**

令 $s_i$ 为最终异常 logit，正常/异常标签分别为 0/1。使用：

$$
\mathcal L=
\mathcal L_{\rm balanced\ BCE}
+\lambda_{\rm rank}(t)
\left[
\widetilde{\mathcal L}_{\rm AP}
+0.1\widetilde{\mathcal L}_{\rm AUROC}
+0.1\widetilde{\mathcal L}_{\rm FPR95}
\right]
+0.1\mathcal L_{\rm normal}.
$$

BCE 和三个排序代理都优化同一个最终输出，不建立三个独立头，也不分别挑三个检查点报告最好数字。正常预测只是辅助约束。

沿用当前代码已有的排序目标定义和初始权重，不再增加一组未经验证的自适应损失权重。$\lambda_{\rm rank}$ 沿用总进度 10%–20% 由 0 线性升至 1；正常辅助权重固定 0.1。去掉上一版额外的几何分支开启门控和正常辅助权重下降日程。

| 指标 | 学习目标关注什么 | 结构上的配合 |
|---|---|---|
| AP ↑ | 异常点在排序前部的精确率与召回 | 逐点细节保留边界；兼容性特征提供可学习的额外判别依据 |
| FPR95 ↓ | 高召回位置仍被排到前面的正常点 | 正常预测与场景状态联合判断，允许正常边界和多种正常解释 |
| AUROC ↑ | 整体正常/异常点对的排序 | 同时覆盖普通正常点与困难正常点，避免只顾少数高分误报 |

**已确定的排序范围。** 历史代码在两扫描微批次内计算排序损失，再累积梯度；本次完整模型在一次参数更新内的完整有效批次共同排序，默认八扫描。对排序损失而言，微批次划分仅是计算调度，不能各自独立确定召回阈值或排序池。

在有效批次上最多取 256 个均匀抽样的异常锚点；正常点保留最高分 512 个，并从其余正常点抽取最多 3,584 个、用抽样权重补偿。AP 的正样本秩使用该有效批次全部异常点；AUROC 使用加权点对 softplus；FPR95 由该有效批次全部异常点确定一个软 95% 召回阈值，再计算加权正常软超阈率。温度沿用 1。无异常的完整有效批次仍训练正常 BCE，不能伪造排序目标。

动机可用简单反例说明：一组扫描的异常分数为 10、20 个正常点均为 9；另一组异常为 1、20 个正常点均为 0。每组单独评价都是 AP=1、AUROC=1、FPR95=0，合并后则为 AP=0.54545、AUROC=0.75、FPR95=0.5。因此，独立微批次排序不是更大批次排序的等价实现。此反例是数学说明，不是本项目真实失分归因；已有 BCE 也会提供跨帧分数约束。

修正后仍然是有限批次、有限样本和训练分布下的代理，不能称为全数据指标的精确或无偏估计，也不保证三个测试指标同步改善。随机正常点覆盖对 AUROC 很重要，不能为了困难负例挖掘全部删除。推理不做逐帧 min–max、分位数或其他按当前帧统计变化的分数归一化。

异常损失通过兼容性解码、log 密度和观测公式回传到正常场与正常上下文编码器；同时回传到实际点骨干。整个网络不设正常分支 stop-gradient，不要求先单独预训练预测器再通过门槛才进行检测。

$\mathcal L_{\rm normal}$ 仅使用可靠、未修改的真实正常扫描；合成异常用于最终判别，不被当作正常重建目标。完整 NLL 的归一化会惩罚一味扩宽分布，但不自动保证校准，也不能完全排除有限训练集上的过度尖锐拟合。

异常梯度可以改变正常场，辅助权重 0.1 也不是理论上的正常性保证；因此“端到端”与“始终是校准的纯正常分布”不能画等号。保持正常辅助目标、检查正常扫描上的覆盖与宽度，是解释该分支含义所需的诊断，不另设预测器预训练门槛。最终模型可能降低某些几何证据的使用权重，不能把这种可学习融合提前宣称为不会掉点的保证。

**9．训练数据与运行边界**

现行主模型的训练数据限定为原始 nuScenes 及其源内合成异常，骨干初始化使用的官方 nuScenes 预训练权重也计入来源披露。采用官方训练集 700 个场景、28,130 个已标注关键帧，官方验证集 150 个场景、6,019 个已标注关键帧。两个分区分别涉及 50、18 个采集日志，实际核对无日志交叉。原始关键帧全部保留，训练与开发的背景、供体分别只从自身分区取得，不随机拆分相邻帧，也不读取旧生成数据。STU 扫描不进入权重训练或正常辅助监督。

逐点标签以当前 `/home/jasongao/Data/Nuscenes/lidarseg/category.json` 为语义依据，按名称读取编号。其中 `movable_object.debris` 定义为留在可行驶路面、不能安全碾过的物体，例如树枝和装满的垃圾袋；`movable_object.pushable_pullable` 包括手推车、独轮车、垃圾桶和购物车。此前引用的旧官方标注指导对这两个类别的描述较宽，不能覆盖本轮原始标签定义。按用户确定的监督范围，原始 `debris` 点保持忽略，只将其语义用于提取真实供体；其他含义不明确或噪声类别也不自动标为异常。已声明的常见正常类别提供正常监督。

合成正例来自真实 `debris` 与 `pushable_pullable` 的语义点和官方实例旋转框的交集。同类重叠框中归属不明确的点剔除，框内含使用者的异常候选不移植。生成器只在同一原始扫描的相邻线束、有限方位间距且没有明显深度断裂的观测点之间构建开放三角面，不补成完整封闭物体，也不跨扫描混合线号重新构面。表面在源道路和目标道路的局部切平面坐标系之间作刚体变换，保留实测尺度、主要观察侧和相对路面的高度；接收位置依据实际可行驶路面及已有点云检查支撑与碰撞。正常车辆、行人、两轮车、锥桶与护栏中被当前标签定义为正常的供体经过同一个移植算子，仍标为正常。请求数量、实例重复限制及各用途见第 9.1 节。

可见性只在原始已有回波的方向上计算。最近供体表面位于原回波之前时，替换对应槽位并保留其余原始点；原始扫描本身不被覆盖。遮挡先于监督范围判断：有效距离外的前景仍应遮挡背景，并保留为观测上下文；随后根据最终保存坐标的 2.5—50 米范围确定哪些点参与监督。包围球包含扫描参考原点时，检查全部方向，不能用一个不完整的角锥漏掉可见表面。上述处理仍是观测可见表面的近似，不构成完整发射过程或缺失物体表面的物理模拟。

每个合成扫描配对其未修改原始扫描，后者仅以可靠正常点提供正常场辅助目标；检测分数不读取该参考扫描。供体来源、原始槽位、道路坐标系和放置参数由数据清单追溯，供体元数据集中保存，记录通过供体编号引用。原始扫描只引用源文件，合成结果只保存改变的槽位。训练保留原始背景、正常控制及仅有 1—4 个有效异常点的扫描；异常请求没有有效正点时不新增记录，但记录失败原因，至少五个异常点的规则只标记开发评价资格。源域合成开发结果说明在这套来源与生成规则下的表现，不能替代 STU 的真实异常评价。

唯一现行生成入口为 `python -m src.data nuscenes --nuscenes-root /home/jasongao/Data/Nuscenes --output results/data/nuscenes --workers 12`，实现位于 `src/nuscenes.py`，数据版本为 `AJAE-V4-NS`，本轮配方修订为 `instance-coverage-1`，官方场景名单取自本机 `nuscenes-devkit==1.2.0`。本轮已按第 9.1 节预算完成实例核定、生成及独立全量读回，替代原生成清单。现行清单为 `results/data/nuscenes/train.json`、`val.json`，数量依据及逐请求结果分别为同目录的 `census.json`、`train_requests.json`、`val_requests.json`。

| 对象 | 训练 | 开发 |
|---|---:|---:|
| 原始扫描 | 28,130 | 6,019 |
| 合成异常扫描 | 15,308 | 1,830 |
| 正常移植对照 | 4,138 | 355 |
| 合计扫描记录 | 47,576 | 8,204 |
| 有效正常监督点 | 1,132,824,195 | 196,866,743 |
| 有效异常监督点 | 326,479 | 41,795 |
| 仅有 1—4 个有效异常点的扫描 | 5,708 | 664 |
| 至少五个有效异常点的扫描 | 9,600 | 1,166 |
| 可用异常标注实例／实际成功使用实例 | 1,284／1,280 | 187／186 |
| 可用异常实测观察／实际成功使用观察 | 5,655／5,327 | 781／710 |
| 可用正常实例／实测观察 | 1,029／4,257 | 256／1,039 |

表内正常点和异常点总数包含不同记录重复使用的背景及供体，不是独立点或形状数量。开发的 1,166 个五点合格扫描中，主开发参考组为 875 个，距离覆盖诊断组为 291 个。现行评价入口只使用前者，包含 21,021,061 个有效正常点及 34,036 个有效异常点；不会将诊断组拼入主开发三指标。纯正常记录和正常控制另用于误报诊断。本轮没有正式训练，也没有在新数据上获得 AP、AUROC 或 FPR95 成绩。

独立核验逐条通过正式 `Scans` 读取全部 55,780 条记录，共 1,936,688,320 个扫描返回点，并与原始扫描和 21,631 个替换文件独立重建的结果比较；652,978 个替换点及 11,732 份供体观察均通过相应检查，数据失败为零。供体检查重新读取原始逐点语义和官方旋转框，确认点归属；训练和开发的场景、采集日志、原始扫描编号、关键帧编号、标注实例编号五项交集均为零。每次移植的来源场景不同于接收场景。替换槽位唯一合法，坐标有限、强度在 [0,1]，未替换的归一化坐标与强度逐位不变；原始未替换 `debris` 始终忽略。所有替换均位于原返回之前，训练和开发最大单位射线方向差分别为 $8.866571163699818\times10^{-8}$、$8.019262494335683\times10^{-8}$，均低于预设 $2\times10^{-7}$ 容限。

训练、开发实际扫描读回段分别耗时 36.54、6.77 秒。开发核验曾因核验器将几何留出失败误预期为普通无供体状态而中断，核对全部 600 条后修正了状态名；没有放宽几何、标签或数值条件。含原始元数据重载的开发续跑为 22.56 秒。全量核验还检查了请求预算、每实例至多六观察、每观察每距离层至多两请求（包括失败）、开发三组接收背景不重叠；实际每实例最大请求数为训练 37、开发 34。本项没有独立重算每次三角拓扑、道路支撑和碰撞，解析几何与边界测试覆盖相应实现，不能将一致性检查扩大为完整物理真实性。

本轮生成使用 12 个 CPU 工作进程，每个数值库线程数为 1；完整核定和最终生成耗时 168.07 秒，不含两次已废弃尝试。新数据总逻辑大小为 149,328,237 字节，约 142.41 MiB。其中训练、开发清单分别为 88,083,438、13,909,963 字节，替换文件分别占 26,296,398、2,693,326 字节，其余为核定和请求结果。原始点云未复制。生成后 Windows E 盘剩余 25,937,256,448 字节，超过固定 10 GB 安全余量。清单与生成源纳入版本控制，替换数组继续遵守现有忽略规则留在本地。`recipe` 中的供体点数、角度邻接、道路残差、碰撞余量、距离条件和尝试次数均属人工几何假设，不能称为最优配方或已验证的性能贡献。正常辅助分支所见额外未修改扫描计入数据访问量与时间，不增加新的监督目标。

现行训练入口要求显式传入 `--train-manifest`、`--val-manifest` 和 `--output`，并继续要求 `--epochs`；已取消旧 STU 206 训练清单、val19 开发清单和输出目录的默认值。命令形式为 `python -m src.train --train-manifest <nuScenes训练清单> --val-manifest <本阶段开发清单> --output <本轮输出目录> --epochs <完整遍历次数>`。正式训练尚未启动；第 13、14 节的历史命令不作为现行启动命令。

若采用移动点坐标的公开增强，必须同步更新有效射线表示：使用扫描原点近似时，由增强后的坐标重新计算距离、方向与角度块；不能一边移动点、一边沿用原点对应的旧射线。仅作坐标变换时则一致变换点、原点和方向。这样只保证模型输入内部一致，不把几何扰动重新命名为物理真实的传感器采样；正常辅助监督仍来自未修改扫描。

本版不新增独立生成模型、时序输入、相机、语言模型、法线真值、完整正常三维真值或逐场景优化。推理只有一套网络、一次主干前向，所有目标块预测并行/分块执行。

“两次遍历”和第 14 节的十次遍历均是既往实验预算，不是新解码器已经收敛的证据。后续按已确认阶段分别报告新模型的首轮 STU 验证评价、使用 val19 开发后的结果及最终隐藏测试结果。仅用 nuScenes 训练与完全不使用 STU 选择模型或参数是不同条件；历史 val19 开发经历保持披露，不因更换模型而将其重新描述为从未接触的独立测试集。

**9.1．从研究目标出发的数据体系、数量预算与实际产出**

本节预算已按用户授权实施，生成器、训练组批及开发选择范围已同步更新；预算表表示事先约定的请求上限，实际成功数量另表报告。实现和数据一致性已经核验，异常多样性是否足够、是否改善真实分割性能仍未证实。

改版前复核发现：训练的 1,270,346 个正点中，1,154,999 个处于 10 米以内，约占 90.9%；30—40 米有 311 个，40—50 米仅 8 个。按每条记录的正点距离中位数统计，至少 30 米的训练记录只有 56／19,907，开发只有 6／4,009。当时实际使用的异常供体片段为训练 113 份、开发 25 份，它们还不是经实例轨迹核验的独立物体数。这些事实否定了“继续沿原分布增加合成次数即可充分覆盖”的判断，但不证明本轮修改必然提高检测性能。

研究要检验的是：在单帧实际点特征之外，目标区域不可见的正常回波预测能否帮助区分异常与难以预测的正常结构，并在新供体和稀疏观测上保持逐点排序能力。数据生成不读取模型异常分数、正常场残差或 STU 样本来决定标签。异常定义继续限定为明确插入的道路障碍物，原始 debris 和其他忽略类别维持忽略；使用语义标签筛选供体属于有语义来源的合成异常监督，不能称为完全没有利用异常素材。

| 数据内容 | 为现行模型提供什么 | 应避免的解释错误 |
|---|---|---|
| 全部原始已标注扫描中的可靠正常点 | 学习正常判别，以及由块外上下文预测正常回波 | 原始扫描并非每个点都正常，忽略点不补成负例 |
| 一个明确道路障碍物的可见插入点 | 训练最终异常头及同一分数的 AP、AUROC、FPR95 排序 | 标签不由高度、距离、稀疏程度或预测残差定义 |
| 相同生成过程产生的正常物体移植 | 使复制痕迹、表面插值及道路内凸起不能单独决定异常 | 正常控制不能始终是近处大汽车而异常始终是小碎片 |
| 远距路人、正常锥桶、护栏、路缘、遮挡边界等真实正常点 | 学会保留难预测的正常结构，约束高召回时的误报 | 不能将正常场低似然直接当成异常真值 |
| 修改帧对应的未修改扫描 | 给正常场提供原始上下文与原始正常返回的辅助监督 | 当前实现是另一趟原始扫描辅助似然，不是合成点同坐标的正常反事实真值 |

所有修改帧仍按单帧真实接收方向形成观测；配对参考仅用于训练。物体位置不依照模型的 1°、2°、4° 块边界安排，也不要求物体完整落在某个留出块内。记录角跨度并在评价时检查跨块情况，避免生成器人为保证正常场永远看到无污染上下文。首轮每个异常版本只插入一个物体，不以增加物体数量、正点比例或合并多个稀疏物体来提高 AP 或凑足五点条件。

**原始资源、物体身份与可用表面。** 本地已有完整的 34,149 个已标注关键帧及 297,737 个额外 LiDAR 扫描；后者分别有 245,255 个属于训练、52,482 个属于开发，缺少现成逐点语义标签。本轮使用全部已标注关键帧，并检查候选实例在这些关键帧中的连续观察；没有使用额外未标注扫描。原先提出的相邻未标注扫描供体扩展尚未验证表面归属，不能把它描述为已经实现，也不能直接把框内点补成物体真值。每份可用表面始终来自一个实测扫描。官方逐点标签与关键帧的对应关系、实例和轨迹结构见 [nuScenes 数据结构](https://raw.githubusercontent.com/nutonomy/nuscenes-devkit/master/docs/schema_nuscenes.md)。

下表是原始元数据中可按实例轨迹管理的候选资源，不是已通过几何与语义核验的供体数。框内回波可能来自地面或背景，不能将整个三维框内的点直接赋予物体标签；逐点语义与框标注也不是完全一一覆盖。

| 候选类别 | 训练实例／框标注 | 开发实例／框标注 | 至少一框有 8 个 LiDAR 点的训练／开发实例 |
|---|---:|---:|---:|
| debris | 124／2,396 | 40／620 | 106／29 |
| pushable_pullable | 1,473／21,273 | 211／3,332 | 1,338／190 |
| 合计 | 1,597／23,669 | 251／3,952 | 1,444／219 |

本轮已将 pushable_pullable 中可分离且不含使用者的物体纳入候选，以增加垃圾桶、购物车、手推车等实际形态。它们只有在明确的道路障碍物插入中才提供正标签，原场景仍忽略。现行正常类别中的锥桶、护栏、车辆及行人保持正常，并提供正常移植控制。不能将所有忽略类别自动改为异常。

| 通过语义归属、支撑与实测表面检查的类别 | 训练标注实例／保留观察 | 开发标注实例／保留观察 |
|---|---:|---:|
| debris | 51／238 | 12／39 |
| pushable_pullable | 1,233／5,417 | 175／742 |
| 异常候选合计 | 1,284／5,655 | 187／781 |
| 正常控制合计 | 1,029／4,257 | 256／1,039 |

异常候选检查覆盖上述全部 1,597／251 个标注实例。正常控制采用预先确定的有限候选库：每个映射为正常的候选类别，训练最多选 256 个、开发最多选 64 个实例，合计检查 1,945／470 个；因此正常控制核定数不是全部 nuScenes 正常物体的资源上限。供体被拒绝的主要原因是框内或语义交集点数不足、局部支撑面不可靠、没有符合邻接规则的实测三角面；逐项数量保存在 `census.json` 的 `extraction_failures`，这些数是被拒绝的单帧观察次数，不是被拒绝实例数。

先用实例轨迹关联同一物体，再以该帧的实际语义点确认表面归属。无可靠身份的片段不进入声称“独立实例隔离”的供体库。每实例首轮最多保留 6 份具有不同可见表面或观察方向的实测观测；6 是抑制连续帧重复与控制计算量的预算上限，不代表每个物体必然有 6 个视角。几何始终保留真实尺寸；不靠任意拉伸、旋转开放背面或补未观测表面扩大形状数。低矮物体应依语义可靠性、地面估计误差及跨观察一致性判断，不再将“最高点不足 15 厘米”本身作为删除理由。正常供体采用相同的表面构建和可见性流程。

旧生成器将放置距离限制在源观察距离的 0.75—1.25 倍。本轮已取消该比例限制，改为目标物体中心距不小于源观察中心距，并检查实测表面的观察侧、道路支撑和遮挡：近处可靠表面可以在更远处重新接受稀疏射线采样，稀疏远距表面不能自动视为完整近距表面。每实例以实测表面较充分的观察为起点，后续选择与已选观察方向尽量分离、至少相差 3° 的观察，最多六份。跨距离移植的强度仍是源传感器实测值的插值，尚不是经过标定的距离衰减模拟；正常控制共用此过程，不能将这种移植称为完整的传感器物理仿真。

**形状记忆的控制与检验。** 不能保证网络完全不记形状。可控制的是身份交叉、近重复形状和曝光次数，可检验的是移除这些重复来源后性能是否仍成立。

官方训练与开发场景、日志保持隔离。同一官方实例的全部已使用关键帧观察、表面及移植变体归到同一物体身份下；没有可靠依据的不同实例不擅自合并。官方实例标识不跨场景保持，故不同 token 不能作为绝对独立真实物体的证明。

原计划尝试从具有开发实例的可靠几何组中留出约 20%。实际核定否定了当前自动归组判据的科学充分性：同实例近视角观测的表面误差只反映重复性，不能校准不同实例之间的形状误匹配率。开放表面的相似平面也不代表完整物体相同。首次单链接分组把 1,471 个可用异常实例合成 56 组，最大组 1,395；改为固定代表直接比较后仍有最大 483 成员的候选组，对留出近邻的排除删除了 5,174／5,655 个训练观察。这些结果不能被解释为发现了可靠形状家族，也不能用它们删除约 91.5% 的主任务资源。两次未发布的生成均已废弃，相关分组和排除实现已经删除。

当前执行既定的“不可靠归组不进入留出结论”退路：所有通过语义、实例归属与表面检查的对象仍参与主任务，按官方标注实例分配请求；`geometry_group=null`、`geometry_reliable=false`、`shape_holdout=false` 明确表示几何身份未知。预留的 600 次几何留出请求全部记录为 `geometry_holdout_unavailable`，不以普通供体补齐。没有读取 STU 或模型分数来改变这一决定。若将来要建立形状留出，需要先有独立的跨实例相似与不相似证据；本次数据不声称完成了这一验证。

当前开发只支持官方场景、日志及标注实例隔离的检验，不支持可靠新几何组的结论。官方 nuScenes 预训练以及保留的原始背景，仍可能包含同类或相近形状，即使这些点未被标为异常。数据统计分别报告标注实例、有效观察和生成次数，不能以同一供体的新位置或新网格编号增加独立形状数。

训练请求按实例和有效观察分配，避免高质量大物体因容易放置而占据多数记录。参考与距离覆盖请求交错分配，防止任一角色先耗尽共享供体预算。同一实例、同一实测观察、同一距离层首轮最多分配两个不同接收背景的生成请求；每实例最多分配 6×5×2＝60 次请求，失败也占用预算。实例预算在生成前确定；放置失败不通过扩大少数容易供体的份额补齐。这个上限限制重复，并不证明 60 次不会过拟合。每个固定记录在一次数据遍历中最多访问一次，后续多遍访问另行计数。

**训练数量预算。** 采用全部 28,130 个官方训练关键帧，预先划分为两个互不重叠的 14,065 背景组，分别承担参考异常和距离覆盖请求，每个背景最多请求一个异常版；再从全部背景中按场景均衡选取 14,065 个，各请求一个正常移植版。生成的供体必须来自不同接收场景，并保持整个官方分区的来源边界。

| 训练用途 | 固定原始记录／生成请求预算 | 数量依据 |
|---|---:|---|
| 原始背景与正常场学习 | 28,130 | 全部官方训练关键帧，各保留一次 |
| 不按距离过采样的异常请求 | 14,065 | 占异常请求的一半，按物体和场景安排，保留未经距离配额修改的参照 |
| 按距离覆盖的异常请求 | 14,065 | 占另一半，五个距离层各 2,813 次 |
| 正常移植控制请求 | 14,065 | 每两个异常请求配置一个控制请求，使用对应背景和同一生成过程 |
| **记录上限** | **70,325** | 原始记录加全部请求均形成有效新观测时的上限 |

70,325 比改版前 54,622 条记录约多 28.7%，本轮实际生成 47,576 条。预算用于分配物体和观测覆盖，70,325 不是达到 SOTA 所需的样本量。50% 距离覆盖与 1:2 控制请求比例是本轮预先约定的资源分配，没有文献或现有训练结果证明其最优。“不按距离过采样”的参考请求按接收扫描真实道路点的距离直方图抽取距离层，也不表示恢复了真实道路异常发生率，因为背景和供体本身仍经过任务筛选。

| 目标放置距离 | 覆盖部分训练请求 | 开发覆盖请求 |
|---|---:|---:|
| [2.5,10) 米 | 2,813 | 400 |
| [10,20) 米 | 2,813 | 400 |
| [20,30) 米 | 2,813 | 400 |
| [30,40) 米 | 2,813 | 400 |
| [40,50] 米 | 2,813 | 400 |
| 合计 | 14,065 | 2,000 |

训练计划中明确分配给 30—50 米的请求为 5,626 次，占全部异常请求的 20%；另一半参考请求还可能产生远距观察。请求的距离按传感器到放置物体中心的欧氏距离安排，生成结果另按有效异常点的实际距离统计，不能把物体中心距离当作异常点距离，也不能把请求数当成功数。距离层是覆盖设计，不是异常定义、难度真值或 STU 分布估计。

每个请求最多尝试 16 个合法位置，保持所分配实例、观察和距离层；没有支持位置即记录失败，不沿用“不断换成容易物体直到成功”的策略。分别记录无合法位置、无回波命中、仅区间外替换、1—4 个有效正点和至少 5 个有效正点。异常请求若没有有效正点，不新增训练记录，原始背景仍保留；这是一项样本选择规则，并不意味着区间外的回波一定没有变化。正常控制则须产生有效的移植正常点，不能用异常点数为零将其删除。训练保留 1—4 个正点，正式评价仍只选全扫描至少五个异常点的记录。禁止为满足距离配额而放大物体、复制点、补射线，或者叠加多个小物体凑点。

每层接受量同时受请求预算、真实表面支持和实例重复上限约束。如果有效供体不足，生成数量应低于上限并公开缺口；不能将近距重复样本计入远距覆盖。正常控制在有共同几何支持的条件下匹配异常的距离、有效移植点数、角跨度和强度条件，点数不是全扫描的正常点总量；无法匹配的部分单独报告，不能为匹配而改变物理尺寸或任意删补回波。1:2 只规定控制与异常的请求比例，不保证接受记录数或点数仍是此比例。正常类别的固有形态差异不要求抹平。

距离之外，记录每对象的有效正点数 0、1—4、5—15、16—63、至少 64，以及实测高度、角跨度、背景语义和可见性条件。它们用于覆盖统计和组批；不对全部属性建立要求等量的笛卡尔积，也不作为模型输入或人工异常打分。低矮、稀疏与远距并非同一个属性。绝对点数变化不能替代实例、视角与几何组覆盖。

**点级学习的数量与组批。** 当前 BCE 在有效批次内分别对正常点和异常点取均值，排序锚点也按异常点抽取。近处大物体的几百个点会比远处的几个点占更多正类梯度；若远距只占全部正点不到 5%，整体 95% 点召回甚至可以漏掉全部远距正点。因此，帧级距离配额还需结合每层正点总量、实例数及实际批次组成解释，不能宣称按帧均衡后问题已经消失。

训练组批代码已实现：在同一训练序列中，用至多 20% 的更新安排远距批次，其中异常记录的有效正点距离中位数至少为 30 米，保留整帧及全部原有有效标签，不为分组屏蔽跨越距离边界的正点；同时加入实际正常背景和正常控制。其余更新混合距离，为同一评分函数提供跨距离监督，但不据此宣称已经保证评分校准。困难批次中的样本优先按相近可见正点数量组合，减少同批次一个密集物体压过多个稀疏物体。20% 是上限，仍受不放回访问、实例上限和实际远距产能约束；不足时报告实际比例，不反复复制少数远距物体凑满，也不追加总更新量。有效批次仍为 8，微批次仍只负责显存调度，当前损失公式、模型结构和官方评价不因此改变。分层采样主动改变的是训练关注权重，不应声称与原随机采样具有完全相同的统计目标。

令最终训练记录数为 N，完整遍历 E 次，扫描访问量为 EN，更新数为 ceil(EN/8)。若达到记录上限，一次遍历为 70,325 次扫描访问、8,791 次更新；其中至多有 42,195 次额外原始配对读取，只额外运行正常场辅助路径，不是额外完整骨干样本。每个原始背景在一次遍历中最多承担三次正常辅助目标：原始记录一次、异常版一次、正常控制版一次。数量报告必须披露这类重复权重。训练遍数单独确定，不能将重复遍历计为新增独立数据。

本轮实际 N＝47,576，一次完整遍历为 5,947 次更新，包含 19,446 次修改帧对应的额外原始配对读取。以种子 0 生成一次遍历的顺序检查时，47,576 条记录各访问一次，安排 318 个远距批次，占更新数 5.35%；没有复制样本补到 20%。这些是训练顺序核验结果，不是已经执行的模型训练。

**开发数量与评价用途。** 使用官方 150 个开发场景、6,019 个原始关键帧。预先分配 2,000 个背景用于主开发异常请求、2,000 个用于距离覆盖异常请求、2,000 个用于正常控制请求，三组接收背景互不相同；剩余背景仍保留原始记录。分配在各场景内尽量均衡且先于生成结果，各角色供体均只来自官方开发分区，不把训练供体移入开发。各组属于同一个开发设计，不假称为三个独立测试集。

| 开发用途 | 原始记录／请求预算 | 使用方式 |
|---|---:|---|
| 原始正常观察 | 6,019 | 检查正常判别和正常场行为，不混入官方式异常帧总体 |
| 主开发异常请求 | 2,000 | 不按距离过采样；在固定成功记录中按官方帧和点规则汇总三指标，选同一个模型 |
| 覆盖诊断异常请求 | 2,000 | 五距离层各 400；查看远距、低点数和留出几何组表现，不与主集拼接制造更高 AP |
| 正常移植控制请求 | 2,000 | 其中 1,000 个请求匹配主集条件、1,000 个匹配覆盖条件；用主集确定的工作阈值检查误报 |
| **记录上限** | **12,019** | 三组生成请求均成功时的上限，原始背景只存一份引用 |

覆盖诊断中每个距离层预留 120 个留出几何组请求，合计 600 个；它们包含在 2,000 个覆盖请求中，不是额外增加 600 帧。当前几何留出无法可靠核定，这 600 次全部保留缺口；其余 1,400 个请求使用官方开发分区内未用于训练的标注实例。不能把这些普通实例重新命名为新形状家族。

**预算执行与实际距离产能。** 本轮训练 42,195 次、开发 6,000 次生成请求全部得到成功或失败结果，未追加请求凑数。生成前按源观察距离核定的异常实例／观察数如下；它只是“该表面允许尝试放到这一层”的必要条件，不等于该层一定有合法道路位置或可见回波。各层复用同一实例，不能跨行相加为独立物体数。

| 目标中心距离层 | 源观察条件允许尝试的训练实例／观察 | 开发实例／观察 |
|---|---:|---:|
| [2.5,10) 米 | 1,102／3,376 | 139／408 |
| [10,20) 米 | 1,264／5,494 | 178／727 |
| [20,30) 米 | 1,281／5,646 | 186／779 |
| [30,40) 米 | 1,284／5,655 | 187／781 |
| [40,50] 米 | 1,284／5,655 | 187／781 |

| 用途 | 训练预算／实际记录 | 开发预算／实际记录 |
|---|---:|---:|
| 原始背景 | 28,130／28,130 | 6,019／6,019 |
| 参考异常 | 14,065／7,628 | 2,000／1,084 |
| 距离覆盖异常 | 14,065／7,680 | 2,000／746 |
| 正常控制 | 14,065／4,138 | 2,000／355 |
| 合计上限／实际记录 | 70,325／47,576 | 12,019／8,204 |

下表按请求的物体中心距离合并参考与覆盖异常，成功要求至少一个有效异常点，五点资格按完整扫描计算。开发请求包含上述 600 次不可用的几何留出，各层各 120 次；表中没有把它们扣掉来提高成功比例。

| 目标中心距离层 | 训练请求／成功记录／五点合格 | 开发请求／成功记录／五点合格 | 成功使用的训练／开发实例 |
|---|---:|---:|---:|
| [2.5,10) 米 | 14,476／6,599／6,255 | 2,041／793／732 | 1,098／139 |
| [10,20) 米 | 4,813／4,670／3,064 | 696／562／360 | 1,258／175 |
| [20,30) 米 | 3,155／2,666／262 | 453／304／55 | 1,169／154 |
| [30,40) 米 | 2,862／1,305／19 | 410／164／19 | 840／104 |
| [40,50] 米 | 2,824／68／0 | 400／7／0 | 66／7 |

远距确实是实际产能约束：训练 40—50 米的 2,824 次异常请求中，976 次没有该距离的道路候选，1,660 次未找到满足支撑及碰撞条件的位置，119 次没有可见命中，1 次观察侧不支持，68 次成功但全为 1—4 个有效异常点。30—40 米训练的 1,305 条成功记录中也仅 19 条有五点评价资格。以上是固定请求与至多 16 次候选位置下的实测产出，不是穷举全部合法位置后的物理容量上限。

| 有效异常点本身的距离 | 训练正点 | 开发正点 |
|---|---:|---:|
| [2.5,10) 米 | 276,186 | 34,753 |
| [10,20) 米 | 41,022 | 5,656 |
| [20,30) 米 | 7,097 | 1,024 |
| [30,40) 米 | 2,085 | 349 |
| [40,50] 米 | 89 | 13 |

中心距离表与点距离表描述不同对象，边界附近的点可跨层，不能互相替代。按有效异常点距离中位数至少 30 米计，训练有 1,264 条、开发有 155 条，其中五点合格分别只有 20、15 条。实际 30—50 米正点合计训练 2,174、开发 362，分别仅占全部正点约 0.666%、0.866%，近距点主导尚未消除。数据已经增加了有可追溯实例的稀疏远距观察，但远距主评价证据仍不足，也不能把当前正点比例当作 STU 的比例。

训练控制的 4,138 条成功记录中，1,117 条对应同背景成功异常，并通过同距离层、有效点数比、可测角跨度比和强度区间的匹配约束；其余 3,021 条所在背景的异常请求未成功，作为普通正常移植保留，不能声称也完成了属性配对。开发 355 条控制均通过匹配约束，控制接收背景与异常背景分开。所有控制都共用移植算子，但属性匹配与同一背景配对是两件事。配对点数及角跨度比例限定在 [0.5,2]，强度采用 10%—90% 分位区间是否重叠并允许 1/255 容差；这些是透明的近似约束，不是已证实的最优设置。

主开发集在正式训练前形成固定的评价总体。它是预先规定抽样规则、再经过插入成功和五点资格筛选的参考合成总体，不是真实异常自然分布；同时报告请求数、有效插入数、合格帧数和异常点占比，不补采至凑满 2,000 个合格帧。模型选择继续采用现有 AP 优先、FPR95 和 AUROC 依次裁决的规则，只保留同一模型的三项结果，不为三个指标分别选择检查点。覆盖诊断按层报告时，先沿用完整扫描的五点资格，再按距离筛选点；不能在每个距离层重新执行五点筛选。仅 1—4 点的扫描另行报告在主集同一工作阈值下的检出情况，清楚标为补充诊断。纯正常与正常控制的误报使用主集正点确定的同一个 95% 召回工作阈值，不给每层另调阈值；误报率以有效正常点为分母，分别报告原始正常点和移植正常点，不能混入忽略点。

AP 受异常点比例影响；AUROC、FPR95 也会随正常和异常的条件分布改变。后续方法比较必须使用同一开发记录与标签总体，不能将增加异常数量或改变远近比例后的 AP 与原开发 AP 直接解释为方法提升。距离平均、实例平均和几何组平均只用于解释，不替代官方逐点汇总三指标。同一物体的重复观察不能作为独立样本估计置信度；需要不确定性时，以来源日志或物体组为重采样单位。同一生成器的源域开发不能自动证明对新生成机制或真实 STU 的泛化。

**模型作用的最小检验与依据。** 首次主模型训练后，可以在预先选定的开发异常和正常控制上，保持实际点特征、标签和权重固定，仅在相近距离条件内打乱完整正常预测与查询点的对应关系；同一预测内部的假设、核与先验结构保持不变。这个额外推理用于判断最终检测是否依赖正确的正常预测，不新增模型或训练队列。它只提供功能依赖证据，不能单独证明相对其他架构的收益。若使用该诊断修改方法，相关样本继续属于开发数据，不能改称独立测试。

本规划参考了相邻工作的具体问题：[LiON](https://arxiv.org/html/2309.10230v3) 指出缩放已知物体可能造成点密度捷径，并强调物体来源多样性；[LIDO](https://arxiv.org/html/2604.23604) 在合成中处理线束采样和遮挡，同时报告远距性能下降与跨传感器差异；[NDP](https://arxiv.org/html/2604.09232v2) 讨论少量被忽略物体反复作为异常来源的过拟合风险，其附录也承认没有显式遮挡的合成仍可有效。这些证据支持控制合成捷径和检验泛化，不能为本节某个数量、比例或 SOTA 结果背书。nuScenes 的 32 线观测不会因多帧供体构建就成为真实的 128 线单帧观测；跨传感器结果仍需后续真实评价。

本节的实例核定、预算内生成、训练组批实现及独立读回已完成。11 项生成器与边界测试通过；训练相关测试 49 项通过，另有针对完整遍历顺序与主开发选择范围的检查。现有证据支持标签与输入的一致性、标注实例隔离及所列覆盖数量；不支持“新形状留出已经建立”“40—50 米五点评价充分”或“SOTA 已可实现”。下一项能够检验模型竞争力的动作，是按现行模型在这套数据上真实训练，并在同一主开发总体报告三指标与远距补充诊断；若要继续扩展数据，应针对已测出的远距支撑和未观测表面缺口取得新证据，不能只追加随机插入次数。本轮未启动该训练。

**9.2．正常背景的训练与验证划分（已完成）**

按逐步构建的要求，本步只建立正常背景，当前产物为 `results/data/background/train.json` 和 `results/data/background/val.json`。两文件直接引用 `/home/jasongao/Data/Nuscenes` 的原始扫描与逐点标签，未读取旧合成数据、提取供体、生成异常或启动训练。后续物体与异常构建使用这里确定的官方分区边界。

| 已核实对象 | 训练 | 验证 |
|---|---:|---:|
| 官方场景 | 700 | 150 |
| 采集日志 | 50 | 18 |
| 已标注原始关键帧 | 28,130 | 6,019 |
| 原始有效回波 | 976,677,792 | 208,978,400 |
| 2.5—50 米内的正常监督点 | 668,791,803 | 144,251,466 |
| 2.5—50 米内的忽略类别点 | 2,584,428 | 562,786 |
| 监督距离范围外的点 | 305,301,561 | 64,164,148 |
| 异常正点 | 0 | 0 |

划分单位为官方场景，不随机打散相邻帧；采集日志也互不重叠。全部关键帧保留一次，没有按距离、场景难度或模型得分筛掉背景。训练与验证均覆盖 Boston Seaport 和 Singapore 的 Onenorth、Queenstown、Holland Village 四个采集区域，因此不声称地理区域完全隔离。

“正常背景”指用于正常监督的原始扫描，不表示扫描内每个点都已确认为正常。沿用当前 20 类已知正常、12 类忽略的定义；`debris`、`pushable_pullable`、噪声和其他忽略类别不改成正常。只有实际回波、处于 2.5—50 米且属于已知正常类的点参与正常监督。忽略类别和距离范围外的点仍保留为模型可见上下文；XYZ、原始点顺序保持不变，强度沿用原读取器除以 255 的归一化。全量实读未发现 XYZ 全零空槽。

| 正常监督点的实际距离 | 训练 | 验证 |
|---|---:|---:|
| [2.5,10) 米 | 409,109,861 | 87,390,323 |
| [10,20) 米 | 150,437,481 | 33,254,004 |
| [20,30) 米 | 60,442,328 | 13,192,083 |
| [30,40) 米 | 31,247,997 | 6,685,817 |
| [40,50] 米 | 17,554,136 | 3,729,239 |

两文件中的 `summary` 保存原始类别点数、监督范围、正常点距离分布及采集区域帧数；`split` 保存场景和日志身份；`records` 保存每帧原始扫描、标签与位姿。各记录 `role=original`、`anomaly=0`，不包含合成替换。验证记录的 `eligible=False` 仅表示不满足“至少五个异常点”的官方异常检测评价条件，不影响它作为正常预测与误报分析的输入；本步不能据此计算有意义的 AP、AUROC、FPR95。

现有 `src.data nuscenes` 入口新增 `--background-only`，在供体提取之前返回，输出目录须为空。执行命令为：

```bash
OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 \
/home/jasongao/Study/AJAE/.venv/bin/python -m src.data nuscenes \
  --background-only --nuscenes-root /home/jasongao/Data/Nuscenes \
  --output results/data/background --workers 12
```

实际构建耗时 22.59 秒；12 个 CPU 工作进程每个底层数值库单线程，没有使用 GPU。构建前可用内存约 22.43 GB、Windows E 盘剩余 51,875,409,920 字节；两份输出共 31,158,600 字节，没有复制原始点云。

独立身份核验将新文件与官方场景列表和原始元数据比较：每场景帧数全部符合 `nbr_samples`，两集并集覆盖全部 34,149 个原始样本；扫描编号、样本编号、文件路径及帧索引集内唯一，两侧交集均为零。另以正式 `Scans` 逐条读回全部 1,185,656,192 个原始回波，并从原文件独立计算期望标签，逐元素比较坐标、归一化强度、原始槽位及正常／忽略标签，失败为零；各类和各距离汇总均一致，耗时 19.33 秒。两项针对背景入口范围、边界标签及原始上下文保留的测试通过。这些结果支持本步划分与监督语义正确，不证明异常物体覆盖、模型收敛或跨数据集性能；后续步骤仍分别开展。

**9.3．首批真实异常候选物体的选取（已完成）**

本步按用户明确的“几十个不同物体即可，同类别可以有多个”执行。依据 nuScenes 原始逐点标签、官方实例框和原始相机图像，从 70 个实际复核的候选中选出 56 个标注实例，每实例保留一次原始点云观测。这里只完成对象与点的选取，不实施路面放置、表面补全或合成训练。图像用于离线核定对象，不作为点云模型的新输入。

选取来源为 `animal`（原编号 1，16 个实例）、`movable_object.debris`（编号 10，13 个）和 `movable_object.pushable_pullable`（编号 11，27 个）。这三个标签是定位候选的入口，接收决定还要求具体物体可辨，不能把整类所有点直接视为异常。STU 的正常语义采用 SemanticKITTI 的 19 类；其他车辆包括多种车辆，围栏覆盖部分隔离设施。因此警车、施工车辆、护栏，以及杂物标签内的树枝、交通标志，均不能只凭 nuScenes 标签名称不同而纳入。无人乘坐、用于人工搬运货物的器具按本研究语义核定接收；不是 STU 官方发布的逐类对应表。依据为 [SemanticKITTI 官方类别映射](https://raw.githubusercontent.com/PRBonn/semantic-kitti-api/master/config/semantic-kitti.yaml)、[原论文类别定义](https://arxiv.org/html/1904.01416v3#A3)和 [nuScenes 官方标注说明](https://raw.githubusercontent.com/nutonomy/nuscenes-devkit/master/docs/instructions_nuscenes.md)。

| 实际物体组 | 训练实例 | 验证实例 | 本批包含的外观 |
|---|---:|---:|---|
| 动物 | 16 | 0 | 犬、鸥类、公鸡、雁鹅类；物种细分为图像判读 |
| 袋包 | 9 | 1 | 软质袋或袋组、背包 |
| 垃圾容器 | 11 | 11 | 大型垃圾箱、罩顶桶、平盖带轮桶、街旁小桶、开口塑料桶 |
| 旅行箱 | 0 | 1 | 软壳拉杆旅行箱 |
| 办公椅 | 1 | 0 | 无人办公转椅 |
| 工具箱 | 1 | 0 | 带脚轮金属工具箱 |
| 搬运车 | 2 | 3 | 两轮手扶搬运车、料斗式搬运车、购物小拉车 |
| 合计 | 40 | 16 | 56 个官方实例标识 |

训练侧来自 35 个场景、19 个采集日志，保留 2,788 个原始点；验证侧来自 14 个场景、8 个采集日志，保留 2,575 个原始点。合计 5,363 点，每份观察 8—601 点，保留原始传感器坐标、强度及线束编号。训练与验证归属继承已确定的正常背景分区，不将验证侧物体挪入训练。当前份数是选型结果，不能当作足够训练、类别平衡或未见形状评价已经成立的证据。

点提取同时要求原始语义标签属于目标类别、世界坐标位于目标有向实例框内，并排除同类别其他实例框的重叠区域。没有将整个框内点都当成目标：入选办公椅框内 198 点，只有 114 点属于目标类别，另外 84 点为地形；入选旅行箱框内 28 点，目标只占 16 点，另外 12 点为人行道。所有提取点均保留原扫描行号，不缩放、不补点、不重采样。原背景中的这三类仍保持忽略，本步没有把原场景物体改标成异常；只有后续明确合成的道路内障碍才提供正标签。

14 个候选没有进入本批点云包。原因包括图像不清、投影与人体或邻接家具混叠、疑似跨场景重复，以及具体语义尚未核定。例如另一只硬壳箱的部分原标签为 11 的点投影到人的上身附近，故不能只凭标签接收；一件疑似清洁推车的物体在完整施工场景图中更像施工设备，已撤销该细分名称。两件红架黑斗器具疑似同一实物跨场景重复，均未计入本批。图像复核为 AI 判读，不是新的人工作业真值；相机与激光扫描时间差及原标注噪声仍然存在。

当前对象目录为 `results/data/objects/catalog.json`，记录原始实例、帧、框、点索引、相机证据、分区、具体外观及暂缓原因。`preview.png` 展示全部 56 个入选对象的真实照片和点投影；中文使用宋体，英文与数字使用 Times New Roman，已检查实际字体、中文字符覆盖及渲染。`points.npz` 保存按实例编号索引的原始五列点数组，按现有忽略规则仅保留在本地；目录中的原文件路径和行号可用于重建，不需要该压缩包才能追溯。

独立核验重新读取全部入选原始扫描，并独立重建有向框的坐标变换与语义交集；56 份观察来自 54 个原始扫描，5,363 个点索引全部一致，点云包与原文件对应行逐字节相同。入选点没有重复行，训练与验证的实例、扫描、样本、场景和日志交集均为零。核验确认提取与分区正确，不替代人工逐点标注。

不同官方实例标识不保证跨场景为不同实物，同款容器也可能有相近形状；本批未完成跨分区近重复几何排查，不能写成 56 种独立形状。[STU 论文第 4.1 节](https://arxiv.org/html/2505.02148v1)说明其异常对象避免与 nuScenes 的杂物、可推拉物体对象重合。因此，本批对象是辅助异常监督来源，用来学习向未见道路障碍泛化，不能宣称已复现 STU 的异常正类，也不构成三项指标提升的证据。

**10．计算、精度与必要的工程核验**

默认每点三个尺度、每尺度四假设八核，约 96 次高斯—射线解析计算；十万点约 960 万次，不是 96 次骨干前向。场参数分解按块缓存，逐点射线与注意力按 2,048 点一块执行，训练时重计算中间激活。现行完整模型实测共有 13,215,474 个参数，其中兼容性模块为 10,592 个；比升级前完整场模型的 13,208,962 个增加 6,512 个，约 0.0493%。相对最初实际观测模型的总增量为 334,961 个，仍在原定 200 万以内。参数量是实现规模，不能据此推定运行耗时或检测收益。

**有效批次排序的显存实现。** 若无法保留八扫描的全部激活，采用已有 Gradient Cache 原理：先在参数不变时分微批次前向收集点分数；在合并分数上计算检测损失及各分数梯度；再重算各微批次，用缓存的分数梯度向完整网络反传，同时计算该微批次正常辅助损失；最后统一更新一次参数。只跨微批次保留分数、梯度及必要状态，不保留全部骨干激活，不使用跨更新的陈旧分数队列。

这仍是端到端链式求导，不是对正常分支停止梯度。等价性要求重算使用相同增强、抽样、随机状态和输入，参数在两次前向间不变；BatchNorm 等可变状态只按一次逻辑前向更新，不能重复累计。数值不一致时应先修复，不能声称已复现完整有效批次梯度。该方案增加一次训练前向，额外时间计入预算，不增加推理前向次数。

这里的等价对象是“保持相同微批次划分与前向行为，将这些分数合并后求一个联合损失”的梯度。训练态 BatchNorm 使用当前批次统计，因此这不自动等价于把八帧一次性输入骨干的前向与梯度。实现不因此替换已有骨干归一化层；只要求固定的微批次前向与重算一致，以及排序损失的有效批次范围一致。正常预测分支继续采用逐点/逐 token LayerNorm。

结构允许骨干使用 AMP，但升级前实现经独立进程重载核验后采用全模型 FP32，现行模型保留这一数值设置。协方差求解、射线密度、log-CDF 差、logsumexp、兼容性头、最终线性层及排序代理均使用 FP32。仅在最后把 BF16 输出转为 FP32，不能恢复已经丢失的排序精度。骨干每次处理最多两个原有注意力窗口，并在反向传播时重算注意力矩阵；窗口大小、窗口内的点及注意力公式均保留。

尾部概率采用稳定 log-CDF 差；$1-\exp(-H)$ 用稳定 expm1/log 形式。极小 $H$ 时利用归一化极限，不能把所有低质量或远尾预测统一截断为同一个常数。

例如取 $s=\max_m\log h_m$、$\lambda=e^s\bar\lambda$、$H=e^s\bar H$，计算：

$$
\log p(r)=\log\bar\lambda(r)-\log\bar H(U)-H(r)
-\log\!\left(\frac{1-e^{-H(U)}}{H(U)}\right).
$$

最后一项在小 $H$ 时使用展开式，其内层对数为 $-H/2+H^2/24+O(H^4)$。协方差的数值下限不能被宣传为物理噪声水平。

最初方案审定时的独立 NumPy/SciPy 标量数值核验得到：三维场与一维射线表达相对误差约 $3.1\times10^{-14}$；解析积分误差约 $1.3\times10^{-15}$；条件密度及混合密度积分误差低于 $5\times10^{-16}$；重叠核交换次序结果不变；极低幅度及远尾 log 密度保持有限。审定时另行核算了上述分组与合并排序反例。这些数字属于实现前的数学核验；升级前 PyTorch 实现的检查及真实扫描执行另列于第 13 节，不能作为新解码器已完成核验的记录。

以上只验证数学计算，不验证真实网络梯度、目标泄漏、GPU 可重复性、开销或检测精度。实现时需要一次必要核验：改变目标块距离不应改变相应正常场参数；异常梯度应到达正常分支；保存重载的完整分数与指标应在预设容差内一致。这些是完整模型的正确性检查，不是重新启动旧模型对照队列。

**11．相邻工作与新颖性边界**

| 工作 | 已有内容 | 本方案需要成立的区别 |
|---|---|---|
| NDP、REL | 可学习异常校准、正负证据、端到端逐点评分 | 学习的是目标不可见条件下的正常三维回波分布，并把它用于同一最终点级判别 |
| DRAEM | 重建与异常判别端到端联合训练 | 不是以“预测后再判别”作为新意，而是检验共享几何—射线约束的实际增量 |
| COVAL | 用户所给全文中的缺失位置特征重建、共同可见特征一致性 | 此处预测可归一化的真实回波距离分布，推理保留正常参照；不要求 factual/counterfactual 合成对 |
| PLiNK | LiDAR 回波的概率形式与生存概率关系 | 复用概率建模基础；本方案是上下文到正常场的前馈预测和异常判别，不逐场景拟合地图 |
| Volumetrically Consistent 3D Gaussian Rasterization、3DGEER、LiDAR-RT | 高斯表示、射线计算和渲染 | 不主张高斯观测模型本身新；增量落在正常解释学习如何服务未知异常分割 |

已核对资料：

- [NDP，正文 3.2–3.5](https://arxiv.org/html/2604.09232v2)。
- [REL，正文 3.2–3.3](https://arxiv.org/html/2511.06720v1)。
- [DRAEM，ICCV 2021 原文](https://openaccess.thecvf.com/content/ICCV2021/papers/Zavrtanik_DRAEM_-_A_Discriminatively_Trained_Reconstruction_Embedding_for_Surface_Anomaly_ICCV_2021_paper.pdf)。
- [COVAL](https://openreview.net/forum?id=Hly2QYcuLW)：此前已读取用户提供 PDF 提取文本的 3.3.1–3.3.2。
- [PLiNK，正文 II–III](https://arxiv.org/html/2411.01725v1)。
- [Volumetrically Consistent 3D Gaussian Rasterization，方法及解析积分](https://arxiv.org/html/2412.03378v2)。
- [3DGEER，ICLR 2026 官方论文条目](https://proceedings.iclr.cc/paper_files/paper/2026/hash/e112a4671e8779aa9f640a0e3f81bd26-Abstract-Conference.html)。
- [LiDAR-RT，CVPR 2025 官方论文条目](https://openaccess.thecvf.com/content/CVPR2025/html/Zhou_LiDAR-RT_Gaussian-based_Ray_Tracing_for_Dynamic_LiDAR_Re-simulation_CVPR_2025_paper.html)。
- [Smooth-AP，作者项目页及论文](https://www.robots.ox.ac.uk/~vgg/research/smooth-ap/)：可微 AP 排序代理已有基础。
- [Gradient Cache，原文 3.2–3.3](https://aclanthology.org/2021.repl4nlp-1.31.pdf)：复用链式求导分解控制有效批次学习的显存；原文的 NLP 性能与时间结果不能直接外推到本模型。
- [PyTorch BatchNorm 官方说明](https://docs.pytorch.org/docs/stable/generated/torch.nn.BatchNorm1d.html)：用于核对当前批次统计和运行统计的区别，不意味着修改项目依赖版本。

上述资料记录最初架构审定时对核心相邻机制的核查，其中当时补核了梯度缓存原文及 BatchNorm 官方说明；本节不是后续文献调研的完整目录，也不构成无遗漏证明。实现和写作必须明确引用这些基础；没有检索到完全相同的组合，也不足以独立证明会议级创新。

**12．最终判断**

本版是现行主模型。此次小范围升级保留实际观测路径与正常几何学习，只让兼容性解码按核属于哪个正常假设来读取分布，并加入每个完整假设的条件距离概率与先验。该结构与正常场的假设定义一致，但这种一致性尚不是精度提高的实验证据。

剩余的不确定性集中在三个实质问题：块外信息是否足以预测复杂正常结构；新增分支是否学出有用且可迁移的判别表示；收益是否值得计算开销。前者不会因换成更大生成器自动解决，后两者也不能由架构推理保证。

对本模型的任务判断保持统一：辅助预测更准但最终三指标不改善，不能算检测贡献；最终三指标改善但正常分支未提供可验证作用，也不能据此认定所主张机制成立。端到端训练和更少手工规则是设计要求，不是性能或新颖性的证明。后续先完成唯一完整模型的实现与主训练，机制归因不再扩展成训练前的多模型门槛。

“尽量没有手工设计”在这里落实为：不手工规定什么几何是异常，不设异常高度/尺寸/污染比例，不手工组合多项分数，不用后处理修正逐点判断。保留的人工选择是网络容量、留出规则、观测模型、数值参数和损失权重；这些必须公开，不称为完全无先验。

仅 nuScenes 的数据体系正在按步骤重建：正常背景划分与首批真实候选物体选取已完成，当前物体尚未放置成新的训练或开发异常扫描。后续仍以同一逐点分数评价 AP、AUROC 和 FPR95。正式训练尚未启动；历史 STU 206 实验和新结构的实现检查均不能替代新模型成绩。

**13．代码落实与真实执行记录（2026-09-23）**

**历史范围：本节原样保留升级前兼容性解码器的实现与执行记录。节内“当前”“本轮”等表述指当时版本；参数、梯度、耗时、显存和重载证据不能自动外推到现行两级解码器。**

本节记录最初实现核验，当时复用 `results/data/native/train.json` 的 6,185 帧：正常 nuScenes 2,512 帧、含合成异常的 nuScenes 2,504 帧、含合成异常的 STU 720 帧、正常 STU 449 帧。沿用 `assets/val.json` 的开发集规则，未新增独立最终评测集。历史 N1 权重和结果保留；训练命令只启动完整模型。后续主实验的数据变更见第 14 节，本节的资源实测只适用于当时列出的输入。

| 实施位置 | 当前行为 |
|---|---|
| `src/normal.py` | 构造三尺度嵌套角度块，在跨块注意力前排除目标块；预测四假设、每假设八个完整协方差高斯核；解析求射线密度和共享假设联合似然；学习逐点兼容性 |
| `src/model.py` | 保留 N1 实际观测组件；拼接 163 维状态并输出逐点分数；检测梯度回传正常场和骨干，推理只有一次骨干前向 |
| `src/train.py` | 显式指定完整数据遍历次数；八扫描共同计算 BCE 和排序代理；缓存分数梯度后逐扫描重算并释放激活，再统一更新参数；正常辅助权重为 0.1 |
| `src/evaluate.py` | 保持原始回波顺序与正式指标计算；新增正常预测诊断入口，报告中位数距离误差、90% 区间覆盖率、区间宽度和联合负对数似然 |
| `vendor/litept/model.py` | 主模型使用 FP32 注意力，按完整窗口分块并重算中间矩阵，控制显存；历史模型加载保留原计算路径 |
| `tests/test_normal.py`、`tests/test_training.py` | 覆盖信息边界、解析概率、端到端梯度、八扫描共同排序、恢复训练、真实扫描及独立进程重载 |

当前数据缺少逐束发射原点，使用扫描参考原点近似；增强后从当前点坐标重新生成方向、距离和角度块。有效距离外的真实回波仍输出分数并参与上下文，但其三项距离 log 密度输入设为零，不参与正常辅助似然；这不把忽略点标记为正常。

必要核验直接对应本方案的科学含义：改变目标块距离和强度时，该块场参数逐位不变；改变目标点数时只出现既定浮点容差内的批量运算差异，目标特征对本块场的梯度严格为零。共享联合似然先累加同一假设下的全部有效射线，再对假设求和。独立数值积分核对解析密度，CPU 与 CUDA 核对极低幅度及远尾梯度。包含 BatchNorm 和随机失活的例子核对缓存求导与同一前向分组下的直接共同损失，覆盖八扫描、尾部不足八扫描和全正常批次。

实现中发现并处理了两个实际问题。混合精度版本在同进程重载时一致，但独立进程的完整逐点分数出现约 0.004 的最大差异，超过事先规定的容差；改用稀疏卷积的另一种执行算法仍未解决。因此主模型统一使用 FP32，未放宽分数和指标容差。直接保存全部 FP32 注意力矩阵又使整卡占用接近 16 GiB，该次核验已停止；改为完整窗口分块及反向重算后，完成下面的八扫描核验。以上失败不能作为模型精度收益或退化的证据。

在 RTX 5080 Laptop（16 GiB）上，按训练顺序取索引 `[6111, 356, 3132, 2560, 6048, 2867, 6124, 4326]`，共 551,583 个真实回波。其中监督正常点 493,530 个、异常点 585 个，五帧具备正常辅助监督资格。执行一次完整共同损失的反向传播、不更新参数，耗时 53.78 秒，PyTorch 实际分配显存峰值为 7,135,818,240 字节（6.65 GiB）；运行时整卡读数约 8.7 GiB，包含显存缓存与桌面等额外占用。全部重算分数与首次前向的最大差异为零；共同阈值对应的软召回为 0.9500001073，梯度均有限。本地记录为 `results/preflight/field/preflight.json`，不包含正式训练结果。

另取清单中点数最多的异常 STU 和正常 STU 扫描（索引 5,377、5,969，各 129,167 点），确认仅用检测损失即可使正常编码器、场参数、假设权重、兼容性模块和骨干收到非零有限梯度；再执行一次隔离的优化器更新，并核对全部原始点的保存重载。两扫描共同求导及该次更新耗时 12.29 秒，分配显存峰值 3,965,857,792 字节（3.69 GiB）。包含独立进程重载在内，分数最大差异为 $5.0664\times10^{-7}$；AP、AUROC、FPR95 的最大差异分别为 $3.1009\times10^{-6}$、$7.0991\times10^{-7}$、0 个百分点，均小于事先规定的 $10^{-5}$ 个百分点容差。临时模型和逐点数组已由测试删除，未作为正式训练检查点保留。

最终执行 `pytest tests/test_normal.py tests/test_training.py -q -s`，67 项通过，耗时 73.77 秒；唯一依赖告警为 `locale.getdefaultlocale` 的弃用提示。部分测试使用明确标识的合成小例子验证公式、恢复状态和梯度语义，其打印的指标不是科学实验结果。上述显存和耗时只描述列出的真实核验输入；八帧核验未创建优化器状态，不能直接当作整轮训练的最大资源需求或推理延迟。

训练入口为 `python -m src.train --epochs <完整遍历次数>`；当前默认训练集已按第 14 节改为 NDP 合成数据，开发集和官方初始化权重不变。`--check` 只执行一次真实批次反向核验。`--resume` 从相同配置的检查点恢复参数、优化器和随机状态；`--eval-every` 指定开发集评估间隔，默认只在预算终点评估。

正常预测诊断入口为 `python -m src.evaluate normal --checkpoint <模型路径> --manifest <含未修改正常扫描的训练清单> --output <诊断文件>`。该入口按正常点聚合距离误差、覆盖率和宽度，按扫描等权聚合块内点数归一化后的联合似然；它不作为启动异常分割训练的门槛。

本轮确认的是完整实现及上述范围内的执行正确性。正式训练后的 AP、AUROC、FPR95、正常分布校准、未见异常泛化和长期训练开销仍未知；不能把初始化模型上的损失或实现测试换算成检测收益。

**14．采用 NDP 合成数据的主实验（2026-09-23）**

**历史范围：本节原样保留旧解码器的 STU 206＋Perlin 训练及 val19 结果。这是目标域正常数据参与训练的历史实验，不是现行两级解码器或下一步仅 nuScenes 训练路线的结果。原始记录与指标不因本次升级而改写。**

用户确认本轮采用相同的 STU 206＋Perlin 合成数据和 STU 验证规则，保留本模型的优化器、学习率调度与八扫描共同排序，不移植 NDP 的额外坐标增强、实例粘贴或软异常监督。只训练本文件的完整模型，不开展隐藏测试集实验。这一比较检验完整方法在相同数据来源与评测规则下的表现；预训练、损失及优化设置仍有差异，不能据此单独归因于网络结构。

数据生成采用 [NDP 公开代码的固定版本](https://github.com/343gltysprk/ndp/blob/f11dfbe4181db03a6a036d46b52a308f45d0e255/ood_augmentation.py)。序列 206 的 449 帧按编号依次生成一次，不在每轮重新合成。参数抽样与噪声生成分别沿用两个种子均为 2025 的独立随机流；局部半径为 0.75—1.5 米，高度幅度参数为 0.25—1.0 米。这是公开代码采用的随机高度参数，区别于论文中的固定 0.4 米描述。其余网格、三层 Perlin 噪声、道路类别、抬升阈值及最大连通簇规则均沿用代码。

449 帧共包含 56,196,767 个真实回波。训练有效距离仍为 2.5—50 米，异常原始类别为 2，类别 0 忽略，其余类别作为正常监督；合计 14,029 个有效异常点。138 帧只有 1—4 个有效异常点，训练完整保留；3 帧未产生坐标或标签修改。验证集的至少五个有效异常点筛选规则只用于验证，不反向删减训练数据。逐帧独立执行官方函数，对全部 449 帧的坐标、强度和完整打包标签进行逐位比较，全部一致；官方空抬升导致的聚类异常没有在这些帧上触发。

生成入口是 `python -m src.data ndp --output results/data/ndp`。`vendor/ndp/augmentation.py` 保留公开算法并移除导入时读写，`src/data.py` 将修改存为原始回波槽位上的增量，清单位于 `results/data/ndp/train.json`；整个增量目录约 2.3 MiB，无需复制原始 206 数据。

每次训练读取一帧 Perlin 扫描及其对应的未修改原始扫描。Perlin 扫描进入完整模型，由检测损失训练实际观测路径、正常预测网络、兼容性解码器和异常头；原始扫描仅进入共享的正常预测网络，提供联合负对数似然辅助损失，不再执行一次 LitePT 骨干。两份扫描分别根据自身坐标构造射线与角度块；原始扫描不参与合成扫描的异常分数计算。推理保持单帧输入，不需要干净参照。

| 设置 | 本轮采用值 |
|---|---|
| 训练预算 | 449 帧完整打乱遍历 10 次，共 4,490 次合成扫描访问，以及对应的 4,490 次正常辅助扫描访问 |
| 更新与排序 | 连续按八扫描组成共同损失，共 562 次参数更新；末批为两扫描，不逐轮丢弃尾部 |
| 初始化 | 原 nuScenes 预训练；与 NDP 的 SemanticKITTI／Panoptic-CUDAL 预训练不同 |
| 随机种子 | 模型及训练顺序为 0；固定合成数据使用官方的 2025 |
| 优化器 | 当前 AdamW；骨干峰值学习率 0.0002，新增参数 0.002；权重衰减 0.005，偏置与归一化参数不衰减 |
| 学习率 | 前 5% 更新预热，从峰值的 10% 开始，随后余弦衰减至峰值的 1% |
| 检测与正常损失 | 保留共同二元交叉熵、AP／AUROC／FPR95 排序代理及权重 0.1 的正常辅助似然；排序权重在预算的 10%—20% 之间递增 |
| 数值与内存 | FP32，保持完整窗口分块及反向重算；每次处理两扫描、逐扫描释放激活，八扫描仍共同排序 |
| 验证 | 训练预算终点一次完整 `val19` 验证，同一检查点报告三指标；不报告隐藏测试结果 |

验证沿用 `assets/val.json`：19 个公开序列共 8,659 帧，其中 1,960 帧满足官方异常点数与距离规则。参考 [NDP 论文表 1](https://arxiv.org/html/2604.09232v2)，NDP-EE 验证集 AP 为 74.24%、FPR95 为 1.43%、AUROC 为 99.53%；这些是对方公布的结果，本地没有重新训练 NDP。`val19` 已用于开发，本轮改善只能支持相应开发集上的比较，未知异常泛化仍需后续证据。

新增核验直接针对配对监督的含义：干净扫描辅助似然与共享假设联合预测公式及梯度一致，缓存求导与直接共同损失一致，稀少异常帧能够读取且辅助目标确实来自原始扫描。与原有相关回归共同执行，70 项通过，耗时 64.12 秒；这些结果确认实现范围内的正确性，不证明最终检测性能。

随后按本轮真实训练顺序取索引 `[373, 306, 186, 18, 247, 76, 240, 349]`，共 1,007,814 个回波，其中有效正常点 986,415 个、有效异常点 306 个。一次完整八扫描共同反向传播耗时 88.60 秒，PyTorch 分配显存峰值为 7,228,628,480 字节（6.73 GiB）；八帧均提供干净扫描辅助监督，重算分数最大差异为零，软召回为 0.9500000477，梯度均有限。该核验不更新参数，记录位于 `results/preflight/ndp/preflight.json`；显存数值不包含后续创建的 AdamW 状态，也不是最终指标。

主训练命令为 `python -m src.train --epochs 10 --workers 2 --threads 2 --save-every 25 --output results/train/ndp`，配套设置 `OMP_NUM_THREADS=2`、`MKL_NUM_THREADS=2`、`OPENBLAS_NUM_THREADS=2`。输出位于 `results/train/ndp/0/field/`，运行中每 25 次更新保存恢复状态并检查 Windows E 盘；保持至少 10 GB 安全余量。

主实验在提交 `17188f1` 的实现上启动。启动核验确认首个正式参数更新成功：共同损失为 1.047233，正常辅助负对数似然为 3.741517，未裁剪梯度范数为 1.570847，按既定阈值 1 裁剪；重算分数差异为零，无数值溢出。按原调度，此时排序损失权重为零，进入预算的 10% 后逐步启用；八扫描共同排序已在上述独立真实批次中按全权重核验。首个更新及启动开销约 95.45 秒，恢复文件 `last.pt` 已保存。启动阶段整卡占用约 9.5 GiB，包含缓存和桌面占用；E 盘检查时剩余 26,890,805,248 字节。

2026-09-23 训练与完整验证均已结束。562 次计划更新全部成功，实际访问 4,490 帧，无数值溢出；训练耗时 32,608.96 秒（约 9 小时 3 分钟），完整验证及指标计算耗时 1,258.58 秒（约 21 分钟）。验证覆盖同一清单中的 1,960 帧、193,879,969 个有效点，按官方程序统一汇总。全程 PyTorch 分配显存峰值为 7,480,004,096 字节（6.97 GiB）。

| 同一检查点的点级指标 | NDP-EE 公开验证结果（%） | 本轮完整模型（%） | 本轮减 NDP-EE（百分点） |
|---|---:|---:|---:|
| AP ↑ | 74.24 | 75.690029 | +1.450029 |
| FPR95 ↓ | 1.43 | 1.085961 | −0.344039 |
| AUROC ↑ | 99.53 | 99.803424 | +0.273424 |

原始结果保存在 `results/train/ndp/0/field/result.json`，完整评测记录保存在同目录 `epoch1.json`。这里的 `epoch1` 表示唯一一次预算终点评估，训练仍为十次完整数据遍历。`best.pt` 与 `last.pt` 均标记完成，实际模型参数逐位相同，均对应第 562 次更新；没有从不同检查点分别挑选三项指标。已核对结果文件、评测记录、检查点中的指标、配置、训练数据身份及评测帧数与点数的一致性。本次核对未重新运行第二次验证。

这次结果表明，在已声明的训练设置及 STU 开发验证集上，完整模型的三个点级指标均超过 NDP-EE 论文表 1 的公开值，支持继续检验该方法。该比较仍有预训练、优化与损失差异，且只有一个随机种子；不能单独据此证明增益来自观测过程约束，也不构成独立测试或统计显著性结论。下一项能辨别方法贡献的实验是保持本轮数据和训练预算，去掉观测过程约束进行关键消融；本次未启动额外实验。

**15．两级兼容性解码器的真实核验记录（2026-09-23）**

**历史范围：本节保留两级解码器引入后、稀疏卷积数值问题修复前的核验。节内“当前”“本轮”和“尚未解决”均指当时状态；该固定案例随后已按第 16 节定位并修复，原失败事实和容差保持不变。**

当时 `tests/test_normal.py` 完整运行结果为 28 项通过、1 项失败，耗时 37.58 秒；另外单独选择的两项训练回归通过，其余 45 项未选中执行。新结构当时尚未完成全部数值核验，不能将这次运行写成全部通过。

已通过的检查覆盖：目标块距离、强度和点数不能进入对应正常场预测；同一组核在不同假设内的分组能够产生不同学习表示；核与假设的编号排列不改变结果；分块计算一致；区间外距离密度不进入判别表示；仅经新增假设特征的检测梯度可以回传正常场和先验权重；尖锐分布与极低先验下仍保持有限计算和梯度。这些检查确认相应实现性质，不证明最终异常识别能力。

GPU 真实输入取自已有 `results/data/native/train.json` 中的两个 nuScenes 记录，而不是把整份历史混合清单作为新训练集：含合成异常的 `anomaly_nuscenes` 最大点数记录为索引 175，场景 `scene-0030`，34,845 个回波；未修改的 `normal_nuscenes` 最大点数记录为索引 130，场景 `scene-0025`，34,816 个回波。前者仅用于异常判别，后者提供正常辅助监督。本次没有读取 STU 样本或使用 val19 评价，也没有生成下一步仅 nuScenes 的正式训练数据。

在这两个扫描上，异常样本的二元交叉熵梯度已到达骨干、正常编码器、场参数、假设权重及兼容性模块，并保持非零且有限；缓存反向传播、正常辅助样本身份及一次隔离的优化器更新均通过检查。保存后同进程重载和新进程重载的全部逐点分数，均满足原先设定的 `atol=1e-5, rtol=1e-5`。这里只执行了实现检查所需的一次参数更新，没有形成正式训练后的模型或新指标成绩。

唯一失败出现在新进程重载后的排序指标一致性检查。固定官方指标程序在同一 nuScenes 异常扫描上计算重载前后的数值差异，结果如下；这些是复算差异，不是 STU 基准成绩。

| 指标 | 重载前后绝对差（百分点） | 原定允许差（百分点） | 结果 |
|---|---:|---:|---|
| AP | $4.196949865420407\times10^{-6}$ | $10^{-5}$ | 通过 |
| AUROC | $5.046867737235061\times10^{-5}$ | $10^{-5}$ | 未通过 |
| FPR95 | 0 | $10^{-5}$ | 通过 |

逐点分数接近并不保证排序指标差异一定低于更严格的指标容差，因而不能用分数检查通过来覆盖这一失败。本次保留原定容差，也不为追逐该微小排序差异更换主模型机制。当前数值跟踪显示差异先出现在骨干，已排除新正常场作为该差异的最早来源；稀疏卷积库 `spconv` 的自动算法选择是候选原因，尚未证实。本轮调查在此范围收束，不修改第三方稀疏卷积实现或追加 GPU 复算；该指标一致性失败仍保留，问题尚未解决。临时检查点和逐点数组已清理，没有保留本轮训练检查点。

**16．稀疏卷积数值修复与真实回归（2026-09-23）**

后续在相同失败案例上逐层比较输出，并核对 `CONV_CPP` 的实际卷积描述符。最早差异出现在骨干 `enc1.block0.conv.0`：不同进程为该层选用了 `m64n64k8` 和 `m64n32k16` 两种计算核。由此将第 15 节的候选原因落实为该案例中可定位的选核差异。仅将稀疏卷积算法限制为 `Native` 仍未通过，AUROC 重载差为 $3.3645784910163457\times10^{-5}$ 个百分点，超过原定 $10^{-5}$ 容差；没有将这次尝试记为修复完成。

最终在 `vendor/litept/model.py` 中使用本地 `SubMConv3d` 作为现行完整场模型的权威执行实现。它保留原 `KRSC` 布局的同一卷积权重和稀疏输入、输出索引；先用 `F.linear` 计算中心偏移贡献，再按固定核偏移顺序计算邻接贡献。每个偏移中的输出索引唯一，贡献按该固定顺序累加；所有路径最后统一加入偏置，包含没有非中心邻接的孤立点，避免漏加偏置。完整场模型的输入卷积与六处卷积位置编码采用这一实现，卷积结构、可学习参数和监督定义不变。

真实回归继续使用第 15 节固定的两个 nuScenes 记录：索引 `[175, 130]`，回波数 `[34845, 34816]`。这两个旧清单记录仅用于复现与修复原缺陷，没有恢复旧数据训练路线。完整检查执行真实二元交叉熵梯度、正常辅助、缓存反传、一次隔离的优化器更新、保存重载及独立进程复算，结果如下。

| 核验对象 | 实测结果 |
|---|---:|
| 同进程与新进程重载的逐点分数最大差 | 0 |
| 官方 AP、AUROC、FPR95 重载差 | 均为 0 个百分点 |
| 两扫描共同反向传播及一次更新耗时 | 3.623205658 秒 |
| PyTorch 分配显存峰值 | 3,108,697,600 字节 |
| 包含完整检查的总耗时 | 22.17 秒 |

同一权重与修复前实现的逐点分数最大差为 $5.960464477539062\times10^{-7}$，满足原先的 `atol=1e-5, rtol=1e-5`。重载检查继续使用原官方指标程序及 $10^{-5}$ 个百分点的指标容差，没有改变指标、放宽容差或改变待检样本。

另以独立的 FP64 稠密卷积作为参考，核对稀疏卷积前向值以及输入、权重、偏置梯度。四个案例覆盖边长为 3、5 的卷积核，分别作用于不规则双批次点集和孤立单点；它们同时检查索引方向、权重布局与孤立点偏置。连同其余正常场检查，运行结果为 32 项通过、1 项真实扫描检查未在该次运行中重复选择，耗时 8.55 秒；真实扫描检查的结果已在上表单独记录。

这些结果确认了本机当前软硬件和所列固定输入下的数值修复，不是跨硬件、跨库版本或所有形状逐位一致的普遍保证。本次没有使用 STU 样本或 val19，没有正式训练现行模型；隔离的一次更新与零重载差只支持实现结论，不构成 AP、AUROC 或 FPR95 的新检测成绩。

随后将真实扫描回归改为当时的 `results/data/nuscenes/train.json`；该清单是本轮实例覆盖改版前的版本，以下索引不能用于定位现行记录。选取至少五个有效异常点的最大异常扫描与最大原始扫描，索引为 `[1268, 2094]`，回波数为 `[34848, 34880]`。异常扫描的未修改参考与独立原始扫描合计提供两个正常辅助样本；仅检测损失的梯度同时到达骨干、正常编码器、场参数、假设权重、兼容性模块和最终异常头。隔离更新后的同进程、新进程重载分数最大差均为 0，三个官方指标差均为 0，原容差不变。新进程评分没有读取配对正常参考，确认其只用于训练。两扫描反向传播及一次更新耗时 11.972826748 秒，PyTorch 分配显存峰值为 2,805,107,712 字节，完整检查耗时 101.78 秒；当时数据生成与读回核验正在并行运行，该时间不作为无干扰吞吐基准。另一次针对当时代码的检查覆盖正常场、稀疏卷积、四项移植几何、新数据标签与配对监督、训练循环及恢复行为，40 项通过，耗时 24.54 秒。检查所用临时参数更新不进入正式训练；这些历史数值修复证据不替代本轮数据核验。
