**AJAE 完整模型审定方案｜2026-09-22｜再次复核确认版**

**采用“观测约束的正常回波场与逐点兼容性分割网络”。** 输入单帧 LiDAR，输出每个真实回波的一个异常分数。实际观测路径保留已经有效的骨干、多尺度上下文和原始点细节；正常路径预测可学习的三维回波场，经射线观测得到正常距离分布；最终解码器学习实际点与正常预测是否兼容。所有可学习模块联合训练。

本版替代上一版软表面元、固定宽参考分布、固定污染比例、块内参考点重加权和十二项统计证据的设计。后续开发只实现、训练这一完整结构，不安排 N1、四平面首版或普通条件模型的重训队列。

本文件确定主模型的结构选择，并不声称已经找到全局最优模型。2026-09-23 已按本方案完成代码实现；实现对应关系与最初实测结果见第 13 节。随后经用户确认，主实验改用 NDP 的 STU 206＋Perlin 合成数据，保留本模型的优化设置，具体范围与执行记录见第 14 节。三项指标全面 SOTA 与创新有效性仍待结果验证。

**最终复核结论：保留主体网络和上次确定的有效批次共同排序，不再增加模块。** 本次进一步澄清含 BatchNorm 时梯度缓存的等价范围。联合预测与逐点评分、几何增强后的射线一致性、正常辅助监督与最终异常判别的边界维持下文定义。以下是唯一完整实施方案，不另开旧模型训练队列。

**1．任务、贡献与设计判断**

目标是单帧、LiDAR 单模态的未知异常点级分割，优化 AP、AUROC 和 FPR95。正常集合包含道路、车辆、行人、路缘等协议内正常结构。道路中间、高出地面、点少、预测不准，均不能单独成为异常定义。少回波、低矮、远距和弱遮挡是整体方法可能自然获益的场景。

模型回答两个相互补充的问题：

- 实际点及其周围场景，在已学习的正常/异常判别空间中是否合理？
- 隐去待检验区域的实测回波后，周围信息支持哪些正常三维结构？这些结构通过实际射线能否解释待检验回波？

第二条路径提供一种受约束的表示与学习信号，不是在同一输入之外凭空增加信息。正常预测失败也不直接等于异常，最终判断仍由有正常与异常监督的逐点解码器学习。

拟主张的贡献：

> 用目标区域不可见的上下文预测共享正常三维回波场，通过明确的射线关系构成正常回波联合分布；将该分布与实际点之间的兼容性纳入端到端异常分割，使正常几何学习直接服务于逐点异常排序。

贡献对象是这个完整学习机制及其被验证的收益。高斯、射线积分、掩码预测、注意力、重建与判别联合训练本身均已有先例。

**2．证据基础与本次删改**

核对代码版本：[v4 / 79997c1aa91f479bbe111e717bfa206e2bb093f6](https://github.com/JasonGao1010/Anomaly/tree/79997c1aa91f479bbe111e717bfa206e2bb093f6)。

现有四平面候选终点记录为 AP 81.50734%、FPR95 0.07806%、AUROC 99.96646%；同期 N1 为 80.08333%、0.08157%、99.97082%。候选全量重载结果尚未核实。它说明方向已有初步开发收益，不能证明新架构会进一步上涨，也不能说明三个指标已全面改善。

| 复审对象 | 审定处理 | 原因 |
|---|---|---|
| 已有实际点编码与六尺度融合 | 保留组件，随完整模型训练 | 已有任务内证据；不增加无依据的骨干替换 |
| 单平面或手工表面元求交、排序 | 替换为连续三维回波场的解析射线积分 | 支持有限厚度、不同朝向与重叠结构，减少硬分支 |
| 人工宽参考分布及固定污染比例 | 删除 | 缺少任务依据，还可能提供吸收异常的便捷通道 |
| 用目标块内其他回波修改正常假设权重 | 删除 | 正常参照始终由块外生成，避免块内异常互相“证明正常” |
| 十二项人工统计证据 | 替换为可学习的回波兼容性解码 | 不预先规定哪些残差、熵或阈值一定有用 |
| 以正常预测质量作为检测训练门槛 | 不采用 | 正常预测是辅助任务；最终依据是异常分割三指标 |

这些修改针对明确的表示和优化风险，尚不是已证实的性能改进。

**3．确定的整体结构**

| 部分 | 实施配置 |
|---|---|
| 实际点编码 | LitePT-S，5 cm 体素；现有原始点 MLP、mean/max 聚合、六尺度条件融合与逐点回填 |
| 实际点状态 | 64 维场景状态＋64 维点细节投影＋64 维体素内位置编码，相加得到 64 维 $f_i$ |
| 正常上下文 | 1°、2°、4° 嵌套角度块；各目标块读取同层 5×5 邻域去除自身的 24 块 |
| 上下文编码 | 原始坐标、强度、单位方向共 7 维→64→64；仅块内 mean/max 池化，投影至 128 维 |
| 正常假设解码 | 四个 128 维查询，两层四头交叉注意力，前馈宽度 256；三尺度共享参数 |
| 正常回波场 | 每个假设八个各向异性三维高斯基函数；中心、完整协方差、幅度与假设权重均由网络预测 |
| 射线观测 | 解析计算沿射线的回波密度和累计量，构成归一化的条件距离分布 |
| 兼容性解码 | 实际点查询读取预测射线分布的参数 token；每尺度得到 32 维学习特征 |
| 最终头 | $[f_i,e_i^{1},e_i^{2},e_i^{3},\ell_i^{1},\ell_i^{2},\ell_i^{3}]$，163→128→64→1 |
| 输出 | 每个真实点一个 FP32 logit；同一分数用于 AP、AUROC、FPR95 |
| 排序学习范围 | 默认一次更新八扫描共同计算，微批次仅用于调度；显存不足采用分数梯度缓存与重算 |

四个假设、八个基函数是容量设置，不是四类正常物体或八种异常模板。三个尺度是留出范围，不是异常尺寸规则。这些是确定实现所需的默认值，没有理论或实验保证它们最优。

**4．实际观测路径与严格的信息边界**

实际路径沿用 [src/model.py](https://github.com/JasonGao1010/Anomaly/blob/79997c1aa91f479bbe111e717bfa206e2bb093f6/src/model.py) 中已有效的点细节和上下文组件。新训练使用项目固定的官方 nuScenes LitePT-S 预训练权重初始化骨干，新分支与新异常头重新初始化。骨干继续可训练。

复用组件不意味着再次训练 N1，也不继承历史自制合成数据训练后的异常头权重。论文需披露官方预训练数据和权重。

对某尺度目标块 $B$，正常场参数只允许依赖块外上下文 $C_B$、固定网格位置和尺度。逐点 MLP 后先做各块独立池化，排除目标块，随后才运行上下文交叉注意力。不能先整帧混合特征，再遮掉最后一层目标 token。

禁止正常场预测器读取目标块的距离、强度、点数、三维近邻、体素、骨干特征或异常分数。禁止跨块 BatchNorm、全扫描统计或全局 token 将这些信息带入正常场。使用逐点/逐 token LayerNorm。

上下文输入为 $(x/U,y/U,z/U,I,d_x,d_y,d_z)$，$U$ 为协议距离上限；不增加人工距离方差、密度阈值、地面高度等描述子。mean/max 是对学习特征的标准池化。块位置与尺度经学习投影加入，空块使用缺失掩码；上下文全空时使用学习到的空上下文 token，不直接输出“正常”。

同层邻块中的点可能仍含异常；多尺度留出降低部分污染风险，但不能保证任何大小的异常都被完整遮住。尤其不能声称 4° 是未知物体大小的上界。

目标射线的方向和原点只进入观测计算；目标距离只在比较、正常辅助监督和最终异常判别时使用。最终判别器可以读取实际点；这是任务输入，不构成对正常预测的前向信息泄漏。

**5．正常三维回波场**

对每个目标块、每个尺度，网络输出 $K=4$ 个正常假设及权重：

$$
\pi_k=\operatorname{softmax}(b)_k,\qquad
\lambda_k(x)=\sum_{m=1}^{M}a_{km}
\exp\!\left[-\frac12(x-c_{km})^\top\Sigma_{km}^{-1}(x-c_{km})\right],
\quad M=8.
$$

$c$ 是三维中心，$\Sigma$ 是正定的完整三维协方差，$a>0$ 是幅度，单位为每米。采用 Cholesky 参数化保证正定，用正值参数化保证幅度为正；仅保留数值稳定所需的极小值。

中心、朝向、三个轴尺度与幅度都通过梯度学习，不预设平面、物体类别、道路高度或人工绝对/相对噪声公式。较扁、较长或较宽的核可组合表示不同结构；高斯尾部没有硬边界。所谓“正常”仍是任务中的正常场景，不是说正常数据必须服从单个高斯分布。

同一个假设内的多个核共同存在；不同假设表示不同正常解释。所有射线共享该块预测的同一组场参数，不能让每个目标点自行生成一套能解释自己的三维结构。

共享范围是同一块内的多条射线；不同块和不同留出尺度分别预测，不宣称已经得到跨整帧、跨视角或跨距离一致的全局地图。多个假设无需被人为强制成不同类别；若上下文足够明确，权重集中本身不构成错误。

初始化使用覆盖有效距离区间的较宽场，并用不同的查询初始化打破假设/核的对称性；不从目标点位置初始化中心。初始化尺度仅是优化起点，随后可学习。

$\lambda$ 是用于正常距离预测的有效回波率场。仅凭留出距离无法唯一恢复真实占据、反射率或完整场景；不把它包装成可辨识的物理世界模型。

**6．由射线生成正常观测分布**

单位方向射线为 $x_i(r)=o_i+r d_i$。以当前 STU 有效距离区间 $[L,U]=[2.5,50]$ 米为例：

$$
H_{ik}(r)=\int_L^r\lambda_k(o_i+t d_i)\,\mathrm dt,\qquad
Z_{ik}=1-\exp[-H_{ik}(U)],
$$

$$
p_{ik}(r\mid\text{有效区间内有记录回波})=
\frac{\lambda_k(o_i+r d_i)\exp[-H_{ik}(r)]}{Z_{ik}},
\qquad L\le r\le U.
$$

这是一个归一化的距离密度：近处回波率影响远处被观测到的相对概率。同一几何场通过不同射线产生彼此关联的预测，而不是若干独立自由距离回归器。

该形式采用生存概率/首次事件的有效观测模型。真实设备可能记录最强回波而非理想首次回波，扫描运动也可能使射线原点近似有误，因此不能称为完整、精确的传感器仿真。仅有回波点、缺少可靠原始发射记录时，不监督丢失回波，不把空角度格当成自由空间，也不将 $Z$ 宣称为校准后的实际 ray-drop 概率。

区间是评价协议的有效范围，不是异常阈值。其他数据使用其固定协议边界，训练和评测必须一致；区间外或忽略标签的点可作为上下文，不因此被标为正常。若真实束原点不可用，明确使用扫描参考原点近似，不能虚构精确发射轨迹。

**解析计算，不做稠密射线步进。** 单个高斯沿射线仍为一维高斯响应。令 $\Sigma=AA^\top$、$v=A^{-1}d$、$w=A^{-1}(o-c)$：

$$
\mu=-\frac{v^\top w}{v^\top v},\qquad
\tau=(v^\top v)^{-1/2},\qquad
\log h=\log a-\frac12\|w+\mu v\|^2,
$$

$$
\lambda_m(r)=h\exp[-(r-\mu)^2/(2\tau^2)],
$$

$$
\int_L^b\lambda_m(r)\,\mathrm dr
=h\tau\sqrt{2\pi}
\left[\Phi\!\left(\frac{b-\mu}{\tau}\right)
-\Phi\!\left(\frac{L-\mu}{\tau}\right)\right].
$$

各核积分相加得到 $H$。这在上述有效回波率假设内支持重叠核，不需要硬求交、深度排序、最近表面选择或额外人工宽分量。解析高斯射线积分本身已有先例，不作为本工作的发明。

仅有条件回波位置监督时，幅度可能不可充分辨识；小累计量极限下分布接近沿射线的归一化场响应。因此创新主张依托“共享三维场对预测的约束”，不能仅靠公式中出现透射率就声称已经学会真实遮挡。

本方案直接定义有记录回波条件下的预测模型，$\pi_k$ 是这个条件模型的混合权重，不是完整物理生成过程的无条件场景先验；不能从当前条件密度反推未观测射线的真实占据或返回概率。

**7．共享正常解释与可学习逐点判别**

正常辅助任务使用一个假设共同解释目标块内的有效正常回波：

$$
P(R_B\mid C_B,Q_B,N)
=\sum_{k=1}^{K}\pi_k\prod_{i\in B_{\rm valid}}p_{ik}(r_i).
$$

实现顺序为：每个假设先累加各射线的 log 概率，再对假设做 logsumexp。不能先为每点挑选最佳假设再求和。辅助负对数似然按块内有效点数归一化，再在尺度和扫描间汇总；同一组完整概率参数同时用于辅助学习和判别。

默认使用全部有效正常目标回波，以分块累计控制显存；删除“每块最多 32 个参考点”。没有目标块内的后验重加权，也没有固定污染比例。推理的假设先验 $\pi_k$ 始终来自块外上下文。

逐点正常 log 密度为：

$$
\ell_i^s=\log\sum_k\pi_k^s p_{ik}^s(r_i).
$$

为避免把丰富的分布过早压成一个残差，每个核同时提供其射线参数 token：

$$
t_{ikm}^s=\operatorname{MLP}
\left(\mu_{ikm}/U,\ \log(\tau_{ikm}/U),\
\log(h_{ikm}U),\ \log\pi_k\right).
$$

共享 MLP 为 4→16→64，加入学习尺度编码后分成 32 维 key 和 32 维 value。实际点状态 $f_i$ 和实际距离的学习编码形成 32 维 query，经四头注意力读取每尺度的 $K M=32$ 个 token，输出 $e_i^s\in\mathbb R^{32}$。

最终头输入 64＋3×32＋3＝163 维，MLP 163→128→64→1，隐藏层采用标准归一化和 GELU。三个 $\ell$ 提供绝对兼容程度，避免仅靠归一化注意力在“全部解释都很差”时仍必须选出一个解释。它们不是手工异常分数或标签，最终头可结合语义、实际细节和学习到的分布特征决定如何使用。

不输出区域统一异常分数，不将块级联合 NLL 复制到每点；也不对最终分数做邻域平均、连通域过滤、道路硬掩码或按点数删除目标。

联合分布约束用于正常训练；推理提供的是逐点边缘密度与预测分布特征，最终头同时读取实际场景上下文。边缘密度高并不证明整块观测能由一个正常假设共同解释，也不证明该点一定正常。论文不能把当前逐点头描述为精确的区域联合异常检验。

**8．一个训练目标，直接服务三指标**

令 $s_i$ 为最终异常 logit，正常/异常标签分别为 0/1。使用：

$$
\mathcal L=
\mathcal L_{\rm balanced\ BCE}
+\lambda_{\rm rank}(t)
\left[
\widetilde{\mathcal L}_{\rm AP}
+0.1\widetilde{\mathcal L}_{\rm AUROC}
+0.1\widetilde{\mathcal L}_{\rm FPR95}
\right]
+0.1\mathcal L_{\rm normal}.
$$

BCE 和三个排序代理都优化同一个最终输出，不建立三个独立头，也不分别挑三个检查点报告最好数字。正常预测只是辅助约束。

沿用当前代码已有的排序目标定义和初始权重，不再增加一组未经验证的自适应损失权重。$\lambda_{\rm rank}$ 沿用总进度 10%–20% 由 0 线性升至 1；正常辅助权重固定 0.1。去掉上一版额外的几何分支开启门控和正常辅助权重下降日程。

| 指标 | 学习目标关注什么 | 结构上的配合 |
|---|---|---|
| AP ↑ | 异常点在排序前部的精确率与召回 | 逐点细节保留边界；兼容性特征提供可学习的额外判别依据 |
| FPR95 ↓ | 高召回位置仍被排到前面的正常点 | 正常预测与场景状态联合判断，允许正常边界和多种正常解释 |
| AUROC ↑ | 整体正常/异常点对的排序 | 同时覆盖普通正常点与困难正常点，避免只顾少数高分误报 |

**已确定的排序范围。** 历史代码在两扫描微批次内计算排序损失，再累积梯度；本次完整模型在一次参数更新内的完整有效批次共同排序，默认八扫描。对排序损失而言，微批次划分仅是计算调度，不能各自独立确定召回阈值或排序池。

在有效批次上最多取 256 个均匀抽样的异常锚点；正常点保留最高分 512 个，并从其余正常点抽取最多 3,584 个、用抽样权重补偿。AP 的正样本秩使用该有效批次全部异常点；AUROC 使用加权点对 softplus；FPR95 由该有效批次全部异常点确定一个软 95% 召回阈值，再计算加权正常软超阈率。温度沿用 1。无异常的完整有效批次仍训练正常 BCE，不能伪造排序目标。

动机可用简单反例说明：一组扫描的异常分数为 10、20 个正常点均为 9；另一组异常为 1、20 个正常点均为 0。每组单独评价都是 AP=1、AUROC=1、FPR95=0，合并后则为 AP=0.54545、AUROC=0.75、FPR95=0.5。因此，独立微批次排序不是更大批次排序的等价实现。此反例是数学说明，不是本项目真实失分归因；已有 BCE 也会提供跨帧分数约束。

修正后仍然是有限批次、有限样本和训练分布下的代理，不能称为全数据指标的精确或无偏估计，也不保证三个测试指标同步改善。随机正常点覆盖对 AUROC 很重要，不能为了困难负例挖掘全部删除。推理不做逐帧 min–max、分位数或其他按当前帧统计变化的分数归一化。

异常损失通过兼容性解码、log 密度和观测公式回传到正常场与正常上下文编码器；同时回传到实际点骨干。整个网络不设正常分支 stop-gradient，不要求先单独预训练预测器再通过门槛才进行检测。

$\mathcal L_{\rm normal}$ 仅使用可靠、未修改的真实正常扫描；合成异常用于最终判别，不被当作正常重建目标。完整 NLL 的归一化会惩罚一味扩宽分布，但不自动保证校准，也不能完全排除有限训练集上的过度尖锐拟合。

异常梯度可以改变正常场，辅助权重 0.1 也不是理论上的正常性保证；因此“端到端”与“始终是校准的纯正常分布”不能画等号。保持正常辅助目标、检查正常扫描上的覆盖与宽度，是解释该分支含义所需的诊断，不另设预测器预训练门槛。最终模型可能降低某些几何证据的使用权重，不能把这种可学习融合提前宣称为不会掉点的保证。

**9．训练数据与运行边界**

允许直接采用公开、可追溯的正常训练数据和已有合成配方，在同一来源上训练这一完整模型。公开配方是控制样本来源、提高可比性的选择，不是保证分布覆盖充分。

主模型以该公开配方、预训练来源、标签映射和划分形成一次完整训练配置；不混入来源不清的旧缓存。没有可靠异常标注的 void 点不自动当成异常。正常辅助分支所见额外未修改扫描也计入训练数据访问量和时间。

若采用移动点坐标的公开增强，必须同步更新有效射线表示：使用扫描原点近似时，由增强后的坐标重新计算距离、方向与角度块；不能一边移动点、一边沿用原点对应的旧射线。仅作坐标变换时则一致变换点、原点和方向。这样只保证模型输入内部一致，不把几何扰动重新命名为物理真实的传感器采样；正常辅助监督仍来自未修改扫描。

本版不新增独立生成模型、时序输入、相机、语言模型、法线真值、完整正常三维真值或逐场景优化。推理只有一套网络、一次主干前向，所有目标块预测并行/分块执行。

“两次遍历”是既往开发预算，不是新结构已经收敛的证据。实际训练时长依据统一完整训练配置确定，不用中间小实验数量代替充分训练。开发集可以选检查点，但已用于开发的 val19 不能再称独立最终评测。

**10．计算、精度与必要的工程核验**

默认每点三个尺度、每尺度四假设八核，约 96 次高斯—射线解析计算；十万点约 960 万次，不是 96 次骨干前向。场参数分解按块缓存，逐点射线与注意力按 2,048 点一块执行，训练时重计算中间激活。新增参数控制目标为 200 万以内；本次实现实际增加 328,449 个参数，完整模型为 13,208,962 个参数。

**有效批次排序的显存实现。** 若无法保留八扫描的全部激活，采用已有 Gradient Cache 原理：先在参数不变时分微批次前向收集点分数；在合并分数上计算检测损失及各分数梯度；再重算各微批次，用缓存的分数梯度向完整网络反传，同时计算该微批次正常辅助损失；最后统一更新一次参数。只跨微批次保留分数、梯度及必要状态，不保留全部骨干激活，不使用跨更新的陈旧分数队列。

这仍是端到端链式求导，不是对正常分支停止梯度。等价性要求重算使用相同增强、抽样、随机状态和输入，参数在两次前向间不变；BatchNorm 等可变状态只按一次逻辑前向更新，不能重复累计。数值不一致时应先修复，不能声称已复现完整有效批次梯度。该方案增加一次训练前向，额外时间计入预算，不增加推理前向次数。

这里的等价对象是“保持相同微批次划分与前向行为，将这些分数合并后求一个联合损失”的梯度。训练态 BatchNorm 使用当前批次统计，因此这不自动等价于把八帧一次性输入骨干的前向与梯度。实现不因此替换已有骨干归一化层；只要求固定的微批次前向与重算一致，以及排序损失的有效批次范围一致。正常预测分支继续采用逐点/逐 token LayerNorm。

结构允许骨干使用 AMP，但本次实现经独立进程重载核验后采用全模型 FP32。协方差求解、射线密度、log-CDF 差、logsumexp、兼容性头、最终线性层及排序代理均使用 FP32。仅在最后把 BF16 输出转为 FP32，不能恢复已经丢失的排序精度。骨干每次处理最多两个原有注意力窗口，并在反向传播时重算注意力矩阵；窗口大小、窗口内的点及注意力公式均保留。

尾部概率采用稳定 log-CDF 差；$1-\exp(-H)$ 用稳定 expm1/log 形式。极小 $H$ 时利用归一化极限，不能把所有低质量或远尾预测统一截断为同一个常数。

例如取 $s=\max_m\log h_m$、$\lambda=e^s\bar\lambda$、$H=e^s\bar H$，计算：

$$
\log p(r)=\log\bar\lambda(r)-\log\bar H(U)-H(r)
-\log\!\left(\frac{1-e^{-H(U)}}{H(U)}\right).
$$

最后一项在小 $H$ 时使用展开式，其内层对数为 $-H/2+H^2/24+O(H^4)$。协方差的数值下限不能被宣传为物理噪声水平。

方案审定时的独立 NumPy/SciPy 标量数值核验得到：三维场与一维射线表达相对误差约 $3.1\times10^{-14}$；解析积分误差约 $1.3\times10^{-15}$；条件密度及混合密度积分误差低于 $5\times10^{-16}$；重叠核交换次序结果不变；极低幅度及远尾 log 密度保持有限。审定时另行核算了上述分组与合并排序反例。这些数字属于实现前的数学核验；本次 PyTorch 实现的检查及真实扫描执行另列于第 13 节。

以上只验证数学计算，不验证真实网络梯度、目标泄漏、GPU 可重复性、开销或检测精度。实现时需要一次必要核验：改变目标块距离不应改变相应正常场参数；异常梯度应到达正常分支；保存重载的完整分数与指标应在预设容差内一致。这些是完整模型的正确性检查，不是重新启动旧模型对照队列。

**11．相邻工作与新颖性边界**

| 工作 | 已有内容 | 本方案需要成立的区别 |
|---|---|---|
| NDP、REL | 可学习异常校准、正负证据、端到端逐点评分 | 学习的是目标不可见条件下的正常三维回波分布，并把它用于同一最终点级判别 |
| DRAEM | 重建与异常判别端到端联合训练 | 不是以“预测后再判别”作为新意，而是检验共享几何—射线约束的实际增量 |
| COVAL | 用户所给全文中的缺失位置特征重建、共同可见特征一致性 | 此处预测可归一化的真实回波距离分布，推理保留正常参照；不要求 factual/counterfactual 合成对 |
| PLiNK | LiDAR 回波的概率形式与生存概率关系 | 复用概率建模基础；本方案是上下文到正常场的前馈预测和异常判别，不逐场景拟合地图 |
| Volumetrically Consistent 3D Gaussian Rasterization、3DGEER、LiDAR-RT | 高斯表示、射线计算和渲染 | 不主张高斯观测模型本身新；增量落在正常解释学习如何服务未知异常分割 |

已核对资料：

- [NDP，正文 3.2–3.5](https://arxiv.org/html/2604.09232v2)。
- [REL，正文 3.2–3.3](https://arxiv.org/html/2511.06720v1)。
- [DRAEM，ICCV 2021 原文](https://openaccess.thecvf.com/content/ICCV2021/papers/Zavrtanik_DRAEM_-_A_Discriminatively_Trained_Reconstruction_Embedding_for_Surface_Anomaly_ICCV_2021_paper.pdf)。
- [COVAL](https://openreview.net/forum?id=Hly2QYcuLW)：此前已读取用户提供 PDF 提取文本的 3.3.1–3.3.2。
- [PLiNK，正文 II–III](https://arxiv.org/html/2411.01725v1)。
- [Volumetrically Consistent 3D Gaussian Rasterization，方法及解析积分](https://arxiv.org/html/2412.03378v2)。
- [3DGEER，ICLR 2026 官方论文条目](https://proceedings.iclr.cc/paper_files/paper/2026/hash/e112a4671e8779aa9f640a0e3f81bd26-Abstract-Conference.html)。
- [LiDAR-RT，CVPR 2025 官方论文条目](https://openaccess.thecvf.com/content/CVPR2025/html/Zhou_LiDAR-RT_Gaussian-based_Ray_Tracing_for_Dynamic_LiDAR_Re-simulation_CVPR_2025_paper.html)。
- [Smooth-AP，作者项目页及论文](https://www.robots.ox.ac.uk/~vgg/research/smooth-ap/)：可微 AP 排序代理已有基础。
- [Gradient Cache，原文 3.2–3.3](https://aclanthology.org/2021.repl4nlp-1.31.pdf)：复用链式求导分解控制有效批次学习的显存；原文的 NLP 性能与时间结果不能直接外推到本模型。
- [PyTorch BatchNorm 官方说明](https://docs.pytorch.org/docs/stable/generated/torch.nn.BatchNorm1d.html)：用于核对当前批次统计和运行统计的区别，不意味着修改项目依赖版本。

上述内容来自此前对核心相邻机制的核查；本次仅重核梯度缓存原文及 BatchNorm 官方说明，没有新增一百篇完整精读，也不构成无遗漏证明。实现和写作必须明确引用这些基础；没有检索到完全相同的组合，也不足以独立证明会议级创新。

**12．最终判断**

推荐把本版作为下一次完整实现的唯一主模型。竞争潜力来自：保留已经有效的逐点判别能力，用更灵活且受共同几何约束的正常预测提供参照，再让最终异常损失学习如何利用这种参照，而不是不断叠加手工异常规则。

剩余的不确定性集中在三个实质问题：块外信息是否足以预测复杂正常结构；新增分支是否学出有用且可迁移的判别表示；收益是否值得计算开销。前者不会因换成更大生成器自动解决，后两者也不能由架构推理保证。

对本模型的任务判断保持统一：辅助预测更准但最终三指标不改善，不能算检测贡献；最终三指标改善但正常分支未提供可验证作用，也不能据此认定所主张机制成立。端到端训练和更少手工规则是设计要求，不是性能或新颖性的证明。后续先完成唯一完整模型的实现与主训练，机制归因不再扩展成训练前的多模型门槛。

“尽量没有手工设计”在这里落实为：不手工规定什么几何是异常，不设异常高度/尺寸/污染比例，不手工组合多项分数，不用后处理修正逐点判断。保留的人工选择是网络容量、留出规则、观测模型、数值参数和损失权重；这些必须公开，不称为完全无先验。

后续主线是训练这个已实现的完整模型，确认同一分数在统一协议下的 AP、AUROC 和 FPR95。实现检查不替代训练后的开发集表现，也不替代观测过程约束的机制归因。

**13．代码落实与真实执行记录（2026-09-23）**

本节记录最初实现核验，当时复用 `results/data/native/train.json` 的 6,185 帧：正常 nuScenes 2,512 帧、含合成异常的 nuScenes 2,504 帧、含合成异常的 STU 720 帧、正常 STU 449 帧。沿用 `assets/val.json` 的开发集规则，未新增独立最终评测集。历史 N1 权重和结果保留；训练命令只启动完整模型。后续主实验的数据变更见第 14 节，本节的资源实测只适用于当时列出的输入。

| 实施位置 | 当前行为 |
|---|---|
| `src/normal.py` | 构造三尺度嵌套角度块，在跨块注意力前排除目标块；预测四假设、每假设八个完整协方差高斯核；解析求射线密度和共享假设联合似然；学习逐点兼容性 |
| `src/model.py` | 保留 N1 实际观测组件；拼接 163 维状态并输出逐点分数；检测梯度回传正常场和骨干，推理只有一次骨干前向 |
| `src/train.py` | 显式指定完整数据遍历次数；八扫描共同计算 BCE 和排序代理；缓存分数梯度后逐扫描重算并释放激活，再统一更新参数；正常辅助权重为 0.1 |
| `src/evaluate.py` | 保持原始回波顺序与正式指标计算；新增正常预测诊断入口，报告中位数距离误差、90% 区间覆盖率、区间宽度和联合负对数似然 |
| `vendor/litept/model.py` | 主模型使用 FP32 注意力，按完整窗口分块并重算中间矩阵，控制显存；历史模型加载保留原计算路径 |
| `tests/test_normal.py`、`tests/test_training.py` | 覆盖信息边界、解析概率、端到端梯度、八扫描共同排序、恢复训练、真实扫描及独立进程重载 |

当前数据缺少逐束发射原点，使用扫描参考原点近似；增强后从当前点坐标重新生成方向、距离和角度块。有效距离外的真实回波仍输出分数并参与上下文，但其三项距离 log 密度输入设为零，不参与正常辅助似然；这不把忽略点标记为正常。

必要核验直接对应本方案的科学含义：改变目标块距离和强度时，该块场参数逐位不变；改变目标点数时只出现既定浮点容差内的批量运算差异，目标特征对本块场的梯度严格为零。共享联合似然先累加同一假设下的全部有效射线，再对假设求和。独立数值积分核对解析密度，CPU 与 CUDA 核对极低幅度及远尾梯度。包含 BatchNorm 和随机失活的例子核对缓存求导与同一前向分组下的直接共同损失，覆盖八扫描、尾部不足八扫描和全正常批次。

实现中发现并处理了两个实际问题。混合精度版本在同进程重载时一致，但独立进程的完整逐点分数出现约 0.004 的最大差异，超过事先规定的容差；改用稀疏卷积的另一种执行算法仍未解决。因此主模型统一使用 FP32，未放宽分数和指标容差。直接保存全部 FP32 注意力矩阵又使整卡占用接近 16 GiB，该次核验已停止；改为完整窗口分块及反向重算后，完成下面的八扫描核验。以上失败不能作为模型精度收益或退化的证据。

在 RTX 5080 Laptop（16 GiB）上，按训练顺序取索引 `[6111, 356, 3132, 2560, 6048, 2867, 6124, 4326]`，共 551,583 个真实回波。其中监督正常点 493,530 个、异常点 585 个，五帧具备正常辅助监督资格。执行一次完整共同损失的反向传播、不更新参数，耗时 53.78 秒，PyTorch 实际分配显存峰值为 7,135,818,240 字节（6.65 GiB）；运行时整卡读数约 8.7 GiB，包含显存缓存与桌面等额外占用。全部重算分数与首次前向的最大差异为零；共同阈值对应的软召回为 0.9500001073，梯度均有限。本地记录为 `results/preflight/field/preflight.json`，不包含正式训练结果。

另取清单中点数最多的异常 STU 和正常 STU 扫描（索引 5,377、5,969，各 129,167 点），确认仅用检测损失即可使正常编码器、场参数、假设权重、兼容性模块和骨干收到非零有限梯度；再执行一次隔离的优化器更新，并核对全部原始点的保存重载。两扫描共同求导及该次更新耗时 12.29 秒，分配显存峰值 3,965,857,792 字节（3.69 GiB）。包含独立进程重载在内，分数最大差异为 $5.0664\times10^{-7}$；AP、AUROC、FPR95 的最大差异分别为 $3.1009\times10^{-6}$、$7.0991\times10^{-7}$、0 个百分点，均小于事先规定的 $10^{-5}$ 个百分点容差。临时模型和逐点数组已由测试删除，未作为正式训练检查点保留。

最终执行 `pytest tests/test_normal.py tests/test_training.py -q -s`，67 项通过，耗时 73.77 秒；唯一依赖告警为 `locale.getdefaultlocale` 的弃用提示。部分测试使用明确标识的合成小例子验证公式、恢复状态和梯度语义，其打印的指标不是科学实验结果。上述显存和耗时只描述列出的真实核验输入；八帧核验未创建优化器状态，不能直接当作整轮训练的最大资源需求或推理延迟。

训练入口为 `python -m src.train --epochs <完整遍历次数>`；当前默认训练集已按第 14 节改为 NDP 合成数据，开发集和官方初始化权重不变。`--check` 只执行一次真实批次反向核验。`--resume` 从相同配置的检查点恢复参数、优化器和随机状态；`--eval-every` 指定开发集评估间隔，默认只在预算终点评估。

正常预测诊断入口为 `python -m src.evaluate normal --checkpoint <模型路径> --manifest <含未修改正常扫描的训练清单> --output <诊断文件>`。该入口按正常点聚合距离误差、覆盖率和宽度，按扫描等权聚合块内点数归一化后的联合似然；它不作为启动异常分割训练的门槛。

本轮确认的是完整实现及上述范围内的执行正确性。正式训练后的 AP、AUROC、FPR95、正常分布校准、未见异常泛化和长期训练开销仍未知；不能把初始化模型上的损失或实现测试换算成检测收益。

**14．采用 NDP 合成数据的主实验（2026-09-23）**

用户确认本轮采用相同的 STU 206＋Perlin 合成数据和 STU 验证规则，保留本模型的优化器、学习率调度与八扫描共同排序，不移植 NDP 的额外坐标增强、实例粘贴或软异常监督。只训练本文件的完整模型，不开展隐藏测试集实验。这一比较检验完整方法在相同数据来源与评测规则下的表现；预训练、损失及优化设置仍有差异，不能据此单独归因于网络结构。

数据生成采用 [NDP 公开代码的固定版本](https://github.com/343gltysprk/ndp/blob/f11dfbe4181db03a6a036d46b52a308f45d0e255/ood_augmentation.py)。序列 206 的 449 帧按编号依次生成一次，不在每轮重新合成。参数抽样与噪声生成分别沿用两个种子均为 2025 的独立随机流；局部半径为 0.75—1.5 米，高度幅度参数为 0.25—1.0 米。这是公开代码采用的随机高度参数，区别于论文中的固定 0.4 米描述。其余网格、三层 Perlin 噪声、道路类别、抬升阈值及最大连通簇规则均沿用代码。

449 帧共包含 56,196,767 个真实回波。训练有效距离仍为 2.5—50 米，异常原始类别为 2，类别 0 忽略，其余类别作为正常监督；合计 14,029 个有效异常点。138 帧只有 1—4 个有效异常点，训练完整保留；3 帧未产生坐标或标签修改。验证集的至少五个有效异常点筛选规则只用于验证，不反向删减训练数据。逐帧独立执行官方函数，对全部 449 帧的坐标、强度和完整打包标签进行逐位比较，全部一致；官方空抬升导致的聚类异常没有在这些帧上触发。

生成入口是 `python -m src.data ndp --output results/data/ndp`。`vendor/ndp/augmentation.py` 保留公开算法并移除导入时读写，`src/data.py` 将修改存为原始回波槽位上的增量，清单位于 `results/data/ndp/train.json`；整个增量目录约 2.3 MiB，无需复制原始 206 数据。

每次训练读取一帧 Perlin 扫描及其对应的未修改原始扫描。Perlin 扫描进入完整模型，由检测损失训练实际观测路径、正常预测网络、兼容性解码器和异常头；原始扫描仅进入共享的正常预测网络，提供联合负对数似然辅助损失，不再执行一次 LitePT 骨干。两份扫描分别根据自身坐标构造射线与角度块；原始扫描不参与合成扫描的异常分数计算。推理保持单帧输入，不需要干净参照。

| 设置 | 本轮采用值 |
|---|---|
| 训练预算 | 449 帧完整打乱遍历 10 次，共 4,490 次合成扫描访问，以及对应的 4,490 次正常辅助扫描访问 |
| 更新与排序 | 连续按八扫描组成共同损失，共 562 次参数更新；末批为两扫描，不逐轮丢弃尾部 |
| 初始化 | 原 nuScenes 预训练；与 NDP 的 SemanticKITTI／Panoptic-CUDAL 预训练不同 |
| 随机种子 | 模型及训练顺序为 0；固定合成数据使用官方的 2025 |
| 优化器 | 当前 AdamW；骨干峰值学习率 0.0002，新增参数 0.002；权重衰减 0.005，偏置与归一化参数不衰减 |
| 学习率 | 前 5% 更新预热，从峰值的 10% 开始，随后余弦衰减至峰值的 1% |
| 检测与正常损失 | 保留共同二元交叉熵、AP／AUROC／FPR95 排序代理及权重 0.1 的正常辅助似然；排序权重在预算的 10%—20% 之间递增 |
| 数值与内存 | FP32，保持完整窗口分块及反向重算；每次处理两扫描、逐扫描释放激活，八扫描仍共同排序 |
| 验证 | 训练预算终点一次完整 `val19` 验证，同一检查点报告三指标；不报告隐藏测试结果 |

验证沿用 `assets/val.json`：19 个公开序列共 8,659 帧，其中 1,960 帧满足官方异常点数与距离规则。参考 [NDP 论文表 1](https://arxiv.org/html/2604.09232v2)，NDP-EE 验证集 AP 为 74.24%、FPR95 为 1.43%、AUROC 为 99.53%；这些是对方公布的结果，本地没有重新训练 NDP。`val19` 已用于开发，本轮改善只能支持相应开发集上的比较，未知异常泛化仍需后续证据。

新增核验直接针对配对监督的含义：干净扫描辅助似然与共享假设联合预测公式及梯度一致，缓存求导与直接共同损失一致，稀少异常帧能够读取且辅助目标确实来自原始扫描。与原有相关回归共同执行，70 项通过，耗时 64.12 秒；这些结果确认实现范围内的正确性，不证明最终检测性能。

随后按本轮真实训练顺序取索引 `[373, 306, 186, 18, 247, 76, 240, 349]`，共 1,007,814 个回波，其中有效正常点 986,415 个、有效异常点 306 个。一次完整八扫描共同反向传播耗时 88.60 秒，PyTorch 分配显存峰值为 7,228,628,480 字节（6.73 GiB）；八帧均提供干净扫描辅助监督，重算分数最大差异为零，软召回为 0.9500000477，梯度均有限。该核验不更新参数，记录位于 `results/preflight/ndp/preflight.json`；显存数值不包含后续创建的 AdamW 状态，也不是最终指标。

主训练命令为 `python -m src.train --epochs 10 --workers 2 --threads 2 --save-every 25 --output results/train/ndp`，配套设置 `OMP_NUM_THREADS=2`、`MKL_NUM_THREADS=2`、`OPENBLAS_NUM_THREADS=2`。输出位于 `results/train/ndp/0/field/`，运行中每 25 次更新保存恢复状态并检查 Windows E 盘；保持至少 10 GB 安全余量。完整训练后的三项指标尚未产生，不提前填写本模型结果。
