# Validated Python 3.13 environment; CUDA binaries must match the installed PyTorch.
# This host uses /home/jasongao/Study/AJAE/.venv/bin/python with torch 2.12.0+cu130.
# nuScenes official scene names use the installed nuscenes-devkit==1.2.0.
# Install it separately with --no-deps: its full SDK declares NumPy < 2,
# while this project only imports its fixed scene split lists.
torch==2.12.0
numpy==2.4.6
scipy==1.17.1
scikit-learn==1.8.0
timm==1.0.27
addict==2.4.0
flash_attn==2.8.3.post1
torch_scatter==2.1.2+pt212cu130
spconv-cu126==2.3.8
cumm-cu126==0.7.11
pytest==8.4.2
ijson==3.5.1
